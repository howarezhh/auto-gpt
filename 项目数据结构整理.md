# 项目数据结构整理

整理时间：2026-05-07

本文按当前代码中的 SQLAlchemy 模型整理，主要依据 `app/models/` 与 `app/schemas/`。当前真实持久化表共 15 张；`conversations`、`dashboard`、`metrics`、`health` 等是从日志、模型、提供商和账户数据聚合出的展示结构，不是独立数据库表。

## 类型与精度约定

- `INTEGER`：整数。
- `TEXT`：文本或 JSON 字符串。
- `BOOLEAN`：布尔值。
- `DATETIME`：时间。
- `FLOAT`：浮点数。
- `NUMERIC(24, 9)`：金额字段，对应 `DB_MONEY_PRECISION=24`、`DB_MONEY_SCALE=9`。
- `NUMERIC(24, 12)`：价格或倍率字段，对应 `DB_PRICE_SCALE=12` / `DB_MULTIPLIER_SCALE=12`。

## 表关系总览

- `providers` 是提供商主表。
- `provider_models` 是提供商挂载模型表，关联 `providers.id`。
- `model_catalogs` 是全局模型主表，按 `model_name` 与 `provider_models` 对齐。
- `api_client_keys` 是外部 Bearer API Key 主表，可绑定用户账户和默认提供商。
- `api_client_key_provider_bindings` 是 API Key 与提供商授权关系表。
- `user_accounts` 是管理端/用户端登录账户表。
- `request_logs` 是请求、认证、路由、计费、错误和 trace 的统一日志主表。
- `api_client_billing_records` 是 API Key 维度账单明细。
- `user_account_billing_records` 是用户账户共享余额维度账单明细。
- `app_settings` 是单行系统设置表。
- `api_key_policy_templates` 是 API Key 策略模板表。
- `admin_audit_logs` 是管理端操作审计表。
- `alert_events`、`alert_subscriptions` 是告警事件与订阅表。
- `uploaded_assets` 是浏览器侧上传素材表。

## 1. `providers` 提供商表

用途：保存上游提供商地址、密钥、容量、健康、熔断、维护窗口和基础路由权重。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 提供商 ID |
| `name` | TEXT | NOT NULL, UNIQUE | 提供商名称 |
| `base_url` | TEXT | NOT NULL | 上游基础地址 |
| `api_key` | TEXT | NOT NULL | 上游密钥 |
| `provider_type` | TEXT | NOT NULL, default `openai_compatible` | 提供商类型 |
| `group_name` | TEXT | NULL | 分组名 |
| `region_tag` | TEXT | NULL | 地域标签 |
| `enabled` | BOOLEAN | NOT NULL, default `true` | 是否启用 |
| `priority` | INTEGER | NOT NULL, default `100` | 路由优先级 |
| `weight` | INTEGER | NOT NULL, default `100` | 权重路由权重 |
| `timeout_ms` | INTEGER | NOT NULL, default `30000` | 请求超时 ms |
| `max_retries` | INTEGER | NOT NULL, default `1` | 单提供商重试次数上限 |
| `max_active_requests` | INTEGER | NULL, default `1000` | 最大活跃请求数 |
| `max_active_streams` | INTEGER | NULL, default `1000` | 最大活跃流式请求数 |
| `max_qps` | INTEGER | NULL | 最大 QPS |
| `max_error_rate` | FLOAT | NULL, default `80.0` | 最大错误率阈值 |
| `first_token_timeout_sec` | INTEGER | NULL, default `60` | 首 Token 超时秒数 |
| `maintenance_window` | TEXT | NULL | 维护窗口 |
| `maintenance_mode_enabled` | BOOLEAN | NOT NULL, default `false` | 是否维护模式 |
| `auto_circuit_break_enabled` | BOOLEAN | NOT NULL, default `true` | 是否自动熔断 |
| `auto_recover_enabled` | BOOLEAN | NOT NULL, default `true` | 是否自动恢复 |
| `circuit_breaker_threshold_override` | INTEGER | NULL | 熔断阈值覆盖 |
| `recovery_probe_interval_sec_override` | INTEGER | NULL | 恢复探测间隔覆盖 |
| `models_json` | TEXT | NOT NULL, default `[]` | 旧版模型列表 JSON |
| `health_status` | TEXT | NOT NULL, default `unknown` | 健康状态 |
| `last_check_at` | DATETIME | NULL | 最近检查时间 |
| `last_latency_ms` | INTEGER | NULL | 最近延迟 ms |
| `failure_count` | INTEGER | NOT NULL, default `0` | 失败次数 |
| `success_count` | INTEGER | NOT NULL, default `0` | 成功次数 |
| `circuit_state` | TEXT | NOT NULL, default `closed` | 熔断状态 |
| `credential_rotated_at` | DATETIME | NULL | 密钥轮换时间 |
| `credential_hint` | TEXT | NULL | 密钥提示 |
| `remark` | TEXT | NULL | 备注 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |
| `updated_at` | DATETIME | NOT NULL | 更新时间 |

索引/约束：`id` 索引，`name` 唯一。

## 2. `provider_models` 提供商模型挂载表

用途：保存某个提供商支持的模型、模型级路由参数、能力、价格倍率、健康状态。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 挂载 ID |
| `provider_id` | INTEGER | FK `providers.id`, NOT NULL | 所属提供商 |
| `model_name` | TEXT | NOT NULL | 模型名 |
| `enabled` | BOOLEAN | NOT NULL, default `true` | 是否启用 |
| `priority` | INTEGER | NOT NULL, default `100` | 模型级优先级 |
| `weight` | INTEGER | NOT NULL, default `100` | 模型级权重 |
| `health_status` | TEXT | NOT NULL, default `unknown` | 模型健康状态 |
| `last_check_at` | DATETIME | NULL | 最近检查时间 |
| `last_latency_ms` | INTEGER | NULL | 最近延迟 ms |
| `failure_count` | INTEGER | NOT NULL, default `0` | 失败次数 |
| `success_count` | INTEGER | NOT NULL, default `0` | 成功次数 |
| `last_error` | TEXT | NULL | 最近错误 |
| `circuit_state` | TEXT | NOT NULL, default `closed` | 模型熔断状态 |
| `circuit_opened_at` | DATETIME | NULL | 熔断打开时间 |
| `supports_stream` | BOOLEAN | NOT NULL, default `true` | 支持流式 |
| `supports_vision` | BOOLEAN | NOT NULL, default `false` | 支持图片 |
| `supports_tools` | BOOLEAN | NOT NULL, default `false` | 支持工具调用 |
| `supports_chat_completions` | BOOLEAN | NOT NULL, default `true` | 原生支持 `/v1/chat/completions` |
| `supports_responses` | BOOLEAN | NOT NULL, default `true` | 原生支持 `/v1/responses` |
| `context_window_tokens` | INTEGER | NULL | 上下文窗口 |
| `max_input_tokens` | INTEGER | NULL | 最大输入 Token |
| `max_output_tokens` | INTEGER | NULL | 最大输出 Token |
| `price_multiplier` | NUMERIC(24, 12) | NOT NULL, default `1` | 渠道倍率 |
| `input_price_per_1k` | NUMERIC(24, 12) | NULL | 直接输入价，底层每 1k |
| `output_price_per_1k` | NUMERIC(24, 12) | NULL | 直接输出价，底层每 1k |
| `cache_price_per_1k` | NUMERIC(24, 12) | NULL | 缓存价，底层每 1k |
| `created_at` | DATETIME | NOT NULL | 创建时间 |
| `updated_at` | DATETIME | NOT NULL | 更新时间 |

索引：`id`、`provider_id`、`model_name`。

## 3. `model_catalogs` 全局模型表

用途：以模型为主视角维护基础能力、基础价格、速度标签和启用状态。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 模型 ID |
| `model_name` | TEXT | NOT NULL, UNIQUE | 模型名 |
| `display_name` | TEXT | NULL | 展示名 |
| `enabled` | BOOLEAN | NOT NULL, default `true` | 是否全局启用 |
| `supports_stream` | BOOLEAN | NOT NULL, default `true` | 支持流式 |
| `supports_vision` | BOOLEAN | NOT NULL, default `false` | 支持图片 |
| `supports_tools` | BOOLEAN | NOT NULL, default `false` | 支持工具调用 |
| `supports_chat_completions` | BOOLEAN | NOT NULL, default `true` | 支持 chat 入口 |
| `supports_responses` | BOOLEAN | NOT NULL, default `true` | 支持 responses 入口 |
| `context_window_tokens` | INTEGER | NULL | 上下文窗口 |
| `max_input_tokens` | INTEGER | NULL | 最大输入 Token |
| `max_output_tokens` | INTEGER | NULL | 最大输出 Token |
| `input_price_per_1k` | NUMERIC(24, 12) | NULL | 基础输入价，底层每 1k |
| `output_price_per_1k` | NUMERIC(24, 12) | NULL | 基础输出价，底层每 1k |
| `cache_price_per_1k` | NUMERIC(24, 12) | NULL | 基础缓存价，底层每 1k |
| `speed_label` | TEXT | NULL | 速度标签 |
| `remark` | TEXT | NULL | 备注 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |
| `updated_at` | DATETIME | NOT NULL | 更新时间 |

索引：`id`、`model_name`。

## 4. `api_client_keys` 外部 API Key 表

用途：保存对外 Bearer API Key、所属账户、额度、限速、路由策略、模型/端点/IP 白名单和使用统计。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | API Key ID |
| `name` | TEXT | NOT NULL | 名称 |
| `remark` | TEXT | NULL | 备注 |
| `tenant_name` | TEXT | NULL | 租户 |
| `project_name` | TEXT | NULL | 项目 |
| `app_name` | TEXT | NULL | 应用 |
| `environment_name` | TEXT | NULL | 环境 |
| `key_prefix` | TEXT | NOT NULL | 密钥前缀 |
| `key_hash` | TEXT | NOT NULL, UNIQUE | 密钥哈希 |
| `raw_key_encrypted` | TEXT | NULL | 加密后的明文密钥 |
| `enabled` | BOOLEAN | NOT NULL, default `true` | 是否启用 |
| `expires_at` | DATETIME | NULL | 过期时间 |
| `token_limit_total` | INTEGER | NULL | 总 Token 上限 |
| `request_limit_daily` | INTEGER | NULL | 日请求上限 |
| `token_limit_daily` | INTEGER | NULL | 日 Token 上限 |
| `cost_limit_daily` | NUMERIC(24, 9) | NULL | 日金额上限 |
| `qps_limit` | INTEGER | NULL | QPS 上限 |
| `rpm_limit` | INTEGER | NULL | RPM 上限 |
| `tpm_limit` | INTEGER | NULL | TPM 上限 |
| `prompt_tokens_used` | INTEGER | NOT NULL, default `0` | 已用输入 Token |
| `completion_tokens_used` | INTEGER | NOT NULL, default `0` | 已用输出 Token |
| `total_tokens_used` | INTEGER | NOT NULL, default `0` | 已用总 Token |
| `cost_limit_total` | NUMERIC(24, 9) | NULL | 总金额上限 |
| `total_cost_used` | NUMERIC(24, 9) | NOT NULL, default `0` | 已消费金额 |
| `balance_amount` | NUMERIC(24, 9) | NULL | 历史 API Key 余额字段 |
| `total_recharge_amount` | NUMERIC(24, 9) | NOT NULL, default `0` | 历史 API Key 累计充值 |
| `route_mode` | TEXT | NOT NULL, default `failover` | 路由模式 |
| `default_provider_id` | INTEGER | FK `providers.id`, NULL | 默认提供商 |
| `owner_user_id` | INTEGER | FK `user_accounts.id`, NULL | 归属用户 |
| `manual_allow_fallback` | BOOLEAN | NOT NULL, default `true` | 手动模式允许回退 |
| `allowed_model_names_json` | TEXT | NOT NULL, default `[]` | 允许模型列表 JSON |
| `allowed_endpoint_paths_json` | TEXT | NOT NULL, default `[]` | 允许端点列表 JSON |
| `allowed_source_ips_json` | TEXT | NOT NULL, default `[]` | 允许来源 IP JSON |
| `preferred_provider_ids_json` | TEXT | NOT NULL, default `[]` | 偏好提供商 JSON |
| `preferred_region_tags_json` | TEXT | NOT NULL, default `[]` | 偏好地域 JSON |
| `max_candidate_count` | INTEGER | NULL | 最大候选数 |
| `latency_bias` | INTEGER | NOT NULL, default `1` | 延迟偏好权重 |
| `success_rate_bias` | INTEGER | NOT NULL, default `1` | 成功率偏好权重 |
| `cost_bias` | INTEGER | NOT NULL, default `0` | 成本偏好权重 |
| `last_used_at` | DATETIME | NULL | 最近使用时间 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |
| `updated_at` | DATETIME | NOT NULL | 更新时间 |

索引/约束：`id`、`key_prefix`、`key_hash`、`owner_user_id`；`key_hash` 唯一。

## 5. `api_client_key_provider_bindings` API Key 与提供商授权表

用途：保存 API Key 可访问哪些提供商。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 绑定 ID |
| `api_client_key_id` | INTEGER | FK `api_client_keys.id`, NOT NULL | API Key |
| `provider_id` | INTEGER | FK `providers.id`, NOT NULL | 提供商 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |

索引/约束：`api_client_key_id`、`provider_id`；唯一约束 `api_client_key_id + provider_id`。

## 6. `user_accounts` 登录账户表

用途：保存后台管理员和普通用户登录账户、共享余额池、账户级额度。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 用户 ID |
| `username` | TEXT | NOT NULL, UNIQUE | 用户名 |
| `email` | TEXT | NOT NULL, UNIQUE | 邮箱 |
| `password_hash` | TEXT | NOT NULL | 密码哈希 |
| `role` | TEXT | NOT NULL, default `user` | 角色，`admin` 或 `user` |
| `enabled` | BOOLEAN | NOT NULL, default `true` | 是否启用 |
| `created_by_user_id` | INTEGER | FK `user_accounts.id`, NULL | 创建者 |
| `balance_amount` | NUMERIC(24, 9) | NOT NULL, default `0` | 账户总余额 |
| `frozen_amount` | NUMERIC(24, 9) | NOT NULL, default `0` | 冻结金额 |
| `total_recharge_amount` | NUMERIC(24, 9) | NOT NULL, default `0` | 累计充值 |
| `request_limit_total` | INTEGER | NULL | 总请求上限 |
| `request_limit_daily` | INTEGER | NULL | 日请求上限 |
| `request_limit_monthly` | INTEGER | NULL | 月请求上限 |
| `token_limit_total` | INTEGER | NULL | 总 Token 上限 |
| `token_limit_daily` | INTEGER | NULL | 日 Token 上限 |
| `token_limit_monthly` | INTEGER | NULL | 月 Token 上限 |
| `cost_limit_total` | NUMERIC(24, 9) | NULL | 总金额上限 |
| `cost_limit_daily` | NUMERIC(24, 9) | NULL | 日金额上限 |
| `cost_limit_monthly` | NUMERIC(24, 9) | NULL | 月金额上限 |
| `last_login_at` | DATETIME | NULL | 最近登录时间 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |
| `updated_at` | DATETIME | NOT NULL | 更新时间 |

索引/约束：`id`、`username`、`email`；`username` 和 `email` 唯一。

## 7. `request_logs` 请求日志主表

用途：统一记录外部 `/v1/*`、健康检查、后台测试、认证失败、模型调用、费用和 trace。会话列表也是从本表按 `conversation_key` 聚合。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 日志 ID |
| `log_type` | TEXT | NOT NULL | 日志类型 |
| `provider_id` | INTEGER | FK `providers.id`, NULL | 命中提供商 |
| `provider_name` | TEXT | NULL | 提供商名称快照 |
| `trace_id` | TEXT | NULL | trace ID |
| `model_name` | TEXT | NULL | 实际模型 |
| `requested_model` | TEXT | NULL | 请求模型 |
| `tenant_name` | TEXT | NULL | 租户 |
| `project_name` | TEXT | NULL | 项目 |
| `app_name` | TEXT | NULL | 应用 |
| `environment_name` | TEXT | NULL | 环境 |
| `request_id` | TEXT | NULL | 请求 ID |
| `conversation_key` | TEXT | NULL | 会话聚合键 |
| `session_id` | TEXT | NULL | 会话/客户端 session |
| `source_ip` | TEXT | NULL | 来源 IP |
| `resolved_provider_model_id` | INTEGER | NULL | 命中的挂载模型 ID |
| `request_path` | TEXT | NULL | 请求路径 |
| `http_method` | TEXT | NULL | HTTP 方法 |
| `is_stream` | BOOLEAN | NOT NULL, default `false` | 是否流式 |
| `has_image` | BOOLEAN | NOT NULL, default `false` | 是否含图片 |
| `success` | BOOLEAN | NOT NULL, default `false` | 是否成功 |
| `status_code` | INTEGER | NULL | HTTP 状态码 |
| `latency_ms` | INTEGER | NULL | 延迟 ms |
| `first_token_latency_ms` | INTEGER | NULL | 首 Token 延迟 ms |
| `ttfb_ms` | INTEGER | NULL | 首字节时间 ms |
| `duration_ms` | INTEGER | NULL | 总耗时 ms |
| `tps` | FLOAT | NULL | tokens/s |
| `reasoning_level` | TEXT | NULL | 思维等级 |
| `model_reasoning_effort` | TEXT | NULL | 模型 reasoning effort |
| `attempt_count` | INTEGER | NULL | 尝试次数 |
| `prompt_cost` | NUMERIC(24, 9) | NULL | 输入成本 |
| `completion_cost` | NUMERIC(24, 9) | NULL | 输出成本 |
| `total_cost` | NUMERIC(24, 9) | NULL | 总成本 |
| `billing_status` | TEXT | NULL | 计费状态 |
| `billing_finalized_at` | DATETIME | NULL | 计费完成时间 |
| `billing_event_id` | TEXT | NULL | 幂等计费事件 ID |
| `billing_attempt_count` | INTEGER | NOT NULL, default `0` | 计费尝试次数 |
| `billing_error` | TEXT | NULL | 计费错误 |
| `token_finalize_attempt_count` | INTEGER | NOT NULL, default `0` | Token 补全尝试次数 |
| `token_finalize_error` | TEXT | NULL | Token 补全错误 |
| `billing_multiplier` | NUMERIC(24, 12) | NULL | 计费倍率 |
| `channel_price_input_per_1k` | NUMERIC(24, 12) | NULL | 渠道输入价快照 |
| `channel_price_output_per_1k` | NUMERIC(24, 12) | NULL | 渠道输出价快照 |
| `channel_price_cache_per_1k` | NUMERIC(24, 12) | NULL | 渠道缓存价快照 |
| `api_client_balance_after` | NUMERIC(24, 9) | NULL | API Key 或账户余额快照 |
| `prompt_tokens` | INTEGER | NULL | 输入 Token |
| `completion_tokens` | INTEGER | NULL | 输出 Token |
| `total_tokens` | INTEGER | NULL | 总 Token |
| `cache_read_tokens` | INTEGER | NULL | 缓存读 Token |
| `cache_write_tokens` | INTEGER | NULL | 缓存写 Token |
| `finish_reason` | TEXT | NULL | 完成原因 |
| `upstream_request_id` | TEXT | NULL | 上游请求 ID |
| `request_body_json` | TEXT | NULL | 请求体摘要或受控 payload |
| `response_body_json` | TEXT | NULL | 响应体摘要或受控 payload |
| `response_text` | TEXT | NULL | 响应文本留痕 |
| `message` | TEXT | NULL | 摘要消息/错误消息 |
| `error_type` | TEXT | NULL | 错误类型 |
| `error_code` | TEXT | NULL | 错误码 |
| `retryable` | BOOLEAN | NULL | 是否可重试 |
| `api_client_key_id` | INTEGER | NULL | API Key ID 快照 |
| `api_client_key_name` | TEXT | NULL | API Key 名称快照 |
| `api_client_key_prefix` | TEXT | NULL | API Key 前缀快照 |
| `user_account_id` | INTEGER | NULL | 用户账户 ID 快照 |
| `user_account_name` | TEXT | NULL | 用户名快照 |
| `api_client_auth_result` | TEXT | NULL | 鉴权结果 |
| `api_client_remaining_tokens` | INTEGER | NULL | 剩余 Token |
| `api_client_remaining_requests_daily` | INTEGER | NULL | 日剩余请求 |
| `api_client_remaining_cost_daily` | NUMERIC(24, 9) | NULL | 日剩余金额 |
| `api_client_policy_snapshot_json` | TEXT | NULL | API Key 策略快照 JSON |
| `trace_json` | TEXT | NULL | 路由/重试 trace JSON |
| `created_at` | DATETIME | NOT NULL | 创建时间 |

索引：`id`、`created_at`、`trace_id`、`request_id`、`conversation_key`、`session_id`、`api_client_key_id`、`user_account_id`、`billing_event_id`、`api_client_key_id + created_at`、`user_account_id + created_at`、`log_type + created_at + provider_id + requested_model + success`。

## 8. `api_client_billing_records` API Key 账单明细表

用途：保存 API Key 维度的充值、扣费、调整等账单记录。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 账单 ID |
| `api_client_key_id` | INTEGER | FK `api_client_keys.id`, NOT NULL | API Key |
| `request_log_id` | INTEGER | FK `request_logs.id`, NULL, UNIQUE | 关联请求日志 |
| `record_type` | TEXT | NOT NULL | 记录类型 |
| `amount` | NUMERIC(24, 9) | NOT NULL, default `0` | 金额，扣费通常为负数 |
| `balance_after` | NUMERIC(24, 9) | NULL | 变动后余额 |
| `provider_id` | INTEGER | NULL | 提供商 ID 快照 |
| `provider_name` | TEXT | NULL | 提供商名称快照 |
| `model_name` | TEXT | NULL | 模型名 |
| `prompt_tokens` | INTEGER | NULL | 输入 Token |
| `completion_tokens` | INTEGER | NULL | 输出 Token |
| `total_tokens` | INTEGER | NULL | 总 Token |
| `unit_input_price_per_1k` | NUMERIC(24, 12) | NULL | 输入单价快照 |
| `unit_output_price_per_1k` | NUMERIC(24, 12) | NULL | 输出单价快照 |
| `remark` | TEXT | NULL | 备注 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |

索引/约束：`api_client_key_id`、`request_log_id`、`record_type`；`request_log_id` 唯一。

## 9. `user_account_billing_records` 用户账户账单明细表

用途：保存用户共享余额池的充值、扣费、调整等账单记录，是当前普通用户余额展示的主要账单来源。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 账单 ID |
| `user_account_id` | INTEGER | FK `user_accounts.id`, NOT NULL | 用户账户 |
| `api_client_key_id` | INTEGER | FK `api_client_keys.id`, NULL | 关联 API Key |
| `request_log_id` | INTEGER | FK `request_logs.id`, NULL, UNIQUE | 关联请求日志 |
| `record_type` | TEXT | NOT NULL | 记录类型 |
| `amount` | NUMERIC(24, 9) | NOT NULL, default `0` | 金额 |
| `balance_after` | NUMERIC(24, 9) | NULL | 变动后余额 |
| `provider_id` | INTEGER | NULL | 提供商 ID 快照 |
| `provider_name` | TEXT | NULL | 提供商名称快照 |
| `model_name` | TEXT | NULL | 模型名 |
| `prompt_tokens` | INTEGER | NULL | 输入 Token |
| `completion_tokens` | INTEGER | NULL | 输出 Token |
| `total_tokens` | INTEGER | NULL | 总 Token |
| `unit_input_price_per_1k` | NUMERIC(24, 12) | NULL | 输入单价快照 |
| `unit_output_price_per_1k` | NUMERIC(24, 12) | NULL | 输出单价快照 |
| `remark` | TEXT | NULL | 备注 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |

索引/约束：`user_account_id`、`api_client_key_id`、`request_log_id`、`record_type`；`request_log_id` 唯一。

## 10. `app_settings` 系统设置表

用途：保存单行全局配置，固定 `id=1`。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK, default `1` | 固定设置 ID |
| `route_mode` | TEXT | NOT NULL, default `failover` | 全局路由模式 |
| `default_provider_id` | INTEGER | FK `providers.id`, NULL | 默认提供商 |
| `manual_allow_fallback` | BOOLEAN | NOT NULL, default `true` | 手动模式允许回退 |
| `global_timeout_ms` | INTEGER | NOT NULL, default `30000` | 全局超时 ms |
| `global_max_retries` | INTEGER | NOT NULL, default `2` | 全局最大重试次数 |
| `global_max_request_tokens` | INTEGER | NOT NULL, default `0` | 全局请求 Token 上限，0 表示不限 |
| `max_v1_request_body_bytes` | INTEGER | NOT NULL, default `20971520` | `/v1/*` 请求体字节上限 |
| `max_v1_chat_request_body_bytes` | INTEGER | NOT NULL, default `0` | chat 独立请求体上限，0 表示不单独限制 |
| `max_v1_responses_request_body_bytes` | INTEGER | NOT NULL, default `0` | responses 独立请求体上限 |
| `long_output_stream_threshold_tokens` | INTEGER | NOT NULL, default `8192` | 非流式长输出阈值 |
| `max_non_stream_response_body_bytes` | INTEGER | NOT NULL, default `20971520` | 非流式响应体上限 |
| `stream_token_capture_max_bytes` | INTEGER | NOT NULL, default `1048576` | 流式文本缓存上限 |
| `max_logged_metadata_bytes` | INTEGER | NOT NULL, default `1024` | 日志 metadata 上限 |
| `circuit_breaker_threshold` | INTEGER | NOT NULL, default `3` | 熔断阈值 |
| `auto_health_check` | BOOLEAN | NOT NULL, default `true` | 自动健康检查 |
| `health_check_interval_sec` | INTEGER | NOT NULL, default `300` | 健康检查间隔 |
| `recovery_probe_interval_sec` | INTEGER | NOT NULL, default `30` | 恢复探测间隔 |
| `enable_token_logging` | BOOLEAN | NOT NULL, default `true` | 记录 Token |
| `enable_payload_logging` | BOOLEAN | NOT NULL, default `false` | 记录 payload |
| `enable_stream_response_persist` | BOOLEAN | NOT NULL, default `false` | 持久化流式响应 |
| `mask_sensitive_fields` | BOOLEAN | NOT NULL, default `true` | 脱敏敏感字段 |
| `max_logged_body_bytes` | INTEGER | NOT NULL, default `16384` | 请求/响应体日志上限 |
| `allow_public_user_registration` | BOOLEAN | NOT NULL, default `false` | 允许公开注册 |
| `request_log_retention_days` | INTEGER | NOT NULL, default `90` | 请求日志保留天数 |
| `admin_audit_log_retention_days` | INTEGER | NOT NULL, default `180` | 审计日志保留天数 |
| `route_candidate_cache_ttl_sec` | INTEGER | NOT NULL, default `10` | 路由候选缓存 TTL |
| `model_list_cache_ttl_sec` | INTEGER | NOT NULL, default `15` | 模型列表缓存 TTL |
| `provider_status_cache_ttl_sec` | INTEGER | NOT NULL, default `10` | 提供商状态缓存 TTL |
| `async_request_logging` | BOOLEAN | NOT NULL, default `true` | 异步写请求日志 |
| `global_max_active_requests` | INTEGER | NOT NULL, default `1000` | 全局活跃请求上限 |
| `global_max_active_streams` | INTEGER | NOT NULL, default `1000` | 全局活跃流式上限 |
| `api_key_max_active_requests` | INTEGER | NOT NULL, default `1000` | API Key 活跃请求上限 |
| `api_key_max_active_streams` | INTEGER | NOT NULL, default `1000` | API Key 活跃流式上限 |
| `account_max_active_requests` | INTEGER | NOT NULL, default `1000` | 账户活跃请求上限 |
| `account_max_active_streams` | INTEGER | NOT NULL, default `1000` | 账户活跃流式上限 |
| `provider_max_active_requests` | INTEGER | NOT NULL, default `1000` | 提供商活跃请求上限 |
| `provider_max_active_streams` | INTEGER | NOT NULL, default `1000` | 提供商活跃流式上限 |
| `concurrency_lease_ttl_seconds` | INTEGER | NOT NULL, default `900` | 并发租约 TTL |
| `stream_connect_timeout_seconds` | INTEGER | NOT NULL, default `10` | 流式连接超时 |
| `stream_first_token_timeout_seconds` | INTEGER | NOT NULL, default `60` | 流式首 Token 超时 |
| `stream_idle_timeout_seconds` | INTEGER | NOT NULL, default `120` | 流式空闲超时 |
| `stream_max_duration_seconds` | INTEGER | NOT NULL, default `600` | 流式最大持续时间 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |
| `updated_at` | DATETIME | NOT NULL | 更新时间 |

## 11. `api_key_policy_templates` API Key 策略模板表

用途：保存可复用的 API Key 策略，供批量创建或批量应用。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 模板 ID |
| `name` | TEXT | NOT NULL, UNIQUE | 模板名称 |
| `remark` | TEXT | NULL | 备注 |
| `enabled` | BOOLEAN | NOT NULL, default `true` | 是否启用 |
| `route_mode` | TEXT | NOT NULL, default `failover` | 路由模式 |
| `default_provider_id` | INTEGER | NULL | 默认提供商 ID |
| `manual_allow_fallback` | BOOLEAN | NOT NULL, default `true` | 手动模式允许回退 |
| `token_limit_total` | INTEGER | NULL | 总 Token 上限 |
| `cost_limit_total` | NUMERIC(24, 9) | NULL | 总金额上限 |
| `expires_in_days` | INTEGER | NULL | 有效天数 |
| `allowed_provider_ids_json` | TEXT | NOT NULL, default `[]` | 允许提供商 JSON |
| `allowed_model_names_json` | TEXT | NOT NULL, default `[]` | 允许模型 JSON |
| `created_at` | DATETIME | NOT NULL | 创建时间 |
| `updated_at` | DATETIME | NOT NULL | 更新时间 |

索引/约束：`id`；`name` 唯一。

## 12. `admin_audit_logs` 管理操作审计表

用途：记录管理员对用户、API Key、提供商、模型、设置、账单、告警等关键对象的写操作。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 审计 ID |
| `actor_user_id` | INTEGER | NULL | 操作者 ID |
| `actor_username` | TEXT | NULL | 操作者用户名 |
| `action` | TEXT | NOT NULL | 操作类型 |
| `entity_type` | TEXT | NOT NULL | 对象类型 |
| `entity_id` | TEXT | NULL | 对象 ID |
| `entity_name` | TEXT | NULL | 对象名称 |
| `target_user_id` | INTEGER | NULL | 目标用户 ID |
| `summary` | TEXT | NOT NULL | 操作摘要 |
| `detail_json` | TEXT | NULL | 操作详情 JSON |
| `created_at` | DATETIME | NOT NULL | 创建时间 |

索引：`id`、`actor_user_id`、`actor_username`、`action`、`entity_type`、`entity_id`、`target_user_id`、`created_at`。

## 13. `alert_events` 告警事件表

用途：记录渠道异常、API Key 异常、账户预警、失败率异常和系统指标异常。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 告警 ID |
| `alert_key` | TEXT | NOT NULL, UNIQUE | 告警唯一键 |
| `alert_type` | TEXT | NOT NULL | 告警类型 |
| `severity` | TEXT | NOT NULL, default `warning` | 严重级别 |
| `title` | TEXT | NOT NULL | 标题 |
| `message` | TEXT | NOT NULL | 消息 |
| `payload_json` | TEXT | NULL | 详情 JSON |
| `status` | TEXT | NOT NULL, default `active` | 状态 |
| `first_seen_at` | DATETIME | NOT NULL | 首次发现时间 |
| `last_seen_at` | DATETIME | NOT NULL | 最近发现时间 |
| `last_notified_at` | DATETIME | NULL | 最近通知时间 |
| `acknowledged_at` | DATETIME | NULL | 确认时间 |
| `resolved_at` | DATETIME | NULL | 解决时间 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |
| `updated_at` | DATETIME | NOT NULL | 更新时间 |

索引：`id`、`alert_key`、`alert_type`；`alert_key` 唯一。

## 14. `alert_subscriptions` 告警订阅表

用途：保存每个用户的告警订阅偏好。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 订阅 ID |
| `user_account_id` | INTEGER | FK `user_accounts.id`, NOT NULL, UNIQUE | 用户账户 |
| `enabled` | BOOLEAN | NOT NULL, default `true` | 是否启用订阅 |
| `delivery_channel` | TEXT | NOT NULL, default `in_app` | 投递渠道 |
| `notify_provider_alerts` | BOOLEAN | NOT NULL, default `true` | 渠道异常通知 |
| `notify_api_key_alerts` | BOOLEAN | NOT NULL, default `true` | API Key 异常通知 |
| `notify_account_alerts` | BOOLEAN | NOT NULL, default `true` | 账户预警通知 |
| `notify_failure_rate_alerts` | BOOLEAN | NOT NULL, default `true` | 失败率异常通知 |
| `browser_notifications_enabled` | BOOLEAN | NOT NULL, default `false` | 浏览器通知 |
| `poll_interval_seconds` | INTEGER | NOT NULL, default `30` | 前端轮询间隔 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |
| `updated_at` | DATETIME | NOT NULL | 更新时间 |

索引/约束：`id`、`user_account_id`；`user_account_id` 唯一。

## 15. `uploaded_assets` 上传素材表

用途：保存 playground 或用户端上传的图片等素材文件元数据。

| 字段 | 类型 | 约束/默认 | 说明 |
| --- | --- | --- | --- |
| `id` | INTEGER | PK | 素材 ID |
| `filename` | TEXT | NOT NULL | 原始文件名 |
| `content_type` | TEXT | NULL | MIME 类型 |
| `storage_path` | TEXT | NOT NULL, UNIQUE | 本地存储路径 |
| `public_path` | TEXT | NOT NULL, UNIQUE | 前端访问路径 |
| `file_size_bytes` | INTEGER | NOT NULL, default `0` | 文件大小 |
| `media_kind` | TEXT | NOT NULL, default `image` | 媒体类型 |
| `sha256_hex` | TEXT | NOT NULL, UNIQUE | 文件 SHA256 |
| `enabled` | BOOLEAN | NOT NULL, default `true` | 是否可用 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |

索引/约束：`id`；`storage_path`、`public_path`、`sha256_hex` 唯一。

## 派生展示结构

这些结构会在前端或 API 中展示，但不是独立表。

| 展示结构 | 来源 | 主要字段 |
| --- | --- | --- |
| 概览仪表盘 | `providers`、`provider_models`、`model_catalogs`、`request_logs`、`api_client_keys` | 提供商数、健康数、模型数、24h 请求量、Token、成本、失败率、会话数、API Key 数 |
| 提供商列表 `ProviderOut` | `providers` + `provider_models` + 近期 `request_logs` 聚合 | 提供商配置、脱敏密钥、容量、活跃请求、模型配置、健康、成功率、稳定性、最低价格 |
| 模型配置页 `ModelCatalogOut` | `model_catalogs` + `provider_models` + `providers` | 模型能力、基础价格、绑定渠道数、可用渠道、最低价、平均倍率 |
| API Key 列表 `ApiKeyOut` | `api_client_keys` + `api_client_key_provider_bindings` + `providers` + 账单/日志聚合 | 状态、脱敏/可回显密钥、额度、剩余额度、路由策略、授权渠道、使用统计 |
| 日志中心 `RequestLogOut` | `request_logs` | 请求、鉴权、路由、Token、成本、错误、trace、payload 摘要 |
| 会话列表/回放 | `request_logs` 按 `conversation_key` 聚合 | 会话 key、请求数、成功/失败数、总 Token、开始/更新时间、轮次内容 |
| 指标看板 `Metric*` | `request_logs` 时间窗口聚合 | 请求数、失败率、平均延迟、p95/p99、QPS、Token、成本、图片/流式请求数 |
| 用户首页 | `user_accounts`、`api_client_keys`、`request_logs`、`user_account_billing_records` | 余额、冻结金额、可用余额、API Key、用量趋势、额度预警 |
| 用户账单页 | `user_account_billing_records` + `api_client_keys` | 账户余额摘要、消费/充值记录、关联密钥、模型、Token、单价 |
| 告警中心 | `alert_events`、`alert_subscriptions` + 运行时聚合 | 活跃告警、渠道/API Key/账户/失败率/系统指标异常、订阅偏好 |
| 审计日志页 | `admin_audit_logs` | 操作者、动作、对象类型、对象名、摘要、详情、时间 |
| 使用文档页 | 配置和请求上下文派生 | 外部接入地址、`/v1` 地址、鉴权方式、示例配置 |

## 需要特别注意的结构口径

- 会话没有独立表，依赖 `request_logs.conversation_key` 聚合；如果某请求没有会话键，就不会进入标准会话聚合。
- 健康检查、后台测试、真实外部请求都会进入 `request_logs`，是否展示取决于筛选条件和 `exclude_health_checks`。
- 成本字段在底层仍以 `*_per_1k` 存储，但前端必须按规范换算成 `$/1M` 展示。
- `api_client_keys.balance_amount` 和 `total_recharge_amount` 属于历史 API Key 余额字段；当前账户级余额口径以 `user_accounts.balance_amount/frozen_amount/total_recharge_amount` 为主。
- `request_body_json`、`response_body_json`、`response_text` 是受日志开关、脱敏和字节上限控制的大字段，不应默认理解为完整原始请求或完整原始响应。
- `trace_json` 记录的是本项目聚合后的路由/重试/回退轨迹，不一定一条本地日志对应一条上游日志。
