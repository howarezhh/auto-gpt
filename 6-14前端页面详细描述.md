# 6-14 前端页面详细描述

## 1. 管理端概览页（/）

### 页面定位

管理端概览页是管理员登录后的运行总览首页，用于集中查看中转站当前提供商可用性情况、模型与 API Key 规模、近期请求量、累计 Token 与成本，以及实时请求趋势。页面面向日常运维巡检，核心目标是让管理员快速判断系统是否可用、哪些提供商或模型正在产生主要流量和成本、最近是否出现失败率或延迟异常。

### 页面整体布局

页面使用管理后台统一外壳，左侧是全局导航，当前导航项为“概览”。主内容从上到下分为四个主要区域：顶部运行总览区、指标卡片区、累计用量统计区、实时监控区。页面底部还包含一个“测试结果”弹窗，用于承载批量可用性检测后的回放内容。

顶部区域采用左右结构：左侧显示页面标题、定位说明和主要操作；右侧显示系统信号摘要。中部指标卡片以网格方式排列，展示多项关键运营数字。下方累计用量区域展示总量与排行。实时监控区域展示趋势图、模型热度和提供商可用性条目。

### 顶部运行总览区

顶部左侧显示“运行总览”标识、页面标题“概览”，以及说明文字“统一查看提供商状态、可用巡检与最近代理使用概况。”这一段用于说明本页关注的是系统运行状态，而不是单个对象的配置。

顶部提供两个主要操作：

- “管理提供商”：跳转到提供商管理页面，用于进入提供商新增、编辑、查看和检测等管理流程。
- “检查全部提供商”：触发全部提供商的批量可用性检测。点击后会先打开测试功能选择界面，由管理员选择本次检测的能力范围；确认后开始批量检测，并在完成后刷新概览数据。检测失败时按钮会给出失败反馈，并弹出错误提示。

顶部右侧“系统信号”区域展示过去 24 小时代理请求量。该区域同时展示可用比例进度条、当前路由策略“可用性优先”，以及可用提供商数量与总提供商数量，例如“3/5 全部可用”。管理员可以通过这一块快速判断最近流量是否存在，以及可用提供商占比是否正常。

### 指标卡片区

指标卡片区展示管理端最常看的全局数字。每个卡片由一个指标名称和一个大号数值组成，适合快速扫读。页面包含以下指标：

- 总提供商数：当前系统中配置的提供商总量。
- 全部可用提供商：可用状态为可用的提供商数量。
- 全部不可用提供商：处于不可用状态的提供商数量。
- 模型总数：模型目录中的模型总量。
- 24h 请求量：过去 24 小时内的代理请求数量。
- 24h Token 用量：过去 24 小时内消耗的 Token 数量，按统一 token 缩写规则展示。
- 累计请求量：系统历史累计代理请求数量。
- 累计 Token：系统历史累计 Token 用量。
- 累计成本：系统历史累计价格成本，展示时带金额格式。
- 会话数：已记录的会话数量。
- API 密钥总数：系统内 API Key 总数量。
- 24h 失败率：过去 24 小时请求失败占比，属于风险指标。
- 累计失败数：历史累计失败请求数，属于风险指标。

页面加载后会通过最新数据刷新这些卡片，因此首次模板渲染的数据和后续异步刷新后的数据会保持同一页面内更新。

### 累计用量统计区

累计用量统计区标题为“全量 Token 与成本统计”，顶部右侧显示缓存说明，例如“精确聚合，30 秒短缓存”。这一块用于查看长期累计用量，不限于最近 24 小时。

该区域首先展示四个汇总值：

- 总请求数：累计进入正式路由统计口径的请求数量。
- 总 Token：累计总 Token。
- 输入 / 输出 Token：分别展示累计输入 Token 与输出 Token。
- 总价格成本：累计成本金额。

汇总下方有三个排行面板：

- “模型 Token / 成本排行”：按模型维度展示主要成本来源。每条排行包含排名序号、模型名、请求次数、Token 用量、成本金额、进度条和展开式明细信息。
- “提供商成本排行”：按提供商维度展示成本排行。每条排行包含提供商名称、请求次数、Token 用量、成本金额、进度条和提供商明细。
- “提供商模型组合成本”：按“提供商 × 模型”组合维度展示成本排行。每条排行同时显示提供商名和模型名，并展示请求、Token、成本等信息。该卡片还额外显示组合数。

每个排行面板顶部都有本组统计的请求数、Token 和成本汇总，并显示当前排行项数量。若没有对应数据，页面会显示空状态，例如“暂无模型成本数据”“暂无提供商成本数据”或“暂无提供商模型成本数据”。

### 实时监控区

实时监控区标题为“请求与模型运行态”，用于观察近期流量、延迟、模型热度和提供商可用性。右上角显示刷新状态，默认提示“每 30 秒刷新”，并提供“手动刷新”按钮。页面会定时自动刷新监控数据；当浏览器标签页处于隐藏状态时，自动刷新会暂停以减少无效请求。手动刷新成功后会显示“监控数据已刷新”的反馈。

实时监控区包含四个监控卡片：

- 请求趋势：展示所选时间范围内的请求趋势图，并在标题区域显示请求总数。时间范围下拉框支持“最近 3 小时”“最近 1 天”“最近 1 周”“最近 1 个月”。
- 延迟与首包：展示延迟趋势图，并在标题区域显示 P95 延迟和 P95 首包耗时。时间范围同样支持最近 3 小时、1 天、1 周和 1 个月。
- 模型热度：展示最近 60 分钟内有流量的模型数量，并以排行列表展示热门模型。
- 提供商可用性：展示请求失败率摘要，并以提供商条目展示各提供商的请求次数、失败率和平均延迟。条目会根据失败率呈现不同风险状态，便于管理员快速定位异常提供商。

如果监控接口加载失败，页面会在相关指标位置显示“加载失败”，图表或列表为空时会显示错误空状态，并将刷新状态更新为失败时间。

### 测试结果弹窗

页面预置“测试结果”弹窗，用于展示全部提供商可用性检测后的结果回放。弹窗包含“测试回放”标识、标题“测试结果”、关闭按钮和结果内容区域。管理员可以通过关闭按钮退出弹窗。该弹窗用于承载检测过程和结果详情，避免将大量检测回放直接挤入概览主页面。

### 用户可完成的操作

在本页中，管理员可以完成以下操作：

- 查看系统整体运行状态和关键统计数字。
- 跳转到提供商管理页面继续配置或排查。
- 发起全部提供商可用性检测，并选择检测功能范围。
- 查看累计请求、Token、输入输出 Token 和成本。
- 按模型、提供商、提供商模型组合三个维度分析成本来源。
- 切换请求趋势和延迟趋势的时间范围。
- 手动刷新实时监控数据。
- 查看模型热度排行和提供商可用性摘要。
- 在检测完成后查看测试结果回放，并关闭弹窗。

### 动态反馈与空状态

页面主要动态反馈包括按钮加载与失败反馈、Toast 提示、监控刷新提示、排行空状态和监控加载失败状态。全部提供商检测失败时，“检查全部提供商”按钮会短暂显示失败状态并弹出错误提示。实时监控手动刷新成功会提示刷新完成；失败时会保留页面结构，并在对应区域显示加载失败或错误信息。累计排行无数据时不会展示空白列表，而是使用明确的中文空状态说明当前暂无统计数据。

## 2. 提供商管理页（/providers）

### 页面定位

提供商管理页是管理后台中最核心的对象治理页面，用来集中管理所有上游提供商对象及其挂载模型。这里不仅能看提供商列表，还能新建、编辑、批量导入、可用性检测、协议检测、可信检测、轮换凭据、查看模型挂载详情，并对单个提供商的容量、维护窗口、内容治理和模型能力做精细配置。

### 页面整体布局

页面顶部是“提供商对象目录与治理总览”的标题区，右侧放置三个主要入口：新增提供商、批量导入、检查全部。其下先是摘要指标区，再往下是对象目录主表。列表之下有分页器。页面中后段主要由多个弹窗组成，分别承载对象编辑、批量导入、凭据轮换、测试结果和模型详情。

### 摘要指标区

页面最上方的摘要卡片展示提供商治理概况，用户可快速看到当前提供商的规模、可用性分布、风险分布和模型挂载情况。这里属于一组高密度统计卡，用于替代逐条翻表。

### 对象目录主表

对象目录是页面主体，按行展示每个提供商对象。表格支持服务端分页，并提供以下筛选条件：

- 关键词搜索：可按名称、URL、模型、地区、备注等内容搜索。
- 可用性筛选：全部可用性、全部可用、部分可用、全部不可用、未检测。
- 启用状态筛选：已启用、已停用。
- 可信状态筛选：可信、部分可信、不可信、未检测。
- 熔断状态筛选：闭合、已熔断、半开探测。
- 提供商类型筛选：按类型过滤。
- 分组筛选：按渠道分组过滤。
- 每页数量：20、50、100 条。

表格左侧有全选框，支持批量选中当前页对象。表头操作区提供三种批量检测按钮：

- 可用性检测选中
- 协议检测选中
- 可信检测选中

对象目录表格的主列通常包含对象、状态、模型、容量、质量、优先级和操作等信息。每一行都能直接看见提供商名称及其核心状态，并通过“更多”菜单执行低频操作。

### 行内对象信息

单行提供商会展示以下核心信息：

- 对象：提供商名称，以及通常伴随的 Base URL、类型或分组信息。
- 状态：启用状态、维护模式、可用可用性、可信状态、熔断状态等综合状态。
- 模型：已挂载模型的简要数量或展开后的模型摘要。
- 容量：活跃请求、流式请求、QPS、RPM、首 Token 超时等容量口径。
- 质量：可信、内容完整性、内容检测或其他质量摘要。
- 优先级：路由排序优先级，通常通过提示说明其排序意义。
- 操作：轮换凭据、启用/禁用、删除等。

若某个提供商行被展开，右侧还会显示更详细的对象面板，包括 Base URL、模型能力、维护策略、系统治理状态、检测策略、容量明细、质量明细和凭据信息。备注如果存在，也会在详情区显示。

### 右上角主操作

页面右上角三个按钮分别承担不同用途：

- 新增提供商：打开提供商编辑弹窗，用于创建新对象。
- 批量导入：打开批量导入弹窗，用于一次性导入多个提供商。
- 检查全部：对全部提供商触发可用性检测。

### 提供商编辑弹窗

新增与编辑使用同一个对象编辑弹窗。弹窗内容很完整，几乎覆盖一个提供商的全部治理字段。主要可填写或切换的内容包括：

- 名称
- Base URL
- API 密钥
- 提供商类型
- 渠道分组
- 地区标签
- 优先级
- 提供商请求超时 ms
- 提供商最大重试次数
- 最大活跃请求
- 最大流式请求
- 最大 QPS
- 每分钟最多请求
- 提供商首 Token 超时 s
- 内容检测策略
- 维护窗口
- 模型挂载
- 备注
- 是否启用该提供商
- 是否处于维护模式
- 是否启用自动摘除
- 是否启用自动恢复
- 熔断阈值覆盖
- 恢复探测间隔覆盖

其中“内容检测策略”是一个独立设置区，包含两个开关：

- 启用内容检测
- 流式先缓冲检测

“维护窗口”区域支持不设置、每日、每周、单次三种频率，还能指定星期、开始结束时间或单次起止时间，并固定使用北京时间口径。下面会显示维护窗口预览和反馈信息。

“模型挂载”区域是弹窗里最复杂的部分，负责把模型挂到当前提供商上。这里提供多种添加方式：

- 获取可用模型：从上游探测当前可用模型。
- 添加模型库中模型：从模型管理库中导入当前提供商尚未挂载的模型。
- 添加选中模型：把已勾选的上游模型导入当前提供商。
- 预设模型：通过常用模型名快速插入一批常见模型。
- 添加自定义模型：手工输入模型名称和模型ID。
- 新增空模型：先生成空行，再逐项补全。

模型挂载编辑区还展示一组字段头，说明每个挂载需要填写的关键属性，例如模型名称、模型ID、启用、端点协议、模型分组、倍率和操作。用户可以按行维护多个挂载模型。

弹窗底部提供“取消”和“保存”按钮。保存时如果是新增，会要求填写 API Key；如果是编辑，会要求新密钥不能空。保存成功后弹窗关闭并刷新列表。

### 批量导入弹窗

批量导入弹窗用于一次性导入多个提供商。页面提供一个大文本框让管理员粘贴导入列表，下面有四个动作按钮：

- 填入模板
- 复制模板
- 预览校验
- 确认导入

导入前必须先预览校验，结果区会显示校验或导入反馈。若列表中存在问题，确认导入会被拦截。这个弹窗适合从外部整理好的批量配置直接导入。

### 凭据轮换弹窗

凭据轮换弹窗用于替换某个提供商的上游 API Key。它包含“新 API 密钥”和“凭据备注”两个输入项，底部有“取消”和“提交轮换”按钮。提交后会更新当前提供商凭据，并刷新页面数据。

### 模型详情与测试结果弹窗

页面还提供两个结果型弹窗：

- 测试结果弹窗：用于展示可用性检测、协议检测或可信检测的结果回放。
- 提供商模型详情弹窗：用于查看某个提供商下的全部模型挂载明细。

模型详情弹窗中，每个模型会展示模型名、分组、可信度、上次通过与失败时间、失败次数、可用状态、启用状态、端点协议、能力、倍率、各类价格以及可执行操作。表格底部若没有模型，会显示“当前提供商尚未挂载模型”的空状态。

### 用户可完成的操作

在提供商管理页中，管理员可以完成以下操作：

- 新增单个提供商。
- 批量导入多个提供商。
- 按条件筛选、搜索和分页浏览提供商目录。
- 批量选择当前页提供商并执行可用性检测、协议检测和可信检测。
- 查看每个提供商的可用性、可信状态、熔断状态、容量和质量信息。
- 编辑提供商基础信息、容量、维护窗口、内容治理和自动恢复策略。
- 添加、导入、删除或整理某个提供商下的模型挂载。
- 从上游拉取可用模型并一键挂载。
- 使用常用预设模型或自定义模型快速补齐模型挂载。
- 轮换提供商凭据。
- 启用、停用或删除提供商。
- 查看某个提供商的模型详情和测试回放结果。

### 动态反馈与空状态

这个页面的交互反馈比较多，主要体现在按钮加载、表单保存反馈、筛选刷新、分页切换、空状态和各类弹窗结果上。搜索和筛选会重新加载目录；批量导入有独立的预览与确认流程；模型导入区如果当前没有可导入项，会显示明确的空状态说明。列表没有数据时，表格也不会留白，而是显示“当前没有可导入的上游模型”或“当前提供商尚未挂载模型”等提示，帮助管理员知道下一步该做什么。

## 3. 提供商模型挂载矩阵页（/provider-models）

### 页面定位

提供商模型挂载矩阵页是管理端按“提供商 × 模型挂载”维度治理路由能力的页面。它不是单纯的模型目录，也不是单个提供商详情，而是把所有提供商下的模型挂载拉平成一张矩阵，让管理员集中查看每个挂载的可用状态、可信度、端点协议、能力、倍率、价格、容量和质量，并可以批量测试或逐条编辑。

### 页面整体布局

页面顶部显示“对象挂载”和标题“提供商模型挂载矩阵”，右侧有“返回提供商”按钮，可回到提供商管理页。主体区域由筛选栏、当前页批量操作按钮、跨页选择工具栏、挂载矩阵表格、分页器、挂载编辑弹窗和测试结果弹窗组成。

### 筛选栏

筛选栏位于矩阵表格上方，支持按多种维度缩小挂载范围：

- 模型名称：搜索模型或提供商。
- 提供商：从提供商下拉框中选择指定提供商，默认是全部提供商。
- 启用状态：全部状态、已启用、已停用。
- 模型可用性：全部可用状态、可用、异常、未检测。
- 可信度：全部可信度、可信、异常、未检测。
- 模型分组：OpenAI、DeepSeek、通义千问、智谱 GLM、豆包、Kimi、百川、文心一言、腾讯混元、MiniMax、阶跃星辰、书生浦语、讯飞星火、零一万物、Mistral、Llama、Gemini、Claude、Grok、Cohere、Perplexity、未知分组等。
- 每页数量：10、20、50、100。

搜索输入有短暂延迟后刷新结果；下拉筛选变化会回到第一页并重新加载矩阵。

### 当前页操作区

筛选栏下方有两个针对当前页全部可见挂载的操作按钮：

- 测试本页：对当前页所有模型挂载执行可用性测试。点击后会先选择测试功能范围，再展示批量测试进度与结果。
- 可信检测本页：对当前页所有模型挂载执行可信检测，并展示批量检测结果。

当当前页没有可操作挂载时，这些按钮会不可用或提示当前页没有模型挂载。

### 跨页选择工具栏

表格支持跨页保留选择。左侧表头有“选择当前页模型挂载”的复选框；每一行也有独立复选框。选中任意挂载后，页面会显示批量工具栏，包含：

- 已选数量：例如“已选 3 个挂载”。
- 选择范围提示：例如当前页已选数量，或提示“当前选择可跨页保留”。
- 选择本页：把当前页所有挂载加入选择。
- 清空：清除全部已选挂载。
- 测试选中：对已选挂载执行可用性测试。
- 协议检测选中：对已选挂载执行端点协议检测。
- 可信检测选中：对已选挂载执行可信检测。

跨页选择的意义是管理员可以先在不同页勾选多个挂载，再统一执行检测，不必只局限于当前可见页。

### 挂载矩阵表格

矩阵表格是页面核心。每一行代表一个具体的提供商模型挂载。表格列包括：

- 选择：勾选当前挂载，用于批量操作。
- 提供商：展示提供商名称，以及分组或地区信息。
- 模型：展示模型名称，以及模型分组标签。
- 模型可用性：展示可用、异常或未检测状态；如果有异常原因，旁边会通过提示图标查看原因。
- 可信度：展示可信、异常或未检测状态；异常原因通过提示信息查看。
- 端点协议：展示该挂载使用的协议，例如 Responses、Chat、双协议、Gemini、Claude。
- 能力：展示模型能力标签，例如流式、工具、图片、生图等。
- 倍率 / 价格：展示价格倍率，并展示输入、输出、缓存、缓存写入等价格。
- 质量：展示延迟、失败、成功率或其它质量摘要。
- 操作：提供行内操作按钮。

若当前筛选条件下没有记录，表格会显示“当前筛选条件下没有模型挂载记录”的空状态。

### 行内操作

每行操作区包含以下功能：

- 启用状态标签：显示当前挂载是“已启用”还是“已停用”。
- 编辑：打开挂载编辑弹窗。
- 测试：对这一条模型挂载执行单模型可用性测试。
- 可信检测：对这一条模型挂载执行可信检测。
- 停用 / 启用：切换该模型挂载是否参与路由。

单行测试会打开测试结果弹窗，先显示执行中状态，完成后展示结果详情，并刷新矩阵中的状态。可信检测同样会展示结果弹窗，并根据检测结果反馈可信或异常。

### 挂载编辑弹窗

挂载编辑弹窗用于调整单个提供商模型挂载。弹窗标题会带上当前模型名称，副标题会显示“提供商 / 模型”组合。弹窗说明为“调整可信度、端点协议、倍率、价格和容量。”

弹窗包含以下字段：

- 可信度：可信、异常、未检测。
- 模型名称：平台对外别名。
- 模型ID：上游官方模型名。
- 端点协议：Responses、Chat、双协议、Gemini、Claude。
- 模型分组：与筛选栏一致的模型分组。
- 原生接口路径：用于 Gemini / Claude 等原生协议场景。
- 倍率。
- 账户输入价/1M。
- 账户输出价/1M。
- 账户缓存价/1M。
- 账户缓存写价/1M。
- 最大活跃请求。
- 最大流式请求。
- 最大 QPS。
- 最大 RPM。

价格输入采用每百万 token 的界面口径；容量项留空时表示继承提供商配置。弹窗底部有“取消”和“保存”按钮。保存时会校验模型名称、模型ID、倍率、价格和容量数值；保存成功后关闭弹窗并刷新列表。

### 批量测试与结果弹窗

页面复用“测试结果”弹窗展示单条或批量执行结果。批量可用性测试和批量可信检测都会显示：

- 执行范围，例如可用性测试或可信检测。
- 总数、完成数、通过数、异常数、限流数等摘要。
- 每个挂载的结果明细。
- 每个挂载的耗时和错误信息。

协议检测选中会展示“端点协议检测”结果，包含总数、完成、已更新、未通过、已限流、跳过等摘要。Gemini 和 Claude 原生协议挂载会被标记为跳过，并提示这类模型无需端点协议检测，应使用可用性检测和可信检测。

### 分页器

页面底部展示分页信息，例如“第 1 页，共 1 页 · 共 0 条”。右侧提供“上一页”和“下一页”按钮。翻页会重新加载当前筛选条件下的挂载列表；已选挂载可以跨页保留，除非用户手动清空选择。

### 用户可完成的操作

在提供商模型挂载矩阵页中，管理员可以完成以下操作：

- 按模型名称、提供商、启用状态、可用状态、可信度、模型分组筛选挂载。
- 调整每页显示数量并翻页浏览。
- 查看每个提供商模型挂载的可用、可信、协议、能力、价格、质量和启用状态。
- 对当前页全部挂载执行可用性测试或可信检测。
- 跨页选择多个挂载并批量测试。
- 对选中挂载执行端点协议检测。
- 对选中挂载执行可信检测。
- 编辑单个挂载的模型名称、上游模型ID、协议、分组、可信度、价格、倍率和容量限制。
- 单独测试某个挂载。
- 单独对某个挂载执行可信检测。
- 启用或停用某个挂载。
- 查看批量检测和单条检测的结果回放。

### 动态反馈与空状态

页面会在数据加载、测试执行、协议检测和保存编辑时给出按钮加载态、成功反馈、失败反馈和 Toast 提示。批量任务会在结果弹窗中实时更新进度，不需要等全部结束才看到状态。筛选无结果时显示明确空状态；选择工具栏只在存在已选挂载时出现；当前页无挂载时，测试本页和选择本页等操作会被禁用或给出错误提示。

## 4. 模型配置页（/models）

### 页面定位

模型配置页是管理端的模型中枢页面，用于统一维护模型基础价格、上下文窗口、速度标签、全局启用状态、能力标签，以及每个模型在不同提供商上的绑定、倍率和可路由情况。它同时还承载模型别名映射功能，允许把一个源模型在路由前改写成多个候选目标模型。

### 页面整体布局

页面顶部是概览 Hero 区，包含页面标题、说明文字和三个主要按钮：新增模型、一键测试、刷新。页面中部先展示四个统计卡片，再向下是“模型聚合列表”，底部是“模型映射”区域。页面还包含两个弹窗：模型编辑弹窗和模型映射弹窗。

### 顶部概览区

顶部标题为“模型配置”，说明文字强调这里维护的是模型基础价格、速度标签、全局启用状态，以及不同提供商对同一模型的可用性与价格倍率。右侧三个按钮分别用于：

- 新增模型：打开模型编辑弹窗，创建新的模型目录项。
- 一键测试：对所有模型执行统一批量测试。
- 刷新：重新拉取模型、提供商和映射数据。

### 统计卡片区

页面展示四个统计卡片：

- 模型总数：当前模型目录中的模型数。
- 已启用模型：全局启用的模型数。
- 已绑定提供商：存在绑定渠道的模型数量。
- 可用提供商：当前能参与路由的提供商数量。

这一组卡片用于快速看出模型目录是否完整、可路由关系是否充分、哪些模型还没真正接入可用提供商。

### 模型聚合列表

“模型聚合列表”是页面最主要的内容区。它展示的是按模型聚合后的卡片列表，而不是单纯的数据库行。每个模型卡片都包含多个层次的信息：

- 左侧选择框：用于批量操作。
- 模型名称区：展示模型展示名、模型 ID 和模型分组。
- 状态区：展示模型是否可用、是否启用。
- 能力区：用能力标签展示流式、图片、工具等能力。
- 基础价格区：展示输入、输出、缓存、缓存写等价格。
- 可用提供商区：展示当前有多少可用提供商、已绑定渠道和可信提供商。
- 操作区：测试、编辑、详情、更多操作。

卡片默认是收起状态，只显示核心摘要。点击“详情”后会展开更完整的模型详情面板，包括上下文窗口、价格形态、汇率快照、速度标签、模型分组和绑定渠道预览。

### 筛选区

模型列表上方提供多条件筛选：

- 关键词：可按模型 ID、模型名、速度标签、渠道名搜索。
- 可用：全部可用、可用、异常。
- 模型分组：按模型分组过滤。
- 启用：全部启用状态、已启用、已停用。
- 归属提供商：按提供商筛选。
- 每页数量：10、20、50、100。

筛选支持服务端分页，输入关键词后会自动延迟刷新结果。

### 批量工具栏

模型列表支持批量选择。勾选任意模型后，批量工具栏会出现，提供以下能力：

- 选择本页：勾选当前页全部模型。
- 已选数量：显示当前已选模型数。
- 批量上下文窗口：输入一个 token 数值后，可一键应用到选中的模型。
- 应用到选中模型：将上下文窗口批量写入所选模型。

批量上下文窗口功能适合统一修正一批模型的上下文上限，不需要逐个点开编辑。

### 模型编辑弹窗

点击“新增模型”或某个模型的“编辑”后，会打开模型编辑弹窗。这个弹窗内容最完整，承担模型目录的所有关键配置。它包含：

- 模型 ID
- 模型分组
- 模型名
- 最大上下文窗口 token
- 最大输入 token
- 最大输出 token
- 账户输入价/1M
- 账户输出价/1M
- 账户缓存价/1M
- 账户缓存写价/1M
- 原始币种
- 账户币种
- 原输入价/1M
- 原输出价/1M
- 原缓存价/1M
- 原缓存写价/1M
- 汇率
- 汇率来源
- 汇率时间
- 汇率版本
- 费用组件 JSON
- 价格形态摘要
- 速度标签
- 全局启用
- 模型能力：流式、图片、工具
- 备注

弹窗下半部分还有“渠道绑定与倍率”表格。这里可以逐个渠道决定该模型是否绑定、是否启用、倍率是多少、优先级是多少。右侧预览会同步显示当前基础价格乘以该渠道倍率后的预估价格。

模型编辑弹窗保存时会同时保存价格、币种、汇率快照、费用组件和绑定关系。编辑状态下，模型 ID 不能改；新增状态下，模型 ID 是必填项。

### 价格与币种区域

模型页在价格部分提供了比普通页面更完整的币种信息：

- 账户币种与原始币种都可以显式选择。
- 价格可以分别填写账户口径和原始币种口径。
- 汇率来源、时间和版本会一起展示和保存。
- 费用组件可用 JSON 形式保存额外费用条目。

页面上价格展示以 `$/1M` 口径为主，方便和账单口径保持一致。若原币种与账户币种不同，模型详情里也会显示币种和汇率快照信息，方便管理员核对价格来源。

### 模型详情面板

在模型卡片展开后，会看到更详细的模型面板，主要展示：

- 上下文窗口：上下文、输入和输出 token 上限。
- 价格形态：固定价、阶梯价或未定价。
- 汇率快照：当前价格换算使用的汇率和来源。
- 绑定平均倍率：已绑定渠道的平均倍率，以及可路由平均倍率。
- 速度标签：例如快、均衡、慢。
- 模型分组：模型归属的标准分组。
- 绑定渠道预览：当前模型绑定了哪些提供商，每个绑定的倍率、可路由状态和可信状态。

绑定渠道预览区还能直接发起某个绑定的可信检测，适合在模型层面定位某个渠道是否异常。

### 模型行内操作

每个模型卡片都支持以下操作：

- 测试：对当前模型执行可用性测试。
- 编辑：打开模型编辑弹窗。
- 详情：展开或收起详细信息。
- 更多：打开更多操作菜单。
- 删除：删除该模型，并同时移除相关渠道绑定。

模型卡片中的可用提供商区会用简短摘要告诉管理员当前模型可被哪些提供商承接请求，以及可信提供商是否足够。

### 模型映射区域

模型页下方还有一个独立的“模型映射”区域，标题是“模型别名与多目标择优”。它的作用是把一个源模型映射到多个候选目标模型，从而在路由前先做模型改写。

这个区域展示：

- 模型映射总数。
- 新建映射按钮。
- 每条映射卡片的源模型名、备注、启用状态。
- 当前映射下有多少目标模型、启用了多少个目标。
- 目标模型的预览标签。
- 编辑、测试、删除按钮。

如果还没有任何映射，会显示空状态，并提供“创建第一条”按钮。

### 模型映射弹窗

点击“新建映射”或“编辑”会打开模型映射弹窗。弹窗中可以维护：

- 源模型：一个要被重写的模型名。
- 备注：帮助识别这条映射。
- 启用映射：开关控制是否生效。
- 目标模型列表：至少保留一个目标。

目标模型列表支持逐条添加、移除、启用或停用。管理员可以为同一个源模型放多个候选目标，系统会按会话粘性和配置顺序择优。保存后映射会回到列表中。

### 一键测试与模型映射测试

页面顶部的“一键测试”会对全部模型执行统一批量测试。每条模型测试结果会在测试结果弹窗里按进度显示。模型映射卡片上的“测试”按钮则用于验证某个源模型当前会命中哪个目标模型，并在页面上直接告诉管理员当前选择结果。

### 用户可完成的操作

在模型配置页中，管理员可以完成以下操作：

- 新增模型。
- 修改模型基础价格、币种、汇率快照、费用组件和上下文窗口。
- 调整模型能力、速度标签和全局启用状态。
- 批量修改选中模型的上下文窗口 token。
- 按关键词、可用状态、分组、启用状态和提供商筛选模型。
- 查看模型的可用提供商、绑定渠道、平均倍率和价格形态。
- 对单个模型执行测试。
- 展开或收起模型详情。
- 删除模型。
- 为模型配置绑定渠道与倍率。
- 新建、编辑、测试和删除模型映射。
- 对一个源模型维护多个候选目标模型。
- 用“一键测试”批量验证全部模型。

### 动态反馈与空状态

模型页在保存、测试、批量应用和刷新时都会给出清晰反馈。空状态主要有两类：模型列表为空时显示“暂无模型配置”，模型映射为空时提示还没有模型映射并引导创建第一条。批量操作只有在选中模型后才可用；映射弹窗至少要有一个目标模型才能保存。这样管理员不会在空白页面里猜下一步要做什么。

## 5. 路由设置页（/settings）

### 页面定位

路由设置页是管理后台里控制全站路由与风控策略的地方。这里主要维护可用性优先主策略、候选耗尽后的等待重试、可信提供商开关、自动可用性检测、恢复探测、请求和日志字节限制、全局并发和限流阈值、Responses→Chat 兼容适配以及日志记录开关。它更像是“系统行为总开关”，不是单个对象的业务配置页。

### 页面整体布局

页面顶部是简洁的标题区，下面是一整张纵向表单卡。表单按功能分成多个分组：路由策略、可用性治理、模型 / 路由限制、请求 / 日志 / 缓存限制、Responses→Chat 兼容适配，以及若干日志与账号开关。底部只有一个保存按钮，强调这是一个统一提交的全局设置页。

### 路由策略区

路由策略区用于控制当候选提供商耗尽时系统如何表现。这里包含：

- 候选耗尽等待重试（s）：决定当外部请求没有可用候选时，系统最多等待多久再重新选择候选。说明里明确写出 0 表示立即返回错误，最大 600 s。
- 候选耗尽无限重试：开关式设置。开启后，在正常等待窗口之外还可以持续等待和重试；关闭时则按固定窗口结束。
- 只走可信提供商：开关式设置。开启后，正式路由只允许走官方或可信等级提供商，默认关闭。

每个选项旁边都带有问号帮助提示，说明它的业务意义和对路由行为的影响。

### 可用性治理区

“自动可用性检测”分组负责后台巡检与恢复探测相关配置：

- 启用自动可用性检测：控制是否开启后台定时巡检。
- 自动可用性检测间隔（s）：设置两轮自动巡检之间的最小间隔，页面说明当前下限为 300 s。
- 恢复探测间隔（s）：设置对象被标记为不可用后，多长时间执行一次恢复性探测。
- 挂载总探针限制（RPM）：设置同一提供商的同一挂载模型在 60 秒内最多允许发起多少次真实上游探针请求，默认 4，填 0 表示不限制总额度。
- 单类探针限制（RPM）：设置同一提供商的同一挂载模型、同一种探针类型在 60 秒内最多允许发起多少次真实上游探针请求，默认 4，填 0 表示不限制单类额度。

自动可用性检测、内容防护可信检测和模型端点协议检测都复用这组统一探针限频。后台批量探针按提供商并行执行，同一提供商内不同模型按顺序错峰启动；前一个模型若超过 10 s 未完成，后一个模型会在第 10 s 启动，避免慢模型长期阻塞整批检测。

这一块主要决定后台会不会主动去探上游、多久探一次、恢复时机有多积极。

### 模型 / 路由限制区

这一分组主要控制 token 与候选规模：

- 全局最大请求（tok）：限制单次请求可使用的最大 token。0 表示不限制。
- 最大候选数：每次路由基础保留多少个候选提供商参与后续尝试，默认 10。
- 候选扩展数：当基础候选全部失败后，再追加多少个额外候选继续尝试，默认 5。
- 长输出非流式阈值（tok）：控制长输出请求是否强制转流式。

这些设置直接影响路由候选池和长文本请求的处理方式。

### 请求 / 日志 / 缓存限制区

这一组以字节为单位，控制请求体、响应体、日志和流式缓存的大小上限：

- `/v1` 请求体最大字节数（B）
- Chat 请求体最大字节数（B）
- Responses 请求体最大字节数（B）
- 非流式响应最大字节数（B）
- 流式统计缓存字节数（B）
- 日志 metadata 最大字节数（B）
- 日志正文最大字节数（B）

页面会明确告诉管理员：0 表示不限制、0 表示沿用上一级限制、0 表示只保留摘要等。它的作用是让管理员控制正式请求、日志记录和流式统计不要因为大 payload 失控。

### 运行与并发限制区

页面中后段还展示了一排直接的数值限制项，包括：

- 熔断阈值
- 请求日志保留（d）
- 审计日志保留（d）
- 全局 QPS
- 全局 RPM
- 单账户 QPS
- 单账户 RPM
- 全局最大活跃请求
- 全局最大流式请求
- 单密钥最大活跃请求
- 单密钥最大流式请求
- 单账户最大活跃请求
- 单账户最大流式请求
- 单提供商最大活跃请求
- 单提供商最大流式请求
- 并发租约 TTL（s）

这一组决定了请求并发、流式并发、短窗口限流、租约保活和日志保留等全局规则。

### Responses→Chat 兼容适配区

这是页面里一个独立的大分组，用来控制 Responses 到 Chat 的单向兼容适配。包含：

- 启用单向适配：打开后，`/v1/responses` 可以按模型映射转发到 Chat-only 上游。
- 启用搜索代理：只允许 `web_search` 使用，`file_search` 与 `code_interpreter` 仍直接拒绝。
- 会话存储：数据库、Redis、内存三种方式。
- 会话 TTL（s）
- 最大工具轮次
- 上下文窗口（tok）
- 单快照上限（B）
- 数据库清理间隔（s）
- 单上游地址
- 单上游密钥
- 搜索代理地址
- 模型映射 JSON
- 多上游 JSON

这里的输入很多，说明它负责的是较重的协议兼容和会话存储规则，而不只是一个简单开关。

### 日志与注册开关区

页面底部还有几项总开关：

- 记录 token 使用量
- 记录请求与响应正文
- 记录流式最终回复文本
- 日志中脱敏敏感字段
- 允许公开注册普通用户

这些开关决定系统是否保存更完整的分析数据、是否记录 payload、是否允许用户公开注册。

### 保存按钮

页面底部只有一个“保存设置”按钮。所有分组修改完成后统一提交，适合这种全局性设置页的使用方式。

### 用户可完成的操作

在路由设置页中，管理员可以完成以下操作：

- 调整候选耗尽后的等待和无限重试策略。
- 开启或关闭只走可信提供商。
- 开启自动可用性检测并设置巡检间隔。
- 设置恢复探测间隔。
- 调整全局最大请求 token。
- 调整最大候选数与候选扩展数。
- 设置长输出非流式阈值。
- 限制请求体、响应体、日志正文和 metadata 的字节数。
- 调整请求日志、审计日志保留天数。
- 配置全局、账户、密钥和提供商的 QPS / RPM / 并发上限。
- 配置并发租约 TTL。
- 开启或关闭 Responses→Chat 兼容适配。
- 维护会话存储方式、TTL、工具轮次和快照上限。
- 配置单上游地址、搜索代理地址和多上游映射 JSON。
- 开启或关闭 token 记录、payload 记录、流式最终回复记录、敏感字段脱敏和公开注册。
- 一次性保存整页设置。

### 动态反馈与空状态

这页没有列表式空状态，所有反馈都来自表单校验、帮助提示和保存动作。数值输入如果超出限制会在提交时被拦下；开关和说明通过 Tooltip 辅助理解。页面没有复杂列表，但每个设置项都带有明确的业务语义和单位，适合管理员逐项校准系统行为。

## 6. 内容防护页（/content-guard）

### 页面定位

这是管理端的内容完整性防护工作台，用来配置大模型响应内容检测策略、执行可信探针、治理异常提供商、维护检测规则，并查看探针结果与运行时处置事件。它不是单纯的规则列表，而是把“配置、检测、治理、追溯”放在同一页的安全运营页面。

### 页面整体布局

页面顶部是标题区，显示“内容防护 / 内容完整性防护”，右侧有“刷新”按钮。标题下方有加载失败提示条，失败时显示“内容防护加载失败”、错误信息和“重试”按钮。

主体先展示一排概览指标卡片，然后通过四个标签页切换主要工作区：

- 配置探测
- 提供商治理
- 规则检测
- 检测结果

默认进入“配置探测”标签页。标签页切换不会离开当前页面，而是在同一页内切换对应面板。

### 顶部指标卡片

指标区用于快速查看内容防护整体状态，包含：

- 提供商
- 已隔离
- 低信任
- 待复核
- 检测 P50
- 检测 P95
- 检测 P99

这些指标来自内容防护概览接口。延迟指标带 `ms` 单位；没有数据时以 `-` 展示。

### 配置探测区

“配置探测”标签页左侧是模块配置表单，标题为“模块配置”。右上角有“保存”按钮，用于提交整组内容防护设置。

配置区首先是一组开关卡片，用来控制检测链路和检测能力：

- 启用检测：控制代理检测、自动探针判定和监控自动隔离。
- 直接阻断：高风险命中后是否直接阻断。
- 自动预检：是否启用自动预检任务。
- JSON 探针：是否启用严格 JSON 可选探针。
- 低信任缓冲：低信任提供商是否需要流式缓冲检测。
- 异步复核：是否把可疑结果进入复核流程。
- URL 检测：是否检测未授权外链或短链。
- 增强检测：是否启用本地轻量词法检测。
- 非法内容：是否识别博彩、诈骗、洗钱、黑产等非法语义。
- 广告导流：是否识别优惠码、邀请码、社群联系方式等广告导流。
- 自定义规则：是否对自定义关键词启用混淆匹配。
- 混淆识别：是否识别空格、符号、重复字符或常见数字替换等规避写法。

多个开关旁带有问号帮助图标，悬浮后解释对应开关的业务影响。

开关下方是参数表单，包含：

- 高风险策略：可选切换提供商、阻断、安全错误或仅记录。
- 最大延迟（ms）：限制内容检测等待时间。
- 流式模式：可选透传扫描、300ms 缓冲或全量缓冲。
- 探针协议：可选 Chat Completions 或 Responses。
- 自动预检间隔（s）：控制自动预检任务间隔。
- 最大扫描字节：限制响应正文扫描范围。
- 流式缓冲字节：限制流式内容缓冲上限。
- 高风险置信度阈值：控制高风险判定门槛。
- 增强检测阈值：控制增强检测触发门槛。
- 增强上下文窗口：控制增强检测上下文字符范围。
- URL 白名单：用多行文本维护允许出现的域名。

提交保存时，页面会读取这些开关和输入值，调用内容防护设置接口保存。

### 能力探针区

“配置探测”标签页右侧是“预检能力探针 / 能力探针与可信状态”面板，右上角有“执行探针”按钮。

探针目标通过分段按钮切换：

- 本项目
- 外部提供商检测

选择“本项目”时，页面显示提供商下拉框和模型下拉框。管理员先选提供商，再选该提供商下的模型挂载，用于对项目内已配置渠道执行内容防护探针。

选择“外部提供商检测”时，页面显示外部 Base URL、模型ID、API Key 和端点路径。这里的模型ID表示外部上游官方模型名或唯一 ID，不是本项目自定义模型名称。端点路径可选 `/chat/completions` 或 `/responses`，API Key 仅用于本次检测。

探针类型在页面中动态渲染，包括：

- 固定答案
- 外链广告识别
- 严格 JSON
- 流式污染检测
- 图片检查

页面提示可选能力探针只记录本次结果；固定答案、外链广告识别和流式污染检测齐全时，才会写入模型可信状态。执行探针前，如果缺少必须的可信探针，前端会提示确认。

### 提供商治理区

“提供商治理”标签页用于查看和处置内容完整性异常的提供商。顶部标题为“提供商处置”，右侧有两个批量操作按钮：

- 批量恢复
- 批量隔离

筛选区包含：

- 关键词搜索：可按提供商、模型或异常原因搜索。
- 状态筛选：可查看已隔离、待复核、已通过、未检测等状态。
- 已选数量提示：例如“已选 0 个提供商”。

治理表格包含以下列：

- 勾选框
- 提供商
- 状态
- 评分
- 异常原因
- 模型
- 操作

每行提供三个主要操作：

- 证据：打开内容防护证据弹窗。
- 恢复：恢复该提供商。
- 隔离：隔离该提供商。

批量隔离属于危险操作，执行前会弹出确认；批量恢复会对已选提供商逐个提交恢复请求。

### 规则检测区

“规则检测”标签页用于维护本地内容检测规则，标题为“实时内容合法性检测”。右上角有三个按钮：

- 新增
- 默认
- 保存

筛选区包含：

- 规则关键词搜索：规则名称、标识、匹配项或原因。
- 分类筛选。
- 启用状态筛选。
- 每页数量选择。

如果规则配置无法解析，页面会显示“规则配置异常”提示条，并提供“恢复默认”按钮。

规则表格包含：

- 启用
- 规则 / 分类
- 匹配 / 原因
- 策略
- 操作

每一行规则都可以直接编辑。可编辑内容包括规则名称、规则标识、分类、匹配方式、匹配项、命中原因、风险等级、命中动作、扣分和可信度。修改后行内会出现“未保存”状态，并启用“保存本行”和“撤销”按钮。行内还提供“删除”按钮。

表格底部有分页信息和“上一页 / 下一页”按钮，显示当前页、总页数和总条数。

### 本地内容检测

规则检测区下方还有一个本地检测表单，用来在不调用外部提供商的情况下，用当前规则检测一段文本。

表单包含：

- 端点路径：选择检测时模拟的端点。
- 扫描字节：可指定本次最大扫描字节，不填则使用模块配置。
- 请求上下文 JSON：填写请求上下文，例如用户消息。
- 本次 URL 白名单：为单次检测临时放行域名。
- 本地内容检测：填写要检测的文本。
- 检测按钮。

检测结果区域初始显示“未检测”。提交后页面会展示本地检测结果、风险等级、命中规则、处置动作或错误信息。

### 检测结果区

“检测结果”标签页用于查看最近一次探针结果和运行时内容防护事件。顶部标题为“探针与运行时结果”，右侧有状态徽标，初始状态为“未检测”。

探针结果区包含一个摘要区和一张结果表。结果表字段较多，用于完整追溯每个探针的判断过程，包括：

- 探针
- 端点
- 结果
- 风险
- 状态码
- 原生 / 适配
- 规则
- 分类
- 错误类型
- 处理
- 评分
- 策略
- 写入
- 耗时
- Trace
- 原因
- 原始响应

“原始响应”列提供查看入口，用于查看上游返回摘要或样本，便于追溯提供商实际输出。

执行探针时，页面会先把结果区切到运行中状态，表格中显示正在执行的探针；成功后展示具体行，失败后展示失败原因。

### 运行时事件区

检测结果页下半部分是“运行时事件 / 内容防护事件”，用于查看正式请求过程中产生的内容防护处置记录。

右上角有：

- 导出
- 刷新

筛选区包含：

- 关键词：可按 Trace、提供商、模型或原因搜索。
- 风险等级。
- 动作。
- 模型名。

事件表格包含：

- 时间
- 提供商 / 模型
- 阶段
- 结果
- 原因
- Trace

底部有分页信息和“上一页 / 下一页”按钮。点击“导出”会按当前筛选条件导出运行时事件 CSV。

### 证据弹窗

页面内置“内容防护证据”弹窗，用于从提供商治理表格查看某个提供商的证据详情。弹窗标题为“内容防护证据”，右上角有关闭按钮。

弹窗内容会展示该提供商的内容防护状态、模型状态、最近事件和命中证据。没有相关证据时，会显示空状态提示。

### 用户可完成的操作

在内容防护页中，管理员可以完成以下操作：

- 刷新内容防护概览、提供商治理列表、规则列表和运行时事件。
- 在加载失败后点击重试。
- 开启或关闭内容完整性检测能力。
- 调整高风险策略、流式检测模式、扫描字节、缓冲字节、阈值和 URL 白名单。
- 保存内容防护模块配置。
- 对项目内提供商模型执行内容防护探针。
- 临时输入外部提供商信息并执行检测。
- 选择固定答案、外链广告识别、严格 JSON、流式污染检测和图片检查等探针类型。
- 查看探针结果、Trace、原因和原始响应。
- 搜索并筛选提供商治理列表。
- 查看提供商证据。
- 手动隔离或恢复单个提供商。
- 批量隔离或批量恢复多个提供商。
- 新增、编辑、保存、撤销、删除内容检测规则。
- 恢复默认规则配置。
- 使用当前规则对本地文本做一次检测。
- 按关键词、风险等级、动作和模型筛选运行时事件。
- 导出运行时内容防护事件。

### 动态反馈与空状态

这页的交互反馈比较完整。概览加载失败时会显示页面级错误条；保存设置、执行探针、规则保存、本地检测、隔离恢复等按钮会进入执行态并在完成后反馈结果。规则表支持未保存标记，能提示用户哪些行已被修改。探针结果在未检测、执行中、成功和失败之间切换状态徽标。治理列表、规则列表、探针结果和运行时事件没有数据时，会在表格中显示空行提示。原始响应、异常原因和事件原因通过弹窗或帮助式展示承载，避免把长文本直接挤进主表格。

## 7. IP 管理页（/ip-management）

### 页面定位

这是后台的 IP 访问治理页面，用来配置 IP 管理总开关、可信代理解析、适用范围、规则动作、事件审计和解析测试。它把“安全策略配置、规则维护、命中审计、离线验证”放在同一个页面里，方便管理员直接判断某个来源 IP 应该放行、记录、限流还是阻断。

### 页面整体布局

页面顶部是“IP 管理 / 访问治理”标题区，右侧有“刷新”按钮。标题下方是一排概览指标卡，先告诉管理员当前模块是否开启，以及规则、事件和命中情况的大致规模。

主体通过四个标签页切换：

- 设置
- 规则
- 事件
- 测试

默认显示“设置”。所有标签页都在当前页面内切换，不会跳转离开。

### 顶部指标卡片

概览区包含：

- 模块状态
- 规则总数
- 启用规则
- 事件总数
- 近 1h 阻断
- 近 1h 限流
- 可信代理命中
- 无效头忽略

这些卡片帮助管理员快速判断模块当前是否在工作、规则是否过多、最近是否频繁触发阻断或限流，以及可信代理解析是否被正确命中。

### 设置区

“设置”标签页里是模块总配置。顶部有“模块设置”标题，右侧有“保存”按钮。

这一页最醒目的是一组开关，分别控制：

- 模块总开关：总入口开关，关闭时不解析、不记录、不处置。
- 仅观察：只记录决策，不真正阻断或限流。
- 可信代理解析：只有命中可信代理 CIDR 才读取转发头。
- 外部代理生效：作用于外部 OpenAI 兼容和原生协议入口。
- `/api/*` 生效：作用于内部管理 API。
- 用户端生效：作用于用户页面请求。
- 规则引擎：是否执行规则匹配。
- 允许阻断：是否让阻断规则真正拒绝请求。
- IP 限流：是否让限流规则实际限速。
- 事件记录：是否记录解析、规则和处置结果。
- 头摘要：是否保留转发头摘要用于排查。
- IP 掩码：是否在展示中隐藏部分客户端 IP。
- 失败放行：模块异常时是否继续放行请求。

每个开关都有帮助提示，说明它对真实请求的影响。页面会强调“先观察再执行”的使用顺序，避免一上来就误伤正常流量。

开关下方是具体参数：

- 可信代理 CIDR：一行一个网段，用来定义哪些直连来源可以被信任。
- 可信头优先级：可选择 `Forwarded`、`X-Forwarded-For`、`CF-Connecting-IP` 的优先顺序。
- 事件采样 %：控制事件记录比例。
- 事件保留 d：控制事件保留天数。

### 规则区

“规则”标签页用于维护 IP 规则。顶部有“IP 规则”标题和“新建”按钮。

筛选区提供：

- 关键词：按规则名搜索。
- 作用域：外部代理、`/api/*`、用户端、全部。
- 动作：放行、记录、限流、阻断。
- 状态：启用或停用。
- 查询按钮。

规则表格按行展示每条规则的关键信息：

- 规则
- 状态
- 作用域
- 匹配
- 动作
- 限流
- 优先级
- 操作

其中“匹配”列会显示匹配类型和规范化后的 IP、CIDR 或范围；“限流”列展示 QPS 和 RPM；“操作”列提供编辑、启用/停用、测试、删除。

点击“编辑”会打开规则弹窗；“测试”会把该规则的代表 IP 和作用域带入测试区；“删除”会触发二次确认。

表格下方还有分页区域，用来翻页查看更多规则。

### 事件区

“事件”标签页用于查看 IP 管理的处置审计。顶部有“处置事件”标题，右侧有“清理”按钮，可按保留天数删除旧事件。

筛选区包含：

- 关键词：按路径、Trace 或规则名搜索。
- 解析 IP：按最终识别的客户端 IP 搜索。
- 作用域：外部代理、`/api/*`、用户端。
- 动作：放行、记录、限流、阻断。
- 状态码：按 HTTP 状态码过滤。
- API Key：按关联 API Key 前缀过滤。
- 请求日志 ID：按请求日志关联查询。
- 用户 ID：按所属用户过滤。
- 开始时间
- 结束时间
- 查询按钮

事件表格展示：

- 时间
- 解析 IP
- 直连 IP
- 路径
- 动作
- 执行
- 规则
- 日志
- Trace
- 操作

这里特别强调“解析 IP”和“直连 IP”并列展示，方便判断是否经过可信代理解析；“日志”列会把请求日志、API Key 和用户 ID 关联起来；“操作”列提供详情按钮，能打开完整 JSON 记录。

表格底部同样有分页区域。

### 测试区

“测试”标签页分成两个并排的测试面板，分别是“解析测试”和“规则测试”。

#### 解析测试

这个面板用于模拟代理链和转发头解析结果。输入项包括：

- 直连 IP
- X-Forwarded-For
- Forwarded
- CF-Connecting-IP
- 可信代理 CIDR
- 可信头优先级

点击“测试”后，下面的结果框会直接显示系统会解析出哪个客户端 IP，以及为什么采信或忽略某个头。

#### 规则测试

这个面板用于模拟某个 IP 在指定作用域下会命中哪条规则。输入项包括：

- IP
- 作用域
- 路径

默认路径是 `/v1/chat/completions`。点击“测试”后，结果框会显示命中规则和最终动作，且不会真实阻断或写入正式请求日志。

### 规则弹窗

点击新建或编辑规则时，会打开“IP 规则”弹窗。弹窗内可以填写或修改：

- 规则名
- 状态
- 作用域
- 匹配类型
- 匹配值
- 动作
- 优先级
- QPS
- RPM
- 原因

弹窗底部只有一个“保存”按钮。新建默认可以先停用，或者先启用但搭配仅观察验证。

### 用户可完成的操作

在 IP 管理页中，管理员可以完成以下操作：

- 刷新模块概览、规则和事件。
- 开启或关闭模块总开关。
- 开启或关闭仅观察、可信代理解析、规则引擎、阻断、限流、事件记录、IP 掩码和失败放行。
- 选择该模块作用于外部代理、内部 `/api/*` 或用户端页面。
- 配置可信代理 CIDR、可信头优先级、事件采样率和事件保留天数。
- 新建、编辑、启用、停用、删除 IP 规则。
- 为规则设置匹配类型、匹配值、动作、优先级和限流参数。
- 按关键词、作用域、动作、状态筛选规则。
- 查看命中事件、解析来源、关联日志和 Trace。
- 清理过期事件。
- 用模拟头和代理链做解析测试。
- 用指定 IP、作用域和路径做规则测试。
- 查看单条事件详情 JSON。

### 动态反馈与空状态

这个页面的反馈主要体现在三处：保存配置时会立即反馈结果，规则和事件列表支持分页加载，测试区会把结果直接输出到只读结果框里。规则和事件表没有数据时会显示空状态提示；删除规则、清理事件和执行某些危险动作时会有二次确认。帮助图标分布在标题、字段和按钮旁边，方便管理员在不离开页面的情况下理解每个控制项的作用。

## 8. 调用测试页（/playground）

### 页面定位

这是管理端的在线调试页，用来做两类事情：一类是单请求调试，观察一次真实调用的路由、返回、Token 和图片输入效果；另一类是批量连通性测试，快速判断多个提供商或模型挂载是否整体可用。页面明确强调它只走内部 `/api/playground/*` 链路，不直接暴露对外 `/v1/*` 正式入口。

### 页面整体布局

页面顶部是一个横向 Hero 区，标题为“调用测试”。左侧说明它的用途，右侧给出使用范围和两个关键词提示：

- 单请求
- 批量连通

Hero 下方是模式切换 Tab：

- 模式 A：单请求调试
- 模式 B：批量连通性测试

切换模式会在同一页内切换两个工作区，不会跳转页面。

### 模式 A：单请求调试

这是页面的主调试台，用来提交一次真实请求并查看返回结果。

#### 输入区

输入区标题是“单请求调试”，内部是一个表单，包含：

- 内部接口：可选 `/api/playground/chat-completions` 或 `/api/playground/responses`。
- 提供商：可选自动路由，也可以显式指定某个提供商。
- 模型：必须选择一个已配置模型。
- stream 开关：控制是否走流式返回。
- 用户消息：填写要发送给模型的文本。
- 图片请求设置：可选不附带图片、使用图片链接、上传本地图片、生成图片（image_generation）。
- 图片细节：自动、低细节、高细节。

图片相关设置会随模式联动显示：

- 选“图片链接”时，会出现图片 URL 输入框。
- 选“上传本地图片”时，会出现文件选择框。
- 选“生成图片”时，会自动切换到 Responses，并把这次请求当作生图请求。

页面还会在输入区下方显示一条图片说明文字，告诉用户当前是纯文本、图片链接、上传图片，还是生成图片，并说明图片大小和格式限制。

#### 操作按钮

输入区底部有两个主要按钮：

- 清空：清掉当前表单内容并重置到默认状态。
- 发送测试请求：发起本次真实调试请求。

#### 结果区

右侧是“单请求返回结果”面板，包含：

- 结果元信息区：展示请求过程中的摘要信息。
- 等待请求占位：初始状态下显示“等待请求...”。
- 结果视图：请求完成后显示结构化返回内容。

单请求成功后，页面会把返回内容直接渲染出来，流式请求则会按流式过程逐步刷新结果区。失败时会把错误直接放到结果面板里，并显示失败摘要。

### 模式 B：批量连通性测试

这是第二个工作区，用来快速判断多个提供商或模型是否整体可用。

#### 批量选择区

批量模式顶部是“批量连通性测试”表单，先提供三组快捷按钮：

- 选中启用提供商
- 选中全部提供商
- 清空选择

下面是提供商选择列表，默认显示“正在加载提供商列表...”。加载完成后会以一组可勾选条目呈现，每个条目代表一个可测试提供商。

#### 批量执行按钮

选择好目标后，底部有一个主按钮：

- 批量测试所选提供商

#### 批量结果区

右侧结果面板标题是“批量测试返回结果”，包括：

- 元信息区：显示本次批量测试进度和完成数。
- 空状态：初始显示“等待批量测试...”。
- 结果视图：显示每个提供商的测试回放和汇总。

批量测试是逐个模型、逐个提供商推进的，不是单次把所有结果粗暴塞进一个结果块里。页面内部会持续刷新完成进度，并在每个提供商或模型测试结束后更新结果视图。

### 结果展示特征

这个页面的结果展示不是简单文本，而是带有多层信息的调试输出：

- 会显示请求方式、目标接口、模型名、提供商名、流式状态和耗时。
- 会把图片输入模式下的结果和文本结果区分开。
- 会保留可读的失败信息，方便排查路由、响应和能力不匹配问题。
- 批量测试结果会按提供商分组，再按模型或阶段展示。

### 用户可完成的操作

在调用测试页中，管理员可以完成以下操作：

- 在单请求模式下选择内部接口。
- 指定自动路由或手动指定提供商。
- 选择要测试的模型。
- 开关流式调试。
- 输入用户消息并发起真实请求。
- 选择是否附带图片。
- 使用图片链接、本地上传或生图请求进行多模态测试。
- 清空当前表单。
- 查看单次请求返回结果和错误信息。
- 在批量模式下按启用状态或全量勾选提供商。
- 清空批量选择。
- 发起批量连通性测试。
- 查看批量测试进度、结果和回放。

### 动态反馈与空状态

这个页面的反馈非常直接：请求发出时会显示“请求发送中...”，单请求和批量结果都各自有独立的空状态占位。流式请求会边返回边刷新结果；批量测试会逐步更新进度并在完成后汇总展示。图片模式会根据选择动态显隐输入框，并在说明区提示当前是链接、上传还是生成图片。若模型不可用或没有可测试模型，页面会直接给出错误提示，不会让用户盲猜为什么无法测试。

## 9. 并发探测页（/benchmark）

### 页面定位

这是管理端的真实流式并发探测页面，用来通过真实外部 API Key、真实模型和真实流式文本入口，测试项目在不同并发档位下的稳定性、吞吐、延迟和首事件表现。它不是普通的可用性检测页面，而是面向性能容量判断的压测控制台。

页面强调三点：

- 执行链路使用真实外部 API Key。
- 探测方式是逐模型流式并发扩探。
- 结果会生成 HTML 和 JSON 报告。

### 页面导航与入口

页面顶部提供两个快捷入口：

- 启动探测：跳到压测配置区域。
- 查看结果：跳到结果报告区域。

页面还有锚点导航：

- 压测配置
- 过程观察
- 结果报告

这些入口帮助管理员在配置、实时过程和最终结果之间快速切换。

### 压测配置区

压测配置区标题为“压测参数”。这里负责收集启动真实并发探测所需的全部参数。

#### 连接与入口

这一组包含启动任务的核心连接信息：

- 项目代理地址：填写项目对外代理服务地址，默认是本地地址。
- 外部 API Key：填写真实外部用户调用项目时使用的 API Key。
- 流式入口：可选择 `/v1/chat/completions` 或 `/v1/responses`。
- 输出上限 token：控制每次探测请求的最大输出 token。

这些字段决定压测请求从哪里发出、以什么身份访问、走哪个外部流式入口。

#### 规模与样本

这一组决定压测强度和样本内容：

- 基准并发：本次探测的核心目标并发档位。
- 最大探测并发：用于限制探测向上扩张的最高并发。
- 文本提示词：压测时发送给模型的提示词。

如果最大探测并发填 0，页面会按后端逻辑让任务自行判断探测上限。

#### 模型范围

模型范围区用于选择要真实探测的模型。

这里包含：

- 筛选模型输入框：按模型名快速过滤。
- 当前模型选择状态说明：显示已选数量、当前可见数量、可用于当前入口的模型数量。
- 模型选择列表：展示已启用、已绑定且支持当前流式入口的模型。
- 全选当前可见按钮。
- 清空按钮。

当切换流式入口时，模型列表会重新过滤，只保留能用于该入口的模型。没有可测模型时，会显示“当前没有已绑定且可用于该流式入口的模型。”

#### 高级参数

高级参数默认收起，用来调整更细的探测策略和稳定性判断标准。包含：

- 起始并发：控制第一轮并发档位。
- 探测轮数：控制并发阶梯最多扩张多少轮。
- 放大倍率：控制每轮并发向上扩张的幅度。
- 每轮最少请求：控制每个并发档位的最低样本数。
- 请求倍数：按并发数放大每轮请求量。
- 预热请求：正式探测前先发出的预热请求数量。
- 客户端超时 s：压测器等待单个请求的最长时间。
- 请求超时 s：底层请求超时上限。
- 边界细化轮数：首次不稳定后继续细化稳定边界。
- 进度刷新 s：控制前端和日志读取进度的刷新间隔。
- 成功率阈值：判断档位稳定所需的成功率。
- P95 阈值 ms：总请求 P95 延迟的稳定性阈值。
- 首事件 P95 阈值 ms：流式首事件延迟的稳定性阈值。
- 允许超时数：单档位允许的超时请求数。
- 允许繁忙数：允许出现的 429、503、504 等繁忙或过载错误数量。

每个高级参数旁都有帮助说明，便于管理员理解该参数对探测耗时、精度和稳定性判断的影响。

#### 操作按钮

配置区底部有两个任务按钮：

- 停止任务：当前没有运行任务时不可用；有任务运行时可停止当前任务。
- 开始探测：提交当前配置并启动真实并发探测。

启动前如果没有填写项目代理地址、外部 API Key 或没有选择模型，页面会提示错误，不会提交任务。

### 过程观察区

过程观察区标题为“当前任务”，用于查看正在运行或最近一次任务的状态。

它包含：

- 任务状态徽标：可能显示未开始、运行中、已完成、失败、已停止。
- 进度摘要：显示当前模型、入口、并发、完成请求数和进度百分比。
- 进度条：显示当前任务整体推进程度。
- 即时指标卡片：显示模型、入口、模式、并发、进度、RPS。
- 事件日志窗口：展示任务执行日志，并自动滚动到最新日志。

如果任务正在运行，页面会周期性刷新当前任务状态。任务完成后，进度区域会提示可查看阶段结果和报告。任务失败时，会显示失败原因。

### 结果报告区

结果报告区标题为“阶段指标与报告”。它用于展示探测完成后的汇总与明细。

右侧提供两个报告入口：

- JSON 报告
- HTML 报告

任务尚未生成报告时，这两个入口不可用。报告生成后，管理员可以打开或下载对应报告。

结果区初始空状态提示：任务完成后会展示最高稳定并发、首次不稳定并发和各阶段数据。

任务完成后，结果区会展示两类内容：

- 汇总结论
- 按模型展开的阶段明细

### 汇总结论

汇总结论会展示：

- 探测模型数
- 有稳定档位模型数量
- 基准并发
- 流式入口
- 输出上限
- 当前筛选成功率阈值

这部分用于快速判断本轮探测覆盖了多少模型，以及整体是否得到了可用的稳定并发结论。

### 模型阶段明细

每个模型会形成一组独立结果，包含：

- 模型名
- 入口
- 最高稳定并发
- 首次不稳定并发
- 建议运行并发
- 最高吞吐 RPS
- 建议档位 P95
- 建议档位首事件 P95

模型下方还有阶段表格，逐并发档位展示：

- 并发
- 状态：稳定或不稳定
- 成功请求数
- 成功率
- RPS
- P95(ms)
- 首事件 P95(ms)
- 最大延迟(ms)
- 备注

这个明细用于判断哪个并发档位开始失稳，以及失稳原因是成功率、延迟、首事件延迟、超时还是繁忙错误。

### 用户可完成的操作

在并发探测页中，管理员可以完成以下操作：

- 填写项目代理地址和外部 API Key。
- 选择流式入口。
- 设置输出 token 上限。
- 设置基准并发和最大探测并发。
- 修改压测提示词。
- 搜索模型。
- 单独选择或取消选择模型。
- 一键全选当前可见模型。
- 一键清空模型选择。
- 展开高级参数并调整探测轮数、采样、超时、阈值和稳定性判断条件。
- 启动真实并发探测。
- 停止正在运行的任务。
- 查看任务状态、当前进度、即时指标和事件日志。
- 查看最近一次任务结果。
- 打开 HTML 报告。
- 打开 JSON 报告。
- 按模型查看最高稳定并发、建议并发和每个并发档位的详细表现。

### 动态反馈与空状态

页面会在加载后读取可测模型列表，并自动读取最近一次任务。没有可测模型时，模型选择区显示空状态。没有任务结果时，结果区显示等待任务完成的提示。启动任务后，日志会先显示“任务启动中...”，随后任务状态、进度、即时指标和日志会持续刷新。任务完成、失败或停止后，状态会同步变化；报告可用后，HTML 和 JSON 报告入口会变为可打开状态。

## 10. 运维监控页（/operations）

### 页面定位

运维监控页是管理员查看系统实时运行状态、资源占用、请求质量、内容防护风险和宿主配置建议的页面。它面向生产运维场景，重点帮助管理员判断当前服务是否正常、并发与队列是否接近瓶颈、请求失败是否升高，以及当前宿主配置是否需要调整。

### 顶部状态区

页面顶部展示“实时运行大盘”，并集中显示当前运行状态摘要：

- 状态：显示系统当前是否正常，异常或部分指标不可用时会体现为降级状态。
- 并发：显示当前活跃请求数量。
- P95：显示当前统计窗口内的响应延迟 P95。
- QPS：显示当前统计窗口内的每秒请求量。

顶部还提供统计窗口选择器，管理员可以在 5 分钟、15 分钟、1 小时和 1 天之间切换。切换后，页面会按新的时间窗口重新拉取并展示监控数据。

顶部提供“刷新”按钮，管理员可以手动立即刷新当前监控数据。刷新完成后，按钮和页面轻提示会反馈刷新结果；刷新失败时会显示错误反馈。

### 资源占用区

资源占用区展示三类运行资源：

- 宿主 CPU：展示宿主机整体 CPU 使用情况，并标明宿主 CPU 核心数量。
- 宿主内存：展示宿主机整体内存使用比例，以及已用内存和总内存。
- 项目进程：展示当前项目进程的 CPU 使用情况、内存占用和进程数量。

这一区域明确区分宿主机级资源与当前项目进程级资源，管理员可以据此判断是整机资源紧张，还是项目自身进程压力偏高。

### 请求指标区

请求指标区用一组指标展示当前统计窗口内的流量与结果：

- 请求：总请求数。
- 成功：成功请求数。
- 失败：失败请求数。
- 429：限流类响应数量。
- 5xx：服务端错误响应数量。
- 流式：流式请求数量。
- 图片：图片类请求数量。
- 积压：待补全或待处理的后台任务积压数量。

每个指标下方都有进度条式的占比展示，便于管理员快速判断异常指标是否在总流量中占比较高。

### 内容防护指标区

内容防护指标区展示内容完整性与安全治理相关数据：

- 内容拦截：被内容防护拦截的次数。
- 内容复核：进入复核状态的次数。
- 高风险：高风险命中次数。
- 已隔离：因内容风险被隔离的提供商数量。
- 低信任流量：低信任提供商相关流量占比。
- 最高违规率：当前提供商中的最高违规比例。

如果内容防护功能处于关闭状态，这些指标会直接显示为关闭，避免管理员误以为没有风险数据就是风险为零。

### 全局租约区

全局租约区展示系统并发租约和队列压力：

- 请求：当前全局活跃请求租约数量。
- 流式：当前全局活跃流式租约数量。
- Worker：当前 worker 活跃请求与项目进程数量。

下方会继续展示活跃请求、活跃流式、日志队列、事件队列和计费锁等压力条。管理员可以通过这些信息判断 Redis 并发计数、日志处理队列、事件处理队列和计费补全链路是否出现积压。

该区域还包含一个状态点，用于提示 Redis 或全局租约相关状态是否可用。

### 响应质量区

响应质量区展示当前统计窗口内的延迟和错误质量：

- P50：中位响应延迟。
- P95：95 分位响应延迟。
- P99：99 分位响应延迟。
- TTFB：首包或首 token 平均延迟。

下方还会展示失败率、429 比例和告警数量。管理员可以用它判断当前问题是整体延迟升高、尾延迟异常、限流过多，还是告警事件正在增加。

### 趋势图区

趋势图区包含两张趋势图：

- 请求 / 失败：展示统计窗口内请求量与失败量的变化趋势。
- QPS / P95：展示吞吐与延迟 P95 的变化趋势。

这两张图用于观察短时间波动，帮助管理员判断问题是瞬时尖峰、持续恶化，还是已经恢复。

### 宿主配置方案区

宿主配置方案区用于根据当前宿主资源和管理员输入的公网出口带宽生成配置建议。顶部会展示当前识别到的宿主档位、CPU、内存、带宽和可用资源摘要。

该区域提供“出口带宽 Mbps”输入框和“计算”按钮。管理员必须输入公网出口带宽后，页面才会生成与带宽相关的宿主方案。输入后点击“计算”或按回车，页面会重新计算配置建议，并显示带宽来源与当前带宽信息。

配置方案以多个方案卡片展示，每个方案包含：

- 方案名称和摘要。
- 推荐标识。
- 非流式稳定容量。
- 非流式峰值容量。
- 流式稳定容量。
- 流式峰值容量。
- 请求瓶颈与流式瓶颈。
- 出口带宽、可用带宽、非流式带宽和流式带宽依据。
- CPU、内存、数据库、上游连接和带宽等容量计算依据。
- 当前配置值与推荐配置值的差异。
- 方案说明。

每个方案提供“套用”按钮。管理员点击后，页面会把该方案中可在线生效的系统设置应用到系统中，并刷新监控数据。套用成功或失败都会在按钮和轻提示中反馈。

每个方案还提供“重启项目”按钮。点击后会出现危险操作确认，说明重启会触发项目服务重启，页面可能短暂断开。管理员确认后，系统会触发重启，并给出触发结果反馈。

### 运行时建议区

运行时建议区展示需要通过环境变量、启动脚本、数据库连接池、Redis 连接池、后台 worker 或上游连接池等方式调整的建议项。每个建议项会显示：

- 中文名称。
- 推荐值。
- 对应环境变量或配置键。
- “重启生效”提示。

页面明确提示：一键套用只会更新可在线生效的系统设置，运行时参数需要修改配置并重启 Web 或后台进程后才会生效。

### 用户可完成的操作

在运维监控页中，管理员可以完成以下操作：

- 查看系统当前运行状态、并发、P95 和 QPS。
- 切换监控统计窗口。
- 手动刷新监控数据。
- 查看宿主 CPU、宿主内存和项目进程资源占用。
- 查看请求总量、成功、失败、限流、服务端错误、流式请求、图片请求和积压情况。
- 查看内容防护拦截、复核、高风险、隔离、低信任流量和违规率。
- 查看全局租约、Redis 状态、worker 活跃情况和后台队列压力。
- 查看 P50、P95、P99、TTFB、失败率、429 比例和告警数量。
- 查看请求失败趋势和 QPS 延迟趋势。
- 输入公网出口带宽并计算宿主配置方案。
- 对比不同宿主配置方案的容量、瓶颈、计算依据和推荐改动。
- 套用推荐配置中的在线生效项。
- 触发项目重启。
- 查看需要重启后生效的运行时建议。

### 动态反馈与空状态

页面加载后会自动读取运维监控数据，并以低频轮询方式持续刷新。页面隐藏时会避免不必要的自动刷新。手动刷新、带宽计算、套用配置和重启项目都会提供按钮加载态、成功反馈或失败反馈。

当宿主信息不足或未输入公网出口带宽时，配置方案区会显示需要输入带宽或无法生成方案的空状态。当没有运行时建议时，运行时建议区会显示暂无建议。当内容防护关闭时，内容防护指标会显示关闭状态。

## 11. 使用文档页（/docs）

### 页面定位

使用文档页是平台对外接入说明页，给开发者和集成方提供最短接入路径。页面核心不是讲概念，而是直接告诉用户该填什么、该调什么、该请求什么接口、出错时该看什么字段。这个页面在管理端和用户端共用同一套内容，只是入口位置不同。

### 顶部接入摘要

页面顶部以“开发者接入”和“使用文档”为主，直接给出三类最关键摘要：

- Base URL：平台对外统一接入地址。
- 鉴权方式：Bearer API Key。
- 路由策略：可用性优先。

右侧还单独强调统一接入地址，并提示 OpenAI 兼容、Gemini 和 Claude 各自对应的标准入口。这里提供一个直接可复制的入口地址按钮，方便开发者快速带入 SDK 或客户端配置。

### 接入信息区

接入信息区把正式开始接入所需的基础信息拆成几个可复制卡片：

- 平台地址：平台根地址。
- OpenAI Base URL：OpenAI 兼容入口地址。
- 鉴权 Header：`Authorization: Bearer YOUR_PLATFORM_API_KEY`。
- 环境变量：`OPENAI_API_KEY` 和 `OPENAI_BASE_URL` 的配置示例。

每个卡片都带有对应的复制按钮，便于开发者直接复制到本地环境、脚本或客户端配置中。

### 常用入口区

页面在侧边列出几个最常用的标准路径，帮助开发者快速记住平台支持的正式入口：

- `GET /v1/models`：获取模型列表。
- `POST /v1/responses`：OpenAI 文本入口。
- `POST /v1/chat/completions`：Chat 兼容入口。
- `POST /v1beta/models/{model}:generateContent`：Gemini 原生入口。
- `POST /v1/messages`：Claude 原生入口。

这一区域主要用于减少接入时对接口路径的猜测。

### 最短示例区

最短示例区给出四种最常见的接入演示，每个示例都有代码片段和复制按钮：

- 自检：先调用模型列表接口，确认 API Key、地址和可见模型是否正确。
- Responses 文本请求：展示 OpenAI 兼容新接入的推荐调用方式。
- Chat Completions 请求：展示仍依赖旧 Chat 协议客户端的调用方式。
- Gemini 原生请求：展示 Gemini 原生协议的调用方式。
- Claude 原生请求：展示 Claude Messages API 的调用方式。

每个示例都明确了请求路径、请求头和最小请求体，方便开发者直接改成自己的模型名后发起测试。

### 客户端配置区

客户端配置区给出常见接入方式的填写口径：

- 图形客户端的 OpenAI Compatible 配置。
- 服务端脚本和 CLI 常用的环境变量配置。
- 模型与能力说明。

其中模型与能力说明明确提示：模型名、视觉、工具调用、图片生成等能力都以平台授权和实际路由结果为准，不建议在业务侧自行猜测或传入内部调试字段。

### 错误格式区

错误格式区告诉接入方失败时应该看什么。页面用两块内容说明对外错误的统一结构：

- 普通错误响应：展示 `error` 对象中常见字段，包括 `message`、`type`、`code`、`trace_id`、`retryable`、`recoverable` 和 `category`。
- 流式错误事件：展示 SSE 场景下的错误事件和结束标记。

这里的重点是帮助开发者在接入失败时快速定位问题来源，而不是让他们只看到一个模糊的失败提示。

### 用户可完成的操作

在使用文档页中，开发者可以完成以下操作：

- 复制平台地址、OpenAI Base URL、鉴权 Header 和环境变量。
- 查看平台统一接入地址。
- 快速确认 OpenAI、Gemini 和 Claude 的标准请求路径。
- 复制最短自检请求。
- 复制 Responses、Chat、Gemini 和 Claude 的示例请求。
- 查看客户端配置示例。
- 查看错误响应结构和流式错误示例。

### 动态反馈与空状态

页面中的复制按钮会在复制后给出反馈，方便确认内容已复制成功。由于页面内容以固定接入说明和代码示例为主，正常情况下不会出现复杂的数据空状态；但如果外部地址无法解析或模型信息不足，相关摘要区域会保持基础展示，不会阻塞其它说明内容。

## 12. 用户管理页（/users）

### 页面定位

用户管理页是管理员集中管理平台账号、角色、启停状态、API Key 关联数量、共享账户余额、额度用量和批量冻结策略的页面。它既承担账号创建和账号目录查询，也承担基础治理动作，例如启用、禁用、升降角色、重置密码、删除账号和批量调整冻结金额。

### 顶部总览区

页面顶部显示“账户治理与余额调度”，并提供三个主要入口：

- 新建账号：跳转到创建账号区域。
- 查看账号目录：跳转到用户列表区域。
- 导出用户 CSV：打开用户导出文件。

右侧摘要展示当前筛选条件下命中的账号总数，并显示本页管理员数量和本页启用账号数量。这个区域用于让管理员快速判断当前列表范围和本页账号治理状态。

如果上一轮操作产生错误或成功结果，顶部下方会显示反馈提示。常见反馈包括账号已创建、账号已更新、批量冻结金额已应用、账号已删除，以及目标用户不存在、不能禁用当前管理员自己、不能降级当前管理员自己、不能删除当前管理员自己、最后一个管理员不能删除等保护提示。

### 页面导航区

页面提供锚点导航，管理员可以快速跳到：

- 创建账号。
- 账号目录。
- 批量冻结。

这些入口对应页面内三块核心工作区。

### 指标卡片区

指标卡片区展示当前筛选与当前页的汇总数据：

- 筛选命中：当前筛选条件下的用户总数。
- 本页账号：当前页实际展示的账号数量。
- 本页管理员：当前页中管理员账号数量。
- 本页启用中：当前页中启用账号数量。
- 本页关联 API Key：当前页用户名下 API Key 总数。
- 公开注册：当前系统是否允许公开注册普通用户。

这组指标用于快速判断当前筛选范围内用户结构和账号开放策略。

### 创建账号区

创建账号区用于管理员新增用户。表单包含：

- 用户名。
- 邮箱。
- 初始密码。
- 角色选择：普通用户或管理员。
- 创建后立即启用开关。
- 创建账号按钮。

创建后立即启用开关打开时，新账号创建后可以直接登录并接收 API Key 授权；关闭时，新账号会先处于不可用状态。

旁边的治理状态卡片展示当前策略信息：

- 公开注册状态。
- 当前页容量。
- 当前角色筛选。
- 当前状态筛选。

这部分用于提示管理员当前创建与列表治理所处的系统策略背景。

### 账号目录筛选区

账号目录区顶部提供“选择本页”勾选控件，用于把当前页可见用户批量选中，配合后面的批量冻结功能使用。

筛选表单支持以下条件：

- 关键词：可按账号 ID、用户名或邮箱搜索。
- 角色：全部、管理员、普通用户。
- 状态：全部、启用中、已禁用。
- 每页数量：10、20、50、100。

筛选表单提供“应用筛选”和“清空筛选”两个操作。应用筛选后列表按条件刷新；清空筛选会回到全部用户列表。

筛选区下方还有当前目录摘要：

- 当前命中条数。
- 当前页码和总页数。
- 当前角色筛选。
- 当前状态筛选。

### 用户列表表格

用户列表是该页面的核心治理表格。每一行代表一个用户，表格包含以下信息和操作。

批量列：

- 每个用户都有“纳入批量”勾选项。
- 勾选后该用户会进入批量冻结金额操作范围。

账号列：

- 展示用户名。
- 展示用户 ID 和邮箱。
- 提供“查看详情”入口，进入单个用户详情页查看更完整的余额、资产、日志和账单信息。

角色与状态列：

- 展示启用中或已禁用状态。
- 展示管理员或普通用户角色。
- 用文字说明该账号属于管理员治理或普通用户接入场景。

账户概览列：

- 展示该用户名下 API Key 数量。
- 展示共享账户余额。
- 展示冻结金额。
- 展示可用余额。

额度用量列：

- 展示风险状态，例如正常、低余额、无密钥或其它风险标签。
- 展示可用余额占比。
- 展示今日请求次数和今日 token。
- 展示本月请求次数、本月 token 和本月成本。
- 展示累计请求次数、累计 token 和累计成本。

这列用于让管理员不用进入详情页，也能在总表中看到每个用户的额度和消耗情况。

时间信息列：

- 展示最近登录时间。
- 展示创建时间。
- 如果用户从未登录，会显示未登录。

运维动作列：

- 启用账号或禁用账号。
- 升为管理员或降为用户。
- 删除账号。
- 输入新密码并重置密码。

删除账号会弹出确认提示，确认删除该用户及其名下 API Key。启停、角色变更、重置密码和删除操作都会在完成后回到当前筛选上下文。

### 分页区

当列表有数据时，表格下方展示分页信息：

- 当前第几页。
- 总页数。
- 总条数。
- 上一页入口。
- 下一页入口。

分页会保留当前关键词、角色、状态和每页数量筛选条件。

### 空状态区

如果当前筛选条件下没有用户，页面会显示空状态，提示当前没有可治理账号，并提供两个操作：

- 查看全部账号。
- 创建账号。

这让管理员可以快速放宽筛选或直接新增用户。

### 批量冻结区

批量冻结区用于对账号目录中已勾选的用户批量应用冻结金额。页面会显示：

- 已选账号数量。
- 已选用户 ID 预览。
- 冻结金额输入框。
- 批量应用冻结按钮。

勾选单个用户时，页面会加载该用户当前冻结金额，并把当前值显示出来；勾选多个用户时，如果当前冻结金额不一致，会提示需要输入统一新值。输入框留空表示不修改。

提交前页面会校验：

- 必须至少选择一个用户。
- 必须填写至少一个真正发生变化的额度项。
- 冻结金额必须是大于或等于 0 的数字。

提交成功后，页面会提示批量应用结果，并刷新当前冻结金额预览；失败时会显示错误提示。

### 用户可完成的操作

在用户管理页中，管理员可以完成以下操作：

- 创建普通用户或管理员账号。
- 设置新账号是否创建后立即启用。
- 按关键词、角色、状态和每页数量筛选用户。
- 清空筛选条件。
- 导出用户 CSV。
- 查看用户详情页。
- 查看每个用户的 API Key 数量、余额、冻结金额和可用余额。
- 查看每个用户今日、本月和累计请求、token、成本与风险状态。
- 启用或禁用用户。
- 将普通用户升为管理员。
- 将管理员降为普通用户。
- 重置用户密码。
- 删除用户及其名下 API Key。
- 选择本页用户或逐个勾选用户。
- 批量设置选中用户的冻结金额。
- 翻页查看更多用户。

### 动态反馈与保护机制

页面会对批量冻结操作提供即时选择统计、用户 ID 预览、当前冻结金额预览和输入有效性提示。批量提交时按钮会进入加载状态，提交完成后会显示成功或失败提示。

页面对管理员自身和最后管理员账号有保护机制：不能禁用当前登录管理员自己，不能把当前登录管理员自己降为普通用户，不能删除当前登录管理员自己，也不能删除系统中的最后一个管理员。删除用户前会要求确认，降低误操作风险。

## 13. API 密钥管理页（/api-keys）

### 页面定位

API 密钥管理页是管理员集中管理外部调用凭据的页面。它用于创建、编辑、筛选、启停、删除、轮换 API Key，并管理密钥归属用户、提供商授权、模型白名单、频控、过期时间、策略模板和成本透视。

### 顶部操作区

页面顶部显示“API 密钥管理”，提供三个主要入口：

- 新增 API 密钥：打开新增密钥弹窗。
- 查看全局日志：跳转到日志中心。
- 导出密钥 CSV：导出当前系统 API Key 数据。

右侧密钥总览会显示当前可运维的 API Key 总数、启用数量和禁用数量，并用状态条展示启用占比。

### 概览指标区

概览指标区展示授权与消耗总览，包括：

- 总密钥数。
- 启用中数量。
- 禁用中数量。
- 已过期数量。
- 余额耗尽数量。
- 总请求量。
- 总 Token。
- 累计消费。

下方的余额视图继续展示输入 Token、输出 Token、账户总余额和累计充值。页面还有一块密钥总览摘要，与顶部总览保持同类信息，方便管理员在滚动到页面中部时仍能看到总体状态。

### 密钥列表筛选区

密钥列表支持异步加载和筛选，筛选项包括：

- 搜索名称、备注、前缀或归属用户。
- 状态：全部状态、正常可用、已禁用、已过期、余额耗尽、未绑定提供商。
- 启用状态：全部、仅启用、仅禁用。
- 归属用户。
- 每页数量：20、50、100。

筛选条件变化后，列表会自动刷新；关键词输入会延迟触发搜索，避免每输入一个字都立即请求。

列表右上角显示当前页码、总页数、总条数和已选数量，并提供“刷新”“上一页”“下一页”等操作。

### 批量操作区

密钥列表上方提供一组批量治理按钮：

- 选择本页。
- 清空选择。
- 批量启用。
- 批量禁用。
- 批量删除。
- 批量轮换。
- 批量过期。
- 套用模板。
- 改提供商授权。
- 全部恢复全提供商授权。
- 本页全选。

批量启用、禁用、删除、轮换、过期、套用模板和改提供商授权都要求先选择至少一个 API Key。批量删除、批量轮换、批量过期和全部恢复全提供商授权会要求确认，避免误操作。

批量轮换完成后会打开结果弹窗，展示每个密钥的新明文。页面明确提示这些明文只在当前界面展示，需要立即同步到外部业务系统。弹窗提供复制全部结果按钮。

### 密钥列表表格

密钥列表表格展示每个 API Key 的核心信息：

- 选择：用于纳入批量操作。
- 名称：展示密钥名称和备注。
- 归属用户：展示用户名和用户 ID；未分配时显示未分配或归属异常。
- 密钥：展示明文或掩码；可用明文时提供复制按钮，历史未存明文的密钥会提示不可复制。
- 状态：展示正常、禁用、过期、余额耗尽等状态。
- 授权提供商：展示授权提供商数量、名称，以及模型白名单数量或全部可路由。
- 频控：展示累计 Token 统计，以及 QPS、RPM 限制。
- 账单 / 余额：展示累计消费和共享账户余额。
- 最近使用：展示最近使用时间和过期时间。
- 操作：提供详情、编辑、启用或禁用、删除。

点击“详情”会进入单个 API Key 详情页；点击“编辑”会打开编辑弹窗；点击“删除”会要求确认；启用和禁用会直接执行并刷新列表。

### 新增与编辑密钥弹窗

新增或编辑 API Key 时，会打开密钥表单弹窗。表单包含：

- 名称。
- 策略模板。
- 密钥生成方式：自动生成或自定义密钥。
- 密钥明文。
- 备注。
- 归属用户。
- 过期时间。
- QPS 限制。
- RPM 限制。
- 是否启用。
- 授权提供商。
- 模型白名单。

自定义密钥必须以 `sk-aotu-` 开头，并满足长度和字符规则；页面会实时校验格式。新增密钥选择自动生成时不需要填写明文。编辑已有密钥时，明文留空表示保持当前密钥不变，填写则替换为新密钥。

保存新增密钥后，页面会显示原始 API Key 明文，并提示该明文只会返回一次，需要立即保存。该区域提供复制明文按钮。

策略模板下拉选择后，会自动预填启用状态、过期时间、授权提供商和模型白名单。

### 授权提供商与模型白名单

提供商授权选择器会展示可绑定提供商及其模型信息和可用状态。默认全选表示 API Key 自动同步全部提供商授权；如果取消部分提供商，则变成该密钥的自定义提供商授权。

模型白名单选择器展示模型目录中的模型、启用状态和可用提供商数量。不勾选任何模型表示不限制模型；勾选后，该 API Key 只能访问所选模型，并且仍需满足提供商授权和实际路由条件。

批量更新提供商授权弹窗也使用同类选择器，可把已选 API Key 统一改为指定提供商授权；保持全选时表示恢复自动同步全部提供商授权。

### 策略模板区

策略模板区用于维护可复用的 API Key 治理模板。表格展示：

- 模板名称和备注。
- 授权提供商。
- 模型白名单。
- 有效期。
- 到期策略或启用状态。
- 操作。

管理员可以新增模板、编辑模板或删除模板。新增和编辑模板时可以设置模板名称、多少天后过期、是否启用、备注、授权提供商和模型白名单。

批量套用模板弹窗允许管理员选择一个已启用模板，并应用到当前已选 API Key。套用后会更新对应密钥的策略配置。

### 成本透视区

成本透视区用于按不同维度查看一段时间内的成本和调用情况。管理员可以选择：

- 按用户。
- 按模型。
- 按提供商。
- 最近 7 天、30 天或 90 天。

透视表展示维度值、请求数、Token、累计成本和平均延迟。切换维度或时间窗口会刷新透视数据；也可以点击“刷新透视”手动刷新。

### 用户可完成的操作

在 API 密钥管理页中，管理员可以完成以下操作：

- 新增 API Key。
- 自动生成或自定义 API Key 明文。
- 复制新建后的密钥明文。
- 编辑 API Key 名称、备注、归属用户、启用状态、过期时间、QPS、RPM、提供商授权和模型白名单。
- 按关键词、状态、启用状态、归属用户和每页数量筛选密钥。
- 刷新密钥列表。
- 查看 API Key 详情页。
- 复制可用密钥明文。
- 启用、禁用或删除单个 API Key。
- 选择本页或逐个选择 API Key。
- 批量启用、批量禁用、批量删除、批量轮换、批量过期。
- 批量套用策略模板。
- 批量修改提供商授权。
- 将全部 API Key 恢复为全提供商授权。
- 新增、编辑、删除策略模板。
- 查看按用户、模型或提供商聚合的成本透视。
- 导出密钥 CSV。
- 跳转到全局日志中心。

### 动态反馈与空状态

页面加载时会读取提供商、用户、模型、策略模板、密钥列表、概览指标和成本透视数据。加载中会显示对应空状态；加载失败会在列表、模板表或透视表中显示错误提示。

筛选、刷新、保存、删除、批量操作、套用模板、轮换和授权变更都会提供按钮加载态或轻提示反馈。没有 API Key 时，列表显示当前筛选条件下暂无 API 密钥；没有策略模板时，模板表显示暂无策略模板；成本透视没有数据时，会提示当前时间窗口内暂无成本透视数据。

## 14. 日志中心页（/logs）

### 页面定位

日志中心页是管理员统一查看系统运行记录的后台工作台。页面把对外请求日志和系统内部类型化日志放在同一个入口中，方便管理员围绕请求链路、异常、可用性检测、计费、内容防护、后台任务、审计、用户操作、告警和素材流转进行排查、导出和清理。

页面默认进入请求日志中心，适合从一次外部调用出发查看用户、密钥、模型、提供商、结果、Token、耗时和 Trace 信息。切换到其它日志类型后，页面变为对应类型的专用列表和筛选面板。

### 日志类型切换区

页面顶部提供日志类型切换按钮，可切换查看：

- 请求日志。
- 异常事件。
- 可用性检测日志。
- 计费日志。
- 内容防护日志。
- 调度任务日志。
- 后台审计日志。
- 用户操作日志。
- 告警事件。
- 素材日志。

当前选中的日志类型会改变页面标题、筛选项、汇总指标、表格列和数据来源。请求日志使用独立的请求日志工作区；其它类型统一进入类型化日志工作区。

### 请求日志概览区

请求日志页签下方展示请求日志概览指标，包括：

- 当前请求量。
- 成功请求。
- 失败请求。
- 关联密钥数。
- 输入 Token。
- 输出 Token。
- 总 Token。

这些指标用于快速判断当前筛选范围内的调用规模、成功失败情况、密钥覆盖范围和 Token 用量。切换筛选条件或刷新列表后，概览区会随结果一起更新。

### 请求日志筛选区

请求日志筛选区分为基础筛选和高级筛选。

基础筛选用于快速定位常见请求范围，包括日志类型、提供商、模型名、所属用户、密钥和成功状态。日志类型可区分对话请求、响应请求、内容审核请求、文件请求、通用代理日志、密钥鉴权失败日志、不支持端点日志和 CORS 预检日志。

高级筛选用于更细的链路追踪和治理排查，包括开始时间、结束时间、提供商信任等级、密钥标识、手动用户、手动模型、手动密钥、租户、项目、应用、环境、内容检测结果、内容风险等级、内容处置动作、最终策略、检测阶段、命中分类、是否切换提供商、是否适配跳过和会话键。

部分筛选项来自系统已有数据，例如提供商、模型、用户、密钥、租户、项目、应用和环境；手动输入项则用于按关键字或具体标识补充查询。请求日志默认排除可用性检测类记录，避免后台探针噪声影响正式请求排查。

### 请求日志表格区

请求日志表格用于展示当前筛选范围内的请求明细。表格主要列包括：

- 时间：展示请求发生时间。
- 思维等级：展示请求携带或适配后的思维等级。
- 用户 / 密钥：展示请求关联的用户和 API Key 信息。
- 模型 / 提供商：展示请求模型、实际路由提供商和相关路由信息。
- 结果：展示请求成功或失败状态、状态码和错误摘要。
- 用量 / 用时：展示 Token、耗时、首字节时间、吞吐、成本等调用指标。
- 详情：提供查看完整日志详情的入口。

列表中的状态、方法、Token、耗时、成本、内容防护等信息会以紧凑摘要形式呈现，便于管理员快速扫读。详细错误、Trace、请求体、响应体和策略快照不直接铺满表格，而是通过详情弹层查看。

### 请求日志操作区

请求日志工作区提供以下操作：

- 导出请求日志：按当前筛选条件导出请求日志文件。
- 刷新：重新拉取筛选选项、概览和日志列表。
- 删除筛选：删除当前筛选条件命中的请求日志。
- 清空请求日志：清空请求日志主表中的请求记录。
- 调整每页数量：支持按 50、100 或 200 条分页查看。
- 上一页 / 下一页：在当前筛选结果中翻页。

删除筛选会要求当前存在明确筛选范围或时间范围；仅依赖默认排除可用性检测不允许直接删除，避免误删大量日志。清空请求日志属于更强的清理入口，会通过确认提示强调它只处理请求日志，不会级联删除其它类型化日志。

### 类型化日志区

除请求日志外，其它页签进入类型化日志区。类型化日志区会根据当前日志类型动态展示标题、关键词搜索、专属筛选项、汇总指标、保留策略信息和表格列。

异常事件页签用于排查系统异常，展示异常总数、当前页高危、当前页警告和 Trace 数，可按异常级别、错误码、请求路径、链路 ID 和时间范围筛选。

可用性检测日志页签用于查看可用性探针执行情况，展示检查批次、成功探针、失败探针和运行中数量，可按触发方式、检查结果和时间范围筛选，并在表格中展示提供商、模型、结果、探针数量和耗时。

计费日志页签用于查看 Token 回填与计费处理记录，展示计费事件、Token 回填数量、失败 / 待处理数量和余额变化，可按事件类型、Token 回填结果、计费状态、请求日志 ID 和时间范围筛选。

内容防护日志页签用于追踪内容检测事件，展示检测事件、拦截、复核和高风险数量，可按检测阶段、检测结果、风险等级、规则或原因、处置动作、提供商、模型、请求路径、是否流式、链路 ID、请求日志 ID 和时间范围筛选。

调度任务日志页签用于查看后台任务运行情况，展示任务日志、运行中、失败和跳过数量，可按任务状态、任务名称、触发方式、锁状态和时间范围筛选。

后台审计日志页签用于查看管理员操作记录，展示审计日志、高风险、中风险和管理员数，可按操作动作、对象类型、风险等级和时间范围筛选。

用户操作日志页签用于查看普通用户在用户端的操作记录，展示操作日志、失败数量、当前页数量和用户数，可按用户 ID、操作动作、结果和时间范围筛选。

告警事件页签用于查看系统告警，展示告警事件、活跃告警、已解决告警和高危告警，可按告警类型、严重级别、处理状态和时间范围筛选。

素材日志页签用于查看调用测试、用户端上传和外部文件接口进入系统的素材记录，展示素材事件、失败数量、当前页文件大小和哈希数量，可按操作来源、存储范围、处理结果、链路 ID、请求日志 ID 和时间范围筛选。

### 类型化日志筛选与汇总区

类型化日志区统一提供关键词输入框、每页数量选择和动态筛选表单。关键词的含义会跟随日志类型变化，例如异常事件搜索 trace、异常类型、错误码或摘要；告警事件搜索告警键、类型、标题、消息或载荷；素材日志搜索文件名、完整哈希、Trace 或错误摘要。

类型化日志的汇总指标位于列表上方，用于快速识别当前页或当前筛选范围内的风险、失败、处理量和关键对象数量。页面还会展示与日志查询有关的元信息，例如当前时间筛选字段、事件队列状态和日志保留策略，并提供进入保留策略配置的入口。

### 类型化日志表格区

类型化日志表格的列会随日志类型变化。每类日志都保留详情入口，用于查看该条事件的结构化详情和原始数据。

异常事件表格侧重异常类型、请求路径、级别和错误码；可用性检测表格侧重触发方式、提供商、模型、结果、探针和耗时；计费日志表格侧重事件、请求日志、Token 或金额、状态和错误；内容防护表格侧重阶段、上下文、结果、风险、动作和原因；任务日志表格侧重任务、锁、状态和处理结果；审计和用户操作表格侧重操作者、动作、对象、结果、风险和摘要；告警表格侧重告警标题、类型、级别、状态和出现时间；素材日志表格侧重文件、来源、大小和处理结果。

每个类型化日志页签都支持独立分页，管理员可以在当前类型内调整每页数量并前后翻页。

### 日志详情弹层

点击请求日志或类型化日志的详情按钮后，页面会打开日志详情弹层。弹层用于集中查看该条日志的完整上下文。

请求日志详情会展示请求状态、模型与提供商路由、状态码、耗时、首字节时间、吞吐、Trace ID、请求 ID、客户端请求 ID 等摘要信息，并提供更完整的请求体、响应体、路由 Trace、内容防护结果和策略快照等原始数据。

类型化日志详情会根据事件类型展示结构化字段，例如异常上下文、可用性检测批次、计费处理过程、内容防护证据、后台任务运行信息、审计对象、用户操作对象、告警状态或素材元数据。弹层还提供原始 JSON 查看区域，便于管理员在复杂问题排查时核对完整事件载荷。

### 删除与导出保护

日志中心的导出操作会基于当前筛选条件生成结果，便于管理员把请求日志或类型化日志交给后续审计、排障或数据分析使用。

删除操作采用分类型处理。请求日志删除只影响请求日志记录；类型化日志删除只影响当前页签对应的日志类型。删除筛选前，页面会检查是否存在足够明确的筛选条件或时间范围；没有范围时会阻止操作并提示先指定筛选条件。后端也会保留同样的范围校验，避免前端绕过造成误删。

### 用户可完成的操作

在日志中心页中，管理员可以完成以下操作：

- 切换查看请求日志、异常事件、可用性检测日志、计费日志、内容防护日志、调度任务日志、后台审计日志、用户操作日志、告警事件和素材日志。
- 查看请求日志概览指标。
- 按日志类型、提供商、模型、用户、密钥和成功状态筛选请求日志。
- 使用高级筛选按时间、信任等级、租户、项目、应用、环境、内容防护结果、风险等级、处置动作、最终策略、检测阶段、命中分类、切换状态、适配跳过状态和会话键定位请求。
- 查看请求日志表格中的时间、思维等级、用户、密钥、模型、提供商、结果、用量和用时。
- 打开请求日志详情，查看 Trace、请求体、响应体、路由和策略上下文。
- 导出请求日志。
- 刷新请求日志。
- 删除当前筛选命中的请求日志。
- 清空请求日志。
- 在请求日志中调整每页数量并翻页。
- 在类型化日志中输入关键词搜索。
- 按不同日志类型使用专属筛选条件。
- 查看类型化日志的汇总指标、队列状态和保留策略信息。
- 跳转到保留策略配置入口。
- 查看类型化日志详情和原始 JSON。
- 导出当前类型化日志。
- 删除当前筛选命中的类型化日志。
- 在类型化日志中调整每页数量并翻页。

### 动态反馈与空状态

页面加载请求日志时会同步加载筛选选项、汇总指标和表格数据。刷新、导出、删除和清空操作会提供按钮反馈或提示信息；列表刷新成功或失败后会更新最近刷新状态。

请求日志或类型化日志加载中时，表格会显示加载状态；加载失败时会在表格中显示失败提示；当前筛选条件下没有数据时，会显示空状态。类型化日志切换页签后会重新渲染对应筛选项和表格列，避免不同日志类型之间的筛选条件和展示列混淆。

## 15. 告警中心页（/alerts）

### 页面定位

告警中心页是管理员集中查看和处理系统风险的页面。它把提供商可用性异常、API Key 异常、账户额度预警、失败率异常、Redis 和数据库异常、并发容量、队列积压、流量异常、内容风险、计费异常等事件汇总到一个工作台中，帮助管理员快速判断当前系统是否存在需要立即处理的问题。

页面既展示当前告警状态，也提供订阅配置、实时事件刷新、告警确认和跳转到相关排查页面的入口。管理员不需要逐个打开提供商、API Key、用户、日志或运维页面，就可以先在这里看到风险摘要，再跳到对应模块处理。

### 顶部告警总览区

页面顶部展示告警中心标题和简短说明，说明该页用于汇总渠道可用异常、密钥异常状态、账户额度预警和最近 24 小时失败率。

总览区右侧展示最近 24 小时失败率，包括：

- 失败率百分比。
- 失败请求数和总请求数。
- 失败率进度提示。
- 渠道异常数量。
- 密钥异常数量。

这一块用于快速判断系统整体调用质量。如果失败率升高、渠道异常或密钥异常数量增加，管理员可以继续查看下方的事件流和明细表。

### 指标卡片区

顶部总览下方有四个核心指标卡片：

- 渠道异常：展示当前异常提供商数量。
- 密钥异常：展示当前异常 API Key 数量。
- 账户预警：展示存在余额或额度风险的用户数量。
- 24h 失败数：展示最近 24 小时失败请求数量。

这些卡片用于把告警页的主要风险维度压缩成可快速扫读的数字，方便管理员优先判断问题集中在哪一类对象上。

### 实时运行指标区

实时运行指标区展示系统可用性相关的运行信号，包括：

- 就绪状态。
- Redis 状态。
- 数据库状态。
- 全局活跃请求数。
- 流式活跃请求数。
- 后台积压数量。
- 5xx 数量。
- 429 数量。

页面打开后会刷新这些指标。刷新后，Redis、数据库、活跃请求、后台队列、服务端错误和限流等运行状态会在这里更新，帮助管理员判断告警是否来自基础设施、容量、队列积压或外部请求异常。

### 订阅式告警区

订阅配置区用于设置当前管理员的告警订阅偏好。表单包含：

- 启用订阅：控制是否开启告警订阅。
- 订阅渠道异常：控制是否接收提供商相关异常。
- 订阅 API 密钥异常：控制是否接收 API Key 状态异常。
- 订阅账户预警：控制是否接收用户账户余额或额度预警。
- 订阅失败率异常：控制是否接收失败率类告警。
- 启用浏览器提醒：控制是否通过浏览器通知提醒新告警。
- 轮询间隔(秒)：设置页面自动刷新告警事件的间隔，允许在限制范围内填写秒数。
- 保存订阅：提交当前订阅配置。

保存后页面会提示订阅已保存，并重新刷新告警事件。浏览器提醒开启时，页面会在浏览器权限允许后，对新出现的告警发送系统通知。

### 实时告警事件流

实时告警事件流是页面的核心列表，用于展示当前活跃告警。表格列包括：

- 类型：展示告警所属类型，例如渠道异常、API 密钥异常、账户预警、失败率异常、Redis 异常、数据库异常、并发容量、队列积压、流量异常、内容风险或计费异常。
- 级别：展示告警严重程度，例如严重、警告或提示。
- 标题：展示告警名称或主要问题。
- 详情：按告警类型展示关键上下文。
- 最近出现：展示该告警最近一次出现时间。
- 动作：提供确认和相关排查入口。

告警详情会根据类型展示不同字段。Redis 异常会展示状态、延迟和错误；数据库异常会展示状态、连接池和错误；队列积压会展示队列名、待处理数量和失败数量；渠道异常会展示提供商、失败率和请求数；内容风险会展示提供商、高风险命中数量和隔离状态；计费异常会展示失败数、请求日志和错误；流量或失败率异常会展示统计窗口、错误数量和错误率；API Key 异常会展示密钥、状态和归属；账户预警会展示用户、余额和预警内容。

### 告警事件操作

每条告警事件都提供“确认”按钮。管理员点击后，该告警会被标记为已确认，页面随后刷新事件列表。

不同告警类型还会提供不同的排查跳转入口：

- 渠道异常可跳转到提供商管理页。
- API Key 异常可跳转到 API 密钥管理页。
- 账户预警可跳转到用户管理页。
- Redis、数据库、并发容量和队列相关告警可跳转到运维监控页。
- 流量异常可跳转到日志中心。
- 内容风险可跳转到内容防护页。
- 计费异常可跳转到日志中心的相关排查场景。

这些入口用于把告警页作为“发现问题”的起点，把具体修复动作交给对应业务页面完成。

### 渠道异常区

渠道异常区展示当前存在可用异常或熔断风险的提供商。表格列包括：

- 名称：展示提供商名称。
- 整体可用性：展示全部可用、部分可用、全部不可用或未检测等状态。
- 熔断状态：展示闭合、已熔断、半开探测或未知。
- 最近延迟：展示最近一次延迟数据。
- 建议动作：根据熔断和可用状态给出处理方向。
- 操作：提供进入渠道管理的入口。

如果提供商已熔断，建议动作会引导管理员优先下线或切流，再排查上游连通性；其它异常则提示复查可用性检测与模型可用性。没有渠道级异常时，表格显示空状态。

### 账户预警区

账户预警区展示存在余额或额度风险的用户账号。列表项包含：

- 用户名，并可点击进入对应用户详情页。
- 第一条预警信息。
- 建议优先核对余额与日/月配额。

账户预警主要来自账户可用余额耗尽、可用余额低于阈值、或该用户存在异常 API Key 等情况。没有账户预警时，页面显示暂无预警。

### 密钥异常状态区

密钥异常状态区展示非正常状态的 API Key。表格列包括：

- 名称：展示 API Key 名称。
- 归属用户：展示该密钥所属用户。
- 状态：展示密钥当前状态。
- 余额：展示关联账户余额。
- 最近使用：展示最近一次使用时间。
- 建议动作：根据异常类型给出处理建议。
- 操作：提供查看 API Key 详情的入口。

如果密钥因为额度耗尽、成本额度耗尽或余额耗尽异常，页面建议先补充额度或余额，再评估是否恢复；如果密钥已禁用，提示确认是否恢复启用；如果密钥没有绑定提供商，提示补齐授权提供商；其它异常则提示检查状态与最近日志。没有密钥级异常时，表格显示空状态。

### 自动刷新与浏览器提醒

告警中心会按订阅中的轮询间隔自动刷新实时事件流和系统运行指标。点击“立即刷新”可以手动触发刷新，刷新完成后会显示提示。

如果启用了订阅和浏览器提醒，且浏览器通知权限已允许，页面会对新出现且尚未提醒过的告警发送浏览器通知。页面会记住已经提醒过的告警键，避免同一条活跃告警在当前页面会话中重复提醒。

### 用户可完成的操作

在告警中心页中，管理员可以完成以下操作：

- 查看最近 24 小时失败率、失败请求数和总请求数。
- 查看渠道异常、密钥异常、账户预警和 24h 失败数四类核心指标。
- 查看系统就绪状态、Redis 状态、数据库状态、活跃请求、流式请求、后台积压、5xx 和 429 指标。
- 开启或关闭告警订阅。
- 选择是否订阅渠道异常、API 密钥异常、账户预警和失败率异常。
- 开启或关闭浏览器提醒。
- 设置告警轮询间隔。
- 保存订阅配置。
- 手动刷新实时告警事件流。
- 查看活跃告警的类型、级别、标题、详情和最近出现时间。
- 确认单条告警。
- 从告警事件跳转到提供商管理、API 密钥管理、用户管理、运维监控、日志中心或内容防护页面。
- 查看异常提供商列表和建议动作。
- 进入渠道管理处理提供商异常。
- 查看账户预警用户，并进入用户详情页核对余额和配额。
- 查看异常 API Key 列表，并进入 API Key 详情页处理密钥状态。

### 动态反馈与空状态

页面初始打开时，部分运行指标会先显示加载状态，随后通过刷新更新为实际状态。手动刷新时，刷新按钮会进入加载反馈，成功后提示告警事件已刷新；保存订阅成功后提示告警订阅已保存；确认告警成功后提示告警已确认。

如果当前没有活跃告警事件，事件流表格显示当前没有活跃告警事件；如果没有渠道级异常，渠道异常表格显示当前没有渠道级异常；如果没有账户预警，账户预警区显示暂无预警；如果没有密钥级异常，密钥异常表格显示当前没有密钥级异常。刷新或保存失败时，页面会显示对应错误提示。

## 16. 会话回放页（/conversations）

### 页面定位

会话回放页是管理员按 `conversation_key` 查看代理请求上下文的排障页面。它把同一个会话键下的多次请求聚合起来，让管理员可以回看用户输入、模型输出、命中提供商线路、模型名称、Token 消耗、请求成败和流式或图像请求特征。

该页面适合用于排查多轮对话、Responses 适配会话、同会话路由粘性、模型输出异常、上下文丢失、Token 消耗异常以及某个会话相关请求日志。管理员可以先从会话列表找到目标会话，再在右侧查看完整回放，并跳转到日志中心查看同一会话键下的原始请求日志。

### 顶部概览区

页面顶部展示“会话回放”标题和说明，说明该页会按 `conversation_key` 聚合代理请求，并回看用户输入、模型输出、线路命中与 Token 消耗。

右侧结果集摘要展示：

- 当前结果集中的会话数量。
- 当前检索状态，未输入关键词时显示当前展示全部会话，输入关键词后显示当前检索内容。
- 最近更新时间。
- 当前结果条数。

这一块用于告诉管理员当前列表是否处于搜索状态，以及当前结果集最近一次更新到什么时间。

### 指标卡片区

顶部下方展示四个会话指标卡片：

- 会话数：当前结果集中的会话数量。
- 命中请求量：当前结果集中所有会话累计包含的请求次数。
- 总 Token：当前结果集中所有会话累计消耗的 Token。
- 最近线路：当前选中会话最近一次命中的提供商线路。

这些指标会随着搜索结果和选中会话变化而更新。搜索后，前三个指标反映搜索结果范围；选择某个会话后，最近线路会显示该会话最近命中的提供商名称。

### 会话列表区

会话列表区用于展示可回放的会话集合。列表顶部有：

- 搜索框：可搜索 `conversation_key`、模型、提供商或回复内容。
- 刷新按钮：重新加载会话列表，并尽量保持当前选中的会话。

会话列表中的每个会话项会展示：

- 会话键 `conversation_key`。
- 该会话包含的请求次数。
- 最近一次请求使用的模型。
- 该会话累计 Token。
- 最近回复或错误消息的预览文本。
- 最近更新时间。
- 最近命中的提供商名称。

点击某个会话项后，该会话会变为当前选中项，右侧详情区会加载对应回放内容。列表没有会话时，会显示暂无会话记录。

### 搜索与刷新

管理员可以在搜索框输入关键词来筛选会话。搜索范围包含会话键、请求模型、提供商名称、响应文本和日志消息，因此既可以按明确的会话 ID 找到目标，也可以按模型名、上游提供商或返回内容片段定位会话。

页面会根据搜索内容重新加载列表，并更新顶部结果集摘要和指标卡片。刷新按钮用于重新拉取当前搜索条件下的会话列表；刷新成功后按钮会显示已刷新反馈，刷新失败会显示错误提示。

### 回放详情区

回放详情区默认提示选择左侧会话。选中会话后，详情区标题会显示当前 `conversation_key`，并展示该会话的摘要、时间信息、日志入口和逐轮对话内容。

详情区顶部提供“查看相关日志”按钮。该按钮会跳转到日志中心，并自动携带当前 `conversation_key` 作为筛选条件，方便管理员继续查看原始请求日志、Trace、请求体、响应体、计费和内容防护信息。

详情区还展示当前会话的最近更新时间，用于判断回放内容是否为最新请求。

### 会话摘要区

选中会话后，详情区会展示会话摘要卡片，包括：

- 请求数：当前会话包含的请求日志数量。
- 成功 / 失败：当前会话中成功请求和失败请求的数量。
- 总 Token：当前会话累计 Token。
- 最近模型：当前会话最近一次请求使用的模型。

摘要下方还有会话元信息，包括：

- 首条时间：该会话第一条请求发生时间。
- 最近更新时间：该会话最后一条请求发生时间。
- 最近命中线路：该会话最近一次命中的提供商名称。

这些信息用于快速判断会话规模、是否有失败、最近路由到哪里、是否存在 Token 异常或模型切换问题。

### 对话时间线

对话时间线按请求发生顺序展示该会话中的输入和输出。每一条时间线记录包含：

- 角色：例如用户输入角色、系统或开发者消息、assistant 输出等。
- 提供商或请求模型：优先展示命中的提供商名称，缺失时展示请求模型。
- 创建时间。
- 该条请求关联的 Token。
- 请求形态：流式请求显示 stream，非流式显示 json。
- 内容形态：包含图像输入时显示 vision，否则显示 text。
- 消息正文：展示抽取后的请求消息或模型输出。

页面会从请求体中提取 `messages`、`input` 或 `prompt` 等会话输入，并把模型返回文本作为 assistant 输出加入时间线。多轮请求中已经出现过的历史消息会被去重，只把新增输入和新输出作为回放轮次展示，减少重复上下文造成的阅读负担。

如果一条消息包含图像内容，正文中会以图像附件提示形式体现；如果单条消息过长，回放内容会被截断，避免过长文本把页面撑得不可读。当前会话没有可回放内容时，时间线显示该会话暂无可回放内容。

### 日志联动

会话回放页与日志中心联动。管理员可以从日志中心的请求日志详情或日志列表中的回放入口跳转到本页，并通过 URL 中的 `conversation_key` 自动打开对应会话。

管理员也可以在本页点击“查看相关日志”返回日志中心，日志中心会按当前会话键筛选请求日志。这样可以在“可读对话回放”和“原始请求日志”之间来回切换：会话回放用于理解多轮上下文，日志中心用于查看更细的 Trace、状态码、错误、请求体、响应体、计费与安全策略。

### 数据范围与限制

管理端会话回放面向管理员展示全站会话，而不是只展示某个普通用户自己的会话。会话列表按更新时间倒序展示当前结果页，页面一次加载最多 100 条会话摘要。

单个会话详情最多回放一定数量的请求日志。超过上限时，回放会以最近可加载范围为准，避免一次性渲染过长会话影响页面可用性。每条回放正文也有长度保护，过长内容会被截断显示。

### 用户可完成的操作

在会话回放页中，管理员可以完成以下操作：

- 查看当前结果集会话数量、结果条数和最近更新时间。
- 查看当前结果集累计请求量和累计 Token。
- 查看当前选中会话最近命中的提供商线路。
- 按 `conversation_key` 搜索会话。
- 按模型名搜索会话。
- 按提供商名称搜索会话。
- 按回复内容或日志消息搜索会话。
- 刷新会话列表。
- 点击会话列表中的任意会话查看详情。
- 查看会话的请求数、成功 / 失败数量、总 Token 和最近模型。
- 查看会话首条时间、最近更新时间和最近命中线路。
- 按时间线回看用户输入、系统或开发者消息、assistant 输出。
- 查看每轮请求的时间、Token、流式或非流式形态、文本或图像形态。
- 从当前会话跳转到日志中心查看相关请求日志。
- 通过带 `conversation_key` 的链接直接打开指定会话。

### 动态反馈与空状态

页面加载会话列表时，刷新按钮会显示加载反馈。加载成功后，顶部结果集摘要、指标卡片、会话列表和默认选中会话会同步更新；加载失败时会显示错误提示。

没有任何会话或当前搜索无结果时，会话列表显示暂无会话记录。尚未选择会话时，详情区提示选择一个会话后可查看完整回放、命中线路与 Token 使用。选中的会话不存在或加载失败时，页面会显示错误提示并保留当前可见状态。

## 17. 审计日志页（/audit-logs）

### 页面定位

审计日志页是管理员查看后台关键操作记录的独立页面。它集中追踪账号治理、API Key 维护、提供商配置、模型挂载、系统设置、压测任务、登录安全和其它后台管理动作，帮助管理员还原“谁在什么时间对什么对象做了什么变更”。

该页面偏向合规追溯和变更复核。管理员可以通过关键词、动作和对象类型筛选审计记录，查看每条记录的操作者、优先级、动作、对象、摘要、建议和详细变更内容。

### 顶部概览区

页面顶部展示“操作审计日志”标题和说明，说明该页用于集中追踪后台关键变更。

顶部右侧展示当前筛选命中的审计记录数量，并展示当前可筛选的动作数量和对象类型数量。这个区域用于快速确认当前筛选范围下共有多少审计事件，以及审计数据中有哪些动作和对象类型可用于下拉筛选。

### 筛选条件区

筛选条件区提供一个查询表单，包含：

- 关键词：可按操作者、对象、摘要或实体 ID 搜索审计记录。
- 动作：按具体操作动作筛选，例如创建、更新、删除、禁用、调账、确认告警等历史动作。
- 对象类型：按被操作对象类型筛选，例如 API Key、提供商、模型挂载、用户、认证、压测任务、设置等。
- 每页数量：可选择每页展示 10、20、50 或 100 条。
- 查询按钮：提交筛选条件并刷新审计列表。

筛选结果通过页面地址参数保留，因此翻页时会继续带上当前关键词、动作、对象类型和每页数量。

### 审计明细表格

审计明细表格是页面核心区域，用于按时间倒序展示审计记录。表格列包括：

- 时间：展示审计事件创建时间。
- 操作者：展示管理员用户名和管理员用户 ID。
- 优先级：根据动作类型给出高、中、低的扫读优先级。
- 动作：展示具体操作动作。
- 对象：展示对象类型、对象名称和对象 ID。
- 摘要：展示该次操作的简短说明。
- 建议：根据动作类型给出复核建议。
- 详情：提供展开查看完整审计详情的入口。

列表用于让管理员在不展开详情的情况下先判断操作风险和影响对象。如果没有符合条件的审计记录，表格显示暂无符合条件的审计记录。

### 优先级与建议

页面会根据动作文本给审计记录生成可扫读的优先级：

- 删除、禁用、配额和余额相关动作优先级为高。
- 创建、更新、启用相关动作优先级为中。
- 其它动作优先级为低。

建议列会根据动作类型给出处理方向：

- 删除类动作提示复核是否误删，并确认是否需要恢复。
- 禁用类动作提示确认禁用原因和影响范围。
- 余额或配额类动作提示核对调账或配额口径，避免治理误伤。
- 创建或更新类动作提示检查新配置是否已同步到相关对象。
- 其它动作提示保留追踪即可。

这部分不替代人工判断，但能帮助管理员优先处理高风险变更。

### 审计详情展开区

每条审计记录都可以通过“查看”展开详情。展开后首先显示该记录的对象重点信息和风险等级。

不同对象类型会展示不同的重点提示：

- API Key：关注权限、额度和模型白名单。
- 提供商：关注 Base URL、协议、容量和凭据。
- 提供商模型挂载：关注协议、能力和可用性检测。
- 用户或认证：关注角色、状态和登录安全。
- 压测任务：关注压测启动、停止和报告访问。
- 其它对象：关注配置变更。

详情区还会展示风险等级，缺省时按低风险显示。

### 字段变化与变更前后

如果审计记录包含字段变化，详情区会显示“字段变化”数据，便于管理员快速看到哪些字段被修改。

如果审计记录包含变更前或变更后的数据，详情区会分别展示：

- 变更前。
- 变更后。

这两个区域用于对比配置、权限、额度、规则或其它对象属性在操作前后的差异。对于排查误操作、确认调账口径、验证配置同步是否正确很有价值。

### 安全详情区

如果审计记录包含额外安全详情，详情区会显示“安全详情”。该区域可能包含请求上下文、脱敏后的敏感字段、操作参数、筛选条件、任务信息或其它后台记录的结构化内容。

页面会把结构化详情格式化为可读文本；如果原始内容不是结构化数据，则按原内容展示。过大的审计详情在写入时会被截断，避免无限制保存或展示超大内容。

### 分页区

页面底部展示分页信息，包括当前页、总页数和总记录数。

当存在上一页时，页面显示“上一页”入口；当存在下一页时，显示“下一页”入口。翻页链接会保留当前筛选条件、对象类型、动作和每页数量，保证管理员在同一筛选范围内连续查看审计记录。

### 与日志中心的关系

该页面是独立的操作审计日志页，主要面向后台关键变更追溯。日志中心也提供“后台审计日志”类型化页签，用于在统一日志中心中查询、导出或删除类型化审计日志。

独立审计日志页更适合快速查看后台管理操作的时间线、建议和变更详情；日志中心的后台审计页签更适合和其它日志类型一起做统一筛选、导出和集中日志治理。

### 用户可完成的操作

在审计日志页中，管理员可以完成以下操作：

- 查看当前筛选命中的审计事件总数。
- 查看当前可筛选动作数量和对象类型数量。
- 按关键词搜索操作者、对象、摘要或实体 ID。
- 按动作筛选审计记录。
- 按对象类型筛选审计记录。
- 设置每页数量。
- 提交查询并刷新列表。
- 查看审计记录的时间、操作者、动作、对象和摘要。
- 根据优先级识别高风险、中风险和低风险操作。
- 查看每条记录的复核建议。
- 展开审计详情。
- 查看不同对象类型对应的关注重点。
- 查看字段变化。
- 查看变更前数据。
- 查看变更后数据。
- 查看安全详情。
- 在当前筛选条件下翻到上一页或下一页。

### 动态反馈与空状态

审计日志页主要通过表单提交和分页链接刷新页面。筛选后，顶部记录数、表格内容和分页信息会同步变更。 

没有符合条件的记录时，表格显示暂无符合条件的审计记录。没有上一页或下一页时，对应翻页入口不会显示。详情默认收起，只有管理员主动展开“查看”后才显示字段变化、变更前后和安全详情。

## 18. 用户中心页（/user）

### 页面定位

用户中心页是普通用户登录后的主入口，重点承接“先拿到 API Key、再复制接入信息、随后做自检、最后正式接入”的完整路径。页面把账户概况、实时用量、密钥准备、接入示例、风险提醒和趋势变化放在同一屏可连续浏览的位置，方便用户快速完成第一次接入和后续排障。

### 顶部接入主路径区

页面顶部用一个主视觉区提示当前所处位置，并强调这是一条面向正式接入的工作路径。这里提供两个最核心的入口：前往 API Key 管理页和直接进入接入自检页。右侧同步显示当前账号的基础信息，包括用户名、邮箱、API Key 数量和会话数量，帮助用户确认自己当前登录的是哪个账号。

### 页面锚点导航

顶部下方有一组页内锚点，用来快速跳转到实时监控、密钥准备、接入信息复制和自检区域。用户在长页面中可以直接定位到自己最关心的模块，不需要逐段向下翻找。

### 账户与用量指标区

页面首先展示一排核心指标，用于概览当前账户状态。这里会看到我的 API Key 数、可用余额、今日请求数、本月请求数、总 Token 和累计费用。用户可以借助这些指标快速判断账户是否还能继续调用、近期使用是否升高，以及费用是否接近预期。

### 实时监控区

实时监控区展示当前账号自己的请求与模型使用情况，支持自动刷新和手动刷新。左侧会展示请求趋势，告诉用户最近一段时间请求量的变化；中间展示费用与 Token 的变化，帮助用户同时观察消耗和成本；右侧展示常用模型排行，让用户知道最近最常使用的是哪些模型。

这一块适合排查“为什么今天突然变贵”“哪个模型最常被调用”“是否出现请求波动”等问题。若刷新后数据发生变化，用户可以直接看到最新状态。

### 账户预警区

当账户出现余额不足、可用余额接近耗尽、API Key 异常等情况时，页面会显示预警提醒。提醒内容会告诉用户当前问题属于哪一类，并提示尽快处理。没有风险时，这一块不会出现。

### 我的密钥与接入准备区

这一部分列出当前账号已经拥有的 API Key，并按卡片方式显示名称、脱敏密钥、状态和最近使用时间。用户可以快速确认哪些密钥可用、哪些已禁用、哪些已过期、哪些余额不足或尚未绑定渠道。

如果当前账号还没有可用密钥，页面会直接告诉用户暂时不能进入接入示例，需要先去独立密钥页创建密钥后再回来继续。

### 复制接入信息区

这一块专门提供可以直接拿去使用的接入材料。用户先选择一个可用 API Key，再选择一个示例模型，页面就会根据当前选择生成对应的接入信息。

这里展示的内容包括基础地址、OpenAI 兼容入口、Gemini 原生入口、Claude 原生入口、授权头信息、授权渠道说明，以及可直接复制的请求示例。每个条目旁边都提供复制按钮，方便用户快速把接入地址或请求片段带到自己的代码里。

### 动态接入示例区

接入示例会随着用户选中的 API Key 和模型变化而自动更新。用户可以直接看到适用于当前密钥的 Bearer 头、可用提供商名称、curl 示例和 Python SDK 示例。这样一来，页面不仅告诉用户“接入地址是什么”，还直接给出“应该怎么写请求”。

如果页面上没有任何可回显明文的 API Key，相关示例会退化为提示状态，复制操作也会失去意义，避免用户误拿到无效内容。

### 接入自检与趋势区

页面底部把“先做自检”和“最近 7 天使用趋势”并排展示。左侧是进入接入自检的入口，右侧是近 7 天的请求、Token 和费用趋势图，便于用户判断自己的接入是否稳定，以及近几天的使用节奏是否正常。

### 用户可完成的操作

在用户中心页，普通用户可以完成以下操作：

- 查看当前账号的基础信息。
- 跳转到 API Key 管理页领取或管理密钥。
- 跳转到接入自检页确认接入是否可用。
- 查看自己的 API Key、余额、请求量、Token 和累计费用。
- 观察最近请求趋势、费用趋势和常用模型变化。
- 查看账户预警并判断是否需要补充余额或处理异常密钥。
- 选择可用 API Key 和模型，生成对应的接入示例。
- 一键复制基础地址、各协议入口、授权头和请求示例。
- 查看当前账号支持的授权渠道。
- 从页面底部进入自检流程，验证实际调用是否成功。

### 动态反馈与空状态

页面中的实时监控会自动刷新，也支持手动刷新；刷新后，指标、趋势图和排行都会跟着更新。密钥列表、接入示例和授权渠道都会根据当前账号的真实数据动态变化。

如果账号没有 API Key，页面会明确提示用户先去创建密钥；如果没有可展示的接入材料，相关示例区域会显示为空状态，避免用户误以为已经准备好了接入信息。所有复制按钮都依赖当前选中的密钥和模型，选项变化后内容会同步重算。

## 19. 账号安全页（/user/profile）

### 页面定位

账号安全页是普通用户维护个人登录资料和登录密码的页面。它只处理用户账号本身的资料、安全状态和密码修改，不承担 API Key 创建、轮换或授权配置，这些能力通过页面中的入口引导到独立的 API Key 管理页。

### 顶部账号信息区

页面顶部展示“账号安全”和“个人资料与安全”的标题说明，告诉用户当前页的职责边界。右侧展示当前账号摘要，包括用户名、邮箱、账号创建时间和最近登录时间。用户进入页面后，可以先确认自己正在修改的是哪个账号。

### 操作结果提示区

当用户提交资料或密码表单后，页面会根据处理结果在顶部显示反馈。个人资料保存成功时显示“个人资料已更新”；密码修改成功时显示“密码已更新”。

如果操作失败，页面会展示对应错误，例如当前密码错误、两次输入的新密码不一致、用户名太短、密码长度不足，或用户名、邮箱已被其它账号占用。用户可以根据提示回到对应表单修正输入。

### 页面锚点导航

页面提供三个页内导航入口：账号概览、基础资料、密码与安全。用户可以直接跳到需要处理的区域，尤其适合在页面内容较长或移动端浏览时快速定位。

### 账号概览指标区

账号概览区用一组指标卡展示当前用户的关键状态，包括我的 API 密钥数量、总请求量、总 Token、可用余额、累计费用和会话数。这些信息帮助用户在修改资料或密码前顺手确认账号是否仍在正常使用，以及账户消耗是否符合预期。

### 基础资料表单区

基础资料区提供用户名和邮箱两个输入项，输入框会回显当前账号已有信息。用户可以直接修改用户名或邮箱，然后点击“保存资料”提交。

提交后，系统会校验用户名和邮箱是否可用。若用户名过短、邮箱格式不符合要求，或用户名、邮箱已经被其它账号占用，页面会返回错误提示；保存成功后，页面会显示资料已更新。

### 当前状态侧栏

基础资料旁边展示当前账号状态摘要，包括账号角色、当前余额、累计费用、今日请求、本月请求，以及密钥管理入口。账号角色在这里显示为普通用户；密钥管理项提供“前往独立页面”链接，用户可以跳转到用户端 API Key 管理页处理密钥相关事项。

这一块主要用于让用户在修改资料时同时看到账号使用状态，避免把账号安全与调用额度完全割裂开。

### 密码管理区

密码与安全区域提供修改登录密码的表单。表单包含当前密码、新密码和确认新密码三个输入项，用户填写后点击“更新密码”提交。

提交时会先验证当前密码是否正确，再验证两次新密码是否一致，并检查新密码是否满足最小长度要求。验证失败会返回明确提示；验证通过后，密码更新完成，页面显示密码已更新。

### 用户可完成的操作

在账号安全页，普通用户可以完成以下操作：

- 查看当前登录账号的用户名、邮箱、创建时间和最近登录时间。
- 查看账号相关的 API Key 数量、请求量、Token、余额、累计费用和会话数。
- 通过页内锚点跳转到账号概览、基础资料或密码与安全区域。
- 修改用户名。
- 修改邮箱。
- 保存个人资料。
- 查看资料保存成功或失败的反馈。
- 查看账号角色、当前余额、累计费用、今日请求和本月请求。
- 跳转到独立 API Key 管理页。
- 输入当前密码、新密码和确认新密码。
- 提交登录密码修改。
- 查看密码修改成功或失败的反馈。

### 动态反馈与空状态

该页面以表单提交后的页面反馈为主。资料保存或密码更新后，页面会刷新并在顶部展示成功提示；校验失败时，顶部展示对应错误信息。账号概览和当前状态区域依赖当前账号真实数据，若某些统计暂时为零，会直接显示零值而不是隐藏模块。

页面没有独立弹窗或复杂异步刷新行为。API Key 相关操作不会在本页展开，而是通过“前往独立页面”跳转到专门的密钥管理页，避免用户在账号资料页误操作密钥。

## 20. API Key 管理页（/user/api-keys）

### 页面定位

API Key 管理页是普通用户直接管理接入凭据的主页面。它把“找出哪把密钥能用、复制什么内容、怎样修改授权和状态、何时轮换或删除”放在同一页里处理，是用户端最核心的接入控制台之一。

### 顶部概览区

页面顶部先用主视觉区说明当前页的职责：把密钥当成接入对象来管理。右侧会展示当前接入面概况，包括密钥总数、正常可用数量、是否存在可复制明文的密钥，以及 24 小时请求量和累计 Token。用户可以一眼看出当前账号的接入规模和可用程度。

顶部还提供三个快速入口：进入接入自检、打开账号安全页、复制 `/v1` 入口。用户通常会先在这里完成“去自检”或“先回到安全页”的跳转。

### 操作结果提示区

当用户新建、更新、启停、轮换或删除密钥后，页面会显示明确反馈。成功时会提示 API Key 已创建、已更新、状态已更新、已删除或已轮换；轮换成功时还会提醒用户尽快同步新的明文密钥。

如果某个操作失败，例如没找到当前账号下的 API Key，页面会直接提示错误，让用户知道问题出在对象不存在或权限不匹配。

### 汇总指标区

页面中部先给出一组汇总卡片，帮助用户快速判断当前密钥池的可用程度。这里包括：

- 正常可用的密钥数量。
- 需要关注的密钥数量，例如过期、配额不足、余额不足或未绑定提供商。
- 可复制明文的密钥数量。
- 共享可用余额和冻结金额。

这些指标让用户在进入列表前先知道自己的密钥池是否需要处理。

### 密钥目录区

密钥目录是页面主体，负责展示和管理每一把 API Key。页面支持按名称、备注、掩码和路由主策略搜索，也支持按状态筛选，包括全部、正常、需关注、已禁用和需轮换。

每个密钥对象会显示名称、备注、当前状态、是否还能直接复制明文，以及当前掩码。用户可以通过目录快速判断这把密钥是否能直接用于接入，还是需要先轮换或调整配置。

### 单个密钥卡片区

每个密钥卡片都展示了完整的操作面板。卡片顶部会显示密钥名称、状态徽标，以及“可复制明文”或“需轮换后复制”的提示。卡片中间还会显示当前掩码、路由主策略、授权提供商数量、累计 Token、共享余额和内容检测状态。

用户能直接在卡片内完成这些操作：

- 复制密钥明文。
- 复制 `Authorization: Bearer ...` 请求头。
- 复制 `/v1` 入口。
- 打开该密钥的控制台详情页。
- 启用或停用该密钥。
- 一键轮换该密钥。
- 展开高级设置。

卡片底部还会给出一段状态提示，告诉用户当前密钥是可以直接进入详情页完成复制和自检，还是处于停用、风险或需调整状态。

### 编辑与高级设置区

每个密钥卡片都带有一个可展开的编辑区。展开后，用户可以修改名称、替换密钥、备注和启用状态。

替换密钥输入支持留空保留当前密钥，也支持填写新密钥；页面会实时校验格式，并在输入下方给出反馈。启用状态通过勾选方式控制，用户可以明确决定是否让该密钥继续可用。

编辑区里还会显示两条不可由普通用户关闭的说明：

- 内容检测由管理员统一要求，普通用户不能关闭。
- 提供商授权由管理员统一维护，普通用户默认继承可用提供商集合。

### 删除密钥区

在编辑区底部，页面还提供删除密钥入口。删除前会给出明确风险提示，说明删除后当前接入凭据立即不可恢复，适合只在确认废弃后执行。

### 新建密钥区

页面右侧是新建密钥面板，用于创建新的接入对象。用户可以填写名称、自定义密钥和备注，也可以直接留空让系统自动生成密钥。

新建区支持“创建后立即启用”的开关。默认情况下，新建密钥会进入启用状态，便于马上用于接入。

这里也会提示两条策略说明：

- 新建密钥默认进入内容完整性治理链路，普通用户侧不能关闭。
- 新建密钥默认授权全部可用提供商，并会随管理员侧调整自动同步。

### 搜索与筛选交互

密钥目录支持即时搜索和状态筛选。用户输入关键词后，列表会按名称、备注、掩码或主策略过滤；点击不同状态筛选按钮后，列表会只保留符合条件的卡片。页面同时显示当前可见的密钥数量，筛选后若没有结果，会展示空状态提示。

### 用户可完成的操作

在 API Key 管理页，普通用户可以完成以下操作：

- 查看所有密钥的总览状态。
- 判断哪些密钥正常、哪些需要关注、哪些需要轮换。
- 搜索指定密钥。
- 按状态筛选密钥。
- 复制密钥明文。
- 复制授权请求头。
- 复制 `/v1` 入口。
- 打开单个密钥的控制台详情页。
- 启用或停用某把密钥。
- 一键轮换某把密钥。
- 展开并修改密钥名称、密钥值、备注和启用状态。
- 删除不再使用的密钥。
- 创建新的 API Key。
- 直接选择是否创建后启用。
- 进入接入自检页验证该密钥是否可用。
- 跳转回账号安全页。

### 动态反馈与空状态

页面会根据搜索词和状态筛选实时更新可见密钥数量；如果没有匹配结果，会显示“没有匹配当前筛选条件的 API Key”。如果当前账号根本没有密钥，右侧新建区会成为主要入口，提示用户先创建第一把密钥再回来统一管理。

对于没有明文的历史密钥，页面会明确告诉用户需要轮换后才可以复制。对于被停用、过期、余额不足或未绑定提供商的密钥，页面会用状态徽标和提示语告诉用户为什么这把密钥不能直接拿来接入。

## 21. 可用模型页（/user/models）

### 页面定位

可用模型页是普通用户查看“当前账号到底能调用哪些模型”的只读目录页。它不提供模型创建、编辑或调试能力，而是集中展示模型名称、价格、缓存费用、速度、可命中渠道和说明，帮助用户在正式接入前做模型选型和成本预估。

### 顶部说明区

页面顶部标题为“模型选择与价格理解”，说明这个页面主要回答三个问题：该选哪个模型、价格怎么看、当前账号能命中哪些渠道。页面也明确说明图片上传工具已经从模型页剥离，不会在这里混入调试功能。

### 页面锚点导航

页面提供一个“模型目录”锚点入口，用户可以直接跳到模型列表区域。由于该页主体内容集中在目录表格中，锚点主要用于快速定位列表。

### 模型统计区

模型目录上方展示四个统计指标：

- 可用模型数：当前账号实际可调用的模型数量。
- 有价格模型：已经配置输入价格的模型数量。
- 可命中渠道总数：所有可用模型可命中的提供商渠道数量合计。
- 带备注模型：带有说明备注的模型数量。

这些指标帮助用户先判断当前账号的模型覆盖是否充分、价格信息是否完整，以及可用渠道数量是否足够。

### 模型目录表格

模型目录是页面核心区域，以表格方式列出当前账号可以使用的模型。表格列包括模型、输入价格、输出价格、缓存价格、缓存写入价格、速度、可用渠道和说明。

每一行代表一个可用模型。模型列会优先展示模型显示名，同时在下方展示真实模型标识，便于用户区分展示名称和接入时需要使用的模型名。

### 价格展示区

价格相关列分别展示输入价格、输出价格、缓存读取价格和缓存写入价格。页面以每百万 token 的口径展示价格，并带有对应币种。若模型存在原始定价币种，并且原币种与结算展示币种不同，页面会在对应价格下方补充“原币种”价格，方便用户理解实际价格来源。

缓存价格分为缓存读和缓存写两个维度。缓存读价格用于理解命中缓存时的费用，缓存写入价格用于理解首次写入缓存内容时可能产生的费用。

### 速度与渠道区

速度列展示模型的速度标签，没有配置时显示为空值。可用渠道列展示该模型当前可命中的渠道数量，并在下方列出可用提供商名称。用户可以通过这一列判断某个模型是只有单一渠道可用，还是有多个渠道参与路由。

页面只展示当前账号确实可命中的模型和渠道。模型必须处于启用状态，且至少存在当前用户 API Key 范围内可路由的提供商挂载，才会出现在列表中。

### 说明列

说明列展示模型备注，用于补充模型适用场景、限制、注意事项或管理员维护的其它说明。没有备注时显示为空值占位。

### 用户可完成的操作

在可用模型页，普通用户可以完成以下操作：

- 查看当前账号可调用的模型总数。
- 查看哪些模型已经配置价格。
- 查看所有可用模型可命中的渠道总数。
- 查看哪些模型带有备注说明。
- 查看每个模型的显示名和真实模型名。
- 查看输入、输出、缓存读和缓存写价格。
- 查看原币种价格提示。
- 查看模型速度标签。
- 查看每个模型可命中的提供商数量和提供商名称。
- 根据价格、渠道覆盖和备注判断接入时应该选择哪个模型。

### 动态反馈与空状态

该页面没有复杂的前端交互，也不提供表单提交。数据来自当前账号的可路由模型集合，页面打开时直接渲染。

如果当前账号下没有任何可用模型，表格会显示空状态，提示当前账号还没有可用模型，需要管理员先为用户的 API Key 绑定可用渠道。该空状态也意味着用户当前即使知道模型名，也可能无法通过自己的密钥成功路由到模型。

## 22. 使用文档页（/user/docs）

### 页面定位

使用文档页是普通用户和外部接入方的正式接入说明页。它只保留接入地址、鉴权方式、标准路径、最短自检、客户端配置和错误格式这些必须知道的内容，不再混入后台功能说明或帮助中心杂项。

### 顶部接入摘要区

页面顶部用“开发者接入 / 使用文档”说明这是一页面向正式接入的文档页。右侧会直接展示统一接入地址、鉴权方式和路由策略，帮助用户先建立最核心的接入认知。

顶部还有两个主要按钮：

- 复制接入信息。
- 查看最短示例。

这些按钮会把用户带到页面内对应区域，方便直接跳到真正需要复制或查看示例的位置。

### 统一接入地址区

右侧摘要和页面中部的“接入信息”区域会展示可直接复制的对外地址。这里同时说明了 OpenAI 兼容入口、Gemini 原生入口和 Claude 原生入口分别应该如何使用，避免用户把后台页面地址或内部调试地址误当成正式接入地址。

页面还会明确提示：OpenAI 模型名以 `/v1/models` 返回结果为准，Gemini 和 Claude 则按各自原生入口调用。

### 页面锚点导航

页面提供四个锚点入口：接入信息、最短示例、客户端配置和错误格式。用户可以直接跳到对应说明块，不必整页翻找。

### 接入信息复制区

接入信息区是页面最常用的内容之一，帮助用户一次性拿到正式接入所需的基础材料。这里会展示并允许复制以下内容：

- 平台地址。
- OpenAI Base URL。
- 鉴权 Header。
- 环境变量。

每个条目旁边都有复制按钮，适合直接贴到客户端配置、脚本、环境变量文件或文档里。

### 标准路径说明区

页面右侧的常用入口区域列出了标准路径，告诉用户不同协议应该访问哪些接口：

- `GET /v1/models`
- `POST /v1/responses`
- `POST /v1/chat/completions`
- `POST /v1beta/models/{model}:generateContent`
- `POST /v1/messages`

这一区域的作用是让用户在不打开其它页面的情况下，直接确认自己要走哪条协议路径。

### 最短示例区

最短示例区提供四类最常见的接入示例：

- 获取模型列表，用于自检 API Key、地址和模型可见性。
- Responses 文本请求，作为 OpenAI 兼容新接入的优先示例。
- Chat Completions 请求，供仍依赖旧 Chat 协议的客户端使用。
- Gemini 原生请求。
- Claude 原生请求。

每个示例都以代码块形式展示，并配有一键复制按钮。用户可以直接把示例拿去测试，不需要自己重新拼接路径和头信息。

### 客户端配置区

客户端配置区告诉用户不同客户端应该如何填写参数。这里给出三类信息：

- OpenAI Compatible 的 Base URL、API Key 和模型名。
- 开发环境里常用的环境变量写法。
- 关于模型能力和路由策略的注意事项。

页面同时提醒：外部请求统一由平台按可用性优先策略选择可用提供商，业务侧不要传内部提供商名称、后台调试参数或非标准路由字段。

### 错误格式区

错误格式区说明了请求失败时应该看到什么。这里展示两类错误信息：

- 普通 JSON 错误响应，包含 message、type、code、trace_id、retryable、recoverable 和 category。
- 流式 SSE 错误事件，说明连接建立后如果出错，会尽量发出 `event: error` 并以 `data: [DONE]` 结束。

这一区域的目的是让接入方在排障时知道该保留哪些字段，方便定位鉴权、余额、模型、容量或上游问题。

### 用户可完成的操作

在使用文档页，用户可以完成以下操作：

- 复制统一接入地址。
- 复制 OpenAI Base URL。
- 复制鉴权 Header。
- 复制环境变量片段。
- 查看 OpenAI 兼容、Gemini 和 Claude 的标准路径。
- 复制模型列表自检示例。
- 复制 Responses、Chat、Gemini 和 Claude 的正式请求示例。
- 查看客户端配置示例。
- 查看错误响应字段。
- 了解流式错误的收尾方式。
- 通过页内锚点快速跳转到不同说明区域。

### 动态反馈与空状态

该页面没有表单提交，也没有复杂异步交互。所有内容随当前访问地址和系统配置直接生成，因此用户看到的 Base URL 会跟当前环境保持一致。

当页面上的复制按钮被点击时，会立即把对应地址、Header、示例或环境变量放入剪贴板。若用户没有可用的接入地址或配置，页面也会保持这一区域的说明性内容，不会让用户面对空白文档。

## 23. 我的日志页（/user/logs）

### 页面定位

我的日志页是普通用户查看自己请求记录、排查调用问题和导出日志的入口。页面只围绕当前登录用户拥有的 API Key 展示数据，用户不能在这里看到其它用户的请求记录。

这个页面适合用于确认某次调用是否成功、实际使用了哪个模型和提供商、消耗了多少 token、产生了多少费用、是否命中内容防护、是否发生提供商切换，以及是否可以继续进入会话回放查看完整上下文。

### 顶部标题区

页面顶部显示“我的日志”和“请求记录与排障入口”，明确告诉用户这里不是全站日志中心，而是个人维度的请求记录页面。

标题区的作用是帮助用户快速进入排障语境：如果某次客户端调用失败、费用异常、模型响应不符合预期，用户可以先到这里按时间、密钥、模型、状态或会话键定位对应请求。

### 汇总指标区

标题下方是个人日志汇总指标区，用于展示当前筛选条件下的整体用量概况。这里包含：

- 当前请求量。
- 成功请求数。
- 失败请求数。
- 关联密钥数。
- 输入 Token。
- 输出 Token。
- 总 Token。
- 总费用。

这些指标会跟随筛选条件变化。用户筛选某个模型、某个密钥或某类请求后，顶部汇总会同步反映该范围内的请求数量、成功失败情况、Token 用量和费用消耗。

### 基础筛选区

基础筛选区提供最常用的日志过滤条件，适合用户快速缩小排查范围。用户可以按以下维度筛选：

- 日志类型：全部、对话请求、响应请求、内容审核请求、文件请求。
- 提供商：按当前用户日志中实际出现过的提供商筛选。
- 模型名：按当前用户日志中实际出现过的模型筛选。
- 密钥：按用户自己拥有的 API Key 筛选。
- 密钥标识：按密钥名称、前缀或可识别标识筛选。
- 成功状态：全部、成功、失败。

这些筛选项面向高频排障场景。例如用户只想看某个 API Key 今天是否一直失败，可以选择对应密钥并把成功状态切到失败；如果怀疑某个模型路由异常，可以选择模型名并观察结果列和详情弹窗。

### 高级筛选区

高级筛选区默认收起，用于承载低频但更精细的排查条件。用户展开后可以继续按以下维度过滤：

- 手动模型。
- 手动密钥。
- 租户。
- 项目。
- 应用。
- 环境。
- 内容检测结果。
- 内容风险等级。
- 内容处置动作。
- 最终策略。
- 检测阶段。
- 命中分类。
- 是否切换提供商。
- 是否适配跳过。
- 会话键。

高级筛选区适合更复杂的定位需求。例如用户可以按会话键找到同一轮对话的多条请求，按内容风险等级查看是否有请求被内容防护拦截，按最终策略确认请求是正常放行、仅记录、异步复核、阻断，还是经历了提供商切换。

页面默认会排除可用性检测类日志，避免个人请求列表被系统探测记录干扰。用户主要看到的是和自己业务调用相关的日志。

### 结果列表区

结果列表区是页面的核心工作区，展示筛选后的请求记录。列表顶部显示“结果列表”和“关键列请求记录”，旁边提供“导出请求日志”和“刷新”按钮。

列表还会显示最近刷新时间，帮助用户判断当前数据是否已经更新。如果用户刚刚在客户端发起请求，可以点击刷新，让页面重新拉取最新日志。

### 日志表格列

日志表格按行展示每条请求。每行包含以下信息：

- 时间：展示请求发生时间，方便用户按时间定位问题。
- 思维等级：展示该请求关联的推理或思维强度信息，便于理解请求是否走了对应能力配置。
- 密钥：展示请求使用的 API Key 名称，帮助用户区分不同应用、环境或业务方。
- 模型 / 提供商：展示请求使用的模型以及实际路由到的提供商。
- 结果：展示请求成功、失败或其它终态信息。
- 用量 / 用时：展示 token、费用、耗时、首 Token 延迟、吞吐等核心性能和计费摘要。
- 详情：提供进入请求详情弹窗的按钮，部分有会话键的记录还会提供回放入口。

表格主列只展示排障所需的摘要信息，详细请求头、Trace、内容防护细节、计费明细和原始数据会收纳到详情弹窗中，避免列表过度拥挤。

### 分页与刷新区

列表底部提供分页控制。用户可以选择每页显示 20、50、100 或 200 条记录，并通过上一页、下一页翻页查看历史请求。

刷新按钮用于重新加载当前筛选条件下的日志。刷新过程中按钮会进入加载或反馈状态，刷新完成后会提示已刷新；如果加载失败，会显示失败反馈，避免用户误以为页面已经更新。

页面在刷新日志时会尽量等待最新日志写入完成，减少“请求刚发出但列表暂时看不到”的错觉。若后台日志队列存在积压或写入异常，刷新反馈会提示用户当前数据可能存在延迟。

### 导出日志区

“导出请求日志”按钮会按照当前筛选条件导出请求日志。导出内容用于离线排障、对账、留档或交给技术支持分析。

导出时页面会使用当前的日志类型、模型、提供商、密钥、成功状态、内容防护、会话键等筛选条件，并尽量等待最新日志写入后再生成文件。导出范围有数量上限，避免一次性导出过大数据影响页面和服务稳定。

### 请求详情弹窗

点击某条日志的“详情”后，页面会打开请求详情与 Trace 弹窗。弹窗用于展示单条请求的完整排障信息，主要包含：

- 请求结果概览：成功或失败状态、模型、提供商、状态码、耗时、Trace ID、请求 ID 等。
- 鉴权信息：API Key 名称、密钥标识、所属用户等。
- 请求校验信息：请求方法、路径、状态码、日志类型、请求形态等。
- 模型能力信息：请求模型、实际模型、思维等级、图片输入等。
- 路由与提供商尝试：实际提供商、尝试次数、上游请求和完成原因。
- 客户端请求头：辅助定位客户端调用来源和请求特征。
- 流式信息：是否流式、首 Token 延迟、总耗时、输出速度等。
- 错误响应：失败类型、错误码、是否可重试等。
- 内容防护：检测结果、风险等级、处置动作、命中分类、证据和最终策略。
- 计费信息：输入 token、输出 token、总费用、计费状态等。
- 链路时间线：展示请求在系统中的关键处理节点。
- 原始 JSON：用于排障时查看更完整的结构化数据。

详情弹窗让用户不必离开当前列表，就能从一条请求展开到鉴权、路由、性能、内容安全和计费多个角度排查问题。

### 会话回放入口

如果某条日志带有会话键，详情列会提供“回放”入口。用户点击后可以进入会话回放页面，并按该会话键查看相关请求上下文。

这个入口适合排查多轮对话问题。例如用户发现某条请求输出异常，可以直接跳到同一会话的其它记录，查看前后请求是否影响了当前结果。

### 用户可完成的操作

在我的日志页，用户可以完成以下操作：

- 查看自己 API Key 产生的请求记录。
- 查看当前筛选范围内的请求数、成功数、失败数、Token 用量和费用。
- 按日志类型筛选请求。
- 按提供商筛选请求。
- 按模型筛选请求。
- 按 API Key 或密钥标识筛选请求。
- 按成功或失败状态筛选请求。
- 展开高级筛选并按租户、项目、应用、环境继续过滤。
- 按内容防护结果、风险等级、处置动作、最终策略和检测阶段定位安全相关日志。
- 按会话键查找同一会话的请求记录。
- 查看每条请求的模型、提供商、结果、用量、耗时和费用摘要。
- 打开单条请求详情弹窗。
- 查看请求 Trace、链路时间线和原始 JSON。
- 从日志跳转到会话回放。
- 调整每页数量并翻页查看历史记录。
- 刷新当前列表。
- 按当前筛选条件导出请求日志。

### 动态反馈与空状态

页面会异步加载筛选选项和日志数据。加载过程中，表格区域会提示正在加载日志；如果当前筛选条件下没有记录，列表会显示暂无日志。

当用户修改筛选条件时，页面会自动重新加载数据，并同步更新地址中的筛选参数，方便刷新页面后保留当前视图。文本类筛选会在用户输入后延迟触发，避免每输入一个字符都立即刷新。

如果日志加载失败，表格区域会给出加载失败提示。刷新、导出等按钮也会提供执行中、成功或失败反馈，让用户知道操作是否已经生效。

## 24. 我的操作记录页（/user/operations）

### 页面定位

我的操作记录页用于展示当前用户在账户和密钥管理相关操作中留下的审计记录。它不是请求日志页，也不是系统级审计中心，而是面向普通用户自己的操作留痕入口。

用户可以在这里回看自己做过哪些管理动作，动作是否成功，影响了哪个对象，是否留下了请求追踪标识，以及这条操作的摘要内容是什么。这个页面主要帮助用户确认“我刚才改了什么、删了什么、轮换了什么、导出了什么”。

### 顶部标题区

页面顶部显示“用户中心”和“我的操作记录”，让用户一眼知道当前看的是个人账户审计，不是全局后台日志。

标题区下面给出“操作审计 / 记录列表”的上下文，强调这是一份可按条件检索的留痕清单，方便用户在事后回查操作过程。

### 刷新区

列表右上角有“最近刷新”提示和“刷新”按钮。用户可以手动重新拉取最新的操作记录，查看刚刚执行的资料修改、密码修改、密钥调整或自检动作是否已经写入审计。

刷新区域的作用是让用户快速确认刚才的账户行为是否已经落库，而不是等待页面自然过时。

### 筛选区

页面提供三个基础筛选条件：

- 关键词：可输入动作名、对象名、摘要或 trace。
- 动作：按具体操作类型筛选。
- 结果：按成功或失败筛选。

关键词筛选适合模糊查找。比如用户只记得自己导出过账单、轮换过密钥或者执行过自检，但忘了具体时间，就可以通过关键词快速定位。

动作筛选则面向明确场景，支持常见用户操作类型，包括：

- 资料更新。
- 密码修改。
- 创建 API Key。
- 更新 API Key。
- 启停 API Key。
- 删除 API Key。
- 轮换 API Key。
- 日志导出。
- 账单导出。
- 接入自检。

结果筛选用于区分动作是否执行成功，适合用户排查某次操作有没有失败、是否需要重新执行。

### 记录表格区

页面主体是操作记录表格。每条记录按行展示以下内容：

- 时间：操作发生的时间。
- 动作：本次行为的类型，如创建 API Key、修改密码或导出账单。
- 对象：被影响的对象名称和对象类型。
- 结果：成功或失败。
- 来源 IP：触发这次操作的来源地址。
- 摘要：对这次操作的简短说明。

表格的作用是让用户不点进详情也能迅速看出操作发生在什么时候、改了谁、结果如何，以及有没有留下可追溯的线索。

对象列里会同时给出对象名称和对象类型，方便用户分辨这是账号资料、API Key、导出行为还是自检行为。

### 分页区

表格下方提供分页信息和翻页按钮。用户可以查看当前页数、总页数和记录总量，并通过上一页、下一页浏览更早的操作留痕。

这个页面默认按时间倒序展示最近的记录，适合先看最新的账户操作，再向前追溯历史变更。

### 用户可完成的操作

在我的操作记录页，用户可以完成以下操作：

- 查看自己的账户操作审计记录。
- 按关键词搜索操作、对象、摘要或 trace。
- 按动作类型筛选资料更新、密码修改、API Key 管理、导出和自检记录。
- 按成功或失败筛选操作结果。
- 查看每条记录的时间、对象、来源 IP 和摘要。
- 翻页查看更早的历史操作。
- 刷新列表获取最新记录。

### 动态反馈与空状态

页面会在打开后自动加载操作记录；加载前表格会显示占位状态。用户点击刷新后，页面会重新请求最新数据并更新“最近刷新”时间。

如果当前用户没有任何操作记录，表格区域会显示没有匹配的操作记录，提示用户这里是空的审计列表，而不是页面故障。

如果筛选条件过严，导致没有命中任何结果，页面会保持同样的空状态提示，帮助用户理解问题可能出在筛选范围，而不是数据丢失。

## 25. 我的账单页（/user/billing）

### 页面定位

我的账单页是用户查看账户余额、费用消耗、充值记录和近期消费趋势的核心页面。它面向的是同一账户下共用余额池的所有 API Key 所产生的消费总览，帮助用户判断“还能不能继续用、花了多少、钱都花在哪了”。

这一页既有首屏概览，也有账单明细和趋势图，适合日常查余额、查费用、查某个密钥的消耗，以及导出账单做留存或对账。

### 首屏概览区

页面顶部用“余额与费用概览”作为主标题，先告诉用户这是一个账单中心页面，不是单纯的日志列表。

首屏右侧会突出显示当前可用余额，并附带共享账户金额池的口径说明，让用户第一眼就知道自己还能继续调用多少。旁边还展示 24 小时扣费和账单记录数量，方便快速感知最近是否在持续消耗。

这一块的核心作用是快速回答两个问题：当前余额还剩多少，以及最近费用增长是不是异常。

### 余额统计区

标题下方的统计卡片展示账户层面的关键金额：

- 共享账户总余额。
- 冻结金额。
- 可用余额。
- 累计充值。
- 累计费用。
- 账单记录数。

这些数字都围绕同一个共享账户余额池展开，用户可以直观看到总余额、冻结部分、可继续使用的额度，以及累计充值和累计费用之间的关系。

### 调用与消费摘要区

页面中部还有一组更偏“仪表盘”的摘要卡，帮助用户从不同角度理解消费情况：

- 余额：当前共享账户总金额池。
- 可用额度：扣除冻结金额后还能继续调用的金额。
- 累计消费：当前账号下所有 API Key 共用的消费口径。
- 调用次数：总计、今日、本月。
- Token 消耗：总计、今日、本月。
- 费用消耗：总计、今日、本月。

这一区域适合快速排查是否最近调用量激增、Token 用量上升，或者费用增长与请求增长是否一致。

### 按密钥概览区

账单页右侧提供“密钥消费概览”，最多显示前几条 API Key 的消费情况。每一项会同时展示该密钥的费用和 Token 用量，帮助用户快速识别哪个密钥消耗最大。

这个区域适合做粗粒度定位。例如用户怀疑某个业务系统费用过高，可以先看密钥概览，再进一步下钻到账单记录和请求日志。

### 最近账单记录区

页面下半部分是最近账单记录表，配有“最近 N 条”的条数选择和“导出 CSV”按钮。

用户可以切换查看最近 20、50、100 或 200 条账单。导出按钮会把当前页面对应的账单记录导出成 CSV，方便离线对账、提交财务或本地分析。

这一区域是账单页最直接的明细入口，适合查看每一笔费用对应了什么类型的记录、哪个密钥、哪个模型和多少 Token。

### 账单表格列

账单表格按行展示每一条费用或余额相关记录，字段包括：

- 时间：账单或扣费发生时间。
- 类型：请求扣费、充值或人工调整。
- API Key：对应的密钥名称或标识。
- 金额：本次账单金额。
- 原币种：原始币种金额、汇率和版本信息。
- 余额后：这笔记录完成后的余额。
- 模型：对应的提供商或模型名称。
- Token：本次使用的 Token 数。
- 备注：这条账单记录的补充说明。

类型列会把内部记录类型翻译成用户可读语义，让用户知道这笔记录究竟是调用扣费、充值进账，还是人工调整。

原币种列会同时展示原始金额和汇率相关信息，方便用户追溯账单的换算依据，不会只看到换算后的单一金额。

### 趋势图区

页面底部提供最近 7 天费用趋势图，用来展示近一周的费用变化情况。用户可以借此判断费用是平稳增长、逐步下降，还是某几天突然升高。

趋势图区更适合看整体走势，而不是单笔明细。它与上方的账单记录表配合使用，一个看变化趋势，一个看具体记录。

### 用户可完成的操作

在我的账单页，用户可以完成以下操作：

- 查看共享账户总余额、冻结金额和可用余额。
- 查看累计充值、累计费用和最近扣费情况。
- 查看当前账户下的调用次数、Token 消耗和费用消耗。
- 观察哪些 API Key 消费较多。
- 按最近 20、50、100 或 200 条查看账单明细。
- 查看账单类型、金额、原币种、余额变化、模型和备注。
- 导出账单 CSV。
- 查看最近 7 天费用趋势。

### 动态反馈与空状态

页面会在进入后直接渲染余额概览、统计卡和趋势图，因此用户打开时就能看到完整的账单视图。

如果当前账号没有账单记录，表格区域会显示“暂无账单记录”，提醒用户此页不是错误，而是暂时没有消费历史。

如果用户切换条数后页面重新加载，表格会按照新的限制重新显示最近账单，导出按钮也会跟随当前页面的账单口径输出对应 CSV。

## 26. 会话回放页（/user/conversations）

### 页面定位

会话回放页用于按 `conversation_key` 聚合同一段对话或同一业务会话中的多条请求，帮助用户把离散的请求记录串成完整上下文。它适合排查多轮对话为什么变成了当前结果，也适合确认模型在某个会话里经历了哪些轮次、用了哪个提供商、消耗了多少 token。

这一页本质上是“列表 + 详情”的双栏回放界面：左边找会话，右边看完整回放。用户不需要逐条回到请求日志里翻找，而是直接在会话层面回看整段轨迹。

### 首屏概览区

页面顶部的主标题显示“会话回放”，副标题显示“我的会话回放”，明确这是用户自己的会话轨迹，不是全站通用的聊天记录。

标题旁的概览卡会展示当前结果集的规模、当前检索条件、最近更新时间和结果条数。用户可以一眼看出当前筛到了多少会话，以及列表是否刚刚更新过。

### 统计摘要区

页面上方的统计卡片展示四项关键信息：

- 会话数。
- 命中请求量。
- 总 Token。
- 最近线路。

这一区域用于快速判断当前筛选结果的总体规模和消费情况。比如用户搜索了某个关键词后，可以马上看到命中了多少会话、包含多少请求，以及最近一次由哪个提供商承接。

### 会话列表区

页面左侧是会话列表，也是最主要的入口。每个会话条目会展示：

- `conversation_key`。
- 该会话的请求次数。
- 最近使用的模型。
- 总 Token。
- 回复预览。
- 最近更新时间。
- 最近提供商。

列表支持点击选择。用户点中某个会话后，右侧详情会立即切换到对应回放内容。会话条目本身就是概览卡，帮助用户先扫描哪些会话更值得查看。

如果当前没有任何会话记录，列表区域会显示空状态，提示用户暂时没有可回放的会话。

### 搜索与刷新区

会话列表上方提供搜索框和刷新按钮。用户可以输入：

- `conversation_key`。
- 模型名。
- 提供商名。
- 回复内容关键词。

搜索用于快速定位某段对话，特别适合用户只记得某句话或某个模型名称，却不记得具体时间的情况。

刷新按钮会重新拉取会话列表，并保持当前选中的会话状态。页面也会根据当前 URL 里的 `conversation_key` 自动回到指定会话，方便从日志页或外部跳转时直接定位。

### 回放详情区

页面右侧是会话回放详情区。未选中会话时，这里会提示用户先从左侧选择一个会话；选中后会展示完整回放。

详情区上方的标题会显示当前会话键，下方会先给出一组摘要信息：

- 请求数。
- 成功 / 失败数量。
- 总 Token。
- 最近模型。
- 首条时间。
- 最近更新时间。
- 最近命中线路。

这组摘要用于快速理解该会话的整体表现，适合先确认这段会话是否异常，再继续看逐轮内容。

### 回放内容区

详情区的主体是逐轮回放内容。每一轮会显示：

- 角色。
- 所用提供商或请求模型。
- 创建时间。
- Token 数。
- 请求形态，区分流式或非流式。
- 是否包含图片能力，区分文本或视觉输入。
- 本轮内容正文。

这样用户可以顺着每一轮的顺序查看用户输入、模型回复和中间轮次，理解会话是如何演变到当前结果的。

如果某个会话没有可回放内容，详情区会提示该会话暂无可回放内容，而不是显示空白。

### 相关日志入口

当用户选中某个会话后，页面会出现“查看相关日志”入口。点击后可以跳转到日志页，并带上当前会话键，方便从会话回放直接联动到请求日志明细。

这个入口适合从“会话视角”下钻到“请求视角”。用户先看整段对话，再跳到单条请求，定位具体失败原因、提供商切换或 token 消耗细节。

### 用户可完成的操作

在会话回放页，用户可以完成以下操作：

- 按 `conversation_key` 查看会话聚合结果。
- 搜索会话键、模型、提供商或回复内容。
- 查看会话数量、请求量、总 Token 和最近线路。
- 点击左侧会话切换右侧详情。
- 查看每段会话的请求数、成功失败、首条时间和最近更新时间。
- 查看逐轮回放内容。
- 从回放页跳转到相关请求日志。
- 刷新会话列表。

### 动态反馈与空状态

页面进入后会自动加载会话列表，并在必要时自动打开当前 URL 指定或列表首个会话。这样用户从外部跳转进来时，不需要再手动点一次。

搜索时页面会重新加载会话列表，左侧列表和右侧详情都会同步更新。刷新时按钮会提供执行状态反馈，避免用户误以为没有生效。

如果当前账号没有会话记录，左侧列表会显示暂无会话记录；如果选中的会话没有可回放内容，右侧详情会显示对应空状态。两种空状态分别对应“没有数据”和“没有轮次内容”，语义是分开的。

## 27. 接入自检页（/user/self-test）

### 页面定位

接入自检页是普通用户在正式接入前做最后验证的地方。它不是只看候选线路，也不是单纯查状态，而是把用户准备上线的 API Key 和模型真正拿来跑一次最短真实调用，确认这组参数是否真的可用。

页面的核心目标是帮助用户在正式接业务流量前，先确认密钥、模型、授权范围、候选线路和多模态能力是否正常，并在失败时直接给出摘要、修复建议和下一步动作。

### 首屏概览区

顶部主视觉区显示“接入自检”和“先验证，再上线”，明确这个页面的用途是上线前验证。

右侧概览会展示外部 `/v1` 入口、当前可用密钥数量和模型数量，帮助用户确认自己的接入环境是否已经具备测试条件。

### 页面锚点导航

页面提供四个锚点入口：

- 选择对象。
- 预检查。
- 真实测试。
- 候选线路。

用户可以快速跳到想看的区域，不需要整页滚动。这个设计特别适合先看候选，再回去切对象，或者先看测试结果再回看候选线路的场景。

### 选择对象区

页面先让用户选择真正准备上线的组合，也就是一个 API Key 和一个模型。

这里的下拉框会列出当前账号拥有的 API Key 和可用模型。用户选定后，可以点击“更新检查对象”刷新组合，也可以直接点击“发起真实测试”。

“发起真实测试”按钮在对象没有选全时会保持禁用，避免用户在不完整条件下启动测试。

### 预检查区

预检查区会先列出当前选择组合的基础条件，帮助用户在发请求前就知道这组参数有没有明显问题。这里展示的内容包括：

- 密钥状态。
- 授权提供商数量。
- 模型白名单范围。
- 是否可回显明文。
- 命中候选数。

如果当前账号还没有可自检的 API Key 或模型，这里会显示空状态，提醒用户先去补齐可用对象。

### 快速复制区

页面右侧提供快速复制区，方便用户在不离开页面的情况下拿到接入材料。这里可以复制：

- `/v1` 入口。
- 当前密钥明文。
- `Authorization: Bearer ...` 请求头。

这个区块的作用是让用户把自检成功的参数立刻复制给客户端、脚本或临时测试环境，不需要再手工拼接。

### 真实测试区

真实测试区是页面最核心的功能区。用户可以选择：

- 图片链路是否测试。
- 图片细节等级。
- 图片链接测试。
- 本地图片测试。
- 生成图片测试。

页面默认只测试文本链路。如果切换为图片理解或生成图片模式，会自动追加对应的多模态自检场景。

当用户选择图片链接或本地图片时，页面会显示对应输入框和说明文字；当用户选择生成图片时，会切换到生图测试说明，并关闭图片细节参数。

### 结果摘要区

测试执行后，页面会在结果摘要区显示当前状态、模型、成功与失败场景数量，以及图片链路是否参与测试。

如果测试未开始，这里会显示“尚未执行”；测试进行中会切换为“测试中”；测试完成后会显示成功或失败，并同步更新统计信息。

### 失败原因与修复建议区

真实测试失败后，页面会自动整理失败原因摘要和修复建议。

失败原因区会告诉用户为什么失败，尽量用简短语言说明当前请求没有通过；修复建议区会告诉用户下一步该改什么，比如检查密钥状态、模型名、授权提供商、余额、候选线路，或者去日志页看完整报错。

这两个区块的作用不是堆错误，而是把错误翻译成下一步可执行动作。

### 下一步动作区

页面还会给出下一步动作提示，说明如果成功，就把同一组参数写入业务服务；如果失败，就去密钥页、模型页或日志页继续排查。

这个区块相当于一条简短的处理建议，避免用户停在原始报错里不知道后面该做什么。

### 输出预览与调度链路区

页面提供两个代码预览框：

- 输出预览：展示模型返回的主要文本或响应片段。
- 调度链路：展示请求经过的路由或测试链路摘要。

这两个区域适合技术排查时直接看结果内容，不需要再跳去别的页面。

### 场景明细区

页面下方的场景明细表会逐条列出每一个自检场景的结果。每行会显示：

- 场景名称。
- 端点。
- 类型。
- 状态。
- 渠道。
- 耗时。
- 摘要。

场景明细让用户看到这次真实测试到底跑了几个场景、哪些通过、哪些失败。对于多模态测试，用户可以区分文本、图片理解、图片生成等不同场景的结果。

### 候选线路区

页面最下方是路由候选预览表，用来展示当前组合可命中的候选线路。每行会展示：

- 渠道。
- 模型。
- 可用状态。
- 熔断状态。
- 得分。
- 近期成功率。
- 平均延迟。
- 是否默认线路。

这一区域用于帮助用户理解为什么这组自检会命中某个提供商，也方便在失败时观察是否需要换模型、换密钥或重新选择线路。

### 用户可完成的操作

在接入自检页，用户可以完成以下操作：

- 选择准备上线的 API Key 和模型。
- 查看密钥状态、授权提供商、模型白名单和候选数。
- 复制 `/v1` 接入入口。
- 复制当前密钥明文。
- 复制标准鉴权请求头。
- 选择文本、图片理解或图片生成自检模式。
- 选择图片链接或本地图片进行多模态测试。
- 发起真实测试。
- 查看测试状态、失败原因和修复建议。
- 查看下一步处理动作。
- 查看输出预览和调度链路。
- 查看每个场景的详细结果。
- 查看路由候选线路。
- 下载自检报告。

### 动态反馈与空状态

页面在初始化时会先同步图片模式说明，并准备场景结果表和报告下载按钮。用户一旦发起测试，摘要、失败原因、修复建议、输出预览、调度链路和场景表都会同步刷新。

如果测试失败，页面会把失败信息、建议和下一步动作同时展示出来；如果测试成功，会显示全部场景通过，并允许用户下载 Markdown 报告。

如果某些区域暂时没有可用数据，页面会显示等待测试或暂无结果的空状态，而不是直接留白。这样用户知道当前是没执行测试，还是执行后没有命中内容。

## 28. 初始化管理员页（/setup-admin）

### 页面定位

初始化管理员页是系统首次部署时的安全提示页。它只在系统还没有任何管理员账号时出现，用于告诉部署者：管理员账号不能通过网页创建，必须在服务器后台执行受控脚本创建。

这个页面不是注册页，也不是管理员创建表单。它的核心功能是阻止网页初始化管理员，并给出正确的服务器侧创建方式。

### 认证页外壳

该页面使用认证类页面外壳，而不是登录后的管理后台或用户中心外壳。页面顶部提供全站外观相关控制，包括风格选择和暗黑模式切换。

外观切换区适用于未登录状态，用户即使还没有管理员账号，也可以切换页面风格或主题模式，保证初始化提示在不同主题下都能正常阅读。

### 主提示区

页面主体左侧显示品牌名 `aotu-gpt`、标题“初始化管理员”，以及一段安全说明：当前系统没有管理员账号，但出于生产安全要求，管理员不能通过网页注册或初始化。

如果有人尝试通过网页提交管理员初始化，页面会显示错误提示，说明管理员账号禁止通过网页初始化，需要登录服务器后台执行脚本。

### 命令提示区

主提示区会直接给出服务器侧创建管理员的命令示例：

- `sudo /opt/auto-gpt/.venv/bin/python scripts/create_admin_user.py`

这条命令是页面上最重要的操作指引。部署者需要到服务器项目目录执行脚本，按脚本要求输入管理员用户名、邮箱和密码。

### 返回登录入口

主提示区下方提供“返回登录”链接。用户点击后可以回到登录页。

如果系统仍然没有管理员账号，访问登录页时仍会被引导回初始化管理员页；如果管理员已经通过服务器脚本创建完成，再返回登录页就可以正常登录。

### 后台创建说明区

页面右侧第一个说明卡片是“管理员创建方式”。它用步骤形式说明完整流程：

- SSH 登录服务器并进入项目目录。
- 执行管理员创建脚本，并输入用户名、邮箱和密码。
- 回到网页登录，再进入后台完成渠道、模型和用户配置。

这个区域的目标是把“不能网页创建管理员”转换成可执行的部署步骤。

### 安全说明区

页面右侧第二个说明卡片是“管理员入口必须受控”。它说明三个安全规则：

- 公开页面只能注册普通用户，不能创建管理员。
- 已有管理员可以在用户管理页创建或提升管理员。
- 正式环境必须配置非默认 Session 和 API Key 加密密钥。

这个区块解释了为什么页面不提供管理员创建表单，避免部署者误以为功能缺失。

### 用户可完成的操作

在初始化管理员页，用户可以完成以下操作：

- 查看系统当前缺少管理员账号的提示。
- 查看管理员不能通过网页初始化的安全说明。
- 复制或参考服务器侧创建管理员的脚本命令。
- 查看后台创建管理员的步骤。
- 查看管理员入口受控的安全规则。
- 返回登录页。
- 切换页面主题模式。
- 打开并切换全站风格预设。

### 动态反馈与空状态

该页面没有普通注册表单，也不会在页面内创建管理员。若后端收到网页初始化管理员的提交，会拒绝请求并在页面上显示错误提示。

当系统已经存在管理员账号时，再访问该页面会跳转到登录页，不继续展示初始化提示。这个行为说明该页面只服务于“尚无管理员”的初始部署状态。

## 29. 登录页（/login）

### 页面定位

登录页是系统所有网页登录用户的统一入口。管理员和普通用户都从这里进入系统，登录成功后会根据账号角色自动进入对应区域：管理员进入后台管理端，普通用户进入用户端。

这个页面同时承担受保护页面回跳入口的职责。用户访问需要登录的页面时，系统会先引导到登录页；登录成功后，如果原目标地址仍然适合当前角色，就回到原目标页面，否则进入该角色默认首页。

### 认证页外壳

登录页使用认证类页面外壳，不展示登录后的侧边栏和后台导航。页面顶部保留全站外观控制，用户可以在未登录状态下切换整体风格和暗黑模式。

外观设置包含当前风格、当前模式和一组可选风格预设。用户切换后，页面会立即应用新的视觉风格，并保留到后续访问中。

### 主登录区

页面主体左侧是登录核心区域，顶部显示品牌名 `aotu-gpt` 和标题“账号登录”。标题下方说明登录后的角色分流：管理员用于提供商、模型、日志、账户等治理工作，普通用户用于 API Key、账单、接入自检和调用排查。

如果登录失败，表单上方会出现错误提示，告诉用户账号或密码错误，或账号已被禁用。错误提示不会清空页面结构，用户可以继续修改输入后重新提交。

### 登录表单

登录表单包含两个主要输入项：

- 用户名或邮箱。
- 密码。

表单下方提供“登录”按钮。用户填写账号标识和密码后点击登录，系统会校验凭证并创建网页登录态。

如果用户是从某个受保护页面跳转过来，页面会保留原目标路径。登录成功后，系统会尝试回到原页面；如果没有原目标路径，则按账号角色进入默认首页。

### 注册入口

登录表单下方提供“去注册”入口。未注册的普通用户可以从这里进入注册页。

注册入口只负责跳转，不在登录页中展开注册表单，避免把登录和注册两条流程混在同一表单里。

### 产品用途说明区

页面右侧第一个说明区是“登录后可以做什么”。它把登录后的主要去向拆成三类：

- 管理端：配置提供商、模型和路由规则，查看告警、审计和日志中心。
- 用户端：获取 API Key、复制接入地址、做接入自检，再正式接入业务系统。
- 文档入口：说明 OpenAI 兼容入口使用 `/v1/*`，Gemini 和 Claude 可使用对应原生入口。

这个区域帮助不同角色的用户在登录前理解自己进入系统后要去哪里。

### 安全说明区

页面右侧第二个说明区是“登录态与 API Key 分离”。它强调浏览器登录态和外部 API 调用鉴权不是同一个东西。

说明区会提示：后台和用户端使用服务端 Session 作为网页登录态；生产系统外部调用必须使用 Bearer API Key；普通用户登录后建议先去 API Key 管理页和接入自检页完成接入准备。

### 用户可完成的操作

在登录页，用户可以完成以下操作：

- 输入用户名或邮箱。
- 输入密码。
- 提交登录。
- 在登录失败时查看错误原因并重新输入。
- 从登录页进入注册页。
- 在未登录状态下切换暗黑模式。
- 打开全站风格选择器并切换风格预设。

### 动态反馈与空状态

如果系统还没有任何管理员账号，访问登录页会被引导到初始化管理员页，而不是展示普通登录表单。这个状态用于保证首次部署时先完成受控管理员创建。

如果用户已经登录，再访问登录页会自动跳转到当前角色可访问的页面，不会重复展示登录表单。

如果凭证校验失败，页面会停留在登录页并展示错误提示。成功登录后，系统记录登录结果并跳转到后台管理端、用户端或登录前目标页面。

## 30. 注册页（/register）

### 页面定位

注册页是普通用户自助创建账号的入口。它只用于创建普通用户账号，不用于创建管理员账号。管理员账号仍然必须由受控流程创建或由已有管理员在后台处理。

公开注册默认关闭。只有管理员在系统配置中开放公开注册后，未登录用户才可以在这个页面提交注册表单。注册成功后，用户会自动进入用户端，用于获取 API Key、查看可用模型、做接入自检、查看账单和排查自己的调用日志。

### 认证页外壳

注册页和登录页使用同一套认证页外壳。页面不展示登录后的侧边栏和后台导航，只保留未登录状态下可用的全站风格切换和暗黑模式切换。

用户可以在注册前打开风格选择器，查看当前模式和当前风格，并切换到其它风格预设。切换完成后页面会显示短暂反馈，说明风格已经应用。

### 注册说明区

页面主体左侧顶部显示品牌名 `aotu-gpt` 和标题“用户注册”。标题下方说明公开注册的默认策略：系统默认关闭公开注册；如果管理员已经开放，用户可以直接注册普通用户账号。

说明文字也会告诉用户注册后的主要用途，包括进入用户端获取 API Key、查看模型、做接入自检、管理账单和查看自己的日志。

### 注册表单

当公开注册已开放时，页面会显示注册表单。表单包含四个输入项：

- 用户名。
- 邮箱。
- 密码。
- 确认密码。

用户填写完整后，可以点击“注册并进入用户端”提交。注册成功后，系统会创建普通用户账号、建立网页登录态，并跳转到用户端或登录前保留的目标页面。

### 公开注册关闭状态

当公开注册未开放时，页面不会显示注册表单，而是显示提示：当前未开放公开注册，需要联系管理员在后台创建账号，或等待管理员开放注册。

这个状态让用户明确知道不是页面加载失败，也不是自己缺少某个输入项，而是系统策略不允许公开注册。

### 错误提示区

注册提交失败时，页面会在表单上方展示错误提示。常见失败情况包括：

- 当前未开放公开注册。
- 两次输入的密码不一致。
- 用户名或邮箱等账号信息不满足创建要求。

错误提示会直接显示在注册区域内，用户可以根据提示修改表单后再次提交。

### 返回登录入口

注册区域下方提供“返回登录”链接。已经有账号的用户可以通过这个入口回到登录页。

这个入口也适用于公开注册关闭的场景：如果用户已经由管理员创建账号，可以直接返回登录页完成登录。

### 注册后路径说明区

页面右侧第一个说明区是“注册后路径”。它按步骤告诉普通用户注册完成后应该先做什么：

- 进入用户首页，查看接入主路径。
- 打开 API Key 管理页，获取或创建密钥。
- 到接入自检页发起一条真实请求，确认可用后再接入业务系统。

这个区域把注册后的下一步动作前置展示，降低新用户注册成功后的迷茫感。

### 安全说明区

页面右侧第二个说明区说明“普通用户注册不等于直接外部调用”。它强调网页登录态只用于访问用户端页面，外部系统正式调用仍然必须使用 Bearer API Key。

安全说明还建议用户注册完成后先去文档页复制 OpenAI 兼容入口或原生协议入口信息，再进行正式接入。

### 用户可完成的操作

在注册页，用户可以完成以下操作：

- 查看公开注册是否开放。
- 在开放注册时填写用户名、邮箱、密码和确认密码。
- 提交注册并进入用户端。
- 在注册失败时查看错误原因并修正输入。
- 在公开注册关闭时查看联系管理员或等待开放的提示。
- 返回登录页。
- 切换暗黑模式。
- 打开全站风格选择器并切换风格预设。

### 动态反馈与空状态

如果系统还没有任何管理员账号，访问注册页会先跳转到初始化管理员页，确保系统先完成管理员受控创建。

如果用户已经登录，再访问注册页会自动进入当前角色可访问的页面，不会重复展示注册入口。

如果公开注册关闭，页面会显示清晰的不可注册状态；如果注册信息校验失败，页面会保留注册页并展示错误原因；如果注册成功，系统会自动登录新账号并跳转到用户端或原目标页面。

