# 项目全局规范

前端设计规范：如果任务与前端设计需求有关，请务必先阅读 前端设计规范.md 文件完整内容，如果该项目根目录下面没有该文件，则无需遵守该规范，也不用新建文件！！！！！！！！。

## 1. 规范文件

- 项目根目录必须且仅允许维护一份 `项目全局规范.md` 作为唯一有效的全局规范正文文件。
- 项目根目录必须维护 `《项目全局规范-变更记录.md》` 作为全局规范变更记录唯一落地文件。
- 项目根目录下的问题经验沉淀文件必须为 `项目问题及解决方法记录.md`。

## 2. Python 环境

- 若任务需要使用 Python 环境，必须优先使用项目根目录下已有虚拟环境。
- 若项目根目录下不存在可用虚拟环境，才允许在项目根目录下新建虚拟环境。
- 若需要安装 Python 依赖，必须先指定国内镜像源，再执行安装。

## 3. 变更控制

- 执行任何任务前，必须先检查当前需求是否与本文件冲突。
- 若当前需求与本文件或已确认的历史统一标准冲突，必须先暂停执行，说明冲突点，并等待用户确认后再继续。
- 若用户已明确确认可在某一连续实施任务内由执行者自行裁定局部规范冲突方案，则允许仅在该次连续实施范围内按“最小必要变更、保持可追踪、优先不破坏既有正式对外边界”的原则继续执行；若后续需要把该裁定方式长期化，必须再单独修订本文件。
- 只有当新规则具备长期复用价值、可统一执行、可减少返工或歧义时，才允许写入本文件。
- 规范更新必须最小化、可执行、可检查、可追踪，禁止写入一次性任务要求或临时偏好。



## 4. 实施约束

- 当前任务新增或修改的实现，必须优先复用项目现有目录结构、命名方式、接口风格和数据组织方式。
- 本项目在 Windows 本地执行命令时必须使用 PowerShell 7，即 `pwsh -NoLogo -NoProfile -Command ...`；禁止默认使用 Windows PowerShell 5.1。
- PowerShell 7 中执行复杂命令时，必须先确认外层 shell 解析不会破坏目标命令：正则参数优先用单引号包裹；包含 `$`、`|`、双引号、括号、JSON 或多层命令替换的命令必须拆短、转义或改用脚本块；禁止直接套用 Bash heredoc、Bash 条件语法或未验证的跨 shell 写法。
- 临时 Python 片段必须使用 PowerShell here-string 管道到项目虚拟环境，例如 `@' ... '@ | .\.venv\Scripts\python.exe -`；禁止在 PowerShell 中使用 Bash heredoc 形式 `python - <<'PY'`。
- Git 命令带路径过滤和选项时必须保持参数顺序可验证，例如 `git diff --stat -- <paths>`、`git diff --check -- <paths>`；禁止把选项放在路径后导致命令输出或行为偏离预期。
- 同一业务数据若同时在服务端模板首屏、前端异步刷新接口或多个前端页面中展示，必须复用同一服务层序列化或汇总函数作为唯一口径；禁止模板直接查表另算一套、API 再算一套导致刷新前后或跨页面数据不一致。
- 项目内用于开发、联调、演示、验收的测试数据（如测试账号名、测试 API Key 名称、测试提供商名、测试模型别名、测试项目名、测试渠道名、测试日志示例名等）在不受第三方协议、接口字段、字符集或技术标识限制时，必须优先使用中文命名；仅在需要与外部协议、代码字段、脚本参数或固定英文标识对齐时，才允许使用英文或中英混合命名。
- 若发现历史实现与现行规范不一致，必须先指出不一致；未获明确要求时，仅对当前任务相关范围做最小必要修改，不擅自扩展为全项目重构。
- 若修改 `app/static/` 下会影响浏览器执行结果的静态资源，必须同步更新 `app/templates/base.html` 中对应资源的版本参数，避免页面结构与缓存脚本或样式不一致。
- 面向后台运维或业务用户的界面文案、下拉选项、表格展示值、卡片标题在不涉及接口路径、字段名、变量名、模型名、错误码等技术标识时，必须优先使用中文显示；若底层仍使用英文枚举值，界面层必须做中文标签映射，禁止仅向用户显示英文枚举值。
- 项目中文业务术语必须统一使用“提供商”指代上游模型服务、OpenAI 兼容渠道、聚合渠道或自建推理服务；前端界面、项目文档、提示语、审计摘要、错误说明和测试数据禁止继续使用“中转站”或“中转”作为该对象名称。英文代码字段、数据库表名、接口路径和第三方协议中的 `provider` 可继续保留。
- 后台与用户端前端页面中，凡数值型配置项或展示值已经携带单位时，界面标签或紧邻展示必须直接使用符号单位（如 `ms`、`s`、`B`、`tok`、`d`、`$/1M`），禁止再额外重复一段中文单位说明。
- 前端界面中所有面向用户展示的 token 单价必须统一按“每百万 token”口径显示为 `$/1M`；若底层字段仍为 `*_per_1k`，仅允许在展示层做换算，禁止把 `$/1k`、`美元 / 1k token` 等旧口径继续暴露到界面。
- 表格、日志列表和高密度数据区中的 Tooltip、Popover 等帮助浮层必须使用不透明背景，并且必须挂载到不会被父容器 `overflow` 裁切的层级，禁止出现提示层被卡片、表格壳或滚动容器遮挡的问题。
- 表格、日志列表、矩阵视图中的状态列、结果列、模型列等高密度主字段若需要补充异常原因，主字段必须只保留摘要状态、模型名或必要指标；详细异常必须收纳到同字段旁的问号帮助图标中，并通过不透明 Tooltip 展示，禁止把长异常文本直接塞进主字段正文挤占列宽。
- 全站前端界面中的问号帮助图标必须复用同一套共享组件样式与浮层实现；提示框必须使用不透明背景、挂载到全局顶层、默认显示在图标右上方，且日间模式使用浅色背景、暗黑模式使用明亮暗色背景，禁止同站点内并行维护多套问号图标样式或 Tooltip 交互。
- 全站前端页面在同一页面内的卡面容器必须采用无阴影的一体化视觉；禁止继续通过阴影把 `panel`、`metric`、`hero`、`card` 等内容块做成彼此割裂的悬浮卡片。
- 该项目全站前端界面中的方框类容器、按钮、输入框、下拉框、标签、卡片、弹窗、抽屉、表格壳及其他矩形或近矩形控件必须统一使用直角；禁止继续使用任何圆角、胶囊角或圆角矩形。若 `前端设计规范.md` 中存在圆角相关建议，与本条冲突时必须以本文件为准。
- 全站前端页面中用于布尔设置启停的交互控件必须优先使用滑块式开关，不得继续直接向用户裸露浏览器默认勾选框作为主要视觉形态。
- 全站前端页面中的按钮、图标按钮、导航按钮及其他按钮式交互控件必须默认不显示可见方框或边框；仅允许在 `hover`、`focus-visible` 等指向态显现提示性边界，禁止默认做成边界分明的方框按钮。
- 后台管理页与用户业务页的表单操作按钮、行内操作按钮和同组输入框旁按钮必须默认按内容宽度自适应，使用紧凑横向内边距与 `inline-flex` 行内布局；除明确的全宽提交场景外，禁止按钮默认独占整行、设置过大 `min-width` 或使用超大横向 `padding`。
- 后台管理页与用户业务页中输入框与同组操作按钮必须优先使用同行 `flex` 布局，输入框占用剩余空间，按钮按内容自然收缩；仅在移动端或容器宽度不足时允许自然换行或堆叠。
- 全站前端页面中的按钮、图标按钮及其他点击型交互控件在触发后必须提供明确结果反馈；若该操作不需要展示详细结果，则必须优先在原按钮位置短暂反馈成功或失败；若该操作需要展示具体结果、过程详情或错误内容，则必须使用贴合场景的结果界面承载，禁止点击后无反馈。
- 全站前端页面中的按钮、图标按钮及其他点击型交互控件在执行中若使用转圈加载态，转圈图标必须固定居中显示在原按钮内容区域内，并保持按钮宽高与布局不变；禁止把转圈图标与按钮文字并排挤压或导致按钮尺寸跳动。
- 全站前端主题系统必须同时支持日间模式与暗黑模式，并且必须提供不少于 10 套可一键切换的统一风格/配色预设；风格切换必须在全局壳层生效并持久化用户选择，禁止只在单个页面局部换色或要求用户逐页分别设置。
- 后台管理端与用户业务端的全局主导航在桌面端必须放置于左侧侧边栏，顶部区域仅允许承载轻量工具、账号、主题切换或页面上下文信息；移动端必须提供可折叠导航入口，禁止把全量主导航长期铺在页面顶部。
- 左侧侧边栏若同时承载主导航与底部工具区，桌面端必须将两者拆分为“主导航独立滚动区 + 底部工具固定区”或等效结构；禁止把两类区域放入同一无边界流式容器后依赖字号压缩硬塞进一屏，避免出现按钮重叠、文字压盖或底部操作区挤入主导航列表。
- 后台管理端与用户业务端的数据密集页面外壳必须尽量占满可用屏幕宽度，仅保留统一安全边距；表格、统计卡片、筛选区和操作面板必须优先铺满主内容区。仅正文阅读区、登录/注册卡片、弹窗和代码示例等需要控制可读性的内容允许单独限宽。
- 后台管理页与用户业务页的筛选区必须优先使用同排等宽栅格布局；关键词、状态、归属、分页数量等同层级筛选项在桌面与平板宽度足够时必须尽量排在同一行且控件宽度、高度保持一致，仅在移动端或容器宽度不足时允许自然换行或单列展示，禁止无业务必要地把每个筛选项做成独占整行的堆叠列表。
- 后台管理页与用户业务页中的数据列表、表格和矩阵类视图必须优先使用服务端分页查询和展示；筛选区必须支持业务归属维度与关键词搜索，禁止一次性拉取全量数据后仅在前端本地筛选或分页。
- 后台管理页与用户业务页中的高密度对象列表必须采用“核心主列 + 按需详情”的渐进式披露结构；主表仅允许展示识别、状态、关键指标和高频操作，Base URL、策略配置、长文本、凭据说明、详细规则、低频操作等信息必须收纳到详情行、详情抽屉、弹层或更多菜单中，禁止把所有字段同级铺满主表。
- 后台管理页与用户业务页禁止继续放置脱离实时数据、表单提交或实际操作入口的纯说明性前端内容；对象管理流程、建议阅读顺序、治理提醒、风险提示、判断原则、下一步动作等静态流程卡片、建议卡片、说明卡片必须移除。确有必要保留的操作说明，仅允许紧贴具体控件以内联文案、占位提示、Tooltip、空状态或结果反馈形式出现，不得再单独做成说明性卡片或侧栏说明块。
- 后台管理页与用户业务页禁止放置独立快捷入口卡片、快速跳转卡片或常用动作卡片；跨页面跳转必须优先通过全局导航、页面主操作按钮、锚点导航或贴近业务对象的必要操作呈现，禁止以 `快捷入口`、`快速跳转`、`常用动作` 等独立卡片堆放链接。
- 对外统一代理入口必须使用 `/v1/*` 路径并采用外部 Bearer API Key 鉴权；内部管理或调试调用禁止直接复用对外 `/v1/*` 入口。
- 对外 `/v1/*` 入口只要已接收到外部请求，必须向调用方返回可读结果；正常请求返回正常响应，失败请求必须按具体原因返回错误码、错误说明、`trace_id` 和可重试标记，禁止只在本地记录错误而向调用方空回或直接断流。
- 对外 `/v1/*` 错误响应必须统一返回 OpenAI 兼容的 `error` 对象，并在其中稳定包含错误说明、错误码、错误类型、`trace_id`、`retryable`、`recoverable` 和 `category`；错误分类必须区分请求结构、鉴权授权、余额/限流、模型或能力不支持、路由容量、上游临时故障、网络超时和内部异常，禁止不同入口返回互不兼容的错误结构。
- 项目必须维护唯一的统一错误目录或错误处理层；各业务层、路由层、适配层和后台任务在响应错误、写日志、写 trace、发告警时必须复用同一套错误码、错误说明、处理策略和可追踪元数据，禁止各模块单独拼装错误文本或各自定义同义错误码。新增错误类型时只能先进入统一错误目录，再由对外响应层和对内日志层按同一目录解析；对外错误说明必须优先使用中文，对内可保留必要英文标识用于变量名、参数名和协议字段。
- 对外 `/v1/*` 请求因端点协议或能力不支持被拒绝时，错误详情必须结构化返回 `requested_endpoint`、`required_endpoint`、`required_protocol`、`missing_capability`、`primary_reason`、`reason_details` 和诊断样本；禁止仅返回 `model_not_available`、`route_unavailable` 等泛化原因让调用方猜测。
- 对外 `/v1/*` 流式响应在 SSE 连接建立后发生上游错误、超时、空流或内部异常时，必须尽可能发送 `event: error` 错误事件并以 `data: [DONE]` 收尾；客户端主动断开除外。
- 人类用户登录态必须与对外 API Key 鉴权分离：管理后台端与用户端统一使用基于服务端 Session 的浏览器登录态，对外 `/v1/*` 入口禁止以人类用户登录态替代 Bearer API Key。
- 前端页面路由必须维护统一访问控制规则；受保护页面既必须保留服务端 Session 校验，也必须在前端壳式导航层按登录态与角色做集中拦截，禁止各页面各自散落判断。
- 未登录访问受保护页面时，前后端必须统一跳转到 `/login`，并通过 `next` 参数保留原目标路径；登录成功后仅允许回跳到当前角色可访问的页面。
- 已登录但角色不匹配时，前端路由守卫与服务端页面路由必须统一重定向到该角色的默认首页；管理员默认首页为 `/`，普通用户默认首页为 `/user`。
- 内部 playground 或后台调试能力必须通过 `/api/playground/*` 等内部管理接口接入，不得要求浏览器直接持有外部 API Key。
- 浏览器侧的内部多模态素材上传、预处理和临时资源生成能力必须通过 `/api/playground/assets/*`、`/api/user/assets/*` 等内部 Session 接口接入，禁止把此类浏览器辅助能力直接暴露到对外 `/v1/*` 正式代理入口。
- 对外 `/v1/*` 入口禁止接收仅供内部调试使用的强制路由参数或指定 provider 头；“指定某个具体提供商测试”只允许出现在 `/api/playground/*` 链路。
- 对外响应式接口必须使用标准 `/v1/responses` 路径，禁止新增或沿用非标准 `/v1/response` 路径。
- API Key 相关请求日志必须继续复用 `request_logs` 扩展字段落库，禁止新建第二套重复的请求日志主表。
- 对外 `/v1/*` 请求只要到达应用层，必须写入 `request_logs`；日志必须记录成功状态、HTTP 状态码、失败错误码、失败原因、`trace_id`、请求路径、请求方法、来源 IP 和可用的 API Key/账户上下文。
- 对外 `/v1/*` 请求日志允许异步队列落库，但正式日志列表与用户日志列表在默认刷新场景下必须等待队列空闲，目标实时性必须控制在 2 秒以内，正常情况下优先控制在 1 秒以内；若队列不可用或入队失败，必须回退同步落库。
- 对外 `/v1/*` 请求日志中的请求体信息必须优先记录结构摘要，包括字段名、层级、数组长度、对象键数量、值类型、必要的安全枚举值和截断信息；禁止默认记录完整正文、完整 prompt、图片/音频/base64、密钥或其他敏感大字段。
- 对外 `/v1/*` 请求体在做图片识别、工具识别、内容类型识别或 Token 预估时，凡是读取 `type` 字段的逻辑都必须先确认其值是字符串；若 `type` 实际是对象、数组或其他非字符串值，必须按普通 JSON 对象继续递归处理或计入 JSON 文本，禁止直接拿它参与集合成员判断或其他依赖哈希的比较。
- 模型调用日志必须在 `request_logs` 中统一落库 `session_id`、`user_account_id`、`user_account_name`、`http_method`、`reasoning_level`、`attempt_count`、`ttfb_ms`、`duration_ms`、`tps`、`cache_read_tokens`、`cache_write_tokens`、`billing_multiplier`、`channel_price_input_per_1k`、`channel_price_output_per_1k`、`channel_price_cache_per_1k`、`channel_price_cache_write_per_1k` 等核心分析字段，禁止只在前端临时拼装。
- 对外 `/v1/*` 请求日志与计费中的缓存读取、缓存写入 token 必须仅来自上游 `usage` 中具备官方语义的显式字段；禁止从总输入 token、缓存 miss token 或非 usage 文本反推缓存读写。若上游只返回缓存 miss 或未返回缓存字段，则对应缓存读写字段必须保持未知或空值。
- 对外代理主链路必须可靠写入请求日志接收层；允许通过 Redis Stream、消息队列或等价可靠队列异步批量落库到 `request_logs`，也允许在队列不可用时回退同步落库。费用计算、余额扣减和账单记录必须通过可重试且幂等的后台任务完成，禁止在请求主链路重复扣费。仅全局最大请求 Token 预检允许在进入上游路由前执行请求 Token 估算。
- 系统设置必须支持配置全局最大请求 Token；当该值为 `0` 时表示不限制，若对外 `/v1/*` 请求估算 Token 超过该值，必须在进入上游路由前直接返回明确错误码与错误说明。
- 对外 `/v1/*` 请求 Token 预检必须优先使用快速估算；仅在估算值接近限制阈值时允许使用精确 tokenizer 复核，禁止让大上下文请求默认阻塞在高成本精确计数上。
- 对外 `/v1/*` POST 请求必须在应用层按系统设置限制请求体字节数；即使反向代理未提供或未限制 `Content-Length`，应用也必须在 JSON 解析前执行限量读取和拒绝。
- 对外 `/v1/chat/completions` 与 `/v1/responses` 必须支持独立请求体字节上限；当端点独立上限与 `/v1/*` 全局上限同时存在时，必须按更严格的正数上限执行。
- 对外 `/v1/*` 请求因超过全局最大请求 Token 被拒绝时，`request_logs` 仅允许记录请求元数据、估算 Token、错误码和错误原因，禁止落库请求正文、请求上下文或完整 `token_request_payload`。
- `request_logs` 的 `request_body_json`、`response_body_json`、`response_text` 等大字段必须受 payload 日志开关、流式响应持久化开关和最大记录字节数控制；高并发生产默认禁止保存完整成功请求/响应 payload。
- 请求日志中的 `metadata` 必须受独立字节上限控制；超过上限时仅允许记录字段摘要、键名数量、原始字节数等结构化摘要，禁止把完整大上下文或敏感正文通过 `metadata` 绕过 payload 日志开关。
- 流式响应用于日志或 Token 统计的文本缓存必须有独立字节上限；后台 Token 补全任务禁止携带完整大请求 payload，超出上限时必须跳过请求 payload 补全并保留主链路可用性。
- 对外 `/v1/*` 非流式代理响应必须在应用层按系统设置限制上游响应体字节数；超过上限时必须停止继续读取并向调用方返回明确错误，禁止为了 JSON 解析一次性读入无限制响应体。
- 后台日志中心与统计看板需要提供可导出的结构化结果时，必须优先复用 `request_logs` 的统一筛选口径生成 CSV 或时间维度聚合结果，禁止在前端基于当前页临时数据拼接“伪导出”。
- 管理端日志中心必须支持按用户筛选全量日志；普通用户端日志页必须只展示当前账号名下 API Key 产生的模型调用日志，并强制排除健康检查、后台测试和其他非用户调用日志。
- 管理端必须提供可检索的操作审计日志页面；用户、API Key、模型、系统设置、账户调账等关键后台写操作必须统一记录操作者、对象、摘要、时间与必要变更详情。
- 系统设置必须支持配置 `request_logs` 与管理端审计日志的保留天数，并由后台定时任务按统一配置自动清理；当保留天数为 `0` 时，表示禁用该类日志的自动清理。
- 健康检查类日志必须独立于用户请求/响应日志执行短周期清理，最长仅允许保留 6 小时；用户请求/响应日志不得因健康检查日志保留策略被提前清理。
- API Key 专属路由上下文必须至少包含 `route_mode`、`default_provider_id`、`manual_allow_fallback`、`allowed_provider_ids` 四项约束。
- 人类用户体系必须至少区分 `admin` 与 `user` 两类角色；默认关闭公开注册，管理员必须能在后台切换是否允许普通用户公开注册，并始终保留管理员后台创建账号的能力。
- 管理员账号禁止通过公开网页注册或无登录初始化页面创建；首个管理员必须通过服务器侧受控脚本创建，后续管理员仅允许由已登录管理员在后台用户管理中创建或提升。
- 外部 Bearer API Key 鉴权除密钥本身校验外，业务消费拦截必须统一以账户可用余额为唯一基准；禁止继续使用 API Key 级或账户级 Token 配额、金额配额、调用次数配额作为余额之外的消费拦截条件。
- Token 数量仅允许用于成本计算、日志统计、模型上下文限制、全局最大请求 Token 预检、模型最大输入/输出 Token 等技术安全限制，禁止在前端、接口或鉴权链路中作为用户/密钥消费配额展示或执行。
- 平台 API Key 管理必须同时支持自动生成与管理员自定义录入；自定义密钥必须按统一规则校验长度、前缀和字符集，且后台列表必须支持查看与一键复制当前可回显密钥。
- 管理端 API Key 管理页必须支持服务端分页、服务端筛选、后端导出以及批量启用、批量禁用、批量删除、批量渠道授权更新；禁止仅基于当前前端已加载页数据做本地假分页或伪批量操作。
- API Key 的可授权提供商集合只能由管理员配置；新建 API Key 默认必须授权全部提供商并启用自动同步；管理员必须同时支持单个密钥授权、批量统一授权以及一键将全部密钥恢复为“全部提供商授权”；提供商新增或删除后，处于自动同步模式的 API Key 授权集合必须自动更新。
- 普通用户端必须支持对当前账号名下 API Key 执行新增、编辑、启停、删除和明文复制；用户侧自助管理仅允许作用于本人名下密钥，禁止修改密钥归属账号以及账户级额度、余额、冻结金额等管理员治理字段。
- 普通用户端必须提供 API Key 详情页，并支持一键轮换当前账号名下 API Key；轮换后旧密钥必须立即失效，详情页必须仍可提供最新明文复制能力。
- 普通用户端必须支持日志 CSV 导出、账单 CSV 导出和接入自检入口；接入自检至少需要覆盖当前账号名下 API Key 状态、授权提供商、目标模型与可命中路由候选的可视化检查。
- 需要支持后续回显或复制的 API Key 明文禁止只做不可逆哈希存储；必须使用受控密钥进行加密存储。历史未存明文的旧密钥不得伪造回显，必须明确提示管理员通过编辑重置。
- 后台提供商价格录入、编辑和展示界面必须统一使用“每百万 token”作为用户可见单位；底层数据库与计费链路若继续复用 `*_per_1k` 字段，仅允许在界面或接口适配层做双向换算，禁止直接改写历史存量数据口径。
- 后台模型管理必须同时支持固定价、阶梯价和未定价三种价格形态；模型基础价字段仍可保留为当前计费与展示所需的代表值，但官方完整价格区间、缓存命中价、缓存写入价或等价分档信息必须以结构化数据附加保存并可在界面中查看，禁止只保留单一数值而丢失官方分档信息。
- 当官方价格页同时存在缓存命中、缓存写入、缓存存储或等价缓存口径时，必须在结构化价格数据中分别记录；界面对外展示的“缓存价”默认按缓存命中价口径换算，若该模型另有缓存写入或存储费用，必须同步展示并明确口径，禁止混用不同缓存口径。
- 后台提供商新增与编辑流程必须支持基于当前提供商配置请求上游 `/models` 拉取可用模型列表，并支持把选中的一个、多个或全部上游模型一键导入当前提供商模型配置。
- 后台提供商批量导入模板中的 `模型` 字段允许为空；`模型能力` 字段允许为空，若为空则默认按流式、文本、工具调用和图像理解能力创建导入模型配置，且不得引入图片生成作为导入模型能力字段。
- 后台提供商新增与编辑流程中的模型配置禁止维护模型基础输入价、输出价、缓存价和模型能力；提供商特有的渠道倍率、优先级、权重等路由字段必须继续在提供商配置中维护。
- 后台必须提供独立的模型聚合配置能力；模型基础价格、模型能力、速度标签、全局启用状态和可用渠道必须以“模型”为主视角统一维护，禁止仅依赖提供商页面分散维护。
- 对外 `/v1/*` 请求进入路由前允许按管理员启用的模型映射规则将源模型解析为一个或多个目标模型；多目标映射必须只在满足 API Key 权限、渠道授权、健康与容量约束的目标中选择，并在请求日志 trace 中记录源模型、目标模型与选择策略；映射阶段禁止再以模型能力字段一致性作为前置拦截条件。
- 模型管理必须维护单模型最大上下文窗口 Token 配置，并支持将同一个上下文窗口 Token 批量应用到选中的多个模型。
- 模型管理必须维护单模型最大输入 Token、最大输出 Token、是否支持工具调用、是否原生支持 `/v1/chat/completions`、是否原生支持 `/v1/responses` 等能力字段；路由、健康检查和对外能力声明必须以这些字段为准。
- 模型管理页可见的模型能力展示必须统一收敛为流式、仅文本、工具调用和图像理解四类，禁止在模型管理页把图片生成作为模型能力标签或开关展示。
- 提供商新增或编辑时新增的模型若不存在于模型管理，必须自动补入模型管理；从提供商移除某模型时禁止自动删除模型管理中的模型；从模型管理删除某模型时必须同步删除所有提供商中的该模型挂载；提供商管理中的“挂载模型数”必须按去重模型名统计，禁止把同一模型在多个提供商的挂载记录重复计为多个模型。
- 提供商中已挂载模型的模型基础价格、模型能力、速度标签、全局启用状态等模型共享配置必须以模型管理为准；提供商仅保留自身启用、倍率、优先级、权重等路由配置。
- 提供商级健康状态只能作为其启用模型健康度的聚合可用性展示，不得直接代表单模型健康；模型路由、健康筛选、能力验收和故障定位必须以 `provider_models.health_status` 及能力探针结果为准。前端展示必须区分“整体可用性”与“模型健康”，禁止统一使用“状态”造成语义混淆。
- 同一模型的渠道价格必须采用“模型基础价 × 渠道倍率”口径；倍率定义为单个渠道对单个模型的价格倍率，渠道最终价格必须按该公式换算。
- 模型平均倍率必须作为只读派生指标由已绑定渠道倍率自动计算，禁止作为独立可编辑字段；若需要改变平均倍率，必须通过调整对应渠道绑定关系或单渠道倍率实现。
- 用户端必须提供可用模型页面，并且仅允许展示当前账号已授权范围内可访问模型的价格、速度标签和可用渠道，禁止向普通用户暴露后台全量模型池。
- 普通用户端必须提供可访问的 `使用文档` 页面；该页面允许复用统一外部接入文档模板，但用户端导航中必须存在独立入口，且内容口径必须与管理端 `使用文档` 页面保持一致。
- 账户级余额可视化必须至少展示账户总余额、冻结金额、可用余额、已消费金额、累计充值金额和消费明细；账户余额必须以 `user_accounts` 共享金额池为唯一口径，同一用户名下所有 API Key 共用同一账户余额，禁止继续以名下 API Key 余额求和冒充账户余额。
- 管理端用户管理必须支持批量账户余额调整；批量调整仅允许作用于账户余额、冻结金额或等价余额治理字段，禁止继续提供调用次数、Token 或金额配额类批量调整入口。
- 告警中心必须提供订阅配置与主动提醒能力；订阅规则至少需要支持渠道异常、API Key 异常、账户预警和失败率异常四类告警的启停配置，并提供统一的事件流或主动提醒入口。
- 当 `APP_ENV` 为 `prod` 或 `production` 时，`SESSION_SECRET_KEY` 与 `API_KEY_ENCRYPTION_SECRET` 必须使用非默认高强度随机值；若仍为空、仍为占位值或长度不足，应用必须拒绝启动。
- 对外正式支持的 OpenAI 兼容能力必须与实际 `/v1/*` 路由完全一致；未正式开放的能力禁止继续保留在对外能力声明、主要筛选项或正式流量统计口径中形成“半实现”状态。
- 渠道或模型若对外宣称支持 OpenAI 兼容文本、流式、`/v1/responses` 或图片能力，必须分别完成独立验收；模型级 Chat Completions / Responses 协议支持禁止按模型名家族硬编码，未探测或新增模型默认两种协议都视为支持，只有健康检查、端点探针或明确配置失败时才允许收紧。
- 对外 `使用文档`、用户侧 API Key 接入示例以及 legacy OpenAI 图片端点错误提示，若涉及图片生成或图片编辑能力，必须统一明确声明本项目当前正式支持的公网接入方式为 `POST /v1/responses + tools=[{"type":"image_generation"}]`；文生图与编辑图都必须给出可直接复制的 Responses 示例，禁止继续让外部调用方从零猜测应使用 `responses` 还是旧版 `images/*`。
- 若对外额外实现 legacy OpenAI 图片端点兼容层（如 `/v1/images/generations`、`/v1/images/edits`），文档、示例和错误提示必须同时明确三件事：该能力只是对现有 `Responses + image_generation` 内核的兼容适配；正式推荐公网入口仍是 `/v1/responses`；当 `response_format=url` 且平台未提供对象存储托管时，只允许返回 data URL 兼容值，禁止伪装成临时公网文件地址。
- 后台健康检查、用户接入自检、playground 或其他验活链路若覆盖图片能力，必须将图像理解与图片生成分开构造探针并分开展示结果；页面、报告和日志预览禁止直接回显图片 base64 或完整二进制结果，只允许展示结构摘要、图片数量、格式与截断标记。
- 对外 `/v1/*` 流式图片生成请求在关闭完整流式响应持久化时，`request_logs` 仍必须保留安全的结果摘要，至少包含图片数量、格式、近似字节数、是否存在 partial image 与截断标记；允许额外落库如 `[生成了 N 张图片]` 的占位文本摘要，禁止因为不保存完整 SSE 正文而让流式生图日志完全失去可观测性。
- 对外 `/v1/*` 请求日志在补全 Token/费用时，若图片生成响应缺少上游 `usage`，必须优先保持未知或留空，禁止从 `[生成了 N 张图片]`、安全摘要文本或其他非真实输出正文反推 `completion_tokens`。
- 对外 `/v1/chat/completions` 与 `/v1/responses` 必须继续支持标准 SSE 流式返回，并在日志中保留 `is_stream`、首 Token 延迟和流式响应留痕能力，禁止为了简化实现而退回只支持一次性 JSON 返回。
- 对外 `/v1/chat/completions` 与 `/v1/responses` 默认必须按用户请求入口相对独立处理；进入上游时必须使用请求入口对应的原生 OpenAI 兼容端点，禁止在两者之间自动互转、预选互转或失败后互转重试。仅在显式启用 Responses→Chat 单向兼容适配模式时，允许 `/v1/responses` 请求转换为上游 `/chat/completions`；该模式必须默认关闭、只允许单向转换、不得影响原生 Responses 路由。
- 对外 `/v1/chat/completions` 请求必须只路由到原生支持 `chat/completions` 的模型渠道；对外 `/v1/responses` 请求默认必须只路由到原生支持 `responses` 的模型渠道。原生支持的判定必须优先来自健康检查或端点探测结果，未探测数据默认不作为拒绝依据；只有健康检查或明确配置已证明对应端点不可用时，才允许返回明确错误码与原因，禁止改投另一端点伪装兼容。若显式启用 Responses→Chat 单向兼容适配模式，可将配置允许的 Responses 请求投递到 Chat-only 上游，但响应、日志和能力声明必须明确这是兼容适配而非原生 Responses。
- 后台提供商模型挂载矩阵必须以挂载级协议配置作为单个提供商渠道下单个模型的端点协议权威来源；挂载级协议仅允许等价表达 `both`、`chat_completions`、`responses`，新增挂载默认使用 `responses`；历史提供商级 `protocol_type` 仅允许作为兼容字段存在，禁止作为单模型协议可用性的权威判断。
- 对外路由候选必须满足模型挂载级端点协议能力，Chat Completions 请求禁止命中 Responses-only 模型挂载，Responses 请求禁止命中 Chat-only 模型挂载。
- 对外 `/v1/responses` 图片请求以及携带 `previous_response_id`、`reasoning`、`text`、`include`、`store`、`truncation`、`background` 等原生 Responses 字段的请求，默认必须按原始 `responses` 协议透传到命中的原生 Responses 上游；禁止降级为 `/v1/chat/completions`。仅在显式启用 Responses→Chat 单向兼容适配模式且请求未携带无法适配的内置工具或加密状态字段时，允许转换为 Chat 请求；适配层必须维护 `previous_response_id` 对应的消息历史，并保持历史前缀不可变。
- 对外 `/v1/responses` 请求若包含 `previous_response_id` 或 `encrypted_content` 等上游状态上下文，且请求模型为模型别名或可映射模型时，必须优先映射回同一会话最近一次成功使用的实际目标模型；若该实际目标模型因权限、容量、健康、熔断或上游可恢复故障不可用，才允许按健康优先候选继续故障转移到其它可用目标，并必须在 trace 中记录 stateful 路由锁定与故障转移过程；禁止对该类请求执行端点互转适配。
- 对外 `/v1/chat/completions` 与 `/v1/responses` 的非流式请求若声明的最大输出 Token 超过系统长输出阈值，必须拒绝并要求调用方使用 `stream=true`；若超过模型最大输出 Token，必须在路由前拒绝。
- 对外工具调用是 `/v1/chat/completions` 与 `/v1/responses` 的标准协议能力，不作为模型级路由硬过滤条件；工具请求仍必须满足请求入口对应端点原生可用、权限、容量、流式、图像理解、图片生成等其它硬约束，禁止为了工具调用在 Chat Completions 与 Responses 之间互转。
- Responses→Chat 单向兼容适配层若启用，必须覆盖纯文本、流式文本、多模态图片输入、自定义 Function Calling 和 `previous_response_id` 会话续写；必须将 `instructions` 仅在首轮固化为单条 system 消息，历史消息读取后禁止重排或改写，工具输出必须紧跟对应 assistant `tool_calls` 且顺序一致。内置 `web_search` 默认返回明确错误，仅在显式启用搜索代理时允许把搜索结果注入最新 user 消息；`file_search` 与 `code_interpreter` 必须直接返回不支持错误。
- Responses→Chat 单向兼容适配层的会话快照必须受 TTL、单条快照字节上限和存储后端清理策略控制；数据库存储必须提供过期快照定时清理，Redis 存储必须使用 TTL，内存存储必须至少在读取时惰性清理；若请求显式 `truncation=disabled` 且未通过 `context_management` 显式请求 compaction，快照超过上限时必须返回明确错误，禁止静默丢弃上下文。
- 后台批量健康检查必须按 `chat/completions` 与 `responses` 分别执行原生工具调用探测并用于观测展示；工具探针失败禁止直接阻断正式工具请求路由，除非后续存在用户明确启用的独立健康门禁功能。
- 后台批量健康检查必须分别标记模型端点能力为原生支持 `chat/completions` 或原生支持 `responses`；工具调用、图像理解与文本端点能力都必须分别按端点记录探测结果，未进行探测时默认保持双协议可路由，禁止以跨端点共用探测结果作为正式支持能力展示。
- 后台手动提供商健康检查与"检查全部"批量健康检查在单个提供商内必须采用"同类探针并行、异类探针串行"顺序执行；同一阶段内允许按模型并行探测，但阶段完成后前端必须立即显示该阶段反馈，禁止全量结束后才统一回显过程。
- 后台手动提供商健康检查与"检查全部"批量健康检查必须设置 300 秒最小触发间隔；单个探针首次失败时仅允许立即重试一次，禁止无上限重试或高频重复探测上游。
- 后台模型配置页或模型挂载矩阵中的单模型即时测试必须在 10 秒总预算内返回所有已完成和超时结果；默认文本测试仅执行流式文本探针；同一能力只允许执行一次且禁止重试；测试端点必须按挂载级协议选择一个端点，优先选择 `responses`，仅当该挂载不支持 `responses` 时选择 `chat_completions`，禁止同一次单模型测试同时探测两个端点。
- 后台健康检查所有能力测试（文本、流式文本、工具调用、图像理解、图片生成）必须只执行一次，禁止失败后自动重试；文本测试必须默认只测试流式文本，禁止同时测试非流式和流式两种模式。
- 提供商模型挂载矩阵的协议字段 `protocol_type` 必须作为单个提供商下单个模型的端点协议权威来源；该字段仅允许 `responses`（默认）、`chat_completions`、`both` 三种取值；健康检查与能力测试必须根据该字段选择测试端点，优先测试 `responses`，若不支持则测试 `chat_completions`。
- 提供商模型挂载矩阵必须作为独立后台页面通过左侧导航进入，禁止继续作为提供商对象目录页内的附属卡片展示。
- 提供商模型挂载矩阵中的模型健康字段与可信度字段必须相互独立：模型健康仅表示模型是否可用，用户可见状态仅允许 `健康`、`异常`、`未检测`；可信度仅表示模型所在提供商返回内容是否正常可信，用户可见状态仅允许 `可信`、`异常`、`未检测`。两者的手动编辑、测试更新和异常原因展示禁止相互覆盖或混用。
- 提供商对象目录中的状态字段必须明确区分可用性与可信属性；`全部可用` 仅表示该提供商所有启用模型均可用，禁止解释为任一模型可用。可信属性必须由该提供商所有模型可信度汇总计算：全部可信显示 `可信`，一个及以上异常且非全部异常显示 `部分可信`，全部异常显示 `不可信`，全部未检测显示 `未检测`，部分未检测且未达到全部可信时显示 `部分可信`。
- 提供商模型健康、模型可信度和提供商可信属性只要不是正常态（健康或可信），前端必须在状态旁使用全站统一问号帮助图标展示原因；问号图标与 Tooltip 必须复用全站共享组件。
- 后台测试功能标准化必须由 `app/utils/test_features.py` 统一提供；前端测试功能选择器仅允许 `text_stream`、`vision`、`tools`、`image_generation` 四个选项，禁止提供 `text`（非流式文本）选项；后端 `normalize_test_features` 和 `phase_keys_from_test_features` 函数禁止在多个路由文件中重复定义，必须从共享模块导入；测试功能到测试阶段的映射禁止对 `text` 选项做特殊处理，禁止同时添加 `text` 和 `text_stream` 阶段键。
- 后台模型管理的模型级健康检测必须同时按"不同模型并行"和"同一模型不同绑定渠道并行"执行；模型聚合状态仅允许面向用户展示为"健康"或"异常"，且只要存在任一可用绑定渠道即为健康。
- 对外代理路由必须遵守 provider 容量配置；若 provider 达到活跃请求、活跃流式请求、QPS 或失败率上限，路由候选必须排除该 provider，无法找到可用候选时必须返回明确限流或服务不可用错误。
- 对外代理的模型映射选择和提供商候选选择必须统一采用健康优先主策略：先按硬约束筛除权限、能力、容量、熔断等不可用候选；若同一会话最近一次成功使用的实际模型或提供商仍在可用候选中，必须优先复用该实际目标；若近期实际目标不可用或请求无明确会话标识，模型映射必须按健康层级、当前负载和路由得分选择目标模型，提供商候选必须按健康度、当前负载、权重和路由得分等因素做分发，避免所有请求集中到单一模型或提供商；新会话禁止查找或套用会话粘性。
- 对外代理的模型映射候选评估必须透传本次请求的能力约束，包括流式、视觉、工具调用、图片生成、原生 `chat/completions` 与原生 `responses`；禁止先按普通文本能力选择映射目标，再把能力不匹配问题延迟到二次路由或上游失败阶段暴露。
- 前端界面、使用文档和对外能力说明中，历史 `route_mode` 字段仅允许作为内部兼容字段存在；用户可见策略必须统一展示为“健康优先”，禁止继续把手动优先、故障切换、权重分流、会话粘滞、优先级等旧策略作为可选主策略暴露。模型映射配置禁止继续维护或对外暴露映射策略、目标模型优先级、目标模型权重字段；目标模型选择只允许由健康优先算法结合权限、能力、容量、健康、负载、路由得分和同会话近期成功目标决定。
- 对外代理候选选择必须把提供商并发、流式并发、QPS 和 RPM 容量作为硬约束；任一提供商达到对应上限时，即使健康、权重、会话粘性或其它条件满足，也禁止被选取。
- 对外代理路由最大候选数必须支持全局系统设置，默认值为 `10`，路由设置页面必须允许管理员配置 `1-500` 的自然数；API Key 级 `max_candidate_count` 仅在显式配置时覆盖全局值，未配置时必须继承全局设置。
- 提供商默认容量必须为最大活跃请求 20、最大流式请求 10、最大 QPS 20、每分钟最多请求 20；API Key 默认 QPS/RPM 必须为 20；系统全局、API Key、账户和提供商维度的活跃并发默认必须为 20，流式并发默认必须为 10。统一调整已有容量数据时只能执行非破坏性回填，禁止清空、删除或重建业务数据。
- 提供商最大重试次数必须统一默认并回填为 `2`；新增提供商默认最大重试次数必须为 `2`；提供商最大重试次数必须小于等于全局最大重试次数；全局最大重试次数不得小于 `2`。
- 对外代理重试必须区分可恢复与不可恢复错误：请求结构错误、鉴权授权失败、模型不存在、能力不支持、请求入口无原生端点支持等逻辑或功能错误禁止通过等待反复重试；上游临时故障、网络抖动、超时、限流、容量不足、余额或额度可能刷新等可恢复错误才允许按路由策略等待、换渠道或换映射模型重试。
- 对外 `/v1/*` 请求在候选因容量、失败率、熔断、模型临时不健康或上游临时失败而耗尽时，必须在响应尚未建立前按当前路由策略重新选择候选并等待重试；正常模式默认最大等待窗口为 600 s，路由设置必须允许在 0-600 s 内配置，超过窗口后必须停止内部重试并返回明确服务不可用错误；仅在显式启用无限重试模式时，允许对非项目自身问题绕过最大等待窗口并持续等待重试。
- 对外代理请求获取 provider 容量租约后，必须在请求正常结束、上游异常、客户端中断或流式生成器退出时释放租约，禁止异常路径遗留活跃计数。
- 对外 `/v1/*` 实时并发计数、并发租约和短窗口 QPS/RPM 限流必须使用 Redis 等共享状态实现，禁止在 Gunicorn 多 worker 生产链路中仅依赖进程内存计数。
- 生产环境中会影响 `/v1/*` 路由、模型列表、provider 状态或权限判断的一致性缓存必须使用 Redis 等共享缓存；仅低风险、可短期不一致且不影响正式流量决策的派生数据允许退回进程内缓存。
- 高频读取路径的缓存必须优先使用可配置 TTL，并在系统监控或等价观测入口暴露命中率、L1 命中率和当前缓存条目数；写操作必须主动失效受影响缓存，禁止通过硬编码极短 TTL 代替失效机制而长期拉低缓存命中率。
- 对外 `/v1/chat/completions` 与 `/v1/responses` 用户请求必须尽量保持上游 prompt cache 命中率：代理层禁止无业务必要地重排稳定前缀、插入每次变化的系统内容、改写用户稳定上下文或破坏同一会话路由粘性；仅在官方协议明确支持且不改变调用语义时，才允许补充 provider 专属 prompt cache 控制字段。
- Redis 并发租约释放必须具备幂等性；释放前必须确认租约存在，并通过原子脚本或等价机制同时删除租约和递减相关计数，禁止重复释放导致计数为负。
- 对外 Bearer API Key 鉴权缓存禁止保存原始明文 Key；缓存 Key 必须基于 SHA256 哈希，并且 API Key、用户账户、授权 provider、模型白名单、额度或余额等关键写操作必须主动失效受影响缓存。
- SSE 流式请求必须使用独立流式并发租约，并在生成器 `finally` 中释放全局/API Key/账户/provider 容量租约；客户端取消必须记录 499 或等价状态且不得计为 provider 失败。
- SSE 流式请求必须支持连接上游超时、首 Token 超时、空闲超时和最大持续时间控制；流式 read timeout 可保持不限制，但必须在生成器层终止异常长连接，并在日志或 trace 中保留首 Token 延迟、总耗时和终态错误码。
- 生产可观测性必须以 Redis 全局活跃计数、数据库健康、连接池状态、后台任务积压、请求错误率和 provider 失败率为核心指标；`RuntimeStateService` 仅允许作为当前 worker 进程内参考，禁止作为多 worker 全局容量依据。
- 后台任务积压类监控指标必须只统计确实需要对应后台任务处理的正式候选数据；Token/计费补全积压仅允许统计正式模型调用日志，禁止把鉴权失败、健康检查、压测插入、审计或其它非计费日志混入该口径。
- Token/计费补全积压与对应 backfill 候选必须继续排除 `api_client_key_id` 为空的内部请求日志；内部管理、playground、自检或其它无外部 API Key 结算语义的请求，禁止仅因 `billing_finalized_at` 为空而反复进入补全积压或重试队列。
- 运维监控页面展示资源占用时，必须明确区分宿主机/分区级指标与当前项目/进程级指标；禁止把宿主机分区已用、可用或剩余容量标成项目占用。
- 运维监控接口默认禁止采集项目目录递归占用；若后续重新启用项目目录大小统计，必须经用户确认后作为可选低频能力实现，禁止进入默认实时轮询链路。
- 运维监控接口按分区采集数据库、Redis、流量、趋势、提供商、后台任务和项目进程等指标时，单一分区采集失败必须局部降级并在响应中暴露错误摘要；禁止因非核心分区异常导致整个监控接口返回 500。
- 监控类异常必须写入统一 `alert_events` 告警事件流，并保留时间、对象、摘要、严重级别和处理状态；禁止另建第二套脱离告警中心的监控告警表。
- 内容完整性监控若发现同一提供商在 10 分钟内高风险命中不少于 3 次，必须自动将该提供商及其挂载模型标记为内容完整性 `blocked` 并从路由候选中排除，同时继续通过统一 `alert_events` 告警事件流记录处置状态；禁止只告警不隔离。
- 对外代理候选选择必须排除内容完整性评分 `content_integrity_score <= 20` 的提供商；该类提供商必须通过管理员恢复评分或状态后才能重新进入候选，禁止仅靠权重、价格或健康度继续参与普通路由。
- 内容完整性总开关关闭时，代理内容检测、健康探针内容判定和监控自动隔离都必须同时停用；禁止在关闭状态下继续产出内容高风险告警或自动处置。
- 内容完整性防护必须作为相对独立的功能模块维护：模块配置、检测接口、内部提供商检测和外部渠道检测必须统一封装在内容防护模块接口中；其它页面、路由、服务或监控链路需要读取或触发内容防护能力时，必须复用该模块服务或统一数据结构，禁止在页面或其它业务层重复拼装检测规则、配置口径或外部渠道探测逻辑。
- 内容完整性文本检测规则必须通过内容防护模块的统一规则注册表读取、保存和执行；禁止在代理链路、健康探针、前端页面或其它业务层散落硬编码可增删的文本检测规则。
- 对外 `/v1/*` 流式响应执行内容完整性防护时，只有在尚未向客户端发送任何 SSE 数据前允许因高风险命中切换候选 provider；一旦已发送下游数据，禁止切换 provider 或拼接其它 provider 内容，必须发送标准 `event: error` 并以 `data: [DONE]` 收尾，同时记录污染证据和终态日志。
- 低信任 provider 的流式内容完整性预检默认必须采用“字节阈值或最大等待时间先到即检测”的双阈值模式，默认最大等待时间为 300ms；首段放行后必须继续按 chunk 滑动窗口检测，并在日志中记录检测耗时、缓冲等待、重试 provider 数和最终处理策略。
- 本地或 CI 高并发压测必须使用独立 Redis 端口或独立逻辑库隔离 benchmark 状态，并且必须在压测前后清理专用 benchmark API Key 的 `request_logs` 与 Redis 队列；压测结束前必须等待请求日志落库队列和 Token finalize 后台任务进入空闲，禁止复用共享 Redis 状态或在强制终止进程后直接根据残缺日志判断系统吞吐。
- 思维等级字段必须统一收敛为 `无`、`low`、`medium`、`high`、`xhigh` 五档；其中 `无` 表示不指定，若请求参数显式传入思维等级，则日志与展示链路必须按原值透传记录。
- 对外 `/v1/*` 代理链路为兼容官方协议而补充 `reasoning_effort`、`reasoning.effort` 等标准字段时，必须保留调用方原始思维等级字段与未识别扩展参数，禁止因日志识别或协议适配删除原始请求字段。
- `使用文档` 页面必须优先面向外部接入用户，聚焦生产接入地址、鉴权方式、标准 `/v1/*` 路径、第三方客户端配置与一键复制能力；禁止继续以后台模块说明或帮助中心杂项为主体结构。
- `使用文档` 页面中的外部接入地址必须由统一配置或当前访问地址生成，禁止在模板中硬编码固定公网 IP、域名或协议。
- 外部接入文档中的代码示例块、环境变量块等教程型代码展示区域必须统一使用纯黑色背景和白色文字，禁止继续使用偏蓝、灰蓝或其他非纯黑底色。

## 5. Git 与 GitHub 提交规范

- 需要将项目上传或同步到 GitHub 时，必须先检查本地仓库状态、当前分支、远程配置和提交人信息，再执行推送。
- 本地存在未提交改动时，必须先整理为明确提交，再进行远程绑定或推送，禁止带未提交工作区直接发布。
- 若项目尚未配置远程仓库，必须先新增 `origin` 后再执行推送。
- 本项目按单人维护模式处理 GitHub 同步：例行提交和推送时禁止主动执行 `git fetch`、`git pull`、`git merge`、`git rebase` 或 unrelated histories 合并流程。
- 需要执行标准上传流程时，必须按以下顺序操作：`git status -sb`、`git branch --show-current`、`git remote -v`、`git log --oneline -3`、`git add .`、`git commit -m "<清晰提交信息>"`、`git push -u origin main`。
- 若推送时出现 `fetch first`、`non-fast-forward`、`rejected` 或远程历史不一致提示，必须暂停并向用户说明远程状态异常；未获用户明确确认前，禁止自动合并、变基或强制推送。
- 仅在用户明确要求覆盖远程历史时，才允许使用 `--force-with-lease` 或等效受保护强制推送；禁止使用无保护的 `--force`。

cd D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt
git status -sb
git branch --show-current
git remote -v
git log --oneline -3

# 1. 先添加所有文件
git add .

# 2. 使用简短的第一行信息
git commit -m "填写标题"

# 3. 然后补充详细说明（使用 git commit --amend）
git commit --amend -m "填写标题" -m "详细更新内容："

git push -u origin main

如果 git remote -v 发现还没绑远程，先执行：

git remote add origin https://github.com/howarezhh/auto-gpt.git


## 6. Linux 部署与云服务器启动规范

- 面向 Ubuntu 云服务器的一键部署脚本必须放在项目根目录；`start_aliyun.sh` 作为历史兼容的通用部署底座保留，云厂商专属入口允许用包装脚本注入默认参数，例如腾讯云使用 `start_tencent.sh`，例行更新使用 `update_tencent.sh`。
- Linux 部署脚本必须继续复用项目根目录下的 `.venv`、`.env`、`requirements.txt` 和 `data/`，禁止额外引入第二套独立运行目录。
- 一键部署脚本在项目根目录不存在 `.venv` 时，必须先创建 `.venv`，并使用国内镜像源安装依赖。
- 一键部署脚本必须默认将应用绑定到 `127.0.0.1:8000`，并通过 `nginx` 在 `80` 端口对外反向代理，禁止默认直接将 `uvicorn` 裸暴露到公网。
- 一键部署脚本必须写入并启用统一命名的 `systemd` 服务 `aotu-gpt.service`，保证后端开机自启和常驻运行。
- 生产环境 `aotu-gpt.service` 必须使用 `Gunicorn + UvicornWorker` 多 worker 启动，并默认通过 `WEB_CONCURRENCY`、`GUNICORN_TIMEOUT`、`GUNICORN_KEEPALIVE`、`GUNICORN_GRACEFUL_TIMEOUT` 控制 worker 数与超时参数；Windows `run.ps1` 仅允许作为本地开发启动方式。
- 高并发生产部署必须支持将 Web 请求进程与后台队列消费进程分离；Web 进程允许通过 `ENABLE_BACKGROUND_WORKERS=false` 只负责接收与转发请求，日志落库、Token 补全、计费结算等后台任务必须由独立 worker 进程（如 `scripts/run_background_workers.py`）消费 Redis 队列。
- 当 Web 请求进程关闭后台 worker 时，必须保留请求日志可靠入队能力，并确保至少存在一个独立后台 worker 进程负责消费队列；禁止在无人消费队列的生产形态下长期运行。
- 2C2G 轻量云服务器默认必须采用保守资源参数：Web worker 不超过 2 个，数据库连接池与上游连接池必须可通过部署入口下调，且必须优先启用独立后台 worker 服务，避免后台任务挤占对外请求响应。
- 生产环境 Web worker 启动阶段禁止执行 `create_all`、自动 DDL 或内置迁移逻辑；数据库结构变更必须通过独立迁移或初始化步骤完成后，再启动或滚动发布 Web worker。
- 一键部署脚本默认禁止自动执行数据库初始化或迁移；仅允许在显式设置 `RUN_DB_INIT=1` 时执行独立数据库初始化，且执行前必须先生成 PostgreSQL 备份。
- 云服务器例行更新项目代码、依赖、静态资源或服务配置时，禁止同步更新、初始化、迁移、清空或重建云服务器数据库；只有用户明确要求执行数据库变更时，才允许按独立迁移流程和备份要求处理。
- 云服务器例行更新脚本必须支持 GitHub 直连失败后的可配置镜像拉取；镜像源必须通过环境变量可覆盖，禁止把单一镜像作为不可替换依赖。
- 生产环境 `aotu-gpt.service` 必须设置文件描述符限制，当前统一使用 `LimitNOFILE=1048576`。
- 一键部署脚本必须写入并启用统一命名的 `nginx` 站点配置 `aotu-gpt`，并在启用前移除默认站点，避免 `server_name _` 冲突。
- 一键部署脚本写入的 `nginx` 反向代理必须保留 SSE 透传配置：`proxy_buffering off`、`proxy_request_buffering off`、`proxy_read_timeout 600s`、`proxy_send_timeout 600s`。
- 若 `.env` 中 `LOCAL_PROXY_API_KEY` 为空，一键部署脚本允许自动生成随机值并写回 `.env`。
- 数据库性能优化必须包含关键复合索引：`provider_models` 表的 `(provider_id, model_name)` 索引用于路由匹配，`request_logs` 表的多维度查询索引用于日志分析，所有启用状态查询必须使用部分索引（`WHERE enabled = true`）避免扫描禁用数据；数据库连接池大小必须根据 `web_concurrency` 动态计算，默认为 `workers × 10 连接`，生产环境建议 40-60 个连接。
- 热点数据查询必须实现多级缓存：L1 内存缓存（TTL 60秒）用于 `ModelCatalog` 等频繁查询，L2 Redis 缓存（TTL 300秒）用于跨 Worker 共享，缓存更新/删除时必须主动调用 `invalidate()` 保证数据一致性；高频认证查询必须使用异步缓存避免锁竞争。
- 日志写入必须采用异步批量模式：请求日志添加到内存队列后立即返回，后台任务每秒或每100条批量写入数据库，应用关闭时必须优雅刷新队列中的所有日志，避免数据丢失；禁止在热路径同步写入日志阻塞主请求流程。
