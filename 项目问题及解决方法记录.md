---

**19. 启动时 8000 端口被占用，FastAPI 无法启动**

问题现象：

- 启动 `run.ps1` 后，`uvicorn` 报错
- 提示 `error while attempting to bind on address ('127.0.0.1', 8000)`
- Windows 报错 `[Errno 10048] 通常每个套接字地址...只允许使用一次`

原因：

- 本机 `8000` 端口已被其他程序占用
- 原始脚本只会清理“本项目自己之前启动的旧进程”，不会处理其他程序占用

解决方法：

- 已增强 `run.ps1`
- 启动前会先清理本项目旧 `uvicorn` 进程
- 默认不会强杀其他应用
- 如果 `8000` 被其他程序占用，则自动切换到 `8001` 之后的空闲端口启动

当前更安全的原则：

- 只自动停止本项目自己的旧进程
- 不默认强杀其他应用
- 首选端口不可用时自动避让到备用端口

---

**20. 直接按端口杀进程会误伤其他应用**

问题现象：

- 为了解决端口占用，若直接按 `8000` 端口查 PID 后强制结束
- 可能会把浏览器、代理工具、其他本地服务一起杀掉

原因：

- “端口占用者”并不一定属于当前项目
- 开发环境中多个程序可能复用常见端口

解决方法：

- 已把启动脚本调整为安全默认值
- 仅清理本项目自身旧进程
- 对其他程序占用端口时，默认不终止对方
- 改为自动切换备用端口启动

补充：

- 如果确实有强制释放首选端口的需要，可以在脚本中手动开启对应开关
- 但不建议作为默认行为

---

**21. provider 的 base_url 和 api_key 是否会覆盖官方 GPT 配置**

结论：

- 不会“覆盖官方 GPT 本身”
- 它们只是本项目内部保存的“上游提供商配置”

实际工作方式：

- 客户端先请求本项目本地代理，例如 `/v1/chat/completions`
- 本项目再根据路由规则选择一个 provider
- 然后把请求转发到该 provider 的 `base_url`
- 并携带该 provider 对应的 `api_key`

这意味着：

- 如果某个 provider 配的是 OpenAI 官方地址，那么就走官方
- 如果配的是其他提供商，那么就走那个提供商
- 本项目是“代理与路由层”，不是修改官方 SDK 配置

---

**22. 健康检查多久执行一次**

问题说明：

- 后台自动健康检查由调度器定时执行
- 数据库默认值是 `60` 秒

本次排查结果：

- 当前数据库里的实际配置不是默认值
- 当前项目实际设置为 `45` 秒执行一次

原因：

- 健康检查间隔来自 `app_settings.health_check_interval_sec`
- 不是固定写死在代码里
- 如果在页面设置里改过，就会按数据库值执行

解决方法：

- 已确认调度逻辑工作正常
- 如果想降低日志频率，可在“路由设置”中：
  - 关闭自动健康检查
  - 或把检查间隔改大，例如 `300` 秒

---

**23. 日志中心时间显示是下午 15 点，而不是当前 23 点**

问题现象：

- 日志中心显示时间类似 `2026/4/23 15:03:42`
- 但本地实际时间是北京时间晚上 `23` 点左右

原因：

- 后端数据库使用 `datetime.utcnow()` 存储时间
- 前端之前直接 `new Date("YYYY-MM-DD HH:mm:ss")`
- 浏览器没有按“UTC 存储、本地时区显示”正确处理，导致显示偏差

解决方法：

- 已修正前端时间格式化逻辑
- 现在会把后端返回的 UTC 时间按本地时区正确显示

结果：

- 日志、会话回放、相关页面的时间显示已统一

---

**24. 日志中心看起来在频繁自动刷新**

问题现象：

- 日志页面不断出现新记录
- 容易误以为页面在自动轮询刷新

原因：

- 页面本身没有自动轮询代码
- 真正频繁新增的是后台自动健康检查日志
- 每一轮健康检查会写入：
  - `health_check_provider`
  - `health_check_model`

因此表现出来像“日志页一直在刷”

解决方法：

- 已为日志接口增加“排除健康检查日志”能力
- 已让日志中心默认隐藏健康检查日志
- 需要查看巡检日志时可取消勾选“隐藏健康检查日志”

补充：

- 本次还加了前后端防缓存处理，避免旧页面或旧脚本干扰

---

**25. 隐藏健康检查日志按钮勾选后无法取消**

问题现象：

- “隐藏健康检查日志”勾选后正常生效
- 但取消勾选后列表仍然没有显示健康检查日志

原因：

- 后端接口默认 `exclude_health_checks=true`
- 前端最初只在勾选时传 `true`
- 取消勾选时没有显式传 `false`
- 导致后端继续使用默认值，看起来像“取消无效”

解决方法：

- 已修正前端参数传递逻辑
- 现在无论勾选还是取消，都会显式传参：
  - 勾选：`exclude_health_checks=true`
  - 取消：`exclude_health_checks=false`

结果：

- 现在可以正常切换显示/隐藏健康检查日志

---

**26. 日志中心“刷新”按钮点击无效**

问题现象：

- 点击“刷新”按钮后，用户感觉没有任何反馈
- 尤其在数据没有明显变化时，像是按钮失效

原因：

- 之前虽然绑定了刷新逻辑，但缺少明显的可视反馈
- 同时浏览器缓存或接口缓存会让“刷新”不够直观

解决方法：

- 已将日志刷新改为强制刷新模式
- 每次刷新请求都会追加时间戳参数，绕过缓存
- 页面增加“最近刷新时间”显示
- 手动点击刷新后会弹出提示，显示当前日志条数

结果：

- 现在点击“刷新”后能明确看到：
  - 刷新时间更新
  - 刷新提示弹窗
  - 最新数据重新请求

---

**27. 调用测试页面勾选 stream 后，结果不能实时输出**

问题现象：

- 在“调用测试”页面勾选 `stream`
- 模型虽然走的是流式接口
- 但前端页面仍然要等全部响应结束后才一次性显示

原因：

- 前端之前统一使用 `await response.text()`
- 这会一直等到完整响应结束
- 即使后端返回的是流，也被前端一次性读完了

解决方法：

- 已为调用测试页面增加真正的流式读取逻辑
- 当前端勾选 `stream` 时：
  - 使用 `response.body.getReader()` 按分片读取
  - 逐步解析 SSE 数据
  - 边收到边更新“结果输出”区域
- 流结束后再渲染完整结果卡片

结果：

- 现在调用测试页已经支持真正实时流式显示
- 非流式请求仍保持原有一次性返回逻辑

---

**28. 用户希望“只看代码判断能不能提供商”，而不是实际运行项目**

问题现象：

- 用户明确要求先不要启动项目
- 只基于代码判断当前项目是否具备真正的提供商能力

原因：

- 提供商项目的核心不是“页面能打开”
- 而是要看后端是否真正实现了统一入口、路由分发、鉴权转发、错误透传、流式转发等关键链路

解决方法：

- 调整判断方式，不以运行页面为准
- 重点核查以下代码能力是否齐全：
  - 是否存在统一代理入口
  - 是否支持不同供应商的请求头与 Base URL 适配
  - 是否支持流式转发
  - 是否支持通用 OpenAI 风格路径转发
  - 是否具备日志、健康检查、配置管理

结果：

- 后续对“能不能提供商”的判断以代码实现完整性为主
- 避免把“前端可访问”误判为“后端提供商可用”

---

**29. 统一 URL 是否真的可以对外部提供各种大模型调用**

问题现象：

- 用户重点追问：外界是否真的可以只通过一个统一 URL 调用各种 GPT/AI 大模型
- 关心项目是否只是管理后台，还是已经具备实际代理能力

原因：

- 很多所谓“提供商”只做了配置界面，没有实现统一的兼容代理层
- 如果后端没有 OpenAI 兼容接口、通用子路径转发和供应商适配，就不能算真正可对外提供统一入口

解决方法：

- 已将后端代理能力补齐为统一入口模式，重点包括：
  - OpenAI 兼容的 `chat/completions`
  - `responses`
  - `embeddings`
  - 通用 `POST /v1/{subpath}` 转发
  - 流式 SSE 转发
  - 供应商 API Key / Base URL / 模型映射管理

结果：

- 该项目的设计目标已经从“后台管理面板”升级为“统一模型提供商入口”
- 外部系统理论上可以通过统一地址访问不同上游模型
- 但最终是否完全可用，仍取决于各供应商接口差异是否都已在适配层处理

---

**30. 提供商是否需要自己实现上下文传输和上下文管理**

问题现象：

- 用户询问：上下文管理到底应该由提供商负责，还是由某个渠道自身的对话机制负责

原因：

- 提供商和聊天应用的职责容易混淆
- “代理层”和“会话层”并不是同一个东西

解决方法：

- 明确职责边界：
  - 提供商核心职责是鉴权、路由、转发、日志、限流、监控、统一协议适配
  - 对话上下文通常由调用方在请求体中携带并维护
  - 若上游模型平台本身有会话 ID / thread / response ID 机制，则提供商只负责透传
- 如果业务需要“统一会话管理”，则应在提供商之上额外实现会话存储层，而不是把基础代理层和会话产品层混为一体

结果：

- 项目定位被进一步明确：
  - 默认是“模型代理层”
  - 不是完整的“多轮会话业务系统”
- 上下文应优先由外部调用方维护，提供商只做必要透传和可选记录

---

**31. 用户对前端配色和布局不满意，认为现有页面布局混乱**

问题现象：

- 用户多次指出前端布局“很乱”
- 希望严格参考提供的图片风格
- 明确要求整体配色以白色、浅色为主

原因：

- 之前的页面更偏向功能堆叠式管理后台
- 缺乏统一的视觉层次、留白、卡片间距、主次信息组织和风格约束
- 前端优化如果只改颜色、不重做布局结构，通常达不到用户预期

解决方法：

- 明确前端重构目标：
  - 整体切换为纯白、浅色体系
  - 参考目标图片重做页面布局
  - 统一按钮、表格、卡片、导航、表单的视觉语言
  - 强化间距、分区、状态反馈与交互层次

结果：

- 前端后续不应只做“样式修补”
- 应按完整设计重构的标准推进

---

**32. 管理页面及其它页面按钮点击后缺少反馈**

问题现象：

- 用户指出多个页面中的按钮点击后“没有一点反馈”
- 包括管理页及其它页面都存在相同体验问题

原因：

- 虽然按钮可能已绑定实际请求逻辑
- 但缺少以下体验层反馈：
  - 按下态动画
  - hover / active 状态
  - loading 态
  - 成功 / 失败提示
  - 禁用态与防重复点击

解决方法：

- 后续交互要求已明确为每个功能按钮补齐完整状态反馈：
  - 点击缩放或波纹动画
  - 请求期间 loading 图标或文字
  - 请求中禁用重复提交
  - 成功 / 失败 toast 提示
  - 表单按钮、刷新按钮、保存按钮统一交互规则

结果：

- 前端验收标准不再只是“按钮能触发请求”
- 而是“每个可点击操作都有明确视觉反馈和状态变化”

---

**33. 用户追问统一地址如何对外访问，项目需要明确实际入口**

问题现象：

- 用户明确问到：
  - 统一地址是什么
  - 外部应该如何访问

原因：

- 若项目没有把“管理后台地址”和“API 代理地址”区分清楚
- 用户很容易不知道实际对外调用入口

解决方法：

- 明确提供商至少应提供两类地址说明：
  - 后台管理地址：用于配置供应商、查看日志、测试调用
  - 对外 API 地址：用于第三方程序按统一协议直接请求
- 文档层面应明确示例：
  - 基础地址
  - 认证方式
  - 典型调用路径
  - 示例请求体

结果：

- 项目不仅要“有接口”
- 还必须把统一入口文档化，否则对外不可用性很高

---

**34. 用户要求按 skills 机制查找前端界面相关能力并安装**

问题现象：

- 用户要求根据 skills 逻辑查找适合前端界面重构的 skill
- 并希望下载安装到指定技能目录

原因：

- 当前任务已经超出普通样式修改
- 需要借助专门的前端设计类 skill 提升重构质量

解决方法：

- 已按 `find-skills` 逻辑执行前端相关 skill 搜索
- 已成功安装并确认以下技能存在于全局技能目录：
  - `frontend-design`
  - `frontend-design-ultimate`
  - `tailwindcss`

结果：

- 后续进行前端重构时，可直接复用这些技能的工作流
- 减少纯手工摸索式改版

---

**35. 部分前端技能安装失败或目标目录理解存在歧义**

问题现象：

- 某些搜索到的 skill 没有成功安装
- 同时用户提到“下载保存到图片目录下面”，但未给出明确图片目录路径

原因：

- 技能生态中不同仓库的 skill 命名不一致
- 某些仓库拉取超时，某些 skill 名称本身不存在
- `skills add` 默认安装到全局技能目录，不会自动复制到任意业务目录

解决方法：

- 已区分成功与失败的安装结果
- 并明确当前默认安装位置为：
  - `C:\Users\32863\.agents\skills`
- 若需要复制到其它目录，必须提供准确目标路径

结果：

- 已避免把“搜索到 skill”误认为“已成功安装”
- 也避免了把“全局技能目录”和“项目图片目录”混淆

---

**36. 全局路由策略的 4 种模式含义不清晰，容易被管理员配错**

问题现象：

- 管理员在“路由设置”页只能看到：
  - `manual`
  - `failover`
  - `weighted`
  - `sticky`
- 但页面没有清楚解释每种模式到底如何命中、适合什么场景、失败后是否会切换

原因：

- 后端虽然已经实现了 4 种路由模式
- 但前端设置页缺少面向管理者的人类说明文案
- 造成“有能力、但不可理解”的配置问题

解决方法：

- 在“全局路由策略”区域增加 4 张模式说明卡
- 对每种模式补充：
  - 命中方式
  - 适用场景
  - fallback 行为
  - 风险提示
- 同时把下拉框文案改成中英混合说明，而不是只显示英文枚举值

结果：

- 管理员不需要看源码，也能理解四种模式的区别
- 显著降低了路由策略误配的概率

---

**37. 路由设置页无法直观看到“当前实际命中逻辑”**

问题现象：

- 即使已经设置了：
  - 路由模式
  - 默认提供商
  - `manual_allow_fallback`
- 页面仍然无法直观看出：
  - 当前首选会命中谁
  - 失败后是否会继续切换
  - 没有默认提供商时会退化成什么逻辑

原因：

- 设置项是静态表单
- 但真实命中逻辑是“模式 + 默认提供商 + fallback 开关”共同决定的
- 缺少动态解释面板，管理员只能靠猜

解决方法：

- 在设置页新增“当前实际命中逻辑说明”区域
- 根据当前表单值实时生成：
  - 当前模式
  - 当前默认提供商
  - fallback 开关状态
  - 分发步骤说明
  - 风险警告
- 对以下高风险场景给出明确提示：
  - `manual` 模式但未设置默认提供商
  - `manual` 模式关闭 fallback
  - `failover` 模式未设置默认提供商

结果：

- 管理员在保存前就能看到真实生效逻辑
- 避免“以为会自动切换，实际不会切换”的误判

---

**38. 日志中心部分请求没有记录 Token 用量**

问题现象：

- 日志列表中有些 AI 对话记录的 `Token` 字段显示为 `-`
- 特别是在以下场景中更明显：
  - 流式输出
  - 某些 OpenAI-compatible 提供商
  - 用户取消请求
  - 上游兼容实现未返回 `usage`

原因：

- 原实现只依赖上游响应中的 `usage`
- 一旦上游不返回 `usage`，本地就不会补算 token
- 所以日志会出现空值，导致统计不完整

解决方法：

- 新增后台 token 补算服务 `TokenUsageService`
- 对缺失 token 的日志，在写入后异步补算：
  - `prompt_tokens`
  - `completion_tokens`
  - `total_tokens`
- 计算依据优先级为：
  - 先用上游原始 `usage`
  - 若无 `usage`，再基于请求体和回复文本本地精确计算
- 同时增加定时回填任务，自动修复历史遗漏日志

结果：

- 绝大多数 GPT 对话日志都能补齐 token
- 后台统计、会话回放、日志聚合结果更完整可靠

---

**39. Token 补算不能影响用户流式输出体验**

问题现象：

- 用户明确要求：
  - 不能影响流式输出速度
  - 不能增加明显延迟
  - 不能占用过多 CPU 和内存

原因：

- 如果在主请求链路中同步计算 token
- 尤其是流式场景下，会拖慢返回速度
- 与“像直接调用大模型一样”的体验目标冲突

解决方法：

- 采用“主链路直通，后台异步补算”的策略
- 新日志写入后，延迟约 `250ms` 触发一次单条异步补算
- 再由调度器每 `15s` 做一轮轻量回填，补旧数据和漏网数据
- 流式链路中仅额外收集用于计算的文本，不在 SSE 返回链路里做阻塞式 token 统计

结果：

- 用户首包和流式输出体验不受明显影响
- token 统计仍能较快补齐
- 兼顾了体验、准确性和资源消耗

---

**40. 流式场景下如果只保存截断文本，会导致 completion token 不准确**

问题现象：

- 项目原本为了控制日志体积，只保存受大小上限限制的 `response_text`
- 但如果直接拿这份截断文本去计算 token
- 长回复的 `completion_tokens` 会被低估

原因：

- “日志展示文本”与“token 计算文本”是两个不同目标
- 展示可以截断，但计算不能只靠截断内容

解决方法：

- 在流式处理中额外维护一份仅用于 token 补算的完整输出文本缓冲
- 该缓冲不参与用户响应，不用于页面大段展示
- 只在日志写入后交给后台 token 服务计算 `completion_tokens`

结果：

- 长流式回复的 token 统计更准确
- 同时保持原有日志大小控制策略不变

---

**41. 本地精确补算 Token 需要 tokenizer 依赖，项目原先缺失**

问题现象：

- 想在本地补算 token 时，项目环境里没有可用的 tokenizer 依赖
- 无法对 GPT 类模型做较精确的 token 计数

原因：

- 原项目依赖中没有引入 `tiktoken`
- 因而只能依赖上游 `usage`，不能独立完成本地补算

解决方法：

- 在项目依赖中新增：
  - `tiktoken`
- 并安装到该项目虚拟环境中
- 同时在代码里加入兼容处理：
  - 若依赖不可用，则不阻塞主程序启动
  - 若依赖可用，则启用本地补算能力

结果：

- 项目具备了本地 token 精确补算的基础能力
- 避免把 token 统计完全绑定在上游实现质量上

---

**42. “记录 Token 使用量”开关应同时控制后台补算行为**

问题现象：

- 如果管理员关闭了“记录 Token 使用量”
- 但后台补算任务仍继续运行
- 会造成“前台关闭、后台仍在算”的行为不一致

原因：

- token 补算属于 token 记录功能的一部分
- 若没有统一受设置项控制，就会产生功能语义不一致

解决方法：

- 将日志写入后的异步补算与定时回填任务，都统一绑定到：
  - `enable_token_logging`
- 当该开关关闭时：
  - 不再调度单条补算
  - 不再执行周期性回填

结果：

- 设置项行为与系统实际执行完全一致
- 管理员对 token 记录开关的预期更明确

---

## 本轮所有新增功能与最终能力总结

### 1. 统一代理与分发能力

- 已保留并强化 OpenAI-compatible 统一入口：
  - `POST /v1/chat/completions`
  - `GET /v1/models`
  - `POST /v1/responses`
  - `POST /v1/embeddings`
  - `POST /v1/{subpath:path}`
- 系统可作为统一代理层，对外提供统一 URL 与统一密钥接入方式
- 用户请求进入系统后，可按模型级候选池分发到不同 GPT 提供商
- 支持管理员内部通过 `X-Aotu-Provider-Id` 手动指定 provider

### 2. 模型级路由能力

- 路由已从“provider 级”升级为“provider_model 级”
- 系统会根据用户传入的 `model`，只在支持该模型的候选提供商中分发
- 同一个 provider 下不同模型可以拥有独立的：
  - 启用状态
  - 优先级
  - 权重
  - 健康状态
  - 熔断状态
  - stream / vision 能力
- 某个 provider 的单个模型异常时，只会剔除该模型，不影响 provider 下其它模型

### 3. 全局路由模式能力

- 系统已实现 4 种全局路由模式：
  - `manual`
  - `failover`
  - `weighted`
  - `sticky`
- 路由排序会综合参考：
  - 模型健康状态
  - 模型优先级
  - provider 优先级
  - 最近成功率
  - 最近平均延迟
  - 最近失败率对应的动态降权
- 前端已补充模式说明和“当前实际命中逻辑说明”，便于管理员避免误配

### 4. 文本、图片与流式支持

- 文本非流式请求可稳定透传
- 文本流式请求支持 SSE 原样转发
- 图片输入已兼容 OpenAI-compatible 多模态消息格式
- 支持 `content` 数组中的：
  - `text`
  - `image_url`
- 代理层尽量保持“透传优先”，不主动改写用户 payload

### 5. 流式容错与真实直连体验

- 流式请求在首包前失败时支持自动切换下一个 provider
- 一旦已经开始输出 token，则不会中途换线，避免语义错乱
- 修复了流式建连失败时可能触发 `ResponseNotRead` 的问题
- 已记录流式关键链路信息：
  - 命中 provider
  - 建连耗时
  - 首 token 耗时
  - 总时长
  - 是否中途断流

### 6. 健康检查与熔断恢复

- 健康检查已升级为“模型级探活”
- 支持：
  - provider 级检查
  - provider_model 级检查
  - 单个模型手动测试
  - 单个 provider 手动测试
  - 全量批量测试
- 健康状态分层支持：
  - `healthy`
  - `degraded`
  - `unhealthy`
  - `unknown`
- 熔断状态支持：
  - `closed`
  - `open`
  - `half_open`
- 恢复探针会在达到恢复窗口后，允许模型以 `half_open` 方式重新进入候选池

### 7. 日志、审计与可观测性

- 请求日志已记录关键字段：
  - 请求时间
  - 请求路径
  - 请求模型
  - 是否 stream
  - 是否包含图片
  - 命中 provider
  - 命中模型
  - 状态码
  - 总耗时
  - trace
  - 错误摘要
- 新增字段包括：
  - `requested_model`
  - `resolved_provider_model_id`
  - `request_id`
  - `conversation_key`
  - `upstream_request_id`
  - `response_text`
- 任意失败请求都可以查看路由尝试链路
- 可以区分：
  - 上游鉴权失败
  - 模型不存在
  - 流式断流
  - 用户取消

### 8. Token 统计与完整对话留存

- 系统现在不仅能记录上游直接返回的 token
- 在上游不返回 `usage` 时，也能在后台补算：
  - `prompt_tokens`
  - `completion_tokens`
  - `total_tokens`
- 补算不会阻塞用户主请求链路
- 会话记录可按 `conversation_key` 串联成完整对话历史
- 支持“会话回放页”查看：
  - 用户输入
  - 模型输出
  - 命中线路
  - token 消耗

### 9. 后台管理界面能力

- 后台已形成统一管理界面，主要包括：
  - 概览页
  - 提供商管理
  - 模型管理
  - 路由设置
  - 日志中心
  - 会话回放
  - 调用测试
- 左侧导航位置已调整到更靠上
- “日志中心”已改为在同一导航框架内切换，不再跳新页面
- “概览页”和“提供商管理页”已增强为更强的信息驾驶舱风格

### 10. 调用测试与前端体验优化

- “调用测试”结果区已改为人类易读卡片展示，不再直接输出 JSON
- 结果卡片可展示：
  - 命中提供商
  - 耗时
  - 模型
  - 状态
  - 响应 ID
  - 创建时间
  - 模型回复
  - Token 统计
- 调用测试中的模型选择，已改为从“提供商管理”里已配置模型自动同步
- 提供商编辑页已加入模型填写说明、预设模型快捷选择、自定义模型输入

### 11. 对外访问与统一入口能力

- 系统本身可以作为对外统一 GPT 代理入口
- 外部系统可通过统一地址调用，而不是分别对接不同提供商
- 对外访问时，用户只需要：
  - 统一基础地址
  - 平台 API Key
  - 标准 OpenAI-compatible 请求体
- 若部署在公网服务器并正确开放端口、反向代理、配置安全策略后，即可公网访问

### 12. 当前最终结论

- 该项目现在已经能够完成“统一管理 GPT 提供商、按模型级进行自动/手动分发、进行模型级健康检查、稳定接收用户请求并返回模型结果”的核心目标
- 并且已经补齐了：
  - 流式主链路稳定性
  - 模型级路由
  - 健康检查
  - 熔断恢复
  - 日志与审计
  - Token 统计
  - 会话回放
  - 后台管理体验
- 当前系统已具备作为“统一 GPT 提供商调度与管理平台”的可用形态

---

**43. 提供商列表卡片中的“测试”按钮原先只测了 provider 连通性，不能代表真实可用性**

问题现象：

- 提供商列表中的 `测试` 按钮点击后，虽然能返回结果
- 但原先实际上只检查了：
  - `GET /models`
- 这只能说明提供商 API 可连通
- 不能说明该提供商下的模型是否真的能正常调用

原因：

- provider 级测试与 model 级测试语义不同
- 原实现把“提供商测试”简化成了连通性测试
- 导致测试结果与管理员直觉不一致

解决方法：

- 将 `提供商测试` 调整为完整测试流程：
  - 先做 provider 连通性检查
  - 再对该 provider 下所有已启用模型逐个探测
- 返回结果中增加：
  - `provider_success`
  - `health_status`
  - `models_total`
  - `models_success`
  - `models_failed`
  - `model_results`

结果：

- “测试”按钮现在能更真实反映该提供商是否可投入路由
- 不再出现“提供商 API 能连上，但模型其实不可用”却仍提示成功的问题

---

**44. 模型测试按钮前端反馈不一致，有的只提示“已测试模型”**

问题现象：

- `测模型` / `测试模型` 按钮在不同入口的反馈不一致
- 有的入口会展示详细测试结果
- 有的入口只弹出：
  - `已测试模型 xxx`
- 管理员无法直接看到：
  - 成功还是失败
  - 健康状态
  - 状态码
  - 耗时
  - 错误信息

原因：

- 前端两个模型测试入口虽然都调用了测试接口
- 但提示文案没有统一复用测试结果格式化逻辑

解决方法：

- 统一模型测试按钮的前端反馈逻辑
- 两个入口都改为使用同一套结果展示文案
- 测试后统一展示：
  - 成功/失败
  - 健康状态
  - 状态码
  - 耗时
  - 结果说明

结果：

- 模型测试反馈现在前后一致
- 管理员无需再进入日志页才能理解测试结果

---

**45. Provider 测试结果原先不能完整驱动前端状态刷新与判断**

问题现象：

- 前端点击 `测试` 按钮后虽然会刷新列表
- 但后端原返回结果过于粗略
- 无法清楚告诉前端：
  - provider 是否成功
  - 模型总体成功率
  - 当前 provider 的最终健康状态

原因：

- 原返回结构只有很基础的：
  - `success`
  - `latency_ms`
  - `status_code`
  - `message`
- 缺少对“测试覆盖范围”和“聚合结果”的表达

解决方法：

- 扩展 provider 测试接口返回结构
- 增加聚合字段：
  - `provider_success`
  - `health_status`
  - `models_total`
  - `models_success`
  - `models_failed`
  - `model_results`
- 前端根据这些字段生成更准确的测试结果提示

结果：

- 前端能够根据真实测试结果作出正确反馈
- 管理员可以直接从提示信息判断该提供商当前是否可用

---

**46. Provider 不可达时，模型状态曾被错误显示为 `degraded/closed`**

问题现象：

- 当 provider 连通性失败时
- 某些模型状态没有被明确标成：
  - `unhealthy`
  - `open`
- 而是可能落成：
  - `degraded`
  - `closed`

原因：

- 原先复用了模型级健康更新逻辑
- 该逻辑会根据失败次数与熔断阈值决定是 `degraded` 还是 `unhealthy`
- 但“provider 整体不可达”属于更高层级故障，不应该走普通模型探针失败的降级逻辑

解决方法：

- 增加专门的“provider 不可达”状态处理逻辑
- 一旦 provider 连通性失败：
  - provider 直接标记为 `unhealthy/open`
  - 该 provider 下已启用模型统一标记为 `unhealthy/open`
  - 同步写入错误信息和检测时间

结果：

- 当前状态展示与真实故障层级一致
- 管理员可以一眼看出这是“整条提供商不可达”，而不是“单模型轻微异常”

---

**47. Provider 测试成功后，若不重新持久化刷新结果，前端刷新时可能仍显示旧状态**

问题现象：

- 有些情况下 provider 连通测试已经成功
- 但页面刷新后仍可能看到旧状态

原因：

- 刷新 provider 聚合状态后，如果没有再提交数据库
- 重新读取时仍可能拿到旧值或不完整值

解决方法：

- 在 provider 连通测试成功并刷新 provider 聚合状态后
- 追加数据库提交，确保最新状态被持久化

结果：

- 测试后的状态刷新更稳定
- 前端重新拉取列表时能拿到一致结果

---

**48. 提供商与模型测试按钮需要同时满足三件事：可测、可反馈、可回写状态**

问题现象：

- 仅仅“按钮能发请求”并不代表功能完整
- 管理类测试按钮必须同时满足：
  - 后端真的执行了正确测试
  - 前端能正确反馈测试结果
  - 测试结果能正确更新数据库状态

原因：

- 早期实现更偏向“接口可调用”
- 但缺少对完整管理闭环的校验

解决方法：

- 本轮对以下三层进行了统一校验与修复：
  - 后端测试语义
  - 前端反馈文案
  - 测试后状态回写
- 并通过本地验证确认：
  - provider 测试接口返回完整结果结构
  - model 测试接口返回详细结果
  - 测试后 provider/model 状态正确更新

结果：

- 提供商列表卡片里的“测试”和“测模型”按钮现在形成了完整闭环
- 能真正作为后台管理使用，而不只是表面可点击
# 2026-04-24 静态资源缓存导致页面脚本与模板不一致

- 问题现象：页面模板已更新，但浏览器仍加载旧版 `app.js` 或 `app.css`，出现按钮点击无效、列表不渲染、页面状态停留在旧占位内容等现象。
- 适用范围：所有依赖 `app/templates/base.html` 引入的前端静态资源变更。
- 原因：修改 `app/static/` 下资源后，未同步更新 `app/templates/base.html` 中资源 URL 的版本参数，浏览器继续命中缓存。
- 解决方法：每次修改会影响浏览器行为的静态资源后，同步更新 `base.html` 中对应资源的 `?v=` 参数，并让页面执行一次硬刷新或重新加载完整页面。
- 排查方式：当接口正常但页面交互失效时，优先检查浏览器实际加载的静态资源版本是否与当前代码一致。

# 2026-04-24 API Key 额度累计不能只依赖同步响应

- 问题现象：新增 API Key Token 额度统计后，部分请求日志会在异步 token 回填后才得到完整的 `prompt_tokens`、`completion_tokens`、`total_tokens`，导致 API Key 聚合用量与日志明细不一致。
- 适用范围：所有同时存在“请求聚合额度统计”和“日志异步补 token”的模块。
- 原因：如果只在主请求成功时同步累加 API Key 用量，而不处理后续日志 token 回填产生的差值，聚合统计会长期偏小。
- 解决方法：在日志 token 回填逻辑中同步计算与原值的 delta，并把差值补回到 API Key 聚合字段。
- 排查方式：当单密钥总用量与该密钥日志求和不一致时，优先检查异步 token 回填链路是否同步更新了聚合统计。

# 2026-04-24 外部 `/v1/*` 不得保留内部“指定提供商测试”能力

- 问题现象：对外 Bearer API Key 入口如果继续接受 `X-Aotu-Provider-Id` 之类的内部调试头，会把“指定某个具体提供商测试”的能力泄漏到外部调用链。
- 适用范围：所有同时存在“正式对外入口”和“后台内部调试入口”的代理项目。
- 原因：在拆分 `/v1/*` 和 `/api/playground/*` 时，只拆了鉴权和路径，没有同时清掉外部链路上残留的内部强制路由参数。
- 解决方法：外部 `/v1/*` 只允许按 API Key 路由策略执行，不再接受内部强制指定 provider 的头；“指定提供商测试”只保留在 `/api/playground/*`。
- 排查方式：当发现外部请求还能通过自定义头强制命中某个 provider 时，优先检查对外 router 是否仍保留了内部调试参数。

# 2026-04-24 日志筛选不能只按当前健康检查枚举精确匹配

- 问题现象：日志中心勾选“隐藏健康检查日志”后，列表里仍可能出现历史 `health_check` 类型日志。
- 适用范围：所有存在历史日志数据、且日志类型枚举发生过演进的筛选接口或下拉选项接口。
- 原因：后端过滤只排除了当前使用的 `health_check_provider` 与 `health_check_model`，没有兼容旧版本遗留的 `health_check` 类型。
- 解决方法：健康检查过滤统一按兼容表达式处理，至少覆盖旧值 `health_check` 和新值 `health_check_*`，不要只写当前枚举的精确排除列表。
- 排查方式：当“隐藏健康检查日志”仍能看到健康检查记录时，优先统计数据库里的 `log_type` 分布，确认是否存在历史枚举值未被过滤。

# 2026-04-24 路由评分不能混入健康检查日志

- 问题现象：同一 provider/model 明明真实业务请求成功，但自动健康检查失败后，`weighted`、`failover` 等模式的近期成功率和延迟会被明显拉低，导致路由判断失真。
- 适用范围：所有把近期请求日志用于动态路由评分、权重衰减、故障切换排序的模块。
- 原因：路由统计直接聚合 `request_logs` 最近窗口数据时，没有排除 `health_check`、`health_check_provider`、`health_check_model` 等健康检查日志。
- 解决方法：路由专用统计必须只统计真实代理流量日志，至少排除所有健康检查类型；若后续还有内部探测、回放、压测日志，也应统一排除。
- 排查方式：当路由评分与实际用户流量表现明显不一致时，优先按 `log_type` 分组检查最近窗口，确认是否被健康检查或内部流量污染。

# 2026-04-24 路由热路径不能反复做遗留数据同步和无索引聚合

- 问题现象：请求量上来后，路由入口响应时间会越来越依赖数据库扫描时间，`/v1/*` 首包延迟容易被放大。
- 适用范围：所有在请求主链路里同时执行 provider 列表读取、遗留数据修复、近期日志聚合的代理系统。
- 原因：路由候选构建热路径里重复触发 `ProviderService.sync_legacy_provider_models`，并对 `request_logs` 最近窗口做聚合；而日志表缺少围绕 `created_at`、`provider_id`、`requested_model`、`success` 的联合索引。
- 解决方法：把遗留数据同步迁出热路径，只在启动迁移或后台修复任务中执行；同时为路由统计所需字段补充索引，必要时把近期路由指标做增量汇总或缓存。
- 排查方式：当 `/v1/*` 没有明显上游延迟却仍变慢时，优先检查是否在热路径做了隐式修复逻辑，并查看路由统计 SQL 是否走索引。

# 2026-04-24 半开恢复状态必须持久化，不能只停留在当前 Session

- 问题现象：熔断恢复窗口到期后，同一个已打开线路会在多个请求里被重复当作“第一次探测”，出现并发重复探测和状态抖动。
- 适用范围：所有使用 `open -> half_open -> closed` 熔断恢复状态机的路由模块。
- 原因：候选筛选阶段把 `provider_model.circuit_state` 从 `open` 改成 `half_open` 后，没有立即持久化；请求结束后 Session 关闭，数据库仍保持 `open`。
- 解决方法：进入 `half_open` 时应原子化持久化，并记录探测时间；必要时增加单模型恢复锁或 compare-and-set 语义，避免并发请求同时抢到探测资格。
- 排查方式：当同一条已熔断线路在恢复窗口后被多个请求同时探测时，优先检查 `half_open` 状态是否真的落库。

# 2026-04-24 会话粘滞不能用候选列表取模代替一致性路由

- 问题现象：`sticky` 模式虽然对同一 key 可复现，但可能稳定命中较低优先级线路；候选列表一旦增减，原有粘滞映射也会整体漂移。
- 适用范围：所有需要“同会话尽量命中同一路线”且同时关注健康度、权重、扩缩容稳定性的路由场景。
- 原因：当前实现只是对排序后的候选列表做哈希取模，没有使用一致性哈希、rendezvous hashing 或带健康约束的会话亲和算法。
- 解决方法：若业务要求真实会话粘滞，应改为健康感知的一致性哈希方案，并把默认优先级、权重、摘除节点后的最小扰动一起纳入设计。
- 排查方式：当 `sticky` 模式出现“同一用户长期命中次优线路”或“节点增减后会话大面积漂移”时，优先检查是否仍在使用简单取模策略。


# 2026-04-24 成本与质量指标不能只埋在 trace_json 里

- 问题现象：需要按 provider / model 展示成功率、平均首 Token 时间、稳定性评分时，如果关键字段只存在 `request_logs.trace_json`，前端展示和后台聚合都会变得脆弱且成本高。
- 适用范围：所有需要把路由追踪信息进一步用于管理面板统计、排序依据、成本质量分析的模块。
- 原因：`trace_json` 适合保存完整尝试链路，但不适合承担高频聚合字段；像 `first_token_latency_ms` 这类指标如果没有一等字段，查询层只能反复解析 JSON。
- 解决方法：把需要长期复用的关键指标提升为明确字段落库，例如 `request_logs.first_token_latency_ms`；配置类信息如模型单价应直接存放在 `provider_models`，聚合展示再由服务层统一计算。
- 排查方式：当管理后台新增性能/成本统计需求时，优先检查当前指标是否仍只存在于 JSON 追踪字段中；若是，应先评估是否提升为结构化字段。

# 2026-04-24 推送 GitHub 时不能默认远程仓库为空

- 问题现象：本地仓库首次执行 `git push -u origin main` 时，若远程 `main` 已存在初始化提交或其他历史提交，会收到 `fetch first` 拒绝，无法直接推送。
- 适用范围：所有需要把本地现有仓库首次绑定并发布到 GitHub 的项目。
- 原因：只配置了远程地址并尝试直接推送，没有先确认远程分支是否已有历史；当本地与远程是不同根提交时，还可能触发 unrelated histories 和冲突处理。
- 解决方法：先检查本地是否有未提交改动并完成提交，再抓取远程分支状态；若远程已有历史提交，则先执行抓取/合并，按最小必要原则解决冲突并形成合并提交，之后再推送。
- 排查方式：当首次推送出现 `fetch first`、`non-fast-forward` 或远程已存在内容提示时，优先检查远程 `main` 是否已有初始化提交，并确认是否需要先合并远程历史。

# 2026-04-25 服务器部署不能直接复用 Windows 启动脚本

- 问题现象：项目在阿里云 Ubuntu 上部署时，若直接沿用 `run.ps1` 或 `start_project.py` 这类面向 Windows 的启动方式，会出现虚拟环境路径、服务常驻方式、反向代理配置不适配的问题。
- 适用范围：所有同时需要支持本地 Windows 开发启动和 Linux 服务器正式部署的 Python Web 项目。
- 原因：Windows 启动脚本默认依赖 `Scripts\\python.exe`、PowerShell 以及前台 `uvicorn` 启动，不满足 Linux 下 `.venv/bin/python`、`systemd` 常驻和 `nginx` 反向代理的部署需求。
- 解决方法：单独提供面向 Linux / 阿里云 ECS 的一键部署脚本，统一完成 `.venv`、`.env`、依赖安装、`systemd` 服务和 `nginx` 站点配置，避免让运维手工拼装部署步骤。
- 排查方式：当项目在服务器上能手动前台启动、但无法稳定公网访问或无法开机自启时，优先检查是否仍在复用本地 Windows 启动脚本，而没有使用 Linux 专用部署入口。


# 2026-04-24 金额账单不能只在主请求成功时同步扣费

- 问题现象：如果账单金额只在主请求返回时同步计算，遇到上游未返回 `usage`、流式回填 token、日志异步补 token 等场景，API Key 的累计消费、余额和账单流水会与最终日志不一致。
- 适用范围：所有同时存在“请求即时落日志”和“异步 token 回填 / 重算”的计费系统。
- 原因：请求成本依赖 `prompt_tokens`、`completion_tokens`、`total_tokens`，而这些字段在部分链路里并不是首次落日志时就完整可得。
- 解决方法：账单系统必须复用同一条 `request_logs` 记录做重算；当 token 回填发生时，按成本 delta 同步更新 API Key 聚合消费、余额和账单流水，而不是只在首次成功响应时扣费一次。
- 排查方式：当“日志里的金额”“API Key 累计消费”“余额变化”“账单流水”四者不一致时，优先检查 token 回填链路是否同时触发了成本重算和账单 delta 同步。


# 2026-04-25 FastAPI SessionMiddleware 不能漏掉 itsdangerous 依赖

- 问题现象：项目新增基于 `SessionMiddleware` 的浏览器登录态后，应用启动会直接报错 `ModuleNotFoundError: No module named 'itsdangerous'`。
- 适用范围：所有在 FastAPI / Starlette 项目中首次启用 `starlette.middleware.sessions.SessionMiddleware` 的场景。
- 原因：`SessionMiddleware` 运行时依赖 `itsdangerous`，但该依赖不会自动在项目 `requirements.txt` 中显式出现；若环境里未预装，就会在导入中间件时失败。
- 解决方法：在项目依赖中显式加入 `itsdangerous`，并使用项目既有 `.venv` 与国内镜像源完成安装，避免只在本机环境偶然可用、换环境后启动失败。
- 排查方式：当新增 Session 登录态后应用在启动阶段而非请求阶段就报错时，优先检查 `requirements.txt` 是否已显式包含 `itsdangerous`。

# 2026-04-25 GitHub 上传必须按“先提交、再抓取合并、最后推送”顺序执行

- 问题现象：本地项目准备上传到 GitHub 时，若直接执行 `git push -u origin main`，容易遇到 `fetch first`、`non-fast-forward`、远程已有初始化提交或本地与远程不是同根历史等报错。
- 适用范围：所有需要把本地已有仓库首次上传到 GitHub，或后续继续同步到已有远程仓库的项目。
- 原因：推送前没有先确认本地工作区是否已提交、远程仓库是否已绑定、远程 `main` 是否已有历史，以及本地与远程是否需要先合并。
- 解决方法：固定按以下顺序执行：先运行 `git status -sb`、`git branch --show-current`、`git remote -v`、`git log --oneline -3` 确认状态；再执行 `git add .` 和 `git commit -m "<清晰提交信息>"`；若未配置远程则先 `git remote add origin <仓库地址>`；随后执行 `git fetch origin main`，按实际情况执行 `git merge origin/main` 或 `git merge origin/main --allow-unrelated-histories`；完成冲突解决并提交后，再执行 `git push -u origin main`。
- 排查方式：当推送被拒绝或提示远程已有内容时，优先检查是否漏做了本地提交、远程抓取和历史合并，而不是直接尝试强推覆盖。

# 2026-04-25 管理后台端与用户端不能只靠零散页面判断访问权限

- 问题现象：项目同时存在管理后台端、用户端和登录页时，如果只在个别页面里手写跳转逻辑，容易出现前端壳式导航仍可点击受保护页面、会话失效后停留在半可用页面、登录后回跳到错误角色页面等问题。
- 适用范围：所有同时存在“未登录页 + 管理后台页 + 普通用户页”的 Session 登录项目。
- 原因：页面访问控制分散在各路由或各按钮里，没有统一的前端路由守卫，也没有统一的 `next` 回跳规则。
- 解决方法：把受保护页面的访问规则集中维护为统一路由表；服务端继续负责 Session 校验并在未登录时跳转 `/login?next=原路径`；前端壳式导航在进入页面前先按登录态和角色拦截；登录成功后只允许回跳到当前角色可访问的页面，否则回到角色默认首页。
- 排查方式：当发现未登录仍能误入后台页、登录后落到错误页面、或会话过期后页面只报接口 401 不跳登录时，优先检查是否已经同时实现“服务端重定向 + 前端路由守卫 + next 回跳白名单”三层闭环。


# 2026-04-25 只存 API Key 哈希会导致后台无法再次展示或复制已有密钥

- 问题现象：后台创建新密钥时虽然能拿到一次明文，但列表页、详情页和后续运维过程中无法再次显示完整密钥，也无法提供稳定的一键复制能力。
- 适用范围：所有需要后续查看、复制或重新分发平台凭证的 API Key / Token 管理场景。
- 原因：原实现只保存 `key_hash` 和 `key_prefix`；其中 `key_hash` 是不可逆的，一旦明文没有额外保存，就无法从数据库恢复完整密钥。
- 解决方法：继续保留 `key_hash` 用于鉴权匹配，同时新增受控加密存储字段保存密钥明文密文；后台展示和复制时只解密管理员可访问的数据；对历史未保存明文的旧密钥，不伪造回显，直接提示管理员通过编辑重置为新密钥。
- 排查方式：当出现“新建后能看到明文、刷新列表后就再也拿不到完整密钥”或“旧密钥无法复制”的情况时，优先检查是否仍然只有不可逆哈希，而没有明文加密存储方案。

# 2026-04-25 生产环境密钥不能继续使用默认占位值

- 问题现象：项目即使未配置真实 `SESSION_SECRET_KEY` 或 `API_KEY_ENCRYPTION_SECRET` 也能直接启动，浏览器 Session 签名和 API Key 加密会退化到代码里的固定占位字符串。
- 适用范围：所有同时使用服务端 Session、密钥加密存储或签名 Cookie 的 Python Web 项目。
- 原因：配置层给安全敏感字段提供了可直接生效的默认占位值，但启动阶段没有强制校验生产环境必须替换。
- 解决方法：对 `session_secret_key`、`api_key_encryption_secret` 这类安全配置增加启动校验；开发环境可显式允许弱默认值，生产环境必须使用高熵随机值并通过 `.env` 或密钥管理系统注入。
- 排查方式：当发现不同环境共享同一套签名能力、Session 可在多实例间异常通用或数据库里的密钥密文可被默认配置解开时，优先检查是否仍在使用代码中的默认占位 secret。

# 2026-04-25 基于 Session 的后台表单不能缺少 CSRF 防护

- 问题现象：后台或用户中心使用浏览器 Session 维持登录态时，若修改资料、改密、创建用户、切换状态等表单接口没有 CSRF 校验，登录用户可能被第三方站点诱导发起跨站状态变更请求。
- 适用范围：所有使用 Cookie / Session 做鉴权，且存在 HTML 表单写操作的后台管理系统或用户中心。
- 原因：后端只校验了 Session 登录态，没有给 POST 表单增加一次性 CSRF Token、来源校验或双提交 Cookie 机制。
- 解决方法：为所有基于 Session 的写操作统一接入 CSRF 防护，并在模板表单里注入和校验同一套 token；仅依赖 `SameSite=Lax` 不能替代完整的 CSRF 防护。
- 排查方式：当后台存在“已登录即可直接 POST 生效”的表单接口时，优先检查模板是否输出 CSRF token、后端是否在提交时验证 token。

# 2026-04-25 对外 OpenAI 兼容能力不能只在服务层和日志层半实现

- 问题现象：项目内部已经存在 `embeddings` 的代理、日志和 token 统计代码，但对外 router 没有暴露 `/v1/embeddings`，会出现“后台统计里看起来支持、外部接入实际不可用”的能力断层。
- 适用范围：所有声称兼容 OpenAI 多端点、同时存在对外路由层、服务层、日志层和文档层的提供商代理项目。
- 原因：功能演进时只补了服务层或统计层，没有同步补齐正式对外入口、文档和接入验证。
- 解决方法：每新增一个对外兼容能力，都必须同时补齐正式路由、鉴权链路、日志/计费链路、接入文档和最小烟测；若暂不对外开放，则应删除未落地的半成品入口暗示。
- 排查方式：当日志枚举、统计逻辑或服务方法里出现某能力，但 `/v1/*` 实际路由和外部文档里缺失或不一致时，优先检查是否存在“半实现”状态。

# 2026-04-25 Linux 一键部署脚本必须支持幂等跳过与部署后健康校验

- 问题现象：阿里云或 Ubuntu 部署脚本每次执行都重复安装系统依赖、重复创建环境，或部署结束后没有立即校验本机与公网是否可访问，导致“脚本显示成功但服务实际不可用”这一类问题难以及时发现。
- 适用范围：所有需要反复重跑、支持增量修复的 Linux 一键部署脚本。
- 原因：脚本只覆盖“首次安装”路径，没有为已完成项提供显式跳过逻辑，也没有把本机反向代理验证、公网访问验证纳入部署流程。
- 解决方法：系统依赖、虚拟环境、Python 依赖、环境变量补齐等步骤都应在执行前先检测当前状态；已完成项直接跳过。部署完成后至少校验本机监听地址和对外代理地址，必要时提示安全组或防火墙仍未放行。
- 排查方式：当脚本重跑耗时异常长、每次都重复安装相同依赖，或部署完成后浏览器打不开页面但终端没有明显报错时，优先检查脚本是否具备幂等检测、Nginx 反代检查和公网健康检查。

# 2026-04-25 外部接入文档不能硬编码固定公网 IP

- 问题现象：`使用文档` 直接写死某个公网 IP 或协议后，项目一旦迁移域名、切到 HTTPS、换机器或多环境部署，页面展示和复制出的接入地址就会立即失真。
- 适用范围：所有面向外部用户展示 Base URL、示例请求和环境变量的接入文档页面。
- 原因：文档模板直接硬编码部署地址，没有从配置、请求 Host 或统一文档变量中生成对外接入地址。
- 解决方法：把外部 Base URL 收敛为单一配置来源，由模板统一渲染；若环境未知，至少提示管理员在配置项中维护，而不是把真实地址散落在多个 HTML 片段里。
- 排查方式：当迁移部署后文档页复制出的地址仍指向旧 IP、旧协议或旧域名时，优先全文检索模板里是否存在硬编码地址。

# 2026-04-25 OpenAI Responses 回复提取不能只兼容 chat.completions 结构

- 问题现象：内部 playground 明明已经收到上游返回结果，但页面仍显示“未提取到可显示的回复内容”。
- 适用范围：所有同时兼容 `/chat/completions` 与 `/responses`，并在前端统一渲染模型回复的代理或调试页面。
- 原因：前端回复提取逻辑只覆盖了 `choices[].message.content`、`choices[].delta.content` 或极少量 `output_text` 结构，没有兼容 `responses` 接口常见的 `output[].content[].text`、`text.value`、`response.output_text.delta`、`response.completed.response` 等事件与对象形态。
- 解决方法：统一抽出文本提取函数，对字符串、数组、嵌套对象、`output` 内容块和流式 delta 事件做递归兼容；SSE 聚合时不要只看 `choices`，也要识别 `response.output_text.delta` 和 `response.usage`。
- 排查方式：当页面显示“已返回结果”但正文为空时，优先检查原始返回体或 SSE 事件是否其实走的是 `responses` 结构，而前端仍按 `chat.completions` 口径解析。

# 2026-04-25 后台测试按钮不能只给简短 toast，必须能查看结构化结果

- 问题现象：提供商管理里的“测试”“测试模型”按钮虽然会执行健康检查，但如果只弹一条简短 toast，管理员无法直观看到状态码、耗时、健康状态和模型明细，排查效率很低。
- 适用范围：所有后台里的连通性测试、模型探测、探针检查、批量校验等“即时测试”操作。
- 原因：前端只把后端返回结果拼成一行提示，没有提供可停留查看的结构化结果容器。
- 解决方法：保留成功/失败 toast 作为即时反馈，同时追加弹窗或侧边结果面板，至少展示测试对象、状态码、耗时、健康状态、结果说明；若是提供商级测试，还应展示模型级明细。
- 排查方式：当测试按钮点击后用户仍需要去日志中心或控制台二次查结果时，优先检查当前前端是否只有 toast，而没有结构化测试结果展示层。

# 2026-04-25 FastAPI TestClient 与 APScheduler 生命周期组合可能在本地烟测时报 Event loop is closed

- 问题现象：本地使用 `TestClient(app)` 并触发完整 lifespan 时，`APScheduler` 可能在测试线程退出或事件循环切换阶段报 `RuntimeError: Event loop is closed`，导致功能烟测被中断。
- 适用范围：所有在本项目里直接复用真实 `lifespan`、并同时启用了 `APScheduler` 的本地接口烟测或回归脚本。
- 原因：测试环境里的事件循环生命周期与实际服务进程不同，调度器在 `lifespan` 启停阶段可能尝试向已关闭的事件循环注册回调。
- 解决方法：本地功能烟测优先先显式调用 `init_database()` 完成迁移，再使用不依赖完整 lifespan 的请求验证；若必须覆盖 lifespan，则需要对调度器启动行为做专门隔离。
- 排查方式：当接口功能本身已可用，但 `TestClient` 在启动阶段直接报事件循环关闭错误时，优先检查是否是 `APScheduler` 与测试生命周期冲突，而不是误判为业务接口实现失败。

# 2026-04-25 用户隔离日志查询不能只靠页面筛选，必须在后端做 key 归属交集约束

- 问题现象：如果普通用户日志页只把“所属 API Key”做成前端下拉，而后端在收到 `api_client_key_id` 时直接按该 ID 查询，就可能被构造参数越权查看其他用户日志。
- 适用范围：所有“当前用户仅可查看自己资源”的分页列表、日志中心、账单中心或会话回放接口。
- 原因：服务层同时支持“显式单个资源 ID 过滤”和“当前用户资源集合过滤”时，若代码让单个 ID 条件覆盖集合约束，就会形成权限旁路。
- 解决方法：当接口既有单个资源 ID 过滤又有当前用户资源集合约束时，后端必须强制做交集校验；若指定 ID 不属于当前用户，则应直接返回空结果或拒绝，而不是回退为按该 ID 直接查询。
- 排查方式：当用户态页面支持通过查询参数指定资源 ID 时，优先检查后端是否仍同时保留并生效了“当前用户资源集合”限制，而不是仅依赖前端隐藏不可选项。

# 2026-04-25 httpx 连接池启用 HTTP/2 时不能假定运行环境已安装 `h2`

- 问题现象：将共享上游客户端切换为 `httpx.AsyncClient(http2=True)` 后，应用在启动阶段直接报错，导致回归脚本和服务进程都无法进入 lifespan。
- 适用范围：所有使用 `httpx` 连接池并希望复用 HTTP/2 的 Python 代理、提供商或网关项目。
- 原因：`httpx` 只有在运行环境额外安装 `h2` 依赖时才能启用 HTTP/2；若项目依赖文件里只安装了基础版 `httpx`，强制传入 `http2=True` 会在客户端初始化阶段抛出 `ImportError`。
- 解决方法：共享上游客户端必须先检测 `h2` 是否可用；已安装时再启用 HTTP/2，未安装时自动回退到 HTTP/1.1，而不是在启动阶段硬失败。
- 排查方式：当应用一启动就报 “Using http2=True, but the 'h2' package is not installed” 时，优先检查连接池初始化逻辑是否无条件开启了 HTTP/2。

# 2026-04-25 用户端默认提供商选择不能依赖手工同步勾选 allowed_provider_ids

- 问题现象：普通用户在用户端给 API Key 选择了“默认提供商”后，保存时仍提示无效或看起来像“默认提供商无法选择”。
- 适用范围：所有同时维护 `default_provider_id` 与 `allowed_provider_ids` 的 API Key / 路由策略管理页面。
- 原因：后端校验要求默认提供商必须属于 `allowed_provider_ids`，但用户端表单把“默认提供商选择”和“可用提供商勾选”拆成了两个独立输入，未自动保持一致。
- 解决方法：用户端提交时必须自动把 `default_provider_id` 合并进 `allowed_provider_ids`；同时只允许选择管理员已启用的提供商，避免用户绑定到已停用线路。
- 排查方式：当页面明明能选默认提供商，但提交后报 `default_provider_id must be one of allowed_provider_ids` 或保存失败时，优先检查表单提交逻辑是否自动补齐了默认提供商对应的授权列表。

# 2026-04-25 关闭完整正文记录时仍需保留最小请求内容，否则日志会只剩回答没有提问

- 问题现象：日志里可以看到提供商回复内容，但看不到用户原始提问，尤其在流式响应链路中更容易表现为“只记录回答，不记录问题”。
- 适用范围：所有支持“关闭完整请求正文记录”但仍需保留会话回放、问题定位或最小审计能力的 LLM 代理系统。
- 原因：请求体写日志依赖 `enable_payload_logging`，而流式回复留存又可由独立开关继续开启，导致请求体被关掉后，日志与会话回放会只剩下响应侧内容。
- 解决方法：当关闭完整正文记录时，仍应保留一份经过脱敏和截断的最小请求摘要，至少包含 `model`、`messages`、`input`、`prompt`、`instructions`、`metadata` 等与用户提问直接相关的字段。
- 排查方式：当日志详情或会话回放只能看到 assistant 回复，`request_body_json` 为空而 `response_text` 仍存在时，优先检查请求体留存是否被“完整正文开关”整体短路了。

# 2026-04-26 Pydantic 更新模型 `model_dump()` 后不要再按对象属性访问嵌套项

- 问题现象：模型聚合列表编辑某个现有模型时，前端提交更新请求后无法保存，服务端会在处理渠道绑定时抛出类似 `AttributeError: 'dict' object has no attribute 'provider_id'`。
- 适用范围：所有使用 Pydantic 更新对象、且更新载荷里包含嵌套列表对象的 Python 后端服务。
- 原因：更新流程先对 `payload` 执行了 `model_dump()`，把嵌套的 `provider_bindings` 从 Pydantic 对象转换成了普通 `dict`，但后续逻辑仍按 `binding.provider_id` 这类属性方式读取。
- 解决方法：对需要保留对象类型的嵌套字段，应在 `model_dump()` 时显式排除，再直接使用 `payload` 上的原始字段值；或者统一把后续处理改为字典访问，但不能两种类型混用。
- 排查方式：当新增接口正常、编辑接口失败，且失败点集中在嵌套字段访问时报“dict 没有某属性”时，优先检查更新链路是否对 payload 先做了 `model_dump()` 并丢失了对象类型。

# 2026-04-26 受 Session 保护的后台接口不能直接用未登录 TestClient 冒烟

- 问题现象：本地使用 `TestClient(app)` 直接请求后台接口时，虽然接口代码本身已存在，但返回结果是 `401`，看起来像“新接口不可用”。
- 适用范围：所有使用服务端 Session 保护后台页面和后台 API 的本项目功能烟测场景。
- 原因：后台接口依赖浏览器登录态或 Session 中间件，未先完成登录或未注入有效 Session 的 `TestClient` 请求会被统一鉴权拦截。
- 解决方法：后台接口冒烟测试应优先采用两种方式之一：先完成登录并携带有效 Session 后再走路由测试；若当前只需验证分页、筛选或序列化逻辑，则直接调用对应 service 做只读验证。
- 排查方式：当新增后台接口通过导入和编译校验后，`TestClient` 仍返回 `401` 时，优先检查是否缺少登录态，而不是先怀疑业务实现本身失败。

# 2026-04-26 OpenAI 兼容提供商不能把“文本可用”等同于“图片和 responses 可用”

- 问题现象：同一条对外 API Key 和同一渠道下，`/v1/chat/completions` 文本非流式、文本流式都返回 `200`，但带图片的 `chat/completions` 与 `/v1/responses` 请求都返回上游 `502 Bad gateway`。
- 适用范围：所有接入 OpenAI 兼容上游、并对外宣称支持文本、流式、图片、多模态或 `/v1/responses` 的代理/提供商项目。
- 原因：部分上游或渠道只对文本链路实现稳定兼容，图片能力和 `/responses` 能力可能未开通、未真正兼容，或在网关层被单独拦截；因此“文本调用通过”不能证明其余能力可用。
- 解决方法：验收时必须分开测试至少四类能力：文本非流式、文本流式、`/v1/responses`、图片请求；若某一项失败，后台能力声明、可用模型说明和用户文档都必须按真实支持范围收口，不能继续笼统标记为“完全兼容 OpenAI”。
- 排查方式：当文本请求稳定成功，但图片或 `/v1/responses` 单独失败且上游返回 `502`、`404`、`405` 或不兼容报错时，优先检查该渠道是否只支持文本链路，而不是先怀疑本地鉴权或日志模块。

# 2026-04-26 API Key 聚合 Token 字段不能只校验 `total_tokens_used`

- 问题现象：同一把 API Key 在测试前 `total_tokens_used=0`，完成两次成功请求后变成 `89`，与两条账单记录中的 `42 + 47` 一致；但同一行的 `prompt_tokens_used` 与 `completion_tokens_used` 却出现了与 `total_tokens_used` 不相等的值。
- 适用范围：所有同时维护 API Key 聚合总 Token、输入 Token、输出 Token 三类累计字段的计费或配额系统。
- 原因：聚合更新链路可能只保证了 `total_tokens_used` 与账单一致，但 `prompt_tokens_used`、`completion_tokens_used` 在补录、失败重试、回填或旧逻辑迁移后未同步校正，导致明细累计字段漂移。
- 解决方法：回归测试与账单对账时，必须同时校验 `prompt_tokens_used + completion_tokens_used == total_tokens_used`，并与最新成功账单记录逐项核对；若不一致，应优先检查聚合更新逻辑、失败请求回滚和历史回填脚本。
- 排查方式：当账单记录和 `total_tokens_used` 看起来正常，但输入/输出累计 Token 明显偏大、偏小或相加不等于总量时，优先检查 API Key 聚合字段是否出现局部更新或历史脏数据残留。

# 2026-04-26 Token 回填写日志时，失败请求的估算 Token 不能进入 API Key 成功额度累计

- 问题现象：失败的 `/v1/responses` 或鉴权失败请求在日志回填后会得到 `prompt_tokens`，若 API Key 聚合字段直接跟着日志 delta 走，`prompt_tokens_used` 会被失败请求抬高，而 `total_tokens_used` 与成功账单汇总不一致。
- 适用范围：所有会为失败请求、鉴权失败请求或上游失败请求补算 Token，但又需要按成功调用统计 API Key 配额累计的代理/提供商项目。
- 原因：日志回填的目标是补齐观测数据，而 API Key 聚合字段的目标是反映成功调用的额度消耗；若两者共用同一套 delta 累加逻辑，失败请求补出的估算输入 Token 就会污染配额累计。
- 解决方法：日志回填后，API Key 的 `prompt_tokens_used`、`completion_tokens_used`、`total_tokens_used` 必须按成功请求日志统一重算，或至少在同步累计时显式排除失败请求与 `/v1/models` 等非计费路径。
- 排查方式：当失败日志里能看到 `prompt_tokens`，但 API Key 的输入 Token 累计异常偏大、且成功日志汇总对不上时，优先检查“日志回填”和“API Key 配额聚合”是否错误复用了同一套增量更新逻辑。

# 2026-04-26 `/v1/responses` 流式 SSE 的 usage 可能包在 `response.completed.response.usage`，不能只解析顶层 usage

- 问题现象：`/v1/responses` 流式请求成功返回，但日志只拿到部分或为空的 token 字段，常见表现为 `prompt_tokens` 有值、`completion_tokens` 和 `total_tokens` 为空，账单停在 `pending_tokens` 或金额偏小。
- 适用范围：所有接入 OpenAI Responses API 流式事件、并从 SSE 事件中提取 usage、finish_reason 或文本内容的代理/网关项目。
- 原因：Responses API 的流式事件经常把最终 usage 包在 `response.completed` 事件的 `response.usage` 嵌套结构里，而不是直接放在事件顶层；若解析器只读取顶层 `usage`，就会漏掉最终输入/输出/总 token。
- 解决方法：解析 SSE 事件时，必须同时兼容顶层 `usage` 与嵌套 `response.usage`；若需要保留流式正文，还应兼容 `part.text` 这类事件级文本字段。
- 排查方式：当 `/v1/responses` 流式最后一个 SSE 事件里能看到 `response.completed.response.usage`，但本地日志仍缺 `completion_tokens` 或 `total_tokens` 时，优先检查事件解析器是否遗漏了嵌套 `response` 包装层。

# 2026-04-26 OpenAI 兼容上游的 `/responses` 图片请求可能需要内部适配为 `chat/completions`，且大 `data:` 图片体不宜直接走共享异步客户端

- 问题现象：同一上游、同一模型、同一张本地图片，直接用官方 `/responses` 图片格式经代理转发会返回 `502/524`，但把同一图片内容改成 `chat/completions` 的 `image_url` 结构后可成功返回。
- 适用范围：所有对外暴露官方 `/v1/responses`，但上游实际只稳定支持 `chat/completions` 图片能力的 OpenAI 兼容代理/提供商项目。
- 原因：部分 OpenAI 兼容上游对文本 `/responses` 兼容良好，但对图片版 `/responses` 兼容不完整；同时，大体积 `data:image/...;base64,...` 请求体在共享异步客户端链路中更容易触发不稳定的上游超时或网关错误。
- 解决方法：对外仍保持官方 `/v1/responses` 入参不变；当命中“图片 + OpenAI 兼容上游”时，内部应把请求适配为上游实际稳定支持的 `chat/completions` 图片格式，再把返回结果包装回 `/responses` 兼容结构；图片非流式请求应优先使用已验证稳定的独立发送通道，避免与普通文本流量共用不稳定的大包体传输链路。
- 排查方式：当文本 `/responses` 正常、图片 `/responses` 失败，但同一图片改走直连 `chat/completions` 能成功时，优先检查是否需要在代理层做“`responses` 图片 -> `chat/completions` 图片”的内部适配，而不是继续只盯着用户侧 JSON 格式。

# 2026-04-26 响应包装层同时保留 `output_text` 与 `output[].content[].text` 时，日志提取不能重复拼接同一段文本

- 问题现象：`/v1/responses` 成功返回后，日志中的 `response_text` 会把同一句回答重复记录两遍，例如页面看到“检测到作弊者，比赛终止检测到作弊者，比赛终止”。
- 适用范围：所有会把上游响应重新包装成另一种兼容格式，并在日志层同时从多个字段提取文本摘要的代理/网关项目。
- 原因：同一条回答既存在于 `output_text`，又存在于 `output[].content[].text`；若日志提取逻辑按多个字段无差别顺序拼接，就会把同一段整句文本重复追加。
- 解决方法：文本提取阶段必须对相邻重复片段去重，至少避免把同一段完整回答因兼容包装字段重复而再次拼接到 `response_text`。
- 排查方式：当接口返回正文正常、但日志详情中的 `response_text` 明显整句重复且 `response_body_json` 同时存在 `output_text` 和 `output[].content[].text` 时，优先检查日志文本提取函数是否缺少重复片段过滤。

# 2026-04-26 后台“提供商测试”若不复用正式代理链路，会出现接口已修复但测试页仍误报失败

- 问题现象：对外 `/v1/chat/completions`、`/v1/responses` 已经能成功调用，但后台“提供商测试”弹窗仍显示 `provider connectivity failed`、`模型通过 0/1` 或旧的 30 秒超时结果。
- 适用范围：所有同时提供正式代理入口与后台健康测试入口的 LLM 代理/提供商项目。
- 原因：后台测试器单独使用了 `/models` 连通性探测、旧的文本 `ping` 或独立客户端逻辑，没有复用正式代理链路的真实入参、图片适配、超时和传输策略，导致测试口径与真实流量分裂。
- 解决方法：后台提供商测试必须改为复用正式代理同口径的探测逻辑，至少要按实际对外支持的 `/v1/chat/completions` 与 `/v1/responses` 路径逐项探测，并把失败原因按端点原样回显，而不是继续先测 `/models` 再笼统报“连通性失败”。
- 排查方式：当真实 API 调用成功，但后台测试页仍显示 `provider connectivity failed`、固定约 30 秒失败或模型结果与正式接口不一致时，优先检查测试器是否还在走独立的旧健康检查实现。

# 2026-04-26 Linux 部署健康检查不能在 `systemctl restart` 后只探测一次

- 问题现象：`start_aliyun.sh` 执行到本地健康检查时立即报 `curl: (7) Failed to connect to 127.0.0.1 port 8000`，但几秒后 `systemd` 状态和 `journalctl` 显示 `Uvicorn running on http://127.0.0.1:8000`，实际服务已启动成功。
- 适用范围：所有通过 `systemd` 重启 Python Web 服务后，立即用 `curl` 做本地或公网探活的部署脚本。
- 原因：`systemctl restart` 返回成功不代表应用已经完成启动监听；若脚本在服务完成应用启动前只探测一次，就会把“启动中”误判为“启动失败”。
- 解决方法：部署脚本中的健康检查必须改为有限次轮询重试，并在失败日志里区分“仍在等待启动”和“最终探活失败”；仅在重试耗尽后才输出 `systemctl status` 并退出。
- 排查方式：当脚本报本地探活失败，但 `systemctl status` 已显示 `active (running)`、且 `journalctl` 中随后出现 `Application startup complete` 时，优先检查探活是否执行过早，而不是先怀疑服务启动配置错误。

# 2026-04-26 自定义 HTTP 响应头不能直接写入中文渠道名

- 问题现象：代理接口在返回流式或非流式结果时，如果把中文提供商名直接写入 `X-Proxy-Provider-Name`，Starlette 会在构造响应头阶段抛出 `UnicodeEncodeError: 'latin-1' codec can't encode character ...`，导致请求直接失败。
- 适用范围：所有通过 FastAPI / Starlette / ASGI 自定义响应头回传渠道名、模型名、备注等非 ASCII 文本的接口。
- 原因：HTTP 响应头在 Starlette 初始化时会按 `latin-1` 编码，中文等超出 `latin-1` 范围的字符不能直接写入头部。
- 解决方法：需要回传中文文本时，必须先统一编码为可安全传输的 ASCII 形式，例如 UTF-8 百分号编码，并同步提供显式编码标识；浏览器或前端读取该头时再按约定解码展示。
- 排查方式：当接口日志显示业务逻辑已执行到返回阶段，但堆栈落在 `starlette.responses.init_headers` 且异常是 `latin-1` 编码失败时，优先检查最近新增的自定义响应头是否直接写入了中文或其他非 `latin-1` 字符。

# 2026-04-26 聚合告警落库前必须先把 `datetime` 转成 JSON 安全值

- 问题现象：打开 `/alerts` 告警中心时直接返回 `500 Internal Server Error`，服务层堆栈显示 `TypeError: Object of type datetime is not JSON serializable`。
- 适用范围：所有把聚合视图、告警快照、统计载荷等字典对象以 JSON 字符串形式持久化到数据库的 Python 后端服务。
- 原因：聚合 payload 经常混入 `datetime` 或 `date` 字段；若直接调用 `json.dumps()` 落库，标准 JSON 编码器不会自动处理这两类对象。
- 解决方法：在写入 `payload_json` 前，必须先做一层递归 JSON 安全转换，把 `datetime` / `date` 统一转成 ISO 8601 字符串，再执行序列化；不要假设上游 service 返回的聚合字典天然可直接 `json.dumps()`。
- 排查方式：当页面渲染前的 service 逻辑、事件持久化逻辑或缓存写入逻辑在 `json.dumps()` 处报 `datetime is not JSON serializable` 时，优先检查聚合 payload 中是否混入了时间字段。

# 2026-04-26 编辑现有聚合模型时，未显式操作的未绑定渠道不能被前端默认提交为新绑定

- 问题现象：在模型聚合列表中编辑某个已有模型并保存后，页面看起来像“生成了一个新模型”，实际是该模型被额外挂到了原本未绑定的渠道上。
- 适用范围：所有使用前端可编辑表格收集“是否绑定 / 是否启用 / 权重优先级”等布尔与数值组合配置的后台管理页面。
- 原因：编辑态表格里存在“当前未绑定”的候选行时，如果前端仅按最终 DOM 勾选状态组装提交载荷，而不区分“初始未绑定”和“用户本次显式勾选”，就可能把未实际操作的候选行误提交成新绑定。
- 解决方法：编辑态收集表格数据时，必须同时记录每一行的初始绑定状态和本次是否被用户显式改动；对初始未绑定且本次未被显式勾选的行，提交时必须保持 `bound=false`，避免误创建新绑定。
- 排查方式：当“编辑保存后多出一个模型”但主模型表并未新增记录、只是渠道绑定表多出一条关联时，优先检查前端编辑表单是否把未操作的候选渠道误当成新增绑定一起提交了。

# 2026-04-26 后台测试展示层若不展开正式代理双端点明细，会继续放大“测试失败但真实调用成功”的误判

- 问题现象：后端测试逻辑已经按正式代理口径同时探测 `/v1/chat/completions` 与 `/v1/responses`，但后台弹窗或批量测试卡片仍只显示单条模型总结果，用户看不到到底是哪一个正式端点成功、哪一个失败。
- 适用范围：所有把多端点探测结果汇总成 `model_results[].endpoint_results[]` 返回给前端的代理/提供商后台项目。
- 原因：展示层只消费模型级 `success/message` 聚合字段，没有继续展开 `endpoint_results`，导致定位信息在前端被截断，测试口径虽然已修正，观察口径却仍停留在旧的一维结果。
- 解决方法：前端测试弹窗和批量测试结果必须把每个模型下的正式端点明细一起展示，至少回显端点名、成功/失败、状态码、耗时和返回消息；模型级结构也应显式带上 `model_name`，避免列表展示退化成匿名结果。
- 排查方式：当后台测试页提示“模型失败”但真实外部调用至少一个正式端点成功时，优先检查前端是否遗漏了 `endpoint_results` 渲染，而不是先怀疑后端探测本身仍在走旧逻辑。

# 2026-04-26 说明型卡片如果只展示“操作顺序”文本而不绑定实际交互，会让用户误以为功能失效

- 问题现象：页面右侧展示了“第一层 / 第二层 / 第三层”的阅读顺序卡片，视觉上像可操作功能，但点击后没有任何滚动、聚焦或展开行为，用户会认为对应功能不能用。
- 适用范围：所有在后台页面中使用卡片、步骤块、操作层级块来承载“导航说明”或“阅读顺序”提示的前端界面。
- 原因：模板只输出了说明文案，未绑定按钮语义、目标锚点和点击事件；视觉层又与可点击卡片高度相似，导致用户预期和实际行为不一致。
- 解决方法：这类说明卡片若承担导航意义，必须直接绑定到页面真实交互，例如聚焦搜索框、滚动到概览区、定位到时间线，并给目标区域短暂高亮反馈；若不准备支持交互，则需要在视觉上明确降级为纯说明文本，避免误导。
- 排查方式：当用户反馈“这里为什么点了没反应”而页面本身并无接口报错时，优先检查该块是否只是静态文案却采用了近似按钮/卡片的视觉表达。

# 2026-04-26 管理端受保护页面白名单若遗漏新页面，会导致 `next` 回跳与前端路由守卫口径不一致

- 问题现象：新增了管理端页面后，服务端页面本身可以正常访问，但登录后携带 `next` 参数不会回跳到目标页，前端壳式路由守卫也可能把该页面误判成公开页或非管理页。
- 适用范围：所有同时维护“服务端登录后回跳白名单”和“前端壳式导航路由前缀白名单”的后台项目。
- 原因：管理端页面前缀被分别维护在后端角色白名单与前端路由前缀数组中；当新增页面时，只改其中一处，另一处遗漏，就会出现权限判断和回跳逻辑口径分裂。
- 解决方法：新增管理端页面时，必须同步更新服务端 `is_route_allowed_for_role` 使用的白名单与前端壳式导航的管理路由前缀；更稳妥的做法是把这类路由前缀收敛成单一配置源，避免双份维护。
- 排查方式：当管理员登录后无法按 `next` 返回目标页，或某个管理页在壳式导航里行为异常但直接刷新又能打开时，优先检查前后端是否遗漏了该页面前缀。

# 2026-04-26 后台“服务端分页”如果先全量序列化再内存切片，会在数据增长后退化成伪分页

- 问题现象：列表页在数据量较小时看起来有分页、筛选和搜索，但随着记录增多，接口响应会明显变慢，内存占用上升，导出、筛选和切页体验一起恶化。
- 适用范围：所有后台管理列表中声称支持服务端分页、服务端筛选，但实现里先把查询结果全量拉回应用层再做 Python 切片或状态过滤的项目。
- 原因：分页和部分状态过滤没有下推到数据库，而是先全量查询并序列化，再在内存中做二次筛选和 `start:end` 切片，导致每次翻页都在重复处理整个结果集。
- 解决方法：分页、筛选、排序和状态过滤必须尽量全部下推到数据库层完成；只有页面当前所需的数据行才允许进入序列化流程，避免把“分页接口”做成“全量查询接口”。
- 排查方式：当某个列表接口已经带了 `page/page_size` 参数，但翻到后面几页仍越来越慢，或代码里出现“先 list/serialize 全部结果，再 `start:end` 切片”时，应优先认定为伪服务端分页。

# 2026-04-26 需要支持复制的 API Key 明文不应在列表页或首页一次性批量注入 HTML / JSON

- 问题现象：用户首页、密钥列表或后台管理列表为了支持“一键复制”，会把多把 API Key 的明文直接放进模板数据、页面内联 JSON 或首屏接口响应中，导致打开页面就把全部明文暴露给浏览器 DOM。
- 适用范围：所有需要支持密钥明文复制、轮换后回显，但同时又存在首页聚合面板、列表页、多账号后台等场景的代理平台或密钥管理系统。
- 原因：为了图省事，页面直接复用了“带明文”的序列化结果，把复制能力做成了首屏预加载，而不是按单条记录、按动作、按权限即时获取。
- 解决方法：密钥明文应改为按需回显或单条拉取，例如在用户点击“复制/显示”时，通过受控接口或单条详情接口返回当前一把密钥的明文；列表页与聚合首页默认只返回掩码值，避免批量泄露面。
- 排查方式：当页面源代码、模板 `data-*` 属性、首屏 JSON 或列表查询接口中能直接看到多把明文密钥时，应优先整改为“默认掩码、按需回显”，而不是继续扩大复用范围。

# 2026-04-26 SSE 解析若只按 `\n\n` 分隔，会漏掉使用 `\r\n\r\n` 的上游流式事件

- 问题现象：流式代理对客户端本身还能持续转发字节流，但日志里的 `response_text`、`finish_reason`、`usage` 提取为空，自检页流式预览也可能看不到拼接后的输出文本。
- 适用范围：所有需要在代理层解析 SSE `data:` 事件以提取文本、Token 用量或结束原因的 Python 提供商服务。
- 原因：部分上游或网关会使用 CRLF 形式输出 SSE 事件边界；如果解析器只查找 `\n\n`，事件缓冲区不会被正确切片，导致内部解析永远吃不到完整 event。
- 解决方法：SSE 事件切分必须同时兼容 `\n\n` 与 `\r\n\r\n`，或先把缓冲区标准化为 LF 再解析；相关自检逻辑也要复用同一套解析器，避免正式链路和测试链路口径分裂。
- 排查方式：当客户端能收到流式内容，但服务端日志中的流式文本留痕、Token 统计或自检输出始终为空时，优先检查 SSE 解析是否只支持 LF 分隔。

# 2026-04-26 流式路由若不校验 `supports_stream`，会把本可规避的请求打到不支持流式的渠道

- 问题现象：某些模型的普通 JSON 请求可用，但相同模型一开 `stream=true` 就直接报上游 4xx/5xx，或者路由先命中不支持流式的渠道后才失败回退，导致首包变慢、失败率升高。
- 适用范围：所有同时维护“模型支持流式”能力标记和多渠道路由选择的 LLM 提供商项目。
- 原因：候选路由筛选只按模型名、启用状态和视觉能力过滤，没有把 `stream=true` 作为独立能力约束纳入候选集。
- 解决方法：路由层必须显式引入 `require_stream` 约束；当请求是流式调用时，只允许命中 `supports_stream=true` 的渠道模型，避免把兼容性检查留到上游报错后再兜底。
- 排查方式：当后台已明确把某个渠道模型标记为“不支持流式”，但真实流式请求日志仍显示它被优先命中时，应优先检查候选筛选条件是否遗漏了流式能力位。

# 2026-04-26 审计日志明细若直接 `json.dumps(model_dump())`，会被 `datetime` 字段击穿

- 问题现象：后台创建或编辑带 `expires_at` 等时间字段的对象时，业务数据已经写入成功，但接口在记录审计日志阶段抛出 `TypeError: Object of type datetime is not JSON serializable`，最终向前端返回 500。
- 适用范围：所有把 Pydantic `model_dump()` 结果直接作为审计明细、操作快照或数据库 JSON 文本落库的 Python 后端。
- 原因：`model_dump()` 默认保留 `datetime` 原对象；若审计服务直接把结果交给标准 `json.dumps()`，会在响应尾部阶段失败并把原本成功的写操作伪装成接口异常。
- 解决方法：审计明细落库前必须先做 JSON-safe 转换，例如统一使用 `model_dump(mode="json")`、显式时间字符串化，或在公共 `dumps_json` 中补充默认序列化策略。
- 排查方式：当后台写接口只在传入过期时间、时间窗口或时间戳对象时才报 500，且堆栈落在审计日志序列化阶段，应优先检查 `datetime` 是否未经转换直接进入了 JSON 编码器。

# 2026-04-26 共享账户钱包模式下，API Key 的 `balance_amount` 入参若被静默忽略，会让新建密钥首次调用即被余额拦截

- 问题现象：管理员给某个普通用户创建 API Key 时填写了初始余额，列表页也能看到密钥创建成功，但用户第一次通过该密钥调用 `/v1/*` 就收到 `insufficient_balance`。
- 适用范围：所有采用“用户账户共享余额池”模式，同时又允许在 API Key 创建表单中填写余额字段的代理平台。
- 原因：当 API Key 绑定了 `owner_user_id` 后，系统真实校验的是账户余额池；如果创建逻辑仍接受 `balance_amount` 入参却在共享钱包模式下直接丢弃，就会形成“表单允许填写、运行时完全不生效”的语义陷阱。
- 解决方法：共享钱包模式下必须二选一：要么在创建 API Key 时同步把初始金额入到账户余额池，要么在接口和界面上明确禁用该余额字段并提示“请到用户账户余额处充值”。
- 排查方式：当某个绑定用户账户的 API Key 已创建成功却首调即报余额不足，且创建请求里曾传入 `balance_amount` 时，优先检查共享钱包模式是否把该入参静默忽略了。

# 2026-04-26 APScheduler 的 `date` 任务若用 `datetime.utcnow()`，在本地时区调度器下会直接 misfire

- 问题现象：看起来已经成功调用 `scheduler.add_job(..., "date", run_date=...)`，但任务一创建就提示 `Run time of job ... was missed by 7:59:xx`，后续异步补齐、延迟写回、批量刷新等逻辑实际上没有执行。
- 适用范围：所有使用 APScheduler `AsyncIOScheduler` 或其他带本地时区配置的调度器，并用一次性 `date` 任务做延迟异步处理的 Python 后端项目。
- 原因：`datetime.utcnow()` 返回的是无时区的 UTC 时间；调度器若按本地时区解释该时间，就会把“UTC 当前时刻”当成本地时间，结果在东八区等环境下相当于把任务安排到 8 小时前，直接触发 misfire。
- 解决方法：给 `date` 任务设置 `run_date` 时，必须使用与调度器时区一致的当前时间，例如 `datetime.now()` 或显式带时区的 aware datetime；不要再把 naive UTC 时间直接传给 APScheduler。
- 排查方式：当延迟异步任务刚创建就立刻报 `missed by 7:59:xx`、`8:00:xx` 这类接近整时区偏移量的 misfire 时，应优先检查 `run_date` 是否错误地使用了 `datetime.utcnow()`。

# 2026-04-26 图片请求若切到 `requests` 分支但不保留上游 HTTP 状态，会把真实 4xx 误报成 502

- 问题现象：同一条 `/v1/responses` 或视觉请求，普通文本路径能正确返回上游 400/401/429，但图片输入走到同步 `requests.post(...)` 分支后，一旦上游拒绝，请求会在代理层被包装成通用异常，最终对客户端暴露成 502。
- 适用范围：所有在主代理链路里混用 `httpx` 与 `requests`，并依赖统一失败分类、熔断与重试逻辑的多渠道提供商项目。
- 原因：`requests.raise_for_status()` 抛出的异常类型与 `httpx.HTTPStatusError` 不同；若代理层只捕获 `httpx` 的状态异常，`requests` 分支就会落入通用 `Exception` 兜底，导致上游状态码、错误体和失败分类全部丢失。
- 解决方法：为 `requests` 分支单独提炼“带状态码和错误详情”的异常对象，或在分支内部统一转换成代理主链路可识别的错误类型；这样日志、重试、熔断和最终 HTTP 响应才能共享同一套语义。
- 排查方式：当只有图片或特定 HTTP/1.1 分支请求持续返回 502，而上游实际日志显示是 4xx 拒绝时，优先检查同步请求分支是否正确透传了状态码和错误体。

# 2026-04-26 公共日志服务签名若与调用方漂移，会让代理错误分支先因 Python 参数错误崩掉

- 问题现象：后台高密度列表页首屏依赖多个异步接口时，若后端列表接口先全量加载、全量序列化再内存分页，数据量增长后页面会长时间停留在“正在加载”或表现为空白；若初始化 Promise 失败但前端只弹 Toast，不渲染错误占位，用户会误以为页面没有数据。
- 问题原因：服务端分页接口没有真正下推到数据库 `count + limit/offset`，摘要接口也不应为了少量聚合指标加载完整对象集合；前端初始加载失败态缺失，会把接口慢、接口 500、引用数据加载失败都伪装成空白页。
- 解决方法：后台管理列表必须优先使用数据库侧分页、筛选和计数；摘要指标优先读取已维护的聚合字段或轻量 SQL 聚合；前端初始加载和刷新失败必须在表格/列表区域渲染明确错误占位，并保留重试入口或刷新按钮。
- 复用价值：后续新增 API Key、日志、模型、用户、账单、告警等异步数据页时，禁止先全量取数再前端或服务端内存分页；任何首屏异步加载区都必须同时具备加载态、空状态和失败态。

- 问题现象：`/v1/*` 请求一旦进入鉴权失败、模型不可用、上游失败或其他需要写日志的分支，接口不会返回预期的 4xx/5xx 业务错误，而是直接抛出 `TypeError: LogService.create_log() got an unexpected keyword argument ...`，最终把错误放大成 500。
- 适用范围：所有把日志写入统一收敛在公共 service 方法中，并由多个代理、鉴权、健康检查或后台模块共享调用的 Python 项目。
- 原因：`RequestLog` 模型和调用方已经增加了 `trace_id`、`source_ip`、`error_type`、`error_code`、`retryable`、账户额度快照等字段，但公共 `LogService.create_log()` 的函数签名和落库映射没有同步补齐，导致业务错误路径先被 Python 形参校验击穿。
- 解决方法：每次扩展日志模型或日志调用参数时，必须同时对齐三处：`RequestLog` 字段、`LogService.create_log()` 函数签名、`RequestLog(...)` 落库映射；并对错误路径补跑回归，确保“真正报业务错”而不是“先报参数错”。
- 排查方式：当请求只在失败分支、鉴权拒绝分支或日志增强分支报 500，且堆栈落在 `create_log()` 的 `unexpected keyword argument` 时，优先检查日志公共入口的签名是否落后于调用方。

# 2026-04-26 `responses` 图片子集校验若晚于路由筛选，会把参数错误误报成模型不可用

- 问题现象：客户端向 `/v1/responses` 发送图片输入，同时携带 `previous_response_id` 等当前图片适配子集明确不支持的字段时，接口返回的不是 `400` 参数错误，而是 `404 model_not_available`，调用方会被误导去排查模型或路由配置。
- 适用范围：所有对外提供标准端点、但内部只对某些请求形态开放“兼容子集适配”的代理或网关项目。
- 原因：请求先进入候选渠道筛选，等路由阶段走完后才做图片适配字段校验；一旦请求在前面就被路由挡住，就会掩盖真正的“字段不受支持”原因。
- 解决方法：这类“与渠道是否存在无关”的协议级参数校验必须在路由候选筛选之前完成；返回值要明确给出 `unsupported_fields` 与 `explicitly_unsupported_fields`，避免把接入错误伪装成资源不可用。
- 排查方式：当同一个请求既携带图片输入又携带适配层不支持字段，接口却返回 `404` 或“没有可用模型”而不是 `400` 时，优先检查校验顺序是否晚于路由筛选。

# 2026-04-26 后台流式探针若把 `stream_probe=true` 组装成 `stream=false`，会把非流式结果误当成流式验收

- 问题现象：后台“测试提供商 / 测试模型”接口显式传入 `stream_probe=true`，页面也显示在做流式探测，但后端实际构造出来的上游测试载荷仍是非流式请求，导致管理端以为已经验过 SSE 链路，实际只验了普通 JSON。
- 适用范围：所有同时提供“正式流式代理能力”和“后台探针/自检/健康测试”入口的代理或 LLM 网关项目。
- 原因：测试参数已经通过路由层传入 `stream_probe=True`，但探针构造函数在组装 chat 测试载荷时错误地写成了 `payload["stream"] = False`。
- 解决方法：探针既然声明要测流式，就必须把测试载荷中的 `stream` 明确置为 `true`，并把该行为纳入回归脚本，防止后续因为重构再次退化成“伪流式探测”。
- 排查方式：当后台测试页勾选或声明了流式探测，但抓包、上游日志或探针构造函数显示实际请求仍是非流式时，优先检查测试载荷的 `stream` 标记是否被错误写死。

# 2026-04-26 同一业务若在管理端和用户端长期维护两套分叉前端实现，功能很容易漂移到一侧可用另一侧失效

- 问题现象：管理员端“日志中心 / 会话回放”已经演进成完整的壳式交互页，但用户端仍保留独立的服务端模板实现；随着筛选项、详情弹层、回放链路和动态接口持续只在管理端增强，用户端会逐步出现功能缺失、交互失效或口径不一致。
- 适用范围：所有同时存在管理员端与用户端、且两端需要共享同类列表页/详情页/排障页能力的 Web 项目。
- 原因：同一业务被拆成两套独立页面和脚本维护，后续迭代优先修改了其中一套，另一套没有同步迁移，最终形成能力分叉。
- 解决方法：对这类“同源能力、仅权限范围不同”的页面，优先复用同一套前端交互骨架与数据结构，通过用户专属接口做权限收口；不要继续长期维护两份分叉模板。
- 排查方式：当管理员端页面可正常筛选、导出、回放，而用户端同名功能出现缺列、缺入口、不能联动或体验明显落后时，优先检查是否仍在维护两套不同实现。

# 2026-04-28 后台与业务页若长期堆放纯说明性卡片，会挤占操作空间并让页面目标失焦

- 问题现象：提供商管理、模型管理、日志、自检、密钥管理等页面堆放了“对象流程”“治理建议”“判断原则”“下一步动作”之类静态说明卡片，用户进入页面后先看到说明而不是数据、表单或可执行操作。
- 适用范围：所有以管理、排障、录入、调度、自检为主的后台页面和用户业务页面。
- 原因：页面迭代过程中把原本应该贴近控件的说明不断做成独立卡片或侧栏模块，导致信息密度上升但交互价值不增加。
- 解决方法：纯说明性内容不得再以独立卡片存在；页面主体优先保留实时数据、表单、表格、图表、按钮和结果反馈。必要说明只允许压缩到控件附近的内联提示、占位文案、Tooltip、空状态或执行结果说明中。
- 排查方式：当一个卡片不承载实时数据、不触发表单、不提供实际跳转或执行能力，只是在复述流程、建议、风险或顺序时，应优先判定为可移除的说明性卡片。

# 2026-04-28 Jinja2 模板若声明数据驱动区块却未从路由传入上下文，会形成“结构还在、内容全空”的伪完成页面

- 问题现象：模板里已经写了 `{% for feature in provider_features %}`、`{% for step in provider_steps %}`、`{% for note in provider_governance_notes %}` 这类数据驱动区块，但路由层未同步传入对应数组时，页面不会报出明显的前端交互错误，却会留下空白能力区、空白步骤区或空白治理说明区。
- 适用范围：所有使用 FastAPI + Jinja2Templates，把页面静态区块改造为后端数组循环渲染的后台或运维控制台项目。
- 原因：模板重构成数据驱动后，只改了 `templates/*.html`，没有同步修改 `routers/pages.py` 或等价渲染入口的上下文字段，导致模板和数据准备层脱节。
- 解决方法：每次引入新的 Jinja2 循环区块时，必须同时在路由层补齐对应上下文数组，并为这类关键区块增加页面级自检：至少确认“模板变量存在、长度非零、空状态文案可回退”三件事。
- 排查方式：当页面骨架、卡片边框和标题还在，但某一整组能力卡、步骤卡或说明卡完全不显示时，优先检查模板循环变量是否在 `TemplateResponse(..., {...})` 的上下文里被实际传入。

# 2026-04-28 筛选控件若继续复用普通工具栏布局，宽屏下也会退化成逐项堆叠或宽度漂移

- 问题现象：API 密钥、模型、会话、提供商可用率等页面把搜索框、状态、归属、时间窗口等筛选项放在普通 `table-toolbar` 中，部分页面在宽屏下仍出现每个控件独占整行或控件宽度不一致的问题。
- 适用范围：所有使用 Jinja2 模板和项目自定义 CSS 承载后台筛选栏、查询栏、对象目录搜索栏的前端页面。
- 原因：`table-toolbar` 同时被分页、批量操作和筛选区复用；普通 flex 工具栏适合动作按钮，但不适合多筛选项的等宽栅格排布。
- 解决方法：真正承担筛选或查询职责的工具栏必须加 `filter-toolbar`，表单式筛选区使用 `filter-grid`；分页、批量操作等非筛选区域继续使用各自专用类，避免误套筛选栅格。
- 排查方式：当页面出现搜索框、下拉筛选、页容量等控件但桌面端不是同排等宽时，优先检查模板容器是否缺少 `filter-toolbar` 或是否误用了普通 `table-toolbar`。

# 2026-04-29 模型定义若同时散落在模型页和提供商页，会导致路由、价格、展示口径分叉

- 问题现象：后台已存在模型聚合配置页，但提供商编辑页仍可直接维护模型清单和单价，调用测试、可用模型、`/v1/models`、仪表盘统计等位置可能分别读取渠道模型或聚合模型，最终出现同一模型在不同页面启停、价格和可见性不一致。
- 适用范围：所有同时存在模型目录、渠道挂载、API Key 授权、路由候选和用户可见模型列表的 LLM 网关项目。
- 原因：模型基础信息与渠道挂载信息没有明确分层；模型基础价格、展示名、速度标签、全局启用状态应属于统一模型目录，而渠道页只应维护绑定关系、倍率、优先级、权重和健康状态。
- 解决方法：以 `model_catalogs` 或等价模型目录作为唯一模型主视角；渠道模型表仅保存挂载关系和渠道级运行参数。路由候选、用户可见模型、playground 模型下拉、仪表盘模型总数必须优先读取统一模型目录，并在模型或绑定变更后清理路由和模型列表缓存。
- 排查方式：当某个模型在后台模型页已停用或改价，但测试页、用户端、`/v1/models` 或提供商页仍显示旧状态时，优先检查是否有代码直接把 `provider_models` 当作模型主数据源。

# 2026-04-29 页面初始化函数若引用其他页面局部变量，会导致当前页面数据加载前静默中断

- 问题现象：模型配置页模板和接口都存在，数据库也有模型数据，但页面表格一直显示空状态；浏览器控制台可见 `ReferenceError: templateForm is not defined`，且 `/api/models` 没有被正常触发或渲染结果没有落到表格。
- 适用范围：所有把多页面前端逻辑集中在同一个 `app.js` 中，并通过 `body[data-page]` 分发初始化函数的 Jinja2 / 原生 JS 后台项目。
- 原因：某个页面初始化函数中误混入了其他页面的事件绑定逻辑，并直接引用了其他函数作用域里的 `const` 变量；即使写成 `templateForm?.addEventListener(...)`，未声明变量仍会先触发 `ReferenceError`。
- 解决方法：页面专属 DOM 查询、事件绑定和提交逻辑必须放在对应页面初始化函数作用域内；共享逻辑只能抽成独立函数并显式传参，禁止跨初始化函数直接引用局部变量。修改 `app/static/` 下脚本后必须同步更新模板中的静态资源版本参数。
- 排查方式：当页面骨架正常但接口数据不渲染时，优先检查当前 `initXxx()` 是否在数据加载前引用了其他页面的 DOM 变量或函数局部变量，并用控制台错误定位第一个中断点。

# 2026-04-29 API Key 授权维度若只在后端字段存在而未贯穿模板、用户端和代理列表，会形成“配置可存但链路不可控”

- 问题现象：API Key 已有模型白名单字段，代理调用也可能读取该字段，但后台表单、策略模板、用户自检或用户可用模型未同步接入时，管理员无法稳定配置，普通用户页面和 `/v1/models` 容易出现口径不一致。
- 适用范围：所有同时支持渠道授权、模型白名单、策略模板、用户端自检和 OpenAI 兼容 `/v1/models` 的网关项目。
- 原因：授权维度横跨管理端 UI、模板服务、API Key 服务、路由服务、用户可见模型和代理服务；只补其中一层会让“保存、展示、路由、测试、对外返回”任一环节漂移。
- 解决方法：新增或修复 API Key 授权字段时，必须同时检查表单回显与提交、策略模板继承、后端校验、用户端可用模型、自检候选和 `/v1/models` 过滤；空白名单应统一表示“不限制模型，但仍受渠道、模型目录和健康状态约束”。
- 排查方式：当管理员认为已限制某 API Key 的模型，但用户页、调用测试或 `/v1/models` 仍看到其他模型时，优先沿“模板 -> API Key -> 路由候选 -> 用户模型 -> 代理列表”逐层检查字段是否丢失。

# 2026-04-29 删除模型目录项若不清理授权引用，会留下不可见但仍存储的白名单脏数据

- 问题现象：模型聚合配置中删除某个模型后，API Key 或策略模板的模型白名单仍可能保存旧模型名；后续管理页已无法选择该模型，但历史授权数据仍残留，容易造成回显、批量套用和排查口径不一致。
- 适用范围：所有使用模型目录作为主数据源，并允许 API Key、策略模板、用户自检或代理列表引用模型名做授权过滤的网关项目。
- 原因：新增和编辑 API Key 时做了模型存在性校验，但模型删除流程没有同步清理引用该模型名的授权 JSON 字段，导致删除只影响模型目录和渠道挂载，不影响下游授权范围。
- 解决方法：模型目录删除时必须同时清理所有授权作用域中的对应模型名，包括 API Key 白名单和 API Key 策略模板白名单；清理完成后必须刷新路由候选与 `/v1/models` 缓存。
- 排查方式：当模型目录已不存在某模型，但 API Key 详情、模板套用、自检结果或导出数据中仍出现该模型名时，优先检查删除流程是否缺少授权引用清理。


- 问题现象：代理链路虽然使用异步 HTTP 客户端，但在正式高并发流量下容易出现请求排队、日志写入变慢、配额统计延迟、`database is locked` 或整体吞吐无法随机器配置提升。
- 解决方法：正式高并发部署必须迁移到 PostgreSQL/MySQL 等服务型数据库，按数据库配置 SQLAlchemy 连接池；用 Gunicorn/Uvicorn 多 worker 或容器多副本运行；请求日志、Token 统计和计费落库应进入后台队列或批量写入；配额与限流应使用 Redis 原子计数或数据库原子更新，避免每次请求扫描日志聚合。



# 2026-04-29 systemd ExecStart 需要 shell 才能使用 `${VAR:-default}` 默认值展开

- 问题表现：在 `systemd` 的 `ExecStart=` 中直接写 `python -m gunicorn ... -w ${WEB_CONCURRENCY:-4}` 时，不能按普通 Bash 脚本语义可靠展开默认值，可能导致 Gunicorn 参数不符合预期。
- 适用范围：所有由 `systemd` 读取 `.env` / `EnvironmentFile` 并需要在启动命令中使用 shell 默认值、变量拼接或复杂参数展开的服务。
- 原因：`systemd` 不等同于 Bash，`ExecStart` 默认不会执行 shell 参数展开；`EnvironmentFile` 只负责注入环境变量，不负责解释 `${VAR:-default}`。
- 解决方法：若启动命令确实需要 shell 默认值展开，必须使用 `/bin/bash -lc 'exec ... ${VAR:-default} ...'`；若不需要默认值展开，则应在部署脚本中提前把默认值写入 `.env`，并在 `ExecStart` 中使用确定参数。
- 排查方式：服务启动后执行 `systemctl cat <service>` 和 `journalctl -u <service>`，检查最终命令、Gunicorn worker 数、timeout 等参数是否与 `.env` 一致。

# 2026-04-29 Gunicorn 是 Linux 生产启动组件，不适合作为 Windows 本地启动方式

- 问题表现：在 Windows 本地执行 `python -m gunicorn --version` 或尝试用 Gunicorn 启动应用时，会因为缺少 Unix 专用模块 `fcntl` 失败。
- 适用范围：所有需要区分 Windows 本地开发启动和 Linux 生产多 worker 启动的 FastAPI 项目。
- 原因：Gunicorn 面向 Unix/Linux 进程模型设计，依赖 Windows 不提供的系统能力；Windows 本地适合继续使用 Uvicorn 或项目 `run.ps1` 做开发调试。
- 解决方法：Windows 环境只验证 Gunicorn 依赖已安装、配置已写入、部署脚本内容正确；Gunicorn 实际启动、worker 数和 systemd 行为必须在目标 Linux / 阿里云主机验证。
- 排查方式：若 Windows 上出现 `ModuleNotFoundError: No module named 'fcntl'`，不要按应用代码错误处理，应切换到 Linux 环境验证 Gunicorn。

# 2026-04-29 前端刷新按钮必须同时检查模板节点、JS 绑定、接口数据与刷新后渲染一致性

- 问题现象：按钮本身存在且接口可调用，但刷新后页面缺少最近刷新时间、加载态、参考数据未重新拉取，或动态渲染结果比服务端初始模板少了操作入口。
- 适用范围：所有把多页面交互集中在同一个 `app/static/js/app.js` 中，并通过 Jinja2 模板提供按钮与容器节点的后台/用户端页面。
- 原因：刷新链路不只包含点击事件，还依赖模板节点、页面状态、参考数据接口、结果渲染函数和静态资源版本；任一层漂移都会造成“看似能点，实际刷新不完整”。
- 解决方法：检查刷新按钮时必须逐项核对：模板按钮 ID 是否存在、JS 是否绑定点击事件、刷新期间是否有 loading 态、是否重新拉取必要参考数据、是否更新最近刷新时间或 toast、动态渲染结构是否与初始模板保留同等操作能力；修改 `app/static/` 后必须同步更新 `base.html` 资源版本。
- 排查方式：当用户反馈刷新按钮无效或刷新后内容缺操作、缺筛选、缺状态时，优先沿“模板按钮 -> JS 绑定 -> API 请求 -> DOM 渲染 -> 反馈状态 -> 静态版本号”逐层检查。

# 2026-04-29 多 worker 下进程内存并发计数不能代表全局容量

- 问题现象：单 worker 或本地功能验证时 provider 活跃请求上限能生效，但切到 Gunicorn 多 worker 后，每个 worker 都维护自己的内存计数，实际全局可打到同一 provider 的请求数可能放大到 `worker 数 × 单 worker 上限`。
- 适用范围：所有用 Gunicorn/Uvicorn 多 worker 部署，并通过 Python 进程内字典、锁或本地变量实现并发计数、QPS 计数或租约的代理网关。
- 原因：Gunicorn worker 是独立进程，内存状态不共享；进程内计数只能约束当前 worker，不能在多 worker、容器多副本或多机部署中提供全局精确限制。
- 解决方法：进程内计数只用于短期低阈值功能验证；生产全局容量控制必须迁移到 Redis、数据库原子计数或其他共享状态，并用原子递增、TTL 租约和幂等释放避免重复释放或泄漏。
- 排查方式：若生产中 provider 明明设置了上限但上游仍被多个 worker 同时打爆，优先检查限流计数是否仍在进程内存中，而不是 Redis 等共享存储中。

# 2026-04-29 StreamingResponse 的并发租约必须在生成器 finally 中释放

- 问题现象：非流式请求能在函数返回前释放并发计数，但 SSE 流式请求若在返回 `StreamingResponse` 前释放，会导致长连接仍在传输时计数已回落；若只在外层异常释放，客户端断开或生成器内部异常又会遗留租约。
- 适用范围：所有 FastAPI / Starlette 中使用 `StreamingResponse` 承载 SSE、文件流、上游代理流，并需要并发计数或资源租约的接口。
- 原因：`StreamingResponse` 返回后，实际数据发送发生在异步生成器迭代期间；路由函数的 `finally` 无法覆盖整个流式生命周期。
- 解决方法：流式租约必须包裹在异步生成器中，并在生成器 `finally` 释放；若返回响应前发生异常，则由路由外层异常分支释放。释放逻辑必须幂等，避免生成器和外层异常重复释放时计数为负。
- 排查方式：若流式请求开始后 Redis 活跃计数立刻归零，或客户端断开后活跃计数不回落，优先检查租约释放是否放在 `StreamingResponse` 的生成器生命周期内。

# 2026-04-29 API Key 鉴权缓存必须覆盖所有授权与账户写操作的主动失效

- 问题现象：API Key 鉴权结果被 Redis 缓存后，如果编辑、禁用、轮换、删除密钥，或调整用户启停、额度、余额、provider 授权、模型白名单时没有主动清理缓存，请求可能在 TTL 内继续按旧权限通过。
- 适用范围：所有使用短 TTL 缓存外部 Bearer API Key 鉴权上下文的网关、代理和计费系统。
- 原因：鉴权缓存保存的是一次数据库鉴权后的安全快照；只依赖 TTL 过期会让安全写操作存在短暂滞后窗口。
- 解决方法：缓存 Key 只能基于密钥 SHA256 哈希，禁止保存明文 Key；所有 API Key、用户账户、授权 provider、模型白名单、额度和余额写操作完成后必须调用对应缓存失效方法，优先清理具体 API Key，其次清理所属用户下的全部 API Key 缓存。
- 排查方式：禁用 API Key 或账户后仍能调用 `/v1/*` 时，优先检查写操作路径是否调用了 `ApiKeyAuthCache.invalidate_api_key` 或 `ApiKeyAuthCache.invalidate_user`。

# 2026-04-29 计费后台任务必须以日志记录为幂等源，不能按任务执行次数扣费

- 问题现象：Token 回填或计费任务失败重试后，如果每次任务都按当前日志金额直接扣余额，会导致同一条请求日志重复生成账单或重复扣减账户余额。
- 适用范围：所有把日志补全、Token 估算、账单生成和余额扣减放入后台任务的 API 网关或计费系统。
- 原因：后台任务天然可能重复执行、超时重试或被多个 worker 抢到；如果没有按 `request_log_id` 查找既有账单并计算差值，扣费就不具备幂等性。
- 解决方法：计费任务必须以 `request_log_id` 或 `request_id` 作为幂等源，先读取既有账单金额，再按新旧金额 delta 更新 API Key 累计消费和账户余额；任务状态需记录 `billing_finalized_at`、`billing_event_id`、`billing_attempt_count` 和错误信息。
- 排查方式：发现余额扣减大于日志总费用，或同一 `request_log_id` 出现重复账单时，优先检查后台计费任务是否按日志 ID 做差值扣费和完成标记。

# 2026-04-29 流式 read timeout 设为 None 后必须在生成器层控制空闲与最长持续时间

- 问题现象：SSE 上游连接已经建立，但上游长时间不再发送 chunk，或者异常长连接持续占用 worker、socket、上游连接池和 Redis 活跃流式计数。
- 适用范围：所有使用 httpx/异步客户端代理 SSE，并为了支持长流式响应把 read timeout 设为 `None` 的代理网关。
- 原因：连接层 read timeout 关闭后，客户端库不会主动终止空闲或超长读取；如果生成器层没有单独的空闲超时和最大持续时间，租约释放只能等连接自然结束。
- 解决方法：流式连接超时、首 Token 超时、空闲 chunk 超时和最大持续时间必须分层配置；read timeout 可保持 `None`，但每次读取上游 chunk 必须通过 `asyncio.wait_for` 或等价机制施加生成器层超时，并在 `finally` 释放租约。
- 排查方式：若 Redis 流式活跃计数长期不回落、上游连接池被少量流式请求占满，优先检查流式生成器是否只依赖 httpx read timeout，而缺少空闲和最大持续时间控制。

# 2026-04-30 Windows PowerShell 5.1 执行无 BOM UTF-8 中文脚本可能误解析变量

- 问题现象：`run.ps1` 在 PowerShell 7 中可正常解析，但用 Windows PowerShell 5.1 后台执行时，中文输出文本被误读成乱码，并在包含 `$变量:` 形态的字符串附近报 `InvalidVariableReferenceWithDrive` 或多余 `}` 解析错误。
- 适用范围：所有包含中文字符串、以无 BOM UTF-8 保存，并需要兼容 Windows PowerShell 5.1 的 `.ps1` 启动脚本。
- 原因：Windows PowerShell 5.1 对无 BOM UTF-8 文件的默认编码识别不稳定，中文字节被按系统 ANSI 编码误读后可能改变脚本文本结构，引发变量边界解析错误。
- 解决方法：本地优先使用 PowerShell 7 的 `pwsh.exe -File .\run.ps1` 启动；若必须兼容 Windows PowerShell 5.1，则将脚本保存为 UTF-8 with BOM，并把字符串中紧跟冒号或特殊字符的变量写成 `${变量名}`。
- 排查方式：若 `run.ps1` 明明在编辑器中语法正常，但后台启动日志出现乱码和 `InvalidVariableReferenceWithDrive`，优先检查执行宿主是否为 Windows PowerShell 5.1，以及脚本是否为无 BOM UTF-8。
## 2026-04-30：`autoflush=False` 下同步派生表前必须显式 `flush`

- 问题：项目数据库 Session 使用 `autoflush=False`。创建或更新 Provider 后立即调用模型目录同步时，新增的 `ProviderModel` 若尚未 `flush`，后续查询无法读到，导致 `model_catalogs` 未生成，路由候选为空并出现 `model_not_available`。
- 解决方法：在替换 Provider 模型配置并刷新 Provider 状态后，调用 `db.flush()`，再执行 `ModelCatalogService.sync_model_catalogs(db)`。
- 复用场景：任何“先新增/修改 ORM 对象，再用查询同步派生表、缓存、统计表或关联索引”的链路，都必须在查询前显式 `flush`。


- 复用场景：环境切换、数据库类型迁移、开发库转正式库时，如果用户确认不需要历史日志，应优先采用“配置数据迁移 + 日志数据清空”的策略，降低迁移风险和后续清理成本。

# 2026-04-30 提供商测试因单个上游模型不可用导致整体失败

- Context: 管理端提供商测试会同时探测当前提供商所有已启用模型的 `/chat/completions` 与 `/responses`。若任一已启用模型在两个正式端点均失败，提供商整体结果会显示失败或降级。
- Symptom: `formal proxy probe failed for all models` 或 `formal proxy probe success, models N/M healthy`；模型明细中上游返回 `service_unavailable`、`全部渠道不可提供当前模型`。
- Fix: 先直接验证上游 `/models`、单模型 `/chat/completions`、`/responses`；确认是单个模型不可用时，只禁用该提供商下对应 `provider_models.enabled`，并通过 `ProviderService.update_provider_model` 刷新 provider 状态、模型目录与路由缓存，避免直接裸 SQL 修改导致缓存不一致。
- Prevention: 批量导入上游模型后必须逐个正式端点验收；不可用模型禁止保持启用状态，否则会拖累提供商测试结果和路由候选健康度。

# 2026-04-30 async 请求主链路禁止直接执行同步 DB 或同步 Redis IO

- 问题：FastAPI async 端点中若直接调用同步 SQLAlchemy `Session` 查询/提交，或在 async 鉴权依赖中调用同步 Redis 客户端，1000 并发下会阻塞事件循环，导致请求排队、SSE 首 Token 延迟升高和整体吞吐下降。
- 解决方法：对外 `/v1/*` 主链路必须使用 `redis.asyncio` 的统一 `RedisService` 客户端；仍需复用同步 SQLAlchemy 时，必须把 DB 查询、日志写入、provider 状态更新等阻塞片段集中放入 `run_in_threadpool`，并在线程池内创建短生命周期 `SessionLocal()`。
- 复用场景：新增 async 路由、鉴权依赖、限流依赖、代理主链路、SSE 生成器日志落库、provider 路由候选查询时，都应先检查是否存在同步 IO 直接运行在事件循环线程中。

# 2026-04-30 实时配额判断不能依赖 request_logs 聚合，余额扣减必须条件更新

- 问题：若 API Key 或账户的日/月请求、Token、费用额度在鉴权链路通过 `request_logs` 聚合判断，高并发下日志表会成为热点；若计费只读取余额对象后在内存中修改，多个后台计费任务并发时可能滞后放行或扣成负数。
- 解决方法：请求数、Token、费用等实时额度必须前移到 Redis 计数，数据库日志仅用于异步归档、展示和对账；余额扣减必须使用数据库条件更新或等价原子策略，例如 `WHERE balance_amount - frozen_amount >= delta`，扣减失败时中止计费并进入重试/告警流程。
- 复用场景：新增限流、账户额度、余额扣费、后台账单重算、日志回填任务时，都必须区分“实时准入计数”和“历史统计展示”，禁止把大日志表聚合放回对外请求主链路。

# 2026-04-30 多 worker 后台任务必须有分布式锁，监控统计禁止整批加载日志对象

- 问题：Gunicorn 多 worker 下每个 FastAPI lifespan 都可能启动 APScheduler，若健康检查、日志清理、批量回填等共享任务没有分布式锁，会重复执行；管理端监控若把窗口内 `RequestLog` ORM 全量加载到 Python 计算百分位或时间序列，高流量后会放大数据库和应用内存压力。
- 解决方法：共享定时任务必须通过 Redis `SET NX EX` 或等价机制加锁，锁释放必须校验 token，Redis 不可用时生产链路应跳过共享任务而不是无锁并发执行；监控统计必须优先用数据库聚合，只允许读取有限数量的必要标量列做近似百分位或峰值估算。
- 复用场景：新增 APScheduler 任务、批量后台任务、监控接口、日志统计接口、管理端看板和 `/metrics` 输出时，都必须先检查多 worker 重复执行与大窗口日志加载风险。

# 2026-04-30 多 worker 下共享缓存与启动 DDL 必须按生产边界拆分

- 问题：进程内 dict 缓存只在当前 worker 生效，路由候选、模型列表、provider 状态等数据在多 worker 下会短时间不一致；Web worker 启动时自动 `create_all` 或执行迁移逻辑，在多 worker、滚动发布或多实例部署时会产生并发 DDL 风险。
- 解决方法：可 JSON 序列化且影响正式流量一致性的缓存优先写入 Redis，无法安全序列化的 ORM 对象不做跨 worker 缓存；生产 Web worker 启动必须跳过 DDL，数据库初始化或迁移只能由独立脚本、Alembic 或部署前步骤执行。
- 复用场景：新增缓存、路由派生数据、模型列表、provider 状态摘要、启动初始化逻辑或数据库结构变更时，都必须先判断是否跨 worker 共享以及是否会在生产启动阶段触发 DDL。

# 2026-04-30 Windows 中文路径下虚拟环境校验禁止依赖 stdout 路径精确比较

- 问题：`run.ps1` 在中文项目路径下调用 `.venv\Scripts\python.exe` 后，PowerShell 接收到的 `sys.executable` 或 `sys.prefix` 输出可能发生编码失配，导致脚本用字符串精确比较时误判项目虚拟环境无效。
- 解决方法：虚拟环境归属校验必须尽量放在 Python 进程内部完成，并通过退出码回传结果；校验重点应为 `sys.prefix` 是否等于期望 `.venv`、`sys.prefix != sys.base_prefix`、`.venv\pyvenv.cfg` 是否存在，避免依赖中文路径 stdout 文本比较。
- 复用场景：Windows 启动脚本、部署脚本或健康检查脚本需要校验 Python 虚拟环境时，优先使用进程内部判断与退出码，不要把非 ASCII 路径打印到宿主 shell 后再做精确比较。
## 2026-04-30 22:15:24 +08:00 - API Key 详情累计 Token 与日志统计不一致

问题：回归测试中，API Key 详情接口返回的 `total_tokens_used` 为 0，但同一 Key 的请求日志与最近使用统计已经记录了 50 Token。

原因：详情接口读取 `api_client_keys` 表中的累计计数字段，而日志最终化链路已经写入 `request_logs`；当累计计数字段未同步回填时，会出现详情累计值与日志聚合值不一致。

解决方法：在 API Key 详情序列化前调用 `ApiKeyService.reconcile_usage_counters`，以成功请求日志为统一口径回填当前 Key 的累计 Token 计数，再刷新对象并返回详情。

复用规则：后续若出现“详情累计值”和“日志聚合值”不一致，必须优先检查是否存在异步最终化后未回填累计字段的问题，并以 `request_logs` 成功请求聚合结果作为校准口径。

## 2026-04-30 22:28:17 +08:00 - 生产启动脚本禁用 worker 启动 DDL 后必须单独初始化数据库

问题：生产环境按规范设置 `ENABLE_STARTUP_DB_INIT=false` 后，Gunicorn worker 不会在启动阶段执行 `create_all` 或迁移逻辑；如果一键部署脚本只启动服务而不先执行独立数据库初始化，新 PostgreSQL 数据库可能缺表，服务启动后健康检查或业务接口失败。

解决方法：生产部署脚本必须在启动或重启 Web worker 前执行一次独立、可重复的数据库初始化步骤，例如调用项目虚拟环境中的 `scripts/run_startup_db_init.py`；Web worker 仍保持 `ENABLE_STARTUP_DB_INIT=false`，避免多 worker 并发 DDL。

复用规则：后续调整 Linux、systemd、Docker 或 CI/CD 部署流程时，必须把“数据库初始化/迁移”和“Web worker 启动”拆成两个步骤，禁止因为启动脚本一键化而把生产 DDL 放回 worker lifespan。

## 2026-04-30 23:35:00 +08:00 - sudo 前置环境变量会导致 RUN_DB_INIT 未传入部署脚本

问题：在服务器执行 `RUN_DB_INIT=1 sudo bash start_aliyun.sh` 时，`sudo` 默认会清理调用方环境变量，脚本内读取到的 `RUN_DB_INIT` 仍可能是默认值 `0`，日志表现为 `Skipping database initialization. Set RUN_DB_INIT=1 to run it explicitly.`，导致生产 PostgreSQL 新库未创建表，Gunicorn worker 启动失败，健康检查无法连接 `127.0.0.1:8000`。

解决方法：需要显式把变量传入 `sudo` 后的命令环境，例如执行 `sudo RUN_DB_INIT=1 bash start_aliyun.sh`；如果仍失败，再用 `journalctl -u aotu-gpt -n 200 --no-pager` 查看真实 Python 异常。

复用规则：Linux 部署脚本依赖环境变量开关时，排障必须先确认脚本日志是否真实显示开关生效；涉及 `sudo` 时优先使用 `sudo VAR=value command` 形式，避免变量被环境清理。

## 2026-04-30 23:42:00 +08:00 - 绝对路径执行 scripts 下 Python 脚本时必须显式设置项目根目录导入路径

问题：部署脚本使用 `/opt/auto-gpt/.venv/bin/python /opt/auto-gpt/scripts/run_startup_db_init.py` 这类绝对路径执行脚本时，Python 默认把脚本所在目录 `/opt/auto-gpt/scripts` 放入 `sys.path[0]`，不会自动包含项目根目录 `/opt/auto-gpt`，导致脚本内 `from app.config import get_settings` 报 `ModuleNotFoundError: No module named 'app'`。

解决方法：部署脚本执行项目内一次性 Python 脚本时，必须显式设置 `PYTHONPATH="$PROJECT_ROOT${PYTHONPATH:+:$PYTHONPATH}"`，或改用从项目根目录可解析模块的执行方式，确保 `app` 包可导入。

复用规则：凡是 Linux 部署、CI/CD、systemd、cron 或一次性维护脚本需要执行 `scripts/` 下 Python 文件并导入项目包时，必须先确认项目根目录在 Python 导入路径中。

## 2026-04-30 23:58:00 +08:00 - 数据库初始化不等于迁移本地业务数据


解决方法：正式迁移前必须先备份源库和目标库，再按依赖顺序迁移用户、Provider、模型、API Key、授权关系和系统设置等配置数据；不需要保留历史日志时，可按既定策略清空 `request_logs`、审计日志、告警事件和账单流水。

复用规则：部署新服务器时必须明确区分“初始化表结构”和“迁移业务数据”；看到初始化管理员页面时，优先检查目标库 `user_accounts` 中是否存在 `role='admin'` 的账号，而不是重复执行建表初始化。

## 2026-05-02 10:52:09 +08:00 - 删除提供商前必须先清理引用关系

问题：提供商被系统默认配置、API Key 默认提供商、API Key 授权关系或请求日志引用时，直接删除 `providers` 记录会触发外键约束失败，表现为管理页点击删除某个具体提供商失败。

解决方法：删除提供商前必须先将 `app_settings.default_provider_id`、`api_client_keys.default_provider_id`、`request_logs.provider_id` 中指向该提供商的值置空，并删除 `api_client_key_provider_bindings` 中对应授权关系；删除后同步失效相关 API Key 鉴权缓存、用户缓存、路由候选缓存和模型列表缓存。

复用规则：后续删除被配置、授权或日志引用的核心对象时，必须先梳理所有外键和缓存引用，按“解除引用 → 删除对象 → 失效缓存/同步派生目录”的顺序处理，避免页面删除失败或缓存继续命中旧对象。

## 2026-05-02 11:18:00 +08:00 - 缓存 Token 必须从上游 usage 统一提取后再计费

问题：不同上游对缓存命中和缓存写入 Token 使用不同字段名，例如 `cache_read_tokens`、`cached_tokens`、`input_tokens_details.cached_tokens`、`cache_creation_input_tokens`，且部分响应会嵌套在 `response.usage` 中；若只读取单一字段，日志、账单和导出会出现缓存用量缺失或计费口径不一致。

解决方法：缓存 Token 提取必须集中复用 `LogService.extract_cache_tokens`，同时支持常见 snake_case、camelCase 和嵌套 usage 字段；异步 Token 补全任务必须在计费前把 `cache_read_tokens`、`cache_write_tokens` 写回 `request_logs`。

复用规则：后续新增上游兼容格式或调整计费字段时，必须先扩展统一提取函数，再复用到代理同步日志、异步补全、账单计算和导出展示链路，禁止在各模块分别手写字段解析。

## 2026-05-02 11:35:00 +08:00 - Python 条件表达式与 or 混用必须显式加括号

问题：新增提供商时，前端已经传入 `base_url` 和 `api_key`，但获取上游模型仍提示必须提供 Base URL 和 API 密钥。原因是后端使用了 `payload.base_url or provider.base_url if provider else None`，在 `provider` 为空时整个表达式直接返回 `None`，导致新增场景忽略了用户输入。

解决方法：条件回退逻辑必须写成 `payload.base_url or (provider.base_url if provider else None)`，并把同类的 `api_key`、`timeout_ms` 一并拆成清晰变量，避免依赖运算符优先级。

复用规则：后续在服务层实现“优先使用请求值，否则回退已有对象值”的逻辑时，禁止把 `or` 与三元条件表达式裸混在一起；必须拆成中间变量或显式括号，尤其是新增对象时对象可能为 `None` 的场景。

## 2026-05-02 11:52:00 +08:00 - 复制按钮不能只依赖 navigator.clipboard

问题：云服务器页面若通过非安全上下文、反向代理异常协议、嵌入页面或部分移动浏览器访问，`navigator.clipboard.writeText` 可能不可用或被权限拦截，导致所有复制按钮随机失败。

解决方法：全站复制能力必须集中封装，优先使用 `navigator.clipboard.writeText`，失败或不满足安全上下文时回退到临时 `textarea` 选中并执行 `document.execCommand("copy")`；复制按钮自身必须显示复制中、已复制或复制失败状态。

复用规则：后续新增复制入口时必须使用统一的 `data-copy-text` 或统一复制函数，禁止在业务模块中直接调用 `navigator.clipboard.writeText`。

## 2026-05-02 16:40:00 +08:00 - PostgreSQL 升级新增 ORM 字段前必须先补结构

问题：代码新增 ORM 映射字段后，生产 PostgreSQL 旧库只通过 `create_all` 无法自动补列；若初始化流程在补列前执行 ORM 查询或同步任务，会报 `UndefinedColumn`，例如 `provider_models.cache_price_per_1k`、`request_logs.channel_price_cache_per_1k` 缺失，并导致 `/ready` 返回 503。


复用规则：后续新增 SQLAlchemy 模型字段时，必须同步检查 PostgreSQL 旧库升级路径，确保迁移函数在任何 ORM 查询新字段之前执行；Web worker 启动阶段仍禁止执行 DDL。

## 2026-05-02 17:10:00 +08:00 - `/ready` 可服务判断不能被历史后台积压直接置为 503

问题：服务进程、数据库和 Redis 均正常时，历史 `request_logs` 待补全数量过多会触发后台积压告警；若 `/ready` 直接因该告警返回 503，部署脚本会误判服务不可用，导致更新流程失败。

解决方法：`/ready` 的 HTTP 503 仅用于核心依赖不可用，例如数据库或 Redis 不可用；后台积压、近期错误率等应保留在响应体和告警中心中展示，不应阻断基础可服务性探测。

复用规则：后续新增健康检查指标时，必须区分“服务能否接流量”的 readiness 条件和“需要运维关注”的 monitoring 告警，禁止把所有 warning 都直接转成 `/ready` 503。

## 2026-05-02 17:30:00 +08:00 - 健康检查日志不能混入用户请求日志长期保留和计费补全

问题：健康检查日志写入 `request_logs` 后，如果不单独标记完成或短周期清理，会长期堆积并被后台待补全统计误认为计费/Token 任务积压。

解决方法：健康检查类日志写入时必须直接标记为 `billing_status='skipped'` 且设置 `billing_finalized_at`；数据保留任务必须固定清理 6 小时前的健康检查日志，并且用户请求/响应日志的长期保留策略不得影响健康检查日志短周期清理。

复用规则：后续新增巡检、探测、后台测试等非用户调用日志时，必须先明确是否参与用户日志统计、计费补全和长期保留；不参与的日志必须在写入和清理链路中显式排除。

## 2026-05-02 17:55:00 +08:00 - 表单分区不能用 label 包裹复杂表格

问题：模型编辑弹窗中“渠道绑定与倍率”用 `<label>` 包裹整张表格，浏览器会把表格内点击行为关联到 label 内的第一个控件，导致点击任意渠道区域都像是在切换第一行渠道。

解决方法：复杂表格、分区说明和多控件组合必须使用 `div`、`fieldset` 或语义化分区承载；`label` 只包裹单个输入控件或通过 `for` 指向唯一控件。

复用规则：后续新增弹窗表单、批量编辑表格或绑定关系表时，禁止用一个 `<label>` 包裹多个可交互控件；否则会出现焦点、点击命中和无障碍语义错乱。

## 2026-05-02 18:20:00 +08:00 - 模型基础属性与提供商路由属性必须分层维护

问题：把提供商模型行简化时，误将渠道倍率、优先级、权重等提供商特有路由字段也移出提供商配置，导致提供商新增/编辑无法直接维护自身路由策略。

解决方法：模型基础价格、模型能力等跨提供商共享属性归模型管理维护；提供商模型启用状态、渠道倍率、优先级、权重等单个提供商的路由属性必须保留在提供商配置中。服务层同步时，模型能力从模型目录同步到提供商挂载模型，价格按“模型基础价 × 渠道倍率”派生。

复用规则：后续调整模型、提供商、渠道绑定相关页面时，必须先区分“模型共享属性”和“提供商路由属性”，禁止为了收口一个字段而连带迁移其它层级的配置字段。

## 2026-05-02 18:35:00 +08:00 - 模型目录必须作为提供商可用模型全集

问题：提供商新增或发现模型后，如果模型管理没有对应记录，会出现提供商可用模型无法在模型管理中统一查看和维护；同时若同步逻辑用提供商旧价格反推倍率，可能破坏“模型基础价 × 提供商倍率”的一致口径。

解决方法：提供商保存模型配置时必须先补齐缺失的模型目录记录，再按模型目录同步共享能力和派生价格；提供商移除模型只删除挂载关系，不删除模型目录；模型管理删除模型时再级联删除所有提供商挂载。

复用规则：后续涉及模型目录、上游模型发现、渠道绑定或路由候选同步时，必须保持模型管理是全集与共享配置源，提供商只保存挂载和路由参数。

## 2026-05-02 21:14:04 +08:00 - 上游模型可能只支持 OpenAI 兼容协议中的单一入口

问题：同一模型在 `/v1/chat/completions` 调用成功，但 `/v1/responses` 返回 `Unsupported model`、`InvalidParameter` 等兼容性错误时，若代理层直接把首次失败返回给客户端，会误判模型不可用。

解决方法：对外代理在上游返回端点或模型兼容性错误时，先将 `responses` 请求转换为 `chat/completions` 请求重试，或将 `chat/completions` 请求转换为 `responses` 请求重试；重试成功后再把上游响应转换回客户端原请求入口的响应结构。

复用规则：后续新增 OpenAI 兼容端点、健康检查或接入自检时，不能只用单一入口失败判断模型不可用；必须区分“模型完全不可用”和“该上游只支持另一种兼容入口”。

## 2026-05-02 21:43:00 +08:00 - OpenAI 兼容端点互转必须先做安全判定

问题：`chat/completions` 与 `responses` 可以在简单文本、普通图片和基础流式场景互转，但工具调用、状态上下文、reasoning、结构化输出和复杂多模态对象存在语义差异；若继续强行互转，可能丢字段、错改响应结构或让客户端收到看似成功但实际有损的结果。

解决方法：端点 fallback 前必须先做请求安全判定，仅允许简单文本、普通图片和基础流式进入互转；请求或响应一旦包含高风险能力，必须返回 `endpoint_fallback_conversion_unsafe` 或 `endpoint_response_conversion_unsafe` 等明确错误，并给出不转换的字段和原因。

复用规则：后续新增 OpenAI 兼容协议适配、健康检查或接入自检时，必须同时展示原生支持与适配支持，trace 中保留 `endpoint_fallback`，禁止把适配成功伪装成原生成功。

## 2026-05-02 22:05:00 +08:00 - 外部 `/v1/*` 请求不能只记录错误而空回

问题：外部调用方只关心 HTTP/SSE 实际收到的内容；如果服务端只把上游失败、内部异常或流式中断写入本地日志，却让客户端收到空响应、断流或泛化 500，调用方无法判断是鉴权、额度、路由、上游兼容、超时还是内部错误。

解决方法：`/v1/*` JSON 请求必须统一返回 OpenAI 风格错误体，包含 `message`、`type`、`code`、`trace_id`、`retryable` 和必要 `detail`；SSE 连接已建立后的异常必须尽可能发送 `event: error`，再发送 `data: [DONE]` 收尾。

复用规则：后续新增外部正式端点或流式能力时，必须先设计客户端可见错误返回，再做本地日志记录；禁止只满足后台可观测而忽略外部调用方的错误可诊断性。

## 2026-05-02 22:35:00 +08:00 - 长上下文代理必须同时治理入口体积、能力声明和日志成本

问题：外部写代码插件或长上下文客户端会提交大体积消息、工具定义、状态上下文和长流式输出；如果代理只做 OpenAI 兼容转发而缺少请求体大小限制、模型上下文窗口标记、工具能力验收和日志/Token 统计成本控制，可能出现上游上下文超限、客户端能力误判、进程内存占用升高、Token 补全任务堆积或日志存储增长过快。

解决方法：正式支持长上下文前，必须建立端到端治理：入口层限制 `Content-Length` 与 JSON 解析体积；模型目录维护上下文窗口、工具调用、Responses 状态、reasoning 等能力字段；路由前按模型窗口和输出预留做预校验；日志只持久化受限摘要；Token 统计任务不得携带完整大 payload 或无限累积流式输出。

复用规则：后续评估 Codex、Claude Code、Cursor、Continue 等写代码客户端接入时，必须按“客户端协议能力需求 → 本项目 `/v1/*` 实际支持能力 → 上游模型真实能力 → 日志与计费成本”逐项验收，禁止只因简单文本请求可用就宣称完整适配长上下文编程代理。

## 2026-05-02 23:24:49 +08:00 - 无 `Content-Length` 的大请求不能依赖框架自动解析

问题：如果路由参数直接声明为 `payload: dict`，FastAPI 会在进入业务逻辑前解析完整 JSON；当反向代理未限制或请求缺少 `Content-Length` 时，超大上下文会先进入 Python 内存，应用层限制无法真正前置生效。

解决方法：对外 `/v1/*` POST 入口必须在路由内通过请求流限量读取 body，超过 `max_v1_request_body_bytes` 立即返回 `request_body_too_large`；读取成功后再手动 JSON 解析，并拒绝空 body、非法 JSON 和非对象顶层结构。

复用规则：后续新增正式外部 POST 端点时，禁止直接依赖框架自动把请求体绑定为 dict；必须先确认该端点是否需要应用层体积预检，尤其是可接收长文本、文件内容、上下文数组或工具定义的入口。

## 2026-05-02 23:24:49 +08:00 - 云服务器例行更新必须隔离数据库变更

问题：代码更新、依赖更新、服务重启和静态资源发布通常是高频操作；如果这些流程顺带执行数据库初始化、迁移、清空或重建，云服务器上的生产数据会面临非预期结构变更或数据丢失风险。

解决方法：例行云服务器更新只处理代码、依赖、配置和服务；数据库变更必须由用户明确要求后作为独立步骤执行，并在执行前完成备份、迁移范围确认和回滚方案确认。

复用规则：后续任何部署脚本、远程更新步骤或运维说明中，只要涉及云服务器生产环境，必须默认“不碰数据库”；出现 `RUN_DB_INIT=1`、迁移命令、清表、重建库等动作时必须单独提示并等待确认。

## 2026-05-03 00:20:00 +08:00 - 日志 metadata 不能绕过正文日志保护

问题：即使关闭完整 payload 日志，客户端仍可能把长上下文、文件摘要、会话状态或敏感内容放进 `metadata`。如果日志摘要直接保留完整 `metadata`，会造成数据库膨胀、敏感内容泄露和后台列表查询变慢。

解决方法：请求日志摘要必须对 `metadata` 单独设置字节上限；超过上限时只保留结构化摘要，例如字段数量、键名列表、原始字节数和数据类型，不保留完整内容。

复用规则：后续新增日志摘要字段时，必须判断该字段是否可能承载用户正文、文件内容或长上下文；若可能，必须使用独立上限或摘要化策略，禁止借“元数据”名义绕过 payload 日志开关。

## 2026-05-03 00:20:00 +08:00 - 非流式上游响应也需要应用层字节上限

问题：非流式请求若直接调用 `response.json()`，代理端会先把上游响应完整读入内存；长代码生成、日志分析或异常上游返回大 JSON 时，即使入口请求受控，也可能在响应阶段产生内存压力。

解决方法：非流式上游响应必须使用限量读取，累计字节超过系统设置的最大非流式响应体字节数时立即停止读取并返回明确错误；长输出场景应优先要求客户端使用流式请求。

复用规则：后续新增非流式代理链路、图片 JSON 链路或第三方上游调用时，禁止无上限读取响应体后再解析；必须先定义响应体字节上限和超限错误行为。

## 2026-05-03 09:20:00 +08:00 - 左侧侧栏不能把主导航与底部工具区混在同一滚动流里

问题：桌面端左侧侧栏既包含主导航列表，也包含暗黑模式、首页、文档、账号菜单等底部工具区。如果直接把整个侧栏作为单一流式容器滚动，后续一旦放大导航字号、增加菜单项或缩窄侧栏宽度，底部工具区就可能被挤进主导航区域，出现文字压盖、按钮重叠和账号区错位。

解决方法：侧栏结构必须拆成两段：主导航区域单独承担纵向滚动，底部工具区固定在侧栏底部并独立占位；不要再通过继续压缩字号或减少间距去硬塞同一容器。桌面侧栏若存在弹出式账号菜单，还应优先在侧栏内部或向上展开，避免溢出到屏幕外侧。

复用规则：后续新增后台或用户端左侧菜单、增加导航项、调整字号或改侧栏宽度时，必须先确认主导航区和底部工具区是否已经物理分区；未分区前禁止继续堆功能或放大导航项。

## 2026-05-03 12:05:00 +08:00 - 金额与单价不能混用浮点或低精度 Numeric

问题：提供商价格界面按“每百万 token”录入，但底层字段按 `*_per_1k` 存储；当价格很小（例如每百万价格小于 0.001 美元）时，如果数据库字段仍使用 `FLOAT` 或 `NUMERIC(18,6)`，换算到每千 token 后会被直接截成 0，导致日志金额、账单、额度校验和 Redis 成本配额累计全部失真。

解决方法：金额统一使用高精度 `Decimal` 和 `NUMERIC(24,9)`，单价与倍率统一使用 `NUMERIC(24,12)`；计费时只在最终金额落库前按金额精度量化，单价和倍率全程保留更高精度；Redis 成本配额计数禁止继续使用 `incrbyfloat`，改为按金额最小精度缩放后的整数累计。

复用规则：后续凡是涉及价格、倍率、余额、额度、账单、日志成本或成本统计汇总的链路，必须先确认“存储精度、计算精度、展示精度、缓存计数精度”四个环节一致；禁止只修前端显示或只修数据库字段其中一端。

## 2026-05-03 12:28:00 +08:00 - 内部代理方法新增必填参数后必须同步检查所有调用方

问题：`ProxyService._forward_json_with_endpoint_fallback()` 新增必填 `setting` 参数后，正式代理主链路已同步更新，但健康检查 `HealthService._probe_formal_endpoint()` 仍沿用旧调用方式，导致健康检查在真正请求上游前就抛出参数缺失异常，表现为 `chat/completions` 和 `responses` 同时失败。

解决方法：给新增必填参数的方法补齐全部调用方，并优先在跨模块共用入口处做一次全文检索；健康检查链路调用正式代理辅助方法时，必须补传与正式代理一致的运行时 `setting`。

复用规则：后续修改 `ProxyService`、`RouterService`、鉴权、计费或健康检查这类跨模块共用服务的方法签名时，必须在提交前全局检索所有调用点，并至少做一次针对“非主链路调用方”的回归检查。

## 2026-05-03 15:40:00 +08:00 - 本地后台页面异常时必须先确认实际命中的服务进程

问题：本地 `8000` 端口可能仍被更早启动的旧 `uvicorn` 进程占用。此时即使工作区代码已经修复，浏览器点击“检查全部”或进入调用测试页，实际命中的仍是旧进程，页面会持续显示已经在源码中修过的历史错误，造成“代码已改但现象不变”的错觉。

解决方法：遇到“页面现象与当前源码不一致”时，必须先检查目标端口的监听进程、Python 解释器路径和启动时间，确认是否仍在命中旧服务；若是旧进程，先停止旧进程，再用当前项目虚拟环境重新启动服务后复测。

复用规则：后续凡是本地页面、接口或健康检查结果与当前代码明显不一致时，必须把“端口占用进程核对”作为第一批排查动作，禁止仅根据浏览器现象继续修改代码。

## 2026-05-03 15:40:00 +08:00 - 非生产环境下 Redis 不可用时应降级而不是阻断本地联调

问题：`playground`、`/v1/*` 自检链路和路由筛选会依赖 Redis 并发租约与渠道容量快照；当本地 Redis 未启动时，如果这些能力直接抛出 `redis_unavailable`，会导致调用测试页、自检页和部分正式入口全部返回 503，即使上游模型本身可用也无法完成联调。

解决方法：在非生产环境下，Redis 不可用时并发租约与渠道容量控制必须降级为本地放行模式：并发租约返回空 lease，容量快照返回零值并跳过 provider lease；同时保留 `/ready`、监控和告警对 Redis 异常的显式暴露，避免把降级误写成 Redis 正常。

复用规则：后续新增仅用于限流、并发控制、容量快照或短期缓存的 Redis 依赖时，必须同时判断该依赖在非生产环境是否允许降级；若允许降级，页面联调链路不能被 Redis 缺失直接阻断。

## 2026-05-03 16:25:00 +08:00 - 本地 Redis 降级不能只覆盖并发与容量，还必须覆盖限流链路

问题：并发租约和 provider 容量在非生产环境已支持 Redis 缺失时降级，但 API Key 限流校验仍直接依赖 Redis。结果是本地 `APP_ENV=dev` 且 Redis 未启动时，调用测试页虽然能完成路由和上游请求准备，最终仍会在 `validate_redis_rate_limits()` 阶段返回 `503 redis_unavailable`。

解决方法：非生产环境下，Redis 不可用时限流服务也必须降级为 no-op，包括实时额度种子写入、QPS/RPM/TPM 校验、日配额校验和用量累计写入；生产环境仍保持严格依赖 Redis，不允许静默放过。

复用规则：后续凡是新增 Redis 限流、额度、计数或短窗口统计能力时，必须同时检查“本地联调是否允许降级”和“生产是否保持硬依赖”两条路径，禁止只修并发控制却遗漏限流鉴权链路。

## 2026-05-03 17:15:00 +08:00 - 批量健康检查禁止在 provider 汇总后再把同一批模型重复探测一遍

问题：`check_all` 若先调用“按 provider 汇总检查”，而该汇总函数内部已经逐个模型完成正式探针，再在外层循环中重新遍历同一批模型执行 `check_provider_model()`，会把每个模型的 `chat/completions`、`responses` 和可选 tools 探针全部重复一次，导致“检查全部模型”耗时近乎翻倍，并额外放大上游请求量和健康检查日志写入量。

解决方法：批量健康检查需要同时返回 provider 级与模型级结果时，必须复用 provider 汇总阶段已经得到的 `model_results`，禁止再次对同一批启用模型发起第二轮正式探针。

复用规则：后续新增“汇总结果 + 明细结果”这类批量检查接口时，必须先确认汇总阶段是否已经执行过明细探针；若已执行，明细输出只能复用已有结果，不得重复打上游。

## 2026-05-03 18:25:00 +08:00 - 手动健康检查不能把所有模型和所有探针完全串行执行

问题：当单个提供商挂载较多模型时，若 `chat/completions`、`responses`、`tools` 等探针都按“模型一条一条串行”执行，后台“测试提供商 / 检查全部”会明显变慢；同时如果允许高频重复触发，又会在短时间内把同一批探针重复压到上游，放大提供商并发压力。

解决方法：手动健康检查在单个提供商内必须改为“同类探针并行、异类探针串行”：同一阶段内按模型并行探测，阶段完成后立即向前端回显进度；单个探针首次失败仅允许立即重试一次；同一类手动健康检查必须设置至少 300 秒冷却时间，避免短时间重复打上游。

复用规则：后续新增任何“按模型批量探测上游能力”的后台功能时，都必须同时检查三点：是否按阶段并行而不是全量串行、是否把阶段进度实时回显给前端、是否设置最小触发间隔和有限重试边界。

## 2026-05-03 19:05:00 +08:00 - 表格容器内的帮助 Tooltip 不能继续依赖局部 absolute 定位

问题：日志表格外层若使用 `overflow: auto` 或类似滚动壳，问号帮助提示如果仍作为单元格内部的 `position: absolute` 浮层渲染，即使提高 `z-index`，也会继续被父级裁切，看起来像“提示框被卡片挡住”或“提示框只显示一半”。

解决方法：这类 Tooltip 必须改为单例浮层并挂载到 `document.body` 或等效的顶层浮层容器，由触发按钮在显示时动态计算位置；同时浮层背景必须使用不透明表层色，避免下层表格文字透出影响可读性。

复用规则：后续只要在表格、日志列表、滚动面板或任何 `overflow` 容器中新增帮助提示、下拉层或 Popover，都必须先检查是否会被父级裁切；若存在裁切风险，必须直接走顶层浮层方案，不得仅靠增大 `z-index` 试图修补。

## 2026-05-03 19:45:00 +08:00 - 简单按钮操作若只更新数据而不回显结果，用户会误判为没有生效

问题：刷新、导出、清空、选择本页、新增空行等简单按钮，若点击后既不打开结果界面，也不在按钮附近给出短暂成功/失败反馈，用户很容易把“按钮已触发但结果较轻”误判为“按钮没反应”。

解决方法：为这类简单按钮统一提供原位短反馈能力：成功时在原按钮位置短暂显示如“已刷新”“已清空”“准备导出”，失败时短暂显示如“刷新失败”，同时按需保留 Toast 作为补充。

复用规则：后续新增任何不会打开独立结果页的简单按钮操作时，都必须优先检查是否已有原位短反馈；若没有，必须补充统一的原位成功/失败提示，禁止只依赖用户自行观察页面细微变化判断结果。

## 2026-05-03 20:20:00 +08:00 - 回归脚本若仍依赖旧初始化流程，会把无关改动误判为回归失败

问题：`stage8_log_regression_check.py`、`stage9_regression_check.py` 仍假设可以通过 `/setup-admin` 网页初始化管理员；但当前项目已经改为“管理员只能通过服务器侧受控方式创建”。结果是脚本会在最前面的管理员初始化步骤失败，即使本次改动只涉及前端日志页或测试按钮，也会被脚本误报为整体回归失败。

解决方法：涉及登录态、后台页面或测试接口的临时回归校验，必须改用“直接创建临时管理员 / 用户 + TestClient 登录”的方式，绕开已废弃的网页初始化管理员链路；旧脚本若继续保留，也必须同步更新其建号前置步骤。

复用规则：后续凡是编写或维护集成回归脚本时，只要依赖账号初始化、登录或权限前置，就必须先检查脚本假设是否仍与当前正式鉴权流程一致；若正式流程已变更，脚本必须同步调整，禁止让过期脚本长期阻断无关改动验收。

## 2026-05-03 21:10:00 +08:00 - 健康检查结果与真实调用失败原因必须区分“降级”与“不可用”

问题：模型健康状态回写如果把所有失败统一按“第一次失败 -> degraded，达到阈值才 unhealthy”处理，会把“模型不存在、订阅不存在、权限被拒、端点本身不支持”这类明确不可用的失败压成普通降级，导致提供商模型矩阵、日志状态和真实外部调用结果不一致；前端即使刷新，也看不到明确异常原因。

解决方法：健康检查结果中已判定为 `unhealthy` 的模型必须按 `unhealthy` 原样落库；正式代理链路在上游返回模型/订阅/权限/不支持类错误时，也必须直接把对应模型回写为 `unhealthy` 并保留 `last_error`，不能继续等失败阈值累计后才变异常。

复用规则：后续凡是新增健康状态写回逻辑、失败计数逻辑或上游错误分类逻辑时，都必须先判断该失败是“短暂异常”还是“模型明确不可用”；明确不可用的失败禁止再套用统一的降级阈值逻辑。

## 2026-05-03 21:10:00 +08:00 - OpenAI 兼容端点互转时不能丢失 usage 里的缓存读写明细

问题：当 `/v1/chat/completions` 与 `/v1/responses` 之间发生成功适配时，若转换后的响应只保留总输入/输出/总 Token，而没有把 `cached_tokens`、`cache_creation_tokens` 等缓存明细同步映射到目标 usage 结构，日志中的 `cache_read_tokens`、`cache_write_tokens` 就会在适配成功链路上变成空值，造成缓存字段统计不完整。

解决方法：在 chat -> responses、responses -> chat 的响应适配中，必须同步保留缓存读写明细，并映射到目标端点兼容的 usage 字段结构（如 `input_tokens_details` 或 `prompt_tokens_details`）；后续日志提取与补全统一继续复用这些字段。

复用规则：后续凡是新增 OpenAI 兼容响应适配、日志补全或 usage 归一化逻辑时，都必须同时检查“总 Token”“缓存读”“缓存写”“reasoning”等 usage 子字段是否完整透传，禁止只保留总量字段。

## 2026-05-03 23:40:00 +08:00 - `provider_name=null + trace=[] + attempt_count=0` 的 404 说明失败发生在本地候选路由阶段

问题：对外 `/v1/chat/completions` 或 `/v1/responses` 日志若出现 `provider_name = null`、`trace = []`、`attempt_count = 0` 且状态码为 `404`，很容易被误判成“上游 404”或“接口路径不存在”；实际上这类日志通常表示请求在本地路由阶段就没有找到任何可用候选，提供商选择逻辑根本还没进入上游调用。

解决方法：排查时必须优先检查本地候选条件是否被全部筛空，包括模型名是否存在、模型是否启用、对应 provider/model 是否声明支持当前端点、API Key 是否绑定该 provider、模型白名单是否放行、provider 或 model 是否被禁用/熔断/维护中、容量过滤是否把候选全部排除。只有当 `trace` 中已经出现具体 provider 尝试记录时，才进入上游 404 或端点 fallback 排查。

复用规则：后续只要日志里再次出现这组特征组合，就必须先按“本地无候选”路径排查，不得直接把问题归因到上游服务或 HTTP 路径拼写错误。

## 2026-05-04 00:15:00 +08:00 - OpenAI 兼容模型能力自动推断若漏掉模型家族，会把工具调用错误拦死在本地路由阶段

问题：当前项目会在提供商导入模型、`models` 自动发现和历史数据修复时，基于模型名自动推断 `supports_tools`。如果某个实际支持工具调用的模型家族没有被纳入推断名单，例如 `glm-*`，那么模型会被错误标记为“不支持工具调用”，随后 `/v1/chat/completions` 或 `/v1/responses` 的工具请求会在本地候选过滤阶段直接返回 `model_tools_not_available`，连上游都不会打到。

解决方法：维护统一的工具能力模型名提示列表，并把已确认支持工具调用的模型家族补入其中；至少要用独立回归校验覆盖“自动推断 -> 路由过滤 -> 实际工具请求”整条链路，避免只修了推断函数却漏掉真实请求入口。

复用规则：后续每次新增 OpenAI 兼容模型家族接入时，只要依赖模型名自动推断能力，就必须同步检查该家族是否已纳入 `supports_tools`、`supports_vision` 等提示列表，并补一条端到端回归，防止再次出现“模型本身支持 tools，但本地路由先拦截”的假性不支持。

## 2026-05-04 00:40:00 +08:00 - `/v1/responses` 图片请求不能在进入上游前统一压成 chat 适配子集

问题：`/v1/responses` 本来就是更适合承载图片、状态上下文、reasoning 与结构化输出等原生能力的入口。如果在本地代理层看到图片请求就先按“能否安全转成 `/v1/chat/completions`”做强校验，再直接改写为 chat 上游请求，那么 `previous_response_id`、`reasoning`、`text`、`include`、`store`、`truncation`、`background` 等原生 Responses 字段会在真正打到上游前就被本地错误拦截或提前收窄，导致代理兼容性比真实上游更差。

解决方法：`/v1/responses` 图片请求和携带原生 Responses 字段的请求，必须先按原始 `responses` 协议透传上游；只有当上游明确返回端点或模型兼容性错误时，才允许在满足安全条件的前提下回退到 `/v1/chat/completions`。端点 fallback 必须是“先尝试原生，再按上游错误决定是否降级”，禁止作为本地预处理默认路径。

复用规则：后续只要新增 OpenAI 兼容端点适配、图片链路支持或 agent 调用兼容层，就必须先检查是否错误地把 richer endpoint 预先压缩成 simpler endpoint；凡是 `responses` 原生字段还可能被上游接受的请求，都必须先原样透传，不得在进入上游前先按 chat 子集做本地强校验。

## 2026-05-04 00:55:00 +08:00 - 同名模型的能力位已按模型主视角共享，回归脚本不能再在不同 provider 上做矛盾声明

问题：当前项目会把 `supports_stream`、`supports_tools`、`supports_vision`、`supports_chat_completions`、`supports_responses` 等能力位收敛到模型管理主视角，再同步回各 provider 挂载模型。若回归脚本或导入逻辑仍尝试用同一个模型名在不同 provider 上声明互相矛盾的能力，例如第一个 provider 把 `stream-model` 标成 `supports_stream=false`、第二个又标成 `true`，后创建或同步的记录会被统一能力覆盖，导致测试以为“路由过滤坏了”，实际上是脚本前提已不成立。

解决方法：凡是需要验证不同能力位分支的回归或初始化数据，必须使用不同模型名分别构造，或者先显式修改模型管理中的共享能力配置，再让 provider 挂载同步；不得再默认同名模型可以在不同 provider 上长期持有不同能力位。

复用规则：后续只要维护回归脚本、测试夹具、批量导入逻辑或模型同步逻辑，并且涉及同名模型跨 provider 挂载，就必须先判断该字段是模型共享能力还是 provider 路由配置；共享能力禁止在多个 provider 上做相互冲突的本地声明。

## 2026-05-04 00:00:00 +08:00 - 1000 并发长连接流式代理不能只看连接池参数，必须审查同步数据库与进程内后台任务

问题：对外 `/v1/*` 流式代理即使配置了较大的上游连接池和 Redis 并发租约，仍可能因流结束阶段同步写数据库、每次鉴权仍访问数据库、APScheduler 按 worker 进程内调度 Token/计费任务等环节成为瓶颈，导致 1000 并发长连接下响应变慢、线程池或数据库连接池耗尽、后台任务丢失或重复执行。

解决方法：高并发流式链路必须把鉴权热路径、路由候选、日志写入、Token/计费结算和 provider 状态更新拆成可压测的独立环节；流式生成器 `finally` 中禁止执行阻塞式数据库操作，日志和计费应进入可靠队列或专用后台 worker；API Key 缓存命中路径必须避免每次仍查库；多 worker 生产环境的后台任务必须使用共享队列或单独 worker，而不是依赖每个 Web worker 内置调度器。

复用规则：后续凡是声明支持 1000 并发、长连接 SSE、快速响应或生产级吞吐，必须同时提供连接池、Redis 租约、数据库连接池、后台任务队列、日志写入延迟和压测结果的闭环证据；禁止只根据配置项数值判断并发能力已经达标。

## 2026-05-04 00:00:00 +08:00 - 回归脚本必须跟随“管理员禁止网页初始化”规范更新

问题：项目规范已明确管理员账号禁止通过网页注册或初始化，首个管理员必须通过服务器侧脚本创建；若旧回归脚本仍向 `/setup-admin` 提交表单创建管理员，就会在现行实现中稳定返回 403，导致回归失败被误判为登录或页面功能异常。

解决方法：回归脚本需要改为直接调用 `UserAuthService.create_user(..., role=USER_ROLE_ADMIN, ...)` 或复用 `scripts/create_admin_user.py` 的受控创建逻辑来准备管理员测试账号；禁止继续把 `/setup-admin` 当作可创建管理员的测试入口。

复用规则：后续凡是安全规范改变了账号、密钥、权限初始化方式，必须同步更新测试夹具和回归脚本；测试准备步骤必须遵守现行安全规范，不能沿用已废弃的网页初始化路径。

## 2026-05-04 00:00:00 +08:00 - 本地回归测试必须隔离内置调度器和 Redis 依赖

问题：本地执行回归脚本时，应用 lifespan 会启动 APScheduler；若本地 Redis 未启动，`token_usage_backfill` 等定时任务会持续尝试 Redis 分布式锁并输出连接失败，甚至在测试结束时出现取消异常。这会污染测试输出，也可能掩盖真正的断言失败。

解决方法：回归测试应显式关闭或隔离后台 scheduler，或者为测试提供独立 Redis；若测试目标不包含后台任务，必须避免 lifespan 自动调度任务干扰主链路断言。

复用规则：后续新增回归脚本时，必须明确是否需要 scheduler 和 Redis；不需要时应在测试入口设置隔离开关或 mock 调度器，避免测试结果依赖本机 Redis 是否启动。

## 2026-05-04 20:30:00 +08:00 - 阿里云更新脚本不能只依赖 GitHub 直连

问题：阿里云服务器执行 `update_aliyun.sh` 时如果只运行 `git fetch origin main`，一旦服务器到 GitHub 的网络不稳定，整个代码更新、依赖刷新和服务重启流程都会阻塞。

解决方法：更新脚本必须先尝试原 remote，失败后按镜像 URL 列表重试，并允许通过 `GIT_MIRROR_URLS`、`GIT_FETCH_RETRIES`、`GIT_FETCH_TIMEOUT_SECONDS` 覆盖镜像和重试参数；镜像拉取仍应写入同一个 `refs/remotes/<remote>/<branch>`，后续 reset 逻辑不需要分叉。

复用规则：后续维护云服务器更新脚本时，凡是依赖 GitHub、PyPI、系统包仓库等境外网络资源，都必须保留国内镜像或可配置备用源，禁止把单一外部地址作为不可替换依赖。

## 2026-05-05 21:45:00 +08:00 - 对外 `/v1` 未知路径和浏览器预检必须有可观测返回

问题：第三方 GUI 或编程 Agent 可能只探测 `/v1`、调用尚未支持的 `/v1/messages`，或先发送 `OPTIONS /v1/chat/completions` 浏览器预检。如果这些请求只走框架默认 404/405，就会返回不可读的普通错误，并且绕过统一 `request_logs`，导致用户看到“客户端无法访问且后台没有日志”。

解决方法：对外 `/v1` 和未知 `/v1/*` 必须提供兜底返回，认证通过后写入 `request_logs` 并返回带 `trace_id` 的 `unsupported_endpoint`；`OPTIONS /v1/*` 必须写入轻量预检日志并返回 CORS 预检头，避免浏览器型客户端在正式请求前被 405 拦截且无业务侧记录。所有到达应用层的 `/v1/*` 请求日志必须至少记录成败、错误码、失败原因和请求体结构摘要，禁止默认记录完整正文内容。

复用规则：后续新增或调整 OpenAI 兼容端点时，必须同时检查基础路径、未知路径、方法不匹配和 OPTIONS 预检的返回与可观测性，禁止只验证成功的 `POST /v1/chat/completions`。

## 2026-05-06 20:20:00 +08:00 - 第三方 Agent 报模型不正确时必须先对照 `/v1/models` 与云端挂载

问题：Codex、Cursor、ChatGPT 客户端或其他第三方 Agent 接入 `/v1/responses` 时，若请求体中的 `model` 没有在当前云端数据库中同时满足“模型管理启用、provider 已挂载启用、API Key 绑定 provider、端点能力匹配、工具能力匹配”等条件，请求会在本地候选路由阶段返回 404。此时日志可能显示 `log_type=responses`、`provider_name=null`、`trace=[]`、`attempt_count=0`，容易被误判为公网地址、密钥或上游接口不可用。

解决方法：排查时必须先用同一个 Bearer Key 请求 `GET /v1/models`，确认客户端实际请求的模型名是否出现在云端可用模型列表中；再用同一模型分别最小测试 `POST /v1/responses` 和 `POST /v1/chat/completions`。若本地可用而云端不可用，优先检查云端数据库中的模型管理、provider 模型挂载、API Key 授权、健康状态和共享缓存，而不是继续改代码或重跑普通部署脚本。

复用规则：后续第三方 Agent 接入失败时，必须把“客户端请求模型名”和“云端 `/v1/models` 返回模型列表”作为第一组证据；只有模型存在且能力匹配后仍失败，才进入公网、nginx、上游兼容性或客户端配置排查。

## 2026-05-06 23:05:00 +08:00 - Codex Responses 工具 schema 中的 `type` 不一定是字符串

问题：Codex Desktop 调用 `/v1/responses` 时会携带完整工具定义，工具参数通常是 JSON Schema。Schema 内部会出现 `properties.type = {"type": "string"}` 这类字段，此时某个对象的 `type` 值本身是 dict。若本地代理在 Token 估算、图片识别、工具上下文识别或日志摘要中直接执行 `value.get("type") in {...}`，Python 会因 dict 不可哈希抛出 `TypeError: unhashable type: 'dict'`，导致请求在进入上游前变成 500。

解决方法：所有基于 `type` 字段判断消息部件、图片部件或工具调用类型的逻辑，必须先确认 `type` 值是字符串；若不是字符串，应按普通 JSON 对象继续递归处理或计入 JSON 文本，不得把它直接拿去做集合成员判断。

复用规则：后续维护 OpenAI Responses、Chat Completions、工具调用、结构化输出或多模态兼容逻辑时，凡是读取请求对象中的 `type` 字段，都必须把它当作外部输入校验类型；不能假设它永远是协议 item type 字符串。

## 2026-05-07 10:35:00 +08:00 - 流式健康检查不能复用请求级 SQLAlchemy Session 与 ORM 实例

问题：后台“测试提供商（流式）”和“检查全部（流式）”如果把 `Depends(get_db)` 生成的请求级 `db` 会话，以及该会话里查出来的 `provider` ORM 实例，直接传入 `StreamingResponse` 背后的异步 worker，再在检查收尾阶段执行 `db.refresh(provider)` 或继续写库，可能出现 `Instance '<Provider ...>' is not persistent within this Session`。前端会先看到某些阶段结果，但最终整体测试被异常打断，状态回写也可能不完整。

解决方法：所有流式健康检查 worker 必须在 worker 内部自行创建并关闭独立 `SessionLocal()`，再重新查询当前 provider / provider 列表执行检查；收尾汇总阶段禁止依赖可能已脱离当前会话的旧 ORM 实例做 `refresh`，应优先按主键重新获取持久化对象。

复用规则：后续凡是把数据库查询结果带入 `StreamingResponse`、后台异步任务或长生命周期 SSE worker，都必须先确认会话边界；请求级 `Session` 和其 ORM 实例禁止跨流式任务生命周期直接复用。

## 2026-05-07 10:40:00 +08:00 - 工具调用健康检查必须按模型原生端点能力选择探针

问题：若模型配置为支持工具调用，但只原生支持 `/v1/responses`、不支持 `/v1/chat/completions`，而健康检查仍固定用 `chat/completions` 工具探针测试，就会把实际可用的模型误判为“不支持 tools”。这会导致后台能力展示、健康状态和真实对外路由结果不一致。

解决方法：工具调用健康检查必须依据模型能力位选择原生端点：优先走模型已声明支持的原生工具调用端点；至少要区分 `chat/completions` 与 `responses`，禁止固定把所有工具探针都压到单一端点。

复用规则：后续凡是新增或维护模型能力探测、兼容性展示或工具调用验收逻辑时，都必须让探针路径与模型声明的原生端点能力一致，不能用单一路径代替全部能力判断。

## 2026-05-07 11:05:00 +08:00 - 本地候选路由阶段筛空时必须返回结构化诊断信息

问题：当 `/v1/chat/completions` 或 `/v1/responses` 请求在本地候选路由阶段被全部筛空时，日志常见形态是 `provider_name=null`、`trace=[]`、`attempt_count=0`、`status_code=404`。如果此时只返回通用的 `model_not_available` 或 `model_tools_not_available`，排查人员仍然不知道到底是提供商未授权、模型被禁用、端点能力不匹配、健康状态异常还是容量限制导致。

解决方法：无候选 404 必须同时产出结构化路由诊断，至少包含当前请求要求、候选前数量、最终候选数量、原因计数摘要和少量样本对象；该诊断既要写入请求日志 trace，也要放进对外错误 detail，方便云端日志、前端详情和外部调用方统一定位。

复用规则：后续凡是新增候选过滤条件、容量限制条件或权限限制条件时，都必须同步纳入无候选诊断输出；禁止只改筛选逻辑而不更新排查信息。

## 2026-05-07 11:40:00 +08:00 - 对外请求失败时必须把上游/本地失败原因完整落到标准错误对象

问题：对外 `/v1/*` 链路在上游超时、连接失败、流式中断或多提供商全部失败时，如果只抛出笼统的 502/500，而没有把最后一次失败的状态码、错误码、错误消息和尝试次数写入错误 detail，外部调用方会拿到“请求失败但不知道为什么”的结果。

解决方法：失败退出点必须统一返回结构化错误对象，至少包含 `message`、`code`、`last_error` 或等价字段、`last_status_code`、`attempt_count`，流式场景还要继续输出 `event: error` 并以 `[DONE]` 收尾；上游异常应按异常类型归一成明确的错误码，不允许只剩空白或泛化信息。

复用规则：后续凡是新增代理、重试、流式转发或健康探测失败处理逻辑时，都必须先检查最终返回是否仍然带有可读原因；如果错误信息不足，先补齐结构化 detail，再考虑调整重试策略。

## 2026-05-08 20:05:00 +08:00 - 图片自检与验活结果不能直接回显原始 base64

问题：当用户自检、playground 或健康检查覆盖 `image_generation` 场景时，上游响应里可能直接携带图片 base64。若页面结果区、下载报告、日志预览或 SSE 事件预览直接回显这些字段，就会出现大块二进制文本撑爆页面、报告失真和日志膨胀，同时也会把“已返回响应”误当成“已成功生成图片”。

解决方法：图片验活链路必须单独校验“是否真的提取到生成图片结果”，并且在页面、报告和日志预览里只保留结构化摘要，例如 `generated_images_count`、`mime_types`、`[生成了 N 张图片]` 或截断标记；原始图片内容只允许在专门的图片展示区域或文件存储链路中承载，禁止直接落到代码块、日志文本或普通 JSON 预览里。

复用规则：后续凡是新增 `image_generation`、图片编辑、partial image 或其他二进制结果场景，都必须先补“成功判定”和“安全预览”两层适配，再接入自检、日志、下载报告或调试页面。

## 2026-05-08 20:45:00 +08:00 - 历史模型能力位漂移会把 image_generation 请求误挡在本地路由层

问题：某些已接入的 `gpt-5.*` 模型虽然实际上支持 `/v1/responses + image_generation`，但历史库里的 `supports_tools` 可能仍为旧值 `False`。此时文本请求能正常命中上游，图片生成请求却会在本地候选筛选阶段被误判为“不支持 tools / image_generation”，根本不会发到上游，看起来像“提供商不支持生图”，实则是本地能力元数据漂移。

解决方法：路由筛选、能力展示和图片生成支持判断不能只盯数据库里的裸能力位；必须采用“显式配置优先，模型名推断兜底”的有效能力判断。对 `gpt-5`、`gpt-4.1`、`gpt-4o` 等已知工具模型，若历史能力位缺失或滞后，应允许运行时按模型名推断 `supports_tools`，再据此判断 `image_generation` 可用性。

复用规则：后续凡是新增模型能力过滤、健康检查探针或前端能力展示时，都必须优先使用统一的“有效能力”判断函数，禁止一处按原始字段、一处按推断能力，避免再次出现“文本可用但生图被本地误拦”的分裂状态。

## 2026-05-08 22:10:00 +08:00 - 流式图片生成日志必须保留安全摘要

问题：流式 `image_generation` 请求在关闭完整流式正文持久化时，日志中可能完全没有可解析的响应内容，只剩下请求成功状态。这样会导致日志中心、自检页和排障流程无法判断这次流式请求是否真的生成了图片，也无法区分是文本流、识图流还是生图流。

解决方法：在流式图片生成链路里，即使不保存完整 SSE 正文，也必须把图片数量、格式、近似字节数、partial 标记和截断标记写成安全摘要，同时允许保留类似 `[生成了 N 张图片]` 的占位文本，供日志派生字段和前端摘要使用，禁止把整段 base64 直接落库。

复用规则：后续凡是新增流式图片生成、自检报告或日志详情展示时，都必须同时兼顾“安全留痕”和“不可回显原始二进制”两件事，不能因为关闭正文持久化就让图片生成日志失去可观测性。

## 2026-05-08 22:12:00 +08:00 - 图片生成缺失 usage 时不要从摘要文本反推 Token

问题：纯图片生成响应有时不会返回完整 `usage`，如果 Token 补全逻辑把 `[生成了 N 张图片]` 或其他安全占位文本当成普通文本去计数，就会把图片摘要误当输出正文，导致 `completion_tokens`、`total_tokens` 和后续费用统计失真。

解决方法：图片生成响应缺少上游 `usage` 时，Token 补全必须优先保持未知或留空；只有上游真实返回 usage 时才允许写入 token 统计，禁止从安全摘要文本反推 completion tokens。

复用规则：后续凡是新增图片生成、partial image、流式摘要或日志补全逻辑时，都必须先判断这是不是“真实正文”，不能把日志展示文本当成 token 统计输入。

## 2026-05-08 23:08:00 +08:00 - 对外生图能力若只支持 Responses 口径，文档和错误提示必须同步收口

问题：代理主链路即使已经支持 `Responses + image_generation`，如果外部文档仍只给文本示例，或者 legacy `images/*` 误调时只返回通用 unsupported endpoint，调用方仍然不知道文生图、编辑图应该如何构造请求，也容易误以为平台“不支持生图”或“只支持旧版 Images API”。

解决方法：对外 `使用文档`、用户侧 API Key 接入页和 `/v1/images/*` 误调用错误提示，必须同时明确三件事：正式公网入口是 `POST /v1/responses`；工具要写 `{"type":"image_generation"}`；编辑图要把源图放进 `input` 并把工具动作设为 `edit`。至少要提供一条 generate 示例和一条 edit 示例。

复用规则：后续凡是新增 OpenAI 兼容子集能力时，都不能只补后台实现不补对外接入口径；如果某能力有“正式支持路径”和“历史误用路径”，必须在文档和错误提示里一起收口，避免实现与接入说明脱节。

## 2026-05-08 23:58:00 +08:00 - legacy Images 兼容层不能硬编码固定外层模型，也不能盲传未经验证的工具字段

问题：把旧版 `/v1/images/generations`、`/v1/images/edits` 适配到 `Responses + image_generation` 时，如果直接把外层模型硬编码成单一固定值（例如 `gpt-5.4`），一旦该模型在当前授权渠道上熔断、未挂载或暂时不可用，legacy 兼容请求就会在本地路由层直接失败；同时，若把旧版 Images API 的 `n` 等字段原样塞进 `image_generation` 工具参数，而当前上游并不接受该字段，也会在进入兼容链路后被上游 400 拒绝。

解决方法：legacy Images 兼容层必须优先从“当前 API Key 授权范围 + 当前可用 provider 挂载 + image_generation 能力 + 非熔断状态”中动态选择可用的外层 Responses 模型，不能只硬编码单一模型；对于尚未被当前上游验证支持的 legacy 参数，必须在兼容层提前做白名单筛选或明确拒绝，并返回可读错误说明，禁止把未验证字段盲传到 `image_generation` 工具后再让调用方自己猜为什么失败。

复用规则：后续凡是新增基于兼容适配层的旧协议入口时，都必须同时做两件事：一是把“外层路由模型选择”做成基于当前可用性的动态选择，而不是静态写死；二是把未验证参数在适配层提前裁剪或拒绝，避免把兼容层做成“看似支持、实则随机失败”的半成品。

## 2026-05-09 00:18:00 +08:00 - 旧版 Images API 的多图与 variation 兼容，不能伪装成“上游天然支持”

问题：当前上游图片内核已经稳定跑通 `Responses + image_generation`，但旧版 `/v1/images/*` 的 `n` 多图输出和 `/v1/images/variations` 语义并不是被上游原生一一对应支持。如果继续在对外层面直接宣称“完全等价官方旧 Images API”，调用方会误以为这些行为是上游天然支持，而不是本地兼容层聚合或适配的结果，后续在计费、日志数量、时延和语义细节上都容易产生误解。

解决方法：对于旧版 `n>1` 请求，应在兼容层显式做多次内部生成并聚合回 `data[]`，同时在文档中说明这是平台内部聚合行为；对于 `/v1/images/variations`，若当前底层并无严格同构的原生命令，应通过兼容适配层映射到现有图片编辑内核，并明确标注它属于“旧客户端兼容能力”，不是现有正式主工作流的原生等价接口。

复用规则：后续凡是把旧协议适配到新内核时，只要出现“平台内部 fan-out、聚合、语义近似映射”这类情况，都必须在实现和文档里同时显式标注，不允许把兼容适配伪装成上游天然支持的完全等价能力。

## 2026-05-10 23:59:00 +08:00 - API Key 维度的统计与日志不能混入 `/v1/models` 等发现型请求

问题：当同一个 API Key 既调用了真实模型接口（如 `/v1/chat/completions`、`/v1/responses`），又调用了 `/v1/models` 这类发现型接口时，如果 API Key 详情页的统计、分析和日志列表直接按 `api_client_key_id` 汇总全部 `request_logs`，就会把“模型发现请求”误算成“模型调用请求”。这会让累计请求数、成功数、日志列表和筛选结果看起来像发生了重复请求，进而误判为存在额外 Token 消耗或重复打上游。

解决方法：凡是面向 API Key 维度展示“模型调用统计”“模型调用分析”或“该 Key 的调用日志”时，查询口径必须收敛到真实路由流量日志类型（当前为 `chat`、`responses`）；`/v1/models`、健康检查、后台测试等非模型调用日志不得混入这些视图。若需要看 API Key 的全量访问行为，应由独立的全量日志视图承载，不能与模型调用统计共用同一口径。

复用规则：后续凡是新增 API Key 详情页卡片、按 Key 聚合报表、按 Key 过滤日志、用户端我的 Key 用量页或导出能力时，都必须先确认是否只应统计真实模型调用；如果页面目标是“用量/计费/模型调用分析”，默认只允许纳入 `chat`、`responses` 等真实路由流量日志。

## 2026-05-29 20:45:00 +08:00 - 多 worker 压测下数据库连接池总量不能超过 PostgreSQL 实际连接上限

问题：本项目生产启动口径是多 worker Web 进程，但数据库连接池配置按单进程写成 `DB_POOL_SIZE=30`、`DB_MAX_OVERFLOW=70`。当本地按 4 worker 压测时，理论总连接申请上限会放大到 `(30 + 70) × 4 = 400`，而当前 PostgreSQL `max_connections` 只有 `100`。高并发下会直接出现 `FATAL: remaining connection slots are reserved for roles with the SUPERUSER attribute`，继而触发请求 500、502 和客户端 `ReadError`，压测数据也会被数据库连接耗尽严重污染。

解决方法：数据库连接池必须按“所有 worker 总和”统一规划，而不是只看单 worker。若 PostgreSQL `max_connections=100`，应把 `DB_POOL_SIZE`、`DB_MAX_OVERFLOW` 和 `WEB_CONCURRENCY` 联动收紧，并为迁移脚本、管理端、调度器和排障连接预留余量；压测前必须先核对 `show max_connections`、`pg_stat_activity` 和应用总连接池预算，避免还没测到代理上限就先把数据库打满。

复用规则：后续凡是调整 `WEB_CONCURRENCY`、数据库连接池、压测并发、后台任务并发或部署拓扑时，都必须先做“总连接预算 = 单 worker 连接池上限 × worker 数 + 其他进程余量”的校验；若未完成这一步，压测结果默认不可信，且不能据此判断代理主链路真实吞吐。
## 2026-05-29 压测链路容易被假瓶颈误导

- 问题：本地真实压测若使用 Python `httpx` 压测器或单 worker FastAPI mock 上游，压测结果会先被客户端或 mock 上游限制在几十 RPS，无法反映代理服务自身上限。
- 影响：会误判代理主链路性能，导致把优化方向错误集中在数据库或路由逻辑上。
- 解决方法：高并发压测应使用更高吞吐的压测客户端（本项目新增 `scripts/benchmark_proxy_node.js`），并使用轻量 mock 上游（本项目新增 `scripts/mock_openai_upstream_threaded.py`）先校验 mock 上游裸吞吐高于目标值，再压测代理。

## 2026-05-29 请求成功状态同步更新 provider 行会形成数据库热点

- 问题：每个成功请求同步更新同一 provider/provider_model 的成功计数、延迟和健康状态，会在单渠道高并发压测下形成行锁热点。
- 影响：即使上游很快，主链路也会被同一数据库行串行化，表现为吞吐下降、延迟升高。
- 解决方法：请求级精确数据必须以 `request_logs` 为准；provider 成功状态只做短间隔节流刷新，失败状态和健康检查仍保留及时更新。

## 2026-05-29 后台 Token/计费补全不能为每个请求创建 APScheduler 一次性任务

- 问题：高并发下每条日志都创建 APScheduler date job，会让后台任务瞬时争抢数据库连接池，并可能引发 `QueuePool limit ... timed out`。
- 影响：后台补全任务会反向拖垮对外 `/v1/*` 主链路，出现 500、超时或大量 `idle in transaction`。
- 解决方法：请求主链路仍同步写最小 `request_logs`；Token/计费补全改用有界后台队列和少量 worker，压测主链路时可临时关闭周期性 scheduler。

## 2026-05-29 同步 request_logs 落库是 1000 RPS 的架构边界

- 问题：当前全局规范要求 `/v1/*` 到达应用层必须同步写入最小 `request_logs`。在本机 PostgreSQL `max_connections=100` 且多实例压测时，吞吐提升到约数百 RPS 后会首先遇到数据库连接池与提交路径瓶颈。
- 影响：仅靠增加线程、进程或实例会很快受到 PostgreSQL 连接数、连接池、同步提交和日志表索引写入限制，无法稳定达到 1000 RPS。
- 解决方法：不修改规范时，应通过 PgBouncer、提高 PostgreSQL `max_connections`、合理配置每 worker 池大小、减少后台 DB 争抢来提升上限；若目标是稳定 1000 RPS，需要确认将日志主链路改为异步队列/批量落库或独立日志管道。

## 2026-05-29 高并发压测中 Web 进程与后台任务混跑导致吞吐受限

- 问题：对外 `/v1/*` 压测时，每个 Web 进程同时启动请求日志落库 worker 与 Token/计费结算 worker，多个 Web 进程横向扩展后会产生大量后台 DB 写入和行锁竞争，挤占 Web 请求 CPU 与数据库连接池，表现为吞吐扩展不线性、DB pool timeout、队列消费跟不上。
- 解决方法：将 Web 请求进程与后台队列消费进程分离。Web 进程设置 `ENABLE_BACKGROUND_WORKERS=false`，只负责鉴权、路由、上游转发和可靠入队；独立运行 `scripts/run_background_workers.py`，通过 `REQUEST_LOG_QUEUE_WORKER_COUNT`、`REQUEST_LOG_QUEUE_BATCH_SIZE`、`TOKEN_FINALIZE_WORKER_COUNT` 控制日志落库和 Token/计费消费能力。
- 复用价值：后续所有 1000 RPS 级别压测或生产部署，都应优先采用“多 Web worker + 独立后台 worker”的形态；若只增加 Web worker 而不分离后台任务，吞吐和延迟会被后台 DB 写入放大拖慢。

## 2026-05-29 压测 mock upstream 自身成为瓶颈导致误判

- 问题：Python `ThreadingHTTPServer` mock upstream 在高并发下会被自身线程调度、连接积压或 TIME_WAIT 影响，导致代理侧出现上游读超时，容易把 mock 的瓶颈误判为代理服务瓶颈。
- 解决方法：新增并优先使用 `scripts/mock_openai_upstream_node.js` 作为高并发本地 mock upstream；压代理前必须先直压 mock upstream，确认 mock 吞吐显著高于目标代理吞吐。
- 复用价值：后续所有“不调用真实上游”的真实链路压测，都必须先证明 mock upstream 不是瓶颈，再解读代理压测数据。

## 2026-05-30 11:46:11 +08:00 - 本地高并发压测若缺少 Redis，会把异步日志与共享并发体系打回同步退化模式

问题：本项目的高并发链路已经把请求日志接收、并发租约和部分缓存一致性建立在 Redis 上。如果本地压测环境没有 Redis，`request_logs` 会回退为同步写库，后台 worker 会持续重试 Redis 连接，请求主链路和后台面都会出现额外阻塞，最终得到失真的吞吐和延迟数据。

解决方法：真实本地压测前必须先确认 `REDIS_URL` 可用；当前项目已补充 `scripts/start_benchmark_stack.ps1`，会优先拉起本地 `redis-server`、mock upstream、独立后台 worker 和多 Web worker，再执行 warmup 与正式 benchmark。若 Redis 不可用，则该轮压测结果默认不可信，不能用来判断代理主链路上限。

复用规则：后续凡是进行 500 并发以上流式压测或 1000 RPS 级别非流式压测时，都必须先验证 Redis 已就绪，并使用统一压测启动脚本或等价流程，禁止直接在“无 Redis 退化形态”下解读性能数据。

## 2026-05-30 11:46:11 +08:00 - 桌面工具环境下的子进程生命周期会影响压测方式，需采用同进程起栈并压测

问题：在当前桌面工具环境里，单独起一个命令启动 mock upstream、Redis 或多 Web worker 后，这些子进程不一定会在下一次工具调用中继续稳定存活；如果按“先起服务、再换一个命令压测”的方式执行，很容易出现端口看似启动、下一步实际已被回收的假失败。

解决方法：把“启动 Redis / mock upstream / Web worker / 后台 worker、执行 warmup、执行正式 benchmark、最后清理”收敛到同一个脚本调用内。当前项目已把这套流程固化进 `scripts/start_benchmark_stack.ps1` 的 `-RunBenchmark` 模式，避免被外部工具的子进程回收策略污染压测结果。

复用规则：后续凡是在类似桌面代理工具、受控沙箱或会话型执行环境里做高并发压测时，都必须优先采用“同进程起栈并压测”的脚本模式，禁止默认假设前一条命令启动的后台进程会长期存活到下一条命令。

## 2026-05-30 12:05:00 +08:00 - 请求日志异步化若仍在主链路序列化整份日志，会把 CPU 热点从数据库转移到 JSON 编码

问题：即使 `request_logs` 已经改成 Redis 异步落库，如果请求主链路在 `enqueue` 前仍同步执行 `dumps_json({"kwargs": ...})`，高并发下就会把大量 CPU 时间消耗在日志对象序列化、trace 展开和大字典编码上。表面上看日志已经“异步”，实际上主链路仍在为每个请求支付完整日志序列化成本。

解决方法：主链路只负责把原始日志对象放入进程内 ingress 队列，真正的 JSON 序列化与 Redis `LPUSH` 改由独立异步批处理器执行；若 Redis 批量入队失败，再由批处理器统一回退到直接批量落库。当前项目已在 `RequestLogQueueService` 中加入本地 ingress queue + batch flush 机制，显著降低了非流式 1000 RPS 场景下的主链路固定 CPU 成本。

复用规则：后续凡是把同步写库改造成“异步队列”的地方，都必须继续检查主链路里是否还残留大对象序列化、压缩、签名或格式转换等 CPU 热点；如果这些操作还在请求线程里执行，就不能把该链路视为真正完成了异步化。

## 2026-05-30 12:40:00 +08:00 - 单机混跑形态下，后台日志批大小过大会拉长事务并反向压低前门吞吐

问题：当 Web 进程和独立后台 worker 在同一台机器上混跑时，如果请求日志后台消费仍使用过大的批大小（例如 500）和过多 worker，同步提交阶段会形成更长事务、更重 WAL/索引写入和更明显的 CPU 抢占，结果不是更快，而是把前门 `/v1/*` 吞吐从约 900 RPS 再往下拖。

解决方法：单机真实压测应优先使用更保守的后台消费参数，例如 `REQUEST_LOG_QUEUE_WORKER_COUNT=1`、`REQUEST_LOG_QUEUE_BATCH_SIZE=100`、`TOKEN_FINALIZE_WORKER_COUNT=1`，让后台落库保持“有批量但不过重事务”的区间。当前项目已把 `scripts/start_benchmark_stack.ps1` 的默认后台参数收敛到这一组更稳的值，用于本地同机压测。

复用规则：后续凡是做“Web 前门 + 后台落库/计费 worker”同机混跑压测时，都不能默认认为批越大越好、worker 越多越好；必须把“事务长度、WAL 写入、索引压力、CPU 争抢”一起纳入调参，并优先用压测结果而不是经验值决定后台批大小。

## 2026-05-30 14:35:00 +08:00 - 压测若复用共享 Redis 或在队列未排空时强停进程，会把吞吐数据系统性污染

问题：本项目的请求日志、并发租约和部分限流状态都依赖 Redis。若 benchmark 直接复用开发/生产 Redis 的默认库，或者 `SkipBackgroundWorker` 压测后不清理队列，后续带后台 worker 的压测就会先消费历史脏积压；如果 benchmark 一结束就强杀 Web/worker 进程，还会留下“客户端成功了，但异步日志还没落库”的半成品状态。

解决方法：压测必须使用独立 Redis 端口或独立逻辑库隔离 benchmark 状态；压测前后都要清理 benchmark 专用 API Key 的 `request_logs` 与 `request_logs:queue/processing` 队列；压测结束前必须等待请求日志落库和 Token finalize 后台任务真正进入空闲。当前项目已将这套流程固化进 `scripts/start_benchmark_stack.ps1`。

复用规则：后续凡是做本地或 CI 高并发 benchmark，都必须优先检查“Redis 是否独立、队列是否清空、后台是否排空”这三个前置条件；只要其中任意一项不满足，该轮 benchmark 结果默认不可信。

## 2026-05-30 14:35:00 +08:00 - 所谓“批量落库”里若每条日志仍单独 flush，一样会把后台写入打回逐条事务热点

问题：请求日志后台 worker 虽然按 batch 从 Redis 取数，但如果批处理循环里每条日志都调用一次 `flush` 去提前拿主键，再继续派发 Token finalize，数据库层面仍会退化成“单条 insert + 单条 flush”的高频往返，实际吞吐和 WAL 压力都接近伪批处理。

解决方法：批处理应先整批 `add`，关闭逐条 `flush` 和逐条 finalize 派发，统一 `flush/commit` 后再根据已生成的日志 ID 派发 Token finalize。当前项目已在 `RequestLogQueueService` 中改成“整批 flush/commit，再统一 enqueue finalize”的落库方式。

复用规则：后续凡是新增异步批量落库、批量写审计日志、批量写账单或批量写统计快照时，都必须检查批处理内部是否残留逐条 `flush`、逐条 `refresh`、逐条 RPC 或逐条二次入队；只要这些动作还在循环里，吞吐就会被伪批处理重新拖回去。

## 2026-05-30 14:35:00 +08:00 - API Key 热路径若本地 L1 命中后仍走线程池鉴权，会白白损失并发

问题：同一 API Key 的高并发请求在多数时间都能命中本地 L1 鉴权缓存，但如果命中后仍然走一次 `threadpool + scoped session` 的完整包装，再去做相同的缓存命中判断，请求主链路还是会为每个请求支付不必要的线程切换和调度开销。

解决方法：对本地 L1 命中的 API Key 鉴权结果，应直接在异步请求协程里完成轻量校验与上下文构建，只有缓存 miss 时才回退到线程池和数据库/Redis 完整鉴权链路。当前项目已在 `require_api_client_auth` 中补上本地缓存 fast-path。

复用规则：后续凡是把同步鉴权、配置读取或对象装配包装成“线程池兼容层”时，都必须继续检查是否存在“热路径明明已经在本地缓存命中，却仍然先切线程再返回”的情况；只要命中缓存的请求仍然跨线程，该热路径就还有继续压缩固定开销的空间。

## 2026-05-30 14:35:00 +08:00 - 单机同机压测的后台 worker 最优值必须在“隔离 Redis + 队列排空 + 主链路优化”后重新测定

问题：如果在 Redis 污染、队列未排空或主链路仍有明显固定开销时就提前下结论，很容易误以为 `1/1` 后台 worker 一定优于 `2/2`。但在修复 benchmark 污染、批量落库伪批处理和本地鉴权 fast-path 之后，后台 worker 的最优点会重新变化。

解决方法：单机同机 benchmark 的后台参数必须在“压测环境已隔离、benchmark 数据已清理、队列排空逻辑已正确、主链路固定成本已压缩”之后重新做 A/B 测试，再决定默认值。当前项目在这组前置条件成立后，`REQUEST_LOG_QUEUE_WORKER_COUNT=2`、`TOKEN_FINALIZE_WORKER_COUNT=2` 的默认组合优于先前的 `1/1` 结论，已同步更新 benchmark 脚本默认值。

复用规则：后续凡是调整后台批大小、日志 worker 数、Token finalize worker 数或 Web/worker 混跑比例时，都必须基于“已净化 benchmark 环境”的新基线重测，禁止沿用历史脏数据条件下得出的最优参数。

## 2026-05-30 - 当前这台机器的同机全链路最优点已进一步收敛为 `14 web / 1 日志 worker / 1 finalize worker`

问题：在继续修正 warmup、重新扩大前门 Web 并发并做长时间压测后，单机同机形态的最优点再次发生漂移。`16` 个 Web worker 虽然能把 `web-only` 压到约 `1018 RPS`，但在带后台日志与 finalize 的真实全链路里，过多 Web worker 和过多后台 worker 会一起放大 CPU 抢占、数据库连接池竞争和 WAL/索引写入争用，导致总吞吐反而回落。

解决方法：必须把“前门 Web worker 数”和“后台日志/Token worker 数”一起联调，而不是分别取各自局部最优。当前项目在本机 mock upstream 真实压测下，已验证的最优同机组合为 `WebWorkerCount=14`、`REQUEST_LOG_QUEUE_WORKER_COUNT=1`、`REQUEST_LOG_QUEUE_BATCH_SIZE=100`、`TOKEN_FINALIZE_WORKER_COUNT=1`。对应实测结果为：非流式 `4000/800` 约 `956.32 RPS`、非流式 `8000/800` 约 `993.45 RPS`、流式 `2000/500` 约 `850.14 RPS`，且均为 `0 failures`。当前项目已把 `scripts/start_benchmark_stack.ps1` 的默认参数同步收敛到这组更优默认值。

复用规则：后续凡是在同一台机器上同时跑 Web 前门、Redis、mock upstream 和后台落库/计费 worker 时，都不能把 `web-only` 的最优并发度直接照搬到真实全链路；必须用“前门 worker 数 + 后台 worker 数”的组合压测重新寻找拐点，并优先保留“0 失败、队列可排空、长压测仍稳定”的参数组合。

## 2026-05-31 - 真实上游 `/models` 返回列表不等于模型实际可调用

问题：真实 OpenAI 兼容上游可能在 `/v1/models` 中返回大量模型，但其中部分模型会在实际 `/v1/chat/completions` 或 `/v1/responses` 调用时返回“没有可用的上游账户”等错误。若功能测试脚本只按模型列表自动挑选通用优先级模型，会把不可用模型误判为代理项目故障。

解决方法：真实上游功能测试必须支持显式指定模型，或把候选模型收敛到已确认可用的模型集合。本项目当前真实上游测试脚本已支持 `--model-name` 与 `--candidate-models`，并默认仅在 `gpt-5.5`、`gpt-5.4` 中选择。

复用规则：后续凡是接入第三方 OpenAI 兼容上游，不能只以 `/models` 拉取结果作为验收依据；必须至少用目标模型分别完成非流式、流式、`/v1/chat/completions` 与 `/v1/responses` 的真实探针。

## 2026-05-31 - 异步流式上游上下文未进入成功时禁止强行 `__aexit__`

问题：流式上游连接在 `asynccontextmanager.__aenter__()` 阶段失败时，如果异常处理仍无条件调用同一个上下文的 `__aexit__()`，可能把原始网络错误覆盖成 `generator didn't stop after athrow()`，导致日志和调用方都看不到真实上游错误，并增加流式链路挂住风险。

解决方法：流式连接上下文必须维护 `entered` 标记，只有 `__aenter__()` 成功返回后才允许执行 `__aexit__()`；fallback 上游上下文也必须使用同样模式。本项目已在 `ProxyService._open_stream_with_endpoint_fallback` 中补充 `stream_context_entered` 与 `fallback_context_entered` 保护。

复用规则：后续凡是手动调用异步上下文管理器的 `__aenter__/__aexit__`，必须把“进入成功”和“需要退出”作为显式状态记录；禁止在 `__aenter__` 失败路径无条件清理未进入的上下文。
## 2026-05-31 告警中心接口返回 datetime 导致 JSON 序列化 500

- 问题现象：访问 `/api/alerts/feed` 时，服务端日志反复出现 `TypeError: Object of type datetime is not JSON serializable`，接口返回 `500 Internal Server Error`。
- 问题原因：告警面板 payload 中包含 `datetime` 等非原生 JSON 可序列化对象，直接传给 `JSONResponse` 会触发 Starlette 标准 `json.dumps` 序列化失败。
- 解决方法：接口返回前必须复用 `app.utils.json_utils.to_jsonable` 做递归转换，再交给 `JSONResponse`。
- 复用价值：后续所有手写 `JSONResponse` 的接口若返回服务层聚合 payload，应先确认是否包含 `datetime`、`date`、`Decimal` 等对象，并统一走 `to_jsonable` 或等价转换，避免同类 500。

## 2026-05-31 工具调用健康检查错误跳过可执行模型

- 问题现象：提供商健康检查中的“工具调用检查”阶段显示“当前阶段无可执行模型，已跳过”，即使该提供商下已有 `gpt-5.*` 等实际支持工具调用的模型。
- 问题原因：健康检查阶段筛选直接读取 `provider_models.supports_tools` 原始字段；当历史数据尚未手工勾选该字段时，会把本应可执行的模型全部排除。与此同时，项目其他路由与能力展示已使用“显式配置 + 模型名推断”的统一能力判断，两者口径不一致。
- 解决方法：健康检查中的工具阶段目标筛选必须改为复用 `ProviderService.provider_model_supports_tools()` 统一判断，避免只因原始布尔字段未同步就整阶段跳过；前端对真正被跳过的阶段应显示“已跳过”，不要继续用“未知”误导排查。
- 复用价值：后续凡是做模型能力筛选、健康检查探针挑选、能力徽标展示或路由候选过滤时，都必须优先复用统一能力判断函数，禁止在某个子流程里退回只读原始布尔字段，避免再次出现“页面显示支持、检查阶段却跳过”的口径漂移。

## 2026-05-31 回归脚本复用共享测试 API Key 时会被历史配额状态污染

- 问题现象：运行 `stage10_tools_regression_check.py`、`stage12_error_response_regression_check.py` 时，业务请求未进入目标断言路径，而是直接返回 `insufficient_quota` / `Api key token quota exhausted`。
- 问题原因：回归脚本依赖的测试 API Key 或账户配额可能被历史调试、压测或其他回归运行消耗完；此时脚本失败反映的是测试前置状态不干净，而不是当前被测功能必然回归。
- 解决方法：运行依赖 API Key 额度的回归脚本前，必须先重置或准备独立测试 Key 的请求数、Token 与金额配额，并避免复用已被压测污染的共享 Key。
- 复用价值：后续排查回归脚本失败时，若错误码为 `insufficient_quota`，应先核对测试 Key/账户额度前置条件，再判断业务链路是否真的失败，避免把环境配额问题误判为功能回归。

## 2026-06-01 09:39:46 +08:00 - 模型官方价格存在固定价、阶梯价与未定价三种形态，单一价格字段不足以表达真实计费口径

- 问题现象：模型配置页需要录入官方真实价格后，发现不同厂商价格口径并不统一；同一模型可能同时存在固定价、按输入区间阶梯计价、按输出区间二次分档、缓存命中价、缓存写入价和未公开定价等情况。若只保留 `input/output/cache *_per_1k` 三个数值字段，就会丢失真实官方阶梯信息，日志和账单也无法知道本次请求到底命中了哪一档价格。
- 问题原因：历史结构默认把“模型价格”视为单一固定值，没有为官方分档、缓存细分口径和未定价状态预留结构化表达，也没有把请求实际命中的价格档位回填到 `request_logs`。
- 解决方法：模型目录层必须新增 `pricing_mode + pricing_json` 结构化价格数据，统一支持 `fixed`、`tiered`、`unpriced` 三种价格形态；请求日志层必须增加 `pricing_tier_key / pricing_tier_name` 记录实际命中档位；计费时按 `prompt_tokens`、`completion_tokens` 和渠道倍率解析真实命中价，再回填 `channel_price_*` 和档位信息。
- 复用价值：后续凡是新增官方模型价格、同步第三方模型目录、排查日志费用为零、对账或导出账单时，都必须优先基于结构化价格数据和实际命中档位处理，禁止再把官方价格强行压扁成单一固定价。

## 2026-06-01 暗黑模式组件漏白来自浅色硬编码与第三方表格默认色

- 问题现象：切换暗黑模式后，仪表盘用量统计、调用测试、告警中心等页面仍可能出现白色卡片或白色表格区域。
- 问题原因：部分页面组件写死了 `#fff`、浅色渐变或 Bootstrap `.table` 默认背景，未完全接入 `--surface`、`--surface-soft`、`--text`、`--line` 等主题变量；移动端表格卡片化规则也会重新写入浅色背景。
- 解决方法：暗黑模式必须为通用表格、卡片化表格行、仪表盘摘要卡、调用测试结果卡和告警列表类组件补齐统一覆盖，优先使用主题变量和 `color-mix()`，避免逐页散落修补。
- 复用价值：后续新增前端卡片、表格、结果区、空状态或批量列表时，必须同时检查暗黑模式和移动端表格化状态，禁止把浅色硬编码作为组件默认背景后不提供暗色覆盖。

## 2026-06-01 运维监控磁盘指标口径混淆会误导为项目占用过大

- 问题现象：运维监控页“磁盘”环形卡显示约 `74.6%` 与 `69.8 GB`，容易被理解为当前项目占用了几十 GB。
- 问题原因：卡片百分比读取的是项目所在宿主机分区已用比例，副文本读取的是同一分区可用空间；这两个值都不是当前项目目录占用，但页面标题未说明口径。
- 解决方法：后端单独提供 `host.project_disk`，按项目目录递归统计实际占用并缓存 60 秒；前端资源卡改为“项目目录”，读取 `project_disk.percent` 与 `project_disk.used_bytes`。
- 复用价值：后续新增运维指标时必须先确定口径是宿主机、分区、进程、worker 还是项目目录，并在字段名和界面标题中体现，禁止把不同层级指标混在同一卡片中展示。

## 2026-06-01 后台积压误统计历史鉴权失败和压测日志导致服务假降级

- 问题现象：运维监控页显示服务 `degraded`，原因是后台积压 `pending_finalize_logs=2755`，但 Redis、数据库、CPU、内存均正常。
- 问题原因：后台积压统计条件过宽，把 `api_client_auth`、`bench_concurrent`、`bench_insert` 等历史鉴权失败或压测日志也纳入 Token/计费补全待处理口径；这些日志并不等同于当前正式业务请求积压。
- 解决方法：`SystemMetricsService._background_snapshot()` 的 `pending_finalize_logs`、`billing_failed_logs`、`token_failed_logs` 统一限制为正式模型调用日志类型 `chat`、`responses`。修复后状态恢复为 `ready`，真实待补全正式调用日志为 `11`，失败数为 `0`。
- 复用价值：后续新增后台任务积压监控时，必须先定义“该后台任务真正要消费的候选数据集合”，禁止用宽泛日志表条件直接代替任务队列口径，避免假告警和假降级。

## 2026-06-01 提供商状态与单模型健康混用会误导可用性判断

- 问题现象：提供商管理页把提供商级 `health_status` 直接显示为“状态/健康”，容易让用户误以为一个模型异常就代表整个提供商不可用。
- 问题原因：提供商是多模型挂载容器，`providers.health_status` 是启用模型健康度的聚合结果，而真实路由、探针和故障定位需要落到 `provider_models.health_status`。
- 解决方法：前端列表和监控展示把提供商级字段改为“整体可用性”，使用“全部可用 / 部分可用 / 全部不可用 / 未检测”标签；模型矩阵和筛选继续显示“模型健康”，并以启用模型健康分布辅助判断。
- 复用价值：后续新增告警、筛选、路由排查或健康报告时，必须明确区分提供商聚合可用性与单模型健康，避免把容器级摘要当成模型级结论。

## 2026-06-01 项目目录递归占用统计缓存过短会造成监控额外开销

- 问题现象：运维监控接口已改为通过 `host.project_disk` 递归统计当前项目目录实际占用，但缓存周期若只有 60 秒，监控页面持续刷新时仍会周期性触发扫盘。
- 问题原因：项目目录占用统计需要递归遍历文件树，成本高于读取 CPU、内存、进程等轻量指标；本地项目位于哪个盘并不改变该成本，关键是避免高频目录遍历。
- 解决方法：后端 `SystemMetricsService.PROJECT_DISK_CACHE_SECONDS` 统一调整为 6 小时，`host.project_disk.cached_seconds` 返回 `21600`；前端继续读取缓存后的项目目录占用，不额外触发扫盘。
- 复用价值：后续新增监控指标时，凡涉及递归文件系统扫描、全表扫描、批量外部探测等高成本采样，都必须设置明显长于页面刷新频率的缓存或异步采集机制。

## 2026-06-01 模型健康状态不能直接复用启停状态

- 问题：模型配置页原有“状态”列表达的是模型全局启停，容易与模型健康检测结果混用；若批量检测按模型或渠道串行执行，也会在模型和渠道数量增加后明显变慢。
- 解决方法：模型表格必须把启停列与健康状态列分开；模型健康状态按绑定渠道聚合，只要存在任一可用绑定渠道即为健康。模型级健康检测需要双层并行：不同模型并行、同一模型不同绑定渠道并行，探测结束后再统一落库并刷新聚合状态。

## 2026-06-01 服务启动期循环导入会导致本地重启失败

- 问题现象：本地执行 `run.ps1` 重启后，`uvicorn` 在导入应用时失败，错误为 `ImportError: cannot import name 'ApiKeyService' from partially initialized module`。
- 问题原因：启动期形成 `api_key_service -> router_service -> model_catalog_service -> health_service -> proxy_service -> api_key_service` 的循环导入；其中 `model_catalog_service` 顶层导入 `HealthService`，但该依赖只在模型健康探测路径需要。
- 解决方法：移除 `model_catalog_service.py` 对 `HealthService` 的顶层导入，在 `_probe_catalog_health` 与 `_finalize_catalog_health_test` 内按需惰性导入，避免普通启动路径加载完整代理服务链。
- 复用价值：后续服务层新增跨模块依赖时，若只在低频任务、探测、后台作业或可选能力中使用，应优先使用局部导入或抽取轻量公共模块，避免启动期循环导入。

## 2026-06-01 内部请求日志误入 Token/计费补全积压会造成假积压与无意义重试

- 问题现象：运维监控显示后台积压 `11`，但逐条排查发现这些日志的 `api_client_key_id` 全为空，属于历史内部请求；`token_finalize_attempt_count` 却被 15 秒 backfill 反复推高到 `14/15`。
- 问题原因：后台积压统计和 `TokenUsageService.backfill_missing_usage()` 仅按 `billing_finalized_at is null` 与日志类型筛选，未排除无外部 API Key 结算语义的内部请求日志。
- 解决方法：后台积压统计与 Token/计费 backfill 候选统一追加 `api_client_key_id is not null` 约束，只保留真正需要 API Key 结算的正式调用日志；对已有本地历史内部脏日志执行一次性清理。
- 复用价值：后续新增任何“待补全 / 待结算 / 待重试”监控或后台任务口径时，都必须先确认对象是否真的存在对应结算语义或消费动作，禁止把内部请求、演示请求或无结算对象日志混入正式积压。

## 2026-06-01 自动夹具式并发探测会污染真实性能结论

- 问题现象：并发探测在未显式提供外部 API Key 时，会通过自动创建测试夹具生成专用提供商、测试用户和测试密钥；探测结果容易被夹具上的单模型、单候选和并发上限配置提前卡死，得到的不是项目真实对外能力。
- 问题原因：压测脚本把“便于自动跑通”与“真实外部链路评估”混在同一流程中，自动夹具会引入额外限流、路由收缩和隔离配置，破坏真实结论。
- 解决方法：真实并发探测必须只走项目对外 `/v1/*` 流式文本接口，并强制使用外部用户实际调用时使用的 API Key；禁止在真实性能评估链路里自动创建专用夹具、专用提供商或指定单一渠道。
- 复用价值：后续新增压测、自检、验收或性能报告能力时，若目标是“评估项目整体对外能力”，必须优先复用真实外部鉴权、真实路由和真实分发策略，禁止用便捷夹具替代正式链路后再把结果当成真实性能结论。

## 2026-06-01 22:56:53 +08:00 - 后台即时测试按钮不能默认跑完整多模态能力探针

问题：提供商管理、模型管理中的“测试”“检查全部”“一键测试”若默认执行文字、tools、图像理解、图片生成等完整能力探针，且遇到失败还逐项重试，就会让一次普通点击被最慢的高成本探针拖住，表现为按钮长时间转圈、结果迟迟不返回，并额外放大上游请求量。

解决方法：人工点击的即时验活默认只跑轻量文本链路，并且只探测模型声明支持的正式文本端点；完整能力验收保留给专项探针、定时能力检查或明确的能力验收入口。批量测试必须跨提供商、跨模型、跨绑定渠道并行执行，并继续保留 300 秒冷却与单探针失败最多立即重试一次。

复用规则：后续新增后台即时测试入口时，必须先判断用户是否需要“快速验活”还是“完整能力验收”；若是按钮式即时反馈，禁止默认把图片生成、视觉、工具调用等高成本探针混入主流程。
## 2026-06-01：外部路由候选耗尽只单轮失败返回

- 问题：对外 `/v1/*` 请求原先只执行一轮候选选择和上游尝试；当所有候选被容量、失败率、熔断或临时上游错误耗尽时，会立即向调用方返回失败，不能体现提供商整体自动恢复与重路由能力。
- 解决方法：将候选耗尽等待重试接入代理主链路，在响应尚未建立前按当前路由策略重新计算候选并重试；默认最大等待 600 s，可在路由设置中配置 0-600 s；到期后返回 `all_providers_unavailable_after_retry` 服务不可用错误并保留 trace。

## 2026-06-01：后台测试按钮缺少能力选择导致探测范围不透明

- 问题：提供商、模型、批量健康测试若由后端固定测试范围，用户无法在“只测文本”“追加工具调用”“追加图片理解”“专项图片生成”之间明确选择，容易出现测试耗时、成本和结果语义不符合当前运维目标。
- 解决方法：后台测试入口统一先弹出测试功能多选框，默认仅勾选文本，并让文本同时覆盖非流式与流式链路；图片理解、工具调用、图片生成按用户勾选追加执行，其中图片生成仅作为显式专项选择运行。

## 2026-06-03：中文路径下补丁工具与 PowerShell 引号处理

- 问题：在当前中文路径工作区内，apply_patch 启动失败并返回 The system cannot find the path specified；同时 pwsh -Command 中若直接使用 PowerShell 变量或包含 HTML/CSS 双引号，容易被外层 shell 提前展开或解析失败。
- 影响：会阻塞代码补丁落地，并可能把字面换行标记或错误转义写入前端资源。
- 解决方法：优先使用小范围、可校验的 PowerShell 行替换；含 PowerShell 变量的命令必须使用单引号包裹 -Command 内容，或在双引号外层中用反引号转义变量符号；修改后必须用 rg、片段读取和语法检查确认没有字面换行标记、错误反斜杠选择器或语法异常。

## 2026-06-03：模型目录缓存会让离线回归误判路由不可用

- 解决方法：涉及路由、`/v1/models`、工具调用、图片理解或错误响应的离线脚本，创建 provider/model 夹具后必须显式启用当前临时库的 `ModelCatalog`，并调用 `ModelCatalogService.invalidate_model_runtime_cache()`；对熔断/半开状态切换类断言，测试环境可关闭 L1 短缓存或在状态变更后再次失效缓存。
- 复用价值：后续新增本地或 CI 回归脚本时，凡使用独立数据库但复用 Redis/进程缓存，都必须在夹具写入后清理运行时缓存，禁止直接依据残留缓存判断路由、模型列表或健康状态。

## 2026-06-03：计费扣减后鉴权缓存节流会导致余额耗尽仍放行

- 问题：Token/计费补全完成后，API Key 鉴权缓存失效受 `api_key_auth_usage_invalidate_interval_ms` 节流影响；当本次结算发生非零余额或费用扣减时，旧缓存可能仍保留扣减前余额，导致后续鉴权短时间内错误放行。
- 解决方法：Token 用量类缓存失效可以继续节流；但 `billing_delta` 非零时必须强制失效对应 API Key 和账户鉴权缓存，确保余额、费用额度和账户级控制在结算后立即进入鉴权链路。
- 复用价值：后续任何会改变 API Key、账户余额、额度、授权范围、启停状态或路由策略的后台任务，都必须主动失效鉴权缓存；性能节流只能用于低风险统计字段，不能覆盖余额和权限边界。

## 2026-06-03：同一数据库 Session 捕获查询异常后必须回滚

- 问题现象：运维监控烟测调用 `SystemMetricsService.collect()` 时，设置快照读取异常被捕获后继续复用同一个 SQLAlchemy Session，后续请求日志聚合查询报 `current transaction is aborted`，导致整个指标采集失败。
- 问题原因：PostgreSQL 事务内任一语句失败后，当前事务会进入 aborted 状态；即使业务代码捕获了异常，也必须先执行 `rollback()`，否则同一 Session 的后续查询都会被拒绝。
- 解决方法：在 `SystemMetricsService._limits_snapshot()` 捕获设置读取异常后，对传入 Session 执行 `db.rollback()` 再回退到环境配置默认值，保证后续流量、队列、provider 聚合仍可继续采集。
- 复用价值：后续任何服务层函数若在共享数据库 Session 中捕获 SQLAlchemy/数据库异常并继续执行后续查询，必须先回滚事务，禁止只吞异常后继续复用已失败事务。

## 2026-06-03：PostgreSQL 布尔列迁移禁止使用整数默认值


## 2026-06-03 API Key 管理页密钥为空

- 问题：`api_client_keys` 表缺少模型新增字段 `route_exhausted_retry_infinite_enabled` 时，ORM 查询 API Key 会因缺列失败，管理页无法正常拿到包含 `raw_api_key` 的序列化结果，表现为密钥显示为空或列表数据异常。
- 解决方法：在 `init_database()` 启动初始化流程中补充 `api_client_keys` 字段迁移，缺失时自动执行 `ALTER TABLE api_client_keys ADD COLUMN route_exhausted_retry_infinite_enabled BOOLEAN NOT NULL DEFAULT false/0`。
- 复用建议：后续 `ApiClientKey` 模型新增持久化字段时，必须同步补充启动迁移或独立迁移，并用真实数据库检查 ORM 查询是否仍可成功。


## 2026-06-03：引用型页面误用重聚合接口会让小数据量页面依然很慢

- 问题：概览、路由设置、调用测试、API 密钥管理、模型引用选择等前端页面只需要提供商/模型的轻量引用数据，却直接请求包含完整模型挂载、质量聚合、日志统计甚至敏感字段的重接口，导致即使提供商数量很小，页面首屏和交互刷新仍被后端聚合与大 payload 拖慢。
- 解决方法：为引用型页面拆分轻量接口（如 GET /api/providers/options、GET /api/providers/playground、GET /api/providers/summary、GET /api/models/options），前端统一改用轻量接口，并为这些短周期引用数据增加 10-30 秒本地缓存与并发请求去重；涉及提供商/模型新增、编辑、删除后必须主动失效对应前端缓存。
- 复用价值：后续新增任何筛选器、下拉框、授权选择器、调用测试器或概览型页面时，若页面只消费名称、状态、可用数量或能力摘要，必须优先复用轻量引用接口，禁止直接复用带重聚合和大字段的管理详情接口。

## 2026-06-03：本地启动脚本检测到 Redis 不可用时不能只降级

- 问题：本地 `.env` 已配置 `REDIS_URL=redis://127.0.0.1:6379/0`，但 `run.ps1` 检测到 6379 未监听时直接清空当前进程的 `REDIS_URL`，导致实时并发、限流、队列等依赖 Redis 的本地能力不可用；即使系统已安装 `redis-server.exe`，启动脚本也不会自动拉起。
- 解决方法：本地开发环境中，启动脚本应先解析 PATH 中可用的 `redis-server.exe`，在本地 Redis 端口未监听时启动绑定 `127.0.0.1` 的 Redis 实例并等待端口就绪；只有找不到 Redis 程序或启动失败时，才允许临时清空 `REDIS_URL` 降级启动管理端。
- 复用价值：后续本地一键启动脚本处理 PostgreSQL、Redis、后台 worker 等可选依赖时，应优先尝试复用本机已有运行时自动恢复服务，再执行明确提示的降级路径，避免悄悄关闭影响关键能力的依赖。

## 2026-06-03：本地启动脚本重复执行会在依赖安装窗口抢占端口

- 问题：连续或重复执行 `run.ps1` 时，脚本前段可能判断 8000 未占用，但在执行 `pip install` 等耗时步骤期间，另一个旧启动进程或并发启动进程先绑定 8000，最终当前 `uvicorn` 报 `Errno 10048`。
- 解决方法：本地启动脚本必须使用项目级互斥锁阻止多个 `run.ps1` 同时启动；在真正执行 `uvicorn` 前必须再次解析可用端口并输出最终服务地址，避免端口检查结果在启动前失效。
- 复用价值：后续本地一键启动脚本若包含依赖安装、初始化、迁移或其它耗时步骤，必须在关键启动动作前重新确认端口、锁或外部资源状态，禁止只依赖脚本开头的一次性检查。

## 2026-06-04：ORM 模型新增字段后必须同步完成数据库列补齐再执行业务回填

- 问题：`ApiClientKey` 模型新增 `auto_sync_provider_bindings` 字段后，若数据库未完成列迁移，任何直接查询 `api_client_keys` 的 ORM 读取、回填脚本或管理接口都会先报 `UndefinedColumn`，导致“现有密钥全渠道回填”无法执行。
- 解决方法：新增持久化字段后必须先通过启动迁移或独立 DDL 把数据库列补齐，再执行基于 ORM 的数据回填或批量更新；校验时应同时检查“列是否存在”和“业务回填是否完成”，不能只跑其中一步。
- 复用价值：后续所有模型字段扩展，尤其是权限、额度、路由、状态类字段，都必须遵循“先结构迁移，后数据回填，最后功能验证”的顺序，避免因缺列把回填和接口逻辑一并阻塞。

## 2026-06-04：模型映射命中的目标模型失败后必须自动切换到其它可用映射目标

- 问题：外部请求命中模型映射后，系统原先只会选中一个目标模型继续路由；若该目标模型因上游协议不兼容、端点不支持、参数校验失败或临时异常返回 4xx/5xx，链路会直接把失败返回给调用方，不会继续尝试同一映射中的其它可用目标。
- 解决方法：模型映射解析阶段必须保留有序可用目标列表；代理主链路在“当前映射目标无路由候选”或“当前映射目标上游失败”时，必须把已失败目标加入排除集并自动切换到下一个可用映射目标重试，同时在 `request_logs.trace_json` 中记录 `model_mapping_failover` 留痕。
- 复用价值：后续新增别名模型、保底模型、跨供应商同义模型或灰度映射策略时，必须把“映射目标切换”视为标准容灾能力，禁止把单个上游目标的协议差异或瞬时故障直接暴露给外部调用方。

## 2026-06-04：后台人工模型测试不能复用正式流式超时和通用重试成本

- 问题：模型管理页或提供商页人工点击“测试”图片模型的文本能力时，虽然不同渠道已经并行，但单个渠道内部仍会顺序执行 `text` 和 `text_stream` 探针；若其中某些 `responses` / `responses stream` 端点本身不支持、需要付费、鉴权失败或限流，健康检查还会沿用正式业务的较长流式首包超时，并对明显失败结果再重试一次，导致整次人工测试被最慢失败渠道拖到数秒甚至十秒以上。
- 解决方法：人工触发的即时健康检查必须单独标记为交互式探针模式；该模式下流式探针应使用更短的连接超时和首 token 超时，不能直接复用正式代理链路的长超时；同时对明确不可恢复的失败结果（如 4xx、`not implemented`、`bad_response_status_code`、`invalid api key`、`paid_model_required`、`rate_limit_exceeded` 等）必须跳过二次重试，避免把无意义等待叠加到人工测试总时延。
- 复用价值：后续凡是后台“测试”“检查全部”“一键测试”这类即时验活入口，只要目标是给运维快速反馈，就必须区分“正式业务超时/重试策略”和“交互式探针超时/重试策略”，禁止把正式生产链路的容错成本原样搬到人工测试链路。

## 2026-06-04：OpenAI 兼容模型接入不能只靠少数模型名前缀推断能力

- 问题：当项目接入 DeepSeek、通义、智谱、Kimi、豆包、MiMo 等 OpenAI 兼容模型时，如果默认能力识别仍只覆盖 `gpt/qwen/deepseek/glm` 等少数前缀，路由层会把真实支持工具调用或图片理解的模型误判为“不支持”，进一步导致 `/v1/chat/completions`、`/v1/responses` 请求在候选筛选阶段被错误排除。
- 解决方法：模型默认能力推断必须按“厂商系列 + 官方命名模式”统一扩展，至少补齐 `kimi` / `moonshot-v1-vision` / `doubao` / `mimo` / `qwen-vl` / `glm-4v` 等系列的工具调用、图片理解和端点支持识别；同时保留后台模型目录的显式能力字段，允许管理员覆盖默认推断结果。
- 复用价值：后续每次新增 OpenAI 兼容模型系列时，都必须同步检查默认能力推断、模型目录同步、健康探针与路由筛选四个环节，禁止只在单一页面补一个显示标签。

## 2026-06-04：OpenAI 兼容上游的 chat/responses 端点支持差异必须在路由前声明并在代理层安全互转

- 问题：不同 OpenAI 兼容厂商并不都原生支持 `/v1/responses`；若项目把所有模型默认标记为同时支持 `chat/completions` 和 `responses`，或在路由层硬性要求请求入口与上游原生端点一一对应，就会出现两类问题：一类是把仅支持 `chat/completions` 的模型错误宣称为原生支持 `responses`；另一类是明明可通过安全互转支持的简单文本、工具调用、图片理解请求，在候选筛选阶段就被提前挡掉。
- 解决方法：模型能力层必须区分“原生支持 `chat/completions`”“原生支持 `responses`”与“可通过安全 fallback 支持”；代理层在请求进入路由前，应先评估当前 payload 是否满足无损或低损互转条件，再按“原生支持或可安全 fallback”放宽候选筛选；工具调用互转时还必须同步适配 `tools` 与 `tool_choice` 的 chat/responses 结构差异。
- 复用价值：后续凡是接入新的 OpenAI 兼容上游、扩展新的 `responses` 字段或引入新的工具调用形态，都必须先重新校验端点原生支持矩阵与互转安全规则，禁止把“OpenAI 兼容”直接等同于“所有端点完全等价”。

## 2026-06-04：Kimi 视觉模型不接受普通远程图片 URL，代理层必须按供应商差异规范化图片输入

- 问题：Moonshot/Kimi 的官方视觉文档要求图片输入使用 base64 `data:image/...` 或已上传文件 `file_id`；若项目继续把通用 OpenAI 兼容视觉请求中的普通 `https://...` 图片 URL 原样透传到 Kimi，上游会把它当成不支持的图片输入格式，导致图像理解链路实际不可用。
- 解决方法：代理层在发往上游前必须按“供应商 + 模型系列 + 输入模态”做最小必要规范化；命中 `kimi-*` / `moonshot-v1*` 视觉模型且请求中存在远程图片 URL 时，需自动抓取图片并转成 base64 data URL 后再发送。已是 data URL 或文件 ID 的输入应原样保留，其他供应商链路不得被一并改写。
- 复用价值：后续新增视觉模型供应商时，必须把“是否接受远程 URL / 是否只接受 base64 / 是否支持 file_id”作为独立兼容项核验，禁止仅因其标注 OpenAI 兼容就默认多模态输入格式完全一致。

## 2026-06-04：OpenAI 兼容厂商的原生 responses 能力不能长期沿用旧启发式

- 问题：Qwen 等厂商在早期接入时可能只暴露 `chat/completions`，但后续官方文档会逐步开放或声明 OpenAI 兼容 `responses` 入口；如果项目仍把整条家族前缀永久写死为“仅 chat 原生”，就会低估其正式能力，并导致健康检查、能力展示与复杂请求路由保守退化。
- 解决方法：当官方文档已经明确提供 OpenAI 兼容 `responses` 说明时，应及时更新默认能力推断，把该模型家族从“chat-only”提升为“原生 responses”；若其它厂商只证明了 OpenAI SDK / chat 兼容，而未明确公开 `responses` 协议细节，则应继续按“chat 原生 + 安全互转兜底”处理，避免把适配能力误标为原生能力。
- 复用价值：后续新增或复审 OpenAI 兼容供应商时，必须把“官方当前声明的原生端点矩阵”作为独立核验项，禁止让历史 heuristics 长期滞后于厂商协议演进，也禁止在证据不足时把适配支持误包装成原生支持。

## 2026-06-04：账户余额、API Key 配额与 Redis 实时计数必须分口径治理

- 问题：用户共享账户余额充足时，API Key 自身 Token 配额耗尽仍会被通用 `insufficient_quota` 文案展示为“额度不足”，同时 `api_client_keys.total_cost_used`、Redis 配额计数、账单记录和 `request_logs` 之间可能漂移，导致排查时误以为是账户余额扣错。
- 解决方法：余额只以 `user_accounts` 共享钱包为账户口径；API Key 的 Token/金额上限必须使用独立错误码与文案；用量与费用重算必须以正式 `request_logs` 为主口径，并在重算提交后同步覆盖 Redis 总/日/月配额计数与失效鉴权缓存。
- 复用价值：后续新增任何额度、价格、账单、实时限流或展示逻辑时，必须先区分“账户余额”“账户级配额”“API Key 专属配额”“Redis 实时计数”和“请求日志结算口径”，禁止用一个“额度”概念混合表达。

## 2026-06-04：模型映射后的请求日志必须保留调用方原始模型名

- 问题：当外部请求命中模型映射时，代理主链路会把真正发往上游的 `payload.model` 改写为映射后的目标模型；如果日志结构摘要直接基于改写后的 payload 生成，`request_logs.request_body_json` 中的 `model` 会被错误记录成目标模型，导致排查时把一次“别名模型调用”误看成“调用方原本就请求了目标模型”。
- 解决方法：请求日志中的 `request_body_json` 结构摘要必须始终基于调用方原始请求体生成；正式落库字段仍可分别使用 `requested_model` 记录原始模型名、`model_name` 记录实际命中模型名，但禁止让请求体摘要覆盖调用方原始 `model`。
- 复用价值：后续凡是代理链路会对请求体做模型映射、端点 fallback、协议适配或参数改写时，日志必须区分“原始请求视角”和“内部转发视角”，避免排查误判。

## 2026-06-04：长时间候选耗尽等待重试时 attempt_count 不能回落为 0

- 问题：当 `/v1/*` 请求在路由阶段长期处于 `route_exhausted_wait_retry`，但始终没有真正进入上游连接时，旧的 `attempt_count` 推导只统计 `success/http_error/rate_limited/model_not_found` 等直接尝试事件，最终会把长时间内部重试写成 `attempt_count=0`，既误导日志中心，也会把错误响应体里的 `attempt_count` 一并写错。
- 解决方法：`attempt_count` 推导必须优先统计真实上游尝试事件；若本次请求只发生了 `route_exhausted_wait_retry`，则至少按等待重试轮次回填；同时刷新派生字段时只能用更大的推导值覆盖缺失或更小的旧值，禁止把显式传入的尝试次数再错误压回 `0`。
- 复用价值：后续新增任何“内部等待后重试”“候选耗尽回退”“异步重试窗口”机制时，日志中的尝试次数必须覆盖这类纯内部重试阶段，不能只统计真正发往上游的网络尝试。

## 2026-06-04：无可恢复候选时禁止继续等待路由重试窗口

- 问题：若某模型请求的候选筛除原因全部属于不可恢复条件，例如“没有任何匹配模型挂载”“匹配模型全部不支持工具调用/视觉/Responses”“全部未授权或已禁用”，旧逻辑仍可能因为同时存在 `provider_circuit_open` 等背景原因而误判为可恢复，继续等待最长 600 秒的内部重试窗口，导致用户看到长时间卡住后才失败。
- 解决方法：路由重试判定必须先区分“可恢复原因”和“不可恢复原因”；当 `matching_model_mount_count=0`，或匹配到的模型全部被不可恢复能力/授权/禁用原因筛空时，必须立即停止等待并直接返回明确错误；只有容量、失败率、熔断、模型健康等具备恢复可能性的原因，才允许进入等待重试窗口。
- 复用价值：后续调整路由容灾、熔断恢复或自动等待策略时，必须先判断失败原因是否具备恢复可能，禁止把结构性配置错误与临时可恢复故障混用同一等待逻辑。

## 2026-06-04：超长请求体日志截断后仍必须保持 JSON 可解析

- 问题：旧的请求体日志截断逻辑直接对序列化后的 JSON 字符串按字节裁切，再拼接 `...[truncated]`，这会把 `request_body_json` 截成非法 JSON，后续 `safeJsonParse`、回放、结构化回填和诊断脚本都无法稳定解析。
- 解决方法：当请求体或响应体日志超过字节上限时，必须输出仍然可解析的结构化 JSON 摘要，例如保留 `_truncated`、原始字节数、可读预览、键数量和键名摘要；禁止直接把原 JSON 文本拦腰截断成非法字符串。
- 复用价值：后续所有 payload 日志、metadata 摘要、流式结果摘要都必须遵守“即使截断也保持结构化可解析”的原则，避免观测链路自己制造不可解析脏数据。

## 2026-06-05：健康层级 `0` 不能用 `value or default` 解析

- 问题：路由和模型映射中 `health_tier=0` 表示最高优先级健康候选；若使用 `int(item.get("health_tier") or 1)` 解析，Python 会把合法的 `0` 当作假值并替换为默认值 `1`，导致健康候选与未知候选排序层级被混淆。
- 解决方法：解析健康层级、优先级、权重等允许为 `0` 的数值字段时，必须显式判断 `None` 或使用专门 helper，禁止用 `or` 兜底默认值。
- 复用价值：后续凡是新增路由分桶、优先级分层、成本权重或状态枚举数值时，都需要保留 `0` 的业务含义，避免默认值写法悄悄改变排序结果。

## 2026-06-05：性能配置默认值变更必须同步检查 `.env` 覆盖

- 问题：缓存 L1 TTL 的代码默认值已提高，但当前 `.env` 仍显式配置为 `1` 秒，导致运行时实际配置没有变化，回归脚本也能直接暴露“默认值改了但项目实际不生效”的问题。
- 解决方法：调整缓存、并发、超时、队列等性能类配置时，必须同时检查 `Settings` 默认值与项目 `.env` 覆盖值；若目标是让当前项目立即生效，应同步更新 `.env` 中对应键，且只改本次相关配置，禁止触碰密钥或业务数据。
- 复用价值：后续性能调参、部署参数优化和压测修复都需要先确认运行时最终值，避免只改代码默认值却被环境文件覆盖。

## 2026-06-05：缓存 miss token 不能被当作缓存写入 token

- 问题：DeepSeek 等上游会返回 `prompt_cache_hit_tokens` 与 `prompt_cache_miss_tokens`；其中 hit 可作为缓存读取 token，但 miss 只表示未命中前缀，不等价于已创建缓存或产生缓存写入费用。若把 miss 计入 `cache_write_tokens`，日志缓存率和计费都会被误导。
- 解决方法：缓存读写统计必须只识别具备官方语义的显式 usage 字段；`prompt_cache_hit_tokens` 计入缓存读，`prompt_cache_miss_tokens` 不计入缓存写。只有 `cache_creation_input_tokens`、`cache_write_input_tokens` 等明确创建/写入字段才允许计入缓存写。
- 复用价值：后续新增供应商缓存字段映射时，必须先区分 hit/read、miss、creation/write 三类语义，禁止为了提高表观缓存率而把 miss 或普通输入 token 合并到缓存写入字段。

## 2026-06-05：路由粘性不能把 `user` 字段当作同一会话标识

- 问题：OpenAI 兼容请求中的 `user` 字段通常表示终端用户，而不是单次对话会话；若用它查询最近路由记录，会把同一用户的多个新会话错误粘到旧模型或旧提供商，既破坏负载均衡，也可能降低真实 prompt cache 命中率。
- 解决方法：模型映射与提供商候选选择只允许使用 `session_id`、`conversation_id`、`thread_id`、`session`、`conversation_key` 等明确会话字段查询近期会话路由；没有明确会话字段时按新会话处理，只做健康度、负载、权重和路由得分分发。
- 复用价值：后续凡是实现路由粘性、会话回放、缓存命中优化或请求归因，都必须区分“用户身份”和“会话身份”，禁止把用户级标识升级为会话级粘性依据。

## 2026-06-05：模型映射解析阶段不能丢失当前请求能力约束

- 问题：代理主链路会根据请求内容识别是否需要视觉、流式、工具调用、图片生成、原生 Chat 或原生 Responses 能力，但模型映射解析内部在检查每个目标模型候选时把这些能力参数固定传为 `False`。这会导致映射目标选择只证明“目标模型有路由候选”，不能证明“目标模型能承载本次请求”，后续可能先选中不支持当前能力的目标，再由二次路由失败或上游失败兜底。
- 解决方法：模型映射解析调用 `RouterService.order_candidates` 与 `diagnose_candidate_unavailability` 时必须透传调用方传入的能力约束；若规范要求映射阶段不做能力一致性拦截，也必须至少在 trace 中清晰区分“映射粗选”和“最终能力路由筛选”，避免测试接口与日志把粗选误展示为最终可用。
- 复用价值：后续维护模型别名、多目标映射、端点互转或多模态能力时，必须保证“映射选择”和“最终路由”共享同一套请求能力上下文，避免能力缺失被延迟到上游失败才暴露。

## 2026-06-05：健康检查的部分端点失败不能累计成熔断不可用

- 问题：部分 OpenAI 兼容提供商只支持 `/v1/chat/completions`，不支持 `/v1/responses`；若健康检查把“Chat 成功但 Responses 失败”的 `degraded` 结果继续累计到失败次数，自动熔断会在多次测试后把仍可用模型升级为 `unhealthy/open`，导致提供商整体可用性误显示为“全部不可用”。
- 解决方法：健康检查中 `degraded` 必须表示仍存在可用端点，应保持模型熔断关闭并清零健康检查失败累计；只有所有探针均不可用的 `unhealthy` 才允许打开熔断。前端测试反馈也必须按 `healthy/degraded/unhealthy` 展示可用性，不能只用 `success=false` 把部分可用显示成失败。
- 复用价值：后续新增端点、能力探针或供应商兼容层时，必须区分“全端点验收成功”“至少一个正式端点可路由”和“完全不可用”，避免把能力缺失、端点差异或适配失败误升级成渠道熔断。

## 2026-06-05：模型映射日志展示不能只保留合成模型名

- 问题：`request_logs` 已分别落库 `requested_model` 与 `model_name`，但列表、详情或 CSV 若只展示 `display_model`/`model` 合成值，排查时仍需要再打开原始 JSON 才能确认调用方请求的原始模型和实际路由的映射模型。
- 解决方法：所有面向排障的日志列表、详情页和导出结果必须显式区分原始模型与实际模型；合成展示值只能作为兼容或摘要字段保留，不能替代两列原始字段。
- 复用价值：后续新增端点 fallback、协议适配、模型别名、多目标映射等会改写内部请求目标的功能时，日志展示必须同时保留“调用方视角”和“内部转发视角”，避免观测链路再次把两者混成一个字段。

## 2026-06-05：异步日志队列落库必须容忍历史外键脏数据

- 问题：Redis 请求日志队列可能残留旧压测或回归数据，其中 `provider_id` 指向已经删除的提供商；后台 worker 批量落库时单条外键失败会导致整批回滚，并在每次启动后反复报错。
- 解决方法：请求日志队列消费落库前必须校验可为空外键是否仍然存在；若 `provider_id` 已失效，应置空外键但保留 `provider_name`、模型名、trace、请求路径等排障上下文。
- 复用价值：后续凡是可靠队列异步写入带外键的观测数据，都必须考虑队列滞后期间业务对象被删除的情况，避免历史观测数据阻塞正式日志消费。

## 2026-06-05：主策略收敛后前端不能继续暴露旧策略枚举

- 问题：路由和模型映射后端主策略已收敛为健康优先，但前端仍把历史兼容字段 `route_mode`、`strategy` 渲染为多选下拉，导致用户看到手动优先、故障切换、权重分流、会话粘滞、优先级等旧策略并误以为仍可切换主策略。
- 解决方法：保留历史字段用于数据和接口兼容，但用户可见策略统一显示为“健康优先”；新增、编辑、批量更新和模板提交时固定提交兼容主策略值，旧枚举仅在内部标签映射中兜底为“健康优先”。
- 复用价值：后续任何策略收敛或枚举降级为兼容字段时，必须同步检查模板、动态 JS、详情页、列表页、搜索占位符和文档口径，避免“后端已统一、前端仍多选”的漂移。

## 2026-06-05：服务端模板首屏与前端刷新接口不能分叉计算同一数据

- 问题：首页和提供商页同时存在服务端模板首屏数据与前端异步刷新数据，但首屏模板直接查表或局部计算，刷新接口通过服务层序列化/汇总计算；当能力字段、价格字段、稳定性指标或默认提供商展示口径变化时，页面会出现刷新前后跳数、跨页面名称/ID 不一致或摘要统计与列表状态不一致。
- 解决方法：同一业务数据必须先抽成服务层唯一序列化或汇总函数，页面首屏和 `/api/*` 刷新接口共同复用；前端只做展示格式化，不再维护第二套业务统计逻辑。确需保留兼容字段时，新增统一字段供界面使用，旧字段只做兼容兜底。
- 复用价值：后续新增仪表盘、详情页、列表页、用户端页面或壳内导航页面时，必须先确认首屏模板、异步 API、导出和详情入口是否共享同一口径，避免“页面刚打开是一组数，刷新后变成另一组数”的一致性问题。

## 2026-06-05：服务层脚本验证前必须确保本地数据库结构已初始化

- 问题：直接在脚本中调用 ProviderService.list_provider_dicts 等 ORM 服务时，若本地数据库尚未执行应用初始化补列，可能因缺少 providers.max_rpm 等迁移列而报错；这类错误与当前业务逻辑无关，会干扰验证结论。
- 解决方法：需要通过服务层读取 provider、模型或路由数据前，必须先确认应用初始化或独立迁移已执行；若只验证某个纯函数或统计口径，可使用实际表中已存在字段构造最小输入，避免把数据库结构缺口误判为业务逻辑失败。
- 复用价值：后续维护提供商容量字段、模型挂载统计、路由候选缓存或后台页面汇总时，能快速区分 服务逻辑问题和本地库结构未初始化问题。
## 2026-06-05：脚本从 `scripts/` 目录直接运行时无法导入 `app`

- 问题：直接执行 `.\.venv\Scripts\python.exe scripts\xxx.py` 时，Python 会优先把 `scripts/` 作为导入根，可能出现 `ModuleNotFoundError: No module named 'app'`。
- 解决方法：需要独立运行的 `scripts/*.py` 应在导入 `app` 前把项目根目录加入 `sys.path`，例如通过 `Path(__file__).resolve().parents[1]` 插入 `sys.path`。
- 复用提醒：本项目命令在 PowerShell 7 中执行，临时 Python 片段禁止使用 Bash heredoc `python - <<'PY'`，应使用 PowerShell here-string：`@' ... '@ | .\.venv\Scripts\python.exe -`。

## 2026-06-05：Stateful Responses 的 `encrypted_content` 不能跨模型映射或故障转移

- 问题：`/v1/responses` 请求中携带来自上一次响应的 `encrypted_content` 或 `previous_response_id` 时，该状态通常绑定到原始上游模型或上游账号；如果代理把请求从源模型别名映射到不同目标模型，或在映射目标失败后继续换目标模型，上游会返回 `Could not decrypt the provided encrypted_content`。
- 解决方法：检测到 `previous_response_id` 或任意嵌套 `encrypted_content` 时，禁止端点互转、禁止模型映射到不同目标模型、禁止模型映射故障转移更换目标；无法确认原始模型绑定时返回明确错误，要求调用方使用原始模型继续请求或移除状态上下文重新开始。
- 复用价值：后续处理 Responses 状态续写、reasoning 加密内容、模型别名、多目标映射或上游故障转移时，必须先判断状态上下文是否可跨目标复用，禁止把普通无状态文本请求的容灾策略直接套到 stateful 请求上。

## 2026-06-05：模型映射阶段必须透传当前请求能力约束

- 问题：模型映射解析阶段如果只按“目标模型有任意路由候选”判断可用，而不带入本次请求是否需要流式、工具、视觉、图片生成、原生 Chat 或原生 Responses 能力，就会选中不支持当前能力的目标模型；后续二次路由再失败或继续故障转移，会让日志呈现“先选错、再换错”的连锁问题。
- 解决方法：模型映射调用候选排序和不可用诊断时必须透传当前请求的全部能力约束，并在 trace 中记录 `capability_checks_skipped=false` 与能力要求摘要，确保映射目标本身能承载当前请求。
- 复用价值：后续维护模型别名、能力矩阵、健康检查、路由诊断或接入自检时，必须保证“映射选择”和“最终路由”使用同一能力上下文，避免能力缺失延迟到上游错误才暴露。

## 2026-06-05：Stateful Responses 容灾必须先复用同会话上次成功实际目标

- 问题：`/v1/responses` 携带 `previous_response_id` 或 `encrypted_content` 时，状态通常依赖上一次实际目标模型和提供商渠道；若完全禁止映射故障转移，会在上次目标临时不可用时失去容灾能力；若不优先回到上次实际目标，又容易触发解密失败。
- 解决方法：stateful Responses 必须先用明确会话标识查找最近一次成功路由，并在硬约束筛选后优先选择该实际模型和提供商；只有该目标不可用或发生可恢复上游失败时，才允许继续按健康优先候选重试。端点互转仍禁止，trace 必须记录 stateful 锁定和故障转移。
- 复用价值：后续维护模型别名、多目标映射、上游容灾或 prompt cache 粘性时，需要区分“优先保持状态绑定”和“不可用后容灾”，避免把 stateful 请求简单处理成完全禁止切换或无状态任意切换。

## 2026-06-05：PowerShell 7 命令嵌套时必须先处理外层解析

- 问题：在 `pwsh -Command "..."` 中直接写包含 `|` 的正则、`$LASTEXITCODE`、复杂引号或 Bash 风格语法，外层 PowerShell 会先解析这些字符，导致命令被拆段、变量被提前展开或参数顺序被误用，表现为“命令不兼容”“某个词被当成 cmdlet”“结果不是预期摘要”等错误。
- 解决方法：复杂命令优先拆成短命令执行；正则参数用单引号包裹；需要在双引号外层中保留 PowerShell 变量时必须转义 `$` 或改用脚本块/单引号；临时 Python 代码使用 PowerShell here-string；Git 命令遵守参数顺序，例如 `git diff --stat -- <paths>`、`git diff --check -- <paths>`。
- 复用价值：后续在 Windows + PowerShell 7 环境执行检索、验证、Git diff、内联脚本和条件退出时，必须先确认“外层 shell 解析”和“目标命令解析”不会互相干扰，避免把 shell 语法问题误判为项目代码问题。

## 2026-06-05：MiMo Chat 兼容探针不能继续使用 `max_tokens`

- 问题：小米 MiMo 官方 OpenAI 兼容 Chat 协议使用 `max_completion_tokens` 控制输出长度；若项目健康探针或代理适配层继续向 MiMo 上游发送 `max_tokens`，可能导致 MiMo 文本模型被误判为不可用，或普通文本调用被上游按非法字段拒绝。
- 解决方法：在 MiMo 模型或小米 MiMo 官方 Base URL 的 `/chat/completions` 上游请求中，将 `max_tokens` 映射为 `max_completion_tokens`；其它 OpenAI 兼容供应商继续保持原始字段透传，避免无差别改写协议。
- 复用价值：后续接入 OpenAI 兼容但字段存在供应商差异的模型时，必须优先把差异限制在供应商识别后的最小适配范围内，并补充回归用例证明不会影响其它通用 OpenAI 兼容提供商。

## 2026-06-05：提供商协议能力不能只靠模型挂载字段表达

- 问题：同一模型挂载可能声明支持 Chat 或 Responses，但具体提供商本身可能只开放其中一种 OpenAI 兼容协议；若路由只看 `provider_models.supports_*`，会把请求发到协议不匹配的提供商，外部表现为目标模型“明明存在但请求失败”。
- 解决方法：提供商维护独立 `protocol_type` 字段，并在候选筛选、诊断和健康探针中同时检查提供商协议与模型挂载端点能力；协议字段变更后必须失效 provider/runtime 路由缓存。
- 复用价值：后续新增供应商、模型能力同步、健康检查或接入自检时，必须区分“提供商协议能力”和“单模型挂载能力”，避免把供应商级协议限制误归因到模型健康或模型不存在。

## 2026-06-05：工具和图像理解探针必须按端点分开缓存

- 问题：Chat Completions 与 Responses 已按独立原生端点路由，但工具调用和图像理解探针若仍只记录模型级 `tools` / `vision` 结果，会出现 Responses 探针成功却放行 Chat 工具请求，或 Chat 图像探针失败误挡 Responses 图像请求的问题。
- 解决方法：健康检查对工具调用和图像理解分别写入 `tools_chat_completions`、`tools_responses`、`vision_chat_completions`、`vision_responses` 端点级能力结果；路由过滤有明确请求入口时只读取对应端点 key，避免旧 `tools` / `vision` 模型级缓存继续影响端点独立判断。
- 复用价值：后续新增能力探针、模型能力同步或路由诊断时，必须判断该能力是否受端点协议影响；受影响能力不能只用模型级缓存代表两个端点。

## 2026-06-05：工具字段不能触发模型级硬过滤导致 404

- 问题：部分客户端会默认发送 `tools: []`、`tool_choice: "auto"` 或 `parallel_tool_calls: true`；若代理只按字段存在判断“工具请求”，再用模型级 `supports_tools` 或工具探针结果硬过滤候选，就会在请求到达上游前返回 `model_tools_not_available`，即使目标模型和请求入口本身可用。
- 解决方法：工具语义识别必须只认真实可调用的工具定义、强制工具选择或历史工具上下文；工具调用作为 Chat Completions 与 Responses 的标准协议能力，不再使用模型级 `supports_tools` 或工具探针失败阻断正式路由。端点原生协议、权限、容量、视觉、图片生成和流式仍按既有硬约束执行。
- 复用价值：后续维护 OpenAI 兼容客户端适配、路由诊断、模型映射或健康检查时，必须区分“请求字段存在”“真实工具调用意图”和“模型级能力展示”，避免把默认兼容字段误判成能力不支持。

## 2026-06-05：模型名前缀不能决定 Chat/Responses 协议支持

- 问题：旧实现按模型名前缀把 DeepSeek、GLM、Kimi、MiMo 等模型直接标记为 `supports_responses=false`，模型目录同步又把该值传播到 `provider_models` 和 `model_catalogs`，导致未真实探测过的 `/v1/responses` 请求在路由层被误判为 `responses_not_supported` 并返回 404。
- 解决方法：模型级 Chat Completions / Responses 协议默认均为支持；新增或未探测模型不因静态字段被拒绝；健康检查必须分别记录 `chat_completions`、`responses`、`chat_completions_stream`、`responses_stream` 等端点探针结果，路由只在已有探针明确失败或提供商 `protocol_type` 明确不支持时收紧。
- 复用价值：后续新增模型家族、提供商或能力矩阵字段时，协议可用性必须来自端点级探测或明确配置，禁止把模型名猜测结果写入路由硬过滤条件。

## 2026-06-06：Responses-only 客户端访问 Chat-only 国内模型必须显式走单向适配

- 问题：部分 Agent 只能调用 OpenAI `/v1/responses`，但国内主流 OpenAI 兼容模型常只开放 `/v1/chat/completions`；若把这类请求伪装成原生 Responses 路由，会与端点独立实现规则冲突，也容易在 `previous_response_id`、工具调用和流式事件上丢失语义。
- 解决方法：新增默认关闭的 Responses→Chat 单向兼容适配层；开启后由网关维护 response_id 到 Chat messages 历史的映射，按 Responses 输入结构构造 Chat messages，透传自定义函数工具，拦截或代理内置工具，并把 Chat 非流式/流式结果包装回 Responses 响应。
- 复用价值：后续接入只支持单一协议的上游模型时，必须明确区分“原生端点支持”和“兼容适配模式”，避免把协议转换混入默认路由并污染健康检查、能力声明和排障日志。

## 2026-06-06：B 端表单按钮不能默认满宽或保留过大空白

- 问题：后台表单、筛选区、表格行内操作和批量操作区如果让按钮默认 block 满宽、设置过大 `min-width` 或使用超大横向 `padding`，会导致按钮文字两侧出现大量空白，输入框与按钮关系松散，尤其在“输入框 + 操作按钮”场景中破坏扫读效率。
- 解决方法：按钮默认使用 `inline-flex`、`width: fit-content`、紧凑 `padding-inline` 和可覆盖的 `min-width: unset`；输入框与同组按钮使用 `display:flex` 同行布局，输入框 `flex:1` 占剩余空间，按钮按内容自然收缩；仅移动端或明确全宽提交场景允许堆叠或全宽。
- 复用价值：后续维护后台筛选栏、表单行内操作、批量操作栏和表格运维按钮时，可直接复用该处理方式，避免反复出现 AI 生成按钮过宽、独占整行或布局松散的问题。
## 2026-06-06 对外端点协议不匹配错误不能泛化

- 问题：`/v1/responses` 请求命中只支持 Chat Completions 的提供商，或 `/v1/chat/completions` 请求命中只支持 Responses 的提供商时，如果只返回 `model_not_available`、`route_unavailable` 等泛化错误，调用方无法判断是模型不存在、授权不足、容量耗尽还是端点协议不支持。
- 解决方法：路由候选耗尽时必须保留 `RouterService.diagnose_candidate_unavailability()` 的 `reason_counts` 和 `samples`，并在对外错误 `detail` 中结构化返回 `requested_endpoint`、`required_endpoint`、`required_protocol`、`missing_capability`、`primary_reason`、`reason_details` 和 `diagnostic_samples`。
- 适用范围：对外 `/v1/*` 路由、模型映射、端点协议能力检查和后续新增能力筛选错误。

## 2026-06-06：单模型即时测试不能沿用批量健康检查的双端点串行策略

- 问题：模型配置页单模型测试如果沿用批量健康检查策略，会按文本非流式、文本流式、图片理解、工具调用等阶段串行运行，并在每个阶段同时探测 Chat Completions 与 Responses；上游失败时还可能重试，最终把多个超时串起来，人工点击后长时间无结果。
- 解决方法：单模型即时测试必须使用短探针、单端点、无重试和 10 秒总预算；端点选择来自模型挂载级协议配置，优先测 `responses`，仅在该挂载不支持 `responses` 时测 `chat_completions`；批量健康检查可继续保留更完整的端点观测策略。
- 复用价值：后续新增模型测试、渠道自检、能力按钮或前端即时验活入口时，应先区分“人工即时反馈”和“后台批量观测”，避免把完整巡检策略复用到需要快速返回的交互链路。

## 2026-06-06 运维监控分区异常导致接口整体失败

- 问题：`/api/metrics/system` 在数据库可连通但某个监控分区查询字段缺失或异常时，会因该分区异常直接返回 500，导致运维监控页面整体不可用。
- 影响：数据库迁移未完全执行、局部监控 SQL 口径变更或单个数据源异常时，会放大为整页失败，无法查看其它仍可用的运行指标。
- 解决方法：监控采集必须按分区隔离执行；单一分区异常时回滚当前数据库会话，返回对应空口径，并在响应 `section_errors` 中暴露错误摘要，同时将整体状态降级为 `degraded`。
- 复用规则：后续新增监控分区时必须接入同一分区降级机制，禁止把非核心监控异常直接抛到接口顶层。


- 问题：本地启动连接 PostgreSQL 时，启动迁移向 `provider_models` 新增 `content_probe_last_passed_at` / `content_probe_last_failed_at` 使用了 `DATETIME`，PostgreSQL 报 `type "datetime" does not exist`，导致应用启动失败。

## 2026-06-07：新增 app_settings 字段后测试辅助函数不能强依赖已迁移数据库

- 问题：为 `AppSetting` 新增 `content_guard_rules_json` 后，健康探针辅助函数直接调用 `SettingService.get_cached()`，在测试环境连接到尚未执行新列迁移的 PostgreSQL 时，ORM 查询会因列不存在而失败。
- 解决方法：正式运行仍通过启动迁移和 SQL 迁移补齐字段；测试/探针辅助读取非关键配置时必须提供降级路径，读取失败时回退模块默认规则，避免 schema 过渡期阻断与配置无关的回归检查。
- 复用价值：后续给 `app_settings` 增加新字段时，应同时补模型、默认值、启动迁移、SQL 迁移，并评估热路径或测试辅助函数是否需要 schema-stale 降级。

## 2026-06-07 - 流式预检禁止用 `asyncio.wait_for` 直接取消上游迭代器

问题：在 SSE 内容完整性预检中，如果为了控制 300ms 延迟直接对同一个上游 `aiter_bytes().__anext__()` 使用 `asyncio.wait_for`，超时会取消正在进行的读取操作，可能破坏后续继续读取同一上游流的状态，导致流式响应异常中断或日志无法反映真实原因。

解决方法：预检必须把上游读取包装为显式 `asyncio.Task`，超时未完成时不取消该任务，而是把 pending read task 交给正式流式生成器继续等待；只有在预检已经判定高风险并决定放弃当前 provider 时，才允许取消并回收该 pending task。后续凡是对流式上游做首段探测、协议探测或低延迟采样，都必须避免在仍计划复用同一上游连接时取消正在读取的 `__anext__()`。

复用价值：该规则适用于所有“先预读再透传”的 SSE、文件流、图片流或增量事件流链路，可避免为控制延迟而破坏流状态。

## 2026-06-07：模板新增字段必须同步服务层唯一序列化口径

- 问题：用户详情页模板读取 `account_summary.cost_limit_total`、`cost_limit_daily`、`cost_limit_monthly` 等限额字段，但 `UserQuotaService.serialize_policy()` 只返回余额和使用量字段，导致 `/users/{id}` 渲染时出现 Jinja `UndefinedError` 并返回 500。
- 解决方法：将请求、Token、费用三类限额字段统一补入 `UserQuotaService.serialize_policy()`，让用户端概览、管理员用户详情页和后续 API 复用同一 `account_summary` 口径。
- 复用价值：后续模板或前端页面新增字段时，必须优先检查对应服务层序列化函数是否已经提供该字段；禁止让模板直接假设 ORM 字段已存在于聚合 dict 中，避免空数据 smoke test 或生产页面因字段缺口 500。

## 2026-06-07：协议回归测试桩必须保持提供商级与挂载级协议一致

- 问题：协议路由回归测试中，测试桩把提供商声明为 Responses 或双协议，但对应挂载模型却设置为 `supports_responses=false`，与当前“提供商-模型挂载级协议为权威来源”的规则冲突，导致测试期望和真实路由硬约束不一致。
- 解决方法：构造协议测试数据时必须同时设置提供商 `protocol_type` 和挂载模型 `supports_chat_completions` / `supports_responses`，并让两者表达同一协议能力；若测试目标是提供商级协议过滤，不得把挂载级能力设置成相反值。
- 复用价值：后续维护协议矩阵、健康检查、模型配置和路由诊断回归时，测试桩必须体现“提供商级支持 + 挂载级支持”双重硬约束，避免为通过旧断言而放宽真实路由。

## 2026-06-07：检测/测试入口必须复用统一健康测试口径

- 问题：项目内同时存在提供商健康测试、模型目录测试、挂载模型旧测试、内容防护探测、用户接入自测、Playground 调试和压测等多类“测试/检测”入口。若新增或修改入口时只局部调用 `HealthService.check_provider_model()` 或直接走代理链路，容易遗漏 `app/utils/test_features.py` 的功能选择标准、单端点测试策略、内容防护结果汇总和健康运行日志，造成不同页面测试结果不一致。
- 解决方法：凡属于后台健康测试语义的入口，必须统一经过 `normalize_test_features()`、`phase_keys_from_test_features()` 和 `HealthService` 的同一测试配置；单模型或即时测试必须显式使用单端点策略，优先 `responses`，不支持时再用 `chat/completions`。非健康语义的用户自测、Playground、压测接口必须在代码和前端文案中明确其用途，并在需要展示内容合法性时复用内容防护服务返回结构。
- 复用价值：后续新增测试按钮、批量测试、模型挂载测试、接入自检或内容防护探测时，可直接按该规则判断应接入健康模块还是代理/压测模块，避免接口数量增加后测试能力、内容防护和日志口径再次分裂。

## 2026-06-07：跨服务抽取探针能力时避免顶层导入代理主链路

- 问题：将内容完整性探针从 `HealthService` 抽取为独立服务后，如果新服务在模块顶层导入 `ProxyService`，会形成 `ContentGuardProbeService -> ProxyService -> RouterService -> ModelCatalogService -> ContentGuardProbeService` 的循环导入，导致健康服务、路由服务或回归脚本启动即失败。
- 解决方法：探针服务可以复用代理链路的上游请求辅助函数，但必须对 `ProxyService`、`StreamTimeoutPolicy` 等代理主链路对象采用方法内延迟导入；轻量且无回环风险的模型、配置、规则服务才允许顶层导入。
- 复用价值：后续从健康检查、内容防护、路由或代理服务中抽取共享能力时，必须先检查导入链，尤其禁止新共享服务在顶层依赖代理主链路后又被路由/目录/健康模块顶层引用，避免架构拆分后产生启动期循环导入。

## 2026-06-07：PowerShell 7 外层命令包含变量时必须用脚本块隔离

- 问题：通过 `pwsh -NoLogo -NoProfile -Command "..."` 执行复杂命令时，外层 PowerShell 会先解析 `$port`、`$python`、`$_.Exception` 等变量，导致内层命令变成 `=8019`、`=.Exception.Response` 之类的破坏性文本并触发解析错误。
- 解决方法：凡 `-Command` 内容包含 `$`、`$_`、字符串插值、异常处理或多语句逻辑时，必须优先使用单引号包裹脚本块，例如 `pwsh -NoLogo -NoProfile -Command '& { $port=8019; ... }'`；简单正则或 JSON 参数也必须按项目规范拆短、转义或改用脚本块。
- 复用价值：后续启动服务、HTTP 冒烟、端口探测、临时脚本和复杂 PowerShell 命令都可复用该写法，避免外层 shell 提前展开变量造成误判或命令失败。

## 2026-06-07 上线前外部代理审查问题记录

### 问题：PowerShell 7 外层双引号导致 $ 变量提前展开

- 现象：在 pwsh -Command "... foreach ( in ...) ..." 这类命令中，外层 shell 会先处理 $n，导致 PowerShell 脚本进入解析器时变量名丢失，出现 Missing variable name after foreach。
- 影响：会导致规范读取、批量检查或临时脚本执行失败，且容易误判为目标命令本身错误。
- 解决方法：复杂命令优先使用单引号包裹 -Command 内容；包含 $、JSON、管道或嵌套引号时拆短命令，或使用 PowerShell here-string 管道到项目虚拟环境。

### 问题：Provider 返回层不得泄露上游明文 API Key

- 现象：Provider 运行时对象需要持有上游 API Key，但管理接口、审计日志和前端状态只需要展示掩码；若序列化函数直接返回 provider.api_key，会把上游凭证暴露到浏览器、接口响应或审计 before/after。
- 影响：生产上线后可能造成后端 Provider 凭证泄露，不符合敏感凭证禁止出现在配置或日志中的要求。
- 解决方法：运行时调用仍读取数据库对象的内部字段；所有 provider_to_dict 等对外序列化结果只返回掩码值，并另行保留 pi_key_masked 字段供前端展示。后续若引入 Vault/KMS 或字段级加密，应保持对外序列化层继续只返回掩码。

### 问题：跨 Provider 管理接口失败尾部分支必须覆盖回归

- 现象：响应/聊天管理类请求在全部 provider 均失败后进入尾部分支时，若引用未定义的 trace 变量，会把原本应返回 4xx/5xx OpenAI 兼容错误的问题升级为内部 500。
- 影响：只在异常路径触发，常规成功回归不容易覆盖，但上线后会影响客户端可读错误和审计追踪。
- 解决方法：失败尾部分支必须使用已维护的 	race 列表落库；新增或修改跨 provider 循环时，需要覆盖“全部候选失败后写日志并返回错误”的回归场景。

### 问题：API Key 默认可信策略与 Provider 默认信任等级不一致会导致新配置默认不可用

- 现象：新 Provider 默认信任等级为 standard，而新 API Key 若默认 	rusted_providers_only=true，路由会在上游调用前筛掉所有标准提供商，返回 model_not_available。
- 影响：新部署或新用户刚配置提供商和 API Key 后，外部 /v1/* 请求无法命中 provider，表现为模型不可用，排查成本高。
- 解决方法：新 API Key 默认不强制可信 provider（	rusted_providers_only=false），但仍禁止低信任未授权和已阻断 provider 参与路由；如业务需要强制只走可信/官方 provider，应由管理员显式开启该开关。

### 问题：请求父日志未提交前异步写入子事件会触发外键失败

- 现象：
equest_logs 父记录创建后，若派生的 request 子事件先进入异步日志队列，后台 worker 可能在父事务提交前写入子事件表，导致 FOREIGN KEY constraint failed。
- 影响：主请求可能成功，但统一日志子事件丢失，监控、审计追踪和分阶段诊断不完整。
- 解决方法：从请求摘要派生、带
equest_log_id 外键的子事件必须与父
equest_logs 在同一数据库事务内写入；只有不依赖未提交父记录的独立事件才允许提前异步入队。

### 问题：独立后台 worker 必须消费所有生产队列

- 现象：生产 Web 进程可关闭后台 worker 并交给独立 worker 服务处理，但独立 worker 若只启动请求日志和 Token/计费队列，统一事件日志队列会无人消费。
- 影响：审计、资产、后台任务、异常等 typed logging 事件可能积压在 Redis，监控与追踪不完整。
- 解决方法：独立后台 worker 启动时必须同时启动 LoggingQueue、RequestLogQueueService 和 TokenUsageService，停止时按相反方向优雅关闭。

## 2026-06-07：新增后台页面必须同步前后端路由白名单

- 问题：后台新增独立页面后，如果只在页面路由、左侧导航和前端壳式路由白名单中加入路径，而遗漏 `UserAuthService.ADMIN_PORTAL_PREFIXES`，未登录用户访问该页并登录后会被后端角色回跳逻辑判定为不允许访问，最终回到管理员首页，表现为“页面打不开”。
- 解决方法：新增管理员后台页面时，必须同时检查服务端页面路由、前端 `ADMIN_ROUTE_PREFIXES`、基础模板导航入口和 `UserAuthService.ADMIN_PORTAL_PREFIXES` 是否一致；登录后 `next` 回跳路径也要覆盖新增页面。
- 复用价值：适用于所有后台独立页面、详情页和新模块入口，可避免页面本身存在但登录回跳或角色守卫把用户带回首页的问题。

## 2026-06-07：官方接口族路径判断必须覆盖详情子路径

- 问题：对外官方接口若同时提供列表路径和详情子路径，只按精确路径判断会漏掉子路径；例如 `/v1/models/{model}` 可能被端点白名单、计费、Token 补算或统计查询误当成正式模型调用。
- 解决方法：为接口族维护统一路径判断函数和 SQL 过滤表达式；涉及鉴权、限流、计费、日志补全、用量统计和监控积压时统一复用该口径，禁止各模块散落硬编码精确路径。
- 复用价值：适用于 `/v1/models`、`/v1/files`、`/v1/chat/completions/*`、`/v1/responses/*` 等官方兼容接口族，能减少权限与统计口径不一致。

## 2026-06-07：模型映射阶段不得替代提供商挂载级能力判断

- 问题：同一模型在不同提供商下可能支持不同端点协议、流式、视觉、工具调用、图片生成和内容可信状态；若模型映射阶段提前调用提供商候选路由做能力、容量、熔断或内容完整性筛选，会把提供商挂载级差异误判为目标模型不可用，并导致映射选择和后续 failover 边界混乱。
- 解决方法：模型映射阶段只处理源模型到目标模型的轻量选择，保留映射启停、目标模型存在/启停、API Key 模型白名单、同会话近期成功目标和配置顺序；端点协议、能力、容量、熔断、内容完整性与可信策略统一在选定目标模型后的提供商候选选择阶段判断。
- 复用价值：后续维护模型映射、提供商挂载矩阵、路由诊断和测试接口时，应先区分“模型级映射选择”和“提供商级候选选择”，避免把 provider 级状态提前固化为模型级判断。

## 2026-06-08：移除 ORM 权重字段后要保留旧属性兼容层

- 问题：`Provider` 和 `ProviderModel` 的 `weight` 字段已从 ORM 中移除后，仍有运行中的页面、缓存或旧代码路径按 `obj.weight` 读取对象，导致 `AttributeError: 'ProviderModel' object has no attribute 'weight'`。
- 解决方法：删除数据库列只负责清理持久化结构，ORM 层需要同时提供只读/可写兼容别名，把旧 `weight` 属性映射到 `priority`；同时升级模型选项缓存 key，并在启动流程中主动失效依赖模型/提供商的运行缓存。
- 复用价值：后续任何“字段改名/字段迁移/旧配置下线”场景都可先补兼容别名，再逐步清理外部调用，避免热重载或旧缓存直接炸接口。

## 2026-06-08：流式 Token 计费日志必须显式区分上游 usage 与本地估算

- 问题：OpenAI Chat Completions 流式响应只有在上游请求携带 `stream_options.include_usage=true` 时才会在最终 chunk 返回完整 `usage`；若客户端断开、上游中断或未请求 usage，日志只能本地估算 prompt/output token，不能把估算值当作官方 usage 或缓存命中数。若日志中缺少来源字段，后续成本、缓存读写和审计会混淆“上游精确返回”和“本地补算”。
- 解决方法：代理层在日志开关开启且请求为 Chat/Completions 流式时，发送上游请求前补充内部 `include_usage` 标记，并只把标准 `stream_options.include_usage=true` 发给上游；`request_logs` 必须落库 `token_source`、`upstream_usage_missing` 和 `usage_details_json`，缓存读写、reasoning/audio/prediction token 只从上游 `usage` 显式字段提取。本地估算仅用于补全总 token，并标记为 `estimated`。
- 复用价值：后续新增 Provider 适配、流式协议转换、Token 补算、计费或缓存统计时，应优先保留完整上游 `usage`，并用来源字段区分精确值、估算值和缺失值，避免成本和缓存指标失真。

## 2026-06-08：PowerShell 7 外层双引号会提前解析 `$` 变量

- 问题：在 `pwsh -NoLogo -NoProfile -Command "..."` 中直接编写包含 `$out`、`$p`、`$_.Exception` 等变量的脚本时，外层 shell 会提前解析变量，导致命令变成 `=Start-Process`、`.Exception.Message` 等无效片段。
- 解决方法：包含 PowerShell 变量、管道、JSON 或多层引号的复杂命令必须使用单引号包裹 `-Command '...'`，或拆成更短命令；若需要在双引号外层中保留变量，必须显式转义 `$`。
- 复用价值：适用于本项目所有本地验证、启动服务、接口探测和临时脚本执行，可避免误判为应用启动失败或接口异常。

## 2026-06-08：统计正常但对象目录为空时先查列表字段与单行渲染

- 问题：页面摘要指标来自 `summary`，对象目录来自列表字段；若概览接口列表字段缺失、被旧缓存改名，或单个对象字段渲染异常，可能出现“总数正常但列表为空”。
- 解决方法：前端读取概览数据时必须先归一化列表字段，并在 `summary.provider_count > 0` 但列表为空时回退请求独立列表接口；列表渲染应逐行容错，禁止单个对象异常导致整张表空白。
- 复用价值：适用于提供商、模型、API Key、日志等所有“摘要 + 列表”后台页面，可降低接口字段演进或脏数据导致的整页空表风险。

## 2026-06-08：系统监控分区快照失败必须局部降级

- 问题：`SystemMetricsService.collect(db=None, ...)` 或某个监控分区采集失败时，异常处理若无条件执行 `db.rollback()`，会把原始分区异常升级为 `AttributeError`，导致监控整体失败。
- 解决方法：监控快照的 `_safe_snapshot` 只能在数据库会话存在时回滚；单个分区失败必须写入 `section_errors` 并返回对应空快照，保证其它 CPU、内存、Redis、流量和 Provider 指标继续返回。
- 复用价值：后续新增监控分区、低频采集项或可选依赖时，应保持“局部失败、整体降级”的模式，避免非核心指标拖垮监控接口。

## 2026-06-08：模型简写挂载必须保留 Chat Completions 能力

- 问题：通过 Provider 的 `models: ["glm-5.1"]` 简写创建模型挂载时，若默认只写入 `supports_responses=true`、`supports_chat_completions=false`，`/v1/chat/completions` 下带 `tools` 的请求会被路由诊断为 `chat_not_supported`，即使模型名已推断支持工具调用。
- 解决方法：模型简写生成的默认挂载配置必须使用双协议能力；只有管理员显式提交 `model_configs` 时，才按显式字段限制 Chat Completions 或 Responses。
- 复用价值：适用于批量导入、模型发现、Provider 快速创建和 OpenAI 兼容客户端接入，避免“模型存在但标准端点不可用”的上线问题。

## 2026-06-08：自动隔离 Provider 必须同步关闭低信任路由入口

- 问题：内容完整性自动隔离只更新 `trust_level`、`content_integrity_status` 和熔断状态时，历史数据中已打开的低信任路由入口可能继续保留，重复执行隔离也可能不触发状态修正。
- 解决方法：自动隔离的变更判断和更新字段都必须包含 `low_trust_route_enabled=false`；读取旧对象属性时要使用兼容默认值，避免旧实例缺字段导致异常。
- 复用价值：后续新增 Provider 安全状态字段或隔离动作时，应同时纳入“是否需要修正”的判断与实际更新，防止状态半收敛。

## 2026-06-08：路由和映射结果对象重构必须保留兼容字段

- 问题：`RouteCandidate.dynamic_weight`、`ModelMappingResolution.strategy`、`ModelMappingService._order_targets(strategy=..., sticky_key=...)` 等旧调用面被移除后，回归脚本和上层 trace 构造会直接 TypeError，影响路由策略验证。
- 解决方法：重构内部排序策略时保留轻量兼容字段或兼容参数；实际排序可继续使用当前健康优先、近期会话复用和配置顺序规则，但旧调用面不能无过渡删除。
- 复用价值：适用于路由、模型映射、Provider 候选、健康探针等核心链路重构，能降低上线前回归和外部调用适配风险。

## 2026-06-08：日志中心模板与 JS 契约漂移会导致整页初始化失败

- 问题：日志中心新增或拆分筛选项、类型化日志按钮、详情弹窗节点时，若模板存在节点但 JS 漏声明变量，或 JS 仍读取模板已删除的节点，页面初始化会触发 `ReferenceError` 或空指针，表现为类型化日志 Tab 和按钮全部点不动。
- 解决方法：日志中心变更必须同步检查模板 ID 与 `initLogs` / `initUserLogs` 的必需节点列表；管理端类型化日志能力只能保留在管理端初始化逻辑中，用户端请求日志页不得混入未渲染的类型化日志 DOM 依赖。详情数据应存放在内存数组并通过行索引读取，禁止把整条日志 JSON 塞入 `dataset`。
- 复用价值：适用于所有模板驱动页面的前端重构，尤其是同名页面在管理端和用户端能力不一致时，可避免 DOM 契约漂移造成整页功能失效。

## 2026-06-08：内容防护短链检测不能被普通 URL 白名单抵消

- 问题：内容防护规则若只在“未被白名单允许的域名”中检测短链，`bit.ly`、`t.co` 等短链域名一旦被默认白名单、请求上下文或管理员配置放行，可信探针会出现“短链未被正确识别”的假阴性。
- 解决方法：短链导流应独立检查响应中出现的全部域名；普通外链白名单只影响异常外链规则，不应抵消短链专项风险识别。
- 复用价值：适用于内容防护规则、可信探针和外部渠道检测，后续新增二维码、社群、短链、跳转域名等专项风险时，应避免被通用放行规则提前吞掉。

## 2026-06-08：跨页面 JS 逻辑误植会中断当前页初始化

- 问题：提供商页 `initProviders()` 中误混入挂载矩阵页专属的编辑弹窗变量绑定，浏览器执行到未声明变量时触发 `ReferenceError`，导致后续 `loadProviders()` 不执行，表现为摘要指标正常但对象目录和可用率曲线为空。
- 解决方法：页面初始化函数只能绑定当前页面真实存在且已在本作用域声明的 DOM 契约；跨页面复用功能必须抽成共享函数，页面专属弹窗、筛选器和批量栏绑定应留在对应页面初始化函数内。
- 复用价值：适用于所有模板驱动后台页面，尤其是提供商页、挂载矩阵页、模型页这类字段和操作相近的页面，可避免同名变量或相似 UI 逻辑误植造成整页空白。

## 2026-06-08：表格更多菜单不要依赖原生 details 默认切换

- 问题：提供商对象目录和模型配置页的“更多”菜单复用 `<details>/<summary>` 后，再叠加 fixed 浮层定位、动态表格重渲染和全局点击关闭逻辑，容易出现点击后菜单不展开或瞬间关闭。
- 解决方法：表格行内更多菜单应使用普通按钮作为触发器，由共享 `initActionMenus()` 显式维护 `.is-open`、`aria-expanded` 和 fixed 浮层位置；菜单项点击后再异步关闭其它菜单。
- 复用价值：适用于所有表格、卡片列表和矩阵视图中的更多操作入口，可避免原生 details 行为与自定义浮层/事件委托冲突。

## 2026-06-08：手动健康检测槽位必须在任务结束后释放

- 问题：提供商手动健康检测在启动前写入 `health-check-slot:*` 后只依赖 TTL 过期，若流式检测结束、异常或前端重试，后续点击会被固定 5 分钟拦截，表现为测试任务无法正常开始。
- 解决方法：手动检测槽位只能作为运行中并发保护；普通接口必须在 `finally` 中释放，流式接口必须在流式生成器结束后释放。缓存层需要支持精确单键失效，避免用前缀删除误伤相邻对象。
- 复用价值：适用于提供商、模型、内容防护、批量检测等所有后台手动任务入口，可避免把运行锁误做成不可控冷却。

## 2026-06-08：主路由候选构造必须保证模型筛选逻辑在 provider_model 循环内

- 问题：`RouterService._load_available_candidates_uncached()` 中若 `provider_model` 的能力、健康、内容完整性和 `candidates.append()` 逻辑因缩进漂移脱离 `for provider_model in provider.provider_models` 循环，真实 `/v1/*` 路由会得到空候选，而诊断链路可能仍显示存在可用挂载，进而触发长等待或无限重试。
- 解决方法：所有 provider_model 级筛选、打分和候选追加必须保持在 provider_model 循环体内；回归测试应直接构造启用提供商和启用挂载，断言 `_load_available_candidates_uncached()` 至少返回匹配候选，避免只测诊断函数。
- 复用价值：适用于路由候选、模型映射、能力筛选和内容完整性筛选等多层循环逻辑，可避免缩进漂移导致诊断链路与真实请求链路不一致。

## 2026-06-08：可信检测不能用本地规则自测替代上游实测

- 问题：外链广告识别探针若只把内置污染样本交给本地规则检测，只能证明本地规则可用，不能证明被测提供商模型的真实返回内容没有广告、外链或引流污染。
- 解决方法：可信检测中的外链广告识别必须真实请求被测大模型，再将模型返回内容交给本地内容防护规则扫描；流式污染检测也必须纳入可信状态判断，覆盖非流式探针无法发现的 SSE 事件污染和终止符异常。
- 复用价值：适用于内容防护、提供商可信度、外部渠道检测等所有可信判断场景，避免把“检测器自检”误当成“目标对象可信证明”。

## 2026-06-08：行内更多菜单点击生效但浮层可能被定位到视口外

- 问题：提供商对象目录和模型配置页的“更多”按钮点击后，事件、`.is-open` 和 `aria-expanded` 都已生效，但 fixed 菜单浮层会因外层布局/变换上下文产生坐标偏移，最终出现在视口外，用户表现为“按钮点不动”。认证模板若仍引用旧版 `app.js`，还会放大“代码已修但页面未更新”的排查噪音。
- 解决方法：共享 `initActionMenus()` 必须在打开菜单后基于触发器与浮层真实 `getBoundingClientRect()` 做视口夹取和偏移校正；修改 `app/static/` 执行脚本或样式后，`base.html` 与 `auth_base.html` 的静态资源版本必须保持同步。验证此类问题必须使用真实浏览器记录点击后菜单 DOM 状态和浮层坐标，不能只看事件绑定代码。
- 复用价值：适用于所有表格、卡片列表、矩阵视图的更多菜单、Tooltip、Popover 等浮层交互，可快速区分“点击事件没触发”和“浮层已打开但不可见”。

## 2026-06-08：计费链路不能只做余额大于零预检

- 问题：外部 API Key 请求进入前只判断归属账户可用余额是否大于 0，未按本次请求的模型、最大输出和价格口径做预估费用校验或余额冻结；若余额很小但请求成本很高，请求可能成功返回，后台计费再因余额不足失败，形成成功服务但未扣费。
- 解决方法：对外请求在进入上游前应按模型价格、请求 Token 估算和最大输出上限计算保守预估费用，并执行余额冻结或至少拒绝明显超过可用余额的请求；后台实际结算时按真实 usage 做差额扣减或解冻，失败路径必须有可重试且可追踪的补偿。
- 复用价值：适用于所有后付费/预付费 API Key 计费场景，可避免余额边界下的欠费、重复放行和后续补扣困难。

## 2026-06-08：零价快路径必须覆盖缓存写入价格

- 问题：Token/计费补全的零价格快路径只检查输入、输出和缓存读价格，未检查缓存写入价格；若模型阶梯价中输入、输出、缓存读均为 0，但 `cache_write_price_per_1k` 非 0，带缓存写入 Token 的成功请求会被直接标记为 `no_charge`。
- 解决方法：零价快路径必须复用正式计费价格解析结果，至少同时检查 `channel_price_cache_write_per_1k`；只要存在缓存写入 Token 且缓存写入价可能非零，就必须进入 `BillingService.compute_log_cost()` 正式计费分支。
- 复用价值：适用于 prompt cache、上下文缓存、缓存创建费等分项计费，避免优化路径绕过真实收费项。

## 2026-06-08：价格未解析成功不能永久按零费用 finalize

- 问题：成功请求在 `resolved_provider_model_id` 缺失、ProviderModel 不存在或价格未设置时，会得到 `price_unresolved` / `price_unset` 且总费用为 0；随后仍可能写入 `billing_finalized_at`，导致后续补齐价格或模型挂载后不会自动重算。
- 解决方法：价格缺失应进入独立的待处理或失败状态，保留重试和补偿入口；只有明确免费模型或管理员确认免扣费时，才允许最终标记为 `no_charge` 或完成计费。
- 复用价值：适用于新增模型、迁移价格表、导入提供商和日志补全任务，可防止配置短暂缺失造成永久漏扣。

## 2026-06-08：Web 进程关闭后台 worker 时仍必须可靠入队

- 问题：`RequestLogQueueService.enqueue()` 当前要求本进程存在活跃请求日志 worker 才返回成功；在 Web 进程设置 `ENABLE_BACKGROUND_WORKERS=false`、独立后台 worker 单独消费 Redis 队列的生产形态下，Web 进程会误判没有本地 worker 并回退同步写库，削弱主链路吞吐和可靠入队设计。
- 解决方法：入队判断应区分“本进程是否消费”和“Redis 队列是否可写”；Web 进程只要 Redis 可用且异步日志开关开启，就应允许可靠入队，由独立 worker 消费，并通过监控暴露队列无人消费风险。
- 复用价值：适用于多 worker、高并发和独立后台任务部署，避免后台分离后主链路仍被同步落库拖慢。

## 2026-06-08：API Key 调账与用户账单视图必须同源

- 问题：绑定用户的 API Key 调账会真实写入 `user_account_billing_records`，但 `create_balance_adjustment()` 仅返回一个未落库的 `ApiClientBillingRecord` 临时对象；随后 API Key 账单页仍只读取 `api_client_billing_records`，导致手工调账在用户账单可见、在 API Key 账单不可见。
- 解决方法：API Key 账单视图应统一读取用户账户账单中关联该 Key 的记录，或在调账时同时落库一条幂等的 API Key 账单记录；禁止返回未持久化对象伪装成账单记录。
- 复用价值：适用于充值、扣减、退款、补偿和手工调账，避免多账单视图口径分裂。

## 2026-06-08：计费数据库 schema 漂移会让补全和监控误判

- 解决方法：启动后台 worker、运行监控或执行账务审查前必须先确认 schema 迁移已完成；独立后台 worker 不应假设 Web 进程一定先完成 DDL。账务一致性巡检应固定检查“成功收费日志是否有 API Key 账单和用户账单”“计费状态字段是否存在”“本地/生产 schema 是否与模型一致”。
- 复用价值：适用于本地验证、云服务器更新、独立 worker 启动和历史数据补账，可避免把 schema 漂移误判为业务逻辑正常。

## 2026-06-08：额度字段废弃必须同时清理拦截、日志和展示

- 问题：API Key 模型、迁移、Redis 计数和日志字段仍保留 `request_limit_daily`、`token_limit_daily`、`cost_limit_daily`、`remaining_*` 等额度概念，但管理服务创建、更新和序列化会强制置空；正式限流只检查 QPS/RPM，Token/Cost finalize 也不写请求数计数，导致额度字段看似存在但不会形成真实拦截。
- 解决方法：若项目已统一为“账户共享余额 + QPS/RPM”，应删除或隐藏 API Key 日/总额度字段、剩余额度日志字段和 Redis 请求额度计数；若要恢复额度能力，则必须在鉴权、Redis 计数、日志、后台回填和前端展示中同源实现。
- 复用价值：适用于 API Key 额度、账户额度、实时限流和日志审计，避免半废弃字段误导管理员和用户。

## 2026-06-08：用量汇总与账单流水必须区分统计口径

- 问题：用户概览、趋势、API Key 分析等多处直接汇总 `request_logs.total_cost`，而真实余额变动和账单流水来自 `user_account_billing_records` / `api_client_billing_records`；当日志已计算费用但扣费失败、价格待处理或历史缺流水时，概览费用会与真实账单金额不一致。
- 解决方法：面向“消费统计”的口径可以汇总请求日志，但必须标注为请求成本；面向“余额/账单/已扣费”的口径必须以账单记录为准。跨页面展示同一业务含义时应复用同一服务层汇总函数，禁止模板、API 和后台分析各自汇总。
- 复用价值：适用于用户账单页、API Key 详情页、成本洞察、仪表盘和 CSV 导出，可避免“算出来的成本”和“实际扣款”混用。

## 2026-06-08：缓存写入计费字段必须完整落入模型、日志和账单明细

- 问题：正式计费支持缓存写入价格，但 `ProviderModel` 只有输入、输出、缓存读价格字段；无模型目录价格时缓存写入价会回退输入价且日志快照可能为空，账单记录也只保存输入/输出单价，不保存缓存读写 token 与缓存读写单价，难以复核缓存写入费用。
- 解决方法：缓存写入价格应在价格来源、日志快照、账单记录和导出中保持完整字段；若某来源不支持缓存写入价，应显式记录回退规则和实际使用价格，而不是只在计算过程临时回退。
- 复用价值：适用于 prompt cache、缓存创建费、价格目录迁移和账务追溯，避免分项成本无法复盘。

## 2026-06-08：后台补全结果指标不能把入队当成功

- 问题：`scheduled_token_usage_backfill()` 返回的 `success_count` 等于本轮扫描并入队的日志数，不代表 Token/计费已补全成功；如果队列堵塞、schema 缺列、余额不足或价格未解析，监控仍可能短期显示“成功处理”。
- 解决方法：后台补全任务应区分 scanned、enqueued、finalized、failed、skipped 等指标；监控告警应以最终 `billing_finalized_at`、失败错误和队列积压为准，不把入队数量当成结算成功数量。
- 复用价值：适用于后台任务、运维监控和账务审计，避免队列型任务的过程指标误导为最终结果。

## 2026-06-08：图片生成不能只沿用文本 Token 计费

- 问题：代理已支持 `/v1/images/*` 和 Responses `image_generation` 工具，也会在日志中记录生成图片摘要，但当前计费函数只按输入、输出、缓存读写 token 计算费用；若上游图片生成按张数、尺寸、质量或图片 token 单独计费，成功生图请求可能只收 prompt token 费用甚至零费用。
- 解决方法：图片生成应有独立价格模型和账单字段，至少记录生成图片数量、尺寸/质量、图片计费单位、图片费用和总费用；若上游返回官方 usage 或成本字段，应优先使用官方字段，否则按已配置的图片价格规则计算。
- 复用价值：适用于图片生成、图片编辑、多模态输出和后续视频/音频生成计费，避免把非文本能力错误归入纯文本 token 账单。

## 2026-06-08 内容防护流式延迟上限需要配置层与运行时双兜底

- 问题：内容防护最大检测延迟若只在前端或 schema 限制，历史数据库值或特殊流式模式仍可能绕过上限；例如完整缓冲模式把最大等待时间置空，会导致首段预检等待超过业务要求。
- 解决方法：延迟类热路径约束必须同时在前端输入、Pydantic schema、设置读取回填、运行时调用点做夹取；特殊模式不得把最大等待时间改成无限，只能改变缓冲停止条件。
- 复用场景：所有会影响正式 `/v1/*` 响应延迟的运行时配置、流式预检、重试等待、超时窗口。

## 2026-06-08 内容完整性自动隔离不能依赖告警写入开关

- 问题：监控采集接口若只在 `refresh_alerts=true` 写入告警事件时顺带执行自动隔离，普通轮询或前端非手动刷新只计算 metrics 不写告警时，同一提供商 10 分钟高风险命中 3 次也可能不会被隔离。
- 解决方法：监控指标成功采集并评估出高风险事件后，应独立执行自动处置动作；告警事件写入只负责记录和展示，不能作为隔离动作的唯一触发器。内容完整性总开关关闭时，告警与自动处置必须同时停用。
- 复用场景：所有监控告警、自动治理、风险隔离和后台处置链路，必须区分“状态处置动作”和“告警事件持久化”。

## 2026-06-08：跨模块类型别名移动必须保留兼容导出

- 问题：`user_portal.py` 从 `app.schemas.api_key` 导入 `RouteMode`，但 `api_key.py` 已移除该类型别名，导致 `uvicorn app.main:app` 启动期直接报 `ImportError: cannot import name 'RouteMode'`。
- 解决方法：若历史模块仍从旧 schema 导入共享类型别名，删除或迁移类型时必须同步更新所有引用；在无法一次性清理所有引用时，应在旧模块保留兼容导出，并用 `python -m uvicorn app.main:app` 或导入 `app.main` 做启动期验证。
- 复用价值：适用于 Pydantic schema、路由参数类型、枚举别名和服务层 DTO 迁移，避免只改定义文件导致应用启动阶段失败。

## 2026-06-08 API Key 旧路由策略与旧配额字段残留

问题：API Key 模型、schema、鉴权缓存、路由上下文、前端表单和请求日志中残留
oute_mode、default_provider_id、manual_allow_fallback、API Key 级无限重试、API Key 级最大候选数以及 Token/请求/金额配额字段时，会造成用户可见配置与真实健康优先路由不一致；未执行的 remaining/quota 字段还会污染日志和诊断结论。

解决方法：删除旧字段必须从数据库模型、Pydantic schema、服务层写入/序列化、鉴权缓存 payload、路由上下文、代理日志参数、前端表单 payload、导出列和迁移脚本同时收敛；只保留真实生效的账户余额、QPS/RPM、提供商/模型/端点/IP 授权、内容检测要求和健康优先偏好因子。新增或保留任何用户可见策略字段前，必须确认鉴权、路由、日志和前端展示存在同一执行闭环。

## 2026-06-08 外部 /v1 边界与内部 /api 边界混淆

问题：全局异常处理若把 /api/* 管理端接口也包装成 OpenAI 兼容错误，并写入外部 API Key 请求日志，会破坏管理端 API 的响应契约，并污染外部鉴权、请求和失败率指标。

解决方法：OpenAI 错误对象、CORS 兼容头、预路由拒绝请求日志和 API Key 鉴权失败日志只能作用于外部 /v1/*；内部 /api/* 必须保留 FastAPI/管理端原有错误语义，仅记录内部异常事件或管理端审计。

## 2026-06-08 multipart 请求体大小限制绕过

问题：只检查 Content-Length 后直接调用
equest.form() 会在反向代理未提供或未限制 Content-Length 时绕过应用层请求体大小限制，Files 与 legacy Images multipart 链路尤其容易出现全量读入风险。

解决方法：所有对外 /v1/* 请求体类型都必须在应用层按 stream 累计读取并检查大小；multipart 需先限量读 raw body，再交给表单解析器解析，禁止直接
equest.form() 作为第一步读取。

## 2026-06-08 Redis 请求日志队列恢复重放幂等

问题：请求日志 worker 使用 processing 队列时，如果 DB commit 成功后、Redis lrem 前进程崩溃，恢复流程会把 processing 项重新推回主队列，缺少幂等判断会导致重复请求日志和重复 finalize。

解决方法：入队 payload 必须携带稳定去重上下文；消费落库前先按 	race_id、
equest_id、log_type、请求路径和方法等唯一上下文查重。已有日志直接复用并跳过重复插入，再执行必要的后续 finalize 调度。

## 2026-06-09 IP 管理可信代理头解析与事件去重

问题：可信代理解析如果把 Forwarded 头中未加方括号的 IPv6:port 当作普通 IPv6 地址解析，可能把端口误认为地址段；IP 处置事件若只按 trace_id、路径、动作和规则粗粒度去重，会吞掉同一 trace 下不同来源 IP、解析状态、状态码或处置原因的真实事件。

解决方法：Forwarded 的 for 值必须采用更保守的专用解析：IPv6 必须使用 [addr] 形式才接受，IPv4:port 可剥离端口，unknown、混淆值和无法确认的未括号 IPv6 直接忽略并回退直连来源；事件去重必须覆盖方法、作用域、直连 IP、解析 IP、解析来源、解析状态、动作、原因、规则、执行状态和状态码，仅完全相同的事件才跳过。

复用场景：所有基于代理头、安全审计、风控事件或访问治理的模块，都必须优先保守解析可伪造输入，并让审计去重键覆盖完整业务语义。

## 2026-06-09 启动期调度器引用配置必须可导入

问题：应用 lifespan 启动调度器时，若 `configure_scheduler()` 内部调用配置函数但模块未导入对应名称，会导致 `Application startup failed`，项目无法完整启动；这类问题在普通导入检查中可能不暴露，只有进入 lifespan 后才触发。

解决方法：调度器、后台 worker、lifespan 等启动期路径新增配置读取时，必须同步补齐模块导入，并用 `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000` 或等价完整启动验证，而不能只做 `import app.main`。

复用场景：所有启动期任务注册、后台队列 worker、定时任务和 lifespan 初始化逻辑。

## 2026-06-09 模型字段变更后必须同步数据库迁移

问题：代码模型已包含 `providers.circuit_opened_at` 等运行态字段，但当前连接数据库未应用对应结构变更时，任何 `ProviderService.list_providers()` 或包含 Provider ORM 的页面/验证都会触发 `UndefinedColumn`，从而阻断模型管理、监控或健康检查相关功能。

解决方法：新增 Provider、ProviderModel、RequestLog 等 ORM 字段时，必须同步提供迁移 SQL 或初始化脚本，并在验证前确认目标数据库已应用迁移；若本地验证只关注非 Provider 逻辑，可临时隔离 Provider 查询，但不能把隔离结果当作完整端到端验证。

复用场景：适用于所有 ORM 字段扩展、云服务器更新、本地开发库升级和回归验证，避免代码已更新但数据库结构滞后导致运行期 SQL 错误。

## 2026-06-09 类型化日志表扩展后旧库缺列

问题：`request_content_guard_events`、`asset_events` 等类型化日志表新增字段后，如果当前数据库没有应用对应迁移，日志列表会在 ORM 查询整行对象时触发 `UndefinedColumn`，前端只表现为“日志加载失败”，容易被误判为前端问题。

解决方法：类型化日志模型新增字段时，必须同步迁移 SQL，并在开发/独立初始化兼容路径中补齐非生产补列逻辑；排查时优先对比 ORM 字段与实际表结构，再验证对应 `/api/logging/*` 和模块内日志接口。

复用场景：适用于内容防护日志、素材日志、调度任务日志、计费事件、健康检查事件等所有类型化日志扩展。

## 2026-06-09 复用全局弹层函数必须同步提供页面弹层 DOM

问题：页面按钮复用 `openHealthCheckResultModal()` 等全局弹层函数时，如果当前模板没有渲染函数依赖的 modal DOM，函数会找不到节点并直接返回，用户表现为点击无效、无加载态、无结果反馈。

解决方法：新增或迁移按钮入口时，必须同时确认目标页面存在对应弹层 DOM；若按钮依赖当前状态数据，还必须为“找不到对象/找不到弹层”等边界分支提供 Toast 或按钮短反馈，禁止静默 return。

复用场景：适用于测试结果、证据详情、批量任务进度、日志详情、模型详情等跨页面复用弹层。

## 2026-06-09 后台测试按钮必须先展示过程卡片并兼容日志审计字段

问题：模型挂载行内测试若先弹功能选择器或等待接口返回后才展示结果，长耗时上游调用会被用户误判为按钮不可用；同时 `HealthService` 调用 `LogService.create_log(capability_result=...)`、内容防护路由调用 `AdminAuditService.create_log()` 漏传必填 `entity_id` 时，会让健康测试或可信检测接口返回 500。

解决方法：行内测试/可信检测按钮点击后必须立即打开过程卡片，再在接口成功或失败后刷新同一弹窗；日志服务需要保留 `capability_result` 兼容入口并落入结构化响应字段，内容防护审计日志必须传入稳定 `entity_id`。

复用场景：适用于提供商、模型挂载、内容防护、IP 管理等所有后台手动检测入口，排查时要同时检查前端即时反馈、接口响应和日志/审计落库参数契约。

## 2026-06-09 批量长耗时探针禁止全局串行

问题：模型挂载批量健康测试或可信检测如果对所有目标逐条串行 `await`，首个慢上游会让后续提供商一直停留在“等待中”，弹层表现为长时间 `0/N` 无结果，用户会误判为批量检测卡死。

解决方法：批量检测必须按 `provider_id` 分组调度，不同提供商之间并行执行，同一提供商内部可串行控制并发压力；每个目标必须独立捕获失败、超时和异常并刷新同一进度弹层，禁止一个目标阻塞整批结果反馈。

复用场景：适用于提供商对象目录、模型挂载矩阵、内容防护预检、健康测试、真实上游验证等所有需要调用外部上游的批量检测入口。

## 2026-06-09 接口 200 但前端显示加载失败要排查渲染链路

问题：内容防护运行时事件接口返回 200，但前端仍显示“运行时事件加载失败”，原因是渲染函数内部调用了不存在的 tooltip 增强函数，异常被外层加载 catch 捕获后伪装成接口失败；探针结果渲染也引用了其它页面私有的状态格式函数，成功返回后可能同样看不到结果。

解决方法：排查“接口成功但页面失败”时必须同时看网络响应、前端渲染函数和 catch 内错误反馈；跨页面复用帮助图标、tooltip、状态标签等组件时，只能调用全局真实存在的共享函数，禁止引用页面私有函数或不存在的旧函数名。长耗时探针点击后必须立即写入执行中结果卡片，接口成功或失败都刷新同一结果区域。

复用场景：适用于内容防护事件、健康测试结果、模型测试、日志列表、批量检测进度和所有通过表格/结果卡片渲染接口响应的后台页面。

## 2026-06-09 页面初始化被遗留未声明变量中断

问题：管理页数据接口和服务层都正常，但页面不发起任何请求、统计卡保持模板默认值时，可能是页面初始化函数中残留了已删除控件的未声明变量引用，执行到该监听绑定处直接抛 `ReferenceError`，后续加载函数不会运行。

解决方法：排查这类“数据库有数据但前端显示 0/正在加载”的问题时，先确认浏览器是否实际请求了列表和汇总接口；若没有请求，检查页面初始化函数中的遗留变量、可选控件监听和模板 ID 是否匹配。已删除的筛选控件必须同步删除对应 JS 监听，或显式声明为可选 DOM 节点后再绑定。

复用场景：适用于 API 密钥管理、提供商对象目录、模型配置、日志列表等所有依赖页面初始化后异步加载数据的后台页面。

## 2026-06-09 Responses 成功响应中的 error:null 禁止误判为上游错误

问题：Responses 标准成功响应对象可能包含顶层 `error` 字段且值为 `null`。若内容防护或健康探针只按“存在 error 键”判断上游错误，会把正常 Responses 响应误判为内容完整性异常，导致可信检测批量结果全部显示异常。

解决方法：响应结构校验必须判断 `error` 是否有实际错误内容；`None`、`false`、空字符串、空数组、空对象不得直接视为上游错误。只有非空错误对象、非空错误文本或其它有意义错误值才按 `upstream_error_payload` 拦截。

复用场景：适用于内容防护探针、健康检测、代理响应校验、模型测试结果合法性校验等所有解析 OpenAI Responses 响应对象的链路。

## 2026-06-09 单模型多探针必须并行且结构化失败

问题：预检能力探针若在同一模型内逐个执行固定答案、外链广告识别、流式污染检测、JSON、工具调用等探针，整体耗时会变成所有上游调用耗时之和；任一探针异常若直接冒泡，还会导致接口缺少结构化 `probe_results`，无法正确写回内容完整性字段。

解决方法：同一模型的多个预检探针必须并行执行并保留原始展示顺序；每个探针必须有单探针超时边界，超时或异常要转成标准探针失败结果，继续参与汇总与字段更新。只有必需可信探针齐全时才允许写入模型/提供商可信状态，可选能力探针不得单独把模型标记为可信。

复用场景：适用于内容防护预检、可信检测、健康检查内容探针、自动预检任务和所有需要把探针结果沉淀到模型状态的后台检测链路。

## 2026-06-09 内容检测关键词必须识别否定上下文

问题：内容防护关键词规则若只做裸词命中，会把“没有推广链接”“不包含联系方式”等安全说明误判为广告或导流内容；增强混淆检测在折叠空格和符号后也会放大这类误判。

解决方法：关键词检测和增强词法检测必须在命中后检查短距离否定上下文；“没有、无、不含、不包含、未提供、不提供、不会包含、不会提供、不存在”等明确否定前缀命中时，不应按该关键词计入风险。该逻辑只用于明确否定事实描述，不能把“不要错过”等营销语误当成否定。

复用场景：适用于内容防护规则检测、运行时流式滑动窗口检测、健康测试内容合法性校验、模型测试结果校验和任何基于关键词/短语做轻量安全检测的链路。

## 2026-06-09 同一探针内部多场景禁止串行叠加超时

问题：外链广告识别这类单个探针内部若包含多个独立污染场景，并逐个真实请求上游模型，整体耗时会变成各场景耗时之和；例如 4 个场景每个最多 8 秒时，用户会看到一个探针耗时接近 20-24 秒，并误以为页面或任务卡死。

解决方法：同一探针内部的独立检测场景必须并发执行，并保留每个场景自己的超时、失败原因、检测证据和展示顺序；总超时只作为兜底取消，不能替代单场景边界。若某个场景超时，结果中必须明确显示具体场景名称和秒级上限。

复用场景：适用于内容防护外链广告识别、引用格式污染、长上下文污染、工具上下文污染、模型自检多场景、用户侧一键自检和其它“一个测试项包含多个独立请求”的检测链路。

## 2026-06-09 同一探针多场景优先合并为单次上游请求

问题：外链广告识别探针中的长上下文、工具上下文、Markdown、引用格式四个污染场景虽然属于四个展示子结果，但检测目标、端点、模型和本地判定规则相同；如果每个子场景都单独发起上游请求，即使并发执行也会瞬间放大上游连接数和限流压力。

解决方法：当多个子场景可共享同一上游端点、模型、超时和本地判定规则时，优先构造一个组合 prompt，一次请求上游模型，并要求模型按稳定分段标记返回；服务端本地按标记拆分为多个子结果，再分别复用内容防护规则判定。只有子场景协议、工具形态或流式/非流式要求不同，才拆成独立请求。

复用场景：适用于内容防护外链广告识别、引用格式污染、长上下文污染、Markdown 污染、模型自检多文体场景和其它“一个测试项展示多条子结果但可共享一次模型回答”的检测链路。

## 2026-06-09 探针结果必须能追溯原始上游响应

问题：内容防护或健康探针只展示“格式错误、内容污染、超时”等摘要时，管理员无法判断是上游真的返回异常内容、代理解析错误，还是探针 prompt/协议不兼容；排查时需要反复加日志或复现请求。

解决方法：探针服务返回结果时必须携带脱敏后的原始上游响应摘要或样本，非流式探针保留响应体与提取文本，流式探针保留事件与 chunk 样本；前端每条探针结果提供“原始响应”按钮，用统一弹层展示格式化内容。不同请求形态的探针不能为减少请求数强行合并，只有共享端点、模型、请求形态和判定规则的探针才允许合并。

复用场景：适用于固定答案、外链广告识别、流式污染检测、严格 JSON、工具调用、健康测试内容合法性校验、模型测试和其它所有需要真实调用上游的检测链路。

## 2026-06-09 长文档追加章节禁止使用重复结尾作为唯一锚点

问题：`完整功能分析.md` 这类长文档中存在大量重复的“下一轮将继续补充下一个功能模块。”，如果补丁只用该重复句作为上下文，新增章节会插入到第一个匹配位置，导致章节顺序错乱并造成返工。

解决方法：向长文档追加章节时，必须使用唯一章节标题、文档末尾内容或追加后顺序校验作为定位依据；若发现章节插入到中间位置，应立即按完整章节标题提取该章节并移动到文末，再重新校验章节顺序、禁用词和行尾空白。

复用场景：适用于持续追加轮次文档、审查报告、架构分析、改进方案、问题记录等包含重复分隔语或重复小结句的长 Markdown 文档。

## 2026-06-09 URL 检测禁止把中文句号归一化为裸域名点号

问题：内容防护为了识别混淆 URL，会归一化全角点号；若把中文句号 `。` 也直接替换为英文点号，裸域名正则可能跨中文句子误识别普通英文混排内容，例如把 `API Key 写入前端代码。定期轮换` 拼成疑似域名，导致外链广告识别误判。

解决方法：URL 检测只应把全角英文点、半角变体点或显式 `dot` 混淆写法归一化为域名点号；中文句号必须保留句子边界语义，不得参与裸域名拼接。新增规则时应同时覆盖“正常中文正文 + 英文术语 + 中文句号”的回归样例。

复用场景：适用于内容防护外链广告识别、运行时 URL 检测、健康探针内容合法性校验、日志扫描和所有基于裸域名正则做安全检测的链路。

## 2026-06-10 模型运行健康状态禁止由请求链路直接维护

问题：如果正式请求成功/失败后直接在代理链路中改写 `provider_models.health_status` 或 `provider_models.circuit_state`，高并发下会出现多个用户请求同时触发健康状态维护、探针或熔断切换的问题，既增加上游压力，也会让健康状态来源分散、难以追踪。

解决方法：正式请求链路只写 `request_logs`、计数、最后延迟和最后错误等观测数据；运行健康状态由统一后台任务读取 `request_logs` 短窗口派生指标维护。高频读取使用 Redis 秒级聚合缓存，缓存只保存计数、错误分类和延迟摘要，不保存完整响应；异常信号必须先触发受限数量的健康探针确认，再写入异常状态。

复用场景：适用于提供商模型健康度、熔断状态、路由热状态、短窗口失败率、日志统计缓存和所有需要从正式请求结果反推运行可用性的后台治理链路。

## 2026-06-10 PostgreSQL 回归脚本运行前必须确认测试库存在

问题：本地 PostgreSQL 服务可连接但缺少 `aotu_gpt_test` 等隔离测试库时，`stage36_ip_management_regression_check.py` 这类依赖数据库建表/清表的回归脚本会在业务断言前直接失败，容易被误判为当前代码改动引入问题。

解决方法：运行依赖 PostgreSQL 的回归脚本前，先确认 `DATABASE_URL` 指向的测试数据库已经创建且与开发/生产库隔离；若数据库不存在，先按 PostgreSQL 管理流程创建空测试库，再运行脚本。若当前业务账号没有 `CREATE DATABASE` 权限，必须使用 PostgreSQL 管理员角色预先建库或配置专用测试管理账号；不要把回归脚本指向生产库或共享业务库。

复用场景：适用于 IP 管理、日志、计费、内容防护、用户账户等所有需要真实 PostgreSQL 连接的本地或 CI 回归脚本。

## 2026-06-10 上游连接池并发上限禁止误用 keepalive 空闲连接数

问题：`aiohttp.TCPConnector.limit_per_host` 表示单个上游主机的最大并发连接数，若把它绑定到 `upstream_max_keepalive_connections`，会把“空闲连接保留数量”误当成“并发连接上限”。高并发流式或非流式请求下，业务并发租约尚未耗尽，HTTP 连接池已经排队，表现为首包慢、上游连接长期等待和吞吐下降。

解决方法：单主机并发上限必须按 `upstream_max_connections` 等连接池总容量参数控制；`upstream_max_keepalive_connections` 只用于限制空闲连接保留数量。keepalive 过期时间、DNS 缓存 TTL 和 requests 兜底池阻塞策略必须可配置，并纳入客户端重建指纹，避免运行参数变化后旧连接池继续生效。

复用场景：适用于上游模型 HTTP 客户端、健康探针、内容防护探针、压测客户端和所有使用 aiohttp/httpx/requests 访问提供商的连接池配置。

## 2026-06-10 监控禁止只看平均延迟和单一错误总量

问题：对外代理同时存在非流式短请求、流式长连接、客户端取消、上游失败、系统限流和后台异步队列。若监控只展示总平均延迟、总失败数或当前 worker 进程计数，会把长流式占坑、队列积压、事件循环阻塞、上游异常和主动限流混在一起，容量判断容易失真。

解决方法：运维监控必须拆分总请求、流式请求和非流式请求的 P50/P95/P99；失败终态必须区分客户端取消、上游失败、系统限流和其它失败；后台日志队列、typed logging 队列必须记录最老队列项等待时间；当前 worker 必须采集事件循环延迟，并在告警中使用这些细分指标。

复用场景：适用于系统监控页、告警事件、容量规划、压测验收、发布后观测和所有需要判断系统瓶颈来源的运维链路。

## 2026-06-10 Redis 客户端配置禁止被业务模块绕过

问题：限流、租约、缓存、日志队列和计费去重都依赖 Redis；若各业务模块直接 `Redis.from_url` 创建客户端，会绕过统一连接池上限、连接超时、读写超时和健康检查配置。高并发或 Redis 延迟升高时，不同路径会表现不一致，甚至把 Redis 等待扩散到请求热路径。

解决方法：所有 Redis 客户端必须通过 `RedisService` 统一创建或获取；新增 Redis 相关运行参数时只在 `RedisService.client_kwargs()` 汇总，并同步 `.env.example` 与部署脚本默认值。监控类指标禁止通过扫描大 keyspace 估算正式业务积压，能用数据库正式候选条件或队列长度时优先使用这些确定口径。

复用场景：适用于并发租约、API Key 鉴权缓存、限流、共享缓存、请求日志队列、typed logging 队列、Token/计费去重、系统监控和后台 worker。

## 2026-06-10 请求日志队列必须有失败出口

问题：请求日志异步队列若只有 processing 恢复和失败重入队，遇到持续失败的数据项或数据库约束问题时会无限重试；这会占用消费者吞吐、放大队列积压，并让真正的新请求日志延迟落库。

解决方法：请求日志队列必须记录失败尝试次数；未到上限时带上失败元数据重新入队，达到上限后移动到 Redis 死信队列并从 processing 移除。Token/计费补全达到最大尝试次数时也必须写入可观测死信队列，并保留告警事件。监控快照必须暴露 queued、processing、dead_letter、failure_count 和最老等待时间，便于区分“正在排队”和“已进入失败出口”。

复用场景：适用于请求日志队列、typed logging 队列、Token finalize 队列、后台补全任务和所有 Redis/内存异步消费队列。

## 2026-06-10 短窗口限流不能只覆盖 API Key

问题：只对单个 API Key 做 QPS/RPM 限流时，同一账户可以通过多个 Key 叠加绕过限制；如果没有全局短窗口限制，单个账户或多个 Key 也可能抢占全部入口资源。

解决方法：外部 `/v1/*` 请求鉴权通过后，必须在同一 Redis 原子校验中同时检查全局、API Key、账户三类 QPS/RPM 短窗口；脚本应先检查所有作用域，再统一递增，避免某个下级维度失败时提前消耗全局计数。

复用场景：适用于 API Key 鉴权、账户级限流、全局入口保护、压测限流验收和所有需要多维度共享限流状态的请求入口。

## 2026-06-10 provider 容量计数必须与通用并发计数隔离

问题：provider 容量租约和全局并发租约若共用 `concurrency:provider:*` Redis 键，后续一旦入口并发租约也启用 provider 维度，就会导致同一次请求被重复计入 provider 活跃请求或流式计数，容量预筛和租约判断都会失真。

解决方法：provider 容量服务必须使用独立命名空间（如 `provider_capacity:provider:*`）维护活跃请求和流式计数；通用全局/API Key/账户并发租约继续使用 `concurrency:*`。新增容量维度时要检查 Redis key 命名是否与其它服务重叠。

复用场景：适用于 provider 容量租约、全局并发控制、流式并发池、运维监控容量快照和所有 Redis 共享计数设计。

## 2026-06-10 路由回归脚本必须跟随候选结构演进

问题：路由候选结构从历史权重字段切换为健康度、负载和路由得分后，旧回归脚本仍构造 `dynamic_weight` 或断言旧权重函数，会在业务验证前失败，掩盖真实路由问题。

解决方法：当 `RouteCandidate` 字段或主路由策略调整时，相关 stage 脚本必须同步改为当前结构；旧“权重分发”断言应替换为“同健康层级和同分桶下低负载优先”等当前策略断言。

复用场景：适用于路由策略、模型映射、候选扩展、故障切换、健康优先排序和所有直接构造服务内部 dataclass 的回归脚本。

## 2026-06-10 入口层请求体保护不能只限制字节数

问题：仅在 JSON 或 multipart 解析前限制请求体字节数，可以挡住超大 payload，但无法处理慢客户端长期不发送或极慢发送请求体的情况。慢上传会持续占用连接、协程和 worker，在高并发下会拖慢正常请求。

解决方法：对外 `/v1/*` 请求体读取必须统一使用限量、总超时和空闲超时三重保护；超出字节数返回 `request_body_too_large`，上传过慢返回统一 `request_timeout`，并且日志只记录结构摘要和超时/截断元数据。

复用场景：适用于 JSON 请求体、多模态 base64、multipart 文件上传、鉴权失败后的请求体摘要读取、OPTIONS/异常入口日志和所有需要读取客户端请求体的入口链路。

## 2026-06-10 无效 API Key 重试不能反复查库

问题：API Key 正缓存只能优化有效 Key；如果重复提交同一个无效 Bearer Key，每次都落到数据库查询，会在恶意重试或错误客户端循环重试时放大数据库压力。

解决方法：对无效 Key 使用短 TTL 的 SHA256 哈希负缓存，命中后直接返回统一鉴权失败；缓存禁止保存原始明文 Key，并在 API Key 创建、更新、轮换、删除或用户/授权变更时同步清理相关正负缓存。

复用场景：适用于 API Key 鉴权、入口限流、认证缓存、Redis 共享缓存、多 worker 部署和所有需要处理重复无效凭据的对外入口。

## 2026-06-10 业务并发租约不能晚于请求体解析

问题：如果只在请求体解析后申请 API Key、账户或 provider 业务并发租约，瞬时并发突刺仍会先消耗鉴权、限量读取、JSON/multipart 解析和 worker 资源，入口层可能在真正进入业务租约前已经排队或抖动。

解决方法：对外 `/v1/*` 必须在路由、鉴权和请求体解析前申请独立入口全局并发租约；该租约只保护入口总活跃请求，必须使用 `ingress:*` 等独立 Redis 命名空间，避免与业务并发租约重复计数。

复用场景：适用于外部 API 入口、反向代理后应用入口、全局瞬时并发保护、多 worker 部署、压测入口保护和所有需要在鉴权前削峰的请求链路。

## 2026-06-10 共享缓存前缀失效不能依赖 Redis 大范围扫描

问题：配置、模型、提供商等写操作会主动失效多个共享缓存前缀；如果每次失效都 `SCAN shared-cache:<prefix>*` 后批量删除，在 keyspace 较大或 Redis 延迟升高时，缓存治理本身会占用 Redis 单线程并拖慢租约、限流和日志队列。

解决方法：共享 Redis 缓存前缀失效使用命名空间版本号。写入和读取缓存时把当前版本号编码进真实缓存 key；前缀失效只递增对应版本号，旧 key 依靠自身 TTL 自然过期。Redis 缓存 TTL 增加可配置抖动，避免热门 key 同时失效后集中回源数据库。

复用场景：适用于模型列表、路由候选、提供商运行态、系统设置、日志统计、告警看板和所有通过 `CacheService` 维护的跨 worker 共享缓存。

## 2026-06-10 Token/计费补全候选查询必须有部分索引

问题：Token/计费补全后台任务只处理正式、成功、外部 API Key、未完成计费且未超过最大尝试次数的 `request_logs`。如果该候选查询只依赖通用 `created_at` 或状态索引，日志表膨胀后会在后台任务和监控积压统计中反复扫描大量无关日志。

解决方法：为 `request_logs` 增加与补全候选条件一致的 PostgreSQL 部分索引，索引列按 `created_at DESC, id DESC` 对齐候选拉取排序；当补全候选条件变化时，必须同步更新模型索引、SQL 迁移和回归测试。

复用场景：适用于 Token finalize backfill、计费补全积压统计、后台补偿任务、日志表索引设计和所有依赖正式请求日志筛选的异步治理链路。

## 2026-06-10 长文档轮次追加必须使用唯一定位

问题：`完整功能分析.md` 中每一轮末尾都包含相同的“下一轮将继续补充下一个功能模块。”标记。若追加新轮次时只用该重复标记作为补丁定位点，新内容容易插入到较早轮次之后，导致轮次顺序错乱。

解决方法：追加新轮次时必须使用唯一的前置轮次标题作为定位上下文，或追加后立即用 `rg -n '^## 第|下一轮将继续补充下一个功能模块。' 完整功能分析.md` 校验尾部顺序；若发现插入到中间，使用 PowerShell 7 脚本按新轮次唯一标题提取整块内容，删除原块后移动到文档末尾，再重新校验禁用词、路径痕迹、行尾空格和模块数量。

复用场景：适用于 `完整功能分析.md`、项目审查类长文档、按轮次持续追加的分析文档，以及任何存在重复结尾标记的 Markdown 长文档维护。
