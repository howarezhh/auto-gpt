
1.  内容防护类型化事件有模型和注册表，但没有独立列表/导出接口 `RequestContentGuardEvent` 已存在：[logging\_events.py](./app/models/logging_events.py:154)，也进了请求时间线：[logging\_api.py](./app/routers/logging_api.py:45)。 但 `/api/logging` 只提供异常、健康、计费、后台任务、素材等列表/导出，没有 `/content-guard-events`，前端也没有“内容防护事件”类型化日志 Tab：[app.js](./app/static/js/app.js:2451)。【已完成：已修复，新增内容防护类型化日志列表、导出和前端 Tab】
    
2.  正式迁移缺少 `request_content_guard_events` 等类型化日志表 ORM 有表：[logging\_events.py](./app/models/logging_events.py:154)，但现有内容防护迁移只补 `request_logs`/`providers`/`app_settings` 字段：[2026-06-06\_add\_content\_guard\_governance.sql](./migrations/2026-06-06_add_content_guard_governance.sql:4)。 生产规范禁止 Web worker 自动 DDL，而 [main.py](./app/main.py:80) 仍依赖 `create_all`/启动迁移，生产部署很可能缺表。【已完成：已核验，当前迁移已包含 request_content_guard_events 和 health_probe_events 建表及索引】
    
3.  `record_only` 或关闭“高风险拦截”仍可能污染提供商治理状态 代理在判断是否真正拦截前，就会按 `guard_result.result == block` 记录 provider 违规：[proxy\_service.py](./app/services/proxy_service.py:1088)。 而“是否拦截”是在后面的 `should_block` 才读取策略和开关：[content\_guard\_service.py](./app/services/content_guard_service.py:675)。这会导致“仅记录”仍降级、扣分或熔断。【已完成：已修复，治理惩罚仅在运行时策略决定真实阻断/切换时记录】
    
4.  手动模型健康测试会跑内容探针，但健康日志详情不记录这部分探针明细 `check_provider_model(interactive_mode=True)` 会额外跑 `ContentGuardProbeService.PROBE_PHASE_KEYS`：[health\_service.py](./app/services/health_service.py:237)。 但 `_record_run_results` 只展开 `endpoint_results`，不展开后来挂上的 `content_probe_results`：[health\_service.py](./app/services/health_service.py:1116)。所以日志中心健康详情看不到真实内容探针过程。【已完成：已修复，健康日志事件会展开 content_probe_results】
    
5.  手动模型测试的内容探针不尊重内容完整性总开关 定时内容完整性检查会先判断 `content_guard_enabled`：[health\_service.py](./app/services/health_service.py:992)，但交互式模型测试分支没有这个判断：[health\_service.py](./app/services/health_service.py:237)。这违反“总开关关闭时代理检测、健康探针、监控自动隔离都停用”的规范。【已完成：已核验，按最新全局规范手动模型测试内容探针不受总开关影响，跳过代码修改】
    
6.  自动隔离缺少失败态审计 监控会自动把高风险 provider 和挂载模型设为 blocked：[system\_metrics\_service.py](./app/services/system_metrics_service.py:259)。 但隔离失败没有 `isolation_failed`、错误原因或单独告警状态，告警 payload 只记录成功字段：[alert\_service.py](./app/services/alert_service.py:454)。一旦 DB 写入中途失败，运维侧很难追踪处置是否真的完成。【已完成：已修复，自动隔离异常会写 isolation_failed/isolation_error/isolation_status】
    
7.  内容防护事件证据字段语义不完整 代理写 `request_content_guard` 时，`matched_rules_json` 固定为 `null`：[proxy\_service.py](./app/services/proxy_service.py:2052)。 流式分支还把 `provider_status_after` 写成 `final_strategy`：[proxy\_service.py](./app/services/proxy_service.py:3369)，字段名是“提供商状态”，实际值是处理策略，审计语义错位。【已完成：已修复，typed event 记录真实 matched_rules_json，provider_status_after 不再写策略名】
    
8.  日志中心请求详情展示的内容防护信息太少 请求详情只展示结果、风险、动作、耗时：[app.js](./app/static/js/app.js:10811)。 `categories`、`reason`、`excerpt`、`buffer_wait_ms`、`retry_provider_count`、`final_strategy` 都没有结构化展示，导致污染证据无法直接复核。【已完成：已修复，请求详情内容防护卡片补充分类、原因、证据、缓冲等待、重试提供商数和最终策略】
    
9.  内容防护前端仍使用旧术语“渠道” 例如高风险策略选项“切换渠道”、外部检测“外部渠道”：[content\_guard.html](./app/templates/content_guard.html:137)。这与全局规范“统一使用提供商”冲突。【已完成：已修复，内容防护页面相关旧术语已改为“提供商”】
    
10.  URL 白名单说明把安全绕过行为正常化 页面提示“用户请求中出现过的域名会自动加入本次白名单”：[content\_guard.html](./app/templates/content_guard.html:205)。后端也确实会把请求域名并入允许域：[content\_guard\_service.py](./app/services/content_guard_service.py:601)。这会让 prompt 注入的域名变成合法域名。【已完成：已修复，白名单仅来自显式配置，不再自动合并请求域名】
    
11.  规则编辑器仍丢失 `reason` 后端规则 schema 支持 `reason`：[content\_guard.py](./app/schemas/content_guard.py:43)，前端 normalize 也读了 reason，但保存时固定写空字符串：[app.js](./app/static/js/app.js:8843)。管理员无法维护命中原因。【已完成：已修复，规则表新增命中原因输入并随保存提交 reason】
    
12.  正则规则错误被静默吞掉 `regex` 匹配遇到 `re.error` 直接 `continue`：[content\_guard\_service.py](./app/services/content_guard_service.py:365)。规则保存不报错，运行也不告警，会出现“规则看似启用但永远不生效”。【已完成：已修复，保存时校验正则，运行时历史坏正则会返回可见配置错误】

1.  探针通过后无法恢复 provider 硬隔离状态 [app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1082) 成功分支只把 `provider_model.content_integrity_status` 改为 `passed`，没有恢复 `provider.content_integrity_status`、`trust_level`、`content_integrity_score`、`provider.circuit_state`。但路由排除看的是 provider 硬字段： [app/services/router\_service.py](./app/services/router_service.py:1124) 结果是：模型显示通过，提供商仍然可能被真实路由排除。【已完成：已修复，探针通过会刷新 provider 聚合可信状态并恢复评分/熔断】
    
2.  `refresh_provider_state` 会覆盖熔断状态，但不处理内容完整性状态 [app/services/provider\_service.py](./app/services/provider_service.py:1650) 只按健康状态同步 `provider.health_status/circuit_state`，不聚合模型内容完整性状态。内容探针失败后 provider 被标记 blocked，但后续模型全部 passed 时没有对应恢复路径。【已完成：已修复，refresh_provider_state 现在会聚合启用模型的内容完整性状态】
    
3.  管理员手动标记“可信”伪造的探针结果缺少必需 SSE [app/services/provider\_service.py](./app/services/provider_service.py:1081) 手动 passed 只生成固定答案和外链广告两条结果，没有 `content_sse`。这和当前规范“可信必需探针必须包含流式污染检测”冲突，容易让前端显示可信但证据不完整。【已完成：已修复，手动可信结果补齐 content_sse 流式污染检测证据】
    
4.  内容完整性总开关关闭时，手动可信探针可能被伪装为通过 [app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:876) 和 [app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:895) 在开关关闭时直接返回 pass。规范要求关闭后停用代理检测、健康探针内容判定和监控隔离，不应把“未启用”写成“可信探针通过”。【已完成：已修复，手动探针不再因总开关关闭直接伪造通过，会实际执行检测】
    
5.  SSE 探针最多只读 6 个 chunk，碎片化流可能误判失败 [app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:538) 固定读取 6 个 chunk 后要求看到 `[DONE]`。如果 provider 把固定答案按字符/小片段切分，或者先输出多个空事件，可能还没到 `[DONE]` 就被判定 `sse_missing_done`。【已完成：已修复，SSE 探针改为读取到 DONE、流结束、缓冲上限或 64 个 chunk】
    
6.  SSE 探针未传入自定义规则 JSON [app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:590) 调用 `inspect_sse_event` 时只传了 `url_check_enabled`，没有传 `rules_json=ContentGuardProbeService.rules_json()`，导致 SSE 探针和普通 JSON/文本探针的规则口径不一致。【已完成：已修复，SSE 探针调用 inspect_sse_event 时传入统一自定义规则 JSON】
    
7.  `content_guard_required` 仍然是局部 API Key 策略，和全局规范冲突 [app/services/api\_key\_service.py](./app/services/api_key_service.py:488) 把 `api_client_key.content_guard_required` 写入路由上下文；但规范已要求“只走可信提供商只能作为全局路由设置维护”，API Key 不应有局部内容防护放行开关。【已完成：已修复，鉴权路由上下文不再读取 API Key 局部 content_guard_required】
    
8.  `_with_content_guard_route_policy` 仍是空实现 [app/services/proxy\_service.py](./app/services/proxy_service.py:235) 直接返回原 `route_context`。即使 `ContentGuardService.requires_trusted_provider(...)` 已存在，高风险请求也不会在路由前升级为可信 provider 要求。【已完成：已修复，路由前会按请求特征升级 require_trusted_provider 策略】
    
9.  手动单模型健康测试的内容探针结果没有展开写入健康探针事件 [app/services/health\_service.py](./app/services/health_service.py:261) 会把 `content_probe_results` 挂到 `model_result`，但 `_record_run_results` 只展开 `endpoint_results`： [app/services/health\_service.py](./app/services/health_service.py:1116) 结果是手动测试弹窗能看到内容结果，健康日志明细可能没有对应 probe 事件。【已完成：已核验，当前 _record_run_results 已合并展开 content_probe_results 并写入健康探针事件】
    
10.  生产迁移仍不完整，typed event 表缺正式迁移 内容字段大量在 [app/main.py](./app/main.py:242) 启动 DDL 中补列；但生产规范禁止 Web worker 启动阶段自动 DDL。`request_content_guard_events`、`health_probe_events` 等 typed 表来自模型定义，没有在现有 migrations 中完整创建，生产库容易出现接口 500 或日志为空。【已完成：已核验，当前正式迁移已包含 request_content_guard_events 与 health_probe_events 建表及索引】
    
11.  内容防护页没有 provider 治理闭环入口 后端 overview 返回 provider 列表：[app/services/content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:76)，但页面只展示指标、配置、探针、规则和结果，没有 provider 隔离/恢复/原因/最近事件列表。管理员无法在内容防护模块内完成治理闭环。【已完成：已修复，内容防护页新增提供商治理分区、最近事件、异常原因和隔离/恢复入口】
    
12.  前端 URL 白名单帮助文本在强化危险行为 [app/templates/content\_guard.html](./app/templates/content_guard.html:205) 明确告诉用户“用户请求中出现过的域名会自动加入本次白名单”。这不仅暴露了当前绕过机制，还把危险行为合理化。【已完成：已核验，当前页面说明已改为仅显式配置域名会被视为允许域名】
    
13.  探针结果长原因仍直接塞表格列 [app/static/js/app.js](./app/static/js/app.js:9045) 把 reason 放进 `content-guard-reason-cell` 正文。按全局规范，高密度表格中的长异常原因应收纳到统一问号 Tooltip，否则会挤爆列宽。【已完成：已修复，探针原因改为短摘要加全站共享问号 Tooltip 展示详情】
    
14.  内容防护页面仍是全量展开长页 [app/templates/content\_guard.html](./app/templates/content_guard.html:45) 起配置、探针、规则、本地检测、结果全部平铺，超过两屏后没有折叠、标签页或固定提交动作，违反“复杂长页面必须渐进式披露”的前端规范。【已完成：已修复，页面改为配置探测、提供商治理、规则检测、检测结果四个分区渐进展示】

1.  `/api/content-guard/probe` 会把“局部探针”持久化成模型可信失败 [app/services/content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:231) 允许任意勾选探针；[app/static/js/app.js](./app/static/js/app.js:9015) 内部目标默认 `persist_internal_result: true`。如果只测 JSON/tools，会因缺少固定答案、外链广告、SSE 被写成 review/degraded。【已完成：已修复，后端仅在必需可信探针齐全时持久化内部模型可信状态，前端同步按必需探针判断持久化】
    
2.  外部渠道探测存在 SSRF 风险 [app/schemas/content\_guard.py](./app/schemas/content_guard.py:98) 只校验 `http://`/`https://`，没有阻止内网地址、本机地址、云元数据地址或 DNS rebinding。管理员接口也不应随意向任意 URL 携带密钥发请求。【已完成：已修复，外部检测强制 https 并拦截 localhost、显式内网 IP 以及 DNS 解析到内网/回环/链路本地/保留地址的主机】
    
3.  裸域名外链可绕过异常外链规则 [app/services/content\_guard\_service.py](./app/services/content_guard_service.py:348) 必须出现 `http://` 或 `https://` 才触发 unexpected\_url。实测 `更多资料见 promo-example.invalid/docs` 在“不要提供链接”的请求下返回 `pass low`。【已完成：已核验，当前 extract_domains 已通过域名正则覆盖裸域名，断言该样例会命中 block/high】
    
4.  `image_url` 输出字段被跳过，外链可藏在图片 URL 中 [app/services/content\_guard\_service.py](./app/services/content_guard_service.py:943) `_extract_scan_text` 跳过 `image_url`。实测 Responses 输出里带 `image_url: https://promo-example.invalid/...` 仍返回 `pass low`。【已完成：已修复，通用扫描和 Chat/Responses 结构化响应扫描均纳入 image_url 字段，断言该样例会命中 block/high】
    
5.  规则 JSON 损坏会静默回落默认规则 [app/services/content\_guard\_service.py](./app/services/content_guard_service.py:474) 解析失败直接 `normalize_rules(None)`，接口和前端不会提示“当前规则配置已损坏”，排查时会误以为保存的自定义规则正在生效。【已完成：已修复，规则 JSON 损坏会返回可见 rule_configuration_error / RESULT_ERROR，不再静默回落默认规则】
    
6.  保存正则规则不校验正则有效性，运行时静默忽略 [app/services/content\_guard\_service.py](./app/services/content_guard_service.py:365) `re.error` 后 `continue`，没有任何错误或告警。管理员可能保存了失效规则但完全不知道。【已完成：已核验，当前 schema 保存时校验正则，历史坏正则运行时返回可见配置错误】
    
7.  内容防护总开关关闭后，历史隔离仍继续影响路由 [app/services/router\_service.py](./app/services/router_service.py:236) 始终按 provider 内容状态排除候选，[app/services/router\_service.py](./app/services/router_service.py:1124) 不读取 `content_guard_enabled`。关闭内容防护并不会让历史 blocked/低评分 provider 恢复路由。【已完成：已修复，路由内容策略读取全局总开关，关闭时不再因历史内容完整性状态排除候选】
    
8.  API Key 和用户端仍暴露“内容检测要求/可选”局部开关 [app/templates/api\_keys.html](./app/templates/api_keys.html:326)、[app/templates/user\_api\_keys.html](./app/templates/user_api_keys.html:295)、[app/static/js/app.js](./app/static/js/app.js:12928) 仍读写 `content_guard_required`，与“只能全局维护可信/内容策略”的规范冲突。【已完成：已修复，管理端隐藏局部内容检测开关，用户端不再展示可选语义，运行时不再用旧字段关闭内容防护】
    
9.  内容防护 typed event 没有独立日志列表和导出 模型已定义 `request_content_guard_events`：[app/models/logging\_events.py](./app/models/logging_events.py:154)，但 [app/routers/logging\_api.py](./app/routers/logging_api.py:158) 之后只提供异常、健康、计费、后台任务、素材等列表，没有内容防护事件列表。【已完成：已核验，当前已存在 /api/logging/content-guard-events、导出分支和前端内容防护日志 Tab】
    
10.  请求详情中的内容防护信息严重不足 [app/static/js/app.js](./app/static/js/app.js:10821) 只显示结果、风险、动作、耗时，隐藏了原因、分类、证据摘录、缓冲等待、重试 provider 数和最终策略，导致日志中心无法复盘内容污染。【已完成：已核验，请求详情内容防护卡片已展示分类、原因、证据、缓冲等待、重试提供商数和最终策略】
    
11.  切 provider 时会为一次用户请求写多条 summary 请求日志 [app/services/proxy\_service.py](./app/services/proxy_service.py:2073) 每次内容命中都会写一条失败 `request_logs`，最终成功时 [app/services/proxy\_service.py](./app/services/proxy_service.py:2133) 又写成功日志。统计会把一次请求膨胀为多条请求，失败率被污染。【已完成：已修复，内容防护切提供商中间尝试不再写 summary 请求日志，仅保留 trace/typed evidence，最终结果仍写单条请求日志】
    
12.  流式内容污染会同时标记健康失败 [app/services/proxy\_service.py](./app/services/proxy_service.py:3445) 内容完整性中断后进入 `_mark_failure_async`，把“内容污染”混进 provider/model 健康失败口径，健康异常与内容异常边界不清。【已完成：已修复，流式内容完整性中断不再调用 provider/model 健康失败标记】
    
13.  L3 内容完整性定时任务关闭后仍注册调度 [app/tasks.py](./app/tasks.py:518) 始终注册 `model_l3_content_integrity_health_check`，任务内部再返回空列表。语义上更像“空跑”，后台任务日志会出现运行记录但没有真实检查，容易误导运维判断。【已完成：已修复，只有内容防护总开关和自动预检开关同时开启时才注册 L3 内容完整性调度，否则移除该 job】
46.  现有回归脚本已经失效：`stage33_content_guard_regression_check.py` 仍断言“固定答案 + 外链广告”通过即可可信，但当前实现已要求再包含“流式污染检测”。实际运行失败在 [stage33\_content\_guard\_regression\_check.py](./stage33_content_guard_regression_check.py:274)，而新判断在 [content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1005)。【已完成：已核验，当前回归脚本已按固定答案、外链广告识别、流式污染检测三项必需探针断言可信状态】
    
47.  三层架构仍未真正落地：`proxy_service.py` 继续直接调用 `ContentGuardService.inspect_json_response / inspect_sse_event / inspect_response_text`，绕过 `ContentRuntimeGuardService`，见 [proxy\_service.py](./app/services/proxy_service.py:1080) 和 [proxy\_service.py](./app/services/proxy_service.py:1219)。【已完成：已核对，代理运行时检测入口已统一转调 ContentRuntimeGuardService，未再直接调用 inspect_json_response / inspect_sse_event / inspect_response_text】
    
48.  `ContentRuntimeGuardService` 反向依赖 `ProxyService`，运行时防护层反过来 import 代理层的 SSE 解析、错误构造和写库方法，职责倒挂，见 [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:73)、[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:197)。【已完成：已修复，运行时防护层内聚 SSE 数据解析、限长缓冲、错误详情构造和违规落库，不再反向 import ProxyService】
    
49.  `ContentGuardRuleService` 只是继承旧 `ContentGuardService` 的空壳，没有真正承接规则注册、解析、匹配、评分职责，见 [content\_guard\_rule\_service.py](./app/services/content_guard_rule_service.py:6)。【已完成：已修复，ContentGuardRuleService 改为显式规则服务门面，承接规则解析、序列化、匹配、评分、检测与违规记录入口，不再继承旧服务空壳】
    
50.  `ContentGuardModuleService` 已经把 `/probe` 转给 `ContentTrustProbeService`，但文件里仍保留一整套旧的 `_resolve_probe_target/_run_single_probe/_summarize_probe_results` 死代码，后续很容易双口径漂移，见 [content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:223) 和 [content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:231)。【已完成：已修复，删除模块服务内旧探针目标解析、单探针执行、汇总与聚合死代码，探针统一保留在 ContentTrustProbeService】
    
51.  路由层仍重复实现可信判断，没有复用 `ContentTrustProbeService.get_trust_decision_for_route`。重复逻辑在 [router\_service.py](./app/services/router_service.py:1124)，而服务层已有同类方法在 [content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:100)。【已完成：已核对，路由内容策略诊断与 provider_model 阻断判断已复用 ContentTrustProbeService.get_trust_decision_for_route】
    
52.  内容防护总开关闭合不完整：代理检测会停，但路由仍按 `trust_level/content_integrity_score/content_integrity_status` 排除历史 blocked/低分提供商，见 [router\_service.py](./app/services/router_service.py:236) 和 [router\_service.py](./app/services/router_service.py:1127)。【已完成：已核对，路由内容策略在全局 content_guard_enabled 关闭时返回放行，不再用历史内容状态排除候选】
    
53.  API Key 级 `content_guard_required` 仍在模型、schema、管理端、用户端和路由上下文里维护，违反“只能全局维护”的规范。入口在 [api\_key\_service.py](./app/services/api_key_service.py:488)，前端在 [api\_keys.html](./app/templates/api_keys.html:324)。【已完成：已修复，移除 API Key 和策略模板的 schema、ORM 映射、查询过滤、序列化、鉴权缓存、路由上下文、用户端和管理端前端入口；旧库已有列不再被代码读取】
    
54.  更糟的是 `content_guard_required=false` 并不会真正关闭内容检测，代理判断里如果 API Key 标记“可选”，仍返回 provider 的 `content_guard_enabled`，见 [proxy\_service.py](./app/services/proxy_service.py:1117)。【已完成：已修复，API Key 局部字段不再存在于运行时策略链路，内容防护是否执行仅由全局开关与提供商内容防护开关决定】
    
55.  `low_trust_route_enabled` 仍存在于模型/schema/序列化，但路由完全不读它，是一个“看起来能配、实际不生效”的遗留字段，见 [provider.py](./app/models/provider.py:42)、[provider\_service.py](./app/services/provider_service.py:1248)。【已完成：已修复，移除 Provider ORM 映射和服务写入中的 low_trust_route_enabled，避免继续形成可配置但无效的策略字段】
    
56.  内容防护页和提供商页可信状态口径不一致：内容防护概览按 provider 硬字段统计，见 [content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:37)；提供商页按挂载模型汇总可信状态，见 [provider\_service.py](./app/services/provider_service.py:1152)。【已完成：已核对，内容防护页 provider 序列化已复用 ProviderService.provider_trust_summary 输出 trust_status、trust_status_label、trust_status_reason，与提供商页口径一致】
    
57.  内容防护页只序列化 provider/model 的硬字段，不包含 `computed_trust_status/trust_status_reason`，而提供商页会展示这些字段，跨页天然不一致，见 [content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:175)、[provider\_service.py](./app/services/provider_service.py:1492)。
    
58.  typed 内容防护事件没有独立列表接口，只有请求时间线会混入 “内容防护”。`logging_api.py` 有 `/request-logs/{id}/timeline`，但没有 `/content-guard-events`，路由清单只到 request/exception/health/billing/background/admin/user/asset，见 [logging\_api.py](./app/routers/logging_api.py:93)。
    
59.  内容防护证据字段仍是空的：代理写 typed event 时 `matched_rules_json` 固定 `None`，无法回溯具体命中规则，见 [proxy\_service.py](./app/services/proxy_service.py:2052) 和 [proxy\_service.py](./app/services/proxy_service.py:3365)。
    
60.  内容防护相关 typed 表没有正式迁移覆盖。模型里有 `request_content_guard_events/health_probe_events`，但两个内容防护迁移只改了 `providers/provider_models/request_logs/app_settings/api_client_keys`，见 [logging\_events.py](./app/models/logging_events.py:154) 和 [2026-06-06\_add\_content\_guard\_governance.sql](./migrations/2026-06-06_add_content_guard_governance.sql:4)。
    
61.  内容规则仍可绕过：`bad-example。invalid/ad` 全角句号域名、`加 微 信 abc123` 这种空格拆分联系方式不会命中。位置：`app/services/content_guard_service.py:222`、`:685`。
    
62.  用户明确要求“不要提供链接”时，普通外链只会得到 `review/medium`，不是阻断；默认 `unexpected_link` 规则是 `medium + record`。位置：`app/services/content_guard_service.py:235` 附近。
    
63.  请求体里的域名会被自动加入允许域名，提示词里提到恶意域名会放行同域输出。位置：`app/services/content_guard_service.py:639`。
    
64.  `image_url` 被通用扫描跳过，Responses 输出图片 URL 带推广/外链时可逃过内容防护。位置：`app/services/content_guard_service.py:1025`、`:1075-1080`。
    
65.  外部探测存在 SSRF 风险：schema 只校验 `http/https`，不拦截 `127.0.0.1`、`localhost`、内网、云元数据地址；实际请求直接拼 URL 并带 Bearer。位置：`app/schemas/content_guard.py:93`、`app/services/proxy_service.py:3564`。
    
66.  内容防护总开关关闭后，手动探针仍可执行并持久化内部模型状态；`run_probe`/`run_trust_probe` 没有检查 `content_guard_enabled`。位置：`app/routers/content_guard.py:142`、`:164`。
    
67.  前端允许只勾选部分探针，但内部探针默认 `persist_internal_result: true`；后端状态判定又要求三项必需探针齐全，单项测试会污染模型可信状态为 `review`。位置：`app/static/js/app.js:9003`、`app/services/content_guard_probe_service.py:996`。
    
68.  非流式和流式 typed event 的 `matched_rules_json` 永远写 `None`，无法追溯具体命中的规则。位置：`app/services/proxy_service.py:1927`、`:3240`。
    
69.  `provider_status_after` 写的是 `content_guard_final_strategy`，不是 provider 真实状态，字段名和内容不一致。位置：`app/logging/adapters/request_adapter.py:286`、`app/services/proxy_service.py:3244`。
    
70.  已有 `RequestContentGuardEvent` 表模型，但没有独立列表接口、前端 Tab、导出支持；日志中心只能从 `request_logs` 摘要字段看内容防护。位置：`app/models/logging_events.py:154`、`app/routers/logging_api.py:93-509`。
    
71.  `/api/content-guard/runtime/events` 只是查 `request_logs` 最近 100 条，没有分页、筛选、导出，也没有使用 `request_content_guard_events`。位置：`app/routers/content_guard.py:102`。
    
72.  生产迁移不完整：`request_content_guard_events` 等 typed 日志表没有独立 SQL migration；生产环境 `init_database` 默认直接 return，不会 `create_all` 或补列。位置：`app/main.py:76`、`migrations/`。
    
73.  路由内容完整性筛选没有读取全局 `content_guard_enabled`，总开关关闭后仍可能因历史 `blocked/score<=20` 排除候选。位置：`app/services/router_service.py:1121`、`app/services/content_trust_probe_service.py:100`。
    
74.  API Key 级 `content_guard_required` 仍保留在模型、schema、UI、鉴权上下文中，违背“全局-only 可信策略”。位置：`app/services/api_key_service.py:494`、`app/templates/api_keys.html:326`。
    
75.  可信探针成功路径只更新模型并失效缓存，不刷新 provider 聚合状态；失败路径才调用 `ProviderService.refresh_provider_state(provider)`。位置：`app/services/content_guard_probe_service.py:1083-1090`、`:1095-1105`。
    
76.  监控告警在普通评估阶段就写“已自动隔离”，但真正隔离只有刷新告警/写告警时才执行，展示语义可能先于事实。位置：`app/services/system_metrics_service.py:1277`、`:185`。
    
77.  内容防护页面仍出现“渠道”文案，违反项目统一术语“提供商”。位置：`app/templates/content_guard.html:147`、`:152`、`:246`。
    
78.  内容防护规则服务只是继承旧服务的空壳，没有真正独立的规则注册/解析/评分边界。位置：`app/services/content_guard_rule_service.py:6`。

79.  内容防护 overview 已返回完整提供商/模型治理数据，但前端只把它塞进下拉框，没有治理表、筛选、恢复、批量处理、证据查看入口。接口在 [app/services/content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:31) 返回 `providers`，前端只在 [app/static/js/app.js](./app/static/js/app.js:8905) 渲染为 `<option>`。
    
80.  内容防护规则编辑会丢失规则原因。`normalizeRuleForView` 保留 `reason`，但规则表没有 reason 输入列，`collectRules()` 又固定写 `reason: ""`，保存一次就清空历史原因。见 [app/static/js/app.js](./app/static/js/app.js:8802) 和 [app/static/js/app.js](./app/static/js/app.js:8873)。
    
81.  内容防护规则表是纯前端全量渲染，没有服务端分页/虚拟滚动/搜索。`renderRules()` 直接把 `state.rules` 全部拼 HTML，违反数据列表服务端分页规范。见 [app/static/js/app.js](./app/static/js/app.js:8805)。
    
82.  检测结果的异常原因长文本直接进入表格单元格，没有收纳到统一 Tooltip。见 [app/static/js/app.js](./app/static/js/app.js:9059) 和 [app/static/js/app.js](./app/static/js/app.js:9066)。
    
83.  内容防护页帮助按钮使用原生 `title`，没有复用全站帮助 Tooltip 层，和项目规范冲突。模板大量 `settings-help-btn title=...`，例如 [app/templates/content\_guard.html](./app/templates/content_guard.html:62)。
    
84.  “可信探测”页面按钮实际调用的是 `/api/content-guard/precheck/probe`，后端转到 `run_capability_probe`，不是 `run_trust_probe`。页面标题和真实行为不一致，用户以为在做可信判定，实际可能只是能力探针。见 [app/static/js/app.js](./app/static/js/app.js:9186) 和 [app/services/content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:224)。
    
85.  内容防护总开关关闭时，前端仍允许本地检测和手动探针，控件没有禁用或弱化提示。后端此前也已发现仍可执行，这里是前端语义继续放大误解。见 [app/static/js/app.js](./app/static/js/app.js:8934) 到 [app/static/js/app.js](./app/static/js/app.js:9202)。
    
86.  外部检测表单要求浏览器输入并提交外部 API Key，这对后台管理页来说风险偏高，且与“浏览器不应直接持有外部 API Key”的既有规范方向不一致。见 [app/templates/content\_guard.html](./app/templates/content_guard.html:260) 和 [app/static/js/app.js](./app/static/js/app.js:9008)。
    
87.  日志中心已有 `RequestContentGuardEvent` 表和 registry 映射，但前端没有“内容防护日志”Tab，后端也没有独立列表接口。registry 在 [app/logging/registry.py](./app/logging/registry.py:36)，前端 typed 配置只有异常、健康、计费、后台任务、素材，见 [app/static/js/app.js](./app/static/js/app.js:2451)。
    
88.  类型化日志导出同样不支持内容防护日志。`export_typed_logs()` 只处理五类，其他直接 400。见 [app/routers/logging\_api.py](./app/routers/logging_api.py:310) 和 [app/routers/logging\_api.py](./app/routers/logging_api.py:375)。
    
89.  内容防护 typed event 字段设计有 `guard_stage / guard_result / risk_level / matched_rules_json / action`，但日志中心没有对应筛选字段，仍只能在请求日志里按 `content_guard_result/risk_level` 粗筛。表字段见 [app/models/logging\_events.py](./app/models/logging_events.py:154)，请求日志筛选见 [app/templates/logs.html](./app/templates/logs.html:149)。
    
90.  计费日志把 `token_finalize_events` 和 `billing_process_events` 两张结构不同的表合并成一个 Tab，却共用一个“结果状态”筛选，容易出现某些状态对另一类事件恒为空。前端配置见 [app/static/js/app.js](./app/static/js/app.js:2535)，后端合并逻辑见 [app/routers/logging\_api.py](./app/routers/logging_api.py:236)。
    
91.  后台任务日志功能本质是记录被 `@distributed_job_lock` 包裹的调度任务，不是完整后台队列观测。写入只在 [app/tasks.py](./app/tasks.py:199) 的装饰器链路里发生，独立 worker、队列消费、异步日志落库等后台处理不一定进入 `background_job_events`。
    
92.  后台任务日志 `trigger_type` 基本固定为默认 `scheduler`，装饰器写入时没有把手动/启动/系统触发来源传下去，前端的“触发方式”筛选会失真。默认值在 [app/logging/adapters/background\_job\_adapter.py](./app/logging/adapters/background_job_adapter.py:19)，调用处未传入见 [app/tasks.py](./app/tasks.py:105)。
    
93.  后台任务先写 `running` 再更新同一行；如果进程在运行中崩溃，记录会永久停在 running，没有心跳、超时转失败或 stale 标记。创建 running 在 [app/tasks.py](./app/tasks.py:253)，完成更新在 [app/tasks.py](./app/tasks.py:181)。
    
94.  Redis 分布式锁不可用时会 `unavailable_fallback` 继续执行，但 `BackgroundJobLogRecorder` 仍把非 failed/skipped 结果标为 success，锁系统故障不会在日志结果层突出。见 [app/tasks.py](./app/tasks.py:216) 和 [app/logging/adapters/background\_job\_adapter.py](./app/logging/adapters/background_job_adapter.py:25)。
    
95.  后台任务日志关键词只查 `job_run_id/job_name/lock_key/error`，不查 `result_summary_json`，很多任务实际处理对象、数量、清理结果无法搜索。见 [app/routers/logging\_api.py](./app/routers/logging_api.py:429)。
    
96.  类型化日志 CSV 导出没有稳定表头；字段顺序由当前 items 动态推导，空结果会导出空表头，不适合长期审计。见 [app/routers/logging\_api.py](./app/routers/logging_api.py:598)。
    
97.  用户日志页后端不支持内容防护筛选参数，用户无法按自己的请求排查内容防护命中。管理员请求日志支持该筛选，但用户接口 [app/routers/user\_portal.py](./app/routers/user_portal.py:1075) 到 [app/routers/user\_portal.py](./app/routers/user_portal.py:1124) 没有接收/传递 `content_guard_result` 与 `content_guard_risk_level`。
    
98.  用户日志导出同样忽略内容防护筛选，即使前端或 URL 带了参数也不会生效。见 [app/routers/user\_portal.py](./app/routers/user_portal.py:1182)。
    
99.  自动内容预检的直接服务方法只判断 `content_guard_enabled`，不判断 `content_guard_precheck_auto_enabled`；虽然调度 wrapper 判断了，但其它调用方可绕过自动预检开关。见 [app/tasks.py](./app/tasks.py:421) 和 [app/services/health\_service.py](./app/services/health_service.py:989)。
    
100.  已 blocked 的模型会被 `_should_run_scheduled_content_probe()` 继续判定为需要自动预检，造成被隔离对象仍反复探测；而成功路径又不会恢复 blocked 状态，形成无效消耗。见 [app/services/health\_service.py](./app/services/health_service.py:2175) 和 [app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1082)。

101.  监控自动隔离并不自动执行。`SystemMetricsService.collect()` 只有 `refresh_alerts=True` 才写告警并触发隔离，但健康检查、告警服务、默认系统指标接口都传 `False`，运维页自动轮询也传 `false`。证据：[system\_metrics\_service.py](./app/services/system_metrics_service.py:163)、[metrics.py](./app/routers/metrics.py:53)、[app.js](./app/static/js/app.js:15614)。
    
102.  告警文案会提前宣称“已自动隔离”，但实际隔离只在写告警动作里发生，普通指标响应只评估不处置，前端看到的状态会误导管理员。证据：[system\_metrics\_service.py](./app/services/system_metrics_service.py:1288)。
    
103.  内容防护高风险统计以 `request_logs` 为唯一来源，如果污染发生但请求日志未落库、异步队列积压、或只写了 typed event，自动隔离不会触发。证据：[system\_metrics\_service.py](./app/services/system_metrics_service.py:948)。
    
104.  `content_guard_result=review` 被纳入“违规率”，但高风险隔离只看 `block` 或 `risk_level=high`，中风险长期堆积不会升级为处置。证据：[system\_metrics\_service.py](./app/services/system_metrics_service.py:935)、[system\_metrics\_service.py](./app/services/system_metrics_service.py:959)。
    
105.  `record_only` 策略会把所有命中都改成 `record`，`should_block_for_response()` 不阻断，但 `should_record_violation()` 仍会记录 block/error，策略语义和状态惩罚可能不一致。证据：[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:172)、[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:244)。
    
106.  `async_review` 只是返回 action，没有看到独立复核队列或后台任务；同时 `should_record_violation()` 在异步复核开启时不记录 review，导致“需复核”既不复核也不沉淀。证据：[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:182)、[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:247)。
    
107.  `safe_error` 与 `block` 对外阻断行为基本相同，只是策略名不同，缺少独立的安全兜底响应语义。证据：[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:189)。
    
108.  `content_guard_required=false` 并不会真正关闭请求检测，只要提供商 `content_guard_enabled` 为真仍继续检测；API Key 级字段语义不清。证据：[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:226)。
    
109.  `_with_content_guard_route_policy()` 直接返回原上下文，没有根据图片、工具、端点等请求特征补充可信策略，等于预留函数未实现。证据：[proxy\_service.py](./app/services/proxy_service.py:236)。
    
110.  流式适配链路完全跳过内容防护：只要发生 Chat/Responses/Completions 互转，`stream_guard_enabled` 就被置 false。证据：[proxy\_service.py](./app/services/proxy_service.py:2761)。
    
111.  `full_buffer` 模式把 `max_delay_seconds` 设为 `None`，会一直等到缓冲上限或流结束，长流式响应可能首包长时间不返回。证据：[proxy\_service.py](./app/services/proxy_service.py:2777)、[proxy\_service.py](./app/services/proxy_service.py:1183)。
    
112.  300ms 内没有拿到可检测内容时直接 pass，后续虽然有分块检测，但首段“无内容”本身没有区分上游慢、空流、心跳或异常格式。证据：[proxy\_service.py](./app/services/proxy_service.py:1233)。
    
113.  流式分块检测在 `event_buffer` 达到扫描上限后直接清空，跨窗口拆开的污染文本可能被切断绕过。证据：[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:155)。
    
114.  SSE data 非 JSON 会被直接判定为高风险阻断，但部分上游可能发送非 JSON 心跳、注释或兼容事件，误杀风险高。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:471)。
    
115.  探针成功后不会恢复已经 `blocked` 的模型，只在非 blocked 时改为 `passed`；更不会恢复提供商 `blocked` 或评分。证据：[content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1082)。
    
116.  路由排除 provider 时同时看 `content_integrity_status=blocked` 和 `content_integrity_score<=20`，但成功探针不提升 provider 分数，低分提供商可能永久无法回到候选。证据：[content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:113)、[content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:115)。
    
117.  内容防护 typed event 表存在 `request_content_guard_events`，但后端日志中心没有对应列表接口，只有 timeline 会带出来。证据：[logging\_events.py](./app/models/logging_events.py:154)、[logging\_api.py](./app/routers/logging_api.py:93)。
    
118.  前端 typed log 配置没有内容防护日志 tab，因此内容防护事件无法按独立类型筛选、分页、导出。证据：[app.js](./app/static/js/app.js:2451)。
    
119.  `matched_rules_json` 字段在模型里存在，但代理写 typed event 时固定写 `None`，导致日志无法追溯命中的具体规则。证据：[logging\_events.py](./app/models/logging_events.py:164)、[proxy\_service.py](./app/services/proxy_service.py:1927)。
    
120.  `provider_status_after` 被写成 `final_strategy`，不是 provider 真实状态，字段名与内容不一致。证据：[proxy\_service.py](./app/services/proxy_service.py:3244)。
    
121.  日志页初始化强依赖大量 DOM 节点，任意一个缺失会直接 `return`，会导致整个日志中心按钮全部不绑定，看起来“点不动”。证据：[app.js](./app/static/js/app.js:13620)。
    
122.  typed log 后端导出只覆盖异常、健康、计费、后台任务、素材等类型，没有内容防护导出分支。证据：[logging\_api.py](./app/routers/logging_api.py:286)。
    
123.  生产环境默认禁止 `create_all` 和内置迁移，但 migrations 中没有 typed logging 表的建表 SQL，生产库可能没有这些表，导致日志中心接口 500 或显示 0。证据：[main.py](./app/main.py:78)、[logging\_events.py](./app/models/logging_events.py:154)。
    
124.  内容防护规则 JSON 解析失败会静默回退默认规则，管理员保存了坏配置也不会明确报错，实际规则和页面认知可能不一致。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:522)。
    
125.  URL allowlist 会自动把请求 payload 中出现的所有域名加入允许列表，用户 prompt 里只要提到恶意域名，模型输出同域名链接就可能被放行。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:647)。
    

126.  `ContentGuardModuleService.build_overview()` 每次拉取全量提供商、模型、规则和最近 5000 条延迟样本，`/api/content-guard/overview` 与 `/runtime/settings` 都复用它，轻量设置读取也会触发重查询，页面越大越慢。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\services\content_guard_module_service.py`
    
127.  `/api/content-guard/runtime/events` 只返回最近 100 条，没有分页、总数、时间范围、提供商、模型、风险等级、处置动作筛选，也没有导出能力。前端页面实际还没有调用这个接口。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\routers\content_guard.py`
    
128.  已有 `RequestContentGuardEvent` 类型化表，但日志中心没有“内容防护日志”独立 tab，也没有对应 `/api/logging/content-guard-events` 列表/导出接口，只能混在请求日志或 timeline 里看。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\static\js\app.js`
    
129.  规则编辑器一次性渲染全部规则，服务端上限 200 条，字段又很密集；没有服务端分页、搜索、分类筛选、折叠详情，违反项目高密度列表必须服务端分页和渐进披露的规范。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\static\js\app.js`
    
130.  规则的 `reason` 字段服务端模型支持，但前端保存时固定写成空字符串，导致规则命中说明被清空，审计、检测详情和后续排查都会失真。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\static\js\app.js`
    
131.  正则规则保存时不预编译校验，运行时 `re.error` 被直接吞掉，管理员会看到“保存成功”，但规则实际不生效。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\services\content_guard_service.py`
    
132.  前端分类是下拉框，无法输入新的自定义分类；但服务端 schema 允许任意 `category`。这造成“接口支持自定义，页面不支持自定义”的能力断裂。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\static\js\app.js`
    
133.  内容防护总开关关闭后，前端仍允许编辑规则、执行本地检测、手动探针；后端 `inspect_text()` 也不检查 `content_guard_enabled`，用户会以为模块已关闭，但检测功能仍继续产出结果。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\services\content_guard_module_service.py`
    
134.  可信探测里的外部渠道要求浏览器输入 API Key，但页面没有展示任何“仅本次使用/不落库/风险提示”，也没有脱敏回显说明；对管理员来说安全边界不清楚。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\templates\content_guard.html`
    
135.  外部渠道字段用 `visibility:hidden` 叠在同一 grid 区域，隐藏区域仍影响布局高度，也可能引发可访问性和焦点顺序问题。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\static\css\app.css`
    
136.  `settings-help-btn` 虽然启动时会把 `title` 转成统一浮层，但模板首屏仍大量写原生 `title`；如果 JS 初始化失败或局部加载顺序异常，就会退回浏览器原生 tooltip，和全站统一帮助浮层规范不一致。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\templates\content_guard.html`
    
137.  自动预检 job 已独立于全局健康检查开关，这是正确的；但 scheduler 总是注册该 job，即使关闭预检也只是 job 内返回空数组，后台任务日志会出现“任务存在但无业务处理”的噪音。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\tasks.py`
    
138.  被 `blocked` 的模型会被 `_should_run_scheduled_content_probe()` 每轮继续选中；如果上游长期异常或规则误杀，会形成持续探测和持续失败记录。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\services\health_service.py`
    
139.  探针成功时不会自动解除 `blocked` 状态，只在非 blocked 时改为 `passed`，导致恢复路径依赖管理员手动改状态；自动预检无法完成“失败隔离、成功恢复”的闭环。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\services\content_guard_probe_service.py`
    
140.  `matched_rules_json` 在非流式和流式 typed event 中固定写 `null`，虽然模型表有字段，实际无法按命中规则追踪、筛选或统计。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\services\proxy_service.py`
    
141.  `provider_status_after` 被写成 `content_guard_final_strategy`，字段名表达“处置后提供商状态”，实际保存“策略名称”，语义错位。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\logging\adapters\request_adapter.py`
    
142.  生产环境 `init_database()` 默认跳过 `Base.metadata.create_all()` 和大部分补表逻辑；如果没有独立迁移，`request_content_guard_events` 等类型化日志表线上可能不存在，日志接口会直接不可用或显示 0。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\main.py`
    
143.  内容防护相关查询只给 `request_logs` 建了通用索引，没有针对 `content_guard_result / risk_level / created_at / provider_id` 的复合索引；日志中心按内容风险筛选时会越来越慢。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\main.py`
    
144.  日志中心类型化日志按钮只有五类：异常、健康、计费、后台任务、素材；内容防护作为高风险治理模块却没有独立入口，运营视角缺失。 文件：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\templates\logs.html`
    
145.  本轮未做代码修改、未启动项目，只继续审查并补充问题。下一步应继续扫内容防护与日志中心的接口落库链路、前端初始化失败路径、以及生产迁移缺口。
    

46 previous messages
146.  `content_guard_required=false` 文案误导。API Key 前端显示“内容检测可选”，但路由仍会排除 `blocked`、内容完整性 `blocked`、低分提供商；它不是“可选内容防护”，只是影响部分运行时检测语义。证据：[app/static/js/app.js](./app/static/js/app.js:12332)、[app/services/content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:100)。
    
147.  `_with_content_guard_route_policy()` 是空实现，`payload`、`endpoint_path`、`has_image`、`require_tools` 等入参完全没有参与策略调整，复杂请求不会动态提升可信要求。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:236)。
    
148.  内容防护设置/规则更新只清 `runtime-settings`，没有清 `route-candidates`、`providers-runtime`、`v1-models`，路由候选可能继续使用旧内容完整性策略。证据：[app/services/content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:124)、[app/services/setting\_service.py](./app/services/setting_service.py:179)。
    
149.  路由候选缓存 key 只包含 `content_guard_required`，不包含全局 `content_guard_enabled`、规则版本、provider 内容状态版本；缓存失效不严谨时会产生候选不一致。证据：[app/services/router\_service.py](./app/services/router_service.py:1003)。
    
150.  provider 级内容违规会打开 `provider.circuit_state`，但 `Provider` 模型没有 `circuit_opened_at`，恢复和审计时间只能在模型级看到。证据：[app/services/content\_guard\_service.py](./app/services/content_guard_service.py:773)、[app/models/provider.py](./app/models/provider.py:50)。
    
151.  流式内容防护错误事件内部 `status_code` 固定按 500 分类，而非流式内容防护错误使用 502，错误分类口径不一致。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:6534)。
    
152.  流式分块先做内容检测，再收集日志文本和 usage；命中后直接返回 error，违规 chunk 不会进入 `response_text_parts`，日志证据可能缺失。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:2964)。
    
153.  `pass_through_scan` 名称容易误导。代码只是关闭首段缓冲，后续仍会逐 chunk 扫描并拦截；配置语义和实际行为不清。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:2768)。
    
154.  默认 URL 白名单过宽，`google.com`、`github.com`、`microsoft.com` 等及其所有子域都会被放行，广告/跳转页挂在这些域名时天然绕过异常外链规则。证据：[app/services/content\_guard\_service.py](./app/services/content_guard_service.py:113)、[app/services/content\_guard\_service.py](./app/services/content_guard_service.py:703)。
    
155.  请求 payload 中出现的域名会自动加入允许列表，因此用户 prompt 里包含任意外链，响应中同域外链会被视为授权域名，和“未授权外链检测”目标冲突。证据：[app/services/content\_guard\_service.py](./app/services/content_guard_service.py:647)。
    
156.  `_request_allows_advertising()` 只靠简单中文正则判断用户是否允许广告/链接，用户说“生成推广文案/链接”就会降低外链拦截，边界过宽。证据：[app/services/content\_guard\_service.py](./app/services/content_guard_service.py:1121)。
    
157.  `_extract_scan_text()` 跳过所有 `image_url` 键，响应中的图片 URL、重定向 URL、外部素材 URL 不进入内容检测。证据：[app/services/content\_guard\_service.py](./app/services/content_guard_service.py:1025)。
    
158.  JSON 模式结构检查只校验合并后的文本能否 `json.loads`，没有按请求里的 schema 校验字段、类型、额外字段，结构化污染检测偏弱。证据：[app/services/content\_guard\_service.py](./app/services/content_guard_service.py:858)。
    
159.  Responses 工具调用检测只识别 `output[].type == "function_call"` 且 `arguments` 在 item 顶层的形态，嵌套或兼容变体可能漏检。证据：[app/services/content\_guard\_service.py](./app/services/content_guard_service.py:895)。
    
160.  流式分块所谓“滑动窗口”达到 `max_scan_bytes` 后直接清空，不是连续滑动窗口；跨窗口拼接的污染文本可能漏检。证据：[app/services/content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:130)。
    
161.  流式首段预检在 300ms 内没收到内容会放行，后续才检测；低信任 provider 首个有效 SSE 事件如果超过等待窗口，首段风险仍会先下游暴露。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:1165)。
    
162.  内容防护命中事件的 `matched_rules_json` 固定写 `null`，日志无法追溯具体规则 ID/名称，只能看分类和 reason。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:1927)。
    
163.  `provider_status_after` 在流式事件里写的是 `guard_result.final_strategy`，字段名像 provider 状态，实际是处理策略，语义错位。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:3244)。
    
164.  `RequestContentGuardEvent` 已注册，但日志中心没有独立“内容防护日志”tab/API 导出入口，只能混在请求日志或 timeline 里看。证据：[app/models/logging\_events.py](./app/models/logging_events.py:154)、[app/templates/logs.html](./app/templates/logs.html:11)、[app/static/js/app.js](./app/static/js/app.js:2453)。
    
165.  blocked 模型会持续被自动内容预检选中，但预检通过后 `apply_content_probe_health()` 明确不把 blocked 改回 passed，形成“继续测但不自动恢复”的闭环。证据：[app/services/health\_service.py](./app/services/health_service.py:2174)、[app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1083)。
    
166.  自动内容预检失败后会把 provider 置为 `content_integrity_status=blocked`、`circuit_state=open`，但成功路径只更新 provider\_model，不恢复 provider 级 blocked/degraded 状态。证据：[app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1092)。
    
167.  外链广告识别探针只有普通问答、知识问答、翻译三类短 prompt，覆盖不了常见代码块、Markdown、工具参数、图片 URL、长上下文尾注广告等污染形态。证据：[app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:648)。
    
168.  SSE 探针最多读取 6 个 chunk 就判断，长尾污染、慢速尾巴、分段广告可能在第 6 个 chunk 后出现而漏检。证据：[app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:512)。
    
169.  前端规则保存时把 `reason` 固定写空字符串，服务端 schema 支持 `reason`，但页面没有编辑入口，已有规则原因会被保存操作抹掉。证据：[app/static/js/app.js](./app/static/js/app.js:8873)、[app/schemas/content\_guard.py](./app/schemas/content_guard.py:44)。
33 previous messages
170.  `ContentGuardModuleService` 里保留了一整套已不再使用的探针解析与汇总实现，实际入口已经转到 `ContentTrustProbeService`。后续维护者很容易改错服务，形成“双实现”维护陷阱。 证据：`app/services/content_guard_module_service.py:224-229` 转调新服务；同文件后续仍保留 `_resolve_probe_target`、`_run_single_probe`、`_summarize_probe_results` 等旧实现。
    
171.  规则编辑器允许把所有规则删空并保存。前端 `validateRules()` 只遍历已有规则，不校验空数组；后端 `ContentGuardRulesUpdate.rules` 也允许空列表。保存后规则 JSON 为 `[]`，不会回退默认规则，文本污染检测会大面积失效。 证据：`app/static/js/app.js:8878-8891`、`app/schemas/content_guard.py:72`、`app/services/content_guard_service.py:522-547`
    
172.  自定义正则保存时不做预编译校验，运行时 `re.search` 报错后静默跳过。管理员会以为规则有效，实际永远不命中。 证据：`app/services/content_guard_service.py:377-384`、`app/static/js/app.js:8878-8891`
    
173.  新增规则默认 pattern 是“待填写”，如果管理员直接保存，会生成一条真实匹配“待填写”的规则，而不是占位符，属于错误默认值。 证据：`app/static/js/app.js:9083-9095`
    
174.  规则 UI 没有 `reason` 展示和编辑入口。后端规则模型支持 `reason`，但前端收集时固定写空字符串，导致规则原因不可维护。 证据：`app/templates/content_guard.html:307-319`、`app/static/js/app.js:8812-8848`、`app/static/js/app.js:8873`
    
175.  `/runtime/inspect-text` 不检查全局 `content_guard_enabled`，总开关关闭后测试接口仍会返回命中结果，容易造成“正式链路已关但页面检测仍报警”的语义不一致。 证据：`app/routers/content_guard.py:93-98`、`app/services/content_guard_module_service.py:148-173`
    
176.  `parse_rules_json()` 遇到损坏 JSON 直接回退默认规则，没有告警、审计或前端提示。配置漂移会被隐藏，管理员无法知道自定义规则已失效。 证据：`app/services/content_guard_service.py:522-529`
    
177.  URL 白名单解析对非法结构过于宽松。传入 JSON 对象等非 list 时会退化成按文本行解析，前端也没有域名合法性预检，容易把错误配置悄悄保存。 证据：`app/services/content_guard_service.py:628-645`、`app/static/js/app.js:8999`
    
178.  内容防护页的外部渠道 API Key 只是普通密码框，没有显示/隐藏、清空确认、提交后清除 DOM 值等处理，安全体验边界弱。 证据：`app/templates/content_guard.html:265-267`、`app/static/js/app.js:9008-9022`
    
179.  内容防护页表单标题偏“可信探测”，但实际调用 `/api/content-guard/precheck/probe`，这是能力探针；真正强制必需项的 `/trust-probe` 页面没有用。用户会混淆“能力检测”和“可信检测”。 证据：`app/static/js/app.js:9182-9187`、`app/routers/content_guard.py:152-180`、`app/services/content_trust_probe_service.py:25-36`
    
180.  规则重置仍使用 `window.confirm`，没有复用项目已有统一确认弹窗，交互风格和前端规范不一致。 证据：`app/static/js/app.js:9123-9124`
    
181.  文本规则的 `keyword_any` 使用简单子串匹配，没有词边界、归一化策略说明或误报控制。短关键词可能误伤正常文本，尤其中文/英文混排和 URL 场景。 证据：`app/services/content_guard_service.py:372-375`
    
182.  Responses 响应扫描字段覆盖不完整，只读取 `arguments`、`delta`、`content.text/output_text/delta`，没有覆盖 annotations、citations、refusal、reasoning、metadata 等可能承载污染内容的字段。 证据：`app/services/content_guard_service.py:1084-1095`
    
183.  SSE 探针最多读取 6 个 chunk 就判断，长尾污染、慢速插入广告、终止符后延迟污染都可能漏检。 证据：`app/services/content_guard_probe_service.py:535-553`
    
184.  SSE 探针把 `data:` 之外的行基本忽略，对 `event:`、`id:`、`retry:`、注释行等异常组合没有结构化校验，可能误判不规范 SSE 为通过。 证据：`app/services/content_guard_probe_service.py:585-594`
    
185.  必需可信探针被跳过时只作为 `success=false` 计入 review，但没有明确区分“不支持必需项”和“检测失败”。例如模型未启用流式时，可信检测结果只是复核态，路由侧可能无法稳定表达这是必需能力缺失。 证据：`app/services/content_trust_probe_service.py:43-61`、`app/services/content_trust_probe_service.py:243-276`
    
186.  `request_content_guard_events` 模型存在，但迁移文件没有看到创建该表的 SQL，只给 `request_logs` 加了内容防护字段。生产环境按迁移启动时，独立内容防护事件日志可能直接不可用。 证据：`app/models/logging_events.py:154-169`；`migrations/2026-06-06_add_content_guard_governance.sql` 仅扩展字段和索引。
    
187.  内容防护事件有独立记录器 `record_content_guard()`，但提供商“最近内容防护事件”仍从 `request_logs` 汇总字段读，形成事件表与请求日志字段两套口径。 证据：`app/logging/adapters/request_adapter.py:53-54`、`app/services/provider_service.py:1553-1587`
    
188.  内容防护运行时只在 block/error，或 review 且关闭异步复核时记录违规；默认开启异步复核时 review 不记录独立违规处置，后续复核链路容易断档。 证据：`app/services/content_runtime_guard_service.py:244-248`
    
189.  非流式检测通过时只更新 `provider_model.content_probe_last_passed_at`，没有同步 provider 层可信恢复或清理 blocked/degraded 状态，成功信号无法完整反哺路由状态。 证据：`app/services/content_runtime_guard_service.py:52-61`
    
190.  内容防护运行时事件接口 `/runtime/events` 只查 `request_logs` 最近 100 条，无分页、无筛选、无导出，也没有使用独立事件表，和“统一日志管理/独立事件流”设计不一致。 证据：`app/routers/content_guard.py:101-133`
30 previous messages
191.  流式响应只要发生协议适配，就直接关闭内容防护扫描。`stream_guard_enabled` 排除了 `adapt_chat_response_to_responses`、`adapt_chat_response_to_completions`、`adapt_responses_response_to_chat` 三类场景，这些恰恰是代理层常见转换路径，污染内容会绕过流式防护。 证据：`app/services/proxy_service.py:2761-2765`
    
192.  流式 chunk 命中内容防护后，没有向 trace 写入 `content_integrity_violation` provider 尝试项，只在最终日志里写 `request_content_guard`。这会导致 `content_guard_retry_provider_count`、provider 尝试轨迹和复盘链路不完整。 证据：`app/services/proxy_service.py:2972-2997`、`app/services/proxy_service.py:3178-3179`
    
193.  流式 chunk 命中内容防护后会继续按 502 调 `_mark_failure_async()`，把内容污染和普通上游失败混入健康失败口径，可能误伤 provider 健康状态。内容防护应走内容完整性状态，不应等同网络/协议失败。 证据：`app/services/proxy_service.py:3317-3330`
    
194.  预取阶段命中内容防护并启用 `switch_provider` 时，会先写一条失败请求日志，再尝试其它 provider；如果后续 provider 成功，同一个客户端请求可能产生多条请求日志，日志中心会把候选尝试当成独立请求。 证据：`app/services/proxy_service.py:2828-2874`
    
195.  `ContentGuardResult` 没有保存 `matched_rules`，后续日志只能固定写 `matched_rules_json = null`。误报复核时无法知道具体是哪条规则、哪个 pattern 导致阻断。 证据：`app/services/content_guard_service.py:20-45`、`app/services/proxy_service.py:1926-1928`、`app/services/proxy_service.py:3239-3241`
    
196.  规则命中摘要只拼接规则名称，不包含规则 ID、pattern、match\_type、confidence 明细；规则名称一旦重复或被修改，历史复核不可追踪。 证据：`app/services/content_guard_service.py:601-605`
    
197.  可信探针失败会把 provider 层置为 `blocked/open`，但探针成功只恢复 provider\_model；provider 层 `content_integrity_status=blocked` 不会对称恢复，路由仍会按 provider 级 blocked 排除。 证据：`app/services/content_guard_probe_service.py:1082-1091`、`app/services/content_guard_probe_service.py:1094-1103`、`app/services/router_service.py:1125-1130`
    
198.  `record_violation()` 打开 provider 级熔断时没有设置 provider 的 `circuit_opened_at`，只有 provider\_model 设置了时间。后续熔断窗口、恢复判断和审计时间会缺关键字段。 证据：`app/services/content_guard_service.py:781-788`、`app/services/content_guard_service.py:792-794`
    
199.  `record_violation()` 对 review 级别也会直接把 provider/model 标为 `degraded` 并扣分；但 review 默认只是异步复核/记录语义，当前实现会把“待复核”立即沉淀为路由风险状态，可能放大误报影响。 证据：`app/services/content_guard_service.py:777-791`
    
200.  内容防护监控统计仍完全基于 `request_logs` 汇总字段，而不是独立 `request_content_guard_events`。独立事件流存在时，两边统计口径会不一致。 证据：`app/services/system_metrics_service.py:857-987`
    
201.  内容防护事件模型只被 timeline 使用，没有独立列表接口、筛选接口或导出分支；前端日志中心也没有“内容防护”tab。也就是说模型存在，但功能入口不完整。 证据：`app/routers/logging_api.py:44-54`、`app/routers/logging_api.py:286-376`、`app/templates/logs.html:10-17`
    
202.  迁移目录没有任何类型化日志事件表的建表 SQL，包括 `request_content_guard_events`、`exception_events`、`health_check_runs`、`background_job_events`、`asset_events` 等。生产环境只跑迁移时，日志中心多个接口会直接查不存在的表。 证据：`app/models/logging_events.py:10-354`；`migrations/` 仅有 6 个 SQL，搜索不到这些表的 `CREATE TABLE`。
    
203.  `ContentGuardRulesUpdate.rules` 只限制最大 200 条，不限制最少 1 条；前端和后端组合后仍允许空规则集保存，属于服务端兜底缺失。 证据：`app/schemas/content_guard.py:72-83`
    
204.  `ContentGuardRulePayload` 没有按 `match_type=regex` 预编译校验 pattern，只校验非空；非法 regex 可以保存成功，运行时静默跳过。 证据：`app/schemas/content_guard.py:33-69`、`app/services/content_guard_service.py:377-384`
    
205.  外部渠道探针 `ContentGuardExternalTarget.base_url` 只校验 http/https 前缀，不限制内网地址、localhost、metadata IP 等 SSRF 风险目标。管理员误填或被诱导输入时，服务端会主动请求该地址。 证据：`app/schemas/content_guard.py:93-107`、`app/services/content_trust_probe_service.py:133-160`
    
206.  日志中心从 URL 打开类型化 tab 时会先读取 `page/page_size`，但 `setActiveLoggingTab()` 对非请求 tab 又强制 `typedState.page = 1`，导致类型化日志分页 URL 不可恢复。 证据：`app/static/js/app.js:13656-13668`、`app/static/js/app.js:14093-14112`、`app/static/js/app.js:14572-14573`
    
207.  类型化日志 CSV 导出没有中文表头映射、字段筛选或敏感字段控制，直接把 item 的所有 key 输出。内容防护/异常/素材日志一旦包含内部诊断字段，会原样导出。 证据：`app/routers/logging_api.py:286-383`、`app/routers/logging_api.py:598-608`
    
208.  内容防护本地文本检测接口没有把 `endpoint_path` 和 `request_payload` 的复杂结构做大小限制，`request_payload` 是裸 `dict`，可能被管理端传入超大对象拖慢扫描。 证据：`app/schemas/content_guard.py:86-90`、`app/services/content_guard_module_service.py:148-173`
    
209.  `inspect_sse_event()` 对非 JSON 的 SSE `data:` 一律高风险 block，这对上游返回纯文本流、心跳文本或非标准兼容流会非常激进；但前面的探针又忽略 `data:` 外行，SSE 严格性前后不一致。 证据：`app/services/content_guard_service.py:466-499`、`app/services/content_guard_probe_service.py:585-594`
    
210.  内容防护规则的 `action="allow"` 仍会参与命中、计分类别和风险计算，`high_risk` 判断只排除了非 high 且非 block 的情况；高风险 allow 规则仍可能导致 block，动作语义不清晰。 证据：`app/services/content_guard_service.py:308-333`
35 previous messages
211.  JSON 模式只校验“能否 `json.loads`”，不校验请求里的 JSON Schema、required、type、enum、additionalProperties。证据：`D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt\app\services\content_guard_service.py:858`
    
212.  Responses JSON 模式文本抽取覆盖不足，部分新型输出结构会被当成“未发现文本内容”直接通过。证据：`...\content_guard_service.py:867`
    
213.  工具参数污染检测没有传入当前规则、URL 白名单和 URL 开关，会退回默认规则口径。证据：`...\content_guard_service.py:949`
    
214.  Responses 工具调用只识别顶层 `function_call.arguments`，不覆盖嵌套、并行、流式增量、MCP/custom tool 等形态。证据：`...\content_guard_service.py:919`
    
215.  工具参数顶层必须是 JSON object，数组参数会被直接高风险拦截，兼容性偏窄。证据：`...\content_guard_service.py:939`
    
216.  可疑 key 扫描递归全响应，`contact`、`promo` 等正常业务字段也可能触发广告拦截。证据：`...\content_guard_service.py:89`、`:980`
    
217.  域名白名单默认允许所有子域，允许 `example.com` 会放行任意 `*.example.com`，粒度过粗。证据：`...\content_guard_service.py:703`
    
218.  域名规范化失败时直接放行，畸形域名、无点主机、部分 IP/IDN 场景可能漏检。证据：`...\content_guard_service.py:672`
    
219.  域名提取正则不覆盖 IPv4、IPv6、punycode、中文域名，外链污染可绕过。证据：`...\content_guard_service.py:106`
    
220.  本地检测接口先完整检测，再单独匹配规则生成明细，重复计算且结果与明细可能漂移。证据：`...\content_guard_module_service.py:148`
    
221.  overview 同一响应中重复读取 Setting，配置与规则存在读两次、口径不同步的可能。证据：`...\content_guard_module_service.py:61`
    
222.  overview 一次性拉取所有提供商和启用模型，没有分页/搜索，大规模数据下接口膨胀。证据：`...\content_guard_module_service.py:31`
    
223.  内容防护事件表缺 provider/model/path/stream 等关键上下文，独立筛选和审计能力不足。证据：`...\app\models\logging_events.py:154`
    
224.  类型化日志 sink 会按模型字段静默丢弃多余诊断字段，内容防护上下文很容易丢。证据：`...\app\logging\sinks.py:11`
    
225.  从 request summary 补建内容防护事件时仍不填 `matched_rules_json`，规则命中不可追踪。证据：`...\app\logging\adapters\request_adapter.py:267`
    
226.  页面大量用 `panel-card` 承载大区块，与当前全局前端“页面区块不做卡片化”的规范不一致。证据：`...\app\templates\content_guard.html:45`
    
227.  帮助说明使用原生 `title`，没有复用全站统一 Tooltip。证据：`...\app\templates\content_guard.html:62`
    
228.  探测目标切换仍是原生 radio，交互形态与已有分段控制/统一控件方向不一致。证据：`...\app\templates\content_guard.html:239`
    
229.  数字配置用 `Number(...)` 后未处理 `NaN`，提交 JSON 时会变 `null` 并触发后端 422。证据：`...\app\static\js\app.js:8993`
    
230.  外部渠道 API Key 检测成功后不清空输入框，敏感值继续留在 DOM。证据：`...\app\static\js\app.js:9007`、`:9182`
    
231.  探针失败原因直接铺在表格正文，长异常会挤占高密度表格列宽。证据：`...\app\static\js\app.js:9056`
    
232.  `ContentGuardRuleService` 只是空继承壳，规则注册、解析、匹配算法仍在旧服务里，三层职责没有真正拆开。证据：`...\app\services\content_guard_rule_service.py:6`
    
233.  `ContentGuardModuleService` 仍残留旧版私有探针实现，公开入口已转调新服务，后续维护容易改错位置。证据：`...\app\services\content_guard_module_service.py:224`、`:232`
    
234.  运行时非流式检测通过后会更新 `content_probe_last_passed_at`，把正式请求结果混入“探针通过时间”。证据：`...\app\services\content_runtime_guard_service.py:59`
    
235.  Responses 严格 JSON 探针没有使用 Responses 的结构化输出参数，只靠 prompt 约束，测不到真实 JSON 模式能力。证据：`...\app\services\content_guard_probe_service.py:338`
    
236.  SSE 探针只检查事件和 `[DONE]`，没有聚合流式文本并校验是否等于固定答案，错误内容也可能通过。证据：`...\app\services\content_guard_probe_service.py:512`
    
237.  SSE 探针调用检测时没有传当前 `rules_json` 和 `url_allowlist`，与管理员配置不一致。证据：`...\app\services\content_guard_probe_service.py:590`
    
238.  外链广告探针传了规则和 URL 开关，但没传 URL 白名单，仍会走默认白名单。证据：`...\app\services\content_guard_probe_service.py:722`
    
239.  外链广告探针遇到第一个失败场景就返回，无法形成完整场景矩阵，排查证据不完整。证据：`...\app\services\content_guard_probe_service.py:671`
    
240.  `full_buffer` 流式模式并不是真正完整缓冲，只缓冲到 `content_guard_stream_buffer_max_bytes` 上限后继续输出。证据：`...\app\services\proxy_service.py:2776`、`:1129`
    
241.  流式分块检测会消费完整 SSE 事件，跨事件拼接才出现的污染关键词可能漏检。证据：`...\app\services\content_runtime_guard_service.py:141`、`...\app\services\proxy_service.py:8004`
    
242.  协议适配路径会直接关闭流式内容防护，Chat/Responses 转换场景存在绕过面。证据：`...\app\services\proxy_service.py:2761`
    
243.  内容防护错误被构造成 502 且部分场景标记 retryable/recoverable，调用方可能误判为上游临时故障。证据：`...\app\services\proxy_service.py:6556`
    
244.  API Key 的 `content_guard_required` 只影响路由策略语义，运行时检测仍按 provider 开关执行，“可选”语义不清。证据：`...\app\services\content_runtime_guard_service.py:226`
    
245.  自动内容探针按提供商、模型串行执行，缺并发控制和单模型隔离，模型多时调度周期容易被拖垮。证据：`...\app\services\health_service.py:1028`
    
246.  外部探针目标允许任意 http/https 地址且缺少长度上限，SSRF 和超长输入风险仍在。证据：`...\app\schemas\content_guard.py:93`
    
247.  `main.py` 仍在启动期执行 `Base.metadata.create_all` 与大量 `ALTER TABLE`，和生产禁止 Web worker 自动 DDL 的规范冲突。证据：`...\app\main.py:80`、`:287`
    
248.  内容防护路由本身通过 `main.py` 挂载了管理员依赖，所以“单个接口未写 Depends”不是裸奔问题；但这也说明鉴权依赖分散在挂载层，单测或复用 router 时容易遗漏。证据：`...\app\main.py:1732`
28 previous messages
249.  内容防护设置保存只失效 `SettingService.invalidate_runtime_cache()`，没有同步失效 `providers-runtime`、`route-candidates`、`v1-models`，关闭或调整策略后路由缓存可能仍按旧内容完整性状态命中。证据：`app/services/content_guard_module_service.py:118-125`，对比 `app/services/setting_service.py:179-182`
    
250.  规则保存、规则重置同样没有调用 `ProviderService.invalidate_provider_runtime_cache()`，规则变化后候选筛选、状态展示和模型列表可能延迟不一致。证据：`app/services/content_guard_module_service.py:128-145`，`app/services/provider_service.py:1821-1826`
    
251.  `ContentGuardService.requires_trusted_provider()` 已定义复杂请求应走可信提供商，但代理入口 `_with_content_guard_route_policy()` 直接返回原 `route_context`，长上下文、文件、JSON、工具、图片请求不会自动提升可信路由要求。证据：`app/services/content_guard_service.py:259-276`，`app/services/proxy_service.py:236-244`
    
252.  `requires_trusted_provider()` 即使未来接入，也没有判断内容防护总开关；若关闭内容防护，仍可能改变路由策略。证据：`app/services/content_guard_service.py:259-276`
    
253.  `content_guard_required=False` 时运行时仍会按 `provider.content_guard_enabled` 开启检测，API Key 页面“内容检测可选”与实际语义不一致。证据：`app/services/content_runtime_guard_service.py:226-231`，`app/static/js/app.js:12332`
    
254.  运行时高风险处置只处理 `RESULT_BLOCK`，`RESULT_ERROR` 不会按高风险策略阻断或切换提供商，可能被放行。证据：`app/services/content_guard_service.py:752-754`，`app/services/content_runtime_guard_service.py:177-190`
    
255.  `content_guard_async_review_enabled=True` 时 review 不会落 violation，只依赖最终 request log；如果主日志失败或异步丢失，提供商违规计数和状态不会更新。证据：`app/services/content_runtime_guard_service.py:52-58`、`240-246`
    
256.  `record_violation()` 对 `review` 会降级并扣分，但运行时默认 async review 不调用它；同一个 review 在探针和正式请求入口副作用不一致。证据：`app/services/content_guard_service.py:777-785`
    
257.  正式请求违规会写 `content_probe_last_failed_at`、`content_probe_failure_count`，混淆“探针失败”和“运行时污染”。证据：`app/services/content_guard_service.py:788-794`
    
258.  provider 级只写 `circuit_state=open`，没有 provider 级 `circuit_opened_at`，恢复和诊断只能看模型时间。证据：`app/models/provider.py:50`，`app/services/content_guard_service.py:786-787`
    
259.  自动隔离把 `provider.trust_level` 改为 `blocked`，将“信任级别”和“内容完整性状态”耦合；后续恢复完整性时可能仍被 trust level 永久排除。证据：`app/services/system_metrics_service.py:287-292`
    
260.  探针通过时如果模型原本 `blocked` 不恢复状态，却清零失败次数并更新时间，页面会出现“blocked 但最近通过”的矛盾状态。证据：`app/services/content_guard_probe_service.py:1082-1090`
    
261.  探针通过时不恢复 provider 的 `content_integrity_status` / `circuit_state`，provider 被 blocked 后即便模型通过也可能无法回到路由。证据：`app/services/content_guard_probe_service.py:1082-1103`
    
262.  管理员手动标记模型为 passed 时只补固定答案和外链探针，缺少必需的 `content_sse`，可信决策仍可能认为必需探针不完整。证据：`app/services/provider_service.py:1081-1099`，`app/services/content_guard_probe_service.py:1008-1037`
    
263.  `ProviderService.refresh_provider_state()` 只看健康状态刷新 `circuit_state`，可能把内容防护打开的 circuit 覆盖为 closed。证据：`app/services/provider_service.py:1659-1679`
    
264.  自动隔离统计把 `risk_level == high` 也计入，即使该记录是 `record_only` 或未阻断，也可能触发隔离，策略口径不清。证据：`app/services/system_metrics_service.py:948-962`
    
265.  内容防护监控统计仍依赖 `request_logs` 主表，而不是 `request_content_guard_events` typed 表，和统一日志事件中心方向不一致。证据：`app/services/system_metrics_service.py:857-867`
    
266.  `/api/content-guard/runtime/events` 固定返回最近 100 条，无分页、时间范围、provider/model/path/result/risk 筛选，也无导出。证据：`app/routers/content_guard.py:101-133`
    
267.  该接口只判断 `content_guard_result is not null`，pass/review/block 混在一起，不能只看异常或高风险。证据：`app/routers/content_guard.py:101-133`
    
268.  日志中心 typed API `/api/logging/request-logs` 不支持内容防护 result/risk/action 等筛选，内容防护事件只能散落在 timeline。证据：`app/routers/logging_api.py:93-139`
    
269.  通用 `/api/logs` 虽支持 `content_guard_result/risk`，但列表和导出仍来自 `request_logs`，不能导出 typed 事件 payload。证据：`app/routers/logs.py:35-90`、`116-162`
    
270.  主日志落库后再补 typed events，若事件补写失败，主日志和 typed 日志可能不一致。证据：`app/services/log_service.py:324-329`，`app/logging/adapters/request_adapter.py:61-68`
    
271.  内容防护页初始化没有关键 DOM 总 guard，任一模板元素缺失都可能让整页 JS 初始化中断。证据：`app/static/js/app.js:8734-8753`
    
272.  provider 列表为空时只把 select 清空，检测按钮仍可操作，用户只能得到“请选择提供商和模型”，缺少空状态和禁用逻辑。证据：`app/static/js/app.js:8905-8921`
    
273.  切换 provider 后没有保留或校验当前模型选择，也没有禁用无模型场景的检测按钮，可能提交空 model。证据：`app/static/js/app.js:8913-8921`
    
274.  前端保存规则会丢弃 `reason` 字段，`collectRules()` 固定 `reason: ""`。证据：`app/static/js/app.js:8871-8873`，`app/schemas/content_guard.py:44`
    
275.  本地检测前端只提交 `{ text }`，无法传 endpoint、request\_payload、扫描字节和 URL 白名单上下文，和真实请求检测口径不一致。证据：`app/static/js/app.js:9145`，`app/schemas/content_guard.py:86-90`
    
276.  保存设置成功后再 `loadOverview()`，如果刷新失败，会同时出现保存成功和刷新失败，反馈语义混杂。证据：`app/static/js/app.js:9169-9175`
    
277.  探针完成后 `loadOverview()` 会重渲染 provider/options，长探针后用户选择状态容易丢失。证据：`app/static/js/app.js:9182-9193`
    
278.  探针 toast 只读 `summary.content_guard_result`，后端只返回 `status` 时可能显示空/未知。证据：`app/static/js/app.js:9192`
    
279.  `content_guard_block_on_high_risk` 与 `content_guard_high_risk_strategy` 语义重叠，组合状态可能让“切换渠道”实际不生效。证据：`app/templates/content_guard.html:72-84`、`151-156`
    
280.  `pass_through_scan` 会取消首段缓冲，低信任 provider 可能已向客户端输出污染内容，前端没有足够风险提示。证据：`app/templates/content_guard.html:174-177`，`app/services/proxy_service.py:2766-2774`
    
281.  外部探针允许 `http://`，可能把 API Key 发送到明文 HTTP。证据：`app/schemas/content_guard.py:105-106`
    
282.  外部探针 `api_key` 没有最大长度限制，异常超长密钥可能造成内存、审计或日志风险。证据：`app/schemas/content_guard.py:95`
    
283.  内容防护设置审计写完整 payload，URL 白名单等可能敏感；规则审计只记录 ids，不记录 diff，既有泄露风险又不利于追踪。证据：`app/routers/content_guard.py:30-61`
    
284.  synthesized `request_content_guard` typed event 把 `review` 也标成 `success`，高风险待复核在日志聚合中容易被当作成功请求。证据：`app/logging/adapters/request_adapter.py:267-287`
    
285.  synthesized `request_content_guard` 的 `guard_stage` 只能粗略区分 `stream_buffer` / `non_stream_response`，无法表达首段缓冲、滑动窗口、结束复核、外链检测、结构检测等真实阶段。证据：`app/logging/adapters/request_adapter.py:267-287`
    
286.  `request_stream` 补事件固定 `sse_error_sent = not log.success`、`done_sent = True`，实际是否已发送标准错误事件和 `[DONE]` 没有来自流式链路的真实字段，内容防护中断场景会被日志“补成成功收尾”。证据：`app/logging/adapters/request_adapter.py:223-245`
    
287.  `request_error_response` 补事件把 `recoverable` 直接等于 `log.retryable`，丢失统一错误目录里 retryable/recoverable 分离语义。证据：`app/logging/adapters/request_adapter.py:247-264`
    
288.  `content_integrity_violation` 虽在统一错误目录中存在，但 `_build_content_guard_error_detail()` 在“已切换但非最终失败”时强行把对外错误 payload 标成 `retryable/recoverable=true`，日志或中间错误若暴露会与目录定义的不可恢复语义冲突。证据：`app/services/error_catalog_service.py:510-522`，`app/services/proxy_service.py:6556-6590`
    
289.  内容防护目标切换的隐藏表单使用 `visibility:hidden` 保留布局，占位高度仍参与排版，切换内外部探针时可能出现额外空白和焦点/读屏语义不清。证据：`app/static/css/app.css:3159-3166`
    

36 previous messages
290.  `ContentGuardModuleService` 仍残留一整套 `_resolve_probe_target`、`_resolve_endpoint_path`、`_run_single_probe`、`_summarize_probe_results`、`_aggregate_content_guard_result` 旧探针实现，但实际入口已转调 `ContentTrustProbeService`，模块边界迁移不干净，后续维护很容易改错地方。证据：`app/services/content_guard_module_service.py:224-378`，`app/services/content_trust_probe_service.py:29-280`
    
291.  自动内容预检的配置允许最小 300 s，但分布式任务锁固定 `ttl_seconds=60 * 60`，导致配置小于 1 小时时仍可能被锁限制，无法按界面间隔执行。证据：`app/tasks.py:416-423`，`app/tasks.py:488-523`
    
292.  L3 内容完整性健康检查的 run 阶段使用 `ContentGuardProbeService.PROBE_PHASE_KEYS`，包含 JSON、tools 等可选探针；但实际可信预检强制执行的是 `ContentTrustProbeService.REQUIRED_TRUST_PROBE_KEYS`，健康日志的 phase\_keys 与真实执行范围不一致。证据：`app/services/health_service.py:53`、`1001`，`app/services/content_trust_probe_service.py:22-36`
    
293.  交互式模型健康检查仍直接用旧 `ContentGuardProbeService.PROBE_PHASE_KEYS` 跑内容探针，而不是走 `ContentTrustProbeService`，导致自动预检和手动交互检测使用两套入口与口径。证据：`app/services/health_service.py:242-266`，`app/services/health_service.py:1788-1792`
    
294.  被标记为 `blocked` 的模型会在每次自动预检判断中直接返回 true，后台会持续探测已隔离模型；但通过后又不会真正恢复 blocked 状态，形成持续消耗和状态矛盾。证据：`app/services/health_service.py:2174-2189`，`app/services/content_guard_probe_service.py:1082-1090`
    
295.  路由候选筛选无条件调用内容可信决策，没有检查全局 `content_guard_enabled`；即使内容完整性总开关关闭，历史 blocked、低评分、trust blocked 仍会继续影响正式路由。证据：`app/services/router_service.py:580-600`，`app/services/router_service.py:1110-1131`
    
296.  路由打分始终加入 `content_integrity_score`、`trust_level` 和 `content_violation_count` 权重，即使内容防护总开关关闭，候选排序仍被内容防护历史状态影响。证据：`app/services/router_service.py:1093-1108`
    
297.  `RoutePolicyContext.content_guard_required=False` 不会影响路由层内容完整性排除逻辑，router 仍会排除 content blocked/low score provider；“内容检测可选”的 API Key 语义在路由层也不成立。证据：`app/services/router_service.py:1117-1131`，`app/services/api_key_service.py:488-495`
    
298.  `ContentGuardResult` 里有 `confidence` 和 `score_delta`，但 `to_log_kwargs()` 不写出这两个字段，主日志无法复盘高风险阈值判断与扣分依据。证据：`app/services/content_guard_service.py:24-44`
    
299.  `request_logs` 与 `request_content_guard_events` 都没有 `confidence`、`score_delta` 字段，typed 日志同样无法保存检测置信度和评分变化。证据：`app/models/request_log.py:98-107`，`app/models/logging_events.py:155-170`
    
300.  `_extract_scan_text()` 的 `max_scan_bytes` 控制不严格，遇到单个超长字符串会先整段加入再更新字节数，返回文本可能远超上限，影响扫描、URL 域名提取和长上下文判断性能。证据：`app/services/content_guard_service.py:1025-1053`
    
301.  `_extract_scan_text()` 跳过 key 为 `image_url` 的整个字段，用户请求里的图片 URL 域名不会进入本次 allowlist；如果上游响应引用同一图片域名，可能被误判异常外链。证据：`app/services/content_guard_service.py:1038-1042`，`app/services/content_guard_service.py:649-655`
    
302.  内容防护概览读取所有提供商，不过滤 disabled provider；页面统计和探针候选会混入已停用提供商。证据：`app/services/content_guard_module_service.py:29-36`
    
303.  后端序列化 provider 虽返回 `enabled`，但前端 provider 下拉只显示名称和内容完整性状态，不显示启停状态，管理员无法区分已停用提供商。证据：`app/services/content_guard_module_service.py:174-195`，`app/static/js/app.js:8905-8910`
    
304.  内容防护探针目标解析没有拒绝 disabled provider，只要模型启用就能对已停用提供商发起探针并可能更新内容完整性状态。证据：`app/services/content_trust_probe_service.py:172-190`
    
305.  内容防护页后端提供了 `/api/content-guard/runtime/events`，但前端完全没有调用或展示该接口，运行时内容防护事件在模块页不可见。证据：`app/routers/content_guard.py:101-133`，`app/static/js/app.js` 未检索到 `/api/content-guard/runtime/events`
    
306.  规则操作持久化语义混乱：新增、删除只改前端本地状态，需要另点“保存”；“默认”按钮却直接调用后端立即落库，用户很容易误判哪些操作已保存。证据：`app/static/js/app.js:9088-9138`
    
307.  删除规则没有确认、没有按钮反馈、没有未保存状态提示；删除后只是重渲染本地表格，用户可能以为已删除成功并落库。证据：`app/static/js/app.js:9100-9105`
    
308.  内容防护概览里 `SettingService.get_or_create(db)` 被重复调用两次，配置和规则序列化不是同一次读取对象，虽通常一致，但没有必要且增加事务内口径漂移风险。证据：`app/services/content_guard_module_service.py:54-66`
    
309.  内容防护概览统计 guard latency 仍查 `RequestLog.content_guard_latency_ms` 主表字段，而不是 typed 内容防护事件；和前面统一日志事件化方向继续分叉。证据：`app/services/content_guard_module_service.py:41-52`
    
310.  内容防护页面的探针结果只展示探针、结果、风险、耗时、原因，不展示命中的规则 ID、分类列表、置信度、扣分、最终策略、是否写入 provider 状态；排障证据严重不足。证据：`app/templates/content_guard.html:361-372`，`app/static/js/app.js:9056-9068`
27 previous messages
311.  项目没有常规自动化测试目录，内容防护缺少可被测试框架稳定发现的单元/集成测试。当前只看到阶段脚本和工具脚本，没有 `tests/` 或同类测试套件。
    
312.  [stage33\_content\_guard\_regression\_check.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/stage33_content_guard_regression_check.py:441) 的前端/日志接线检查只是字符串包含校验，不能证明按钮可点、接口可用、页面渲染正确或日志真实落库。
    
313.  [stage33\_content\_guard\_regression\_check.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/stage33_content_guard_regression_check.py:602) 只调用若干函数级检查，没有覆盖真实 `/v1/*` 请求链路、流式中断、切换 provider、缓存失效、后台任务调度和前端交互。
    
314.  [scripts/benchmark\_content\_guard\_overhead.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/scripts/benchmark_content_guard_overhead.py:114) 只基准 `inspect_json_response()`，没有覆盖 `ContentRuntimeGuardService`、流式首段缓冲、滑动窗口检测、日志写入和路由重试成本。
    
315.  内容防护迁移同时存在正式 SQL 与 [app/main.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/main.py:438) 启动期 `ALTER TABLE`，且 `request_logs.content_guard_*` 在 [app/main.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/main.py:616) 又重复定义，容易出现迁移口径漂移。
    
316.  [migrations/2026-06-06\_add\_content\_guard\_governance.sql](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/migrations/2026-06-06_add_content_guard_governance.sql:38) 仍新增 `api_client_keys.trusted_providers_only`，但现行规范要求“只走可信提供商”只能作为全局路由设置维护；这是遗留字段进入正式迁移。
    
317.  [ContentGuardModuleService](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/content_guard_module_service.py:224) 已把 `run_probe/run_trust_probe` 转给 `ContentTrustProbeService`，但同文件后面仍保留 `_resolve_probe_target`、`_run_single_probe`、`_summarize_probe_results` 等重复实现，属于死代码/漂移源。
    
318.  [ContentGuardModuleService.build\_overview](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/content_guard_module_service.py:31) 一次性加载全部提供商和全部启用模型，没有分页、搜索或按需加载，提供商/模型数量变大后内容防护首页会越来越重。
    
319.  内容防护首页的延迟统计只筛 `content_guard_latency_ms is not null`，未像监控页那样限制正式流量类型；[app/services/content\_guard\_module\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/content_guard_module_service.py:50) 与 [system\_metrics\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/system_metrics_service.py:871) 口径不一致。
    
320.  文本检测接口的 `text` 没有最大长度限制，[app/schemas/content\_guard.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/schemas/content_guard.py:86) 只要求最小长度；管理员误贴超大内容时会直接进入扫描链路。
    
321.  自定义正则规则无效时会被静默跳过，[content\_guard\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/content_guard_service.py:377) 捕获 `re.error` 后直接 `continue`，前端保存时也不预校验，管理员会误以为规则已生效。
    
322.  规则 JSON 损坏时会静默回退默认规则，[content\_guard\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/content_guard_service.py:522) 没有告警、错误状态或 UI 提示，实际检测口径会悄悄变化。
    
323.  规则变更审计只记录数量和 ID，[app/routers/content\_guard.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/routers/content_guard.py:51) 不记录规则差异、风险等级、动作或匹配项变化，后续很难追溯是谁把规则改坏了。
    
324.  普通用户 API Key 页面仍暴露“要求内容检测/可选”，见 [user\_api\_keys.html](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/templates/user_api_keys.html:292)；但运行时该字段并不能真正表达“关闭内容防护”，会形成错误预期。
    
325.  自动 L3 内容预检遇到“没有到期模型”时仍把提供商结果记为失败倾向，[health\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/health_service.py:1093) 中 `model_results` 为空会让 `provider_success=False`，健康运行汇总会被污染。
    
326.  L3 内容预检按 provider/model 串行执行，[health\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/health_service.py:1028) 到 [1055](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/health_service.py:1055) 没有并发控制、批次预算或单轮最大模型数，大规模模型目录下后台任务会拖很久。
    
327.  内容预检异常结果被标成 `retryable=True`，[health\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/health_service.py:1157)，但没有区分本地配置错误、实现异常、上游临时错误，后台日志会给出误导性重试语义。
    
328.  健康探针落库时 `endpoint_path=None` 会被记成 `responses` 协议，[health\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/health_service.py:1324)，内容预检异常类日志的协议字段会失真。
    
329.  外部渠道探针会把临时 `ProviderModel` 的 `supports_stream` 和 `supports_tools` 直接设为 `True`，[content\_trust\_probe\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/content_trust_probe_service.py:150)，没有依据外部渠道真实能力，容易触发错误探针。
    
330.  内容防护 URL 默认白名单硬编码在规则服务里，[content\_guard\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/content_guard_service.py:113)，管理员只能追加白名单，不能在页面上审计或移除默认放行域名。
    
331.  自动隔离会直接改 provider 和全部挂载模型状态，[system\_metrics\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/system_metrics_service.py:287)，但没有记录隔离前状态快照，后续恢复时缺少可靠依据。
    
332.  自动隔离事件 payload 每次都会写入新的 `isolated_at`，[system\_metrics\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/system_metrics_service.py:321)，即使 provider 已经隔离且没有状态变化，也会让告警时间语义变得不稳定。
    
333.  `write_monitoring_alerts()` 对每个 active alert 都重写字段并置 `changed=True`，[system\_metrics\_service.py](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/services/system_metrics_service.py:197)，内容高风险告警会导致监控轮询频繁提交数据库。
    
334.  内容防护前端保存规则时 `reason` 被固定写成空字符串，[app/static/js/app.js](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/static/js/app.js:8862)，后端虽然支持 `reason` 字段，但页面无法维护这类解释信息。
    
335.  内容防护前端数值字段对非法数字没有前置校验，[app/static/js/app.js](/mnt/d/AI%E7%9F%A5%E8%AF%86%E5%AD%A6%E4%B9%A0%E6%96%B9%E9%9D%A2/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%99%BA%E8%83%BD%E4%BD%93%E5%85%A8%E5%A5%97%E6%95%99%E7%A8%8B/aotu-gpt/app/static/js/app.js:8993) 直接 `Number(...)` 后取 `Math.min/Math.max`，遇到 `NaN` 会把错误推给后端 422，而不是在字段旁给明确反馈。
    

51 previous messages
336.  `ProxyService._with_content_guard_route_policy()` 是空实现，收了 `payload/endpoint_path/has_image/require_tools` 却直接返回 `route_context`，导致 JSON、文件、长上下文、图像、工具等高风险请求没有接入可信 provider 路由策略。证据：`app/services/proxy_service.py:236-245`、`1446-1452`、`2342-2348`，判断函数在 `app/services/content_guard_service.py:259-276`。
    
337.  `requires_trusted_provider()` 本身有实现且测试覆盖了函数，但没有覆盖代理链路是否实际使用，所以是“能力存在、正式链路没用”。证据：`stage33_content_guard_regression_check.py:213-231`，空包装见上。
    
338.  流式协议适配链路直接禁用内容防护，`stream_guard_enabled` 排除了 chat/responses/completions 三类适配场景。证据：`app/services/proxy_service.py:2761-2765`、`3037-3068`。
    
339.  `buffer_300ms` 不是全局生效，还要 `_stream_content_guard_should_buffer()` 返回真；普通 provider 即使配置 300ms 缓冲也只做分块扫描。证据：`app/services/proxy_service.py:2766-2775`、`app/services/content_runtime_guard_service.py:251-270`。
    
340.  首段预取在 `buffer_300ms` 下 `stop_at_first_event=True`，遇到第一个 SSE 事件就停，不一定满足“字节阈值或最大等待时间先到即检测”的语义。证据：`app/services/proxy_service.py:1175-1178`、`1230-1232`、`2808-2810`。
    
341.  分块扫描只消费完整 SSE 事件，恶意内容跨事件拆分时可能漏检。证据：`app/services/content_runtime_guard_service.py:123-169`、`app/services/proxy_service.py:8004-8039`。
    
342.  `event_buffer` 在完整事件被消费后会删除内容，只对残留不完整数据做文本扫描，缺少跨 chunk/跨 event 滑动窗口。证据：`app/services/content_runtime_guard_service.py:141-155`、`app/services/proxy_service.py:8004-8039`。
    
343.  非流式检测通过时直接写 `provider_model.content_probe_last_passed_at`，没有通过探针状态服务，也不更新 `content_probe_results_json`，混用了“正式请求通过时间”和“可信探针通过时间”。证据：`app/services/content_runtime_guard_service.py:59-60`、`app/services/content_guard_probe_service.py:1047-1108`。
    
344.  外部复用 `db` 会话时 `_run_db_write()` 不提交事务，非流式 pass 写入是否提交依赖后续请求日志落库副作用。证据：`app/services/proxy_service.py:277-281`、`app/services/log_service.py:320-329`。
    
345.  规则 `action="allow"` 没有真正放行语义，命中后仍计入风险、分数、分类，可能 review/block；前端却显示“放行”。证据：`app/services/content_guard_service.py:308-345`、`app/static/js/app.js:8764-8768`。
    
346.  前端允许 `action=allow`，保存时却把 `reason` 强制置空，无法解释放行规则行为。证据：`app/static/js/app.js:8862-8874`。
    
347.  `/api/content-guard/precheck/probe` 可选择任意探针并持久化内部结果；只选 JSON/tools 等可选探针时，可能因缺少必需探针把模型降级/阻断。证据：`app/static/js/app.js:9002-9035`、`9186`，后端 `app/routers/content_guard.py:152-160`，决策 `app/services/content_guard_probe_service.py:1001-1044`。
    
348.  `/precheck/probe` 和 `/trust-probe` 页面语义都像“可信检测”，但一个可选探针，一个强制三项，容易混淆能力探针与可信状态。证据：`app/routers/content_guard.py:141-180`、`app/services/content_trust_probe_service.py:29-36`。
    
349.  探针成功结果把 `native_success=True/adapted_success=False/support_mode="native"` 写死，即使实际可能走 fallback/适配。证据：`app/services/content_guard_probe_service.py:221-251`、`159-179`。
    
350.  `probe_sse()` 最多读取 6 个 chunk 且要求看到 `[DONE]`，长输出或慢流式容易被误判失败。证据：`app/services/content_guard_probe_service.py:535-553`、`604-615`。
    
351.  `probe_sse()` 异常分支返回失败但没有 `content_guard` 字段，后续聚合会缺少风险分类和原因结构。证据：`app/services/content_guard_probe_service.py:625-642`、`254-273`。
    
352.  流式探针复用全局流式超时，没有内容防护探针自己的最大持续时间/最大读取字节配置。证据：`app/services/content_guard_probe_service.py:530-534`。
    
353.  外链广告识别探针样本过弱，只是普通问答，不覆盖短链、二维码、联系方式、社群等诱导污染样本。证据：`app/services/content_guard_probe_service.py:648-668`。
    
354.  外链广告识别探针每次执行三次真实模型请求，自动任务按 provider/model 串行执行时成本和耗时会放大。证据：`app/services/content_guard_probe_service.py:669-765`、`app/services/health_service.py:1028-1055`。
    
355.  内容防护错误响应在“已尝试切换其它渠道”的中间态把 `retryable/recoverable` 强制设为 True，容易误导客户端重试内容污染问题。证据：`app/services/proxy_service.py:6556-6592`。
    
356.  非流式每次检测都追加 `request_content_guard` typed event，即使 pass，高流量下会增加事件和日志体积。证据：`app/services/proxy_service.py:1919-1936`、`2009-2058`。
    
357.  流式 finally 也会追加 `request_content_guard` typed event，即使未启用或未命中。证据：`app/services/proxy_service.py:3232-3249`。
    
358.  typed `request_content_guard` 的 `matched_rules_json` 始终是 `None`，事件流无法还原具体命中规则。证据：`app/services/proxy_service.py:1926-1928`、`3239-3240`，模型字段在 `app/models/logging_events.py:163-164`。
    
359.  日志中心只支持筛 `content_guard_result/risk_level`，不能筛动作、最终策略、分类、是否切换 provider 等关键处置维度。证据：`app/templates/logs.html:149-165`、`app/routers/logs.py:54-55`、`app/services/log_service.py:855-858`。
    
360.  日志导出遗漏 `latency/buffer_wait/retry_provider_count/final_strategy/categories` 等内容防护审计字段。证据：`app/services/log_service.py:1778-1843`、`1879-1883`。
    
361.  后端提供 `/api/content-guard/runtime/events`，但前端没有任何调用和展示入口，内容防护页面看不到正式请求运行时事件。证据：`app/routers/content_guard.py:101-133`，搜索仅命中路由，无前端调用。
    
362.  `/runtime/events` 固定取最近 100 条，没有分页、筛选、时间范围、provider/model 条件，大规模日志下不可用。证据：`app/routers/content_guard.py:103-109`。
    
363.  `/runtime/events` 查的是 `RequestLog` 摘要，不查 `RequestContentGuardEvent` typed event，因此会丢 `matched_rules_json`、更细的事件阶段和事件时间线能力。证据：`app/routers/content_guard.py:105-130`、`app/models/logging_events.py:154-168`。
    
364.  内容防护概览后端返回完整 providers 列表，但前端只用它填下拉框，没有提供 provider/model 内容完整性状态表，管理员无法在内容防护页集中查看状态。证据：`app/services/content_guard_module_service.py:61-79`、`app/static/js/app.js:8905-8917`、`app/templates/content_guard.html:228-283`。
    
365.  本地文本检测 `inspect_text()` 先 `inspect_response_text()`，再单独 `match_text_rules()`，同一次请求做两轮规则匹配，存在性能浪费和结果不一致风险。证据：`app/services/content_guard_module_service.py:148-173`。
    
366.  本地文本检测请求的 `text` 没有最大长度，`max_scan_bytes` 也没有上限，内部接口可被大文本拖慢。证据：`app/schemas/content_guard.py:86-90`。
    
367.  内容防护设置里的 `content_guard_max_scan_bytes`、`content_guard_stream_buffer_max_bytes` 只有下限没有上限，前端也只做最小值约束，可能被设置成极大值导致内存/CPU 风险。证据：`app/schemas/content_guard.py:20-30`、`app/static/js/app.js:8996-8997`。
    
368.  若“高风险拦截”关闭，但“高风险策略”仍选 `switch_provider/block/safe_error`，`decide_runtime_action()` 会对 `RESULT_BLOCK` 返回 `allow`，高风险内容可能被放行且没有明确 `record_only` 语义。证据：`app/services/content_runtime_guard_service.py:177-186`、`app/services/content_guard_service.py:752-764`。
    
369.  “高风险置信度”阈值文案说达到阈值才阻断/切换，但多数 `RESULT_BLOCK` 都带 `action="block"`，`should_block()` 会绕过阈值判断，导致阈值实际不生效。证据：`app/templates/content_guard.html:209-210`、`app/services/content_guard_service.py:320-331`、`760-763`。
    
370.  内容防护日志字段没有持久化 `confidence` 和 `score_delta`，后续无法审计为什么触发阈值、扣分或隔离。证据：`ContentGuardResult` 有字段 `app/services/content_guard_service.py:28-29`，`to_log_kwargs()` 未写入 `34-45`，`RequestLog` 也无列 `app/models/request_log.py:98-107`。
    
371.  非流式 typed event 在调用 `should_block_for_response()` 之前就构造 payload，导致 `provider_status_after/final_strategy` 仍是 `None`，事件与最终日志字段可能不一致。证据：`app/services/proxy_service.py:1919-1936`、`1937`、`2049-2050`。
    
372.  流式分块扫描如果在尚未向客户端输出前命中污染，代码仍直接发送错误事件并结束，没有走“未输出前可切换 provider”的路径。证据：`app/services/proxy_service.py:2964-2997`。
    
373.  流式内容防护命中后发送错误事件时，没有把具体 `guard_result` 的分类、规则、置信度放入 SSE 错误详情，只给通用 `content_integrity_violation`。证据：`app/services/proxy_service.py:2983-2996`、`6534-6545`。
    
374.  日志详情页的内容防护卡片只展示结果、风险、动作、耗时，不展示缓冲等待、重试 provider 数、最终策略、分类、证据片段。证据：`app/static/js/app.js:10822-10829`、`10898-10905`。
    
375.  日志列表摘要直接显示英文动作值，如 `block/record/allow`，没有中文映射，和前端中文优先规范不一致。证据：`app/static/js/app.js:1846-1855`。
    
376.  内容防护页面的“检测结果”区域只承载手动探针结果，不承载运行时请求结果，页面标题容易让管理员误以为这里是完整防护结果中心。证据：`app/templates/content_guard.html:348-380`、`app/static/js/app.js:9045-9070`。
    
377.  前端探针选项默认勾选固定三项，但用户可以取消必需探针；保存内部结果时没有二次提示“这会影响模型可信状态”。证据：`app/static/js/app.js:8923-8931`、`9002-9035`。
    
378.  `ContentGuardExternalTarget.base_url` 允许 `http://`，且仅检查协议前缀，没有 HTTPS 强制和内网地址限制，外部探针存在明文密钥与 SSRF 风险。证据：`app/schemas/content_guard.py:93-107`。
    
379.  外部渠道探针前端把 API Key 直接交给浏览器表单和接口提交，没有额外遮蔽提示、一次性使用说明或审计风险提示。证据：`app/templates/content_guard.html:260-279`、`app/static/js/app.js:9007-9024`。
    
380.  内容防护页面未展示 `content_guard_retry_provider_count`，无法判断一次污染阻断后是否真的尝试过切换其它 provider。证据：落库字段在 `app/models/request_log.py:104-107`，前端详情只展示 `content_guard_latency_ms`：`app/static/js/app.js:10823-10829`。
    

50 previous messages
381.  [router\_service.py](./app/services/router_service.py:245) 真实候选加载存在严重缩进错误：`for provider_model in provider.provider_models` 只包住了 enabled 判断，后续过滤脱离循环，只检查最后一个模型。
    
382.  [router\_service.py](./app/services/router_service.py:248) 内容完整性过滤只可能作用于最后一个挂载模型，不能逐模型排除 `blocked` 模型。
    
383.  [router\_service.py](./app/services/router_service.py:250) `enabled_model_names`、模型名、流式、Chat、Responses、视觉、图片生成等过滤都在 `continue` 后的不可达分支里。
    
384.  [router\_service.py](./app/services/router_service.py:284) 健康状态、路由评分、`candidates.append()` 也在不可达分支里，真实候选可能长期为空。
    
385.  [router\_service.py](./app/services/router_service.py:591) 诊断路径缩进是正确的，导致诊断结果与真实路由行为不一致，排查时会被误导。
    
386.  [content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:100) 路由可信决策未读取全局 `content_guard_enabled`，总开关关闭后仍可能按内容完整性状态排除候选。
    
387.  [content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:109) 可信决策没有使用 `route_context.content_guard_required`，API Key 标记内容防护可选时仍可能被内容状态阻断。
    
388.  [content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1082) 模型探针通过时，如果模型已是 `blocked`，不会恢复为 `passed`。
    
389.  [content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1082) 探针通过只更新模型探针字段，不恢复 provider 级内容完整性状态、评分、信任等级或熔断状态。
    
390.  [provider\_service.py](./app/services/provider_service.py:1339) 凭据轮换只重置健康/熔断，不重置内容完整性状态；换 Key 后仍可能被旧污染状态隔离。
    
391.  [health\_service.py](./app/services/health_service.py:2926) 健康检查成功只恢复健康/熔断，不恢复内容完整性 `blocked`，会出现“健康可用但路由被内容状态拦截”的双状态。
    
392.  [provider\_service.py](./app/services/provider_service.py:1075) 管理员手动标记模型通过时只补固定答案和广告识别结果，漏掉必需的 `sse` 探针。
    
393.  [provider\_service.py](./app/services/provider_service.py:1100) 手动标记 blocked 时，探针结果里的 `content_guard_result` 仍写 `review`，状态和证据不一致。
    
394.  [content\_guard\_service.py](./app/services/content_guard_service.py:385) 文本检测动态追加短链/异常广告外链规则，不在统一规则表内，前端无法查看、关闭或调参。
    
395.  [content\_guard\_service.py](./app/services/content_guard_service.py:949) 工具参数污染检测未传当前规则 JSON 和 URL 白名单，可能绕过管理员配置。
    
396.  [content\_guard\_service.py](./app/services/content_guard_service.py:647) 请求 payload 中出现过的域名会自动进入本次白名单，用户只要在 prompt 中提到恶意域名，上游同域名输出就可能被放行。
    
397.  [content\_guard\_service.py](./app/services/content_guard_service.py:113) 默认白名单硬编码常见域名，管理员清空自定义白名单也无法进入严格模式。
    
398.  [content\_guard.html](./app/templates/content_guard.html:219) 前端只提示“用户请求域名会自动加入白名单”，但没有提示默认白名单不可移除，配置语义不完整。
    
399.  [content\_guard\_service.py](./app/services/content_guard_service.py:471) SSE 检测把非 JSON `data` 直接判为 block，无法兼容纯文本事件、心跳或控制事件。
    
400.  [content\_guard\_service.py](./app/services/content_guard_service.py:471) SSE 多行 `data:` 拼接语义没有完整处理，合法多行 JSON 或分片 JSON 容易被误判。
    
401.  [content\_guard\_service.py](./app/services/content_guard_service.py:837) 递归 JSON 键名只要命中 `contact/ad/promo/qr` 等就高风险拦截，容易误伤正常结构化输出。
    
402.  [content\_guard\_service.py](./app/services/content_guard_service.py:827) Responses 响应结构检查强依赖 `output` 字段，可能误判仅返回兼容字段的响应。
    
403.  [content\_guard\_service.py](./app/services/content_guard_service.py:858) JSON 模式只校验输出能否 `json.loads`，不校验 schema、必填字段或 strict 语义。
    
404.  [content\_guard\_service.py](./app/services/content_guard_service.py:858) JSON 模式没有检查 tool/function arguments 中的结构化 JSON。
    
405.  [content\_guard\_service.py](./app/services/content_guard_service.py:1038) `_extract_scan_text()` 跳过 `image_url` 整个值，污染文本或 URL 可藏在该对象内部。
    
406.  [content\_guard\_service.py](./app/services/content_guard_service.py:1038) 敏感字段跳过只按小写精确匹配，`Authorization`、`API_KEY` 等大小写变体不会被跳过。
    
407.  [content\_guard\_service.py](./app/services/content_guard_service.py:997) 高风险文件引用识别未覆盖 `file_data`、`file_url`、`attachments[].url` 等常见变体。
    
408.  [content\_guard\_service.py](./app/services/content_guard_service.py:1012) 长上下文风险判断只看扫描文本 16KB 或消息数量，没有考虑 token、图片/文件体积、嵌套 content 数量。
    
409.  [content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:327) JSON 探针对 Responses 端点没有设置协议级 `text.format` / schema，只是 prompt 要求 JSON。
    
410.  [content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:488) 严格 JSON 探针要求输出文本完全等于压缩 JSON，合法空格、字段顺序或转义差异都会失败。
    
411.  [content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:429) 固定答案探针要求输出完全等于固定字符串，对合法 wrapper、多 choice 或轻微格式包装兼容性差。
    
412.  [content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:803) 工具调用探针只检查第一条 tool call，后续追加污染工具调用不会失败。
    
413.  [content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:843) 工具调用探针只校验工具名和两个参数值，不检查额外参数、schema 完整性或自然语言混入。
    
414.  [content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:224) `ContentGuardModuleService.run_probe()` 已转调 `ContentTrustProbeService`，但同文件仍保留一整套重复探针实现，形成半废弃代码。
    
415.  [content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:232) 重复的 `_resolve_probe_target()` 和 `ContentTrustProbeService._resolve_probe_target()` 几乎同构，后续修一处漏一处的风险很高。
    
416.  [content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:31) 内容防护概览一次性查询所有提供商和启用模型，无分页；规模稍大时页面加载和序列化成本不可控。
    
417.  [content\_guard.py](./app/routers/content_guard.py:101) runtime events 只读 `request_logs` 汇总字段，没有读取专门的 `request_content_guard_events` 明细表。
    
418.  [logging\_events.py](./app/models/logging_events.py:154) 专门的内容防护事件表有 `matched_rules_json`、`provider_status_after` 等字段，但内容防护页面接口完全没有暴露这些证据。
    
419.  [content\_guard.py](./app/routers/content_guard.py:108) runtime events 固定 `limit(100)`，没有分页、筛选、风险等级、provider、trace\_id 查询，审计检索能力不足。
    
420.  [content\_guard.py](./app/routers/content_guard.py:85) 文本检测、运行时检测、手动探针和外部渠道探针没有写管理员审计日志；虽然路由整体有管理员鉴权，但这些高风险操作缺少操作者留痕。
    

10 previous messages
421.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:226) `enabled_for_request()` 在 `route_context.content_guard_required=False` 时仍返回 `provider.content_guard_enabled`，等于 API Key 标记“内容检测可选”也无法真正关闭运行时检测。
    
422.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:229) `content_guard_required=False` 的语义与前端“内容检测可选”不一致；这里应该优先尊重请求上下文，而不是继续看 provider 开关。
    
423.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:52) 非流式只有 block/error 或关闭异步复核时才记录违规，默认异步复核开启时 review 结果不会写 provider/model 违规状态。
    
424.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:113) 流式首段缓冲检测同样只在 block/error 或异步复核关闭时记录违规，中风险 review 默认没有进入后续复核队列或事件表。
    
425.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:182) `async_review` 只是把 action/final\_strategy 写成字符串，没有实际后台任务、队列、复核人或处理状态，属于策略空壳。
    
426.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:59) 非流式检测通过时只更新 `content_probe_last_passed_at`，没有 commit；如果后续没有其它写入，该“通过时间”可能不会持久化。
    
427.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:59) 正式请求通过后只更新模型 `last_passed_at`，没有更新 provider/model 内容完整性状态或失败计数，无法形成稳定恢复闭环。
    
428.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:64) `inspect_stream_prefetch_buffer()` 没有先调用 `enabled_for_request()`；它依赖外层保证，服务自身入口不完整，容易被未来调用方绕过开关语义。
    
429.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:123) `inspect_stream_chunk()` 也没有内建总开关和 route\_context 判断，模块边界不够自洽。
    
430.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:141) 流式分块检测每次 `_consume_sse_data_payloads(event_buffer)` 后又对剩余 `event_buffer` 做全文扫描，可能反复扫描已处理片段，带来误报和额外开销。
    
431.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:165) 只有当 `event_buffer >= max_scan_bytes` 才清空；长期小片段残留会持续累积上下文，滑动窗口不是严格“窗口”，而是近似累积缓冲。
    
432.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:156) 分块文本扫描直接 decode SSE 原始 buffer，包含协议字段、事件名和 JSON 包装，规则命中可能来自协议外壳而非模型正文。
    
433.  [content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:172) `decide_runtime_action()` 会原地修改 `guard_result.action/final_strategy`，同一个结果对象多次判断时存在副作用。
    
434.  [proxy\_service.py](./app/services/proxy_service.py:1933) 非流式链路在判断 `should_block_for_response()` 时会调用带副作用的 `decide_runtime_action()`，随后再次调用可能重复改写 final\_strategy。
    
435.  [proxy\_service.py](./app/services/proxy_service.py:2761) 流式内容防护在任何适配场景下被直接禁用：Chat→Responses、Chat→Completions、Responses→Chat 都不检测。
    
436.  [proxy\_service.py](./app/services/proxy_service.py:2765) 适配禁用检测没有在响应日志里标明“因适配跳过内容防护”，日志只会显示未命中或默认通过，审计容易误判。
    
437.  [proxy\_service.py](./app/services/proxy_service.py:2778) `full_buffer` 模式把 `max_delay_seconds` 设为 `None`，会等到字节上限或流结束；长流式响应可能长时间不向客户端输出。
    
438.  [proxy\_service.py](./app/services/proxy_service.py:1176) `_prefetch_stream_guard_buffer()` 默认 `stop_at_first_event=True`，300ms 缓冲实际可能只等到第一个 SSE 事件，不一定达到“字节阈值或最大等待时间先到”的完整语义。
    
439.  [proxy\_service.py](./app/services/proxy_service.py:1200) 300ms 到期但 buffer 为空时直接返回 pass，后续虽然分块检测继续，但首段预检日志只会写“未收到可检测内容”，不能体现真实首段为空或上游慢响应风险。
    
440.  [proxy\_service.py](./app/services/proxy_service.py:2812) 流式首段缓冲命中高风险且策略为切换提供商时，会记录一条失败日志后继续重试；最终耗尽时又会记录总失败日志，容易重复统计一次请求的内容拦截次数。
    
441.  [proxy\_service.py](./app/services/proxy_service.py:2974) 流式输出过程中命中高风险时，只调用 `_record_content_guard_violation_by_id()` 更新 provider/model 状态，不立即写完整 request\_log。
    
442.  [proxy\_service.py](./app/services/proxy_service.py:2975) 中途命中高风险时 `_record_content_guard_violation_by_id()` 没有 trace\_id、request\_log\_id、请求路径、API Key 上下文，内容防护事件证据链断裂。
    
443.  [proxy\_service.py](./app/services/proxy_service.py:2991) 流式中途命中后会发送 `event: error` 和 `[DONE]`，但错误事件内容只包含通用 message/code/trace\_id，没有返回命中规则、风险等级、动作和证据摘要。
    
444.  [proxy\_service.py](./app/services/proxy_service.py:3236) 流式终态 typed content\_guard 事件的 `guard_stage` 永远写 `stream_buffer`，即使实际是下游输出过程中的分块检测命中。
    
445.  [proxy\_service.py](./app/services/proxy_service.py:3239) typed content\_guard 事件的 `matched_rules_json` 恒为 `None`，即使规则引擎已经有匹配规则列表。
    
446.  [proxy\_service.py](./app/services/proxy_service.py:3178) 流式成功或失败日志只记录最后一个 `guard_result`，如果中途出现 review 后最终 pass，最终日志会保留 review；如果多个风险片段出现，前序证据会丢失。
    
447.  [proxy\_service.py](./app/services/proxy_service.py:3321) 流式内容防护中断后还会调用 `_mark_failure_async()` 标记 provider/model 上游失败，内容污染和上游故障被混在一起影响健康状态。
    
448.  [proxy\_service.py](./app/services/proxy_service.py:3343) 流式中断最终日志没有显式写 `content_guard_final_strategy`，只靠 `guard_result.to_log_kwargs()`，但中途分块检测结果通常没有 strategy。
    
449.  [proxy\_service.py](./app/services/proxy_service.py:6556) 内容防护错误统一使用 HTTP 502 和 `server_error` 分类，调用方无法区分上游内容污染、代理策略拦截和普通上游故障。
    
450.  [proxy\_service.py](./app/services/proxy_service.py:6587) 切换提供商时错误 payload 标记 `retryable=True`，但如果这是最终返回给客户端的内容策略拦截，客户端重试可能继续触发污染；语义容易误导。
    
451.  [proxy\_service.py](./app/services/proxy_service.py:6767) 候选耗尽时只改 error message/retryable/recoverable，没有把最终 content\_guard detail 里的 `final` 语义重新构造完整，错误结构内外语义可能不一致。
    
452.  [logging\_events.py](./app/models/logging_events.py:154) `RequestContentGuardEvent` 有 `matched_rules_json`，但代理写入 typed 事件时始终给 `None`，字段设计没有被真实使用。
    
453.  [request\_adapter.py](./app/logging/adapters/request_adapter.py:267) 当 trace 里没有原生 content\_guard 事件时，fallback 事件只从 request\_log 汇总字段生成，无法补回命中规则、检测阶段和 provider 状态变化。
    
454.  [request\_adapter.py](./app/logging/adapters/request_adapter.py:279) fallback 的 `guard_stage` 只按 `is_stream` 粗略写 `stream_buffer` 或 `non_stream_response`，无法区分首段缓冲、分块扫描、非流式结构检查、工具参数检查。
    
455.  [logging\_api.py](./app/routers/logging_api.py:44) 请求时间线能读取内容防护事件，但没有独立 `/api/logging/content-guard-events` 列表接口，日志中心无法按内容防护事件维度检索。
    
456.  [content\_guard.py](./app/routers/content_guard.py:101) 内容防护页的 runtime events 没有使用 `RequestContentGuardEvent`，导致它看不到精确检测阶段和事件级证据。
    
457.  [logs.html](./app/templates/logs.html:150) 日志列表只支持按结果和风险筛选内容防护，缺少动作、最终策略、检测阶段、是否重试切换、是否适配跳过等关键筛选项。
    
458.  [app.js](./app/static/js/app.js:10823) 日志详情“内容防护”卡片只展示结果、风险、动作、耗时，漏掉原因、摘录、缓冲等待、重试提供商数、最终策略。
    
459.  [log\_service.py](./app/services/log_service.py:1778) CSV 导出包含结果、风险、动作、原因、摘录，但缺少 `content_guard_latency_ms`、`buffer_wait_ms`、`retry_provider_count`、`final_strategy` 等已落库字段。
    
460.  [app.js](./app/static/js/app.js:10825) 日志详情使用通用 `formatLogStatusLabel()` 展示内容防护结果/风险/动作，而不是专用中文映射；部分枚举如 `safe_error`、`switch_provider`、`async_review` 可能直接裸露英文。
    

71 previous messages
461.  `ContentGuardExternalTarget.base_url` 只校验 `http://` / `https://`，没有禁止 localhost、内网 IP、云元数据地址等，外部探针存在 SSRF 风险。证据：`app/schemas/content_guard.py:93-107`
    
462.  外部探针允许 `http://` 明文地址，管理员填写的外部密钥可能通过明文 HTTP 发送。证据：`app/schemas/content_guard.py:93-107`
    
463.  外部探针 `api_key` 没有长度上限，超大密钥字符串会增加入参、日志和上游请求压力。证据：`app/schemas/content_guard.py:93-96`
    
464.  外部探针 `model_name` 没有长度上限，超长模型名可能撑爆日志、请求体或上游参数。证据：`app/schemas/content_guard.py:93-96`
    
465.  本地检测 `text` 只有 `min_length`，没有 `max_length`，超大文本在截断扫描前已进入请求解析内存。证据：`app/schemas/content_guard.py:86-90`
    
466.  本地检测 `request_payload` 没有结构深度和大小限制，可提交超大嵌套 JSON。证据：`app/schemas/content_guard.py:86-90`
    
467.  通用设置 `SettingUpdate.content_guard_url_allowlist_json` 没有 `max_length`，但专用内容防护设置限制 10000，两个入口校验不一致。证据：`app/schemas/setting.py:39`、`app/schemas/content_guard.py:28`
    
468.  `SettingService.update()` 对 `content_guard_*` 只跳过未显式字段，不做专用校验或规范化，通用 `/api/settings` 可绕过专用接口约束。证据：`app/services/setting_service.py:171-175`
    
469.  规则 JSON 解析失败时静默回退默认规则，管理员保存的损坏规则不会显式报错。证据：`app/services/content_guard_service.py:521-529`
    
470.  非法 `match_type`、`risk_level`、`action` 会静默降级默认值，掩盖配置错误。证据：`app/services/content_guard_service.py:550-572`
    
471.  重复规则 id 会静默跳过后续规则，管理员无法感知规则丢失。证据：`app/services/content_guard_service.py:536-547`
    
472.  `record_violation()` 对 review/block/error 都可能累计违规，但 `should_record_violation()` 默认不记录 review，指标语义不一致。证据：`app/services/content_runtime_guard_service.py:244-248`、`app/services/content_guard_service.py:777-785`
    
473.  `record_violation()` 默认把 block 之外都降级为 degraded，但 review 可能只是低证据异常外链，仍会影响提供商状态。证据：`app/services/content_guard_service.py:780-791`
    
474.  通过 id 记录违规时，`db.get()` 找不到 provider/model 会静默无效，污染证据丢失。证据：`app/services/proxy_service.py:1277-1285`
    
475.  `request_content_guard_events` 有模型但没有正式 SQL migration，生产禁用自动 DDL 时表可能不存在。证据：`app/models/logging_events.py:154-169`、`migrations/` 无该表
    
476.  生产环境 `init_database()` 默认直接 return，但内容防护部分表/字段仍依赖 `create_all` 和内置 DDL。证据：`app/main.py:76-80`
    
477.  `app_settings` 的内容防护字段在两套内置 DDL 中重复维护，后续容易漂移。证据：`app/main.py:287-301`、`app/main.py:814-828`
    
478.  `request_logs` 的内容防护字段也在两套自动 DDL 中重复维护。证据：`app/main.py:438-447`、`app/main.py:616-625`
    
479.  migration 只给部分内容防护字段建索引，缺少常用筛选组合索引，如 `risk_level`、`provider_id + created_at + result`、`trace_id`。证据：`migrations/2026-06-06_add_content_guard_governance.sql:53-55`、`migrations/2026-06-07_extend_content_guard_strategy_metrics.sql:16-18`
    
480.  内容防护迁移仍给 `api_client_keys` 增加 `trusted_providers_only`，与全局规范“只走可信提供商只能作为全局路由设置维护”冲突。证据：`migrations/2026-06-06_add_content_guard_governance.sql:37-39`
    
481.  `ContentGuardRuleService` 只是继承 `ContentGuardService` 的空壳，三层职责在代码所有权上没有真正拆开。证据：`app/services/content_guard_rule_service.py:1-8`
    
482.  `ContentRuntimeGuardService` 反向 import 并调用 `ProxyService` 私有方法，内容防护层依赖代理层实现细节，边界倒挂。证据：`app/services/content_runtime_guard_service.py:61`、`app/services/content_runtime_guard_service.py:123`、`app/services/content_runtime_guard_service.py:213`
    
483.  内容防护页面“可信探测”提交 `/api/content-guard/precheck/probe`，不是强制必需探针的 `/trust-probe`，页面语义和实际接口不一致。证据：`app/static/js/app.js:9186`、`app/routers/content_guard.py:152-169`
    
484.  `/precheck/probe` 允许自选探针且 `persist_internal_result=true`，部分探针失败可能增加失败次数、降级甚至累计后隔离模型。证据：`app/static/js/app.js:9034`、`app/services/content_trust_probe_service.py:64-71`、`app/services/content_guard_probe_service.py:1082-1094`
    
485.  内容防护 overview 一次性读取全部提供商和全部启用模型，没有分页或关键词筛选，提供商多时页面初载会变慢。证据：`app/services/content_guard_module_service.py:31-74`
    
486.  overview 延迟统计每次读取最近 5000 条再在 Python 内排序算百分位，高频刷新会增加数据库和应用层压力。证据：`app/services/content_guard_module_service.py:50-60`
    
487.  URL 白名单解析失败时静默按换行文本处理，没有向管理员暴露配置错误。证据：`app/services/content_guard_service.py:628-645`
    
488.  前端内容防护结果映射缺少 `error`，遇到后端 `RESULT_ERROR` 会直接显示英文或未知态。证据：`app/static/js/app.js:108-114`、`app/static/js/app.js:1471-1477`
    
489.  内容防护结果状态 class 也未识别 `error`，错误结果会落到 `status-unknown`。证据：`app/static/js/app.js:1489-1495`
    
490.  内容防护模板仍显示“切换渠道”“外部渠道”等用户可见文案，违反全局术语“提供商”。证据：`app/templates/content_guard.html:147-152`、`app/templates/content_guard.html:170`、`app/templates/content_guard.html:246`
    
491.  API Key 页面仍大量显示“渠道授权/全渠道/未绑定渠道”，和内容防护关联配置一起加重术语漂移。证据：`app/templates/api_keys.html:339`、`app/templates/user_api_keys.html:305`、`app/templates/user_api_keys.html:409`
    
492.  内容防护目标表单用两个 grid 叠在同一区域，隐藏项使用 `visibility:hidden` 仍参与布局，切换内外部检测时可能留下异常空白高度。证据：`app/static/css/app.css:3113-3166`
    
493.  规则表固定 `min-width:1180px` 且把规则名、分类、匹配、动作、可信度等全部平铺，缺少渐进式披露。证据：`app/static/css/app.css:3221-3239`、`app/templates/content_guard.html:307-323`
    
494.  规则编辑只有整表保存，没有单行保存/撤销/脏状态提示，长表编辑时容易误保存或丢失修改。证据：`app/static/js/app.js:8815-8875`、`app/static/js/app.js:9097-9121`
    
495.  删除规则没有二次确认，也没有撤销入口，误点会直接从当前编辑态移除。证据：`app/static/js/app.js:9108-9114`
    
496.  本地内容检测只传 `text`，没有暴露 `endpoint_path` 和 `request_payload`，无法模拟 Responses、Chat、JSON 模式、工具调用等不同结构场景。证据：`app/templates/content_guard.html:328-342`、`app/static/js/app.js:9147`
    
497.  检测结果表只展示探针、结果、风险、耗时、原因，没有展示 endpoint、trace、状态码、原生/适配成功等排障字段。证据：`app/templates/content_guard.html:361-372`、`app/static/js/app.js:9058-9070`
    
498.  外部探针表单提交后密钥仍留在 DOM 密码框中，没有清空或短暂使用后移除。证据：`app/static/js/app.js:9007-9023`
    
499.  `stage33_content_guard_regression_check.py` 前端覆盖只是字符串存在性检查，不能验证按钮点击、表单提交、接口参数和响应渲染。证据：`stage33_content_guard_regression_check.py:397-501`
    
500.  `stage33` 没有覆盖外部探针 SSRF、HTTP 明文、超大 api\_key/model/text/payload 等输入安全边界。证据：`stage33_content_guard_regression_check.py` 未包含相关断言
    
501.  `stage33` 没有覆盖生产迁移下 `request_content_guard_events` 表是否真实存在。证据：`stage33_content_guard_regression_check.py` 未检查 migration
    
502.  `stage33` 没有覆盖内容防护页面使用 `/precheck/probe` 导致部分探针持久化状态的问题。证据：`stage33_content_guard_regression_check.py:397-501`
    
503.  `stage33` 没有覆盖 `content_guard_required=False` 的真实运行时语义，只做了前端/服务接线字符串检查。证据：`stage33_content_guard_regression_check.py:397-501`
    
504.  流式预取超时且无数据时直接 pass，并把后续检测转为分块扫描；如果上游首个污染片段晚于 300ms，已经失去“输出前切换 provider”的机会。证据：`app/services/proxy_service.py:1195-1210`
    
505.  `stream_should_buffer()` 只对低信任或 require trusted 场景强制缓冲，标准但 unknown/degraded 的提供商默认不做首段缓冲。证据：`app/services/content_runtime_guard_service.py:252-270`
    
506.  非流式内容防护事件里 `matched_rules_json` 固定写 `None`，即使命中规则也无法在事件层追溯具体规则。证据：`app/services/proxy_service.py:1919-1928`
    
507.  `RequestLogRecorder` 从主日志合成 content guard 事件时，把 `provider_status_after` 填成 `content_guard_final_strategy`，字段语义不对。证据：`app/logging/adapters/request_adapter.py:267-286`
    
508.  `content_guard_runtime_events` 直接从 `request_logs` 取最近 100 条，不读专门的 `request_content_guard_events`，事件表价值被绕开。证据：`app/routers/content_guard.py:102-132`
    
509.  `content_guard_runtime_events` 没有分页、时间范围、结果和风险筛选，只能固定取最近 100 条。证据：`app/routers/content_guard.py:102-132`
    
510.  内容防护设置保存后先 `applySettings()` 再 `loadOverview()`，失败情况下可能出现局部状态已变但总览刷新失败的混合 UI 状态。证据：`app/static/js/app.js:9170-9181`
30 previous messages
511.  `content_guard_max_scan_bytes` 只有下限没有上限，管理员可配置超大扫描窗口，放大 CPU/内存消耗。证据：`app/schemas/content_guard.py`、`app/services/content_runtime_guard_service.py`
    
512.  `content_guard_stream_buffer_max_bytes` 也没有上限，低信任流式预检可被配置成超大内存缓冲。证据：`app/schemas/content_guard.py`、`app/services/proxy_service.py`
    
513.  前端“最大延迟”允许到 `3000`，但后端 schema 当前限制 `<=500`，501-3000 的值会前端可填、后端拒绝。证据：`app/templates/content_guard.html`、`app/static/js/app.js`、`app/schemas/content_guard.py`
    
514.  规则动作 `allow` 没有真正放行语义，匹配后仍会参与 review/block 判断。证据：`app/services/content_guard_service.py:312-340`
    
515.  若 `allow` 规则同时配置 `risk_level=high`，仍会被当成高风险直接拦截。证据同上。
    
516.  前端保存规则时固定 `reason: ""`，会把默认规则原有原因全部抹掉。证据：`app/static/js/app.js:8855-8875`
    
517.  内容防护允许保存空规则列表，模块显示启用但文本规则实际全部失效，页面无强提醒。证据：`app/schemas/content_guard.py`、`stage33_content_guard_regression_check.py`
    
518.  regex 规则保存时不校验正则合法性，运行时 `re.error` 被静默跳过。证据：`app/schemas/content_guard.py`、`app/services/content_guard_service.py:374-380`
    
519.  自定义 regex 没有超时或复杂度限制，管理员误填灾难性回溯表达式会造成检测 ReDoS 风险。证据：`app/services/content_guard_service.py:374-380`
    
520.  `_request_allows_advertising()` 规则过宽，用户只要说“提供链接”等普通需求，就可能关闭异常外链/广告字段拦截。证据：`app/services/content_guard_service.py:1121-1131`
    
521.  请求正文中的域名会自动加入允许域名，即使用户是在负面提示中说“不要输出 bad.example”，响应再输出该域名也可能不算异常外链。证据：`app/services/content_guard_service.py:647-654`
    
522.  Chat 响应只检查是否存在 `choices` 字段，`choices=[]` 也能通过结构检测。证据：`app/services/content_guard_service.py:816-824`
    
523.  Responses 响应只检查是否存在 `output` 字段，`output=[]` 也能通过结构检测。证据：`app/services/content_guard_service.py:826-834`
    
524.  HTTP 200 返回 `{"error": ...}` 时结构检测会因存在 `error` 而不拦截，可能把上游错误体当正常响应继续处理。证据：`app/services/content_guard_service.py:816-834`
    
525.  `_find_suspicious_response_key()` 递归扫描所有字段名，合法业务 JSON 中的 `contact`、`promo` 等字段也可能被误判为广告字段。证据：`app/services/content_guard_service.py:837-846`、`app/services/content_guard_service.py:980-991`
    
526.  流式分块检测达到 `max_scan_bytes` 后会清空缓冲，跨边界出现的 URL、关键词或正则片段可能被漏检。证据：`app/services/content_runtime_guard_service.py:133-169`
    
527.  首段流式预检在 `buffer_300ms` 模式下遇到第一个 SSE 事件就停止，而不是继续等到字节阈值或时间阈值，首个事件若只是 role/空 delta 会削弱预检效果。证据：`app/services/proxy_service.py:2799-2808`
    
528.  `record_only` 策略只是不拦截响应，但检测阶段仍会按 block 违规记录并可能隔离提供商，不符合“仅记录”的直觉。证据：`app/services/content_runtime_guard_service.py:41-54`、`app/services/content_runtime_guard_service.py:173-181`
    
529.  `safe_error` 策略没有独立安全替代响应实现，最终仍按 502 `content_integrity_violation` 处理。证据：`app/services/content_runtime_guard_service.py:173-181`、`app/services/proxy_service.py:1978-1988`
    
530.  内容防护违规日志固定 `error_type="server_error"`，把策略/完整性问题归类成服务器错误。证据：`app/services/proxy_service.py:1339-1351`
    
531.  切换提供商时，每个被内容防护拦截的候选都会写一条失败 `request_logs`，一个外部请求可能膨胀成多条失败日志。证据：`app/services/proxy_service.py:1948-1978`
    
532.  监控自动隔离按 `request_logs` 高风险条数统计，因此会把“内部切换尝试次数”当成“真实请求次数”触发隔离。证据：`app/services/system_metrics_service.py:950-970`
    
533.  `content_guard_retry_provider_count` 只按 provider id 去重，无法表达同一提供商不同模型或多次尝试的真实重试次数。证据：`app/services/proxy_service.py:1111-1120`
    
534.  可信探针的 HTTP/网络失败结果没有 `content_guard` 字段，聚合时可能不更新提供商模型可信状态。证据：`app/services/content_guard_probe_service.py:159-215`、`app/services/content_trust_probe_service.py:64-71`
    
535.  `probe_failure()` 把内容完整性失败标成 `support_mode="unsupported"`，混淆“能力不支持”和“内容污染失败”。证据：`app/services/content_guard_probe_service.py:956-980`
    
536.  探针成功固定 `native_success=True`、`adapted_success=False`，没有反映实际是否经过协议适配。证据：`app/services/content_guard_probe_service.py:223-250`
    
537.  `probe_sse()` 只读取最多 6 个 chunk，长流式响应可能尚未到 `[DONE]` 就被误判失败。证据：`app/services/content_guard_probe_service.py:536-616`
    
538.  `probe_sse()` 丢弃 `_fallback_trace`，结果里没有端点回退或适配过程。证据：`app/services/content_guard_probe_service.py:522-528`、`app/services/content_guard_probe_service.py:616-625`
    
539.  可信探针更新状态时基本不维护 `content_integrity_score`，路由打分仍可能给 degraded 提供商完整性加分。证据：`app/services/content_guard_probe_service.py:1063-1118`、`app/services/router_service.py:1093-1110`
    
540.  内容防护自动预检调度间隔可配置，但分布式锁 TTL 固定 1 小时，设置为 300 秒也可能实际一小时才跑。证据：`app/tasks.py:416`、`app/tasks.py:488-523`
    
541.  `_should_run_scheduled_content_probe()` 对 `unknown/degraded/blocked` 直接返回 True，绕过配置间隔控制。证据：`app/services/health_service.py:2168-2178`
    
542.  blocked 模型会被定时反复探测，但探测通过也不会自动解除 blocked，形成无效上游调用。证据：`app/services/health_service.py:2174-2178`、`app/services/content_guard_probe_service.py:1082-1094`
    
543.  管理员手动标记模型可信时只写入固定答案和外链广告识别，缺少必需的流式污染检测确认。证据：`app/services/provider_service.py:1066-1115`
    
544.  `ContentTrustProbeService` 与 `ContentGuardModuleService` 各自维护一份外部目标解析和假 provider 构造逻辑，重复且容易漂移。证据：`app/services/content_trust_probe_service.py:132-166`、`app/services/content_guard_module_service.py:232-266`
    
545.  外部探针构造 `id=0` 的假 Provider/ProviderModel 复用代理路径，外部检测与内部运行时边界不清晰。证据同上。
    
546.  外部探针默认声明 `supports_stream=True`、`supports_tools=True`，即使外部目标未说明具备这些能力。证据：`app/services/content_trust_probe_service.py:151-160`
    
547.  内容防护 overview 查询所有提供商，包括禁用提供商；前端内部检测下拉也会展示它们。证据：`app/services/content_guard_module_service.py:31-74`、`app/static/js/app.js:8906-8913`
    
548.  手动探针 `_resolve_probe_target()` 不校验 provider 是否启用，禁用提供商仍可被触发上游检测。证据：`app/services/content_trust_probe_service.py:168-185`
    
549.  `_resolve_probe_target()` 传入 `provider_model_id` 时不校验模型 `enabled`，直接 API 调用可检测禁用模型。证据：`app/services/content_trust_probe_service.py:174-181`
    
550.  内容防护结果前端映射缺少 `error`，后端正式枚举 `RESULT_ERROR` 到页面会显示原始英文或未知态。证据：`app/static/js/app.js:108-114`、`app/services/content_guard_service.py`
    
551.  日志详情的内容防护动作缺少 `record`、`async_review`、`switch_provider`、`safe_error` 等专门中文映射。证据：`app/static/js/app.js:2311-2395`、`app/static/js/app.js:10828`
    
552.  内容防护运行时事件接口仍从 `request_logs` 合成最近 100 条，不读取 `request_content_guard_events`，专门事件表没有成为查询主口径。证据：`app/routers/content_guard.py:102-132`
    
553.  运行时事件接口没有分页、时间范围、provider、result、risk 等筛选参数。证据：`app/routers/content_guard.py:102-132`
    
554.  内容防护页面规则表把分类、匹配项、风险、动作、分数、可信度全部平铺，和项目“核心主列 + 按需详情”的高密度列表规范冲突。证据：`app/templates/content_guard.html:307-323`
    
555.  本地内容检测只支持纯文本，不能指定 endpoint、request\_payload、JSON 模式或工具调用场景，无法复现实际代理检测上下文。证据：`app/templates/content_guard.html:328-342`、`app/static/js/app.js:9147`
    
556.  外部探针密钥提交后仍保留在密码输入框 DOM 中，没有在检测后清空。证据：`app/static/js/app.js:9007-9023`、`app/static/js/app.js:9186-9196`
    
557.  `ContentGuardSettingsUpdate.content_guard_probe_interval_sec` 只有最小值无最大值，管理员可设置极大值导致自动预检长期不运行。证据：`app/schemas/content_guard.py`
    
558.  通用设置入口的 `content_guard_url_allowlist_json` 无长度上限，专用入口有上限，且两个入口都能影响运行时同一字段。证据：`app/schemas/setting.py`、`app/services/setting_service.py`
    
559.  `stage33_content_guard_regression_check.py` 没有覆盖 `record_only` 是否会隔离提供商这一关键策略语义。证据：`stage33_content_guard_regression_check.py`
    
560.  `stage33_content_guard_regression_check.py` 没有覆盖自动预检调度锁 TTL 与配置间隔是否一致。证据：`stage33_content_guard_regression_check.py`
19 previous messages
561.  类型化日志表缺少正式迁移：`ExceptionEvent`、`HealthCheckRun`、`BackgroundJobEvent`、`AssetEvent` 等表定义在 [logging\_events.py](./app/models/logging_events.py:298)，但 `migrations/` 中未找到对应建表 SQL；生产禁用自动 DDL 时 `/api/logging/*` 会直接不可用或全为 0。
    
562.  `/api/logging/*` 类型日志依赖独立表，和主请求日志 `request_logs` 不是同一存储；若历史数据只在 `request_logs`，新 Tab 会显示 0，用户会误以为日志丢失。证据：[logging\_api.py](./app/routers/logging_api.py:158) 起各接口直接查类型表。
    
563.  日志中心初始化是“全有或全无”：`initLogs` 只要缺任意请求日志 DOM 或类型日志 DOM 就 `return`，导致异常事件、健康检查、计费、后台任务、素材按钮全部不绑定。证据：[app.js](./app/static/js/app.js:13624)。
    
564.  类型日志 Tab 的点击事件在上述初始化校验之后才注册；一个无关筛选控件 ID 变更，就会表现为“下面几个按钮根本点不动”。证据：[app.js](./app/static/js/app.js:13823)。
    
565.  类型日志按钮没有 `aria-selected` / `role=tab` 状态，只切 `.active` 类，键盘和读屏用户无法确认当前日志类型。证据：[logs.html](./app/templates/logs.html:10)、[app.js](./app/static/js/app.js:14105)。
    
566.  页面标题固定为“请求日志中心”，切到异常、健康、计费等类型后标题不变，信息架构误导用户。证据：[logs.html](./app/templates/logs.html:5)。
    
567.  类型日志只有一个共享 `typedPanel`，没有每种日志的独立摘要区；切到健康检查或计费时仍只是表格，无法展示该类型最重要的统计口径。证据：[logs.html](./app/templates/logs.html:216)。
    
568.  类型日志过滤区是动态插入字段，但没有按日志类型保存 URL 之外的完整状态；Tab 切换时 `typedState.page` 被强制置 1，用户从某类型第 N 页查看详情后切回来会丢页。证据：[app.js](./app/static/js/app.js:14115)。
    
569.  普通请求日志与类型日志共用 URL 参数名 `page`、`page_size`，切换 Tab 会互相污染分页状态。证据：[app.js](./app/static/js/app.js:13973)、[app.js](./app/static/js/app.js:14070)。
    
570.  类型日志导出强行写入 `params.set("log_type", state.activeTab)`，但后端 `export_typed_logs` 也用 `log_type` 判断分支；如果 URL 中已有同名过滤语义，未来扩展会冲突。证据：[app.js](./app/static/js/app.js:13814)、[logging\_api.py](./app/routers/logging_api.py:286)。
    
571.  计费日志接口把 `token_finalize` 和 `billing_process` 两张表分别取 `page * page_size` 后内存合并排序，数据多时分页不稳定且越往后越重。证据：[logging\_api.py](./app/routers/logging_api.py:240)。
    
572.  计费日志合并排序使用 `str(created_at)`，不是数据库时间排序；不同时区、空值或序列化格式变化会影响顺序。证据：[logging\_api.py](./app/routers/logging_api.py:281)。
    
573.  计费日志的 `result` 过滤被复用到 `billing_status`：`billing_status or result`，用户按“失败”筛选时可能漏掉 `billing_status=failed` 但 `result` 语义不同的记录。证据：[logging\_api.py](./app/routers/logging_api.py:265)。
    
574.  后台任务日志只查 `background_job_events`，不读取 Redis 中正在运行的 `scheduler:job:*:state`；任务已运行但尚未落库或落库失败时前端显示 0。证据：[logging\_api.py](./app/routers/logging_api.py:426)、[tasks.py](./app/tasks.py:254)。
    
575.  后台任务开始时写一条 `running`，完成时更新同一行；如果进程崩溃，`running` 会永久挂住，日志中心没有超时/失联状态修正。证据：[tasks.py](./app/tasks.py:262)、[tasks.py](./app/tasks.py:161)。
    
576.  后台任务日志的 `trigger_type` 适配器默认永远是 `"scheduler"`，手动触发、启动恢复、补偿任务如果不显式传入会被错误归类。证据：[background\_job\_adapter.py](./app/logging/adapters/background_job_adapter.py:19)。
    
577.  后台任务的跳过状态只把 `status.startswith("skipped")` 归为成功结果，前端筛“失败”时看不到大量锁冲突或锁不可用问题。证据：[background\_job\_adapter.py](./app/logging/adapters/background_job_adapter.py:25)。
    
578.  类型日志队列只有当前 Web 进程内 worker 存活时才允许入队；独立 worker 架构下 Web 进程 `_workers` 为空，会全部同步写库，违背生产分离预期。证据：[queue.py](./app/logging/queue.py:32)。
    
579.  类型日志队列停机时直接 cancel worker，没有优雅 drain 队列；正在 `PROCESSING_KEY` 的事件要等下次启动 recover，异常停机窗口内前端可能短期显示 0。证据：[queue.py](./app/logging/queue.py:60)。
    
580.  类型日志写批次失败会整批回滚，但 worker 只 warning 后循环；没有失败计数、告警事件或死信队列，日志中心静默缺数。证据：[queue.py](./app/logging/queue.py:121)。
    
581.  `DatabaseLogSink.write` 对未知事件模型直接 `return None`，不会报错；新增事件名注册漏了时，前端对应日志永远为 0 且无告警。证据：[sinks.py](./app/logging/sinks.py:12)。
    
582.  请求日志派生类型事件只在 `LogService.create_log` 成功后同步生成；历史 `request_logs` 不会自动回填到新类型表，升级后新 Tab 天然空。证据：[log\_service.py](./app/services/log_service.py:331)。
    
583.  内容防护失败最终落请求日志时只写 `content_guard_retry_provider_count` 和 `content_guard_final_strategy`，没有写 `content_guard_result/risk/reason`，导致请求日志筛“内容防护结果”查不到最终失败。证据：[proxy\_service.py](./app/services/proxy_service.py:6862)。
    
584.  `RequestLogRecorder` 从汇总日志回补内容防护事件时判断 `log.content_guard_result == "block"`，但运行时常用结果可能是 `blocked` / `review` / `pass`，会把阻断事件归成成功。证据：[request\_adapter.py](./app/logging/adapters/request_adapter.py:267)。
    
585.  非流式内容防护先追加 `request_content_guard` typed trace，再阻断和切换；同一请求多候选时会产生多个内容防护事件，但最终普通请求日志只暴露一个模糊终态。证据：[proxy\_service.py](./app/services/proxy_service.py:2004)、[proxy\_service.py](./app/services/proxy_service.py:2060)。
    
586.  流式内容防护的终端 typed 事件固定 `guard_stage: "stream_buffer"`，即使实际是首段后 chunk 检测命中，也会误导排查。证据：[proxy\_service.py](./app/services/proxy_service.py:3327)。
    
587.  流式内容防护为了避免协议转换场景而直接关闭检测：`adapt_chat_response_to_responses` 等为真时 `stream_guard_enabled` 为假；这会让适配链路绕过内容防护。证据：[proxy\_service.py](./app/services/proxy_service.py:2854)。
    
588.  流式首段预取命中后会写日志并释放当前租约，但后续是否重试依赖 `break` 外层逻辑；这类中间阻断对前端只表现为最终失败，缺少“已切换几次”的用户可见链路。证据：[proxy\_service.py](./app/services/proxy_service.py:2921)、[proxy\_service.py](./app/services/proxy_service.py:2962)。
    
589.  内容防护错误详情在 `_build_content_guard_error_detail` 中手写 `error` 对象，后续 `_finalize_or_raise_upstream_error` 又二次改 message/retryable，存在同一错误多处拼装。证据：[proxy\_service.py](./app/services/proxy_service.py:6651)、[proxy\_service.py](./app/services/proxy_service.py:6866)。
    
590.  类型日志导出为空时 `_items_to_csv` 会输出空表头 CSV，用户无法知道是无数据、筛选过严还是接口异常。证据：[logging\_api.py](./app/routers/logging_api.py:598)。
    
591.  `_list_model` 对过滤值只做等值匹配，不支持多选、模糊状态或枚举别名；不同类型日志结构差异大，统一 helper 会限制筛选表达力。证据：[logging\_api.py](./app/routers/logging_api.py:537)。
    
592.  异常日志接口按 `occurred_at` 过滤，素材日志按 `created_at`，健康按 `started_at`，但前端统一显示“开始时间/结束时间”，用户不知道各类型时间字段不同。证据：[logging\_api.py](./app/routers/logging_api.py:177)、[logging\_api.py](./app/routers/logging_api.py:529)。
    
593.  类型日志详情弹窗默认最多展示 `Object.entries(item).slice(0, 24)`，字段多的事件会被截断且无提示。证据：[app.js](./app/static/js/app.js:14365)。
    
594.  健康检查详情只在 `run_id` 存在时拉 `/health-runs/{run_id}`，单条探针或异常行没有统一详情入口，排查会断链。证据：[app.js](./app/static/js/app.js:14251)。
    
595.  清空按钮文案说明“不会清空异常事件、健康检查、计费、后台任务或素材日志”，但页面没有提供这些类型的清理、保留期或归档入口，长期会无限增长。证据：[app.js](./app/static/js/app.js:13795)。
    
596.  素材日志只记录 `sha256_prefix` 前 16 位，不记录完整 hash；排查重复文件或安全事件时无法用日志独立定位完整素材。证据：[asset\_adapter.py](./app/logging/adapters/asset_adapter.py:28)。
    
597.  异常事件会记录 `stack_excerpt` 最多 4000 字符到独立表，前端详情可直接展示原始 JSON；若异常 detail 含敏感信息，日志中心会扩大暴露面。证据：[exception\_adapter.py](./app/logging/adapters/exception_adapter.py:31)。
    
598.  HTTPException 全部进入异常事件表，包括普通 4xx；异常事件会被业务拒绝、无权限、参数错误噪声淹没，真正 5xx 更难发现。证据：[main.py](./app/main.py:1197)。
    
599.  类型日志接口没有返回队列积压状态；当 typed logging 队列未消费时，前端只能看到 0 或旧数据，无法提示“日志仍在落库”。证据：[logging\_api.py](./app/routers/logging_api.py:581) 对比请求日志接口的队列字段。
    
600.  日志中心普通请求区有统计卡，类型日志区没有任何总数以外的摘要；用户点击“计费日志”看不到总金额、失败补全数、待处理数等关键指标。证据：[logs.html](./app/templates/logs.html:19)、[logs.html](./app/templates/logs.html:216)。
12 previous messages
601.  `ContentGuardRuleService` 只是空继承，规则注册、解析、匹配、评分算法仍在 `ContentGuardService`，没有真正形成规范要求的三层职责边界。证据：[content\_guard\_rule\_service.py](./app/services/content_guard_rule_service.py:6)、[content\_guard\_service.py](./app/services/content_guard_service.py:279)。
    
602.  `ContentGuardModuleService` 和 `ContentTrustProbeService` 各自维护探针汇总与聚合逻辑，存在重复实现，后续一个改了另一个漏改会导致前端手动检测和自动可信检测结论不一致。证据：[content\_guard\_module\_service.py](./app/services/content_guard_module_service.py:343)、[content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:243)。
    
603.  规则 JSON 解析失败时静默回退默认规则，管理员保存的规则损坏后界面可能仍显示“有规则”，但运行时实际按默认规则检测。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:522)。
    
604.  `normalize_rules` 对重复规则 ID 直接跳过后者，没有记录错误或警告；如果历史数据或手工写库产生重复规则，真实生效规则不可见。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:536)。
    
605.  `_coerce_rule` 对非法 `match_type`、`risk_level`、`action` 静默降级为默认值，配置错误会变成另一种策略而不是失败。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:550)。
    
606.  schema 层校验了规则 ID 唯一，但服务层仍接受任意 dict 并静默清洗；通用设置写入、历史数据或脚本导入可绕过 API schema，形成双重口径。证据：[content\_guard.py](./app/schemas/content_guard.py:72)、[content\_guard\_service.py](./app/services/content_guard_service.py:536)。
    
607.  regex 规则在检测时逐条 `re.search`，没有超时、复杂度限制或预编译防护，复杂正则可能拖垮热路径。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:349)。
    
608.  regex 编译错误被 `except re.error: continue` 吞掉，管理员无法知道某条规则永远不生效。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:349)。
    
609.  规则动作 `allow` 只是参与匹配，不会真正短路放行；`allow` 规则命中后仍可能因为高风险或分数被 block，语义误导。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:312)。
    
610.  命中规则的 `score_delta` 用 `abs()` 汇总，即使未来配置为 0 或非负被裁剪，也不会表达“降低风险”的意图，评分模型只会加重风险。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:313)。
    
611.  URL 白名单默认总是合并 `DEFAULT_URL_ALLOWLIST`，管理员无法配置“严格无外链”模式。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:628)。
    
612.  `allowed_domains` 会把请求体里出现过的所有域名自动加入允许列表；如果用户 prompt 中引用恶意域名要求分析，模型输出同域名广告链接也会被放过。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:647)。
    
613.  `_extract_scan_text` 跳过 `image_url` 整个字段，若 `image_url` 是对象并包含远程 URL 或文本元数据，URL 防护无法看到。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:1025)。
    
614.  `_extract_scan_text` 只统计字符串原始字节，遇到大量 dict/list 层级没有节点数上限，恶意深层 JSON 会造成递归和遍历压力。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:1029)。
    
615.  `_extract_response_scan_text` 对 Responses 只取 `output[*].content[*].text/output_text/delta` 等少数字段，遗漏 `summary`、`reasoning`、嵌套 item 等可能携带污染文本的位置。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:1055)。
    
616.  Chat Completions 的 `choices[*].message.content` 若是数组多模态结构，当前 `add()` 只接收字符串，会漏扫文本片段。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:1060)。
    
617.  `record_violation` 对 review 也会把 provider 状态改成 `degraded` 并扣分，`record_only` 或待复核场景会污染路由健康。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:767)。
    
618.  `record_violation` 对严重命中只设置 `provider.circuit_state = "open"`，未同步 `circuit_opened_at`，熔断恢复逻辑可能缺少开启时间。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:785)。
    
619.  `record_violation` 直接修改 provider/model 状态，没有写统一告警事件；和规范要求“自动隔离同时记录 alert\_events”不一致。证据：[content\_guard\_service.py](./app/services/content_guard_service.py:767)。
    
620.  `enabled_for_request` 在 `content_guard_required=false` 时仍返回 provider 的 `content_guard_enabled`，API Key 上“内容检测可选”实际不能关闭运行时检测。证据：[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:226)。
    
621.  `should_record_violation` 在异步复核开启时不记录 review 结果，导致中风险命中既不会入违规状态，也没有看到异步复核队列实现。证据：[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:244)。
    
622.  `stream_should_buffer` 只对低信任或强制可信路由启用首段缓冲，标准信任 provider 即使开启全局内容防护也可能只走放行后扫描。证据：[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:251)。
    
623.  `stream_should_buffer` 的强制可信分支要求 `content_guard_required=true`，当 API Key 标记可选时，即使全局只走可信提供商，也不会触发缓冲保护。证据：[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:258)。
    
624.  `high_risk_strategy` 支持 `safe_error`，但运行时决策只是返回策略字符串，代理层并没有独立安全错误内容生成策略，容易和普通 block/switch 混同。证据：[content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:172)。
    
625.  `ContentGuardSettingsUpdate.content_guard_url_allowlist_json` 只限制长度，不校验 JSON 或域名格式；错误配置要到运行时才被静默拆行解析。证据：[content\_guard.py](./app/schemas/content_guard.py:28)、[content\_guard\_service.py](./app/services/content_guard_service.py:628)。
    
626.  外部检测允许 `http://` 明文地址，且没有内网、localhost、云元数据地址拦截，存在 SSRF 与密钥泄露风险。证据：[content\_guard.py](./app/schemas/content_guard.py:100)。
    
627.  外部检测的 `base_url`、`api_key`、`model_name` 没有最大长度限制，异常长输入可能进入请求构造、日志或 DOM 展示链路。证据：[content\_guard.py](./app/schemas/content_guard.py:93)。
    
628.  `run_trust_probe` 强制覆盖 `probe_keys` 为必需三项，用户在前端选择其它探针时会被忽略，交互预期和实际执行不一致。证据：[content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:29)。
    
629.  可信探针缺失任一必需项时只返回 `review`，不会立即 block；如果某模型不支持流式，缺 `content_sse` 会长期降级而不是明确“不具备可信资格”。证据：[content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:996)。
    
630.  `apply_content_probe_health` 在失败次数达到 3 次时直接把 provider 和 model 都 blocked/open circuit，缺少“10 分钟内 3 次高风险”的时间窗口判断。证据：[content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1047)。
    
631.  内容探针通过时只恢复 model 的 `content_integrity_status=passed`，不恢复 provider 的 `content_integrity_status`，provider 可能仍保持 degraded/blocked 影响路由。证据：[content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1074)。
    
632.  内容探针通过时如果 model 已 blocked，不会恢复 blocked；管理员或自动恢复没有明确路径，blocked 可能成为单向状态。证据：[content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1074)。
    
633.  `apply_content_probe_health` 序列化结果里的 `status` 先写旧状态，后面部分路径才覆盖；失败路径和通过路径状态字段语义不完全一致。证据：[content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1075)。
    
634.  外链广告识别探针只测 3 个非常简单 prompt，无法覆盖长上下文、工具调用、Markdown 链接、引用来源等真实污染场景。证据：[content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:648)。
    
635.  外链广告识别探针将 prompt 中的“不要提供链接/广告”作为请求上下文，而 `allowed_domains` 会从请求体抽取域名白名单；未来一旦 prompt 包含示例域名，探针可能自我放行。证据：[content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:648)、[content\_guard\_service.py](./app/services/content_guard_service.py:647)。
    
636.  SSE 探针只读取最多 6 个 chunk，慢速污染或后段污染不会被可信检测发现。证据：[content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:512)。
    
637.  SSE 探针使用固定答案 payload，不覆盖模型在普通长流式回答中插入广告、外链或模板污染的情况。证据：[content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:514)。
    
638.  路由候选过滤无论 `content_guard_required` 是否为 true，都会调用内容策略过滤 blocked/低分 provider；API Key 的“内容检测可选”并不等于可走低信任/已隔离 provider，语义不清。证据：[router\_service.py](./app/services/router_service.py:237)、[content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:100)。
    
639.  `get_trust_decision_for_route` 只看 provider 级 `content_integrity_score`，没有 model 级评分；单个模型污染会影响整个 provider 或被 provider 分数掩盖。证据：[content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:100)。
    
640.  路由打分仍给 `trust_level=blocked` 一个 -100 分的惩罚项，但前面已经硬过滤 blocked；这段分值逻辑基本不可达，说明内容策略过滤与评分职责混杂。证据：[router\_service.py](./app/services/router_service.py:1093)。
21 previous messages
641.  内容防护前端把配置、探测、规则编辑、本地检测、结果渲染全部塞进 `initContentGuardPage` 一个超大函数，职责边界不清，后续任何小改都容易互相影响。证据：[app/static/js/app.js](./app/static/js/app.js:8741)。
    
642.  内容防护首页初始加载失败时只弹 Toast，指标仍停留在 `-`、规则区仍可能显示“加载中”，没有页面级错误态和重试入口，用户无法判断是无数据还是接口坏了。证据：[app/static/js/app.js](./app/static/js/app.js:8975)。
    
643.  提供商下拉在没有可检测提供商时直接渲染为空字符串，没有“暂无提供商/去添加提供商”的空状态，提交时才报错。证据：[app/static/js/app.js](./app/static/js/app.js:8912)。
    
644.  每次刷新概览都会重建提供商下拉，没有保留用户当前选择的提供商/模型，可能在用户准备检测时被刷新重置。证据：[app/static/js/app.js](./app/static/js/app.js:8968)。
    
645.  探针选项每次渲染都按固定默认集合勾选，会丢失用户手动勾选/取消勾选状态。证据：[app/static/js/app.js](./app/static/js/app.js:8930)。
    
646.  内容防护规则表没有服务端分页、关键词搜索、分类筛选或启用状态筛选，规则上限已到 200 时只能一次性渲染全表，不符合数据列表分页规范。证据：[app/schemas/content\_guard.py](./app/schemas/content_guard.py:72)、[app/templates/content\_guard.html](./app/templates/content_guard.html:307)。
    
647.  规则编辑只暴露名称、分类、匹配、风险、动作、扣分、可信度，不暴露 `reason` 字段，保存时还固定写成空字符串，导致后端规则原因被前端清空。证据：[app/static/js/app.js](./app/static/js/app.js:8879)。
    
648.  规则删除按钮没有二次确认，也没有撤销，对高风险检测规则这种安全配置来说误删成本过高。证据：[app/static/js/app.js](./app/static/js/app.js:9106)。
    
649.  恢复默认规则使用 `window.confirm`，与项目统一弹层/反馈体系割裂，且无法展示将覆盖哪些规则。证据：[app/static/js/app.js](./app/static/js/app.js:9130)。
    
650.  新增规则默认填入“待填写”作为真实匹配项，如果用户直接保存，会把占位词作为有效规则写入，造成误判。证据：[app/static/js/app.js](./app/static/js/app.js:9091)。
    
651.  规则 ID 自动用时间戳生成但界面不允许编辑，用户无法建立稳定、可读、可迁移的规则标识。证据：[app/static/js/app.js](./app/static/js/app.js:8798)。
    
652.  规则校验只检查 ID/名称/分类/匹配项，未在前端提示正则合法性、重复 pattern、动作与风险等级是否矛盾，错误要到后端或运行时才暴露。证据：[app/static/js/app.js](./app/static/js/app.js:8885)。
    
653.  `score_delta` 使用 `Number(value || -8)`，用户输入 `0` 会被当成 falsy 变成 `-8`，导致“零扣分”无法保存。证据：[app/static/js/app.js](./app/static/js/app.js:8878)。
    
654.  `confidence` 使用 `Number(value || 0.7)`，用户输入 `0` 会被强制变成 `0.7`，前端数值语义错误。证据：[app/static/js/app.js](./app/static/js/app.js:8879)。
    
655.  设置里的数值项也大量使用 `Number(value || 默认值)`，空值和 `0` 的语义混在一起，部分字段无法表达管理员真实输入。证据：[app/static/js/app.js](./app/static/js/app.js:9000)。
    
656.  自动预检间隔前端强制最小 300 秒，但没有解释保存后调度器是否会立即重排，用户可能以为保存即生效。证据：[app/static/js/app.js](./app/static/js/app.js:9002)、[app/tasks.py](./app/tasks.py:530)。
    
657.  URL 白名单说明写“用户请求中出现过的域名会自动加入本次白名单”，这实际是在提示可被请求体影响检测结果，安全语义非常危险。证据：[app/templates/content\_guard.html](./app/templates/content_guard.html:219)。
    
658.  外部渠道 API Key 输入框会把密钥长期保留在 DOM 中，检测完成后不清空，旁人或浏览器插件可读取。证据：[app/templates/content\_guard.html](./app/templates/content_guard.html:267)、[app/static/js/app.js](./app/static/js/app.js:9016)。
    
659.  外部渠道检测没有“显示/隐藏密钥”控件，也没有检测后清除按钮，密钥可用性和安全性都不足。证据：[app/templates/content\_guard.html](./app/templates/content_guard.html:260)。
    
660.  外部渠道检测只允许选择 `/responses` 与 `/chat/completions`，但页面没有解释会拼接到 base\_url 还是替换路径，容易造成重复 `/v1/v1` 或错误端点。证据：[app/templates/content\_guard.html](./app/templates/content_guard.html:275)。
    
661.  外部渠道后端临时构造 `Provider(id=0)`、`ProviderModel(id=0)`，多个外部检测结果在日志/trace 中无法区分具体目标。证据：[app/services/content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:137)。【已完成】
    
662.  外部渠道返回给前端的 target 包含 base\_url，若后续结果弹层或日志展示，可能泄露敏感内网地址或第三方接口地址。证据：[app/services/content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:162)。【已完成】
    
663.  内容防护结果表把长原因直接放在表格正文，没有统一问号 Tooltip 收纳，长错误会挤爆表格。证据：[app/templates/content\_guard.html](./app/templates/content_guard.html:369)、[app/static/css/app.css](./app/static/css/app.css:3291)。【已完成】
    
664.  规则表强制 `min-width: 1180px`，移动端只能横向滚动，缺少抽屉式或详情式渐进披露。证据：[app/static/css/app.css](./app/static/css/app.css:3221)。【已完成】
    
665.  内容防护的探针复选框仍是浏览器默认 checkbox 视觉，和全站启停类开关规范不一致。证据：[app/templates/content\_guard.html](./app/templates/content_guard.html:282)、[app/static/css/app.css](./app/static/css/app.css:3180)。【已完成】
    
666.  内容防护分段切换把 radio 设为 `opacity:0` 且 `pointer-events:none`，若焦点样式不足，键盘用户很难感知当前焦点。证据：[app/static/css/app.css](./app/static/css/app.css:3128)。【已完成】
    
667.  隐藏的外部/内部检测区在 CSS 中一处 `display:none`，另一处又覆盖成 `display:grid; visibility:hidden`，同一 hidden 语义不统一，后续维护容易出错。证据：[app/static/css/app.css](./app/static/css/app.css:3074)、[app/static/css/app.css](./app/static/css/app.css:3163)。【已完成】
    
668.  本地内容检测只展示结果、风险、置信度和命中规则名，不展示命中片段、分类、处置动作，排查价值不足。证据：[app/static/js/app.js](./app/static/js/app.js:9156)。【已完成】
    
669.  本地内容检测命中 `review` 时按钮反馈显示“命中”并按错误态处理，无法区分“阻断”和“待复核”。证据：[app/static/js/app.js](./app/static/js/app.js:9162)。【已完成】
    
670.  探测完成 Toast 只展示总结果，不展示目标、通过数、失败探针，用户还要自己去看表格。证据：[app/static/js/app.js](./app/static/js/app.js:9199)。【已完成】
    
671.  探测成功后立即 `loadOverview()`，会重置页面上的选择状态和探针勾选状态。证据：[app/static/js/app.js](./app/static/js/app.js:9200)。【已完成】
    
672.  API Key 管理端把“内容检测 要求/可选”混在路由策略列里，缺少独立筛选，管理员无法快速找出可选检测的密钥。证据：[app/static/js/app.js](./app/static/js/app.js:12337)。【已完成】
    
673.  API Key 创建/编辑模板中没有看到模板级 `content_guard_required` 控件初始化，批量套模板可能无法同步内容检测策略。证据：[app/static/js/app.js](./app/static/js/app.js:12170)。【已完成】
    
674.  用户端允许普通用户自己关闭“要求内容检测”，这和内容完整性作为全局治理能力的定位冲突，可能让用户绕过强制检测语义。证据：[app/templates/user\_api\_keys.html](./app/templates/user_api_keys.html:293)、[app/routers/user\_portal.py](./app/routers/user_portal.py:793)。【已完成】
    
675.  `content_guard_required=false` 并不等于关闭检测，运行时会回退到提供商的 `content_guard_enabled`，前端“可选”文案会误导用户。证据：[app/services/content\_runtime\_guard\_service.py](./app/services/content_runtime_guard_service.py:226)。【已完成】
    
676.  路由缓存 key 区分 `guard` 和 `guard-optional`，但候选内容策略仍会排除 blocked/低分提供商，容易造成“可选检测”缓存语义与实际候选筛选不一致。证据：[app/services/router\_service.py](./app/services/router_service.py:1036)、[app/services/content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:111)。【已完成】
    
677.  `ProxyService._with_content_guard_route_policy` 是空实现，说明内容防护路由策略没有根据请求类型、端点、工具、多模态动态调整。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:240)。【已完成】
    
678.  路由评分仍给内容完整性分数加 bonus/penalty，但 blocked 和低分又是硬排除，评分与硬约束职责混在一起。证据：[app/services/router\_service.py](./app/services/router_service.py:1093)。【已完成】
    
679.  自动预检任务锁 TTL 固定 1 小时，但任务触发间隔可配置，长探测或大量模型时可能和下一轮调度重叠/跳过。证据：[app/tasks.py](./app/tasks.py:425)。【已完成】
    
680.  自动预检会对 `blocked` 模型每轮都返回 True，可能持续探测已隔离模型，浪费资源并放大失败记录。证据：[app/services/health\_service.py](./app/services/health_service.py:2177)。【已完成】
    
681.  内容探针失败次数没有时间窗口，累计 3 次就 blocked，临时网络波动也可能永久隔离模型。证据：[app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1092)。【已完成】
    
682.  探针通过时如果模型此前是 blocked，不会恢复为 passed，但 provider 级 blocked 也没有自动恢复路径，容易形成只能人工清理的半死状态。证据：[app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:1085)。【已完成】
    
683.  `run_trust_probe` 会强制覆盖用户传入的 probe\_keys，页面若调用 trust-probe 类接口，用户勾选项会被无视。证据：[app/services/content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:29)。【已完成】
    
684.  未知探针被标记为 skipped 但不是异常，前端可能只看到“已跳过”，无法发现前后端 probe key 不匹配。证据：[app/services/content\_trust\_probe\_service.py](./app/services/content_trust_probe_service.py:211)。【已完成】
    
685.  SSE 探针最多读取 6 个 chunk，后段污染无法覆盖，之前固定答案正常但后续插广告的流式模型可能漏检。证据：[app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:538)。【已完成】
    
686.  外链广告识别探针串行跑 3 个场景，没有单场景超时和总超时控制，慢渠道会拖垮手动检测体验。证据：[app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:648)。【已完成】
    
687.  探针调用复用正式代理转发，但没有在结果中明确标注“这是检测流量”，容易混入普通请求日志/指标理解。证据：[app/services/content\_guard\_probe\_service.py](./app/services/content_guard_probe_service.py:171)。【已完成】
    
688.  内容防护迁移只给 `request_logs.content_guard_result` 建索引，风险等级、创建时间组合、provider 维度没有索引，日志中心按风险/时间筛选会慢。证据：[migrations/2026-06-06\_add\_content\_guard\_governance.sql](./migrations/2026-06-06_add_content_guard_governance.sql:53)。【已完成】
    
689.  第二份迁移只给 latency 建部分索引，没有给 `content_guard_final_strategy`、`retry_provider_count`、`buffer_wait_ms` 建查询索引，治理分析能力不足。证据：[migrations/2026-06-07\_extend\_content\_guard\_strategy\_metrics.sql](./migrations/2026-06-07_extend_content_guard_strategy_metrics.sql:10)。【已完成】
    
690.  内容防护相关 typed event 表在模型中存在，但迁移搜索不到对应 `CREATE TABLE`，生产禁用自动 DDL 时日志接口可能直接查不到表。证据：[app/models/logging\_events.py](./app/models/logging_events.py:154)。【已完成】
    
691.  回归脚本主要是静态字符串扫描和少量单元断言，没有真实浏览器点击内容防护页面，无法发现按钮失效、布局溢出、状态丢失问题。证据：[stage33\_content\_guard\_regression\_check.py](./stage33_content_guard_regression_check.py:593)。【已完成】
    
692.  回归脚本检查模板里存在字段，但不验证字段能成功提交到后端并影响运行时策略。证据：[stage33\_content\_guard\_regression\_check.py](./stage33_content_guard_regression_check.py:599)。【已完成】
    
693.  回归脚本没有覆盖外部渠道 SSRF、明文 HTTP、内网地址、超长响应、超长 API Key 等安全边界。证据：[stage33\_content\_guard\_regression\_check.py](./stage33_content_guard_regression_check.py:682)。【已完成】
    
694.  回归脚本没有验证迁移脚本实际能从空库建出 typed logging 表，只检查代码字符串，无法发现生产库缺表。证据：[stage33\_content\_guard\_regression\_check.py](./stage33_content_guard_regression_check.py:593)。【已完成】
    
695.  日志列表的内容防护筛选只支持 result/risk 两个通用字段，没有 guard\_stage、action、final\_strategy、retry\_count 等内容防护专属筛选。证据：[app/routers/logs.py](./app/routers/logs.py:54)。【已完成】
    
696.  请求日志导出也只带 result/risk 过滤，无法按内容防护处置策略导出污染事件。证据：[app/routers/logs.py](./app/routers/logs.py:134)。【已完成】
    
697.  内容防护事件写入 trace 时 `matched_rules_json` 固定为 `null`，导致日志详情无法还原到底命中了哪些规则。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:2011)。【已完成】
    
698.  非流式检测使用原始 `response` 做检测，但日志展示使用 `client_response`，如果响应经过模型名恢复/协议适配，检测证据与用户看到的响应可能不一致。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:1967)。【已完成】
    
699.  流式检测在协议适配场景直接禁用，chat/responses 适配流量可能完全绕过流式内容防护。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:2854)。【已完成】
    
700.  流式首段未启用缓冲时默认结果写“未启用首段缓冲审核”，但后续仍可能做分块扫描，日志语义容易让人误以为整条流都未检测。证据：[app/services/proxy\_service.py](./app/services/proxy_service.py:2876)。【已完成】
    
