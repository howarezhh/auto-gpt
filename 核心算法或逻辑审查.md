# 核心算法或逻辑审查

## 1. 审查范围与依据

本次审查依据当前项目实际代码，不依据历史设想或页面文案推断。重点阅读了以下核心文件：

- `app/routers/proxy.py`：对外 `/v1/*` 入口、鉴权、并发租约、请求体限制、流式/非流式入口分发。
- `app/services/proxy_service.py`：模型请求主链路、模型映射、Token 预检、上游调用、重试、内容防护、日志写入。
- `app/services/router_service.py`：候选提供商与模型筛选、容量硬过滤、健康排序、评分、诊断。
- `app/services/model_mapping_service.py`：源模型到目标模型的映射选择与映射失败回退。
- `app/services/api_key_service.py`：API Key 鉴权、授权提供商、模型白名单、偏好与 bias 策略上下文。
- `app/services/provider_capacity_service.py`：Redis provider 级并发、流式并发、QPS、RPM 租约。
- `app/services/provider_health_state_service.py`：Redis 热健康状态、EWMA 延迟、5 分钟成功率。
- `app/services/openai_error_service.py` 与 `app/services/error_catalog_service.py`：错误分类、retryable/recoverable/category。
- `app/services/content_trust_probe_service.py`、`app/services/content_runtime_guard_service.py`、`app/services/content_guard_service.py`：内容完整性预检、运行时检测、隔离与评分。
- `app/services/provider_service.py`、`app/models/*.py`、`app/models/app_setting.py`：提供商、挂载模型、系统设置与默认值。
- `stage14_*`、`stage25_*`、`stage27_*`、`stage28_*` 回归脚本：现有策略的约束性测试。

结论先行：项目当前真实运行策略不是旧的 `manual / failover / weighted / sticky` 多模式策略，而是统一的“健康优先 + 会话最近成功目标优先 + 容量硬过滤 + 映射模型配置顺序 + 候选耗尽等待重选”策略。

## 2. 当前完整请求链路

### 2.1 对外入口

对外请求进入 `app/routers/proxy.py` 的 `/v1/*` 路由：

1. 先通过 `require_api_client_auth` 解析 Bearer API Key。
2. 通过 `ConcurrencyService.acquire` 申请全局/API Key/账户级请求并发租约。
3. 对 JSON、multipart、raw body 做应用层大小限制。
4. 按端点调用 `ProxyService.forward_json_request` 或 `ProxyService.forward_stream_request`。
5. 请求结束后释放入口并发租约。

入口层的并发租约只覆盖调用方/API Key/账户维度；上游提供商级并发和 QPS/RPM 由 `ProviderCapacityService.async_lease` 在真正调用候选提供商时单独申请。

### 2.2 API Key 策略上下文

`ApiKeyService.authenticate_request` 鉴权后生成 `RoutePolicyContext`：

- `allowed_provider_ids`：来自 API Key 与提供商绑定，是候选 provider 的硬约束。
- `preferred_provider_ids`：来自 API Key 偏好提供商。
- `preferred_region_tags`：偏好区域标签。
- `latency_bias`：延迟惩罚倍率。
- `success_rate_bias`：成功率得分倍率。
- `cost_bias`：成本得分倍率。
- `require_trusted_provider`：来自全局 `trusted_providers_only`。

模型白名单在请求进入模型映射前先检查一次：如果请求的原始 `model` 不在 API Key 白名单内，直接拒绝。模型映射阶段又会检查每个目标模型是否被该 API Key 允许。

### 2.3 模型选择逻辑

当前模型选择分两层：

1. 原始请求模型：来自 payload 的 `model` 字段。
2. 映射目标模型：如果 `model_mappings` 中存在启用的源模型映射，则调用 `ModelMappingService.resolve_for_request` 选择目标模型。

映射目标选择规则：

- 只读取启用的 targets。
- 过滤掉被本轮 failover 排除的目标。
- 过滤掉 API Key 不允许的目标模型。
- 过滤掉模型目录 `ModelCatalog` 不存在或未启用的目标。
- 如果同一会话最近一次成功使用的目标模型仍在映射目标中，则优先该目标。
- 否则按映射配置顺序选择第一个可用目标。

注意：映射阶段只判断目标模型是否在模型目录中可用，不提前判断目标模型是否真的有健康 provider、是否有容量、是否支持本次端点能力。provider 能力和健康会在下一阶段由 `RouterService` 判断。如果当前映射目标后续无候选或上游失败，才递归切换到下一个映射目标。

### 2.4 提供商与挂载模型候选筛选

`RouterService.get_available_candidates` 负责生成候选。实际硬过滤如下：

提供商层过滤：

- provider 必须启用。
- provider 的 `circuit_state` 不能是 `open`。
- provider 不能处于维护模式。
- provider 必须通过内容完整性路由决策。
- provider 必须在 API Key 授权提供商列表内。
- 若请求要求 Chat Completions，provider 协议必须支持 `chat_completions` 或 `both`。
- 若请求要求 Responses，provider 协议必须支持 `responses` 或 `both`。

模型挂载层过滤：

- provider_model 必须启用。
- provider_model 必须通过内容完整性路由决策。
- provider_model 的模型名必须存在于启用的 `ModelCatalog`。
- 若请求指定模型名，provider_model.model_name 必须等于最终模型名。
- 流式请求必须 `supports_stream=True`。
- Chat Completions 请求必须 `supports_chat_completions=True`。
- Responses 请求必须 `supports_responses=True`。
- 图像输入请求必须 `supports_vision=True`。
- 图片生成请求必须 `supports_image_generation=True`。
- vision / image_generation 探针缓存失败时排除。
- provider_model 熔断为 `open` 时，只有自动恢复开启且超过恢复探测间隔，才允许原子改为 `half_open` 进入极少量探针流量。

候选缓存：

- provider 快照和候选列表使用 `CacheService` 缓存。
- 候选缓存 key 包含模型名、能力要求、端点要求、内容防护开关、授权/偏好/bias、最大候选数等。
- 候选缓存 TTL 来自 `route_candidate_cache_ttl_sec`，默认 10 秒，上限 60 秒。

### 2.5 容量硬过滤

候选生成后，`RouterService._filter_capacity_candidates` 再调用 `ProviderCapacityService.snapshots / async_snapshots` 读取 Redis 容量快照，并硬过滤：

- `active_requests >= max_active_requests` 排除。
- 流式请求中 `active_streams >= max_active_streams` 排除。
- `current_qps >= max_qps` 排除。
- `current_rpm >= max_rpm` 排除。
- provider 最近失败率超过 `max_error_rate` 排除。

真正调用上游前，还会用 `ProviderCapacityService.async_lease` 原子申请 provider 容量租约。租约 Lua 脚本同时检查并递增活跃请求、流式请求、QPS、RPM；退出上下文时通过 lease key 幂等释放。

生产环境 Redis 不可用时，provider 容量不可用会导致请求失败；非生产环境允许本地空快照回退。

### 2.6 候选排序与评分

候选评分公式在 `RouterService._route_score` 中：

```text
route_score =
  health_score
  + provider_model_priority_score
  + provider_priority_score
  + success_score
  + cost_score
  + region_bonus
  - latency_penalty
  - ttfb_penalty
  - saturation_penalty
  - recent_error_penalty
```

具体含义：

- `health_score`：healthy=100，degraded=60，unknown=55，unhealthy=0；half_open 最高 35；open=0。
- `provider_model_priority_score`：`max(0, 30 - provider_model.priority)`。
- `provider_priority_score`：`max(0, 20 - provider.priority)`。
- `success_score`：`success_rate * 40 * success_rate_bias`。
- `latency_penalty`：`min(30, latency_ms / 100) * latency_bias`。
- 流式请求额外 `ttfb_penalty = min(25, ewma_ttfb_ms / 100)`。
- `recent_error_penalty`：`min(45, failure_rate * 100)`。
- `saturation_penalty`：取活跃请求、流式请求、QPS、RPM 的最大使用率，最高扣 40。
- `cost_score`：`max(0, 30 - average(input_price_per_1k, output_price_per_1k)) * cost_bias`。
- 区域命中偏好加 20。

最终排序不是简单按 `route_score`：

1. 如果同一会话最近一次成功 provider_model 仍在候选中，则直接排第一。
2. 否则按 `health_tier` 分组。
3. 每个 health tier 内按 `_balanced_order` 排序：
   - `load_factor` 越低越靠前。
   - `route_score` 越高越靠前。
   - provider_model.priority 越小越靠前。
   - provider.priority 越小越靠前。
   - provider.id、provider_model.id 作为稳定兜底。
4. 最后按全局 `max_candidate_count` 截断，默认 10，允许 1-500。

health_tier 的含义：

- 0：模型或 provider 状态包含 healthy/degraded。
- 1：unknown 或 half_open。
- 2：模型或 provider 有 unhealthy。
- 3：open。

注意：当前排序中，`load_factor` 在同一 health tier 内优先于 `route_score`，所以低负载候选会先于高得分候选。

### 2.7 会话粘性

会话粘性来源于 `ProxyService._extract_session_sticky_key`：

- 优先使用显式 session/conversation metadata。
- Responses `client_metadata.session_id` 可作为会话。
- `prompt_cache_key` 是兜底。
- 不再把普通 `user` 字段当作会话粘性 key。

`RouterService.load_recent_session_route` 从 `request_logs` 中查同一 `session_id` 或 `conversation_key` 最近一次成功的 provider/model。只要该 provider_model 仍通过硬过滤进入候选，即使它当前 health tier 较差，也会排在第一。这一行为有回归测试明确保护。

### 2.8 重试策略

#### 同一 provider 内重试

每个候选 provider 的同 provider 重试次数：

```text
same_provider_retry_budget = max(1, min(provider.max_retries, setting.global_max_retries, 2))
```

实际效果：

- 至少尝试 1 次。
- 最多尝试 2 次。
- provider.max_retries 和 global_max_retries 即使配置大于 2，也会被代码硬上限截断为 2。

同 provider 立即重试条件：

- 统一错误分类必须 `recoverable=True`。
- 429 和 425 不做同 provider 立即重试，避免放大限流。
- 4xx 中只有 408、409 允许同 provider 重试。
- 5xx、超时、网络类错误通常允许。

#### 跨 provider 候选切换

每个候选失败后，外层 for 循环会继续尝试下一个候选。容量超限、429、内容防护 switch provider、上游 5xx/超时等都可以促使切换候选。

#### 映射目标模型 failover

当上游错误可恢复，或分类为 model_unavailable、capability_not_supported、rate_limit、timeout、network、upstream_transient、server_error、capacity_limited、route_unavailable 等，`ProxyService._should_retry_mapped_model` 会尝试映射中的下一个目标模型。

映射 failover 通过递归调用 `_forward_json_request_once` 或 `_forward_stream_request_once` 实现，并把当前失败目标加入排除列表，避免重复选择。

#### 候选耗尽等待重试

两种情况会触发候选耗尽等待：

- 没有候选，但诊断认为是容量、熔断、失败率、健康临时异常等可恢复原因。
- 当前候选全部失败，最后一个错误被分类为 recoverable。

等待策略：

- backoff 序列：1s、2s、5s、10s、15s、30s。
- 正常模式最大等待窗口默认 600s，可配置 0-600s。
- 全局 `route_exhausted_retry_infinite_enabled=True` 时无限等待。
- 到达窗口后返回 503，错误码 `all_providers_unavailable_after_retry`。

当前没有 jitter，也没有读取上游 `Retry-After` 动态调整等待。

#### 流式请求重试边界

流式请求在真正向下游发送 SSE 数据前，仍可因连接失败、打开流失败、内容防护首段阻断等切换候选。

一旦下游已经收到数据，则不再切换 provider。后续异常通过 SSE error / DONE 收尾，并写终态日志。客户端主动取消按 499 记录，不计 provider 失败。

### 2.9 健康与熔断策略

上游成功：

- provider.success_count +1。
- provider.failure_count 清零。
- provider_model.success_count +1。
- provider_model.failure_count 清零。
- provider_model.health_status=healthy。
- provider_model.circuit_state=closed。
- 写入 Redis route success，更新 EWMA 延迟与 5 分钟成功率。

上游失败：

- provider.failure_count +1。
- provider_model.failure_count +1。
- 记录 last_latency_ms 与 last_error。
- 若 `force_unhealthy=True`，直接 provider_model unhealthy/open。
- 否则如果 provider 开启自动熔断，provider_model.failure_count 达到阈值，或 half_open 再失败，则 provider_model unhealthy/open。
- 未达阈值则 provider_model degraded/closed。
- provider 汇总状态由 `ProviderService.refresh_provider_state` 根据启用模型状态计算。

熔断阈值：

- provider 可配置 `circuit_breaker_threshold_override`。
- 否则使用全局 `circuit_breaker_threshold`，默认 3。

恢复探测：

- provider_model 熔断 open 后，若 provider 开启自动恢复且超过恢复探测间隔，可被原子置为 half_open 并进入候选。

### 2.10 内容完整性与评分策略

路由前决策由 `ContentTrustProbeService.get_trust_decision_for_route` 执行：

- provider.trust_level == blocked：排除。
- provider.content_integrity_status == blocked：排除。
- provider.content_integrity_score <= 20：排除。
- 若要求可信 provider，trust_level 不是 official/trusted：排除。
- provider_model.content_integrity_status == blocked：排除。
- provider_model 推导出的内容完整性分数 <=20：排除。

provider_model 内容分数推导：

- blocked = 0。
- passed = 100。
- 最近探针 block = 0。
- 最近探针 review = 60。
- 最近探针 pass = 100。
- degraded 根据失败次数落在 21-60。
- unknown 返回 None，不作为低分阻断。

运行时内容防护：

- 非流式响应完成后执行检测。
- 高风险时根据配置执行 block、switch_provider、safe_error、record_only。
- 流式响应根据配置做首段缓冲或分块滑动窗口检测；低信任 provider 默认需要缓冲。
- 内容违规会记录事件，并可能降低 provider/provider_model 内容完整性状态或分数。

## 3. 当前存在的问题

### 3.1 旧路由模式字段仍在模型、设置、页面和 schema 中残留

`AppSetting`、`SettingService`、`schemas/setting.py`、dashboard、前端 JS 仍存在 `route_mode`、`default_provider_id`、`manual_allow_fallback`，但真实主链路没有按这些字段执行 manual/failover/weighted/sticky 分支。

影响：

- 管理员看到的“模式”可能和真实算法不一致。
- `manual` 仍会校验 `default_provider_id`，但主链路不按 default provider 强制路由。
- 历史字段增加理解成本，也容易让后续开发误接旧策略。

### 3.2 `preferred_provider_ids` 当前效果很弱

`RouterService._order_filtered_candidates` 先把 preferred candidates 放到列表前面，但随后 `_primary_route_order -> _balanced_order` 会按 load、score、priority、id 重新排序，基本抹掉 preferred 的列表位置效果。

影响：

- API Key 配置了偏好提供商，但最终未必得到可解释的偏好加权。
- 偏好字段在 UI/API 上看起来像强策略，实际更像无效或弱影响字段。

### 3.3 同一 health tier 内“低负载”优先级高于“高得分”

`_balanced_order` 的排序 key 是 `(load_factor, -route_score, priority...)`，意味着只要负载更低，就可以压过明显更高分的候选。

同时 health_tier 0 同时包含 healthy 和 degraded。一个 degraded 低负载候选可能排在 healthy 高得分候选前面。

影响：

- 稳定性可能被“瞬时低负载”过度影响。
- 高质量、低延迟、高成功率 provider 在负载略高时可能被过早降级。

### 3.4 会话最近成功目标优先过于强

只要最近成功 provider_model 仍在候选中，就会被排第一。回归测试甚至明确允许 unhealthy health tier 的近期目标优先。

影响：

- 有利于 prompt cache 和 stateful Responses 连续性。
- 但当 provider 已 degraded/unhealthy、低成功率或高延迟时，仍可能被粘住。
- 对“稳”和“快”不总是最优。

### 3.5 数据库 route metrics 对映射模型不够精确

`LogService.route_metric_summary` 按 `RequestLog.provider_id + RequestLog.requested_model` 聚合；但映射请求中：

- `requested_model` 通常是用户原始源模型。
- `model_name` 通常是映射后的目标模型。
- `RouterService` 查指标时用的是当前最终 `provider_model.model_name`。

影响：

- 映射目标模型的 DB 近期成功率/延迟可能查不到。
- Redis `ProviderHealthStateService` 仍能按 provider_model_id 记录热健康，但 DB fallback 和冷启动指标会偏弱。

### 3.6 `max_error_rate` 缺少最小样本保护

provider 最近失败率超过 `max_error_rate` 会被硬过滤，但失败率来自 5 分钟窗口，当前没有最小请求数门槛。

影响：

- 单次或少量失败可能把 provider 短时间排除。
- 对低流量 provider 容易误伤。

### 3.7 同 provider 重试配置被硬上限 2 截断

代码中 `_same_provider_retry_budget` 固定 `min(..., 2)`。即使全局和 provider 都配置更高，也最多尝试 2 次。

影响：

- 配置含义不够直观。
- 对偶发网络抖动较多但同 provider 通常稳定的场景，无法通过配置提高同 provider 重试。

### 3.8 重试 backoff 没有 jitter，也不读取 Retry-After

候选耗尽等待使用固定 1、2、5、10、15、30 秒序列。429 不同 provider 立即切换，但候选耗尽后仍是固定 sleep。

影响：

- 高并发下容易出现同一批请求同时醒来、同时打满 provider。
- 上游明确返回 Retry-After 时无法尊重真实恢复窗口。

### 3.9 候选截断可能降低故障切换覆盖率

`max_candidate_count` 在排序后直接截断。若前 N 个候选都失败，代码会进入等待重选，而不会在同一轮自动扩大到第 N+1 个候选。

影响：

- 候选很多时，默认 10 个之外的可用 provider 可能暂时不会被尝试。
- “快恢复”会受限于候选数配置。

### 3.10 成本评分单位不够严谨

`cost_score = max(0, 30 - average(input_price_per_1k, output_price_per_1k)) * cost_bias`。

问题：

- input/output 简单平均不等于一次请求真实成本。
- 未结合预估输入/输出 token 比例。
- 未纳入 `price_multiplier`、cache read/write 价格。
- 价格字段是 per_1k，界面规范要求展示 per 1M，但算法内部没有清晰说明单位换算。

影响：

- cost_bias 开启时，选择结果可能不符合真实费用最优。

处理结果：已删除成本评分、`cost_bias` 运行时读取、自动加列和迁移残留回填逻辑；成本/价格仅保留展示、计费和日志语义，不再参与主路由评分。（已完成）

### 3.11 response management 类接口没有复用主路由评分

`retrieve_response`、`cancel_response`、`list files` 等管理类转发走 `_ordered_response_management_providers`，只按：

- provider.enabled。
- provider_type == openai_compatible。
- allowed_provider_ids。
- preferred_provider_ids 优先。

它没有使用主路由中的健康、内容完整性、维护模式、协议能力、失败率、score 排序。虽然调用前会申请容量租约，但选择质量明显弱于主请求链路。

影响：

- 管理类接口可能先打到熔断、维护中或内容完整性异常 provider。
- 与主链路行为不一致。

处理结果：`response/file/chat completion` 等管理类转发入口已通过 `RouterService.order_candidates` 获取主路由排序候选，再按 provider 去重后转发，并在 trace 中记录主路由候选摘要。（已完成）

### 3.12 候选缓存缓存的是 ORM 对象，热健康变化可能有短暂滞后

候选和 provider runtime 列表缓存中保存 provider / provider_model 对象。成功/失败会更新数据库和 Redis 热健康，但不一定立即失效候选缓存。

影响：

- 10 秒内可能沿用旧候选和旧 score。
- 内容完整性状态、模型启停、健康变化依赖写操作显式失效或 TTL 到期。

处理结果：候选缓存已改为仅缓存 `provider_id` 与 `provider_model_id` 轻量身份；命中缓存后重新读取当前 provider/model、热健康、容量和能力探针状态，再构建候选评分。（已完成）

### 3.13 存在未使用或语义过期的算法函数

`RouterService._route_selection_weight`、`_sticky_order`、`_sort_by_health_and_sticky` 等函数仍存在，但主排序路径使用 `_primary_route_order` 和 `_balanced_order`。

影响：

- 新维护者容易误读，以为当前是加权随机或 sticky hash 策略。
- 测试仍断言 `_route_selection_weight`，但主链路不使用它，测试对真实选择策略覆盖有限。

处理结果：已删除 `_effective_model_cost`、`_route_selection_weight`、`_sticky_order`、`_sticky_affinity_score`、`_filter_failure_rate_candidates`、`_sort_by_health_and_sticky` 等旧算法函数及相关调用。（已完成）

### 3.14 工具能力探针过滤只在诊断标签中存在，主候选筛选未检查 tools 探针失败

主候选筛选会根据 `supports_tools` 过滤工具能力，但实际 `_load_available_candidates_uncached` 只对 vision 和 image_generation 调用 `_capability_probe_failed`。诊断标签中存在 `tools_probe_unhealthy`，但主路径没有对应排除逻辑。

影响：

- 某模型声明支持 tools，但最近原生工具探针失败时，仍可能进入正式工具请求候选。
- 会把能力失败推迟到正式请求，降低精确性。

处理结果：主候选筛选已在 `require_tools` 时读取 tools 探针状态，探针失败会作为硬过滤排除；诊断仍保留 `tools_probe_unhealthy` 计数用于解释。（已完成）

### 3.15 运行时选择缺少“本轮已失败候选排除集”

一次 route round 中，候选依次失败后会继续下一候选；但进入候选耗尽等待后，下一轮重新排序时没有显式把短时间刚失败的 provider_model 放入本请求的冷却排除集，只依赖健康状态、失败率、熔断、容量变化。

影响：

- 如果失败未立刻触发熔断或失败率硬过滤，下一轮可能再次选择同一批刚失败候选。
- 会拉长等待窗口内的有效探索时间。

处理结果：JSON 与流式主链路已维护本请求内失败候选集合；下一次重选会优先排除刚失败的 `provider_id/provider_model_id`，若排除后无其它候选则放松排除以允许第二轮重试；trace 会记录失败候选集合。（已完成）

## 4. 改进目标

为了让选择策略“稳、快、精确”，建议把目标拆成三类：

- 稳：不可恢复错误不重试；可恢复错误快速切换；低质量 provider 不被粘性或低负载误选；高风险内容 provider 绝不进入正式路由。
- 快：缓存命中高；候选排序计算轻；失败后不重复试明显失败对象；重试等待带 jitter 和 Retry-After；候选扩展不阻塞。
- 精确：评分输入维度口径一致；模型映射、provider 能力、端点协议、历史指标按 provider_model 精确匹配；策略字段和真实行为一致。

## 5. 建议改进方案

### 5.1 收敛为唯一策略名称

建议将当前真实策略明确命名为：

```text
健康优先智能路由
```

主链路只保留这一个策略说明。旧字段处理：

- 后端继续兼容读取 `route_mode/default_provider_id/manual_allow_fallback`，但不作为正式策略字段暴露。
- 设置接口和前端隐藏旧模式选择。
- dashboard 只展示“健康优先智能路由”与核心参数：最大候选数、候选等待窗口、无限重试开关、可信提供商开关。
- 后续迁移中逐步删除旧 schema 中的 `RouteMode`。

### 5.2 调整排序为“硬过滤 + 综合分 + 稳定性门槛”

建议把同 tier 内排序从 `(load_factor, -route_score)` 改为更可解释的综合得分：

```text
final_score =
  health_component
  + reliability_component
  + latency_component
  + ttfb_component
  + preference_component
  + region_component
  + cache_affinity_component
  - load_penalty
  - recent_failure_penalty
```

排序建议：

1. `hard_health_rank`：healthy > degraded > unknown/half_open > unhealthy。
2. `final_score`：高分优先。
3. `load_factor`：只做同分或近似同分时的平衡因素。
4. `priority`、id 兜底。

这样可避免低负载但明显低质量的 provider 抢占第一。

### 5.3 给会话粘性加健康门槛

保留 prompt cache/stateful Responses 需要的粘性，但建议增加条件：

- 最近成功时间在可配置 TTL 内。
- 当前候选健康不能低于 degraded。
- `failure_rate_5m` 低于阈值。
- `route_score` 不低于本轮最高分的一定比例，例如 80%。
- 如果是 stateful Responses 且存在 encrypted state，可提高粘性权重，但仍不能覆盖 blocked/open/capacity 等硬约束。

建议策略：

```text
强粘性：stateful Responses 且原 provider_model 健康、容量可用。
软粘性：普通会话/prompt_cache_key，仅加分，不直接置顶。
失效粘性：近期失败、unhealthy、高延迟、低分时取消置顶。
```

### 5.4 让 preferred_provider_ids 成为明确加分

不要先拼接 preferred 列表再排序。应把偏好纳入 score：

- preferred provider 命中：加 `preferred_provider_bonus`。
- preferred provider 顺序：第 1 个加分最高，后续递减。
- 若管理员需要强制 provider，只允许内部 playground 使用 `forced_provider_id`。

这样 API Key 偏好既可解释，又不会绕过健康和容量。

### 5.5 修正 route metrics 口径

建议把 `LogService.route_metric_summary` 改为优先按：

- `resolved_provider_model_id`
- `provider_id`
- `model_name`

聚合；`requested_model` 仅作为展示与源模型分析字段。

对映射模型：

- 评分读取目标模型 `model_name` 的历史表现。
- 源模型 `requested_model` 用于统计“用户请求了哪个模型”。
- 映射目标 `model_name` 用于统计“实际用了哪个模型”。

这样映射场景下的评分会更精确。

### 5.6 删除失败率硬过滤，保留评分惩罚

已按当前确认方案删除 `max_error_rate` 硬过滤及相关逻辑。后续只在综合评分中保留成功率和近期错误惩罚，避免样本量不足时把 provider 直接排除。

若后续需要进一步增强评分稳定性，可在成功率得分中使用 Wilson lower bound 或 Bayesian 平滑：

```text
smoothed_success_rate = (success + prior_success) / (total + prior_total)
```

这样新 provider、低流量 provider 不会因 1 次失败被误杀，也不会因 0 数据默认 100% 过度乐观。

### 5.7 重试策略加入 jitter、Retry-After 和本轮冷却

建议：

- 候选耗尽等待加入 20%-40% jitter。
- 429/503 若上游返回 Retry-After，作为等待下限或候选冷却时间。
- 本请求内维护 `failed_provider_model_ids`，下一轮重选时优先排除刚失败对象，除非没有其它候选。
- 同 provider 重试配置语义改清楚：`max_attempts_per_provider` 或 `same_provider_max_attempts`。
- 若保留 `max_retries`，明确它是“额外重试次数”还是“总尝试次数”。

### 5.8 候选耗尽前先扩展候选池

当 `max_candidate_count` 截断后的候选全部失败时，建议先扩展下一批候选，而不是直接进入等待：

```text
第一轮：top 10
失败后：top 11-30 中排除已失败候选
仍失败：再进入等待窗口
```

这样默认候选数不需要过大，也能在故障时更快找到可用 provider。

### 5.9 删除成本评分

已按当前确认方案删除成本评分和 `cost_bias`。价格、倍率、cache read/write 单价继续用于展示、计费、账单和日志分析，但禁止参与 provider/model 选择评分。

### 5.10 管理类接口复用主路由过滤

response/file/moderation 等管理类接口至少应复用以下过滤：

- enabled。
- maintenance_mode。
- circuit_state。
- content integrity。
- allowed providers。
- protocol_type。
- capacity。
- health order。

如果请求携带 response_id 且历史日志中能找到原 provider，应优先原 provider；找不到或失败时再健康优先 fallback。

### 5.11 候选缓存改为轻量快照

建议缓存不要直接保存 ORM 对象，而保存轻量结构：

```text
provider_id
provider_model_id
model_name
capabilities
static config version
```

排序时再合并 Redis 热健康和容量快照。

另外建议：

- provider/model 写操作继续主动失效。
- 内容完整性状态变化主动失效。
- 熔断状态变化主动失效。
- 成功/失败不必每次失效，但 Redis 热健康必须每次参与最终排序。

### 5.12 补齐 tools 探针失败过滤

当请求使用 tools 时，主候选筛选应增加：

```python
if require_tools and RouterService._capability_probe_failed(provider, provider_model, "tools", endpoint_path=required_endpoint_path):
    continue
```

这样工具能力失败能在路由阶段被精确排除，而不是进入正式请求后才失败。

### 5.13 增强可观测性

建议每次请求 trace 中记录：

- top candidates 的 provider_id、provider_model_id、health_tier、load_factor、route_score。
- 每个 score 分项。
- 被硬过滤原因计数。
- 最终选中原因：recent_session、highest_score、preferred_bonus、low_load_tiebreak 等。
- 候选扩展轮次。
- Retry-After/jitter/冷却信息。

这样后续“为什么选了这个 provider”可以直接从日志解释。

处理结果：`request_route_decision` trace 已记录 top candidates、score 分项、硬过滤原因计数快照、最终选中原因、候选窗口/扩展数量、失败候选集合；候选耗尽等待 trace 已记录 Retry-After、jitter、base backoff、剩余等待窗口和截断信息。（已完成）

## 6. 推荐落地顺序

第一阶段：不改变外部行为，提升可解释性与准确性。

1. 写明唯一策略名，隐藏旧 route mode 文案。
2. trace 增加 score 分项和候选排序原因。
3. 修正 route metrics 聚合口径，优先使用 resolved_provider_model_id/model_name。
4. 补齐 tools 探针失败过滤。

第二阶段：小幅改变选择算法，提高稳定性。

1. preferred_provider_ids 改为分数加成。
2. 会话粘性增加健康门槛。
3. 同 tier 排序从 load 优先改为综合分优先。
4. 失败率硬过滤增加最小样本。

第三阶段：提高故障恢复速度。

1. 候选耗尽前扩展候选池。
2. 本轮失败 provider_model 冷却。
3. backoff 加 jitter。
4. 读取 Retry-After。

第四阶段：统一边缘链路。

1. response/file 管理类接口复用主路由过滤。
2. 候选缓存改轻量快照。
3. 清理未使用旧函数和旧策略字段。

## 7. 建议的最终策略形态

最终建议形成如下确定性策略：

```text
1. 请求合法性、API Key、余额、端点、模型白名单硬校验。
2. 源模型映射为目标模型候选列表。
3. 对每个目标模型按顺序执行 provider 硬过滤。
4. 硬过滤：授权、启用、协议、能力、内容完整性、熔断、维护、容量。
5. 计算 provider_model 级综合分。
6. 会话粘性只作为健康门槛内的加分或 stateful 场景置顶。
7. 选择 top N；失败时排除本轮失败候选并扩展候选池。
8. 可恢复错误按 provider/model/route 三层重试；不可恢复错误立即返回。
9. 到达全局等待窗口后返回结构化 503。
10. 全链路日志记录选择原因、分数、过滤原因和重试原因。
```

这套策略会比当前实现更稳，因为健康、内容完整性、容量和不可恢复错误都保持硬约束；更快，因为故障时先扩展候选并冷却失败对象，而不是等待后重复打同一批；更精确，因为评分按 provider_model、真实模型、真实成本和实时健康口径统一计算。
