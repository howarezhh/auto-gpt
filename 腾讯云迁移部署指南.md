# 腾讯云迁移部署指南

适用服务器：

- IPv4：`118.24.135.235`
- 登录用户：`ubuntu`
- 规格：2 核 / 2GB RAM / 40GB SSD / 3Mbps
- 系统：Ubuntu

## 1. 上传或拉取项目

推荐先通过 Git 拉取项目到服务器，例如：

```bash
cd /opt
sudo git clone https://github.com/howarezhh/auto-gpt.git aotu-gpt
cd /opt/aotu-gpt
```

若 GitHub 直连不稳定，可先在腾讯云控制台文件管理上传压缩包，再解压到 `/opt/aotu-gpt`。

在 Windows 本地打包时，必须使用 PowerShell 7 运行项目内置脚本：

```powershell
cd D:\AI知识学习方面\AI大模型智能体全套教程\aotu-gpt
pwsh -NoLogo -NoProfile -File .\scripts\build_tencent_package.ps1
```

脚本会在 `dist/` 目录生成 `aotu-gpt-tencent-时间戳.tar.gz` 和对应 `.sha256` 校验文件，并默认排除 `.env`、`.venv`、`data/`、`logs/`、`node_modules/`、本地缓存和运行日志。

上传到服务器后可按以下方式解压：

```bash
sudo mkdir -p /opt
cd /opt
sudo tar -xzf /tmp/aotu-gpt-tencent-YYYYMMDD-HHMMSS.tar.gz
sudo chown -R ubuntu:ubuntu /opt/aotu-gpt
cd /opt/aotu-gpt
```

## 2. 首次部署

```bash
cd /opt/aotu-gpt
sudo bash start_tencent.sh
```

默认行为：

- 复用项目根目录 `.venv`、`.env`、`requirements.txt` 和 `data/`
- 应用只绑定 `127.0.0.1:8000`
- 通过 `nginx` 在 80 端口对外访问
- 写入 `aotu-gpt.service`
- 写入独立后台任务服务 `aotu-gpt-worker.service`
- 默认不执行数据库初始化或迁移

首次创建云端 PostgreSQL 表结构时，必须显式执行：

```bash
cd /opt/aotu-gpt
sudo env RUN_DB_INIT=1 bash start_tencent.sh
```

该命令会在执行数据库初始化前先备份 PostgreSQL。

## 3. 数据库要求

项目当前仅支持 PostgreSQL。云端首次部署或迁移前必须先准备 PostgreSQL 数据库，并确认 `.env` 中的 `DATABASE_URL` 使用 PostgreSQL 连接地址。

## 4. 例行更新

例行更新只更新代码、依赖、静态资源和服务配置，不会初始化、迁移、清空或重建云端数据库：

```bash
cd /opt/aotu-gpt
sudo bash update_tencent.sh
```

GitHub 直连不稳定时可传入镜像：

```bash
cd /opt/aotu-gpt
sudo env GIT_MIRROR_URLS='https://gitclone.com/github.com/howarezhh/auto-gpt.git' bash update_tencent.sh
```

## 5. 腾讯云控制台检查项

- 防火墙放通 TCP 80。
- SSH 登录保持 TCP 22 可用。
- 若后续使用 HTTPS，额外放通 TCP 443。
- 公网访问地址为 `http://118.24.135.235`。

## 6. 常用排查命令

```bash
systemctl status aotu-gpt
systemctl status aotu-gpt-worker
systemctl status nginx
journalctl -u aotu-gpt -f
journalctl -u aotu-gpt-worker -f
curl -i http://127.0.0.1:8000/ready
curl -i http://118.24.135.235/login
```
