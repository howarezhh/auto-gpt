# 内容防护模块问题全审查

本文档用于归档当前线程中已经审查出的内容防护模块问题。

说明：当前可用上下文中，问题 1-640 只保留了压缩摘要，无法恢复逐条原始表述；为避免凭空补写不存在的细节，本文件对 1-640 按审查批次和问题域做完整归档，对当前上下文保留了逐条原文的问题 641-700 做逐条明细保存。

## 一、问题 1-560：前期审查归档

### 1. Schema 与配置入口问题

- 内容防护配置字段在 `app/schemas/content_guard.py`、`app/schemas/setting.py`、`app/models/app_setting.py`、`app/services/setting_service.py` 等多处维护，存在口径漂移风险。
- 内容防护规则、运行时配置、预检配置、外部探测配置混合在同一配置模型中，边界不清。
- 部分配置默认值与运行时兜底值、前端默认值、数据库默认值存在不一致风险。
- 部分配置字段缺少跨字段校验，例如流式模式、最大检测延迟、缓冲字节数、异步复核、高风险策略之间的组合约束不足。
- `content_guard_enabled` 总开关在不同链路中语义不统一，部分链路把它理解为运行时检测开关，部分链路把它理解为治理总开关。
- `content_guard_precheck_auto_enabled` 与可用性检测调度、内容可信探针调度之间存在职责边界不清问题。
- `content_guard_rules_json` 使用 JSON 文本存储规则，缺少版本字段、迁移策略、结构兼容策略和规则变更审计详情。
- URL 白名单字段既支持文本行又支持 JSON 数组，输入格式宽松但错误反馈和前端提示不足。
- 内容防护规则 action、risk、confidence、score_delta 的实际运行语义与前端展示语义未完全闭合。

### 2. 规则引擎与检测算法问题

- `ContentGuardRuleService` 只是空继承，算法仍在 `ContentGuardService`，与三层职责规范不一致。
- 文本检测规则注册、解析、匹配、风险评分没有真正收敛到独立规则服务。
- 规则 JSON 损坏时会静默回退，可能掩盖配置损坏。
- 重复规则 ID 存在静默跳过或前后端处理不一致风险。
- 非法枚举值存在静默降级风险，可能导致错误配置被悄悄吞掉。
- regex 规则缺少复杂度保护、超时保护和 ReDoS 防护。
- `allow` 动作不是真正放行，和界面“放行”文案存在语义差异。
- URL 白名单会合并默认域名，也会从请求体自动提取允许域名，可能被调用方输入影响检测结论。
- 文本抽取对多模态 content、Responses 复杂字段、嵌套 output、工具结果、数组文本等覆盖不足。
- 命中原因、命中片段、分类、规则 ID、置信度等证据在不同返回结构中不完整。
- 高风险与复核结果的阈值和最终动作在规则层与运行时层都有处理，职责重叠。
- 部分正常业务语境与违规推广、广告、导流之间依赖规则词表判断，容易出现误判或漏判。
- 空规则集与默认规则集语义复杂，前后端和服务端均需明确区分“用户清空规则”和“使用默认规则”。

### 3. 运行时代理链路问题

- 代理链路对内容防护策略的接入点分散，部分仍通过 `ProxyService` 包装转发。
- `ContentRuntimeGuardService` 与 `ProxyService` 之间仍存在双向依赖和职责交叉。
- 非流式检测、流式首段检测、流式分块检测的证据记录字段不统一。
- `record_violation` 会把 review 也降级 provider，并未完整写入统一告警事件流。
- `content_guard_required=false` 仍可能启用检测，和 API Key 前端“可选”文案不一致。
- 异步复核开启时 review 不落违规记录，且未见完整复核队列实现。
- 流式缓冲只覆盖低信任或强制可信场景，不是所有流式防护都执行同等首段缓冲。
- 外部检测存在 SSRF、明文 HTTP、内网地址访问、响应长度限制、请求超时等安全边界不足。
- 流式污染检测后段覆盖不足，首段通过后仍可能在后续 chunk 中污染。
- 流式已开始下游输出后，高风险命中只能中止，不能切换 provider；相关日志和错误事件需要更完整。
- 流式分块检测与首段缓冲检测的最终策略、等待耗时、重试 provider 数等日志字段不完整。
- 部分协议适配场景下流式内容防护会被禁用或跳过。

### 4. 可信探针、预检与可用性检测问题

- `ContentGuardModuleService` 与 `ContentTrustProbeService` 聚合逻辑重复。
- `run_trust_probe` 强制覆盖用户选择的 `probe_keys`，前端选择可能不生效。
- 必需探针包括固定答案、外链广告识别和流式污染检测，但可选探针与必需探针的结果聚合容易混淆。
- 探针失败次数无时间窗口，连续或历史累计失败可能直接导致 blocked。
- provider/model 内容状态恢复不完整，blocked 后缺少自动恢复闭环。
- SSE 探针只读固定数量 chunk，固定答案场景覆盖不足。
- 外链广告识别探针请求真实模型但场景有限，无法覆盖复杂广告、短链、隐藏链接、工具参数污染等情况。
- 可用性检测中仍混入内容可信预检职责，和内容防护独立预检配置之间边界不清。
- 自动预检调度与可用性检测调度、锁 TTL、检测间隔之间存在不一致风险。
- blocked 模型可能被反复探测，造成资源浪费和失败噪音。
- 预检结果沉淀到 provider 和 provider_model 的状态逻辑不完全一致。

### 5. 路由候选与 API Key 联动问题

- 路由层 content policy 与评分职责混杂。
- API Key “内容检测可选”语义不清，不能简单理解为关闭检测。
- `只走可信提供商` 作为全局策略与 API Key 局部内容检测开关之间语义容易冲突。
- 用户端允许编辑内容检测开关，可能与全局治理策略冲突。
- 模型映射、候选筛选、内容完整性状态筛选之间的职责边界需要继续收敛。
- 内容完整性 blocked、低分、低信任状态会影响候选，但总开关关闭时是否完全停用存在风险。
- API Key 缓存中保存了内容防护策略，但关键写操作后缓存失效覆盖范围需要确认。
- 模板、批量授权、用户端创建/编辑 API Key 是否同步内容防护策略存在缺口。

### 6. 迁移、DDL 与生产部署问题

- 内容防护字段分散在多份迁移和 `app/main.py` 内置 DDL 中。
- 生产环境禁止 Web worker 自动 DDL，若独立迁移漏跑会导致内容防护字段或类型化日志表缺失。
- 类型化日志相关表存在模型定义，但迁移覆盖不完整风险。
- request_logs 内容防护字段索引不足，按风险、状态、时间、provider 查询可能较慢。
- 内容防护治理字段缺少统一迁移清单，容易漏字段。
- 旧数据回填只覆盖部分 provider/provider_model 状态，历史异常值清理不足。

### 7. 前端内容防护页面问题

- 内容防护页面仍是大函数初始化，配置、探测、规则、本地检测和结果渲染耦合严重。
- 页面缺少分页、搜索、分类筛选、状态筛选。
- 规则编辑体验不足，缺少原因字段、正则校验、危险操作确认和变更预览。
- 外部 API Key 输入后长期停留在 DOM，存在泄露风险。
- 探针按钮反馈、结果表、状态标签、失败原因展示不够完整。
- 状态标签中文映射不统一，部分底层枚举仍可能直接暴露。
- 移动端规则表过宽，依赖横向滚动，不符合渐进披露规范。
- 保存、重置、删除等危险操作反馈和确认不足。
- 本地内容检测结果缺少命中片段、命中规则详情和动作说明。
- URL 白名单和自动预检等高风险配置说明存在误导。

### 8. 日志中心与类型化日志问题

- 类型化日志表缺迁移风险。
- 日志中心 Tab 初始化存在“全有或全无”的问题。
- 计费日志分页聚合不稳定。
- 后台任务日志只查数据库，不覆盖队列或内存状态。
- typed logging 队列问题可能导致日志中心显示 0。
- 内容防护结果字段映射不一致，`content_guard_result`、`guard_result`、`status` 等字段混用。
- 不同类型日志仍存在统一筛选字段过多、专属筛选不足的问题。
- 内容防护日志只支持 result/risk 等少量筛选，缺少 stage/action/final_strategy/retry_count 等维度。
- 详情页未完整展示命中规则、匹配片段、分类、处置动作和 provider 状态变化。

### 9. 测试覆盖问题

- `stage33_content_guard_regression_check.py` 以静态扫描和少量单元断言为主。
- 缺少真实前端浏览器测试，无法发现按钮失效、状态丢失和布局问题。
- 缺少迁移从空库执行验证。
- 缺少 SSRF、明文 HTTP、内网地址、超长响应、超长 API Key 等安全边界测试。
- 缺少策略语义测试，例如总开关关闭、API Key 可选检测、provider 关闭检测之间的组合。
- 缺少调度间隔、锁 TTL、blocked 模型反复探测等后台任务测试。
- 缺少流式污染后段检测和协议适配流式检测测试。

## 二、问题 561-600：类型化日志与日志中心归档

- 类型化日志表存在迁移缺口，生产禁用自动 DDL 时会导致接口不可用。
- 日志中心类型化 Tab 初始化方式不稳，容易出现按钮无法点击或面板不切换。
- 计费日志分页聚合口径不稳定，列表与详情可能不一致。
- 后台任务日志只查数据库，无法反映当前队列、锁、运行中任务和失败任务的实时状态。
- typed logging 队列异常时缺少同步兜底或清晰错误反馈。
- 内容防护结果字段在请求日志、类型化事件、前端详情之间映射不一致。
- 不同日志类型仍共用过多通用筛选项，缺少各自结构化字段。
- 异常事件、可用性检测日志、计费日志、后台任务日志、素材日志的按钮点击与数据加载链路曾被确认存在无效问题。
- 日志中心显示为 0 与接口不可用可能由缺表、接口错误、前端 Tab 绑定或筛选参数不匹配共同导致。
- 类型化日志详情未充分展示各自业务专属结构，例如可用性探针明细、计费 token/余额快照、后台任务锁状态、素材处理结果等。

## 三、问题 601-640：内容防护核心服务归档

- `ContentGuardRuleService` 只是空继承，算法仍在 `ContentGuardService`。
- `ContentGuardModuleService` 与 `ContentTrustProbeService` 聚合逻辑重复。
- 规则 JSON 损坏会静默回退。
- 重复 ID 会静默跳过。
- 非法枚举会静默降级。
- regex 无复杂度和超时保护。
- `allow` 动作不真正放行。
- URL 白名单默认合并默认域名。
- URL 白名单会从请求体自动提取允许域名。
- 响应文本抽取遗漏多模态 content。
- 响应文本抽取遗漏 Responses 复杂字段。
- `record_violation` 会把 review 也降级 provider。
- `record_violation` 未写统一告警。
- `content_guard_required=false` 实际仍可能启用检测。
- `should_record_violation` 在异步复核开启时 review 不落违规。
- 未见完整异步复核队列实现。
- 流式缓冲只覆盖低信任/强制可信场景。
- 外部检测存在 SSRF 风险。
- 外部检测允许明文 HTTP。
- 外部检测长度限制不足。
- `run_trust_probe` 强制覆盖用户选择的 `probe_keys`。
- 探针失败次数无时间窗口，直接累计到 block。
- provider/model 内容状态恢复不完整。
- SSE 探针只读 6 个 chunk。
- 固定答案场景覆盖不足。
- 路由层 content policy 与评分职责混杂。
- API Key “内容检测可选”语义不清。

## 四、问题 641-700：逐条审查明细

641. 内容防护前端把配置、探测、规则编辑、本地检测、结果渲染全部塞进 `initContentGuardPage` 一个超大函数，职责边界不清，后续任何小改都容易互相影响。证据：`app/static/js/app.js:8741`。

642. 内容防护首页初始加载失败时只弹 Toast，指标仍停留在 `-`、规则区仍可能显示“加载中”，没有页面级错误态和重试入口，用户无法判断是无数据还是接口坏了。证据：`app/static/js/app.js:8975`。

643. 提供商下拉在没有可检测提供商时直接渲染为空字符串，没有“暂无提供商/去添加提供商”的空状态，提交时才报错。证据：`app/static/js/app.js:8912`。

644. 每次刷新概览都会重建提供商下拉，没有保留用户当前选择的提供商/模型，可能在用户准备检测时被刷新重置。证据：`app/static/js/app.js:8968`。

645. 探针选项每次渲染都按固定默认集合勾选，会丢失用户手动勾选/取消勾选状态。证据：`app/static/js/app.js:8930`。

646. 内容防护规则表没有服务端分页、关键词搜索、分类筛选或启用状态筛选，规则上限已到 200 时只能一次性渲染全表，不符合数据列表分页规范。证据：`app/schemas/content_guard.py:72`、`app/templates/content_guard.html:307`。

647. 规则编辑只暴露名称、分类、匹配、风险、动作、扣分、可信度，不暴露 `reason` 字段，保存时还固定写成空字符串，导致后端规则原因被前端清空。证据：`app/static/js/app.js:8879`。

648. 规则删除按钮没有二次确认，也没有撤销，对高风险检测规则这种安全配置来说误删成本过高。证据：`app/static/js/app.js:9106`。

649. 恢复默认规则使用 `window.confirm`，与项目统一弹层/反馈体系割裂，且无法展示将覆盖哪些规则。证据：`app/static/js/app.js:9130`。

650. 新增规则默认填入“待填写”作为真实匹配项，如果用户直接保存，会把占位词作为有效规则写入，造成误判。证据：`app/static/js/app.js:9091`。

651. 规则 ID 自动用时间戳生成但界面不允许编辑，用户无法建立稳定、可读、可迁移的规则标识。证据：`app/static/js/app.js:8798`。

652. 规则校验只检查 ID/名称/分类/匹配项，未在前端提示正则合法性、重复 pattern、动作与风险等级是否矛盾，错误要到后端或运行时才暴露。证据：`app/static/js/app.js:8885`。

653. `score_delta` 使用 `Number(value || -8)`，用户输入 `0` 会被当成 falsy 变成 `-8`，导致“零扣分”无法保存。证据：`app/static/js/app.js:8878`。

654. `confidence` 使用 `Number(value || 0.7)`，用户输入 `0` 会被强制变成 `0.7`，前端数值语义错误。证据：`app/static/js/app.js:8879`。

655. 设置里的数值项也大量使用 `Number(value || 默认值)`，空值和 `0` 的语义混在一起，部分字段无法表达管理员真实输入。证据：`app/static/js/app.js:9000`。

656. 自动预检间隔前端强制最小 300 秒，但没有解释保存后调度器是否会立即重排，用户可能以为保存即生效。证据：`app/static/js/app.js:9002`、`app/tasks.py:530`。

657. URL 白名单说明写“用户请求中出现过的域名会自动加入本次白名单”，这实际是在提示可被请求体影响检测结果，安全语义非常危险。证据：`app/templates/content_guard.html:219`。

658. 外部渠道 API Key 输入框会把密钥长期保留在 DOM 中，检测完成后不清空，旁人或浏览器插件可读取。证据：`app/templates/content_guard.html:267`、`app/static/js/app.js:9016`。

659. 外部渠道检测没有“显示/隐藏密钥”控件，也没有检测后清除按钮，密钥可用性和安全性都不足。证据：`app/templates/content_guard.html:260`。

660. 外部渠道检测只允许选择 `/responses` 与 `/chat/completions`，但页面没有解释会拼接到 base_url 还是替换路径，容易造成重复 `/v1/v1` 或错误端点。证据：`app/templates/content_guard.html:275`。

661. 外部渠道后端临时构造 `Provider(id=0)`、`ProviderModel(id=0)`，多个外部检测结果在日志/trace 中无法区分具体目标。证据：`app/services/content_trust_probe_service.py:137`。

662. 外部渠道返回给前端的 target 包含 base_url，若后续结果弹层或日志展示，可能泄露敏感内网地址或第三方接口地址。证据：`app/services/content_trust_probe_service.py:162`。

663. 内容防护结果表把长原因直接放在表格正文，没有统一问号 Tooltip 收纳，长错误会挤爆表格。证据：`app/templates/content_guard.html:369`、`app/static/css/app.css:3291`。

664. 规则表强制 `min-width: 1180px`，移动端只能横向滚动，缺少抽屉式或详情式渐进披露。证据：`app/static/css/app.css:3221`。

665. 内容防护的探针复选框仍是浏览器默认 checkbox 视觉，和全站启停类开关规范不一致。证据：`app/templates/content_guard.html:282`、`app/static/css/app.css:3180`。

666. 内容防护分段切换把 radio 设为 `opacity:0` 且 `pointer-events:none`，若焦点样式不足，键盘用户很难感知当前焦点。证据：`app/static/css/app.css:3128`。

667. 隐藏的外部/内部检测区在 CSS 中一处 `display:none`，另一处又覆盖成 `display:grid; visibility:hidden`，同一 hidden 语义不统一，后续维护容易出错。证据：`app/static/css/app.css:3074`、`app/static/css/app.css:3163`。

668. 本地内容检测只展示结果、风险、置信度和命中规则名，不展示命中片段、分类、处置动作，排查价值不足。证据：`app/static/js/app.js:9156`。

669. 本地内容检测命中 `review` 时按钮反馈显示“命中”并按错误态处理，无法区分“阻断”和“待复核”。证据：`app/static/js/app.js:9162`。

670. 探测完成 Toast 只展示总结果，不展示目标、通过数、失败探针，用户还要自己去看表格。证据：`app/static/js/app.js:9199`。

671. 探测成功后立即 `loadOverview()`，会重置页面上的选择状态和探针勾选状态。证据：`app/static/js/app.js:9200`。

672. API Key 管理端把“内容检测 要求/可选”混在路由策略列里，缺少独立筛选，管理员无法快速找出可选检测的密钥。证据：`app/static/js/app.js:12337`。

673. API Key 创建/编辑模板中没有看到模板级 `content_guard_required` 控件初始化，批量套模板可能无法同步内容检测策略。证据：`app/static/js/app.js:12170`。

674. 用户端允许普通用户自己关闭“要求内容检测”，这和内容完整性作为全局治理能力的定位冲突，可能让用户绕过强制检测语义。证据：`app/templates/user_api_keys.html:293`、`app/routers/user_portal.py:793`。

675. `content_guard_required=false` 并不等于关闭检测，运行时会回退到提供商的 `content_guard_enabled`，前端“可选”文案会误导用户。证据：`app/services/content_runtime_guard_service.py:226`。

676. 路由缓存 key 区分 `guard` 和 `guard-optional`，但候选内容策略仍会排除 blocked/低分提供商，容易造成“可选检测”缓存语义与实际候选筛选不一致。证据：`app/services/router_service.py:1036`、`app/services/content_trust_probe_service.py:111`。

677. `ProxyService._with_content_guard_route_policy` 是空实现，说明内容防护路由策略没有根据请求类型、端点、工具、多模态动态调整。证据：`app/services/proxy_service.py:240`。

678. 路由评分仍给内容完整性分数加 bonus/penalty，但 blocked 和低分又是硬排除，评分与硬约束职责混在一起。证据：`app/services/router_service.py:1093`。

679. 自动预检任务锁 TTL 固定 1 小时，但任务触发间隔可配置，长探测或大量模型时可能和下一轮调度重叠/跳过。证据：`app/tasks.py:425`。

680. 自动预检会对 `blocked` 模型每轮都返回 True，可能持续探测已隔离模型，浪费资源并放大失败记录。证据：`app/services/health_service.py:2177`。

681. 内容探针失败次数没有时间窗口，累计 3 次就 blocked，临时网络波动也可能永久隔离模型。证据：`app/services/content_guard_probe_service.py:1092`。

682. 探针通过时如果模型此前是 blocked，不会恢复为 passed，但 provider 级 blocked 也没有自动恢复路径，容易形成只能人工清理的半死状态。证据：`app/services/content_guard_probe_service.py:1085`。

683. `run_trust_probe` 会强制覆盖用户传入的 probe_keys，页面若调用 trust-probe 类接口，用户勾选项会被无视。证据：`app/services/content_trust_probe_service.py:29`。

684. 未知探针被标记为 skipped 但不是异常，前端可能只看到“已跳过”，无法发现前后端 probe key 不匹配。证据：`app/services/content_trust_probe_service.py:211`。

685. SSE 探针最多读取 6 个 chunk，后段污染无法覆盖，之前固定答案正常但后续插广告的流式模型可能漏检。证据：`app/services/content_guard_probe_service.py:538`。

686. 外链广告识别探针串行跑 3 个场景，没有单场景超时和总超时控制，慢渠道会拖垮手动检测体验。证据：`app/services/content_guard_probe_service.py:648`。

687. 探针调用复用正式代理转发，但没有在结果中明确标注“这是检测流量”，容易混入普通请求日志/指标理解。证据：`app/services/content_guard_probe_service.py:171`。

688. 内容防护迁移只给 `request_logs.content_guard_result` 建索引，风险等级、创建时间组合、provider 维度没有索引，日志中心按风险/时间筛选会慢。证据：`migrations/2026-06-06_add_content_guard_governance.sql:53`。

689. 第二份迁移只给 latency 建部分索引，没有给 `content_guard_final_strategy`、`retry_provider_count`、`buffer_wait_ms` 建查询索引，治理分析能力不足。证据：`migrations/2026-06-07_extend_content_guard_strategy_metrics.sql:10`。

690. 内容防护相关 typed event 表在模型中存在，但迁移搜索不到对应 `CREATE TABLE`，生产禁用自动 DDL 时日志接口可能直接查不到表。证据：`app/models/logging_events.py:154`。

691. 回归脚本主要是静态字符串扫描和少量单元断言，没有真实浏览器点击内容防护页面，无法发现按钮失效、布局溢出、状态丢失问题。证据：`stage33_content_guard_regression_check.py:593`。

692. 回归脚本检查模板里存在字段，但不验证字段能成功提交到后端并影响运行时策略。证据：`stage33_content_guard_regression_check.py:599`。

693. 回归脚本没有覆盖外部渠道 SSRF、明文 HTTP、内网地址、超长响应、超长 API Key 等安全边界。证据：`stage33_content_guard_regression_check.py:682`。

694. 回归脚本没有验证迁移脚本实际能从空库建出 typed logging 表，只检查代码字符串，无法发现生产库缺表。证据：`stage33_content_guard_regression_check.py:593`。

695. 日志列表的内容防护筛选只支持 result/risk 两个通用字段，没有 guard_stage、action、final_strategy、retry_count 等内容防护专属筛选。证据：`app/routers/logs.py:54`。

696. 请求日志导出也只带 result/risk 过滤，无法按内容防护处置策略导出污染事件。证据：`app/routers/logs.py:134`。

697. 内容防护事件写入 trace 时 `matched_rules_json` 固定为 `null`，导致日志详情无法还原到底命中了哪些规则。证据：`app/services/proxy_service.py:2011`。

698. 非流式检测使用原始 `response` 做检测，但日志展示使用 `client_response`，如果响应经过模型名恢复/协议适配，检测证据与用户看到的响应可能不一致。证据：`app/services/proxy_service.py:1967`。

699. 流式检测在协议适配场景直接禁用，chat/responses 适配流量可能完全绕过流式内容防护。证据：`app/services/proxy_service.py:2854`。

700. 流式首段未启用缓冲时默认结果写“未启用首段缓冲审核”，但后续仍可能做分块扫描，日志语义容易让人误以为整条流都未检测。证据：`app/services/proxy_service.py:2876`。
