# 6-8 性能审查

## 审查原则

- 审查范围覆盖性能占用、资源分配、稳定性、可靠性、响应速度与生产部署形态。
- 每发现 5 条问题后先完成对应修复，再继续下一批审查。
- 修复时仅改动与问题直接相关的最小范围，避免覆盖其它会话正在处理的业务改动。
- 本文件中的“已完成”表示该条问题已在当前工作区完成代码或配置层修复，并通过后续验证命令确认基础语法可用。

## 第一批问题与修复

### 1. 数据库连接池默认值写死，未真正按 Web worker 动态分配（已完成）

- 位置：`app/config.py`、`.env.example`
- 问题：全局规范要求数据库连接池大小按 `web_concurrency` 动态计算，历史代码注释也写明 `workers × connections_per_worker`，但实际 `db_pool_size` 默认为固定 `40`、`.env.example` 默认为 `8`，容易导致本地、示例与生产部署口径不一致。
- 影响：worker 数调整后连接池不会随之变化；高并发下可能连接不足，低配机器上也可能分配过多连接。
- 修复：新增 `db_connections_per_worker`，当 `DB_POOL_SIZE=0` 时按 `WEB_CONCURRENCY × DB_CONNECTIONS_PER_WORKER` 自动补齐；当 `DB_MAX_OVERFLOW=0` 时按连接池规模自动补齐；示例配置改为动态默认。

### 2. 上游 HTTP 客户端配置变更重建时旧连接池未关闭（已完成）

- 位置：`app/services/upstream_client.py`
- 问题：`UpstreamClientService` 在配置指纹变化时直接创建新的 `httpx.AsyncClient` 或 `aiohttp.ClientSession`，旧客户端对象没有关闭。
- 影响：运行期配置热变更、测试反复覆盖配置或多次重建时可能遗留 socket、连接池和文件描述符，降低长期稳定性。
- 修复：重建客户端前异步关闭旧 `httpx` 客户端或 `aiohttp` Session，应用关闭时仍保留统一 `aclose()` 兜底。

### 3. 请求日志入口队列满载时本事件循环路径可能抛出异常，导致可靠入队退化（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：当调用方已经位于 Redis 事件循环线程内时，代码直接 `queue.put_nowait()`，入口队列满载会抛出 `asyncio.QueueFull`，未在该分支内降级到 Redis 直写或同步落库。
- 影响：高并发突刺下日志可靠接收能力下降，调用方可能误以为日志已入队成功但实际抛错中断。
- 修复：本事件循环路径捕获 `QueueFull` 后直接写入 Redis 主队列；外层异常兜底逻辑继续保留。

### 4. 请求日志队列方法重复 `@classmethod` 装饰，存在维护与静态检查噪音（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：`_ensure_ingress_worker` 前重复声明两次 `@classmethod`。
- 影响：运行期通常不直接报错，但会制造静态检查噪音，降低关键日志队列代码的可维护性；后续自动化重构也更容易误判。
- 修复：删除重复装饰器，仅保留单个 `@classmethod`。

### 5. 监控接口扫描 Redis 调度任务状态没有硬上限（已完成）

- 位置：`app/services/system_metrics_service.py`
- 问题：`_scheduler_job_states()` 对 `scheduler:job:*:state` 执行 `scan_iter` 时没有结果数量上限。
- 影响：当 Redis 中调度状态 Key 异常膨胀或混入大量历史 Key 时，单次监控请求可能被拖慢，影响运维页面响应速度。
- 修复：调度任务状态扫描最多读取 100 个 Key，超过后返回 `_truncated` 摘要，保留局部降级信号。

### 6. 默认部署资源参数不符合 2C2G 保守运行要求（已完成）

- 位置：`start_aliyun.sh`、`.env.example`
- 问题：阿里云通用部署入口默认 `WEB_CONCURRENCY=4` 且 `ENABLE_BACKGROUND_WORKERS=true`，示例配置也默认 4 个 Web worker；这与全局规范中 2C2G 轻量云服务器 Web worker 不超过 2、优先独立后台 worker 的要求不一致。
- 影响：低配服务器上 Web worker 与后台任务争抢 CPU、内存、数据库连接和上游连接，容易造成响应变慢或抖动。
- 修复：默认 Web worker 改为 2，Web 进程默认关闭内置后台 worker，并保留独立 `aotu-gpt-worker` 服务；数据库池默认改为动态计算。

## 第一批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app`
- 结果：通过，`app` 目录 Python 文件语法编译成功。
- 已执行：使用项目虚拟环境实例化 `Settings(_env_file=None, web_concurrency=3, db_pool_size=0, db_connections_per_worker=7, db_max_overflow=0)`。
- 结果：通过，动态连接池计算结果为 `db_pool_size=21`、`db_max_overflow=10`；显式配置 `db_pool_size=12`、`db_max_overflow=4` 时保持覆盖值不变。

## 第二批问题与修复

### 7. Redis 前缀缓存失效一次性收集全部 Key，Key 空间膨胀时内存占用过高（已完成）

- 位置：`app/services/cache_service.py`
- 问题：`CacheService._redis_invalidate_prefix()` 使用 `list(client.scan_iter(...))` 一次性收集所有匹配 Key。
- 影响：当 `route-candidates`、`providers-runtime` 等前缀积累大量 Key 时，一次失效会额外占用大量内存并拖慢写操作后的缓存失效。
- 修复：改为每 500 个 Key 分批删除，避免一次性加载全部匹配项。

### 8. 路由候选缓存锁字典无上限，模型/策略组合多时会持续增长（已完成）

- 位置：`app/services/router_service.py`
- 问题：`RouterService._candidate_cache_locks` 按 cache key 保存 `Lock`，但没有过期或容量上限。
- 影响：大量模型、不同授权范围、能力组合或测试请求会不断生成新 key，进程内存随时间增长。
- 修复：新增 `CANDIDATE_CACHE_LOCK_LIMIT=2048`，超过上限时清空锁表重新开始，避免长期无界增长。

### 9. `route_candidate_cache_ttl_sec=0` 在路由候选缓存中无法真正关闭缓存（已完成）

- 位置：`app/services/router_service.py`
- 问题：设置模型允许 `route_candidate_cache_ttl_sec=0`，但服务层使用 `or 10` 和 `max(1, ...)`，导致管理员设置 0 后仍会缓存 10 秒或至少 1 秒。
- 影响：排障或强一致性场景无法临时关闭候选缓存，可能读到短时间旧状态。
- 修复：服务层明确保留 `0` 语义，`0` 表示不写入候选缓存；正数仍限制在 60 秒以内。

### 10. `route_candidate_cache_ttl_sec=0` 在 provider 运行时缓存中无法真正关闭缓存（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`ProviderService._runtime_provider_cache_ttl_seconds()` 同样把 0 当作默认值处理，无法关闭 `providers-runtime:list` 缓存。
- 影响：provider 启停、维护、协议或容量状态调整后，强一致性排障场景仍可能短时间读到旧 provider 快照。
- 修复：保留 0 语义，正数仍限制在 60 秒以内。

### 11. `model_list_cache_ttl_sec=0` 在模型列表缓存中无法真正关闭缓存（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：`ModelCatalogService._model_list_cache_ttl_seconds()` 把 0 当作默认值，并强制最小 5 秒。
- 影响：模型启停、模型列表或授权范围调试时，管理员无法临时关闭模型列表缓存。
- 修复：保留 0 语义，正数仍限制在 300 秒以内。

### 12. 内存缓存统计接口不主动清理过期条目，读多写少时统计和内存占用不准确（已完成）

- 位置：`app/services/cache_service.py`
- 问题：L1 内存缓存过期清理主要发生在读取命中目标 key 或写入时；如果系统读多写少且统计页面频繁查看，已过期但未被访问的条目会继续留在 `_store`。
- 影响：缓存监控中的 `memory_entries` 偏高，进程内存释放滞后。
- 修复：`stats_snapshot()` 在生成统计前主动清理过期条目。

## 第二批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\cache_service.py app\services\router_service.py app\services\provider_service.py app\services\model_catalog_service.py`
- 结果：通过，第二批涉及的缓存、路由、提供商与模型服务文件语法编译成功。
- 解决策略：采用“缓存资源上限 + 配置 0 值保留关闭语义 + 分批 Redis 操作”的组合策略，优先处理长期运行后资源占用上升和排障时强一致性不可控的问题。

## 第三批问题与修复

### 13. 数据保留清理对大表执行无上限条件删除，容易形成长事务和锁表（已完成）

- 位置：`app/services/data_retention_service.py`
- 问题：历史日志、健康检查、子事件、审计和后台任务保留清理都使用单条 `DELETE ... WHERE created_at < cutoff`。
- 影响：当 `request_logs` 或事件表数据量较大时，单次清理可能持有长事务、放大 WAL、阻塞在线写入，影响对外请求日志落库和后台任务稳定性。
- 修复：引入 `CLEANUP_BATCH_SIZE=5000` 和 `_delete_batched()`，按主键分批查询和删除；所有保留清理入口统一走批量删除策略。

### 14. Token finalize 服务持有同步 Redis 客户端，停止后台 worker 时未关闭连接（已完成）

- 位置：`app/services/token_usage_service.py`
- 问题：`TokenUsageService._redis_client` 使用 `redis.Redis.from_url()` 懒加载，但 `stop_background_workers()` 没有关闭该同步连接。
- 影响：应用重启、测试反复启动或 worker 生命周期切换时可能遗留 Redis TCP 连接，影响长期资源占用。
- 修复：停止后台 worker 时关闭 `_redis_client` 并置空。

### 15. Token finalize 延迟入队任务没有异常结果消费，异常时可能产生未取回 Task 异常（已完成）

- 位置：`app/services/token_usage_service.py`
- 问题：`asyncio.create_task(_delayed_enqueue_finalize_job(...))` 直接创建后台任务，没有 done callback；任务内部也没有通用异常兜底。
- 影响：延迟入队异常时可能出现未消费异常，且 dedupe key 释放不稳定，导致日志补全任务被短期卡住。
- 修复：为延迟任务增加 done callback，并在任务内部捕获异常后释放 dedupe key；取消路径也释放 dedupe key 后继续抛出取消。

### 16. 通用日志脱敏序列化没有总字节上限，大型结果摘要可能写入超大 JSON（已完成）

- 位置：`app/logging/sanitizers.py`
- 问题：`dumps_sanitized()` 只限制字符串长度和列表前 100 项，没有限制最终 JSON 字节数。
- 影响：大型 dict、嵌套列表或任务结果摘要可能写入大字段，增加数据库 IO 和页面读取压力。
- 修复：新增 `max_bytes` 与 `max_list_items` 参数，默认总字节上限 16384，超限后截断并标记原始字节数；列表超限时追加 `_truncated_items` 摘要。

### 17. 后台任务结果摘要未使用专用大小限制，任务返回列表时容易膨胀后台事件表（已完成）

- 位置：`app/logging/adapters/background_job_adapter.py`、`app/tasks.py`
- 问题：后台任务事件直接保存 `result_summary_json`，虽然经过脱敏，但缺少后台任务专用的结果大小限制。
- 影响：健康检查、清理或批处理任务返回大量结果时，后台事件表会持续写入大 JSON，拖慢运维监控和日志查询。
- 修复：后台任务结果摘要统一限制为 8192 B、单字符串 500 字符、列表最多 50 项；直接更新已有任务完成逻辑，避免绕过记录器限制。

## 第三批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\data_retention_service.py app\services\token_usage_service.py app\logging\sanitizers.py app\logging\adapters\background_job_adapter.py app\tasks.py`
- 结果：通过，第三批涉及的数据保留、Token finalize、日志脱敏、后台任务文件语法编译成功。
- 已执行：使用项目虚拟环境构造大型 `dumps_sanitized()` 输入，验证 `max_string_length`、`max_bytes`、`max_list_items` 截断策略。
- 结果：通过，输出包含截断标记且大小受控。
- 解决策略：采用“分批删除 + 连接生命周期兜底 + 异步任务异常消费 + 写库前摘要截断”的组合策略，优先降低后台任务对在线请求和数据库的长期扰动。

## 第四批问题与修复

### 18. 普通前端 API 请求缺少统一超时和取消机制（已完成）

- 位置：`app/static/js/app.js`
- 问题：前端通用 `api.get/post/put/delete` 直接调用 `fetch()`，多数页面请求没有统一超时边界。
- 影响：网络异常、后端长时间无响应或浏览器连接被挂起时，页面操作会长期等待，占用浏览器连接和后端资源，用户也无法得到稳定失败反馈。
- 修复：新增 `API_TIMEOUT_MS`、`composeAbortSignal()`、`normalizeFetchError()` 和 `fetchWithTimeout()`，通用 API 方法统一走可超时、可取消的请求封装。

### 19. SPA 页面切换后旧页面请求仍可能继续占用浏览器和后端资源（已完成）

- 位置：`app/static/js/app.js`
- 问题：页面初始化和异步加载没有统一取消上一页面未完成请求。
- 影响：用户快速切换页面时，旧页面请求仍继续运行，可能造成重复查询、无效渲染和浏览器连接占用。
- 修复：新增 `currentPageAbortController`，`initializePage()` 开始时取消上一页面请求，并为当前页面创建新的取消信号。

### 20. 快速连续 shell 导航可能出现旧响应覆盖新页面或重复请求（已完成）

- 位置：`app/static/js/app.js`
- 问题：壳式导航连续点击时，旧导航请求和新导航请求并发执行，旧响应有机会晚到并覆盖新页面内容。
- 影响：页面状态可能错乱，重复请求也会增加后端模板渲染和数据库查询压力。
- 修复：新增 `shellNavigationAbortController`，每次 `navigateWithinShell()` 前取消上一轮壳式导航请求，并在 link 与 popstate 处理里忽略正常取消错误。

### 21. 监控自动轮询在页面隐藏时仍持续请求后端（已完成）

- 位置：`app/static/js/app.js`
- 问题：Dashboard 实时监控和运维指标自动刷新没有检查页面可见性。
- 影响：浏览器标签页进入后台后仍持续请求监控接口，增加数据库、Redis 和服务端指标采集压力。
- 修复：自动刷新回调在 `document.visibilityState === "hidden"` 时跳过本轮请求，页面恢复可见后继续按原周期刷新。

### 22. 指标接口 `summary/timeseries` 缺少短缓存，重复刷新会重复打数据库（已完成）

- 位置：`app/services/log_service.py`
- 问题：监控页面多处周期性刷新指标汇总和时序数据，服务层每次都重新查询和聚合日志表。
- 影响：多个管理员页面同时打开或页面高频刷新时，`request_logs` 指标查询会重复消耗数据库 CPU 与 IO。
- 修复：为 `metric_summary()` 和 `metric_timeseries()` 增加 5 秒短缓存，并按查询参数生成稳定缓存 Key，降低重复刷新对数据库的瞬时压力。

## 第四批验证结果

- 已执行：`node --check app/static/js/app.js`
- 结果：通过，前端脚本语法检查成功。
- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\log_service.py`
- 结果：通过，指标服务文件语法编译成功。
- 已同步：`app/templates/base.html` 中前端脚本版本参数更新为 `?v=20260609-01`，避免浏览器继续使用旧脚本缓存。
- 解决策略：采用“请求可取消 + 隐藏页降频 + 短 TTL 指标缓存 + 静态版本同步”的组合策略，优先减少用户无感知场景下的重复请求和长挂起请求。

## 第五批问题与修复

### 23. 账单事件合并分页随页码线性放大查询窗口（已完成）

- 位置：`app/routers/logging_api.py`
- 问题：`/api/logging/billing-events` 同时展示 Token finalize 与 Billing process 两类事件时，先分别查询 `page * page_size` 条，再在 Python 内存中合并排序和切片。
- 影响：页码越大，单次请求读取和序列化的历史事件越多；导出或深分页会放大数据库、内存和 CPU 压力。
- 修复：改为数据库层 `UNION ALL` 合并两类事件的轻量列集合，再统一按 `created_at`、`event_family`、`id` 排序分页；总数仍分别按过滤条件统计后相加。

### 24. 清空请求日志使用无上限全表删除，容易形成长事务（已完成）

- 位置：`app/services/log_service.py`
- 问题：`clear_logs()` 直接执行 `delete(RequestLog)` 并一次提交。
- 影响：日志表较大时可能长时间持锁、放大 WAL/回滚日志，影响请求日志写入和后台补全任务稳定性。
- 修复：新增 `CLEAR_LOGS_BATCH_SIZE=5000`，按主键分批查询和删除，每批提交一次，降低单事务压力。

### 25. 指标汇总按提供商和模型分组没有结果上限，高基数模型会放大响应（已完成）

- 位置：`app/services/log_service.py`
- 问题：`metric_summary()` 对最近窗口内的 `provider_id + provider_name + requested_model` 分组不设上限。
- 影响：模型名异常高基数或压测构造大量模型名时，单次指标接口可能返回大量分组，拖慢监控页面和网络传输。
- 修复：新增 `METRIC_GROUP_LIMIT=500`，指标汇总仅返回请求量最高的前 500 个分组。

### 26. 路由健康指标分组没有硬上限，候选评估可能被高基数日志拖慢（已完成）

- 位置：`app/services/log_service.py`
- 问题：`route_metric_summary()` 同样按 `provider_id + requested_model` 分组，但未限制分组数量。
- 影响：路由、健康检查和候选评估读取近期指标时，可能被大量低价值模型分组拖慢。
- 修复：复用 `METRIC_GROUP_LIMIT=500`，并按请求量倒序取前 500 个分组；指定单个模型时仍只统计该模型范围。

### 27. 系统监控后台积压快照每次刷新都重复执行多条日志表 count（已完成）

- 位置：`app/services/system_metrics_service.py`
- 问题：`_background_snapshot()` 每次采集都对待补全、计费失败、Token 失败分别执行 `count`，同时再查询最近失败后台任务。
- 影响：Dashboard、健康接口和运维页面短时间重复刷新时，会对 `request_logs` 与后台事件表产生重复读压力。
- 修复：新增 `BACKGROUND_SNAPSHOT_CACHE_SECONDS=5` 的进程内短缓存，后台积压摘要在 5 秒内复用，降低重复刷新压力。

## 第五批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\logging_api.py app\services\log_service.py app\services\system_metrics_service.py`
- 结果：通过，第五批涉及的日志 API、日志服务和系统监控服务文件语法编译成功。
- 结果：通过，合并分页返回顺序和总数符合预期。
- 结果：通过，分批删除返回 5，表内剩余 0。
- 解决策略：采用“数据库层合并分页 + 批量删除 + 指标分组硬上限 + 监控短缓存”的组合策略，优先削减深分页、清理操作和监控刷新对数据库的尖峰压力。

## 第六批问题与修复

### 28. Dashboard 近 24 小时请求、失败、Token 指标分三次扫描日志表（已完成）

- 位置：`app/routers/dashboard.py`
- 问题：`build_dashboard_payload()` 对近 24 小时请求数、失败数、Token 总量分别执行独立查询。
- 影响：Dashboard 首屏或自动刷新时，同一时间窗口重复扫描 `request_logs`，增加数据库读压力。
- 修复：新增 `_dashboard_recent_overview()`，用一次聚合查询同时返回请求数、失败数和 Token 总量，并增加 10 秒短缓存。

### 29. Dashboard 提供商健康状态使用多条 count 查询（已完成）

- 位置：`app/routers/dashboard.py`
- 问题：提供商总数、健康、降级、异常状态分别执行独立 `count`。
- 影响：提供商数量增加或 Dashboard 多用户同时刷新时，重复查询放大首屏开销。
- 修复：新增 `_dashboard_provider_status_counts()`，按 `Provider.health_status` 一次分组统计并缓存 10 秒。

### 30. Dashboard 模型健康状态使用多条 count 查询（已完成）

- 位置：`app/routers/dashboard.py`
- 问题：模型总数和 ProviderModel 健康状态分别以多条查询获取。
- 影响：模型目录和挂载模型较多时，首屏状态统计会产生不必要的数据库往返。
- 修复：新增 `_dashboard_model_status_counts()`，ProviderModel 健康状态改为分组统计，状态结果缓存 10 秒；模型目录总数保持独立轻量统计。

### 31. API Key 总览每次 Dashboard 刷新都重复执行全量 Key 与日志聚合（已完成）

- 位置：`app/services/api_key_admin_service.py`
- 问题：`ApiKeyAdminService.get_summary()` 每次都会统计 Key 状态、全量请求日志用量和账户余额汇总。
- 影响：Dashboard 或 API Key 管理页频繁刷新时，全量日志聚合会重复占用数据库 CPU 与 IO。
- 修复：引入 10 秒短缓存，缓存为普通 dict，命中后用 `ApiKeySummaryOut.model_validate()` 还原响应模型。

### 32. 用户端概览已加载 API Key 后，趋势统计仍重复查询 Key ID（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：`get_overview()` 已经读取用户全部 API Key，但 `get_usage_trend()` 内部又调用 `list_owned_api_key_ids()` 再查一次。
- 影响：用户首页每次加载多一次不必要数据库查询，API Key 较多时增加响应延迟。
- 修复：`get_usage_trend()` 支持传入已知 `key_ids`，`get_overview()` 直接复用已加载 Key 列表。

## 第六批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\dashboard.py app\services\api_key_admin_service.py app\services\user_portal_service.py`
- 结果：通过，第六批涉及的 Dashboard、API Key 管理服务和用户端服务文件语法编译成功。
- 结果：通过，提供商和模型状态聚合计数符合预期。
- 解决策略：采用“同窗口指标合并聚合 + 状态分组统计 + 短 TTL 总览缓存 + 复用已加载 Key 列表”的组合策略，降低首屏统计和高频刷新产生的重复数据库往返。

## 第七批问题与修复

### 33. IP 管理事件清理逐条 ORM 删除且单次只处理一批（已完成）

- 位置：`app/services/ip_management_event_service.py`
- 问题：`cleanup_old_events()` 只取最多 5000 条过期事件并逐条 `db.delete(row)`。
- 影响：历史事件积压超过一批时需要多轮任务才能清完；逐条 ORM 删除也会增加内存和对象管理开销。
- 修复：新增 `CLEANUP_BATCH_SIZE=5000`，按主键分批查询并用 `DELETE ... WHERE id IN (...)` 删除，循环直到本轮过期数据清理完。

### 34. Responses→Chat 数据库会话清理使用无上限条件删除（已完成）

- 位置：`app/services/responses_chat_adapter_service.py`
- 问题：`cleanup_expired_database_sessions()` 对所有过期适配会话执行单条无上限 `DELETE`。
- 影响：适配会话大量积压时，清理任务可能产生长事务和锁等待。
- 修复：新增 `DATABASE_CLEANUP_BATCH_SIZE=5000`，按 `response_id` 分批删除过期会话，每批提交一次。

### 35. 类型化日志 processing 队列恢复一次性读取全部积压项（已完成）

- 位置：`app/logging/queue.py`
- 问题：`LoggingQueue._recover_processing_items()` 启动时使用 `lrange(PROCESSING_KEY, 0, -1)` 一次性取出全部处理中队列。
- 影响：异常停机后如果 processing 队列积压较多，worker 启动恢复会瞬间占用大量内存并阻塞启动。
- 修复：新增 `RECOVERY_BATCH_SIZE=500`，按 500 条一批搬回主队列，并用 `ltrim` 逐批裁剪 processing 队列。

### 36. 请求日志 processing 队列恢复一次性读取全部积压项（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：`RequestLogQueueService._recover_processing_items()` 同样一次性读取全部处理中请求日志。
- 影响：请求日志可靠队列在异常重启后，积压恢复可能造成启动期内存尖峰，影响 Web worker 或后台 worker 稳定性。
- 修复：新增 `RECOVERY_BATCH_SIZE=500`，逐批搬运并裁剪 processing 队列。

### 37. 用户维度 API Key 鉴权缓存失效一次性读取用户全部 Key ID（已完成）

- 位置：`app/services/api_key_auth_cache.py`
- 问题：`async_invalidate_user()` 使用 `smembers()` 一次性读取 `auth:user:{id}:api_keys` 集合。
- 影响：单个用户绑定大量 API Key 时，用户更新或批量失效会造成 Redis 响应和进程内存尖峰。
- 修复：改为 `sscan_iter(count=100)` 流式遍历并逐个失效 API Key 缓存，最后删除集合 Key。

## 第七批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\ip_management_event_service.py app\services\responses_chat_adapter_service.py app\logging\queue.py app\services\request_log_queue_service.py app\services\api_key_auth_cache.py`
- 结果：通过，第七批涉及的维护清理、队列恢复和鉴权缓存文件语法编译成功。
- 结果：通过，两个清理函数均删除 5 条过期记录且各保留 1 条未过期记录。
- 解决策略：采用“主键分批删除 + Redis 队列分批恢复 + Redis Set 流式扫描”的组合策略，优先降低异常积压和维护任务对启动期内存、数据库事务和 Redis 响应体大小的冲击。

## 第八批问题与修复

### 38. 后台请求日志导出允许一次性生成 10000 行 CSV（已完成）

- 位置：`app/routers/logs.py`、`app/services/log_service.py`
- 问题：后台日志导出接口允许 `limit <= 10000`，服务层也按 10000 做上限。
- 影响：CSV 在内存中一次性构建，导出上限过高会增加数据库读取、序列化和响应内存占用。
- 修复：新增 `LogService.EXPORT_LIMIT=5000`，服务层强制按 5000 收敛；后台日志导出接口上限同步改为 5000。

### 39. 类型化日志导出允许一次性生成 10000 行 CSV（已完成）

- 位置：`app/routers/logging_api.py`
- 问题：异常、健康检查、账单事件、后台任务、资产事件等类型化日志导出接口允许 `limit <= 10000`。
- 影响：复杂事件包含 JSON 摘要字段，过高导出上限会放大内存响应和 CSV 序列化时间。
- 修复：类型化日志导出 `limit` 上限统一改为 5000，并继续复用现有过滤和分页逻辑。

### 40. 内容防护运行时事件导出允许一次性生成 10000 行 CSV（已完成）

- 位置：`app/routers/content_guard.py`
- 问题：内容防护运行时事件导出允许 `limit <= 10000`。
- 影响：内容防护事件可能包含命中规则、摘要、原因等文本字段，过大导出会增加后台内存占用和响应延迟。
- 修复：导出上限改为 5000。

### 41. 用户端请求日志导出允许一次性生成 10000 行 CSV（已完成）

- 位置：`app/routers/user_portal.py`、`app/services/log_service.py`
- 问题：用户端日志导出入口允许 `limit <= 10000`，最终也走请求日志 CSV 内存构建。
- 影响：普通用户批量导出大日志时可能占用过多应用内存，影响同一 worker 的其它请求响应。
- 修复：用户端日志导出接口上限改为 5000，并由 `LogService.EXPORT_LIMIT` 做服务层兜底。

### 42. 用户账单与后台用户账单导出上限过高（已完成）

- 位置：`app/routers/user_portal.py`、`app/routers/user_accounts.py`、`app/services/user_portal_service.py`
- 问题：用户账单导出和后台用户账单导出允许 `limit <= 10000`，用户端账单服务内部也按 10000 截断。
- 影响：账单记录导出包含金额、余额、模型和 token 字段，过高上限会增加查询和内存 CSV 构建压力。
- 修复：相关导出入口和 `UserPortalService.export_billing_csv()` 内部上限统一收敛为 5000。

## 第八批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\logs.py app\routers\logging_api.py app\routers\content_guard.py app\routers\user_portal.py app\routers\user_accounts.py app\services\log_service.py app\services\user_portal_service.py`
- 结果：通过，第八批涉及的导出路由和服务文件语法编译成功。
- 已执行：扫描 `app/routers` 与 `app/services` 中导出相关 `limit <= 10000` 和 `min(limit, 10000)` 口径。
- 结果：通过，常规导出入口已无 10000 上限残留；压测参数等非导出场景未改动。
- 结果：通过，输出为 1 行表头 + 3 行数据，服务层上限兜底生效。
- 解决策略：采用“统一导出上限 + 服务层兜底 + 不触碰压测参数”的组合策略，降低 CSV 内存响应对数据库读取、序列化 CPU 和 worker 内存的峰值占用。

## 第九批问题与修复

### 43. 上游连接池默认值对 2C2G 部署过高（已完成）

- 位置：`app/config.py`、`.env.example`
- 问题：`UPSTREAM_MAX_CONNECTIONS=1200`、`UPSTREAM_MAX_KEEPALIVE_CONNECTIONS=300` 作为默认值偏激进。
- 影响：低配服务器上可能分配过多上游连接、文件描述符和连接池管理开销，与 2C2G 保守资源要求不一致。
- 修复：默认上游最大连接数改为 200，最大 keep-alive 连接数改为 50，示例配置同步更新。

### 44. requests fallback 连接池强制按 1200 默认扩张（已完成）

- 位置：`app/services/proxy_service.py`
- 问题：`_get_thread_local_requests_session()` 在未配置时使用 `upstream_max_connections=1200` 作为 fallback。
- 影响：即使全局配置收敛，requests fallback 路径仍可能创建过大的线程本地连接池。
- 修复：fallback 默认值改为 200，并继续尊重 `UPSTREAM_MAX_CONNECTIONS` 配置。

### 45. 远程图片自动转 base64 先完整读取响应再检查大小（已完成）

- 位置：`app/services/proxy_service.py`
- 问题：`_fetch_remote_image_as_data_url()` 使用 `response.content` 获取完整远程图片后再检查 50 MB 上限。
- 影响：远程服务器不返回 `Content-Length` 或返回超大响应时，应用会先把整个 body 读入内存，导致内存峰值不可控。
- 修复：改为 `stream=True` 分块读取；先检查 `Content-Length`，无长度时按块累计并在超过上限时立即拒绝。

### 46. 远程图片自动转换使用独立 50 MB 上限且未校验图片类型（已完成）

- 位置：`app/services/proxy_service.py`、`app/services/asset_service.py`
- 问题：远程图片转换路径使用独立 `50 MB` 上限，高于本地图片上传 `10 MB` 上限，也没有根据响应 `Content-Type` 拒绝非图片响应。
- 影响：同类图片数据入口资源口径不一致；错误 URL 返回 HTML 或其它大响应时仍可能被 base64 编码进请求体。
- 修复：远程图片转换复用 `AssetService.MAX_IMAGE_BYTES` 和 `AssetService.IMAGE_CONTENT_TYPES`，统一 10 MB 图片上限并拒绝非 PNG/JPEG/WEBP/GIF 类型。

### 47. 上游错误体读取无独立字节上限（已完成）

- 位置：`app/services/proxy_service.py`、`app/services/health_service.py`
- 问题：流式上游错误响应和健康探针错误响应使用 `aread()` 读取完整错误体后再截断展示文本。
- 影响：异常上游返回超大错误体时，代理和健康检查会先消耗大量内存，再只展示前 500 字符。
- 修复：新增 64 KiB 错误体读取上限；代理主链路和健康检查均按块读取，超限后停止继续读入。

## 第九批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\config.py app\services\proxy_service.py app\services\health_service.py`
- 结果：通过，第九批涉及的配置、代理服务和健康检查文件语法编译成功。
- 已执行：使用模拟 requests Session 构造远程图片响应，`Content-Length` 设置为 `AssetService.MAX_IMAGE_BYTES + 1`，调用 `_fetch_remote_image_as_data_url()`。
- 结果：通过，函数直接返回 400 错误且没有读取响应 body。
- 解决策略：采用“保守连接池默认 + 上游错误体限量读取 + 远程图片流式读取 + 图片入口统一大小和类型口径”的组合策略，降低异常上游和远程素材输入导致的内存、连接和响应延迟风险。

## 第十批问题与修复

### 48. 近 1 MB 前端脚本未启用应用层压缩（已完成）

- 位置：`app/main.py`、`app/static/js/app.js`
- 问题：`app/static/js/app.js` 当前约 900 KB，应用没有启用 GZip 中间件。
- 影响：首屏和页面切换首次加载脚本时传输体积偏大，弱网或跨地域访问响应速度明显受影响。
- 修复：启用 `GZipMiddleware(minimum_size=1024)`，对大体积 JS/CSS/JSON 响应进行压缩传输。

### 49. 静态资源缺少明确长缓存策略（已完成）

- 位置：`app/main.py`、`app/templates/base.html`
- 问题：`/static/` 资源虽然带版本参数，但服务端没有明确 `Cache-Control`，浏览器和代理缓存行为不稳定。
- 影响：用户重复访问后台时，仍可能频繁重新验证或下载大体积 JS/CSS。
- 修复：对 `/static/` 响应增加 `Cache-Control: public, max-age=31536000, immutable`，配合版本参数保证更新可控。

### 50. 上传资产与应用静态资源使用同类缓存口径不清晰（已完成）

- 位置：`app/main.py`
- 问题：`/uploaded-assets/` 没有独立缓存策略，与版本化 `/static/` 资源混用默认行为。
- 影响：用户端或 Playground 图片资产重复展示时可能频繁请求服务端；但上传资产又不适合像版本化静态文件一样永久缓存。
- 修复：对 `/uploaded-assets/` 设置 `Cache-Control: public, max-age=3600`，降低重复加载压力，同时保留较短缓存窗口。

### 51. 静态资源版本参数未随缓存策略更新同步刷新（已完成）

- 位置：`app/templates/base.html`
- 问题：新增长缓存策略后，如果版本参数不更新，浏览器可能继续持有旧 JS/CSS。
- 影响：缓存策略和当前代码变更可能短期不一致，导致前端行为调试困难。
- 修复：CSS 与 JS 版本参数统一更新为 `?v=20260609-03`。

### 52. 前端巨型 bundle 拆分成本较高，缺少低风险传输层缓解（已完成）

- 位置：`app/main.py`、`app/static/js/app.js`、`app/static/css/app.css`
- 问题：当前单个 `app.js` 接近 1 MB、`app.css` 接近 270 KB，立即拆分模块会影响大量页面初始化逻辑。
- 影响：一次性拆分风险高，但不处理又会持续影响首屏传输和重复加载成本。
- 修复：本批先采用低侵入传输层策略：GZip 压缩 + 版本化长缓存；后续若继续前端专项优化，再做模块拆分或按页面懒加载。

## 第十批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\main.py app\services\proxy_service.py app\services\health_service.py`
- 结果：通过，第十批涉及的应用入口和前一批代理/健康文件语法编译成功。
- 已执行：`node --check app/static/js/app.js`
- 结果：通过，前端脚本语法检查成功。
- 已执行：扫描 `GZipMiddleware`、`Cache-Control`、`app.css?v=`、`app.js?v=`。
- 结果：通过，压缩中间件、静态缓存头和版本参数均已落地。
- 解决策略：采用“应用层压缩 + 版本化长缓存 + 上传资产短缓存 + 静态版本同步”的组合策略，先以低风险方式缓解大静态资源传输和重复加载问题。

## 第十一批问题与修复

### 53. 真实并发探测入口允许过高并发上限（已完成）

- 位置：`app/routers/benchmark.py`、`app/templates/benchmark.html`、`scripts/real_concurrency_benchmark.py`
- 问题：后台压测入口允许 `concurrency <= 5000`、`probe_max_concurrency <= 20000`。
- 影响：管理员误填参数时会创建大量异步任务、上游连接和外部请求，容易挤占同机 Web worker、Redis、数据库和上游容量。
- 修复：接口上限收敛为基准并发 1000、最大探测并发 2000；模板输入范围同步更新；脚本直跑时也做同口径钳制。

### 54. 单轮样本和请求倍数上限过大（已完成）

- 位置：`app/routers/benchmark.py`、`app/templates/benchmark.html`、`scripts/real_concurrency_benchmark.py`
- 问题：`sample_requests <= 200000`、`requests_per_concurrency <= 100` 会让每个并发档位生成极大请求数。
- 影响：多模型、多轮探测时总请求量会指数式放大，导致运行时间、内存结果列表、报告体积和日志刷新压力过高。
- 修复：每轮最少请求上限收敛为 10000，请求倍数上限收敛为 20，并在 API、页面和脚本层同步兜底。

### 55. 预热与单请求超时上限过长（已完成）

- 位置：`app/routers/benchmark.py`、`app/templates/benchmark.html`、`scripts/real_concurrency_benchmark.py`
- 问题：`warmup_requests <= 10000`、`client_timeout_s/request_timeout_s <= 3600`。
- 影响：预热阶段可能已经产生大量请求；单请求允许等待 1 小时会让压测子进程长时间占用连接、线程和任务状态。
- 修复：预热请求上限收敛为 1000，客户端和 HTTP 请求超时上限收敛为 600 秒，并同步脚本钳制。

### 56. 压测任务内存状态缺少保留上限（已完成）

- 位置：`app/routers/benchmark.py`
- 问题：`_jobs` 字典只在列表接口返回前 10 条，但内部从不清理已结束任务。
- 影响：长时间运行的后台进程会持续累积任务配置、日志、阶段结果和报告摘要，占用进程内存。
- 修复：新增已结束任务保留策略，最多保留 20 个完成/失败/停止任务，且不会删除正在运行的任务。

### 57. 压测报告文件与完成回读存在稳定性风险（已完成）

- 位置：`app/routers/benchmark.py`
- 问题：`data/benchmark-reports` 没有报告文件保留策略；压测子进程成功退出后还会在持有 `_jobs_lock` 时回读 JSON 报告摘要。
- 影响：报告文件长期堆积会占用磁盘；报告回读路径存在自锁风险，可能导致任务完成后状态刷新卡住。
- 修复：启动压测前清理旧报告，默认仅保留最近 80 个 JSON/HTML 报告文件；压测完成后的报告摘要回读移到锁外执行。

## 第十一批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\benchmark.py scripts\real_concurrency_benchmark.py`
- 结果：通过，第十一批涉及的压测路由和脚本语法编译成功。
- 已执行：构造 `RealConcurrencyBenchmarkRequest` 边界参数，验证上限值可通过、超过上限会被 Pydantic 拒绝。
- 结果：通过，并发、最大探测并发、样本请求、请求倍数、预热请求和超时上限均按新口径生效。
- 已执行：直接调用 `scripts.real_concurrency_benchmark.normalize_args()`，输入旧的大参数上限。
- 结果：通过，脚本层会将并发钳制到 1000、最大探测并发 2000、样本 10000、请求倍数 20、预热 1000、超时 600 秒。
- 已执行：构造 25 个已结束任务和 1 个运行中任务，调用 `_prune_finished_jobs_locked()`。
- 结果：通过，仅保留 20 个已结束任务并保留运行中任务。
- 已执行：使用临时目录构造 6 个报告文件并临时设置报告保留上限为 4，调用 `_cleanup_old_report_files()`。
- 结果：通过，最旧 2 个报告被清理，剩余 4 个报告保留。
- 解决策略：采用“API 限制 + 前端输入范围同步 + 脚本二次钳制 + 内存/磁盘保留策略 + 锁外报告回读”的组合策略，降低真实并发探测对生产后台资源和任务稳定性的冲击。

## 第十二批问题与修复

### 58. 本地代理压测脚本参数缺少硬上限（已完成）

- 位置：`scripts/benchmark_proxy.py`
- 问题：`--requests`、`--concurrency`、`--timeout-s`、`--warmup` 只定义默认值，没有脚本层硬上限。
- 影响：手动执行脚本时可传入极大请求量、并发数或超长超时，导致本地服务、数据库、Redis 和上游 mock 进程被压垮。
- 修复：新增参数归一化，请求数上限 10000、并发上限 500、超时上限 600 秒、预热上限 1000。

### 59. 本地压测夹具写入过大的 provider 容量（已完成）

- 位置：`scripts/benchmark_proxy.py`
- 问题：脚本会把 mock provider 的 `max_active_requests`、`max_active_streams` 写成 `10000`。
- 影响：共享开发数据库中复用该 provider 时，路由容量硬约束会被测试夹具放大，容易掩盖真实容量限制并增加误用风险。
- 修复：改为按压测参数计算受控容量，默认不超过 500。

### 60. 本地压测专用 API Key 不设置 QPS/RPM 限制（已完成）

- 位置：`scripts/benchmark_proxy.py`
- 问题：脚本将压测 API Key 的 `qps_limit`、`rpm_limit` 写为 `None`。
- 影响：如果该 Key 被复用或脚本参数异常，限流层无法兜底，容易造成突发请求冲击。
- 修复：压测 Key 的 QPS/RPM 按受控容量写入，RPM 使用 `capacity_limit * 60`。

### 61. 本地压测 HTTP 连接池随并发无上限扩张（已完成）

- 位置：`scripts/benchmark_proxy.py`
- 问题：`httpx.Limits(max_connections=max(concurrency * 2, 100), max_keepalive_connections=max(concurrency, 20))` 没有硬上限。
- 影响：高并发参数会创建过多连接和 keep-alive 连接，增加文件描述符、内存和连接调度开销。
- 修复：连接池增加硬上限：最大连接 1000、最大 keep-alive 连接 500。

### 62. 本地压测流式响应读取无大小限制且不清理专用日志（已完成）

- 位置：`scripts/benchmark_proxy.py`
- 问题：流式压测会持续读取完整响应字节；脚本复用固定压测 API Key，但运行前不清理旧 `request_logs` 和账单记录。
- 影响：异常流式响应可能造成内存结果统计膨胀；旧压测数据会污染日志积压、计费补全和性能统计口径。
- 修复：单个流式响应读取超过 1 MiB 后标记失败并停止读取；启动前清理该压测 API Key 对应的请求日志和账单记录。

## 第十二批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall scripts\benchmark_proxy.py`
- 结果：通过，本地代理压测脚本语法编译成功。
- 已执行：构造超大 `requests/concurrency/timeout_s/warmup` 参数并调用 `normalize_args()`。
- 结果：通过，参数分别被钳制为 10000、500、600、1000。
- 已执行：源码级检查 `MAX_BENCHMARK_RESPONSE_BYTES`、受控 provider 容量、API Key QPS/RPM、请求日志/账单清理和连接池硬上限。
- 结果：通过，第十二批新增资源保护点均已落地。
- 解决策略：采用“脚本入口参数钳制 + 测试夹具容量收敛 + API Key 限流兜底 + 连接池上限 + 专用压测数据清理”的组合策略，降低本地压测脚本误用对项目运行态和观测口径的影响。

## 第十三批问题与修复

### 63. 内容防护概览一次性序列化全部提供商和模型（已完成）

- 位置：`app/services/content_guard_module_service.py`
- 问题：`build_overview()` 直接加载并序列化全部提供商及其启用模型。
- 影响：提供商和模型数量增长后，内容防护页面首屏接口会产生大查询、大 JSON 和较高模板渲染压力。
- 修复：概览展示 provider 默认限制为 200 条；摘要计数改为 SQL 聚合，保持全局统计口径。

### 64. 内容防护概览延迟样本上限过高（已完成）

- 位置：`app/services/content_guard_module_service.py`
- 问题：近 1 小时内容防护延迟样本一次读取 5000 条后在 Python 内计算分位数。
- 影响：页面刷新会增加数据库读取和 Python 排序开销。
- 修复：概览延迟样本上限收敛为 1000 条，保留近时段趋势参考能力。

### 65. 内容防护概览近期事件样本偏大（已完成）

- 位置：`app/services/content_guard_module_service.py`
- 问题：概览读取 200 条内容防护事件，再按 provider 分桶。
- 影响：事件字段包含原因、摘要等文本，页面刷新时会增加内存和响应体积。
- 修复：近期事件样本上限收敛为 100 条，每个 provider 仍最多展示 5 条。

### 66. 运行时设置接口复用完整 overview 重型查询（已完成）

- 位置：`app/routers/content_guard.py`
- 问题：`/api/content-guard/runtime/settings` 只需要 settings，却调用 `build_overview(db)["settings"]`。
- 影响：运行时配置刷新会额外查询 provider、模型、延迟样本和事件列表，增加后台响应延迟。
- 修复：改为直接读取 `SettingService.get_or_create(db)` 并调用 `serialize_settings()`。

### 67. 内容防护手动探针与自动 L3 预检缺少批量边界（已完成）

- 位置：`app/schemas/content_guard.py`、`app/services/health_service.py`
- 问题：手动探针 `probe_keys` 没有 schema 长度上限；自动 L3 内容完整性预检会按符合条件的模型持续跑完整轮。
- 影响：异常请求体或模型规模扩大时，探针数量和自动检测耗时不可控。
- 修复：`probe_keys` 上限设为 5；自动 L3 内容完整性每轮最多处理 20 个模型，剩余模型留到后续调度周期。

## 第十三批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\schemas\content_guard.py app\services\content_guard_module_service.py app\routers\content_guard.py app\services\health_service.py`
- 结果：通过，内容防护 schema、服务、路由和健康检查文件语法编译成功。
- 已执行：构造 5 个探针键的 `ContentGuardRunRequest`，再构造 6 个探针键的请求。
- 结果：通过，5 个探针键可通过，6 个探针键被 Pydantic 拒绝。
- 已执行：源码级检查概览 provider/延迟/事件上限、概览不再返回完整 rules、runtime settings 不再调用完整 overview、自动 L3 模型预算常量。
- 结果：通过，第十三批新增的内容防护资源边界均已落地。
- 解决策略：采用“概览分页化上限 + SQL 聚合摘要 + 轻量设置接口 + 探针 schema 边界 + 自动任务分批处理”的组合策略，降低内容防护页面刷新和自动预检对数据库、上游请求和响应体积的压力。

## 第十四批问题与修复

### 68. 告警刷新中异常 API Key 查询存在 N+1 用户查询（已完成）

- 位置：`app/services/alert_service.py`
- 问题：`list_abnormal_api_keys()` 已经 outer join 用户表，但循环内仍对每个 API Key 调用 `db.get(UserAccount, ...)`。
- 影响：异常 Key 较多时告警刷新会产生额外数据库往返，增加响应延迟。
- 修复：查询中直接选择 owner 用户名和余额字段，循环内不再逐条查询用户。

### 69. 账户告警候选用户全量加载（已完成）

- 位置：`app/services/alert_service.py`
- 问题：`list_alert_users()` 调用 `UserAuthService.list_users(db)` 加载全部用户后再筛选启用用户。
- 影响：用户规模扩大后，告警刷新会把大量无告警风险用户纳入 Python 内存和后续聚合条件。
- 修复：改为 SQL 侧筛选启用且余额耗尽或余额低于 20% 的候选用户，并限制最多 500 个候选。

### 70. 告警 upsert 每次读取全量告警事件（已完成）

- 位置：`app/services/alert_service.py`
- 问题：`_upsert_events()` 使用 `select(AlertEvent)` 读取全部告警事件。
- 影响：历史已解决告警增多后，每次刷新都要加载无关历史事件，增加内存和数据库压力。
- 修复：仅读取当前 active keys 相关事件和未解决事件，用于更新当前告警和自动恢复旧活跃告警。

### 71. 异常 provider 告警事件构造无上限（已完成）

- 位置：`app/services/alert_service.py`
- 问题：告警刷新会把所有异常 provider 构造成 active events。
- 影响：当大量 provider 同时异常时，单次告警刷新会生成过多事件 payload 并增加 upsert 成本。
- 修复：本轮 provider 告警事件默认最多处理 100 个，概览仍只展示前 20 个异常 provider。

### 72. Provider 页面定时刷新在 SPA 切页后不清理（已完成）

- 位置：`app/static/js/app.js`、`app/templates/base.html`
- 问题：Provider 页面 30 秒轮询只注册 `beforeunload` 清理，SPA 内部切换页面时 interval 仍可能继续运行。
- 影响：用户多次进入/离开 Provider 页面后，后台可能残留多个轮询请求，增加无意义 API 压力。
- 修复：改为复用 `registerPageCleanup()` 在页面切换时清理定时器；静态资源版本同步更新为 `20260609-05`。

## 第十四批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\alert_service.py`
- 结果：通过，告警服务语法编译成功。
- 已执行：`node --check app/static/js/app.js`
- 结果：通过，前端脚本语法检查成功。
- 已执行：源码级检查异常 API Key join 字段、告警用户候选上限、provider 告警上限、告警事件读取范围、Provider 页面 `registerPageCleanup()` 和静态资源版本号。
- 结果：通过，第十四批告警刷新与前端轮询资源保护均已落地。
- 解决策略：采用“告警查询去 N+1 + 候选集合前置筛选 + upsert 读取范围收敛 + 异常 provider 事件上限 + SPA 定时器清理”的组合策略，降低告警面板和 Provider 页面轮询对数据库与后台接口的持续压力。

## 第十五批问题与修复

### 73. 模型列表接口默认仍允许全量返回（已完成）

- 位置：`app/routers/models.py`
- 问题：`/api/models` 默认 `paginated=false`，会返回完整模型目录列表。
- 影响：模型数量和 provider 绑定数量增长后，后台模型页首屏可能产生大 JSON 和大量序列化开销。
- 修复：默认改为分页模式；兼容旧的非分页调用时最多返回 500 条。

### 74. 单模型详情先加载全量目录再查目标模型（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：`get_model_detail()` 先调用 `_load_catalogs_and_providers()` 加载全部模型目录，再在 Python 中查找单个模型。
- 影响：查看或更新单模型详情时会触发无关模型的数据库读取和序列化前置成本。
- 修复：改为先按模型名直接查询目标目录项，仅保留 provider 列表用于绑定详情序列化。

### 75. 一键测试全部模型缺少本轮模型数量上限（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：`test_all_model_health()` 会对全部目录模型创建健康测试任务。
- 影响：模型规模扩大后，即使有并行信号量，也会在单次请求中排队大量上游探针，拖长请求并占用资源。
- 修复：本轮一键测试最多处理 50 个目录模型。

### 76. 模型映射列表无服务层返回上限（已完成）

- 位置：`app/services/model_mapping_service.py`
- 问题：`list_mappings()` 返回全部模型映射。
- 影响：映射规则数量增长后，管理页加载会产生大响应和较多 JSON 解析开销。
- 修复：服务层列表默认最多返回 500 条映射。

### 77. 会话回放无日志条数和文本长度上限（已完成）

- 位置：`app/services/conversation_service.py`、`app/schemas/conversation.py`
- 问题：`get_replay()` 会读取单个会话下全部请求日志，并把完整请求/响应文本拼成 turns。
- 影响：长会话或大响应会导致内存占用和响应体积过大，影响会话详情打开速度。
- 修复：单个会话回放最多读取 200 条日志；单条 turn 文本最多保留 4000 字符；响应增加 `truncated` 与 `log_limit` 标记。

## 第十五批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\model_catalog_service.py app\routers\models.py app\services\model_mapping_service.py app\services\conversation_service.py app\schemas\conversation.py`
- 结果：通过，模型目录、模型映射、会话服务和 schema 语法编译成功。
- 已执行：源码级检查模型列表默认分页、非分页兼容上限、单模型详情直接查询、全量模型测试上限、映射列表上限、会话回放日志和文本上限。
- 结果：通过，第十五批新增资源边界均已落地。
- 解决策略：采用“列表默认分页 + 兼容入口限量 + 单对象查询短路 + 批量测试上限 + 长会话截断”的组合策略，降低模型管理和会话回放在数据增长后的响应体积与后台资源占用。

## 第十六批问题与修复

### 78. API Key 兼容列表接口仍可全量返回（已完成）

- 位置：`app/routers/api_keys.py`、`app/services/api_key_admin_service.py`
- 问题：`GET /api/api-keys` 直接返回全部 API Key，且每条会加载 provider 绑定和归属用户。
- 影响：API Key 数量扩大后，兼容接口会产生大响应、较多关系加载和 JSON 序列化开销。
- 修复：服务层 `list_api_keys()` 增加可选 `limit`；兼容列表接口默认和最大只返回 500 条。

### 79. API Key 导出接口一次性导出全部 Key（已完成）

- 位置：`app/routers/api_keys.py`
- 问题：`/api/api-keys/export` 调用全量 API Key 列表后在内存中拼 CSV。
- 影响：大量 Key 时会放大数据库读取、关系预加载和内存占用，影响后台响应速度。
- 修复：导出接口增加 `limit` 查询参数，默认和最大为 5000，并在审计日志中记录导出上限。

### 80. 用户列表导出无返回上限（已完成）

- 位置：`app/routers/user_accounts.py`
- 问题：`/users/export` 使用 `UserAuthService.list_users(db)` 读取全部用户后统计 Key 数并拼 CSV。
- 影响：用户规模增长后，导出会拉取全量用户对象并扩大内存峰值。
- 修复：导出接口增加 `limit` 查询参数，默认和最大为 5000，并复用分页用户查询只读取本次导出范围。

### 81. 用户账单导出为有限账单行加载全量用户和 Key 映射（已完成）

- 位置：`app/routers/user_accounts.py`
- 问题：`/users/billing-export` 虽限制账单行数，但仍加载全部 API Key 和全部用户构造名称映射。
- 影响：账单导出响应会被无关用户和 Key 规模拖慢，内存占用与导出行数不匹配。
- 修复：先读取本次导出的账单行，再仅按这些行中的 `user_account_id` 和 `api_client_key_id` 查询名称映射。

### 82. 用户选项接口全量加载后在 Python 过滤启用用户（已完成）

- 位置：`app/routers/user_accounts.py`
- 问题：`/api/users/options` 调用 `UserAuthService.list_users(db)` 加载全部用户后再筛选 `enabled`。
- 影响：用户选择器刷新会随着全部用户数量线性变慢，并返回过多候选。
- 修复：改为 SQL 侧筛选启用用户并限制默认和最大 500 条。

## 第十六批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\api_keys.py app\routers\user_accounts.py app\services\api_key_admin_service.py`
- 结果：通过，API Key 与用户管理路由、API Key 管理服务语法编译成功。
- 已执行：源码级检查 API Key 列表/导出上限、用户导出上限、账单导出按需映射查询、用户选项 SQL 侧筛选与上限。
- 结果：通过，第十六批后台管理列表与导出资源边界均已落地。
- 解决策略：采用“兼容列表限量 + 导出上限 + 导出关联数据按需查询 + 选项接口 SQL 侧筛选”的组合策略，降低后台管理入口在数据量扩大后的响应体积、关系加载和内存峰值。

## 第十七批问题与修复

### 83. API Key 批量操作请求体缺少 Key 数量上限（已完成）

- 位置：`app/schemas/api_key.py`
- 问题：`ApiKeyBatchActionIn.api_key_ids` 只做去重和非空校验，没有限制单次批量操作的 Key 数量。
- 影响：异常请求可一次提交大量 ID，放大批量启停、删除、轮换、过期、模板应用等操作的数据库与缓存失效压力。
- 修复：统一设置单次 API Key 批量操作最多 200 个 ID。

### 84. API Key 批量授权 provider 列表缺少数量上限（已完成）

- 位置：`app/schemas/api_key.py`
- 问题：`ApiKeyBatchProviderUpdateIn.allowed_provider_ids` 没有长度上限。
- 影响：批量授权请求可携带过大的 provider ID 列表，增加校验查询、绑定替换和请求解析成本。
- 修复：批量授权 provider ID 列表最多允许 500 个。

### 85. 用户冻结金额批量更新缺少用户数量边界（已完成）

- 位置：`app/routers/user_accounts.py`
- 问题：`/users/batch/quota-policy` 可从表单和文本框解析任意数量用户 ID。
- 影响：一次提交过多用户会拉长事务、缓存失效循环和后台响应时间。
- 修复：用户冻结金额批量更新单次最多处理 200 个用户。

### 86. 用户冻结金额批量更新和预览逐个查询用户（已完成）

- 位置：`app/routers/user_accounts.py`
- 问题：批量更新与 `/api/users/quota-preview` 都按用户 ID 循环 `db.get(UserAccount, id)`。
- 影响：选择用户数量增加时产生 N+1 数据库往返，预览和提交响应线性变慢。
- 修复：改为一次 `IN` 查询加载目标用户，预览和更新共享 200 个用户上限。

### 87. 删除用户前管理员数量校验全量加载用户（已完成）

- 位置：`app/routers/user_accounts.py`
- 问题：删除用户时用 `UserAuthService.list_users(db)` 读取全部用户后在 Python 中统计管理员数量。
- 影响：用户数量增长后，单个删除操作也会加载无关用户对象。
- 修复：改为数据库侧 `count()` 统计管理员数量。

## 第十七批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\schemas\api_key.py app\routers\user_accounts.py`
- 结果：通过，API Key schema 与用户管理路由语法编译成功。
- 已执行：构造 200/201 个 API Key ID、500/501 个 provider ID 的批量请求体。
- 结果：通过，200 个 Key 与 500 个 provider 可通过，超过上限会被 Pydantic 拒绝。
- 已执行：源码级检查用户冻结金额批量上限、批量/预览 `IN` 查询、管理员数量数据库侧 count。
- 结果：通过，第十七批批量操作与预览查询资源边界均已落地。
- 解决策略：采用“批量请求体 schema 上限 + 用户 ID 解析上限 + N+1 合并为集合查询 + 数据库侧聚合计数”的组合策略，控制后台批量操作的事务长度、数据库往返和缓存失效压力。

## 第十八批问题与修复

### 88. 新增 provider 后全量重建自动授权绑定（已完成）

- 位置：`app/services/provider_service.py`、`app/services/api_key_admin_service.py`
- 问题：`create_provider()` 新增单个 provider 后调用 `sync_auto_provider_bindings()`，会扫描全部自动同步 API Key 并重建绑定。
- 影响：API Key 和 provider 数量增加后，单个 provider 创建会触发不成比例的数据库写入和缓存失效。
- 修复：新增 `attach_provider_to_auto_synced_keys()`，只为自动同步 Key 增补当前 provider 的缺失绑定。

### 89. provider 更新后无必要触发全量自动授权同步（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`update_provider()` 更新 provider 基础信息或模型挂载后仍调用 `sync_auto_provider_bindings()`。
- 影响：名称、容量、健康状态、模型挂载等更新会额外扫描全部自动同步 API Key，拉长管理操作响应。
- 修复：provider 更新后不再触发自动授权全量同步，仅保留模型目录同步与运行时缓存失效。

### 90. provider 删除后重复执行全量自动授权同步（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`delete_provider()` 已删除目标 provider 绑定并失效相关 Key 缓存后，又调用 `sync_auto_provider_bindings()`。
- 影响：删除单个 provider 时会额外重扫所有自动同步 Key，且对已删除 provider 没有必要。
- 修复：删除 provider 后移除全量自动同步调用，仅处理被删除 provider 的受影响 Key。

### 91. provider 批量导入循环触发多轮模型目录与授权同步（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`batch_import_providers()` 循环调用 `create_provider()`，每创建一个 provider 都同步模型目录并执行自动授权同步。
- 影响：一次导入 N 个 provider 时会放大为 N 轮重型同步，导致导入耗时和数据库压力急剧增加。
- 修复：批量导入创建阶段跳过单项模型目录和授权同步，导入完成后统一同步一次模型目录，并一次性为新 provider 增补自动授权绑定。

### 92. provider 批量导入文本和条目数缺少上限（已完成）

- 位置：`app/schemas/provider.py`、`app/services/provider_service.py`
- 问题：批量导入请求的 `content` 和解析后的 provider 条目数没有边界。
- 影响：超大文本或超多条目会增加请求解析、校验、审计日志和导入事务压力。
- 修复：导入文本限制为 200KB；单次最多导入 100 个 provider。

## 第十八批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\schemas\provider.py app\services\provider_service.py app\services\api_key_admin_service.py`
- 结果：通过，provider schema、provider 服务和 API Key 管理服务语法编译成功。
- 已执行：构造 200000/200001 字符的 `ProviderBatchImportRequest.content`。
- 结果：通过，200000 字符可通过，超过上限会被 Pydantic 拒绝。
- 已执行：源码级检查 provider 创建轻量授权、批量导入统一同步、provider 更新/删除不再调用全量自动授权同步。
- 结果：通过，第十八批 provider 写操作同步成本治理已落地。
- 解决策略：采用“新增 provider 增量授权 + 批量导入延后统一同步 + 更新/删除去除无效全量同步 + 导入体积边界”的组合策略，降低 provider 管理操作对 API Key 绑定表、模型目录同步和缓存失效链路的压力。

## 第十九批问题与修复

### 93. 启动初始化重复执行用户共享余额回填（已完成）

- 位置：`app/main.py`
- 影响：每次启动会重复扫描 API Key 余额聚合并尝试更新用户余额，增加启动耗时。
- 修复：移除后续重复调用，保留原迁移分支中的必要回填。

### 94. 启动初始化重复失效模型运行时缓存（已完成）

- 位置：`app/main.py`
- 问题：启动末尾连续调用两次 `ModelCatalogService.invalidate_model_runtime_cache()`。
- 影响：启动阶段产生无意义的重复缓存失效，增加跨 worker 缓存抖动。
- 修复：保留一次模型缓存失效，并继续保留 provider 运行时缓存失效。

### 95. 启动阶段默认执行全量 API Key 自动授权同步（已完成）

- 位置：`app/main.py`、`app/config.py`
- 问题：`init_database()` 默认在启动时调用 `sync_auto_provider_bindings()` 或全量回填所有 Key 到所有 provider。
- 影响：多 worker 或频繁重启时会重复扫描 API Key 与绑定表，拖慢启动并增加数据库写压力。
- 修复：新增 `enable_startup_heavy_sync` 配置，默认关闭；只有显式开启时才执行全量授权同步。

### 96. 启动阶段默认执行旧 provider 模型全表兼容同步（已完成）

- 位置：`app/main.py`
- 问题：每次启动都会调用 `ProviderService.sync_legacy_provider_models(db)` 扫描全部 provider 和模型挂载。
- 影响：历史兼容逻辑在常规启动中持续消耗数据库和内存，规模增长后影响冷启动速度。
- 修复：将该旧兼容同步纳入 `enable_startup_heavy_sync` 显式开关。

### 97. 启动历史账单回填无批次上限（已完成）

- 位置：`app/main.py`、`app/config.py`
- 问题：`_backfill_missing_user_billing_records()` 会一次性从历史 API Key 账单补写全部缺失用户账单。
- 影响：历史账单较多时，启动阶段可能形成大事务和长时间数据库占用。
- 修复：新增 `startup_billing_backfill_batch_size`，默认每次最多回填 5000 条，代码侧硬上限 50000 条。

## 第十九批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\main.py app\config.py`
- 结果：通过，启动主模块与配置模块语法编译成功。
- 已执行：构造默认 `Settings()` 并检查 `enable_startup_heavy_sync` 与 `startup_billing_backfill_batch_size`。
- 结果：通过，重型启动同步默认关闭，账单回填批次默认值为 5000。
- 已执行：源码级检查重复缓存失效、启动重型同步开关、账单回填 `LIMIT :batch_size`。
- 结果：通过，第十九批启动初始化资源边界与重复工作治理已落地。
- 解决策略：采用“启动重活显式开关 + 去重初始化步骤 + 历史回填批次化”的组合策略，降低 Web worker 冷启动和多 worker 重启时的数据库压力与缓存抖动。

## 第二十批问题与修复

### 98. Token finalize 默认为每条日志创建延迟任务（已完成）

- 位置：`app/services/token_usage_service.py`、`app/config.py`
- 问题：每条请求日志 finalize 默认创建一个 250ms 延迟 `asyncio` 任务。
- 影响：高并发下短生命周期延迟任务数量会随请求量放大，增加事件循环调度压力和内存占用。
- 修复：新增 `token_finalize_immediate_delay_ms` 配置，默认改为 0；如需延迟可显式配置且最大 5000ms。

### 99. Token finalize 批处理大小缺少显式配置和硬上限（已完成）

- 位置：`app/services/token_usage_service.py`、`app/config.py`
- 问题：worker 使用未声明的 `token_finalize_batch_size` 隐式配置，且没有代码侧硬上限。
- 影响：误配置过大时，单轮 finalize 可能加载和处理过多日志，形成长耗时批次。
- 修复：在配置中声明 `token_finalize_batch_size=50`，并在服务层限制单批最多 500 条。

### 100. 请求日志队列批次与 ingress 队列大小缺少代码侧硬上限（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：`request_log_queue_batch_size` 和 `request_log_ingress_queue_size` 仅依赖配置值。
- 影响：误配置过大会导致单批 Redis/DB 写入过重或 ingress 内存队列过大。
- 修复：请求日志单批最多 500 条，ingress 队列最多 100000 条。

### 101. 请求日志 ingress worker 取消时可能丢失已取出的批次（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：ingress worker 在拿到一批内存队列项目后，如果取消发生在写 Redis/DB 前，当前批次缺少 finally 兜底。
- 影响：关闭或重启时少量已取出日志可能既不在内存队列中，也未写入 Redis/数据库。
- 修复：为当前批次增加取消兜底，取消时同步写入数据库后再退出。

### 102. 定时任务未显式限制同任务重叠和补跑合并（已完成）

- 位置：`app/tasks.py`、`app/config.py`
- 问题：调度器任务只依赖分布式锁，APScheduler 层没有统一设置 `max_instances`、`coalesce` 和 `misfire_grace_time`；Token 回填固定 15 秒不可调。
- 影响：任务错过执行或执行较慢时，调度器层可能积压触发；Token 回填频率也无法按部署资源下调。
- 修复：所有核心定时任务设置 `max_instances=1`、`coalesce=True` 和合理 `misfire_grace_time`；新增 `token_usage_backfill_interval_seconds`，默认 15 秒且下限 15 秒。

## 第二十批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\config.py app\services\token_usage_service.py app\services\request_log_queue_service.py app\logging\queue.py app\tasks.py`
- 结果：通过，配置、Token finalize、请求日志队列、typed log 队列和调度任务语法编译成功。
- 已执行：构造默认 `Settings()` 并检查 Token finalize 与 backfill 相关默认配置。
- 结果：通过，`token_finalize_batch_size=50`、`token_finalize_immediate_delay_ms=0`、`token_usage_backfill_interval_seconds=15`。
- 已执行：源码级检查队列批次硬上限、ingress 取消兜底、Token finalize 默认无延迟、调度器重叠限制与补跑合并参数。
- 结果：通过，第二十批后台队列和定时任务资源边界均已落地。
- 解决策略：采用“默认直接入队 + 批处理硬上限 + 关闭兜底刷盘 + 调度器防重叠与可配置频率”的组合策略，降低后台队列在高并发和重启场景下的事件循环压力、内存峰值和日志丢失风险。

## 第二十一批问题与修复

### 103. 请求日志筛选选项接口多路 distinct 查询无上限（已完成）

- 位置：`app/services/log_service.py`、`app/routers/logs.py`
- 问题：`/api/logs/filter-options` 会对 provider、模型、API Key、用户、租户、项目、应用、环境等字段执行多个 distinct 查询，且没有返回上限。
- 影响：请求日志增长后，下拉选项刷新会扫描和返回大量唯一值，拖慢日志页首屏。
- 修复：筛选选项增加 `limit` 参数，默认 200、最大 500；各 distinct 查询均按上限返回。

### 104. 请求日志筛选选项缺少缓存（已完成）

- 位置：`app/services/log_service.py`
- 问题：同一筛选作用域下，每次打开日志页都会重复执行多组 distinct 查询。
- 影响：日志页频繁刷新或多用户同时访问时，对请求日志表产生重复读压力。
- 修复：按健康检查过滤、用户作用域、API Key 作用域和 limit 构造缓存 key，缓存 10 秒。

### 105. 用户端日志筛选选项无法限制返回规模（已完成）

- 位置：`app/routers/user_portal.py`、`app/services/user_portal_service.py`
- 问题：用户端 `/api/user/logs/filter-options` 复用日志筛选选项，但没有对外暴露 limit。
- 影响：用户拥有较多 API Key 或历史日志维度较多时，用户端日志页也会返回过多下拉项。
- 修复：用户端筛选选项增加 `limit` 参数，默认 200、最大 500，并传递到统一日志服务。

### 106. 审计日志筛选选项 distinct 查询无上限且无缓存（已完成）

- 位置：`app/services/admin_audit_service.py`
- 问题：审计日志页面筛选项会全量 distinct `action` 和 `entity_type`。
- 影响：审计日志长期增长后，页面打开会重复扫描历史操作类型。
- 修复：审计筛选选项增加默认 200、最大 500 的服务层上限，并缓存 30 秒。

### 107. 类型化事件历史回填每次从全量请求日志中找缺失事件（已完成）

- 位置：`app/services/log_service.py`、`app/routers/logging_api.py`
- 问题：`backfill_typed_events_from_request_logs()` 虽有处理 limit，但候选查询直接面向全量请求日志判断缺失事件。
- 影响：历史日志规模大时，手动回填会在全库范围做缺失事件存在性判断。
- 修复：增加 `scan_limit` 候选扫描窗口，默认扫描最近 2500 条、最大 50000 条，再从中处理指定 limit。

## 第二十一批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\logs.py app\routers\logging_api.py app\routers\user_portal.py app\services\log_service.py app\services\admin_audit_service.py app\services\user_portal_service.py`
- 结果：通过，日志路由、类型化日志路由、用户端日志路由和相关服务语法编译成功。
- 已执行：调用 `LogService.get_filter_options(db, limit=3)` 与 `AdminAuditService.get_filter_options(db, limit=3)`。
- 结果：通过，筛选项返回数量受 limit 限制。
- 已执行：源码级检查日志筛选缓存 key、distinct 查询 limit、用户端 limit 传递、typed event 回填 `scan_limit`。
- 结果：通过，第二十一批日志筛选与回填查询边界均已落地。
- 解决策略：采用“筛选项限量 + 短 TTL 缓存 + 用户端同口径传参 + 历史回填候选窗口”的组合策略，降低日志页面和回填工具对大日志表的重复扫描压力。

## 第二十二批问题与修复

### 108. 后台任务列表仍全量加载历史任务再内存分页（已完成）

- 位置：`app/routers/logging_api.py`
- 问题：`_list_background_jobs()` 一次性读取全部 `BackgroundJobEvent`，再合并 Redis 状态、过滤、排序和分页。
- 影响：后台任务事件长期累积后，任务列表会消耗大量内存并拖慢运维页面响应。
- 修复：改为优先复用数据库侧过滤分页的 `_list_background_jobs_db_only()`，仅额外扫描少量 Redis 运行态并在第一页叠加展示。

### 109. 请求日志时间线详情按事件表无上限加载（已完成）

- 位置：`app/routers/logging_api.py`
- 问题：`build_request_log_timeline_payload()` 对每类子事件都全量读取，且没有总事件数限制。
- 影响：单个请求若产生大量重试、流式或内容防护事件，详情页会一次性加载过多行和大 payload。
- 修复：增加每类事件默认 50 条、总计默认 200 条的限制，并返回 `truncated`、`limits` 和各事件表加载状态。

### 110. 健康检查运行详情全量返回探针事件（已完成）

- 位置：`app/routers/logging_api.py`
- 问题：`get_health_run()` 会把指定 run 的全部 `HealthProbeEvent` 一次性返回。
- 影响：大规模 provider/model 健康检查运行可能产生大量探针，导致详情接口响应过大。
- 修复：增加 `probe_limit` 参数，默认 500、最大 1000，并返回 `probes_truncated` 标记。

### 111. 计费事件列表汇总重复执行多条 count/sum 查询（已完成）

- 位置：`app/routers/logging_api.py`
- 问题：`list_billing_events()` 已经计算列表总数后，`_billing_events_summary()` 又分别执行多条 count/sum 聚合。
- 影响：计费事件列表每次刷新都会对 Token finalize 和 billing 表产生重复聚合压力。
- 修复：列表总数复用到汇总函数；失败数、等待数和余额变动改为按事件表单条聚合查询计算。

### 112. 类型化日志列表每次刷新都重复读取队列状态与保留策略（已完成）

- 位置：`app/routers/logging_api.py`
- 问题：每个类型化日志列表响应都会同步读取 Redis 队列长度，并通过设置服务读取保留策略。
- 影响：多个日志 Tab 轮询或并发刷新时，会产生重复 Redis/数据库读取，影响响应速度。
- 修复：队列状态增加 2 秒短 TTL 缓存，保留策略增加 60 秒缓存，保持信息实时性的同时减少重复读。

## 第二十二批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\logging_api.py`
- 结果：通过，类型化日志路由语法编译成功。
- 已执行：源码级检查时间线 per-model/total 限制、健康探针限制、后台任务 DB 分页、队列状态缓存、保留策略缓存与计费汇总聚合。
- 结果：通过，第 108-112 项关键边界均已落地。
- 已执行：使用本地数据库会话调用 `build_request_log_timeline_payload(db, log, per_model_limit=1, total_limit=3)` 与 `_list_background_jobs(db, page=1, page_size=3, ...)`。
- 结果：通过，时间线返回不超过总限制且标记截断；后台任务列表返回不超过页大小。
- 解决策略：采用“数据库分页优先 + 详情接口显式上限 + 聚合查询合并 + 元信息短 TTL 缓存”的组合策略，降低类型化日志页面在大历史数据和多 Tab 刷新场景下的数据库、Redis 与内存压力。

## 第二十三批问题与修复

### 113. 仪表盘会话数每次刷新执行全量 distinct 计数（已完成）

- 位置：`app/routers/dashboard.py`
- 问题：`build_dashboard_payload()` 每次都直接对 `request_logs.conversation_key` 执行 `count(distinct ...)`。
- 影响：请求日志增长后，首页刷新会产生较重的全表/索引聚合压力。
- 修复：新增 `_dashboard_conversation_count()`，对会话 distinct 计数使用 30 秒缓存。

### 114. 内容防护监控按提供商分组结果无返回上限（已完成）

- 位置：`app/services/system_metrics_service.py`
- 问题：`_content_guard_snapshot()` 的 provider 风险分组和高风险 provider 分组没有 limit。
- 影响：提供商数量或历史事件维度增多时，监控接口可能返回和处理过多分组数据。
- 修复：新增 `PROVIDER_METRIC_LIMIT=200`，对相关 provider 分组按事件数排序并限制返回规模。

### 115. Provider 监控快照一次性加载全部提供商（已完成）

- 位置：`app/services/system_metrics_service.py`
- 问题：`_provider_snapshot()` 默认读取全部 Provider，再逐项拼装容量和流量快照。
- 影响：提供商数量过多时，运维监控刷新会消耗过多数据库、Redis 和 Python 内存。
- 修复：Provider 监控快照按 `PROVIDER_METRIC_LIMIT=200` 限制默认采集规模。

### 116. 项目进程兜底扫描可能遍历系统全部进程（已完成）

- 位置：`app/services/system_metrics_service.py`
- 问题：`_related_project_processes()` 在父子进程识别后，还会通过 `psutil.process_iter()` 兜底遍历全部进程且无扫描上限。
- 影响：宿主机进程较多时，监控接口会产生不可控的系统调用开销。
- 修复：新增 `PROCESS_SCAN_LIMIT=500`，兜底扫描达到上限后停止。

### 117. 告警刷新一次性加载全部未解决告警（已完成）

- 位置：`app/services/alert_service.py`、`app/services/system_metrics_service.py`
- 问题：告警 upsert 和监控告警写入会把全部未解决事件一次性读入内存再逐项判断。
- 影响：告警历史积累或异常风暴后，每次刷新告警面板都会放大数据库读取和内存占用。
- 修复：改为仅精确读取当前活跃告警键，失活告警按 500 条批次标记 resolved。

## 第二十三批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\dashboard.py app\services\system_metrics_service.py app\services\alert_service.py`
- 结果：通过，仪表盘、系统监控和告警服务语法编译成功。
- 已执行：源码级检查会话计数缓存、provider 监控上限、进程扫描上限、监控告警分批解决和告警中心分批解决。
- 结果：通过，第 113-117 项资源边界均已落地。
- 已执行：本地数据库会话调用 `_dashboard_conversation_count(db)` 与 `SystemMetricsService.collect(db, window_minutes=1, refresh_alerts=False)`。
- 结果：通过，会话数返回整数，监控采集正常返回状态与分区错误结构。
- 解决策略：采用“高成本聚合缓存 + provider 监控限量 + 系统进程扫描上限 + 告警分批状态迁移”的组合策略，降低首页、监控页和告警页在历史数据增长后的刷新成本。

## 第二十四批问题与修复

### 118. 真实并发压测单轮请求数可被参数组合放大（已完成）

- 位置：`app/routers/benchmark.py`、`scripts/real_concurrency_benchmark.py`
- 问题：`concurrency * requests_per_concurrency` 可超过 `sample_requests` 上限，单轮真实请求量没有独立硬上限。
- 影响：误配置高并发与高倍数时，一轮压测可能生成过多外部请求，冲击代理、Redis、数据库和上游。
- 修复：新增 `MAX_BENCHMARK_STAGE_REQUESTS=10000`；API 层拒绝超限参数，脚本层再次把单轮请求数截断到上限。

### 119. 压测脚本 HTTP 连接池随并发线性放大且无硬上限（已完成）

- 位置：`scripts/real_concurrency_benchmark.py`
- 问题：`httpx.Limits(max_connections=max(concurrency * 2, 200))` 没有上限。
- 影响：当 probe 最大并发较高时，客户端连接池可能占用过多文件描述符和内存。
- 修复：新增 `MAX_HTTP_CONNECTIONS=2000` 与 `MAX_HTTP_KEEPALIVE_CONNECTIONS=1000`，连接池配置按硬上限截断。

### 120. 压测任务启动存在并发竞态窗口（已完成）

- 位置：`app/routers/benchmark.py`
- 问题：启动接口在检查“无运行任务”后才创建子进程并写入 `_jobs`，并发请求可能同时通过检查。
- 影响：可能同时启动多个真实压测子进程，造成资源抢占和压测结果互相干扰。
- 修复：先在 `_jobs` 中写入 `starting` 占位，并把 `starting/running` 都视为活跃任务；子进程启动失败时移除占位。

### 121. 停止压测只 terminate 不兜底 kill（已完成）

- 位置：`app/routers/benchmark.py`
- 问题：停止接口只调用 `process.terminate()`，没有等待和强制清理兜底。
- 影响：子进程不响应 terminate 时可能继续占用请求连接、CPU 和文件句柄。
- 修复：新增 `_terminate_process_with_escalation()`，先 terminate 并等待 5 秒，超时后 kill 并再次等待。

### 122. 手动全量健康检查跨提供商并行缺少全局闸门（已完成）

- 位置：`app/services/health_service.py`
- 问题：`check_all()` 每个阶段会对所有提供商直接 `asyncio.gather`，每个提供商内部又有模型级并行上限。
- 影响：提供商数量较多时，总并发会变成“提供商数 × 每提供商探针并发”，瞬时上游请求和本地连接数过高。
- 修复：新增 `MAX_GLOBAL_PHASE_PROVIDERS=4`，每个阶段最多同时运行 4 个提供商的探针组。

## 第二十四批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\benchmark.py app\services\health_service.py scripts\real_concurrency_benchmark.py`
- 结果：通过，压测路由、健康检查服务和真实并发脚本语法编译成功。
- 已执行：构造合法与超限 `RealConcurrencyBenchmarkRequest`。
- 结果：通过，合法参数通过，超出单轮请求数上限的组合被 Pydantic 校验拒绝。
- 已执行：动态导入 `scripts/real_concurrency_benchmark.py` 检查脚本层 `MAX_BENCHMARK_STAGE_REQUESTS`、HTTP 连接池上限和参数归一化。
- 结果：通过，脚本层资源边界常量和归一化逻辑可用。
- 已执行：运行时检查 `HealthService.MAX_GLOBAL_PHASE_PROVIDERS == 4`。
- 结果：通过，手动健康检查跨提供商阶段并行闸门已配置。
- 解决策略：采用“API 层拒绝 + 脚本层兜底 + 子进程生命周期收口 + 健康检查全局并行闸门”的组合策略，降低压测和手动健康检查对本机、代理链路与上游的冲击。

## 第二十五批问题与修复

### 123. API Key 鉴权 L1 本地缓存无容量上限（已完成）

- 位置：`app/services/api_key_auth_cache.py`、`app/config.py`
- 问题：`ApiKeyAuthCache._local_cache` 仅依赖 TTL 过期，没有最大条目数。
- 影响：长时间多 API Key 流量或攻击性随机 Key 访问会导致 worker 进程内存持续膨胀。
- 修复：新增 `api_key_auth_l1_max_entries`，默认 10000、代码侧最大 100000；写入本地缓存前清理过期项，写入后按最早过期项淘汰。

### 124. 按用户失效 API Key 鉴权缓存扫描无上限（已完成）

- 位置：`app/services/api_key_auth_cache.py`、`app/config.py`
- 问题：`async_invalidate_user()` 对 Redis set 执行 `sscan_iter`，没有最大扫描数量。
- 影响：用户关联 API Key 异常膨胀或 set 污染时，单次用户更新可能长时间占用 Redis 与事件循环。
- 修复：新增 `api_key_auth_user_invalidate_scan_limit`，默认 5000、代码侧最大 100000，扫描达到上限后停止并删除用户映射 set。

### 125. 通用缓存前缀失效可能扫描过多 Redis Key（已完成）

- 位置：`app/services/cache_service.py`
- 问题：`CacheService._redis_invalidate_prefix()` 会持续扫描匹配前缀直到结束，缺少代码侧上限。
- 影响：大前缀或误用前缀失效时，会长时间占用 Redis 连接并阻塞调用方。
- 修复：新增 `REDIS_INVALIDATE_SCAN_LIMIT=10000`，前缀失效扫描达到上限后停止。

### 126. 上游连接池配置缺少代码侧硬上限（已完成）

- 位置：`app/config.py`
- 问题：`upstream_max_connections` 与 `upstream_max_keepalive_connections` 完全依赖配置值。
- 影响：误配置过大时，每个 worker 都可能创建过多连接额度，占用文件描述符和内存。
- 修复：启动配置归一化时将总连接限制在 10-5000，keepalive 限制在 1 到总连接数之间。

### 127. aiohttp 每主机连接数等于全局总连接数（已完成）

- 位置：`app/services/upstream_client.py`
- 问题：`aiohttp.TCPConnector(limit_per_host=settings.upstream_max_connections)` 允许单个上游占满全局连接池。
- 影响：一个慢或异常上游可能挤占连接资源，影响其它 provider 的请求调度。
- 修复：`limit_per_host` 改为 `min(upstream_max_keepalive_connections, upstream_max_connections)`，用更保守的每主机上限隔离单 provider 连接占用。

## 第二十五批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\config.py app\services\api_key_auth_cache.py app\services\cache_service.py app\services\upstream_client.py`
- 结果：通过，配置、鉴权缓存、通用缓存和上游客户端模块语法编译成功。
- 已执行：构造超大 `Settings(...)` 检查上游连接池、鉴权 L1 容量和用户失效扫描上限归一化。
- 结果：通过，上游总连接最大 5000、keepalive 不超过总连接，鉴权 L1 与用户失效扫描最大 100000。
- 已执行：源码级检查鉴权 L1 淘汰、用户失效扫描上限、Redis 前缀失效扫描上限和 aiohttp `limit_per_host` 约束。
- 结果：通过，第 123-127 项缓存与连接池资源边界均已落地。
- 解决策略：采用“缓存容量上限 + 扫描上限 + 配置归一化 + 单上游连接隔离”的组合策略，降低认证热路径、缓存失效和上游连接池在异常规模下的资源失控风险。

## 第二十六批问题与修复

### 128. 模型选项接口全量序列化模型目录（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：`list_model_option_dicts()` 会加载并序列化全部模型目录和提供商绑定。
- 影响：模型数量持续增长后，下拉选项接口会返回过多数据并拖慢页面初始化。
- 修复：新增 `MODEL_OPTIONS_LIMIT=500`，模型选项列表默认最多序列化 500 个模型。

### 129. 模型健康状态筛选会全量序列化后内存过滤（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：带 `health_status` 筛选时，列表页会加载所有匹配目录、序列化全部 provider 绑定，再在内存中过滤健康状态。
- 影响：模型目录较多时，健康筛选页会产生较重 CPU 和内存开销。
- 修复：新增 `MODEL_HEALTH_FILTER_SCAN_LIMIT=1000`，健康筛选默认只扫描有限候选窗口，避免无边界全量序列化。

### 130. 模型列表摘要每次全量序列化目录（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：`model_summary()` 每次都加载全部模型和提供商并完整序列化后再汇总。
- 影响：模型管理页分页查询也会重复计算全局摘要，增加数据库与 Python 计算压力。
- 修复：为模型摘要增加 `model-catalog-summary:v2` 缓存，并在模型运行时缓存失效时同步失效摘要缓存。

### 131. 批量更新模型上下文窗口后重新全量列出模型（已完成）

- 位置：`app/routers/models.py`
- 问题：批量更新完成后通过 `list_model_dicts(db)` 全量序列化模型目录，再筛出刚更新的模型。
- 影响：少量模型批量更新也会触发全量模型和 provider 绑定序列化。
- 修复：改为只复用 provider 列表并序列化本次更新的 `catalogs`。

### 132. 模型映射历史规范化一次性读取全部映射（已完成）

- 位置：`app/services/model_mapping_service.py`
- 问题：`normalize_legacy_mapping_data()` 直接 `select(ModelMapping)` 全量读取所有映射。
- 影响：历史映射数量较多时，启动或维护任务会产生不必要的内存峰值。
- 修复：新增 `NORMALIZE_BATCH_SIZE=500`，按 ID 分批扫描、规范化并提交。

## 第二十六批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\model_catalog_service.py app\services\model_mapping_service.py app\routers\models.py`
- 结果：通过，模型目录服务、模型映射服务和模型路由语法编译成功。
- 已执行：源码级检查模型选项上限、健康筛选扫描窗口、摘要缓存、批量更新局部序列化和映射规范化分批扫描。
- 结果：通过，第 128-132 项关键边界均已落地。
- 已执行：本地数据库会话调用 `list_model_page()`、`list_model_option_dicts()`、`model_summary()` 与 `normalize_legacy_mapping_data()`。
- 结果：原始完整验证受当前数据库缺少 `providers.circuit_opened_at` 既有迁移状态阻断；在隔离 Provider 查询后，上述函数均可执行，分页和选项上限生效。
- 解决策略：采用“选项限量 + 健康筛选候选窗口 + 摘要缓存 + 局部返回序列化 + 历史规范化分批”的组合策略，降低模型管理页和维护任务在模型目录增长后的响应与内存压力。

## 第二十七批问题与修复

### 133. 容量默认值维护脚本失效鉴权缓存时全量收集 Redis Key（已完成）

- 位置：`scripts/apply_capacity_defaults.py`
- 问题：`_invalidate_auth_cache_keys()` 对每个鉴权缓存前缀执行 `list(client.scan_iter(...))`，会先把全部匹配 Key 拉入内存。
- 影响：Redis 中鉴权缓存 Key 较多或前缀被污染时，维护脚本可能产生内存峰值并长时间占用 Redis 连接。
- 修复：新增扫描上限 `AUTH_CACHE_INVALIDATE_SCAN_LIMIT=10000` 与删除批次 `AUTH_CACHE_INVALIDATE_DELETE_BATCH=500`，边扫描边分批删除。

### 134. 内容防护开销压测迭代次数缺少硬上限（已完成）

- 位置：`scripts/benchmark_content_guard_overhead.py`
- 问题：`--iterations` 仅校验最小值，没有最大值。
- 影响：误传极大迭代次数时，单次脚本会长时间占满 CPU，阻塞本机验证或 CI。
- 修复：新增 `MAX_ITERATIONS=20000`，超过上限时直接拒绝运行。

### 135. 内容防护开销压测并发和扫描字节数缺少硬上限（已完成）

- 位置：`scripts/benchmark_content_guard_overhead.py`
- 问题：`--concurrency` 只有最小值校验，`--scan-bytes` 没有边界校验。
- 影响：误传高并发或大扫描文本时，会放大线程池、内存和正则检测 CPU 开销。
- 修复：新增 `MAX_CONCURRENCY=256` 与 `MAX_SCAN_BYTES=262144`，对并发和单样本扫描文本建立脚本级上限。

### 136. 本地代理压测脚本清理历史数据时单事务批量删除（已完成）

- 位置：`scripts/benchmark_proxy.py`
- 问题：压测夹具准备阶段会对专用 API Key 的请求日志与账单记录直接执行无上限 `delete`。
- 影响：历史压测日志积累较多时，单事务删除可能持有大锁、产生大事务并影响数据库响应。
- 修复：新增 `delete_benchmark_rows_in_batches()`，按 1000 条 ID 分批删除请求日志和账单记录。

### 137. 真实上游功能测试脚本存在无边界清理和模型列表收集（已完成）

- 位置：`scripts/real_upstream_functional_test.py`
- 问题：夹具准备阶段同样单事务删除全部历史日志/账单记录，且查询上游模型后会无上限收集全部模型 ID。
- 影响：历史验证数据或上游模型列表较大时，脚本会产生不必要的数据库锁占用和内存开销。
- 修复：新增 `delete_functional_rows_in_batches()` 按 1000 条分批清理；上游模型列表最多收集 `MAX_UPSTREAM_MODEL_OPTIONS=1000` 个 ID。

## 第二十七批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall scripts\apply_capacity_defaults.py scripts\benchmark_content_guard_overhead.py scripts\benchmark_proxy.py scripts\real_upstream_functional_test.py`
- 结果：通过，容量默认值脚本、内容防护压测脚本、本地代理压测脚本和真实上游功能测试脚本语法编译成功。
- 已执行：动态导入脚本并检查 Redis 扫描上限、压测参数上限、批量删除批次和上游模型列表收集上限。
- 结果：通过，第 133-137 项脚本级资源边界均已落地。
- 解决策略：采用“扫描上限 + 分批删除 + 压测参数拒绝 + 外部列表收集上限”的组合策略，降低维护脚本和验证脚本在误配置、大历史数据或外部返回过大时的资源占用与稳定性风险。

## 第二十八批问题与修复

### 138. 自动同步 API Key 提供商绑定一次性加载全部 Key（已完成）

- 位置：`app/services/api_key_admin_service.py`
- 问题：`sync_auto_provider_bindings()` 会一次性读取所有开启自动同步的 API Key，并统一提交。
- 影响：API Key 数量较多时，维护任务会产生较大 ORM 内存占用和长事务。
- 修复：新增 `AUTO_PROVIDER_BINDING_BATCH_SIZE=200` 与 `_iter_api_key_id_batches()`，自动同步按 ID 分批加载、提交和失效缓存。

### 139. 新增提供商时向所有自动同步 Key 绑定缺少分批处理（已完成）

- 位置：`app/services/api_key_admin_service.py`
- 问题：`attach_providers_to_auto_synced_keys()` 一次性读取所有自动同步 Key，并一次性查询全部已存在绑定对。
- 影响：新增提供商或批量补绑定时，Key 数量越多，内存集合和数据库事务越大。
- 修复：改为按 200 个 API Key 一批查询已有绑定、插入缺失绑定并提交。

### 140. 全量授权所有 API Key 到所有提供商一次性加载全部 Key（已完成）

- 位置：`app/services/api_key_admin_service.py`
- 问题：`batch_authorize_all_providers()` 会读取全部 API Key 并在单事务内替换所有绑定。
- 影响：管理员全量授权操作可能造成大事务、长锁和鉴权缓存集中失效。
- 修复：复用 API Key ID 分批加载工具，按批设置自动同步、更新绑定、提交并失效缓存。

### 141. API Key 到所有提供商的历史回填一次性处理全表（已完成）

- 位置：`app/services/api_key_admin_service.py`
- 问题：`backfill_all_api_keys_to_all_providers()` 一次性加载全部 API Key 和 provider 绑定。
- 影响：迁移或维护任务在数据量增长后可能出现 ORM 内存峰值和数据库长事务。
- 修复：改为按 200 个 API Key 分批回填、提交和缓存失效。

### 142. 用户门户概览与自检页面存在重复重型查询（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：用户概览每次刷新都执行会话 `count(distinct ...)`；自检页面先为全部 API Key 计算模型选项，再覆盖为当前 Key 的模型。
- 影响：用户 API Key 和日志规模增长后，普通页面刷新会放大数据库聚合、路由候选计算和 Python 去重开销。
- 修复：用户会话数增加 30 秒缓存；自检页面仅计算当前选中 API Key 的模型选项，并限制最多 500 个模型。

## 第二十八批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\api_key_admin_service.py app\services\user_portal_service.py`
- 结果：通过，API Key 管理服务和用户门户服务语法编译成功。
- 已执行：源码级检查 API Key 自动绑定分批工具、全量授权/回填分批调用、用户会话数缓存和自检模型选项上限。
- 结果：通过，第 138-142 项服务层资源边界均已落地。
- 解决策略：采用“ID 分页批处理 + 小批提交 + 分批缓存失效 + 页面聚合短 TTL 缓存 + 当前对象局部计算”的组合策略，降低 API Key 授权维护与用户门户普通刷新在数据增长后的长事务、内存峰值和响应延迟。

## 第二十九批问题与修复

### 143. IP 管理热路径每个请求重复查询启用规则（已完成）

- 位置：`app/services/ip_management_service.py`
- 问题：`evaluate_request()` 在规则引擎开启时每次请求都会调用 `enabled_rules()` 查询全部启用 IP 规则。
- 影响：对外 `/v1/*` 高频请求开启 IP 管理后，会把规则读取压力叠加到每个请求热路径。
- 修复：为启用规则增加 5 秒轻量缓存，缓存内容使用 `SimpleNamespace` 快照，避免跨 Session 复用 ORM 实例。

### 144. IP 规则变更后缺少规则缓存失效（已完成）

- 位置：`app/services/ip_management_service.py`、`app/routers/ip_management.py`
- 问题：新增、更新、删除、启用和停用规则后没有统一失效规则读取缓存。
- 影响：规则缓存引入后若不主动失效，会导致拦截和限流策略短时间使用旧规则。
- 修复：新增 `invalidate_rule_cache()`，并在规则创建、更新、删除、启停后调用。

### 145. IP 管理概览重复执行多条 count 查询（已完成）

- 位置：`app/services/ip_management_service.py`
- 问题：`build_overview()` 对事件总数、近 1 小时阻断、限流、可信代理命中、无效头忽略、规则总数和启用规则数分别执行查询。
- 影响：运维页面刷新时会产生多次数据库往返和重复扫描。
- 修复：事件侧合并为一条条件聚合查询，规则侧合并为一条条件聚合查询。

### 146. IP 管理事件列表无筛选时默认扫描全历史（已完成）

- 位置：`app/routers/ip_management.py`
- 问题：`/api/ip-management/events` 未传筛选条件时直接对全历史事件执行 count 和分页排序。
- 影响：IP 事件积累后，默认打开页面就可能触发重型全历史查询。
- 修复：无筛选且未指定时间范围时默认限制为近 7 天；显式筛选或显式时间范围仍按用户条件查询。

### 147. 内容防护运行事件导出加载完整 ORM 对象（已完成）

- 位置：`app/routers/content_guard.py`
- 问题：`export_content_guard_runtime_events()` 使用 `select(RequestContentGuardEvent, RequestLog)` 导出，加载两个 ORM 对象的全部列。
- 影响：CSV 导出最多 5000 行时，会额外加载请求日志大字段，增加数据库传输和 Python 内存占用。
- 修复：新增 `_runtime_event_export_query()`，导出仅选择 CSV 所需列。

## 第二十九批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\ip_management_service.py app\routers\ip_management.py app\routers\content_guard.py`
- 结果：通过，IP 管理服务、IP 管理路由和内容防护路由语法编译成功。
- 已执行：源码级检查 IP 规则缓存与失效、IP 概览条件聚合、IP 事件默认 7 天窗口和内容防护导出列级查询。
- 结果：通过，第 143-147 项资源边界均已落地。
- 解决策略：采用“热路径短 TTL 缓存 + 写操作主动失效 + 条件聚合合并 + 默认时间窗口 + 导出列裁剪”的组合策略，降低 IP 管理和内容防护运维页面在高频请求与历史事件增长后的数据库压力和内存占用。

## 第三十批问题与修复

### 148. 内容防护运行事件列表无筛选时默认扫描全历史（已完成）

- 位置：`app/routers/content_guard.py`
- 问题：`/api/content-guard/runtime/events` 和导出接口未传筛选条件时，会直接查询全历史内容防护事件。
- 影响：事件积累后，默认打开页面或导出会触发全历史 count、排序和读取。
- 修复：新增默认近 7 天窗口；显式筛选或显式时间范围仍按用户条件查询。

### 149. 内容防护概览 Provider 状态摘要重复执行多条 count 查询（已完成）

- 位置：`app/services/content_guard_module_service.py`
- 问题：`build_overview()` 与 `build_summary()` 分别对启用 provider、阻断、低信任、待复核状态执行多条 count。
- 影响：内容防护概览和预先防护状态刷新会产生重复数据库往返。
- 修复：新增 `_provider_summary_counts()`，用单条条件聚合查询返回 provider 摘要。

### 150. 内容防护规则列表重复解析规则配置（已完成）

- 位置：`app/services/content_guard_module_service.py`
- 问题：`list_rules()` 会序列化规则列表、计算配置状态和生成分类时重复解析同一份规则 JSON。
- 影响：规则数量增加或含正则校验时，规则页面刷新会重复消耗 CPU。
- 修复：`list_rules()` 只解析一次规则；配置状态复用已解析规则，分类继续基于过滤前规则全集生成。

### 151. 定时 L1/L2 健康检查 Provider 候选无扫描上限（已完成）

- 位置：`app/services/health_service.py`
- 问题：`check_scheduled_text_models()` 与 `check_scheduled_capability_models()` 会先加载全部启用 provider 再筛选模型。
- 影响：provider 规模异常增大时，定时健康检查会在进入模型预算前先产生全量 provider/模型加载开销。
- 修复：新增 `SCHEDULED_PROVIDER_SCAN_LIMIT=200` 与 `_scheduled_enabled_providers()`；自动定时任务按优先级最多扫描 200 个 provider，手动批量检查保留全量。

### 152. 定时内容完整性健康检查 Provider 候选无扫描上限（已完成）

- 位置：`app/services/health_service.py`
- 问题：`check_scheduled_content_integrity_models()` 虽有模型预算，但 provider 候选仍先全量加载。
- 影响：大量 provider 时，内容防护预先检测会在模型预算之外产生不必要的 ORM 加载与排序成本。
- 修复：内容完整性定时检查复用 `_scheduled_enabled_providers(content_guard_only=True)`，自动任务最多扫描 200 个启用内容防护的 provider。

## 第三十批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\content_guard.py app\services\content_guard_module_service.py app\services\health_service.py`
- 结果：通过，内容防护路由、内容防护模块服务和健康检查服务语法编译成功。
- 已执行：源码级检查内容防护默认 7 天事件窗口、provider 摘要条件聚合、规则列表单次解析、计划健康检查 provider 扫描上限和内容完整性 provider 过滤。
- 结果：通过，第 148-152 项内容防护与计划健康检查边界均已落地。
- 解决策略：采用“默认时间窗口 + 条件聚合 + 配置解析复用 + 自动任务 provider 扫描上限”的组合策略，降低内容防护页面、预先检测和计划健康检查在事件、规则与 provider 规模增长后的数据库和 CPU 压力。

## 第三十一批问题与修复

### 153. Responses→Chat 内存会话缓存无容量上限（已完成）

- 位置：`app/services/responses_chat_adapter_service.py`
- 问题：内存存储模式下 `_memory_sessions` 仅在读取过期会话时被动删除，没有最大条目数。
- 影响：长时间大量 response 会话会导致 worker 进程内存持续增长。
- 修复：新增 `MEMORY_SESSION_MAX_ENTRIES=10000`，保存前清理过期会话，超过上限时按插入顺序淘汰最早会话。

### 154. Responses→Chat 环境上游配置解析无数量上限（已完成）

- 位置：`app/services/responses_chat_adapter_service.py`
- 问题：`_env_upstream_specs()` 会从 JSON 配置和模型映射中持续追加上游规格，没有最大数量。
- 影响：误配置大映射时，启动同步可能创建和维护过多 provider/model 夹具。
- 修复：新增 `ENV_UPSTREAM_SPEC_LIMIT=500`，解析到上限后停止追加。

### 155. Responses→Chat 数据库会话清理单次运行无最大批次数（已完成）

- 位置：`app/services/responses_chat_adapter_service.py`
- 问题：`cleanup_expired_database_sessions()` 虽按 5000 条分批删除，但会一直删除到没有过期记录。
- 影响：过期会话积压较多时，单次定时任务可能长时间占用数据库。
- 修复：新增 `DATABASE_CLEANUP_MAX_BATCHES=100`，单次最多处理 100 批，剩余记录留给下一轮定时任务。

### 156. 数据保留清理单表单次运行无最大批次数（已完成）

- 位置：`app/services/data_retention_service.py`
- 问题：`_delete_batched()` 对每张表会持续批量删除到没有候选记录。
- 影响：历史日志长期未清理后，单次保留任务可能运行过久并持续占用数据库写压力。
- 修复：新增 `CLEANUP_MAX_BATCHES_PER_MODEL=100`，每张表单次最多删除 100 批。

### 157. 日志过滤选项缓存 Key 拼接完整 API Key ID 列表（已完成）

- 位置：`app/services/log_service.py`
- 问题：`get_filter_options()` 会把用户作用域内所有 API Key ID 直接拼入缓存 Key。
- 影响：用户拥有大量 API Key 时，缓存 Key 过长，增加缓存存储、传输和日志可读性负担。
- 修复：改为对排序后的 API Key ID 列表计算 SHA256 摘要，仅保留 16 位摘要进入缓存 Key。

## 第三十一批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\responses_chat_adapter_service.py app\services\data_retention_service.py app\services\log_service.py`
- 结果：通过，Responses→Chat 适配器、数据保留服务和日志服务语法编译成功。
- 已执行：源码级检查内存会话容量上限、过期会话主动清理、环境上游配置数量上限、数据库清理批次数上限、数据保留批次数上限和日志过滤缓存 Key 哈希。
- 结果：通过，第 153-157 项后台清理与缓存边界均已落地。
- 解决策略：采用“内存缓存容量上限 + 解析数量上限 + 单次清理最大批次数 + 缓存 Key 摘要化”的组合策略，降低适配器内存模式、过期会话清理、数据保留任务和日志过滤选项在异常规模下的资源占用。

## 第三十二批问题与修复

### 158. 模型目录限制查询锁字典无容量上限（已完成）

- 位置：`app/services/proxy_service.py`
- 问题：`_model_catalog_limit_locks` 会按模型名缓存锁对象，没有最大条目数。
- 影响：大量随机模型名请求会让 worker 进程内锁字典持续增长。
- 修复：新增 `MODEL_CATALOG_LIMIT_LOCK_LIMIT=2048`，达到上限时清空锁字典。

### 159. 路由候选缓存 Key 拼接完整策略列表（已完成）

- 位置：`app/services/router_service.py`
- 问题：`_build_candidate_cache_key()` 会把授权 provider、偏好 provider、区域标签完整拼入缓存 Key。
- 影响：API Key 授权范围较大时，缓存 Key 过长并增加缓存存储和传输成本。
- 修复：新增 `_cache_list_digest()`，策略列表改为 SHA256 16 位摘要。

### 160. 路由容量快照对小授权范围仍可能读取全量 provider（已完成）

- 位置：`app/services/router_service.py`
- 问题：候选加载时会对所有 runtime provider 获取容量快照，即使当前 API Key 只授权少量 provider。
- 影响：授权范围较小的请求仍承担全量 provider 容量快照读取成本。
- 修复：容量快照 provider 集合提前按启用、熔断、维护、授权和端点能力做 provider 级过滤。

### 161. 路由原始候选在最终裁剪前无数量上限（已完成）

- 位置：`app/services/router_service.py`
- 问题：`_load_available_candidates_uncached()` 会先构造所有 provider/model 候选，再在后续排序阶段裁剪。
- 影响：provider/model 挂载异常增长时，单次路由候选构造会产生过多 Python 对象和健康状态读取。
- 修复：新增 `RAW_CANDIDATE_LIMIT=5000`，达到上限后停止继续构造并缓存当前候选窗口。

### 162. 无限路由重试模式下 trace 可持续增长（已完成）

- 位置：`app/services/proxy_service.py`
- 问题：`_append_route_exhausted_retry_trace()` 每轮等待重试都会追加 trace，开启无限重试时没有长度上限。
- 影响：长时间等待路由恢复的请求会持续增加内存占用和最终日志体积。
- 修复：新增 `ROUTE_RETRY_TRACE_LIMIT=100`，追加前保留最近 100 条 trace。

## 第三十二批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\proxy_service.py app\services\router_service.py`
- 结果：通过，代理服务和路由服务语法编译成功。
- 已执行：源码级检查模型目录限制锁上限、路由策略列表摘要化、容量快照 provider 预过滤、原始候选上限和路由重试 trace 上限。
- 结果：通过，第 158-162 项代理主链路与路由边界均已落地。
- 解决策略：采用“锁缓存容量上限 + 策略列表哈希摘要 + provider 级预过滤 + 原始候选窗口上限 + trace 长度上限”的组合策略，降低代理热路径在随机模型、大授权范围、大 provider/model 挂载和长时间重试场景下的内存与缓存压力。

## 第三十三批问题与修复

### 163. 类型化日志 processing 队列启动恢复无批次数上限（已完成）

- 位置：`app/logging/queue.py`
- 问题：`_recover_processing_items()` 会把 Redis processing 队列一直搬回待处理队列，直到全部恢复完成。
- 影响：processing 队列异常积压时，typed log worker 启动会长时间占用 Redis 和事件循环，拖慢应用冷启动或后台 worker 恢复。
- 修复：新增 `RECOVERY_MAX_BATCHES=100`，单次启动最多恢复 100 批，剩余 processing 项留待后续重启或人工排查，并记录达到上限的告警日志。

### 164. 请求日志 processing 队列启动恢复无批次数上限（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：`_recover_processing_items()` 同样会在启动时无上限搬回请求日志 processing 队列。
- 影响：请求日志积压较多时，独立后台 worker 或 Web worker 启动可能被历史 processing 项拖住，影响正式日志消费恢复速度。
- 修复：新增 `RECOVERY_MAX_BATCHES=100`，请求日志单次启动恢复最多处理 100 批，超过后写入告警并让下一轮处理剩余项。

### 165. 类型化日志 worker 失败信封复制过多 processing 原始 payload（已完成）

- 位置：`app/logging/queue.py`
- 问题：`_record_worker_failure()` 每次失败都会把最多 1000 条 processing 原始日志 payload 塞进 dead letter 信封。
- 影响：当单条日志 payload 较大或连续失败时，dead letter 队列会快速膨胀，增加 Redis 内存占用和故障记录写入耗时。
- 修复：新增 `DEAD_LETTER_ITEM_LIMIT=100`，dead letter 仅保留前 100 条样本，同时记录 processing 总数、样本上限和截断标记。

### 166. IP 管理中间件对无关路径和新鲜关闭缓存仍打开数据库会话（已完成）

- 位置：`app/middleware/ip_management_middleware.py`、`app/services/ip_management_service.py`
- 问题：中间件进入后先创建 `SessionLocal()`，再由服务层判断 IP 管理是否启用和当前路径是否属于受控作用域。
- 影响：静态资源、登录页、文档页等不受 IP 管理作用域控制的请求也会产生数据库会话；IP 管理全局关闭时，缓存有效期内仍会重复开会话。
- 修复：中间件先设置 trace 并解析作用域；无作用域直接放行；若存在新鲜设置缓存且全局关闭或该作用域关闭，也直接放行，不再打开数据库会话。

### 167. IP 管理历史事件清理单次运行无最大批次数（已完成）

- 位置：`app/services/ip_management_event_service.py`
- 问题：`cleanup_old_events()` 虽按 5000 条分批删除，但会持续删除到没有过期事件。
- 影响：长期积压历史事件后，手动清理或定时清理可能长时间占用数据库写资源。
- 修复：新增 `CLEANUP_MAX_BATCHES=100`，单次最多删除 100 批，剩余历史事件留给下一轮清理任务。

## 第三十三批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\logging\queue.py app\services\request_log_queue_service.py app\middleware\ip_management_middleware.py app\services\ip_management_service.py app\services\ip_management_event_service.py`
- 结果：通过，typed log 队列、请求日志队列、IP 管理中间件、IP 管理服务和 IP 管理事件服务语法编译成功。
- 已执行：源码级检查 typed log/request log processing 恢复批次上限、dead letter 样本上限、IP 中间件作用域与新鲜关闭缓存短路、IP 事件清理批次数上限，以及第 163-167 项文档完成标记。
- 结果：通过，第 163-167 项后台队列恢复、故障信封、IP 管理热路径和历史事件清理资源边界均已落地。
- 解决策略：采用“启动恢复最大批次数 + dead letter 样本截断 + 请求作用域短路 + 关闭缓存免开会话 + 历史清理最大批次数”的组合策略，降低后台队列异常积压、worker 连续失败、无关请求进入 IP 中间件和历史事件积压场景下的启动延迟、Redis 内存与数据库压力。

## 第三十四批问题与修复

### 168. 告警中心失活告警恢复无单次批次数上限（已完成）

- 位置：`app/services/alert_service.py`
- 问题：`_resolve_inactive_alert_events()` 每次应用告警状态时，会持续把所有非当前活跃的告警标记为 resolved。
- 影响：历史未恢复告警大量积压时，一次告警刷新可能长时间扫描和更新告警表。
- 修复：新增 `ALERT_RESOLVE_MAX_BATCHES=100`，单次最多恢复 100 批失活告警。

### 169. 监控告警失活恢复无单次批次数上限（已完成）

- 位置：`app/services/system_metrics_service.py`
- 问题：`_resolve_stale_monitoring_alerts()` 会持续恢复所有 monitoring 前缀的过期告警，直到没有候选。
- 影响：监控采集或刷新告警状态时，历史监控告警积压会放大单次数据库写入时间。
- 修复：新增 `MONITORING_ALERT_RESOLVE_MAX_BATCHES=100`，监控告警单次最多恢复 100 批。

### 170. 清空请求日志分批删除缺少单次最大批次数（已完成）

- 位置：`app/services/log_service.py`
- 问题：`clear_logs()` 已按 5000 条分批删除，但会持续删到表空。
- 影响：日志表非常大时，一次管理端清空操作仍可能长期占用数据库写资源。
- 修复：新增 `CLEAR_LOGS_MAX_BATCHES=100`，单次最多删除 100 批，管理员可再次触发继续清理剩余日志。

### 171. 模型映射历史规范化分批扫描缺少单次最大批次数（已完成）

- 位置：`app/services/model_mapping_service.py`
- 问题：`normalize_legacy_mapping_data()` 已按 ID 分批读取，但会持续扫描到所有映射完成。
- 影响：历史映射异常增长时，启动兼容迁移或维护调用可能长时间占用数据库会话。
- 修复：新增 `NORMALIZE_MAX_BATCHES=100`，单次最多规范化 100 批映射。

### 172. API Key 自动绑定批迭代器缺少单次最大批次数（已完成）

- 位置：`app/services/api_key_admin_service.py`
- 问题：`_iter_api_key_id_batches()` 会按 200 条一批持续遍历所有 API Key，没有最大批次数边界。
- 影响：全量自动授权同步或全量回填在异常数据规模下仍可能长时间占用数据库和缓存失效链路。
- 修复：新增 `AUTO_PROVIDER_BINDING_MAX_BATCHES=10000`，常规规模不受影响，同时为异常规模提供单次运行保护。

## 第三十四批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\alert_service.py app\services\system_metrics_service.py app\services\log_service.py app\services\model_mapping_service.py app\services\api_key_admin_service.py`
- 结果：通过，告警服务、系统监控服务、日志服务、模型映射服务和 API Key 管理服务语法编译成功。
- 已执行：源码级检查告警恢复、监控告警恢复、清空日志、模型映射规范化和 API Key 自动绑定批迭代器的最大批次数常量及循环边界，以及第 168-172 项文档完成标记。
- 结果：通过，第 168-172 项维护任务单次运行边界均已落地。
- 解决策略：采用“维护任务批处理上限 + 可重复触发继续处理”的策略，降低历史告警、历史日志、模型映射和 API Key 自动绑定在异常规模下对数据库写入、缓存失效和后台任务时长的冲击。

## 第三十五批问题与修复

### 173. Provider 质量指标聚合读取 24 小时内全量路由日志（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`_load_quality_accumulators()` 会读取质量窗口内所有正式路由日志，并在 Python 中解析 trace 统计 provider 与模型质量。
- 影响：高流量环境下，provider 列表、详情和相关接口首次刷新会拉取大量日志行并重复解析 JSON。
- 修复：新增 `QUALITY_LOG_SAMPLE_LIMIT=10000`，质量指标只读取最近 10000 条正式路由日志样本，并继续复用 15 秒短缓存。

### 174. Provider 可用性时序读取单 provider 大窗口全量日志（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`availability_timeseries()` 支持最长 30 天窗口，但查询没有行数上限。
- 影响：单个 provider 历史请求较多时，可用性图表会一次性读取大量请求日志。
- 修复：新增 `AVAILABILITY_LOG_SAMPLE_LIMIT=10000`，查询最近 10000 条日志样本后按时间重新排序并聚合桶。

### 175. Provider 最近内容防护事件查询上限随 provider 数线性放大（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`_build_recent_content_guard_events()` 的查询限制为 `provider 数 * 每 provider 条数`。
- 影响：provider 数量增长后，列表页 recent 内容防护事件查询上限会同步膨胀。
- 修复：新增 `RECENT_CONTENT_GUARD_EVENT_LIMIT=500`，总查询窗口不超过 500 条。

### 176. Provider 主列表序列化每个 provider 的全部模型配置（已完成）

- 位置：`app/services/provider_service.py`、`app/schemas/provider.py`
- 问题：`list_provider_dicts()` 会把每个 provider 的所有 `provider_models` 完整序列化进 `model_configs`。
- 影响：单个 provider 挂载模型过多时，列表响应体和序列化 CPU 会快速膨胀。
- 修复：新增 `PROVIDER_LIST_MODEL_CONFIG_LIMIT=500`；主列表每个 provider 最多返回前 500 个模型配置，并在输出中保留 `model_config_count` 与 `model_configs_truncated` 标记；详情和编辑相关调用仍保留完整数据。

### 177. Provider 轻量列表接口缺少短缓存（已完成）

- 位置：`app/services/provider_service.py`
- 问题：provider options、playground 和 summary 接口每次都重新加载 provider 与模型挂载并序列化。
- 影响：多个页面筛选器、playground 或概览短时间重复刷新时，会产生重复数据库读取与 Python 序列化开销。
- 修复：新增 `provider-light-lists:*` 短缓存，TTL 15 秒，并在 provider 运行时缓存失效时同步清理。

## 第三十五批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\provider_service.py app\schemas\provider.py`
- 结果：通过，Provider 服务与 Provider schema 语法编译成功。
- 已执行：源码级检查 Provider 质量日志样本上限、可用性时序日志样本上限、最近内容防护事件总上限、Provider 主列表模型配置展示上限、截断标记、轻量列表短缓存和缓存失效前缀，以及第 173-177 项文档完成标记。
- 结果：通过，第 173-177 项 Provider 列表、质量指标和可用性图表资源边界均已落地。
- 解决策略：采用“日志样本窗口 + 查询总上限 + 列表响应截断标记 + 轻量列表短缓存”的组合策略，降低 Provider 页面和相关接口在高流量日志、超多模型挂载和多页面重复刷新场景下的数据库读取、JSON 解析和响应体积压力。

## 第三十六批问题与修复

### 178. 模型目录摘要缓存 miss 时仍全量序列化目录和 provider（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：`model_summary()` 虽然有缓存，但缓存 miss 时会加载全部模型目录和全部 provider，并完整序列化后再汇总几个计数。
- 影响：模型目录或 provider 绑定增长后，首次打开模型页或缓存失效后的刷新会产生不必要的 ORM 加载和 Python 序列化开销。
- 修复：改为使用数据库条件聚合直接统计模型总数、启用模型数、绑定 provider 数、可用 provider 绑定数和可路由 provider 绑定数。

### 179. 模型目录分页列表每页仍加载全部 provider 与模型挂载（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：`list_model_page()` 非健康筛选分支只分页查询当前页目录，但随后调用 `ProviderService.list_providers(db)` 加载全部 provider 与全部模型挂载。
- 影响：当前页只有几十个模型时，也会承担全站 provider/model 挂载加载成本。
- 修复：新增 `_load_providers_for_catalogs()`，分页列表只加载当前页目录相关 provider，并通过 `with_loader_criteria` 只预加载相关模型挂载。

### 180. 模型健康状态筛选扫描窗口仍加载全量 provider（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：健康筛选分支虽然限制最多扫描 1000 个目录候选，但仍为这批候选加载全部 provider 与全部模型挂载。
- 影响：provider/model 挂载规模较大时，健康筛选页仍存在无关 provider 绑定序列化成本。
- 修复：健康筛选分支复用 `_load_providers_for_catalogs()`，仅加载扫描候选目录相关 provider 与挂载。

### 181. 单模型健康测试先加载全部目录再查目标模型（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：`test_model_health()` 调用 `_load_catalogs_and_providers()` 后在 Python 中查找目标模型。
- 影响：手动测试单个模型会触发全量模型目录读取和全量 provider 预加载。
- 修复：改为 `get_catalog(db, model_name)` 直接读取目标目录，并只加载该模型相关 provider 挂载后执行探针。

### 182. 一键模型健康测试先加载全量目录再截断 50 个（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：`test_all_model_health()` 先加载全部目录和 provider，再用 Python 切片保留前 50 个模型。
- 影响：模型目录较多时，即使本轮只测试 50 个，也会先产生全量读取开销。
- 修复：调用 `_load_catalogs_and_providers(catalog_limit=MODEL_HEALTH_TEST_ALL_LIMIT)`，在 SQL 层先限制目录数量，并只加载这批目录相关 provider 挂载。

## 第三十六批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\model_catalog_service.py`
- 结果：通过，模型目录服务语法编译成功。
- 已执行：源码级检查模型摘要条件聚合、相关 provider 加载 helper、分页与健康筛选相关 provider 加载、单模型健康测试直接查询、一键健康测试 SQL 层数量上限，以及第 178-182 项文档完成标记。
- 结果：通过，第 178-182 项模型目录摘要、分页列表、健康筛选和健康测试资源边界均已落地。
- 解决策略：采用“数据库条件聚合 + 当前目录窗口相关 provider 加载 + SQL 层先限量 + 单对象直查”的组合策略，降低模型目录页面和健康测试在模型/provider 规模增长后的 ORM 加载、序列化和上游探针前置成本。

## 第三十七批问题与修复

### 183. 用户端 API Key 序列化存在 owner/provider 关系 N+1 风险（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：`list_owned_api_keys()` 只查询 API Key 本体，后续 `serialize_api_key()` 会访问 `owner_user`、`provider_bindings` 和 `binding.provider`。
- 影响：用户拥有较多 API Key 或 provider 绑定时，概览、日志页和账单页可能触发多轮懒加载查询。
- 修复：为 `list_owned_api_keys()` 增加 `selectinload(ApiClientKey.owner_user)` 与 `selectinload(ApiClientKey.provider_bindings).selectinload(ApiClientKeyProviderBinding.provider)`。

### 184. 用户会话数缓存 Key 只使用 Key 数量与最大 ID（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：`_cached_user_conversation_count()` 的缓存 Key 只包含用户 ID、Key 数量和最大 Key ID。
- 影响：当 Key 集合变化但数量和最大 ID 恰好一致时，可能复用错误的会话数缓存。
- 修复：改为对排序后的 Key ID 列表计算 SHA256 16 位摘要，缓存 Key 同时保留用户 ID、数量和摘要。

### 185. 用户日志筛选项为计算 Key 掩码重复完整序列化 API Key（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：`get_log_filter_options()` 为得到 `key_masked` 会对 owned keys 再调用 `serialize_api_key()`，该序列化会解密 raw key 并遍历 provider 绑定。
- 影响：普通筛选项接口会承担不必要的解密和关系序列化成本。
- 修复：筛选项直接用 `ApiKeyService.mask_key_prefix(item.key_prefix)` 生成掩码，并复用预构造的 Key ID 字符串集合。

### 186. 用户账单概览趋势统计重复查询 owned Key ID（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：`get_billing_overview()` 已经加载 owned keys 和 `key_ids`，但调用 `get_usage_trend()` 时没有传入 `key_ids`，导致趋势统计再次查询 Key ID。
- 影响：账单页刷新会增加一次无意义的 API Key ID 查询。
- 修复：`get_billing_overview()` 调用 `get_usage_trend(db, user=user, days=7, key_ids=key_ids)` 复用已加载 ID。

### 187. 用户日志详情为单条日志物化全部 owned Key ID（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：`get_owned_request_log()` 为校验单条日志归属，先加载用户全部 API Key ID，再构造 `IN (...)` 查询。
- 影响：用户拥有大量 Key 时，单条日志详情查询会生成过长 IN 条件并增加内存分配。
- 修复：改为在查询中使用 `select(ApiClientKey.id).where(ApiClientKey.owner_user_id == user.id)` 子查询，不再在 Python 中物化全量 Key ID。

## 第三十七批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\user_portal_service.py`
- 结果：通过，用户端服务语法编译成功。
- 已执行：源码级检查 owned API Key 关系预加载、会话数缓存 Key 摘要化、日志筛选项掩码直接计算、账单趋势复用 Key ID、单日志详情归属子查询，以及第 183-187 项文档完成标记。
- 结果：通过，第 183-187 项用户端 API Key、筛选项、账单概览和日志详情查询边界均已落地。
- 解决策略：采用“关系预加载 + 缓存 Key 摘要 + 避免完整序列化 + 已有 ID 复用 + 数据库子查询归属校验”的组合策略，降低用户端页面在多 API Key、多 provider 绑定和单日志详情访问场景下的重复查询、解密和 Python 内存分配。

## 第三十八批问题与修复

### 188. 管理审计日志未限制 detail/before/after JSON 入库大小（已完成）

- 位置：`app/services/admin_audit_service.py`
- 问题：`create_log()` 会直接把 `detail`、`before`、`after`、`changed_fields` 序列化后写入审计表，没有字节上限。
- 影响：批量操作、大对象变更或导入结果可能把审计日志写成大字段，增加数据库存储、查询和页面详情渲染压力。
- 修复：新增 `AUDIT_JSON_MAX_BYTES=65536` 与 `_bounded_json_text()`，审计 JSON 字段超过 64 KiB 时截断并追加截断标记。

### 189. 后台用户导出复用分页服务导致额外 count 查询（已完成）

- 位置：`app/routers/user_accounts.py`
- 问题：`/users/export` 只需要导出前 N 个用户，却调用 `UserPortalService.list_users()`，该服务会额外执行总数 count。
- 影响：用户表增长后，导出接口会产生无用聚合查询。
- 修复：导出接口改为直接按 `UserAccount.id.desc()` 查询本次导出的有限用户行，不再计算 total。

### 190. API Key 账单记录服务层 limit 缺少硬上限（已完成）

- 位置：`app/services/billing_service.py`
- 问题：`list_billing_records()` 直接使用调用方传入的 `limit`。
- 影响：如果未来内部调用绕过路由层限制，单次账单查询可能加载过多记录。
- 修复：服务层新增 `normalized_limit = max(1, min(int(limit or 50), 500))`，单次最多返回 500 条。

### 191. 用户账单概览服务层 limit 缺少硬上限（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：`get_billing_overview()` 直接使用调用方传入的 `limit` 查询近期账单。
- 影响：内部复用或未来接口调整时，账单页概览可能加载过多记录。
- 修复：服务层新增 `normalized_limit`，账单概览单次最多读取 200 条近期账单。

### 192. 用户账单导出为 Key 名称映射加载完整 owned API Key 对象（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：`export_billing_csv()` 为构造 `api_client_key_id -> name` 映射调用 `list_owned_api_keys()`，会预加载 owner/provider 关系。
- 影响：账单导出只需要 Key 名称，却承担了 API Key 关系加载、对象构造和可能的后续序列化成本。
- 修复：改为仅查询 `ApiClientKey.id` 与 `ApiClientKey.name` 两列构造映射。

## 第三十八批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\admin_audit_service.py app\routers\user_accounts.py app\services\billing_service.py app\services\user_portal_service.py`
- 结果：通过，审计服务、用户管理路由、账单服务和用户端服务语法编译成功。
- 已执行：源码级检查审计 JSON 字节上限、后台用户导出跳过 count、API Key 账单服务层 limit 兜底、用户账单概览 limit 兜底、用户账单导出 Key 名称列级查询，以及第 188-192 项文档完成标记。
- 结果：通过，第 188-192 项审计、用户导出和账单服务资源边界均已落地。
- 解决策略：采用“审计大字段截断 + 导出跳过无用 count + 服务层 limit 兜底 + 关联映射列级查询”的组合策略，降低审计日志、用户导出和账单查询在大对象、用户规模增长和内部复用场景下的数据库与内存压力。

## 第三十九批问题与修复

### 193. 请求日志列表页默认加载完整 payload 大字段（已完成）

- 位置：`app/services/log_service.py`、`app/routers/logs.py`、`app/routers/logging_api.py`、`app/routers/api_keys.py`、`app/services/user_portal_service.py`
- 问题：日志列表、API Key 日志和用户端最近日志只需要概览字段，却默认查询 `request_body_json`、`response_body_json`、`response_text`、`trace_json` 等大字段。
- 影响：请求日志量和 payload 体积增长后，列表刷新会产生不必要的数据库 IO、网络传输和 ORM 对象内存占用。
- 修复：新增 `_lightweight_log_load_options()`，列表/最近日志查询统一排除重 payload 字段，序列化时对这些字段返回空值。

### 194. 请求日志列表序列化重复解析大 JSON 提炼图像观测字段（已完成）

- 位置：`app/services/log_service.py`、`app/routers/logs.py`、`app/routers/logging_api.py`、`app/routers/api_keys.py`、`app/services/user_portal_service.py`
- 问题：`serialize_log()` 每条日志都会解析请求/响应 JSON 并递归扫描图像相关字段，列表页、最近日志和 API Key 日志也会触发该成本。
- 影响：单页 200 条或 payload 较大时，前端普通列表刷新会被 JSON 解析和递归扫描拖慢。
- 修复：`serialize_log()` 与 `serialize_logs()` 增加轻量参数，列表类入口关闭重 payload 派生，仅保留基于 `has_image` 的基础观测字段；单条详情和时间线继续保留完整派生。

### 195. 指标缓存 Key 对大量 API Key 直接拼接完整 ID 列表（已完成）

- 位置：`app/services/log_service.py`
- 问题：`_metric_cache_key()` 将用户拥有的所有 API Key ID 直接拼入缓存 Key。
- 影响：API Key 数量较多时，缓存 Key 过长会增加 Redis/内存缓存键名存储、传输和比较成本，并可能触及键长边界。
- 修复：对排序后的 API Key ID 列表计算 SHA256 16 位摘要，缓存 Key 仅保留 Key 数量与摘要。

### 196. 类型化内容防护日志导出引用未声明筛选参数（已完成）

- 位置：`app/routers/logging_api.py`
- 问题：`export_typed_logs()` 的 `content-guard-events` 分支调用 `list_content_guard_events()` 时传入 `provider_id`、`model_name`、`is_stream`，但导出接口签名中没有声明这些参数。
- 影响：导出内容防护日志时会因未定义变量直接失败，降低运维排查可靠性。
- 修复：为类型化日志导出接口补齐 `provider_id`、`model_name`、`is_stream` 查询参数，使导出分支与列表分支筛选口径一致。

### 197. 内容高风险自动隔离告警快照无上限记录全部模型（已完成）

- 位置：`app/services/system_metrics_service.py`
- 问题：内容高风险自动隔离时，告警 payload 的 `pre_isolation_snapshot` 会记录 provider 下全部模型状态。
- 影响：单个提供商挂载大量模型时，告警事件会膨胀为大 JSON，增加数据库写入、查询和前端详情渲染压力。
- 修复：隔离动作仍更新全部模型状态，但告警前置快照只保留前 50 个模型样本，并记录 `models_total`、`models_snapshot_limit` 与 `models_truncated`。

## 第三十九批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\log_service.py app\routers\logs.py app\routers\logging_api.py app\routers\api_keys.py app\services\user_portal_service.py app\services\api_key_admin_service.py app\services\system_metrics_service.py`
- 结果：通过，日志服务、日志路由、API Key 路由、用户端服务、API Key 管理服务和系统监控服务语法编译成功。
- 已执行：源码级检查轻量日志加载、轻量序列化、指标缓存 Key 摘要、类型化内容防护导出参数、隔离快照模型样本上限，以及第 193-197 项文档完成标记。
- 结果：通过，第 193-197 项日志列表、日志导出、指标缓存和监控告警资源边界均已落地。
- 解决策略：采用“列表排除大字段 + 序列化轻量派生 + 缓存 Key 摘要化 + 导出参数补齐 + 告警快照限样本”的组合策略，降低日志列表、指标缓存和监控告警在大 payload、大 Key 集合和大模型挂载规模下的 IO、CPU 与稳定性风险。

## 第四十批问题与修复

### 198. 请求日志异步队列写库失败后 processing 项只能等重启恢复（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：请求日志 worker 将项目移动到 `processing` 后，如果批量写库失败，只记录 warning 并等待下一轮，当前批次会滞留在 `processing`，只能依赖后续重启恢复。
- 影响：数据库短暂异常或写入错误会让日志实时性下降，日志页等待队列空闲时也可能被 processing 残留拖住。
- 修复：worker 在写库未完成时捕获异常后立即通过 Redis pipeline 将本批项目重新放回主队列并从 `processing` 移除。

### 199. 请求日志异步队列成功写库后逐条 LREM 确认（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：请求日志 worker 写库成功后对批次中每条记录逐条执行 `LREM processing`。
- 影响：批量大小较大时，每批会产生大量 Redis 往返，降低高并发日志落库吞吐。
- 修复：新增 `_ack_processing_batch()`，成功写库后用 Redis pipeline 批量确认 processing 项。

### 200. 类型化日志队列写库失败和确认路径同样存在 processing 滞留与逐条确认（已完成）

- 位置：`app/logging/queue.py`
- 问题：类型化日志 worker 写库失败后本批项目滞留在 `processing`，成功后仍逐条 `LREM` 确认。
- 影响：异常事件、计费事件、后台任务事件等类型化日志在短暂故障后可能积压，正常高吞吐时也会增加 Redis 往返。
- 修复：为类型化日志队列增加批量归队与批量确认 helper，写库失败时立即归队，写库成功后 pipeline 批量确认。

### 201. Token 补全遇到已删除日志时不释放 dedupe Key（已完成）

- 位置：`app/services/token_usage_service.py`
- 问题：`finalize_single_log()` 发现目标 `RequestLog` 不存在时直接返回，没有释放 `token_usage:finalize:dedupe:*`。
- 影响：日志被清理或并发删除后，对应补全 dedupe Key 会保留到 TTL 过期，增加 Redis 残留并延迟后续同 ID 补全任务的自然收敛。
- 修复：目标日志不存在时立即调用 `_release_finalize_job(log_id)`。

### 202. Token 补全批量快路径和 backfill 候选加载完整 RequestLog（已完成）

- 位置：`app/services/token_usage_service.py`
- 问题：`_fast_finalize_no_charge_batch()` 与 `backfill_missing_usage()` 都使用 `select(RequestLog)` 读取完整日志对象。
- 影响：补全任务只需要计费、token 和路由少量字段，却可能加载请求/响应 payload 等大字段，增加后台 worker 内存和数据库 IO。
- 修复：新增 `_fast_finalize_log_load_options()` 并在批量零计费快路径使用列级加载；backfill 候选查询只加载 `id`、模型名和请求路径。

## 第四十批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\request_log_queue_service.py app\logging\queue.py app\services\token_usage_service.py`
- 结果：通过，请求日志队列、类型化日志队列和 Token 补全服务语法编译成功。
- 已执行：源码级检查请求日志队列批量归队与批量确认、类型化日志队列批量归队与批量确认、Token 补全不存在日志释放 dedupe、批量快路径列级加载、backfill 候选列级加载，以及第 198-202 项文档完成标记。
- 结果：通过，第 198-202 项队列异常恢复、Redis 批量确认和 Token 补全读取边界均已落地。
- 解决策略：采用“失败批次即时归队 + 成功批次 pipeline 确认 + dedupe 及时释放 + 后台补全列级加载”的组合策略，提升日志队列异常自愈能力、降低 Redis 往返，并减少 Token 补全后台 worker 的大字段加载成本。

## 第四十一批问题与修复

### 203. 请求日志队列批量写入时逐条查询重复日志（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：`_write_batch()` 与 `_write_kwargs_batch()` 在批量落库时，每条日志都调用 `_find_existing_log()` 单独查询是否已存在。
- 影响：同一批 500 条日志会产生最多 500 次重复检测查询，数据库短时压力随日志吞吐线性放大。
- 修复：抽出 `_write_payloads_batch()`，先按本批 trace_id 一次性加载已有日志，再按 dedupe signature 在内存中匹配。

### 204. 请求日志队列批量写入时逐条校验 provider 是否存在（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：每条日志都通过 `db.get(Provider, provider_id)` 校验 provider。
- 影响：高并发落库时，同一批内重复 provider 会产生大量重复主键查询。
- 修复：新增 `_load_valid_provider_ids()`，先批量查询本批 provider ID 集合，逐条写入时只做集合判断。

### 205. 请求日志队列两个写入入口重复维护同一套落库逻辑（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：Redis raw item 写入和内存 ingress fallback 写入各自维护一套创建日志、记录类型化事件、提交事务和调度 finalize 的逻辑。
- 影响：后续性能修复容易漏改其中一个入口，导致两条路径资源边界不一致。
- 修复：两个入口统一转为 payload 列表后复用 `_write_payloads_batch()`。

### 206. 请求日志队列批内重复项会重复执行创建前检查（已完成）

- 位置：`app/services/request_log_queue_service.py`
- 问题：同一批中若出现相同 trace/request/path/method/log_type 的重复项，旧逻辑仍会逐条走数据库检测和 provider 校验。
- 影响：网络重试或队列恢复造成重复入队时，会放大写库前置成本。
- 修复：新增 `_dedupe_signature()` 与 `seen_signatures`，批内重复项直接复用已加载或刚创建的日志对象，不再重复执行创建前检查。

### 207. 告警事件列表服务层 limit 缺少硬上限（已完成）

- 位置：`app/services/alert_service.py`
- 问题：`list_events()` 直接使用调用方传入的 `limit`。
- 影响：未来内部调用绕过路由或仪表盘默认值时，可能一次解析和返回大量 `payload_json`。
- 修复：新增 `ALERT_EVENT_LIST_LIMIT=500`，服务层将单次告警事件列表读取限制在 1-500 条。

## 第四十一批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\request_log_queue_service.py app\services\alert_service.py`
- 结果：通过，请求日志队列服务和告警服务语法编译成功。
- 已执行：源码级检查请求日志队列批量重复检测、批量 provider 校验、统一写入入口、批内 signature 去重、告警事件服务层 limit 上限，以及第 203-207 项文档完成标记。
- 结果：通过，第 203-207 项请求日志队列批量写入查询边界和告警列表读取上限均已落地。
- 解决策略：采用“批量预取 + 集合校验 + 写入入口合并 + 批内去重 + 服务层 limit 兜底”的组合策略，降低请求日志高吞吐落库时的数据库查询次数，并防止告警列表内部复用时一次性解析过多 payload。

## 第四十二批问题与修复

### 208. 提供商状态摘要为计数加载完整 provider 与模型挂载（已完成）

- 位置：`app/services/health_service.py`
- 问题：`cached_provider_status_summary()` 只需要 provider 状态计数，却调用 `ProviderService.list_providers()` 加载全部 provider 及其 `provider_models`。
- 影响：提供商和模型挂载增长后，状态摘要刷新会产生不必要的 ORM 对象构造和关系加载。
- 修复：改为 SQL 条件聚合，一次查询返回 provider 总数、健康/降级/异常和熔断计数。

### 209. 提供商连通性全量检查预加载无关模型挂载（已完成）

- 位置：`app/services/health_service.py`
- 问题：`check_provider_connectivity_all()` 只测试 provider 连通性，却通过 `_scheduled_enabled_providers()` 预加载 `Provider.provider_models`。
- 影响：L0 连通性计划任务会随模型挂载规模增加而增加无关数据库读取与内存占用。
- 修复：`_scheduled_enabled_providers()` 增加 `include_models` 参数，L0 连通性全量检查传入 `include_models=False`。

### 210. 单个 provider model 健康测试先加载 provider 全部挂载再遍历查目标模型（已完成）

- 位置：`app/routers/providers.py`
- 问题：`/{provider_id}/models/{provider_model_id}/test` 先用 `ProviderService.get_provider()` 预加载全部模型挂载，再在 Python 中遍历查目标模型。
- 影响：单模型即时测试会被同 provider 下其它模型挂载数量放大。
- 修复：provider 使用 `db.get(Provider, provider_id)` 读取本体，目标模型通过 `select(ProviderModel).where(id, provider_id)` 直查。

### 211. 指定 provider 连通性测试为校验少量 ID 加载全部 provider（已完成）

- 位置：`app/routers/providers.py`、`app/services/health_service.py`
- 问题：`/api/providers/test-connectivity` 和 `check_selected_providers()` 在传入 provider ID 时仍先加载全部 provider。
- 影响：只测试少量 provider 时，也会加载全量 provider 与模型挂载。
- 修复：路由预校验仅查询传入 ID 集合；`check_selected_providers()` 在指定 ID 时只查询这些 provider 并预加载其模型。

### 212. 健康检查流式进度队列无上限（已完成）

- 位置：`app/routers/providers.py`、`app/services/health_service.py`
- 问题：`_stream_health_check_events()` 使用无界 `asyncio.Queue()` 缓冲进度事件。
- 影响：客户端读取慢、网络中断或探测进度过多时，内存可能持续增长。
- 修复：新增 `HEALTH_STREAM_PROGRESS_QUEUE_SIZE=200`，流式进度队列使用固定上限；队列满时丢弃最旧的中间进度，保留最新进度继续输出。

## 第四十二批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\health_service.py app\routers\providers.py`
- 结果：通过，健康检查服务和提供商路由语法编译成功。
- 已执行：源码级检查提供商状态摘要 SQL 聚合、L0 连通性跳过模型预加载、单模型测试直查 ProviderModel、指定 provider 连通性只查目标 ID、健康检查流式队列上限，以及第 208-212 项文档完成标记。
- 结果：通过，第 208-212 项健康检查与提供商探测链路资源边界均已落地。
- 解决策略：采用“SQL 条件聚合 + 可选模型预加载 + 单对象直查 + 指定 ID 批查 + 有界流式进度队列”的组合策略，降低健康检查和即时探测在 provider/model 规模增长、客户端慢消费场景下的资源占用。

## 第四十三批问题与修复

### 213. 真实并发探测报告文件重复写入（已完成）

- 位置：`scripts/real_concurrency_benchmark.py`
- 问题：`save_report_files()` 已经写入 JSON/HTML 报告，`main_async()` 随后又在同一路径写入一遍。
- 影响：大型并发探测报告会产生重复序列化、重复 HTML 构建和重复磁盘 IO。
- 修复：报告路径在 `save_report_files()` 内先回填到 report 对象，再一次性写出 JSON/HTML，移除主流程的二次写入。

### 214. 真实上游功能测试等待日志时加载完整大字段（已完成）

- 位置：`scripts/real_upstream_functional_test.py`
- 问题：`wait_for_logs()` 只打印日志摘要，却 `select(RequestLog)` 加载完整 ORM 行，包括请求体、响应体、trace 和内容防护大字段。
- 影响：真实上游联调后等待日志落库时，会把大 payload 读入内存并增加数据库传输量。
- 修复：日志等待查询增加 `load_only()`，仅加载摘要打印所需字段。

### 215. 真实上游功能测试流式读取缺少事件和字节上限（已完成）

- 位置：`scripts/real_upstream_functional_test.py`
- 问题：`_call_stream()` 遍历所有非空行并计数，未限制 SSE 事件数或累计读取字节。
- 影响：上游或代理异常持续输出时，功能测试可能长时间读取大量数据，放大网络、内存和日志压力。
- 修复：新增 `MAX_FUNCTIONAL_STREAM_EVENTS=1024` 和 `MAX_FUNCTIONAL_STREAM_BYTES=1MB`，只统计 `data:` 事件，超过上限立即返回明确错误。

### 216. 代理压测结束前未等待后台日志和计费任务收敛（已完成）

- 位置：`scripts/benchmark_proxy.py`
- 问题：压测完成后立即打印结果并退出，未等待请求日志队列和 Token finalize 积压收敛。
- 影响：压测结论可能基于未落库或未结算的残缺后台状态，且残留队列会污染下一轮压测。
- 修复：新增 `wait_for_benchmark_background_idle()`，先等待请求日志队列空闲，再有界轮询当前 benchmark API Key 的未 finalize 日志数量并输出剩余积压。

### 217. 内容防护开销基准每轮重复创建事件循环（已完成）

- 位置：`scripts/benchmark_content_guard_overhead.py`
- 问题：`runtime_non_stream_operation()` 和 `stream_prefetch_operation()` 在每次测量操作内调用 `asyncio.run()`。
- 影响：事件循环创建/销毁成本混入内容防护检测耗时，导致 p95/p99 开销结果被测量噪声放大。
- 修复：新增线程本地事件循环复用函数 `run_async_operation()`，同步基准操作复用当前线程事件循环执行异步检测。

## 第四十三批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall scripts\benchmark_proxy.py scripts\real_concurrency_benchmark.py scripts\benchmark_content_guard_overhead.py scripts\real_upstream_functional_test.py`
- 结果：通过，压测、真实并发探测、内容防护基准和真实上游功能测试脚本语法编译成功。
- 已执行：源码级检查报告一次写入、功能测试日志轻量读取、流式事件/字节上限、压测后台空闲等待、内容防护基准事件循环复用，以及第 213-217 项文档完成标记。
- 结果：通过，第 213-217 项压测与真实上游测试链路的测量噪声、后台残留和无界读取问题均已落地收敛。
- 解决策略：采用“一次性报告写出 + 摘要字段加载 + 流式双上限 + 后台队列有界等待 + 线程本地事件循环复用”的组合策略，提高性能测试脚本结论可靠性，并降低异常上游或后台积压对本地资源的放大影响。

## 第四十四批问题与修复


- 问题：`read_rows()` 使用 `limit(batch_size).offset(offset)` 逐批读取源表。
- 修复：新增 `single_primary_key_column()`，单主键表改为按主键递增 keyset 分页；仅无单主键表保留 offset fallback。

### 219. 真实上游包装脚本健康等待使用系统时间（已完成）

- 位置：`scripts/run_real_upstream_functional_test.py`
- 问题：`wait_for_live()` 使用 `time.time()` 计算超时窗口。
- 影响：系统时间被同步或手动调整时，健康等待可能提前结束或额外等待，影响本地联调稳定性。
- 修复：改用 `time.monotonic()` 计算等待截止时间和循环判断。

### 220. 真实上游包装脚本日志摘要读取完整 RequestLog 行（已完成）

- 位置：`scripts/run_real_upstream_functional_test.py`
- 问题：`print_log_summary()` 为打印 30 条摘要调用 `.query(RequestLog).all()`，加载完整请求/响应/trace 大字段。
- 影响：功能测试结束时可能把大日志 payload 读入内存，增加数据库和脚本资源占用。
- 修复：API Key 只查 ID，日志查询增加 `load_only()`，仅加载摘要输出所需字段。

### 221. 真实上游包装脚本通过 communicate 无界收集子进程输出（已完成）

- 位置：`scripts/run_real_upstream_functional_test.py`
- 问题：测试子进程 stdout/stderr 使用 `subprocess.PIPE` 后通过 `communicate()` 一次性读入内存。
- 影响：异常输出过多时，包装脚本会无界占用内存，且超时后仍要回收全部管道内容。
- 修复：子进程 stdout/stderr 改写入固定日志文件，结束后按 `MAX_FUNCTIONAL_SUBPROCESS_OUTPUT_BYTES=1MB` 上限回放输出，并显式设置 `PYTHONIOENCODING=utf-8`。

### 222. 独立后台 worker 启动失败时缺少已启动组件清理（已完成）

- 位置：`scripts/run_background_workers.py`
- 问题：Redis、统一日志队列、请求日志队列和 Token finalize worker 顺序启动；若中途某一步失败，已启动组件不会进入原有 `finally` 清理。
- 影响：启动异常时可能遗留后台任务、Redis 连接或未关闭队列，影响后续重启稳定性。
- 修复：为各组件增加启动标记，启动流程和等待流程共用一个 `try/finally`，只关闭已成功启动的组件。

## 第四十四批验证结果

- 结果：通过，迁移脚本、真实上游包装脚本和独立后台 worker 脚本语法编译成功。
- 已执行：源码级检查 keyset 分页、monotonic 健康等待、日志摘要 `load_only()`、子进程输出文件化限量回放、后台 worker 启动标记清理，以及第 218-222 项文档完成标记。
- 结果：通过，第 218-222 项运维脚本和后台 worker 启停链路的资源边界与异常清理均已落地。
- 解决策略：采用“主键 keyset 迁移 + 单调时钟超时 + 摘要字段加载 + 子进程输出落盘限量回放 + 分阶段启动清理”的组合策略，降低迁移、真实上游联调和独立后台 worker 在大数据、异常输出或启动失败场景下的资源占用与残留风险。

## 第四十五批问题与修复

### 223. Web worker 生命周期启动失败时缺少已启动组件清理（已完成）

- 位置：`app/main.py`
- 问题：`lifespan()` 顺序启动 Redis、上游客户端、日志队列、请求日志队列、Token finalize worker 和调度器；若中途失败，原有关闭逻辑不会执行。
- 影响：启动异常时可能遗留 Redis 连接、队列 worker、调度器或上游 HTTP 客户端，影响下一次启动稳定性。
- 修复：为各启动阶段增加布尔标记，将启动、`yield` 和关闭纳入统一 `try/finally`，只关闭已经成功启动的组件。

### 224. 真实上游功能测试等待日志使用系统时间（已完成）

- 位置：`scripts/real_upstream_functional_test.py`
- 问题：`wait_for_logs()` 用 `time.time()` 计算等待截止时间。
- 影响：本机时间同步或手动调整时，日志等待可能提前结束或延长，造成测试结果不稳定。
- 修复：改用 `time.monotonic()` 计算 deadline 和循环判断。

### 225. API Key 创建/更新列表字段缺少长度上限（已完成）

- 位置：`app/schemas/api_key.py`
- 问题：`allowed_provider_ids`、`allowed_model_names`、端点/IP/偏好 provider/区域等列表只做去重归一化，没有 Pydantic `max_length` 上限。
- 影响：异常请求可提交超大列表，放大 JSON 解析、校验、数据库 `IN` 查询和持久化 JSON 字段成本。
- 修复：新增各列表字段上限，核心 provider/model/IP 列表限制 500 项，端点和区域偏好限制 100 项。

### 226. API Key 策略模板列表字段缺少长度上限（已完成）

- 位置：`app/schemas/api_key_policy_template.py`
- 问题：策略模板的 `allowed_provider_ids` 和 `allowed_model_names` 没有长度上限。
- 影响：创建或套用大模板时会放大模板保存、模型校验和批量应用成本。
- 修复：新增模板列表上限，provider 和 model 白名单各限制 500 项。

### 227. 自动同步 provider 绑定时重复校验全量 provider ID（已完成）

- 位置：`app/services/api_key_admin_service.py`
- 问题：当 API Key 开启 `auto_sync_provider_bindings` 时，服务层先读取全量 provider ID，再调用 `_validate_provider_ids()` 对同一批 ID 重新执行 `WHERE id IN (...)` 查询。
- 影响：provider 数量增长时，创建、更新和批量更新自动同步 API Key 会产生一次无意义的大 `IN` 查询。
- 修复：仅在显式传入局部 provider 白名单时执行 provider ID 存在性校验；自动同步分支复用刚读取的全量 provider ID。

## 第四十五批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\main.py scripts\real_upstream_functional_test.py app\schemas\api_key.py app\schemas\api_key_policy_template.py app\services\api_key_admin_service.py`
- 结果：通过，Web 主入口、真实上游功能测试脚本、API Key schema、策略模板 schema 和 API Key 管理服务语法编译成功。
- 已执行：源码级检查 Web lifespan 分阶段清理、日志等待 monotonic、API Key 列表 `max_length`、策略模板列表 `max_length`、自动 provider 同步跳过冗余校验，以及第 223-227 项文档完成标记。
- 结果：通过，第 223-227 项启动异常清理、测试等待稳定性和 API Key 白名单输入边界均已落地。
- 解决策略：采用“生命周期分阶段清理 + 单调时钟等待 + 输入列表硬上限 + 自动同步校验去重”的组合策略，降低启动失败残留、测试偶发等待异常和超大白名单输入对数据库与内存的放大风险。

## 第四十六批问题与修复

### 228. 后台任务 Redis 覆盖状态去重查询加载完整事件行（已完成）

- 位置：`app/routers/logging_api.py`
- 问题：`_latest_background_job_statuses()` 只需要判断每个任务最新数据库状态，却查询完整 `BackgroundJobEvent` ORM 行，并经由 `_background_job_payload()` 读取 `result_summary_json`、`error` 等大字段。
- 影响：后台任务列表叠加 Redis 运行态时，会额外放大数据库读取和 Python 序列化成本，任务结果摘要较大时影响列表响应速度。
- 修复：最新状态查询改为只选择 `job_name`、`status`、`started_at` 三个字段，并在内存中按同一 stale 规则计算 `stale_running`。

### 229. 会话列表预览在 Python 侧截断完整响应文本（已完成）

- 位置：`app/services/conversation_service.py`
- 问题：`list_conversations()` 的最新预览子查询返回完整 `response_text/message`，随后才在 Python 中截断到 180 字符。
- 影响：会话响应文本较长时，列表页只展示短预览却会传输和保留完整大文本，增加数据库、网络和应用内存开销。
- 修复：预览子查询改用 `func.substr(..., 1, 180)` 在数据库侧截断，只返回页面需要的短文本。

### 230. 会话回放查询加载完整 RequestLog 行（已完成）

- 位置：`app/services/conversation_service.py`
- 问题：`get_replay()` 查询 `RequestLog` ORM 整行，但回放构建只需要消息体、响应文本和少量摘要字段。
- 影响：每次回放最多读取 200 条日志时，会把响应体 JSON、trace、usage 等无关大字段一起加载，放大会话页内存和响应延迟。
- 修复：回放查询增加 `load_only()`，仅加载构建 `ConversationReplay` 所需字段。

### 231. 类型化日志多值过滤缺少数量上限（已完成）

- 位置：`app/routers/logging_api.py`
- 问题：`_expand_filter_values()` 会无限展开逗号、中文逗号、分号和管道分隔的过滤值，并直接生成 `IN (...)` 条件。
- 影响：异常请求可构造超长过滤列表，放大 SQL 编译、参数绑定和数据库执行成本。
- 修复：新增 `TYPED_LOG_FILTER_VALUE_LIMIT=50`，过滤值展开时去重并在达到上限后停止追加。

### 232. 用户详情页近期日志读取完整 RequestLog 行（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：`get_user_detail_payload()` 近期日志只展示 20 条摘要，但查询时未使用已有轻量日志加载选项。
- 影响：用户拥有大 payload 日志时，详情页会把请求体、响应体、响应文本、trace 和 usage 详情等大字段读入内存，拖慢页面打开速度。
- 修复：近期日志查询复用 `LogService._lightweight_log_load_options()`，并继续以 `include_payload_fields=False` 序列化摘要。

## 第四十六批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\logging_api.py app\services\conversation_service.py app\services\user_portal_service.py`
- 结果：通过，类型化日志接口、会话服务和用户端服务语法编译成功。
- 已执行：`git diff --check -- app\routers\logging_api.py app\services\conversation_service.py app\services\user_portal_service.py 6-8性能审查.md`
- 结果：通过，仅提示工作区 LF/CRLF 自动转换警告，无空白错误。
- 已执行：源码级检查后台任务状态字段定向查询、会话预览 SQL 侧截断、会话回放 `load_only()`、类型化日志过滤值上限、用户详情近期日志轻量加载，以及第 228-232 项文档完成标记。
- 结果：通过，第 228-232 项日志、会话和用户详情页的大字段读取与无界过滤问题均已落地。
- 解决策略：采用“状态字段定向查询 + SQL 侧预览截断 + 回放字段裁剪 + 多值过滤硬上限 + 复用轻量日志加载”的组合策略，降低日志列表、会话页和用户详情页在大文本、大 payload 或异常过滤输入场景下的资源占用。

## 第四十七批问题与修复

### 233. 用户操作记录列表读取并返回 detail_json 大字段（已完成）

- 位置：`app/routers/user_portal.py`
- 问题：`/api/user/operations` 列表接口查询完整 `UserOperationAuditLog` ORM 行，并把表中所有列都返回给前端。
- 影响：列表表格只展示时间、动作、对象、结果、来源 IP 和摘要，却会额外读取 `detail_json`，操作详情较大时拖慢列表刷新并增加响应体大小。
- 修复：列表查询改为显式选择表格所需字段，响应中不再携带 `detail_json`。

### 234. 用户接入自检流式消费缺少本地读取边界（已完成）

- 位置：`app/routers/user_portal.py`
- 问题：`_consume_self_test_stream()` 持续读取上游 SSE，虽然预览事件数量有限，但累计读取字节、`data:` 事件数和拼接输出文本没有硬上限。
- 影响：异常上游或长输出自检可能占用过多内存和连接时间，影响用户端自检接口稳定性。
- 修复：新增 `SELF_TEST_STREAM_MAX_BYTES=1MB`、`SELF_TEST_STREAM_MAX_EVENTS=512`、`SELF_TEST_STREAM_OUTPUT_MAX_CHARS=4000`，触达上限时停止消费并在预览中记录限制原因。

### 235. 用户日志过滤选项加载完整 API Key 关联对象（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：`get_log_filter_options()` 调用 `list_owned_api_keys()`，会预加载 owner 和 provider bindings，但该接口只需要用户密钥的 `id/name/key_prefix`。
- 影响：用户拥有较多 API Key 或 provider 绑定时，过滤选项刷新会产生不必要的关联查询与对象构造。
- 修复：过滤选项改为直接查询 `ApiClientKey.id/name/key_prefix` 三个字段，并基于轻量行构建 key id 与查询值集合。

### 236. 用户指标汇总接口重复查询全部 API Key ID（已完成）

- 位置：`app/routers/user_portal.py`
- 问题：`/api/user/metrics/summary` 先调用 `list_owned_api_key_ids()`，再将 ID 列表与 `user_account_id` 一起传给 `LogService.metric_summary()`。
- 影响：统计查询已经按 `user_account_id` 限定用户范围，额外 key ID 列表会增加一次查询、缓存 key 哈希成本和 SQL `IN (...)` 参数数量。
- 修复：用户指标汇总接口仅按 `user_account_id` 调用统计服务，移除冗余 key ID 查询和过滤。

### 237. 用户指标时间序列接口重复查询全部 API Key ID（已完成）

- 位置：`app/routers/user_portal.py`
- 问题：`/api/user/metrics/timeseries` 与汇总接口相同，先查询全部用户 API Key ID，再与 `user_account_id` 同时作为统计范围。
- 影响：用户 API Key 数量增长时，时间序列刷新会持续产生冗余 ID 查询和大 `IN` 条件，降低监控响应速度。
- 修复：用户指标时间序列接口仅传 `user_account_id`，让统计服务按用户维度过滤。

## 第四十七批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\user_portal.py app\services\user_portal_service.py`
- 结果：通过，用户端路由和用户端服务语法编译成功。
- 已执行：`git diff --check -- app\routers\user_portal.py app\services\user_portal_service.py 6-8性能审查.md`
- 结果：通过，仅提示工作区 LF/CRLF 自动转换警告，无空白错误。
- 已执行：源码级检查用户操作记录字段选择、自检流式读取上限、日志过滤选项轻量 API Key 查询、用户指标汇总和时间序列去除冗余 key ID，以及第 233-237 项文档完成标记。
- 结果：通过，第 233-237 项用户端操作记录、自检、过滤选项和指标接口资源边界问题均已落地。
- 解决策略：采用“列表字段显式选择 + 流式消费三重上限 + 轻量 API Key 字段查询 + 用户指标去除冗余 key 范围”的组合策略，降低用户端操作记录、自检、过滤选项和监控指标入口的资源占用。

## 第四十八批问题与修复

### 238. 用户日志 API 计算并序列化未使用的 API Key 选项（已完成）

- 位置：`app/services/user_portal_service.py`、`app/routers/user_portal.py`
- 问题：`/api/user/logs` 调用 `UserPortalService.list_logs()` 后丢弃第四个返回值，但服务层仍加载完整 owned API Key 并序列化选项。
- 影响：用户日志列表每次刷新都会额外加载 provider bindings 并构造 API Key 选项，用户密钥较多时拖慢高频日志列表。
- 修复：`list_logs()` 新增 `include_api_key_options` 参数；用户日志 API 传入 `False`，仅查询 key ID 做权限范围校验，不再构造被丢弃的选项。

### 239. 用户日志导出在已有 user_account_id 过滤时重复构造 key ID 范围（已完成）

- 位置：`app/routers/user_portal.py`
- 问题：`/user/logs/export` 已向 `LogService.export_logs_csv()` 传入 `user_account_id=current_user.id`，同时又调用 `list_owned_api_key_ids()` 并传入 `api_client_key_ids`。
- 影响：导出前多一次 ID 查询，且导出 SQL 增加大 `IN (...)` 条件；用户 API Key 数量增长时会放大 SQL 参数和缓存压力。
- 修复：用户日志导出只保留 `user_account_id` 范围过滤，不再传冗余 `api_client_key_ids`。

### 240. 用户日志过滤选项在已有 user_account_id 过滤时仍传 key ID 大列表（已完成）

- 位置：`app/services/user_portal_service.py`
- 问题：`get_log_filter_options()` 调用 `LogService.get_filter_options()` 时同时传入 `user_account_id` 和全部用户 key ID。
- 影响：过滤选项查询和缓存 key 都会携带大 key ID 范围，导致额外哈希、SQL `IN (...)` 参数和查询计划压力。
- 修复：过滤选项查询只传 `user_account_id`；仍保留本地轻量 key 行用于过滤返回选项。

### 241. 请求日志时间线响应序列化完整请求日志 payload（已完成）

- 位置：`app/routers/logging_api.py`
- 问题：`build_request_log_timeline_payload()` 使用默认 `LogService.serialize_logs([log])` 序列化 `request_log`，会携带请求体、响应体、响应文本和 trace 等大字段。
- 影响：时间线前端只使用事件摘要，却会让时间线接口重复传输完整日志 payload，增加响应体与 JSON 序列化成本。
- 修复：时间线中的 `request_log` 改用 `LogService.serialize_log(..., include_payload_fields=False, derive_image_observability=False)`。

### 242. 请求日志时间线路由加载完整 RequestLog 行（已完成）

- 位置：`app/routers/logging_api.py`、`app/routers/user_portal.py`
- 问题：管理端和用户端时间线路由在调用时间线构建前加载完整 `RequestLog` 行。
- 影响：即使时间线响应已不需要大 payload，路由层仍会先把请求体、响应体、trace 等大字段从数据库取出。
- 修复：管理端 `/api/logging/request-logs/{id}/timeline` 和用户端 `/api/user/logs/{id}/timeline` 均改为使用 `LogService._lightweight_log_load_options()` 加载请求日志。

## 第四十八批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\routers\logging_api.py app\routers\user_portal.py app\services\user_portal_service.py`
- 结果：通过，类型化日志路由、用户端路由和用户端服务语法编译成功。
- 已执行：`git diff --check -- app\routers\logging_api.py app\routers\user_portal.py app\services\user_portal_service.py 6-8性能审查.md`
- 结果：通过，仅提示工作区 LF/CRLF 自动转换警告，无空白错误。
- 已执行：源码级检查用户日志 API 按需跳过 API Key 选项、用户日志导出去除冗余 key ID、过滤选项去除冗余 key ID、时间线轻量序列化和时间线路由轻量加载，以及第 238-242 项文档完成标记。
- 结果：通过，第 238-242 项用户日志和请求时间线链路的重复查询、大字段加载与冗余序列化问题均已落地。
- 解决策略：采用“按需返回 API Key 选项 + 用户范围避免重复 key-id IN + 时间线轻量日志加载与轻量序列化”的组合策略，降低用户日志列表、导出、过滤选项和请求时间线的重复查询与大字段传输成本。

## 第四十九批问题与修复

### 243. API Key CSV 导出无用解密明文密钥（已完成）

- 位置：`app/routers/api_keys.py`、`app/services/api_key_admin_service.py`
- 问题：`/api/api-keys/export` 导出 CSV 不包含 `raw_api_key`，但每行仍调用 `serialize_api_key()` 解密明文密钥。
- 影响：导出 5000 条以内 API Key 时会产生大量无用解密操作，增加 CPU 与加密服务开销。
- 修复：`serialize_api_key()` 增加 `include_raw_api_key` 参数；CSV 导出传入 `False`，保留 `has_stored_raw_key` 判断但跳过明文解密。

### 244. API Key 分析近期错误加载完整 RequestLog 行（已完成）

- 位置：`app/services/api_key_admin_service.py`
- 问题：`get_analytics()` 的 recent errors 查询使用 `select(RequestLog)` 读取完整日志行。
- 影响：近期错误卡片只展示时间、路径、模型、提供商、状态码、鉴权结果和消息，却会额外加载请求体、响应体、trace、usage 等大字段。
- 修复：近期错误查询增加 `load_only()`，仅加载 `ApiKeyRecentErrorOut` 所需字段。

### 245. API Key 分析 limit 只做下限不做服务层上限（已完成）

- 位置：`app/services/api_key_admin_service.py`
- 问题：`get_analytics()` 对 `model_limit` 和 `recent_error_limit` 使用 `max(1, limit)`，服务层缺少上限。
- 影响：若内部调用绕过路由参数限制，可能请求过大的模型分布或近期错误列表，放大分组查询与 ORM 对象构造成本。
- 修复：新增 `API_KEY_ANALYTICS_MODEL_LIMIT=50` 与 `API_KEY_ANALYTICS_RECENT_ERROR_LIMIT=50`，服务层统一裁剪 limit。

### 246. API Key 近期用量窗口缺少服务层上限（已完成）

- 位置：`app/services/api_key_admin_service.py`
- 问题：`get_recent_usage()` 只保证 `window_hours >= 1`，未限制最大统计窗口。
- 影响：内部调用传入异常大窗口时，会让近期用量聚合扫描过长时间范围，影响数据库响应速度。
- 修复：新增 `API_KEY_RECENT_USAGE_MAX_WINDOW_HOURS=720`，服务层将窗口限制在 1-720 小时。

### 247. API Key 全量列表服务缺少内部最大返回上限（已完成）

- 位置：`app/services/api_key_admin_service.py`
- 问题：`list_api_keys()` 在 `limit=None` 时可加载全部 API Key；传入 `limit` 时也只做下限处理。
- 影响：若内部调用未走路由参数限制，可能一次性加载并预取大量 API Key、owner 和 provider binding 对象。
- 修复：新增 `API_KEY_LIST_MAX_LIMIT=5000`，服务层无论是否传入 limit 都按最大 5000 条裁剪。

## 第四十九批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\api_key_admin_service.py app\routers\api_keys.py`
- 结果：通过，API Key 管理服务和 API Key 路由语法编译成功。
- 已执行：`git diff --check -- app\services\api_key_admin_service.py app\routers\api_keys.py 6-8性能审查.md`
- 结果：通过，仅提示工作区 LF/CRLF 自动转换警告，无空白错误。
- 已执行：源码级检查 API Key 导出跳过 raw key 解密、近期错误 `load_only()`、分析 limit 服务层上限、近期用量窗口上限、API Key 列表内部最大返回上限，以及第 243-247 项文档完成标记。
- 结果：通过，第 243-247 项 API Key 管理导出、分析与内部服务调用资源边界问题均已落地。
- 解决策略：采用“导出跳过无用解密 + 近期错误字段裁剪 + 服务层 limit/window 硬上限”的组合策略，降低 API Key 管理导出、分析和内部服务调用在大数据场景下的 CPU、内存与数据库扫描压力。

## 第五十批问题与修复

### 248. 提供商下拉选项加载完整提供商模型对象（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`list_provider_option_dicts()` 只需要提供商摘要字段与挂载模型名，却通过 `list_providers()` 加载完整 `Provider` 和 `ProviderModel` 对象。
- 影响：提供商和模型挂载数量增长时，筛选器、下拉框等高频入口会额外读取价格、健康、探针、容量等无关字段，增加数据库传输和 ORM 构造成本。
- 修复：新增 `_list_providers_for_model_name_lists()`，仅加载 provider 摘要字段和 `ProviderModel.model_name`，下拉选项改用该轻量查询。

### 249. 提供商概览轻量列表仍加载完整模型对象（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`list_provider_summary_dicts()` 作为概览轻量列表，仍复用完整提供商列表查询。
- 影响：概览页只展示模型名集合、状态和少量指标，却会把挂载模型的完整字段一起加载，拖慢概览刷新。
- 修复：概览轻量列表复用 `_list_providers_for_model_name_lists()`，按摘要序列化所需字段读取数据。

### 250. 提供商模型发现重复推断同一模型能力（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`discover_models()` 构造每个发现模型时，对同一个 `model_name` 连续调用 `_infer_model_capabilities()` 三次。
- 影响：模型列表较长时会重复执行字符串归类与规则判断，增加 CPU 消耗。
- 修复：列表推导中对每个模型只计算一次 `capabilities`，再复用其中的 stream、vision 和 tools 判断结果。

### 251. 近期内容防护事件加载完整事件与日志 ORM 行（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`_build_recent_content_guard_events()` 查询 `RequestContentGuardEvent` 和 `RequestLog` ORM 对象，但结果只需要事件摘要和日志模型、trace、provider id。
- 影响：提供商详情或概览读取近期内容防护事件时，会额外加载请求日志大字段和事件无关字段，增加内存与响应延迟。
- 修复：查询改为显式选择事件摘要字段和日志关联摘要字段，并直接从结果行构造响应。

### 252. 模型目录授权范围查询与清理读取过多 API Key / 模板数据（已完成）

- 位置：`app/services/model_catalog_service.py`
- 问题：`_collect_user_route_scopes()` 加载用户完整 API Key 行和完整 provider binding；`_remove_model_from_authorization_scopes()` 遍历全部 API Key 与策略模板再解析 JSON 判断是否包含模型。
- 影响：用户密钥、模板或授权范围较多时，模型目录展示和删除模型后的授权清理会产生不必要的全行读取、全表遍历和 JSON 解析成本。
- 修复：用户范围查询增加 `load_only()` 并只加载绑定 provider id；授权清理先按 `allowed_model_names_json.contains(model_name)` 过滤候选，再仅加载改写 JSON 与缓存失效所需字段。

## 第五十批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\provider_service.py app\services\model_catalog_service.py`
- 结果：通过，提供商服务和模型目录服务语法编译成功。
- 已执行：`git diff --check -- app\services\provider_service.py app\services\model_catalog_service.py 6-8性能审查.md`
- 结果：通过，仅提示工作区 LF/CRLF 自动转换警告，无空白错误。
- 已执行：源码级检查提供商轻量选项与概览查询、模型发现能力推断复用、近期内容防护事件字段选择、模型目录用户授权范围轻量读取和模型授权清理候选过滤，以及第 248-252 项文档完成标记。
- 结果：通过，第 248-252 项提供商轻量列表、内容防护事件和模型目录授权范围资源占用问题均已落地。
- 解决策略：采用“轻量字段加载 + 单次能力推断 + 关联摘要行选择 + 授权 JSON 候选过滤”的组合策略，降低提供商列表、模型发现、内容防护事件和模型目录授权清理入口的数据库传输、ORM 构造与 CPU 解析成本。

## 第五十一批问题与修复

### 253. Playground 提供商列表加载完整提供商对象（已完成）

- 位置：`app/services/provider_service.py`
- 问题：`list_provider_playground_dicts()` 只需要提供商摘要、Base URL 和模型能力字段，却继续调用 `list_providers()` 读取完整提供商与模型挂载对象。
- 影响：Playground 页面刷新时会额外加载价格、容量、探针、健康详情等无关字段，提供商模型较多时增加数据库传输和 ORM 构造成本。
- 修复：新增 `_list_providers_for_playground_lists()`，仅加载 Playground 序列化所需 provider 字段和模型能力字段。

### 254. 全部健康检查按提供商懒加载模型挂载（已完成）

- 位置：`app/services/health_service.py`
- 问题：`check_all()` 调用 `_scheduled_enabled_providers(..., include_models=False)` 后，又在后续逻辑访问每个 `provider.provider_models`。
- 影响：全部健康检查会退化为按提供商逐个懒加载模型挂载，提供商数量较多时产生 N+1 查询并拖慢检查启动。
- 修复：`check_all()` 改为一次性预加载 provider models，避免健康检查构建阶段逐个触发懒加载。

### 255. 流式全部健康检查绕过提供商扫描上限（已完成）

- 位置：`app/services/health_service.py`
- 问题：`check_all()` 在存在 `progress_callback` 时不应用扫描上限，`/api/providers/test-all-stream` 可触发全部启用提供商检查。
- 影响：提供商数量较多时，手动流式检查可能一次性占用大量上游连接、数据库会话和运行时间，影响后台稳定性。
- 修复：新增 `MANUAL_PROVIDER_SCAN_LIMIT=200`，手动流式和非流式全部健康检查都继续按上限读取并裁剪 provider 列表。

### 256. 健康检查流式响应客户端断开后后台任务继续运行（已完成）

- 位置：`app/routers/providers.py`
- 问题：`_stream_health_check_events()` 未检测 `request.is_disconnected()`，浏览器关闭或网络断开后仍等待后台健康检查自然结束。
- 影响：用户端已不再接收结果时，后台任务仍可能持续探测上游并占用手动检查槽位、数据库会话和上游连接。
- 修复：流式生成器接收 `Request`，轮询检测客户端断开；断开或生成器退出时取消后台任务，并在 `finally` 中释放手动检查槽位。

### 257. 健康检查进度队列满时反压探测任务（已完成）

- 位置：`app/routers/providers.py`
- 问题：进度队列满时，reporter 丢弃一条旧事件后使用 `await queue.put(payload)`，慢客户端仍可能让健康检查任务等待队列可写。
- 影响：网络慢或前端读取不及时会把响应侧背压传递到实际健康检查任务，拉长探测耗时并增加连接占用。
- 修复：新增非阻塞 `offer_event()`，队列满时丢弃旧事件后用 `put_nowait()` 投递新事件；完成、错误和结束哨兵也统一非阻塞投递。

## 第五十一批验证结果

- 已执行：`.\.venv\Scripts\python.exe -m compileall app\services\provider_service.py app\services\health_service.py app\routers\providers.py`
- 结果：通过，提供商服务、健康检查服务和提供商路由语法编译成功。
- 已执行：`git diff --check -- app\services\provider_service.py app\services\health_service.py app\routers\providers.py 6-8性能审查.md`
- 结果：通过，仅提示工作区 LF/CRLF 自动转换警告，无空白错误。
- 已执行：源码级检查 Playground 提供商轻量查询、全部健康检查模型预加载、手动全部健康检查扫描上限、流式客户端断开取消后台任务、进度队列非阻塞投递，以及第 253-257 项文档完成标记。
- 结果：通过，第 253-257 项提供商 Playground 列表和健康检查流式/批量稳定性问题均已落地。
- 解决策略：采用“Playground 字段裁剪 + 健康检查预加载模型 + 手动扫描硬上限 + 客户端断开取消 + 非阻塞进度投递”的组合策略，降低提供商调试页和健康检查链路的查询数量、连接占用与慢客户端反压风险。
