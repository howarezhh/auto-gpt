# 对话模型兼容性审查矩阵

更新时间：2026-06-05

## 审查范围

- 仅审查对话模型外部调用能力。
- 本轮重点覆盖：文本对话、流式文本、工具调用、可透传的思维/推理扩展字段。
- 历史矩阵中保留的图片理解结论仅作为既有能力记录，本轮不把多模态能力作为完成门槛。
- 不覆盖：Embedding、图片生成、视频、音频等非本次目标能力。
- GPT 系列按用户要求不再重复审查，视为已支持。

## 项目当前对外能力边界

- 对外正式入口为 `POST /v1/chat/completions` 与 `POST /v1/responses`。
- Chat/Responses 自动端点互转 fallback 已禁用；正式路由要求上游原生支持请求端点。
- `/v1/responses` 到 Chat-only 上游的兼容能力仅通过显式开启的 Responses→Chat 单向适配提供；该适配需要共享会话存储承载 `previous_response_id` 状态。
- Kimi/Moonshot 视觉模型若输入普通远程图片 URL，代理层会在发往上游前自动转成 base64 data URL。

## 关键实现证据

- 模型家族能力推断：
  - `app/services/provider_service.py`
  - `_model_name_supports_vision`
  - `_model_name_supports_tools`
  - `_model_name_supports_responses`
  - `_infer_model_capabilities`
- 端点协议判断、显式 Responses→Chat 适配与供应商差异适配：
  - `app/services/proxy_service.py`
  - `_route_endpoint_requirements`
  - `_build_endpoint_fallback_request`
  - `app/services/responses_chat_adapter_service.py`
  - `_normalize_provider_request_payload`
  - `_provider_requires_base64_image_urls`
  - `_convert_chat_completion_to_responses_payload`
  - `_convert_responses_payload_to_chat_completion`
- 工具与视觉可用性探针：
  - `app/services/health_service.py`
  - `_probe_native_tools`
  - `_probe_native_vision`
  - `_build_chat_tool_probe_payload`
  - `_build_responses_tool_probe_payload`

## 官方协议依据

- DeepSeek：
  - `https://api-docs.deepseek.com/`
  - `https://api-docs.deepseek.com/guides/tool_calls`
- 通义 / 百炼：
  - `https://help.aliyun.com/zh/model-studio/compatibility-of-openai-with-dashscope`
  - `https://help.aliyun.com/zh/model-studio/compatibility-with-openai-responses-api`
  - `https://help.aliyun.com/zh/model-studio/image-understanding`
- 智谱 / GLM：
  - `https://docs.bigmodel.cn/cn/guide/develop/openai/introduction`
  - `https://docs.bigmodel.cn/cn/guide/models/text/glm-4.5`
  - `https://docs.bigmodel.cn/cn/guide/models/multimodal/glm-4v-plus`
- 豆包 / 火山方舟：
  - `https://www.volcengine.com/docs/82379/1330310`
  - `https://www.volcengine.com/docs/82379/1330626`
  - `https://www.volcengine.com/docs/82379/1330773`
- Kimi / Moonshot：
  - `https://platform.moonshot.ai/docs/guide/use-kimi-k2`
  - `https://platform.moonshot.ai/docs/guide/use-kimi-vision-model`
- 小米 / MiMo：
  - `https://platform.xiaomimimo.com/docs/en-US/api/chat/openai-api?target=request-body`
  - `https://platform.xiaomimimo.com/docs/en-US/usage-guide/passing-back-reasoning_content`

## 支持矩阵

| 模型家族 | 文本对话 | 工具调用 | 图片理解 | 原生 chat/completions | 原生 responses | 项目当前支持方式 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DeepSeek | 支持 | 支持 | 官方公开文档未见稳定证据，项目按不支持处理 | 是 | 否 | chat 原生；Responses 仅可通过显式 Responses→Chat 单向适配接入 | `deepseek-*` 默认不声明 vision / responses |
| 通义 / Qwen | 支持 | 支持 | 支持 | 是 | 是 | chat / responses 原生 | 已把 `qwen*` 与 `qvq*` 默认纳入 responses 原生能力 |
| 智谱 / GLM | 支持 | 支持 | 支持 | 是 | 官方公开协议证据不足，项目默认按否处理 | chat 原生；Responses 仅可通过显式 Responses→Chat 单向适配接入 | `glm*` 默认不声明原生 responses，避免把适配能力误展示为原生能力 |
| 豆包 / Doubao | 支持 | 支持 | 支持 | 是 | 是 | chat / responses 原生 | `doubao-vision-*` 默认声明 vision |
| Kimi / Moonshot | 支持 | 支持 | 支持 | 是 | 否 | chat 原生；Responses 仅可通过显式 Responses→Chat 单向适配接入；视觉请求自动做 data URL 规范化 | 已补齐 Kimi 图片 URL -> base64 data URL 适配 |
| 小米 / MiMo | 支持 | 支持 | 支持 | 是 | 否 | chat 原生；Responses 仅可通过显式 Responses→Chat 单向适配接入 | `mimo-*` 默认声明 tools；Chat 上游按官方字段使用 `max_completion_tokens` |
| 其他 OpenAI-compatible 对话模型 | 取决于上游 | 取决于上游 | 取决于上游 | 取决于上游 | 取决于上游 | 通过通用 `openai_compatible` provider 接入；正式路由不自动互转端点 | 复杂 Responses 状态字段需原生端点或显式适配承载 |

## 本次补齐内容

1. 扩展模型家族默认能力推断，补齐 DeepSeek、Qwen、GLM、Doubao、Kimi、MiMo 的 tools / vision / responses 识别。
2. 禁用正式路由中的 Chat/Responses 自动端点互转 fallback；Responses→Chat 仅保留为显式开启的单向兼容适配。
3. 为 Kimi/Moonshot 视觉链路新增远程图片 URL 转 base64 data URL 的上游规范化。
4. 新增离线回归脚本：
   - `stage22_model_family_compat_regression_check.py`
   - `stage23_provider_payload_adapter_regression_check.py`
   - `stage24_family_support_matrix_regression_check.py`
5. 根据小米 MiMo 官方 OpenAI 兼容 Chat 协议，将 MiMo Chat 上游请求中的 `max_tokens` 映射为 `max_completion_tokens`，避免可用性探针和普通文本调用因字段不匹配被误判失败。

## 本地验证

在项目根目录使用项目 `.venv` 执行：

```powershell
& .\.venv\Scripts\python.exe .\stage22_model_family_compat_regression_check.py
& .\.venv\Scripts\python.exe .\stage23_provider_payload_adapter_regression_check.py
& .\.venv\Scripts\python.exe .\stage24_family_support_matrix_regression_check.py
```

预期结果均为 `passed`。
