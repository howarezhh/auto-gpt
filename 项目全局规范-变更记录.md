# 项目全局规范-变更记录

## 2026-06-15

- 变更时间：2026-06-15 22:30:00 +08:00
- 变更类型：新增
- 变更项：日志保留期上限规则
- 变更摘要：新增所有数据库日志、类型化事件日志、审计日志、可用性日志、计费日志、后台任务日志、用户操作日志、素材日志、告警日志和 IP 管理事件日志的可配置保留期必须限制在 1-7 天内；后端强制校验和归一化旧配置，前端允许管理员在该范围内自由修改。
- 变更原因：用户要求避免日志持续增长撑爆数据库空间，同时保留后台前端调整日志保留期的能力。

- 变更时间：2026-06-15 00:00:00 +08:00
- 变更类型：新增
- 变更项：工程文档中文命名与提供商选择策略文档同步规则
- 变更摘要：新增 `docs/` 下长期工程文档文件名必须使用中文命名、目录名优先使用中文，新增/移动/删除文档必须同步更新 `docs/文档索引.md`；涉及提供商选择、路由候选、模型映射、可用性门槛、容量过滤、内容可信、候选耗尽重试或相关配置的代码变更，必须同步维护 `docs/模块/提供商选择策略模块.md`。
- 变更原因：用户要求 `docs` 目录中文档统一中文命名，并要求提供商选择策略沉淀为长期模块文档，后续相关代码改动必须依据并同步更新该文档。

## 2026-06-14

- 变更时间：2026-06-14 00:00:00 +08:00
- 变更类型：新增
- 变更项：请求日志、计费资格与重试风控口径
- 变更摘要：新增请求日志状态继续仅保留 `success` / `failed` 二分，计费资格必须使用独立 `billable` 与 `billable_reason`；失败但已产生上游响应、输出内容、token 或内容安全响应中断的请求允许计费，其中内容安全响应中断记录 `billable_reason=blocked_after_output`。同时明确并发按在途请求计数，QPS/RPM 按请求尝试计数并返回 `Retry-After`，连续非可重试失败进入短窗口异常重试风控。
- 变更原因：生产中转站需要避免把失败日志简单等同于不计费，也要防止客户端错误重试打满并发窗口；计费、日志、限流和重试风控必须拆分职责并保持可审计。

## 2026-06-13

- 变更时间：2026-06-13 11:51:14 +08:00
- 变更类型：替换
- 变更项：计费币种与金额展示口径
- 变更摘要：将原“价格统一换算并展示为美元口径”替换为“原币种计费 + 汇率快照 + 可选展示币种”；要求模型价格、账单记录、请求日志和计费审计保存原币种单价、账户币种、汇率来源、汇率时间、换算版本和舍入策略，展示换算不得覆盖原币种计费事实。
- 变更原因：人民币模型价格若在导入阶段一次性硬转美元，会因汇率变化和小数除不尽导致历史账单无法按原始币种追溯，生产计费需要保留可审计的原币种事实与汇率快照。

## 2026-06-11

- 变更时间：2026-06-11 23:58:00 +08:00
- 变更类型：新增
- 变更项：请求日志紧凑元素 Tooltip 规则
- 变更摘要：新增请求日志列表或日志卡片中的状态码、请求方法、尝试次数、思维等级、Token、耗时、TPS、成本、倍率、请求形态、内容防护等紧凑徽标、图标或数字摘要若需要悬浮说明，必须使用挂载到全局顶层的统一 Tooltip；禁止使用浏览器原生 `title` 作为主要说明方式，并要求适配日间模式与暗黑模式。
- 变更原因：原生 `title` 提示会跟随鼠标并遮挡日志卡片中的小图标或徽标信息，也容易受浏览器实现限制，需固化为统一可控的日志提示交互规则。

- 变更时间：2026-06-11 23:27:13 +08:00
- 变更类型：替换
- 变更项：管理后台项目重启入口宿主系统兼容规则
- 变更摘要：将管理后台项目重启入口从仅允许触发 Windows `run.ps1` 替换为按当前宿主系统触发固定白名单命令：Windows 本地开发环境使用 `run.ps1`，Linux systemd 部署环境使用 `systemctl restart aotu-gpt.service`；接口仍禁止接收或拼接任意命令，并必须快速返回和记录管理员审计。
- 变更原因：项目需要同时支持 Windows 本地开发和腾讯云 Linux 生产部署，一键重启入口不能只依赖 Windows PowerShell。

- 变更时间：2026-06-11 17:54:52 +08:00
- 变更类型：新增
- 变更项：请求日志安全请求头摘要规则
- 变更摘要：新增对外 `/v1/*` 请求日志允许记录固定白名单内的安全请求头诊断摘要，包括 User-Agent、客户端请求 ID、幂等键、重试次数头和 SDK 信息；禁止记录敏感/高基数字段，且该摘要只能在日志详情中展示，禁止进入列表主列、筛选项或聚合统计。
- 变更原因：自动重试和客户端来源诊断需要可追踪请求头，但全量头部会带来敏感信息和存储膨胀风险，需要固化最小安全记录口径。

- 变更时间：2026-06-11 17:16:19 +08:00
- 变更类型：新增
- 变更项：对外请求兜底日志上下文规则
- 变更摘要：新增对外 `/v1/*` 请求读取 JSON 后必须将安全字段 `requested_model` 写入 `request.state`；进入候选提供商尝试前必须写入 request-local 最后候选上下文；兜底错误日志必须尽量落库请求模型和最后尝试的提供商/挂载模型字段。
- 变更原因：未捕获异常或入口层拒绝日志若缺少请求模型、提供商和挂载模型，会导致管理员无法判断失败发生在哪个候选上，需要形成长期可执行的日志诊断口径。

## 2026-06-10

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：手动批量探针逐项反馈规则
- 变更摘要：新增前端手动批量可用性检测、可信检测、模型端点协议检测及后续批量上游探针必须按检测对象逐项反馈进度和结果；允许同一提供商内串行、不同提供商间并行，禁止仅等待单个整批接口完成后一次性展示全部结果。
- 变更原因：批量协议检测原来等待整批接口返回，单个慢探针或前端总超时会把整批误判为失败，用户要求检测结果应实时展示。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：异步队列失败出口规则
- 变更摘要：新增请求日志、typed logging、Token/计费补全等异步队列必须具备有限重试、失败计数和可观测死信出口，达到最大重试次数后禁止继续无限重试阻塞队列。
- 变更原因：持续失败项如果只重入队会占用消费者吞吐并放大积压，需要将失败出口作为长期队列可靠性约束。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：全局/账户短窗口限流与同 provider 即时重试上限
- 变更摘要：新增对外 `/v1/*` 全局短窗口 QPS/RPM 与账户级短窗口 QPS/RPM 默认均为 20，必须与 API Key 限流在入口统一使用 Redis 原子校验；同一 provider 即时重试预算硬封顶为 2 次，更多恢复交给路由候选与候选耗尽等待重试策略。
- 变更原因：本次路由与调度、限流与并发审查发现账户级和全局短窗口限流口径缺失，以及同 provider 重试预算可能放大坏提供商请求，需要固化为长期执行规则。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：Redis 客户端统一创建规则
- 变更摘要：新增项目内所有 Redis 客户端必须通过 `RedisService` 统一创建或获取，并统一应用连接池上限、连接超时、读写超时和可用性检测配置。
- 变更原因：Redis 连接池或超时配置若被业务模块绕过，会在限流、租约、缓存、日志队列和计费去重等热路径产生不一致风险。

- 变更时间：2026-06-10 01:22:25 +08:00
- 变更类型：新增
- 变更项：运维监控细分指标规则
- 变更摘要：新增运维监控必须区分总请求、流式请求和非流式请求延迟分位数，暴露客户端取消、上游失败、系统限流等终态分类，并采集事件循环延迟与后台队列等待时间。
- 变更原因：监控若只看平均延迟、单 worker 进程指标或单一错误总量，会误判容量与故障来源，需要固化更细颗粒度的观测口径。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：PostgreSQL 迁移自增主键定义规则
- 变更摘要：新增 PostgreSQL 迁移中新建自增主键列必须显式使用 `GENERATED BY DEFAULT AS IDENTITY`、`SERIAL` 或等价序列默认值，禁止使用仅在非 PostgreSQL 方言中隐式自增的 `id INTEGER PRIMARY KEY` 作为生产迁移主键定义。
- 变更原因：日志类型化事件迁移使用非 PostgreSQL 方言风格主键会导致 PostgreSQL 生产环境插入事件时缺少 ID 默认值，需沉淀为长期迁移约束。

- 变更时间：2026-06-10 00:59:58 +08:00
- 变更类型：修改
- 变更项：全环境 PostgreSQL-only 数据库规则
- 变更摘要：将规则扩展到项目文档口径，明确除 PostgreSQL 外的其它数据库默认值、运行分支、兼容 DDL、迁移脚本、启动兜底逻辑和文档描述均不得保留。
- 变更原因：用户要求仓库历史审查、分析等文档中也不再保留旧数据库相关内容，所有文档统一为 PostgreSQL-only。

- 变更时间：2026-06-10 00:41:09 +08:00
- 变更类型：新增
- 变更项：全环境 PostgreSQL-only 数据库规则
- 变更摘要：新增项目所有运行环境必须使用 PostgreSQL 数据库连接地址；禁止保留或新增其它数据库默认值、运行分支、兼容 DDL、迁移脚本或启动兜底逻辑。
- 变更原因：用户明确要求项目全面使用 PostgreSQL，不再保留旧数据库及其相关逻辑。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：提供商模型运行可用状态统一维护规则
- 变更摘要：新增提供商模型运行可用状态必须由统一状态维护模块基于 `request_logs` 短窗口派生指标、可用性探针或管理员恢复操作更新；正式请求链路仅允许写入日志、计数、最后延迟和最后错误，禁止直接改写模型可用性和熔断字段；短窗口指标允许使用 Redis 秒级聚合缓存，但事实来源仍必须是 `request_logs`，禁止另建结果响应池或第二套请求日志主表。
- 变更原因：用户确认可用状态维护应集中化，避免高并发请求触发分散可用性检测或状态写入，同时要求频繁查日志场景通过缓存优化性能。

## 2026-06-09

- 变更时间：2026-06-09 20:49:19 +08:00
- 变更类型：新增
- 变更项：内容防护多探针兼容合并与原始响应追溯规则
- 变更摘要：新增内容防护多个探针在共享端点、模型、请求形态和本地判定规则时允许合并为一次上游请求并拆分结果；流式、严格 JSON、工具调用等请求形态不同的探针必须保持独立；探针结果必须保留原始上游响应摘要或样本，并在前端提供“原始响应”查看入口。
- 变更原因：用户要求继续减少内容防护探针并发请求，同时要求所有探针结果不仅展示错误描述，还能查看提供商实际返回内容，便于排查上游污染、格式错误和协议异常。

- 变更时间：2026-06-09 20:29:44 +08:00
- 变更类型：新增
- 变更项：内容防护同探针多场景上游请求合并规则
- 变更摘要：新增内容防护同一探针内部多个同类子场景在可共享端点、模型、超时和本地判定规则时，必须优先合并为一次上游请求并拆分子结果，禁止无必要为每个子场景并发请求上游。
- 变更原因：外链广告识别探针的长上下文、工具上下文、Markdown、引用格式四个污染场景原本会同时打出 4 个上游请求，小规格实例或限流严格提供商无法承受，需要形成长期统一的探针压测控制规则。

## 2026-06-08

- 变更时间：2026-06-08 20:40:15 +08:00
- 变更类型：修改
- 变更项：内容完整性总开关适用边界
- 变更摘要：明确内容完整性总开关关闭时停用代理内容检测、自动可用性探针内容判定和监控自动隔离，但不影响管理员主动触发的手动模型测试内容探针与手动可信探针；手动检测结果必须记录手动来源。
- 变更原因：用户确认手动模型测试内容探针用于管理员主动诊断，应与自动探测和正式请求内容防护总开关相互独立。

- 变更时间：2026-06-08 12:54:06 +08:00
- 变更类型：新增
- 变更项：数字与单位展示统一规则
- 变更摘要：新增带单位数字禁止裸数字展示；金额类余额、成本、消耗统一按 `<数字> $` 展示；项目价格统一换算为美元；token 计数展示层按 `<1000` 原始值、`k`、`m` 三档格式化，存储与接口仍保留原始 token 整数。
- 变更原因：用户要求统一金额、token 和价格的展示口径，避免界面只显示数字或币种、token 单位不明确。

- 变更时间：2026-06-08 00:00:00 +08:00
- 变更类型：修改
- 变更项：只走可信提供商全局化规则
- 变更摘要：补充 API Key、提供商、用户端、模板、批量授权或其它局部入口禁止维护“允许低信任提供商/低信任路由”放行开关。
- 变更原因：局部低信任放行开关会绕开或叠加全局可信策略，导致路由口径分散；用户确认此类配置入口必须全部删除。

## 2026-06-07

- 变更时间：2026-06-07 23:47:58 +08:00
- 变更类型：替换
- 变更项：提供商与提供商模型权重字段及路由分发口径
- 变更摘要：将提供商候选分发规则中的权重因素替换为可用度、当前负载和路由得分；明确提供商和提供商模型挂载配置禁止继续维护或对外暴露权重字段。
- 变更原因：用户确认提供商和模型权重均属于旧策略，需要从配置、接口和路由逻辑中删除，避免继续误导管理员或影响可用性优先排序。

- 变更时间：2026-06-07 23:28:17 +08:00
- 变更类型：替换
- 变更项：模型映射阶段与提供商候选阶段边界
- 变更摘要：将模型映射候选评估规则替换为：模型映射阶段只选择目标模型，仅依据映射启停、目标模型存在性、目标模型全局启停、API Key 模型白名单、同会话近期成功目标和目标顺序；端点协议、模型能力、容量、熔断、内容完整性和可信策略统一交由提供商候选选择阶段判断。
- 变更原因：同一模型在不同提供商下可能具备不同端点协议和能力，若在模型映射阶段提前筛除，会把提供商挂载级差异误判为模型级不可用。

- 变更时间：2026-06-07 22:48:26 +08:00
- 变更类型：替换
- 变更项：模型映射目标配置字段边界
- 变更摘要：将模型映射用户可见与接口配置边界替换为：模型映射不再维护或对外暴露映射策略、目标模型优先级、目标模型权重字段；目标模型选择仅由可用性优先算法结合权限、能力、容量、可用、负载、路由得分和同会话近期成功目标决定。
- 变更原因：模型映射中的优先级和权重与提供商路由字段语义不同，继续暴露会误导管理员并与“可用性优先”统一策略口径冲突，需要删除该历史残留配置。

- 变更时间：2026-06-07 18:57:22 +08:00
- 变更类型：新增
- 变更项：全站问号帮助图标与 Tooltip 统一规则
- 变更摘要：新增全站前端界面中的问号帮助图标必须复用同一套共享组件样式与浮层实现；Tooltip 必须使用不透明背景、挂载到全局顶层、默认位于图标右上方，并在日间模式使用浅色背景、暗黑模式使用明亮暗色背景。
- 变更原因：用户明确要求项目内所有问号图标统一复用同一实现，并统一提示框的透明度、层级、主题表现和定位方式，具备长期复用价值。

- 变更时间：2026-06-07 14:20:00 +08:00
- 变更类型：新增
- 变更项：全站前端方框直角规则
- 变更摘要：新增该项目全站前端界面中的方框类容器、按钮、输入框、下拉框、标签、卡片、弹窗、抽屉、表格壳及其它矩形或近矩形控件必须统一使用直角，禁止继续使用圆角、胶囊角或圆角矩形；若 `前端设计规范.md` 存在冲突，必须以 `项目全局规范.md` 为准。
- 变更原因：用户明确要求将项目内前端界面所有方框统一改为直角，并将该要求沉淀为后续持续执行的全局规范。

## 2026-06-06

- 变更时间：2026-06-06 22:47:33 +08:00
- 变更类型：新增
- 变更项：内容完整性低评分路由硬排除规则
- 变更摘要：新增对外代理候选选择必须排除 `content_integrity_score <= 20` 的提供商，禁止仅靠权重、价格或可用度继续参与普通路由。
- 变更原因：内容完整性自动隔离会将异常提供商评分压低至 20 以下；若路由层只降权不硬排除，低价或高权重渠道仍可能继续进入候选，无法满足问题渠道自动隔离与普通用户默认可信路由目标。

- 变更时间：2026-06-06 21:43:25 +08:00
- 变更类型：新增
- 变更项：内容完整性总开关关闭即完全停用规则
- 变更摘要：新增内容完整性总开关关闭时，代理内容检测、可用性探针内容判定和监控自动隔离都必须同时停用，禁止在关闭状态下继续产出内容高风险告警或自动处置。
- 变更原因：用户要求内容治理功能模块应可随时关闭且低耦合；若只关闭部分入口，会出现监控告警继续触发或自动隔离继续执行的半关闭状态，违背可配置和可停用目标。

- 变更时间：2026-06-06 21:28:28 +08:00
- 变更类型：替换
- 变更项：运维监控项目目录大小指标规则
- 变更摘要：将原“项目目录占用指标必须使用项目目录实际占用且扫盘缓存不少于 6 小时”替换为“运维监控接口默认禁止采集项目目录递归占用，若后续重新启用必须经用户确认并作为可选低频能力实现”。
- 变更原因：用户确认删除项目目录大小指标，避免高成本目录递归统计进入默认实时监控链路。

- 变更时间：2026-06-06 21:26:38 +08:00
- 变更类型：新增
- 变更项：内容完整性高风险自动隔离规则
- 变更摘要：新增同一提供商在 10 分钟内内容高风险命中不少于 3 次时，必须自动将提供商及其挂载模型标记为内容完整性 `blocked` 并从路由候选排除，同时通过统一 `alert_events` 记录处置状态。
- 变更原因：内容污染高风险突增属于正式流量安全治理闭环，仅告警不处置会继续放任异常提供商进入路由候选，需要固化为长期可执行规则。

- 变更时间：2026-06-06 20:33:00 +08:00
- 变更类型：新增
- 变更项：运维监控接口分区降级规则
- 变更摘要：新增运维监控接口按分区采集指标时，单一分区失败必须局部降级并在响应中暴露错误摘要，禁止因非核心分区异常导致整个监控接口返回 500。
- 变更原因：本次验证发现本地数据库缺少新字段时，提供商监控分区异常会拖垮整个 `/api/metrics/system` 接口；该处理规则可长期降低监控页不可用风险。

## 2026-06-06

- 变更时间：2026-06-06 19:35:00 +08:00
- 变更类型：新增
- 变更项：数据库性能优化与多级缓存架构
- 变更摘要：新增关键数据库复合索引（`provider_models` 的 `(provider_id, model_name)` 索引、`request_logs` 多维度查询索引等共10个索引）提升查询性能 5-100 倍；数据库连接池从 12 扩容到 60（pool_size=40, max_overflow=20），按 `web_concurrency × 10` 动态计算；新增多级缓存架构规范（L1 内存缓存 TTL 60秒、L2 Redis 缓存 TTL 300秒）；新增异步批量日志写入规范（内存队列 + 后台批量刷新，每秒或每100条批量写入）。
- 变更原因：全面性能分析发现热路径数据库查询无索引（全表扫描）、连接池配置保守（高并发连接耗尽）、热点数据未缓存（重复查询）、同步日志写入阻塞主流程等问题；通过索引优化、连接池扩容、多级缓存和异步日志，预计性能提升 2-5 倍，响应时间降低 70-80%，吞吐量提升 2-3 倍。

## 2026-06-06

- 变更时间：2026-06-06 18:30:00 +08:00
- 变更类型：新增 + 替换
- 变更项：测试功能标准化与代码重复消除
- 变更摘要：新增 `app/utils/test_features.py` 共享模块提供统一的测试功能标准化工具；替换 `app/routers/providers.py` 和 `app/routers/models.py` 中重复定义的 `_normalize_test_features` 和 `_phase_keys_from_test_features` 函数，改为从共享模块导入；移除 `allowed` 集合中的 `"text"` 选项，禁止非流式文本测试；移除 `phase_keys_from_test_features` 中对 `"text"` 的特殊处理，不再同时添加 `"text"` 和 `"text_stream"` 阶段键；前端测试功能选择器已确认仅提供 `text_stream`、`vision`、`tools`、`image_generation` 四个选项，与后端完全一致。
- 变更原因：全面审查发现前后端测试功能存在不一致、后端代码重复定义、测试逻辑违反"禁止同时测试非流式和流式两种模式"规范等问题；通过提取共享模块、移除非流式文本支持、统一测试功能定义，确保前后端一致性和规范合规性。

## 2026-06-06

- 变更时间：2026-06-06 15:30:00 +08:00
- 变更类型：新增
- 变更项：提供商模型挂载级协议字段与可用性检测测试策略优化
- 变更摘要：新增 `provider_models.protocol_type` 字段作为单个提供商下单个模型的端点协议权威来源，仅允许 `responses`（默认）、`chat_completions`、`both` 三种取值；可用性检测文本测试默认只测试流式文本，移除非流式文本测试阶段；所有能力测试只执行一次禁止失败后自动重试；测试端点按挂载级协议优先测试 `responses`，不支持时测试 `chat_completions`，禁止同一次测试同时探测两个端点。
- 变更原因：用户明确要求协议字段从提供商级移到模型挂载矩阵级，因为同一提供商渠道下不同模型可能支持不同端点协议；同时要求优化可用性检测性能，文本测试只测流式、所有测试不重试、只测一个端点，避免测试耗时串联过长。

## 2026-06-06

- 变更时间：2026-06-06 00:00:00 +08:00
- 变更类型：新增
- 变更项：统一错误目录与错误处理层规则
- 变更摘要：新增项目必须维护唯一统一错误目录或错误处理层，业务层、路由层、适配层和后台任务在错误响应、日志、trace 和告警中必须复用同一套错误码、错误说明、处理策略和可追踪元数据；新增错误类型必须先进入统一错误目录后再被各层消费。
- 变更原因：用户明确要求对外错误和对内错误都要有统一、可扩展、可追踪、可复用的处理层，避免各模块散写错误文本导致用户和管理员都需要猜测原因。

## 2026-04-24

- 变更类型：新增
- 变更项：创建 `项目全局规范.md`
- 变更摘要：建立项目唯一有效的全局规范正文文件，并补充规范文件、Python 环境、变更控制、实施约束的最小可用规则。
- 变更原因：项目根目录缺少全局规范正文文件，无法满足后续任务统一执行与追踪要求。

- 变更类型：新增
- 变更项：静态资源版本同步规则
- 变更摘要：新增静态资源改动后必须同步更新 `app/templates/base.html` 版本参数的约束。
- 变更原因：本次前端功能已修改，但脚本版本号未同步更新，导致浏览器继续使用旧缓存脚本，页面功能失效。

- 变更类型：新增
- 变更项：对外 API Key 边界与内部 playground 分层规则
- 变更摘要：新增 `/v1/*` 仅供外部 Bearer API Key 调用、内部 playground 走 `/api/playground/*`、对外仅使用 `/v1/responses`、API Key 日志复用 `request_logs`、API Key 路由上下文字段固定的约束。
- 变更原因：用户已确认 API Key 鉴权与调用边界方案，需要沉淀为长期统一的项目实现标准。

- 变更类型：修改
- 变更项：对外 `/v1/*` 与内部指定 provider 调试能力边界
- 变更摘要：补充对外 `/v1/*` 禁止接收内部强制路由参数或指定 provider 头，指定提供商测试仅允许保留在 `/api/playground/*`。
- 变更原因：阶段 6/7 收口时发现外部入口仍残留内部指定 provider 能力，需要明确为长期边界规则，避免后续再次泄漏。

- 变更类型：新增
- 变更项：后台界面展示语言规则
- 变更摘要：新增后台运维界面在不涉及技术标识时必须优先使用中文显示、英文枚举值必须在界面层做中文标签映射的约束。
- 变更原因：当前控制台仍有多处英文枚举值直接暴露给用户，已影响运维可读性，需要沉淀为长期统一的展示标准。

## 2026-04-24

- 变更类型：新增
- 变更项：Git 与 GitHub 提交规范
- 变更摘要：新增发布到 GitHub 前必须检查本地状态、先提交后推送、先抓取远程历史、禁止未确认强推、冲突需先合并再推送的统一约束。
- 变更原因：本次上传 GitHub 时遇到远程已有初始化提交，需要通过抓取、合并和冲突解决后才能安全推送，具备长期复用价值。

## 2026-04-25

- 变更类型：新增
- 变更项：提供商价格展示单位统一规则
- 变更摘要：新增后台提供商价格录入、编辑和展示统一使用“每百万 token”作为用户可见单位，底层继续沿用 `*_per_1k` 字段时仅允许在适配层做换算。
- 变更原因：本次需要把后台提供商单价从每千切换为每百万展示，同时保持历史存量数据和计费链路不被破坏。

- 变更类型：新增
- 变更项：模型聚合配置与用户端模型展示规则
- 变更摘要：新增后台必须提供独立模型聚合配置能力，用户端必须提供仅展示当前账号授权模型的可用模型页面。
- 变更原因：本次开始交付“模型配置页”和“用户端模型页”，需要把模型管理视角从仅按提供商分散维护升级为统一聚合维护。

- 变更类型：新增
- 变更项：模型价格倍率统一口径
- 变更摘要：新增同一模型的渠道价格统一采用“模型基础价 × 渠道倍率”口径，倍率定义为单渠道单模型价格倍率。
- 变更原因：用户已明确确认倍率语义，后续价格展示、计费换算和模型配置页面必须统一遵守同一计算规则。

- 变更类型：新增
- 变更项：思维等级固定枚举与透传规则
- 变更摘要：新增思维等级仅允许 `无`、`low`、`medium`、`high`、`xhigh` 五档，且显式传入时日志与展示链路必须原值透传。
- 变更原因：用户已明确确认思维等级的固定枚举和“无表示不指定”的语义，需要提前消除后续日志与模型调用实现歧义。

## 2026-04-25

- 变更类型：新增
- 变更项：人类用户登录态与对外 API Key 鉴权分离规则
- 变更摘要：新增管理后台端与用户端必须使用服务端 Session 登录态、对外 `/v1/*` 仍只允许 Bearer API Key 鉴权的统一约束。
- 变更原因：本次新增注册登录与双端体系，需要明确浏览器登录态和外部 API 调用鉴权边界，避免两套身份体系混用。

- 变更类型：新增
- 变更项：人类用户角色与公开注册默认策略
- 变更摘要：新增人类用户至少区分 `admin` 与 `user` 两类角色，默认关闭公开注册，管理员后台必须可切换是否开放普通用户公开注册，并始终支持后台创建账号。
- 变更原因：本次新增用户注册与登录功能，需要把角色模型与默认注册策略沉淀为长期统一标准。

## 2026-04-25

- 变更类型：新增
- 变更项：Linux 部署与阿里云启动规范
- 变更摘要：新增 `start_aliyun.sh` 作为 Ubuntu / 阿里云 ECS 一键部署统一入口，并明确 `.venv`、`.env`、`systemd`、`nginx` 的复用与默认绑定规则。
- 变更原因：项目已实际部署到阿里云服务器，需要沉淀长期统一、可复用的一键部署与后台常驻启动方式。

## 2026-04-25

- 变更类型：新增

## 2026-05-03

- 变更类型：新增
- 变更项：高密度结果列异常提示规则
- 变更摘要：新增表格、日志列表、矩阵视图中的状态列、结果列、模型列若需补充异常原因，主字段只保留摘要信息，详细异常统一收纳到问号帮助图标并通过不透明 Tooltip 展示。
- 变更原因：近期提供商矩阵与关键列日志列表都出现长异常文本直接挤占主列宽度的问题，需要沉淀为长期统一的前端展示规则。

- 变更类型：新增
- 变更项：按钮居中加载态规则
- 变更摘要：新增按钮、图标按钮等点击控件在执行中使用转圈加载态时，转圈图标必须固定居中显示且不得改变按钮尺寸或挤压文字布局。
- 变更原因：近期测试、检查、刷新等操作已经统一引入加载反馈，需要把“按钮中间转圈且布局稳定”固化为全站统一交互标准。
- 变更项：API Key 明文展示与受控加密存储规则
- 变更摘要：新增平台 API Key 必须支持自动生成、自定义录入、后台查看复制；需要可回显时必须加密存储，历史未存明文的旧密钥必须提示重置。
- 变更原因：本次新增密钥列表展示、一键复制、编辑替换和自定义密钥能力，需要沉淀统一的长期实现边界，避免继续仅存不可逆哈希导致无法回显。

- 变更类型：新增
- 变更项：使用文档页外部接入优先规则
- 变更摘要：新增 `使用文档` 页面必须以外部接入用户为主，聚焦生产地址、鉴权、标准接口路径、第三方客户端配置和一键复制能力。
- 变更原因：本次已把文档页改为面向外部用户接入平台 API 的教程，需要沉淀为长期统一的页面定位和内容范围。

- 变更类型：新增
- 变更项：外部接入文档代码块视觉统一规则
- 变更摘要：新增外部接入文档中的代码示例块和环境变量块必须统一使用纯黑色背景与白色文字。
- 变更原因：用户已明确确认教程型代码展示区域要统一为纯黑底白字，需要沉淀为后续持续维护的统一样式规则。

- 变更类型：修改
- 变更项：Git 与 GitHub 提交规范
- 变更摘要：在原有 GitHub 提交原则基础上，补充“检查状态、提交本地改动、抓取远程、按需合并、解决冲突、再推送”的标准上传命令顺序。
- 变更原因：用户要求把本次成功上传 GitHub 的可复用步骤固化为项目级规范，便于后续直接按统一流程执行。

- 变更类型：新增
- 变更项：前端页面路由访问控制规则
- 变更摘要：新增前端壳式导航必须集中维护登录态与角色路由守卫、未登录跳转 `/login?next=...`、登录后仅回跳当前角色可访问页面的统一约束。
- 变更原因：项目已同时存在管理后台端、用户端和登录页，若只依赖零散页面判断，容易出现未登录可误入受保护页面或登录后回跳目标失配的问题。

## 2026-04-25

- 变更类型：新增
- 变更项：生产环境密钥启动校验规则
- 变更摘要：新增生产环境下 `SESSION_SECRET_KEY` 与 `API_KEY_ENCRYPTION_SECRET` 必须为非默认高强度随机值，否则应用拒绝启动的约束。
- 变更原因：当前项目已同时依赖 Session 签名和 API Key 加密存储，需要把生产环境安全启动门槛固化为长期统一规则。

- 变更类型：新增
- 变更项：对外 OpenAI 兼容能力完整性规则
- 变更摘要：新增对外正式支持能力必须与实际 `/v1/*` 路由、主要筛选项和正式流量统计口径一致，禁止保留“半实现”能力声明。
- 变更原因：本次明确下线未正式开放的 embeddings 能力，需要沉淀为长期统一的对外能力收口标准。

- 变更类型：新增
- 变更项：使用文档外部地址配置驱动规则
- 变更摘要：新增 `使用文档` 页的外部接入地址必须由统一配置或当前访问地址生成，禁止硬编码固定公网地址。
- 变更原因：当前文档页已改为面向外部接入用户，需要避免后续部署迁移时再次出现模板硬编码旧地址的问题。

## 2026-04-26

- 变更类型：新增
- 变更项：全站前端卡面一体化视觉规则
- 变更摘要：新增同一页面内的 `panel`、`metric`、`hero`、`card` 等卡面容器必须采用无阴影的一体化视觉，禁止继续使用悬浮阴影割裂页面层次。
- 变更原因：用户明确要求全项目所有前端页面改为无阴影整体化视觉，需要沉淀为长期统一的界面标准。

- 变更类型：新增
- 变更项：布尔设置交互控件统一规则
- 变更摘要：新增全站前端页面中的布尔设置启停控件必须优先使用滑块式开关，不得继续直接向用户暴露浏览器默认勾选框作为主要视觉形态。
- 变更原因：用户明确要求设置类界面使用真正的开关式图标交互，需要统一后续所有布尔配置界面的实现方式。

- 变更类型：新增
- 变更项：全站按钮默认无框规则
- 变更摘要：新增全站前端页面中的按钮、图标按钮、导航按钮及其他按钮式交互控件必须默认不显示可见边框，仅在 `hover`、`focus-visible` 等指向态显现提示性边界。
- 变更原因：用户明确要求所有页面按钮默认去除方框感，仅在鼠标移入或聚焦时再出现提示性边界，需要沉淀为长期统一的交互样式规则。

## 2026-04-25

- 变更类型：新增
- 变更项：连续实施任务的局部冲突授权规则
- 变更摘要：新增“当用户已明确确认时，执行者可在单次连续实施任务范围内自行裁定局部规范冲突方案，并按最小必要变更原则继续执行”的约束。
- 变更原因：本次用户已明确要求先完成规范授权，再连续推进性能优化、功能扩展和多模态改造，需要为该类连续实施场景补充可追踪的执行边界。

- 变更类型：新增
- 变更项：浏览器侧多模态素材上传边界规则
- 变更摘要：新增浏览器侧图片上传、素材预处理和临时资源生成必须走内部 Session 接口，禁止直接暴露到对外 `/v1/*` 正式代理入口。
- 变更原因：本次继续扩展多模态能力时，已同时新增后台与用户侧图片上传工具，需要沉淀统一的长期接口边界，避免后续混入正式外部代理入口。

- 变更类型：新增
- 变更项：日志导出与时间维度统计口径规则
- 变更摘要：新增后台日志导出和时间维度聚合必须复用 `request_logs` 的统一筛选口径生成，禁止前端基于当前页数据临时拼装伪导出结果。
- 变更原因：本次继续补齐日志导出与时间序列统计能力，需要统一后台导出与看板聚合的实现边界，避免后续前后端口径分裂。

- 变更类型：新增
- 变更项：账户级聚合额度控制规则
- 变更摘要：新增管理员必须能够按账户配置调用次数、Token、金额的总额度及日/月配额，并在 Bearer API Key 鉴权链路统一生效。
- 变更原因：本次补齐“按账户防刷量”的核心能力，需要把账户级限额而非仅单 key 限额沉淀为长期统一标准。

- 变更类型：新增
- 变更项：账户级余额可视化规则
- 变更摘要：新增账户视角下必须展示总余额、冻结金额、可用余额、已消费、累计充值和消费明细，若来自名下 API Key 聚合则必须明确按聚合口径展示。
- 变更原因：本次新增账户级余额与冻结金额展示，需要避免后续页面再次只展示单 key 余额而缺失账户总览。

- 变更类型：新增
- 变更项：配额时间窗口与流式能力保留规则
- 变更摘要：新增账户级日/月配额统一按 UTC 自然日与自然月统计，同时明确 `/v1/chat/completions` 与 `/v1/responses` 必须继续支持标准 SSE 流式返回及相关日志留痕。
- 变更原因：本次补齐账户级配额和能力审查时，需要把时间窗口口径与流式能力保留要求固化为长期约束。

- 变更类型：新增
- 变更项：模型调用日志扩展字段标准
- 变更摘要：新增模型调用日志必须统一落库会话、用户、方法、思维等级、尝试次数、TTFB、总耗时、TPS、缓存读写、倍率与渠道价格字段。
- 变更原因：本次补齐日志中心升级、日志扩字段采集和回归验证，需要把日志分析口径固化为长期统一标准。

- 变更类型：新增
- 变更项：管理端与用户端日志隔离规则
- 变更摘要：新增管理端日志中心必须支持按用户筛选全量日志，用户端日志页必须仅展示当前账号自己的模型调用日志，并排除可用性检测与后台测试日志。
- 变更原因：本次开始交付日志中心升级和用户日志隔离页，需要把“管理员看全量、用户只看自己模型调用”的口径固定下来。

- 变更类型：新增
- 变更项：用户端 API Key 自助管理边界规则
- 变更摘要：新增普通用户端必须支持管理本人名下 API Key 的新增、编辑、启停、删除与复制，同时禁止用户修改密钥归属和账户级治理字段。
- 变更原因：本次补齐“用户端像管理员端一样自由管理自身密钥”的能力，需要明确长期边界，避免后续把账户级额度与密钥自助配置混在一起。

- 变更类型：新增
- 变更项：用户端使用文档入口规则
- 变更摘要：新增普通用户端必须提供可访问的 `使用文档` 页面，允许复用统一外部接入文档模板，但用户端导航必须存在独立入口，且内容口径与管理端保持一致。
- 变更原因：本次补齐用户端外部接入文档访问能力，需要把“管理端有文档、用户端也必须可直达”沉淀为长期统一规则。

## 2026-04-26

- 变更类型：新增
- 变更项：管理端关键写操作审计日志规则
- 变更摘要：新增管理端必须提供可检索的操作审计日志页面，并对用户、API Key、模型、系统设置、账户调账等关键写操作统一记录操作者、对象、摘要、时间与必要变更详情。
- 变更原因：本次补齐后台操作审计能力，需要把审计覆盖范围和展示入口沉淀为长期统一标准，避免后续关键管理动作再次无留痕。

- 变更类型：新增
- 变更项：用户端自助运维闭环规则
- 变更摘要：新增普通用户端必须提供 API Key 详情与一键轮换、日志 CSV 导出、账单 CSV 导出和接入自检入口。
- 变更原因：本次补齐用户侧接入运维闭环能力，需要把用户自助排查、导出和密钥轮换的基础能力固化为长期统一要求。

- 变更类型：新增
- 变更项：日志保留策略与自动清理规则
- 变更摘要：新增系统设置必须支持配置请求日志与管理端审计日志保留天数，并由后台定时任务按统一配置自动清理；保留天数为 `0` 时表示禁用自动清理。
- 变更原因：本次补齐日志保留周期配置与定时清理能力，需要把保留策略口径和关闭语义沉淀为长期统一标准。

- 变更类型：新增
- 变更项：管理端 API Key 列表服务端治理规则
- 变更摘要：新增管理端 API Key 管理页必须支持服务端分页、服务端筛选、后端导出和批量启停删，禁止继续依赖前端本地假分页或伪批量操作。
- 变更原因：本次继续收口管理端 API Key 运维能力，需要把列表治理口径统一到后端接口，避免数据量增大后前后端行为失真。

- 变更类型：新增
- 变更项：OpenAI 兼容能力分项验收规则
- 变更摘要：新增渠道或模型若对外宣称支持文本、流式、`/v1/responses` 或图片能力，必须分别独立验收，禁止仅凭文本对话可用就默认判定其他能力同样可用。
- 变更原因：本次真实调用测试发现同一渠道文本链路成功，但图片与 `/v1/responses` 链路可单独失败，需要把兼容能力验收口径固化，避免错误宣称“完全兼容”。

- 变更类型：修改
- 变更项：账户级余额口径规则
- 变更摘要：将账户余额规则从“允许由名下 API Key 聚合展示”修订为“必须以 `user_accounts` 共享金额池为唯一口径，同一用户名下所有 API Key 共用同一账户余额”。
- 变更原因：用户已明确确认共享金额池方案，原有“按 API Key 聚合余额”的规范已与当前业务模型冲突，必须统一替换。

- 变更类型：新增
- 变更项：管理端批量运维扩展规则
- 变更摘要：新增 API Key 管理页必须支持批量渠道授权更新，用户管理页必须支持批量账户额度调整。
- 变更原因：本次继续补齐批量运维能力，需要把 API Key 渠道授权与用户配额批量治理沉淀为长期统一要求。

- 变更类型：新增
- 变更项：告警中心订阅式提醒规则
- 变更摘要：新增告警中心必须提供订阅配置与主动提醒能力，并至少覆盖渠道异常、API Key 异常、账户预警和失败率异常四类告警。
- 变更原因：当前告警页已从静态聚合升级为带订阅和事件流的主动提醒入口，需要形成统一长期规则。

- 变更时间：2026-04-26
- 变更类型：新增
- 变更项：提供商上游模型拉取与导入规则
- 变更摘要：新增后台提供商新增与编辑流程必须支持请求上游 `/models` 拉取可用模型，并支持将选中的一个、多个或全部模型一键导入当前提供商模型配置。
- 变更原因：本次补齐提供商模型导入闭环，需要把“从上游发现模型并快速纳入当前提供商配置”的能力固化为长期统一要求。

## 2026-04-28

- 变更类型：新增
- 变更项：后台与用户业务页说明性卡片收口规则
- 变更摘要：新增后台管理页与用户业务页禁止继续保留脱离实时数据、表单提交或实际操作入口的纯说明性卡片；静态流程、建议、风险、判断、下一步动作类卡片必须移除，必要说明只允许以内联提示、Tooltip、空状态或结果反馈形式贴近具体控件呈现。
- 变更原因：本次页面收口中，用户明确要求移除提供商管理页及同类页面中的说明性前端内容，需要沉淀为全站长期统一的界面约束。

- 变更类型：新增
- 变更项：筛选区同排等宽布局规则
- 变更摘要：新增后台管理页与用户业务页的筛选区必须优先使用同排等宽栅格布局，桌面与平板宽度足够时同层级筛选项应在同一行且控件宽高一致，仅在移动端或容器不足时允许换行或单列。
- 变更原因：API 密钥管理页筛选框在宽屏下逐项独占整行，影响扫描效率；用户要求该布局规则适用于同类页面并写入全局规范。

- 变更时间：2026-04-28
- 变更类型：新增
- 变更项：全局主导航左侧布局规则
- 变更摘要：新增后台管理端与用户业务端的桌面端全局主导航必须放置于左侧侧边栏，顶部区域仅承载轻量工具、账号、主题切换或页面上下文信息；移动端必须提供可折叠导航入口。
- 变更原因：用户明确要求当前顶部主导航迁移到左侧，需要将全局导航布局从顶部主导航修订为长期统一的左侧主导航规则。

- 变更时间：2026-04-28
- 变更类型：新增
- 变更项：数据密集页面宽屏铺满规则
- 变更摘要：新增后台管理端与用户业务端的数据密集页面外壳必须尽量占满可用屏幕宽度，仅保留统一安全边距；表格、统计卡片、筛选区和操作面板优先铺满主内容区，只有正文阅读、登录/注册卡片、弹窗和代码示例等可读性内容允许单独限宽。
- 变更原因：用户要求前端界面宽度尽量占满屏幕，避免页面主体集中在中间小区域，需要沉淀为长期统一的布局标准。

- 变更时间：2026-04-28
- 变更类型：新增
- 变更项：独立快捷入口卡片禁用规则
- 变更摘要：新增后台管理页与用户业务页禁止放置独立快捷入口、快速跳转或常用动作卡片，跨页面跳转必须通过全局导航、页面主操作、锚点导航或贴近业务对象的必要操作呈现。
- 变更原因：用户要求删除所有页面的快捷入口卡片及相关内容，需要沉淀为全站长期统一的前端界面约束。

## 2026-04-29

- 变更类型：修改
- 变更项：Git 提交示例命令占位化规则
- 变更摘要：将《项目全局规范》中 Git 提交示例的固定提交标题与固定详细说明改为通用占位写法，避免把历史一次性提交内容误当作后续任务的默认提交信息复用。
- 变更原因：历史规范示例中保留了特定日期和特定任务的提交文案，已造成后续提交标题沿用旧日期的问题，需要修订为可复用的通用示例。

- 变更类型：新增
- 变更项：模型平均倍率派生指标规则
- 变更摘要：新增模型平均倍率必须由已绑定渠道倍率自动计算，禁止作为独立可编辑字段；需要改变时必须调整渠道绑定关系或单渠道倍率。
- 变更原因：本次完善模型管理时需要明确“平均倍率”的含义、影响因素和变更入口，避免后续把派生统计值当作可手工维护价格字段。

- 变更类型：新增
- 变更项：Linux 生产多 worker 启动规则
- 变更摘要：新增生产环境 `aotu-gpt.service` 必须使用 `Gunicorn + UvicornWorker` 多 worker 启动，Windows `run.ps1` 仅作为本地开发启动方式，并固定 systemd 文件描述符限制与 Nginx SSE 透传配置。
- 变更原因：阶段 2 已将生产启动从单进程 Uvicorn 切换为多 worker 架构，需要沉淀为后续部署脚本和运维文档的长期统一约束。

- 变更类型：新增
- 变更项：Provider 容量路由与租约释放规则
- 变更摘要：新增对外代理路由必须遵守 provider 活跃请求、活跃流式请求、QPS、失败率和首 Token 超时配置，并要求请求正常结束、异常、中断或流式退出时释放 provider 容量租约。
- 变更原因：阶段 3 已引入 provider 级容量控制，需沉淀为后续路由、代理和流式治理的长期统一约束。

- 变更类型：新增
- 变更项：Redis 实时并发计数与限流规则
- 变更摘要：新增对外 `/v1/*` 实时并发计数、租约和短窗口 QPS/RPM 限流必须使用 Redis 等共享状态实现；并发租约释放必须幂等并通过原子脚本或等价机制处理。
- 变更原因：阶段 4 已将实时并发计数和短窗口限流从数据库/进程内存迁移到 Redis，需要沉淀为生产多 worker 链路的长期统一约束。

- 变更类型：新增
- 变更项：API Key 鉴权缓存安全与失效规则
- 变更摘要：新增 Bearer API Key 鉴权缓存禁止保存原始明文 Key，必须基于 SHA256 哈希建缓存，并要求 API Key、用户账户、授权 provider、模型白名单、额度或余额等关键写操作主动失效受影响缓存。
- 变更原因：阶段 5 已引入 Redis 鉴权缓存，需要把密钥安全边界和主动失效要求沉淀为长期统一规则，避免禁用或授权变更后继续放行。

- 变更类型：新增
- 变更项：日志 Token 与计费后台化规则
- 变更摘要：新增对外代理主链路只同步写入 `request_logs` 最小核心字段；Token 估算、费用计算、余额扣减和账单记录必须通过可重试且幂等的后台任务完成；完整 payload 和流式响应文本必须受日志开关与最大字节数控制。
- 变更原因：阶段 6 已将 Token 回填、派生字段刷新和计费扣费迁移到后台任务，需要沉淀为生产高并发链路的长期统一约束，避免主链路重计算和重复扣费。

- 变更类型：替换
- 变更项：SSE 流式并发租约与超时治理规则
- 变更摘要：将原有 provider 级首 Token 超时规则替换为完整 SSE 流式治理规则，要求独立流式并发租约、生成器 finally 释放、客户端取消不计 provider 失败，并要求连接、首 Token、空闲和最大持续时间四类超时控制。
- 变更原因：阶段 7 已补齐流式请求专项治理，需要把防止长连接占满资源的统一约束沉淀为后续代理和日志链路的长期规则。

- 变更类型：新增
- 变更项：生产监控指标与告警统一落地规则
- 变更摘要：新增生产可观测性必须以 Redis 全局活跃计数、数据库可用、连接池状态、后台任务积压、请求错误率和 provider 失败率为核心指标；`RuntimeStateService` 只能作为单 worker 参考；监控类异常必须写入统一 `alert_events` 告警事件流。
- 变更原因：阶段 8 已补齐监控、可用性检测和告警入口，需要沉淀多 worker 场景下的指标口径和告警落地边界，避免后续另建重复监控告警体系。
# 2026-04-30

- 变更时间：2026-04-30
- 变更类型：新增
- 变更项：生产共享缓存约束
- 变更摘要：要求影响正式代理路由、模型列表、provider 状态或权限判断的一致性缓存必须使用 Redis 等共享缓存；仅低风险派生数据允许退回进程内缓存。
- 变更原因：避免 Gunicorn 多 worker 下本地内存缓存不共享导致短时间路由、模型或状态不一致。

- 变更时间：2026-04-30
- 变更类型：新增
- 变更项：生产启动 DDL 约束
- 变更摘要：禁止生产 Web worker 启动阶段执行 `create_all`、自动 DDL 或内置迁移逻辑，数据库结构变更必须由独立迁移或初始化步骤完成。
- 变更原因：避免多 worker 或滚动发布期间并发 DDL 带来的锁等待、失败和结构不一致风险。

- 变更时间：2026-04-30 22:31:38 +08:00
- 变更类型：替换
- 变更项：Git 与 GitHub 提交推送流程
- 变更摘要：将原“先抓取远程、按需合并后推送”的多人协作流程替换为单人维护模式；例行提交推送禁止主动执行 fetch、pull、merge、rebase 或 unrelated histories 合并，推送异常时暂停并等待用户确认。
- 变更原因：用户明确确认本项目仅由本人维护和提交代码，例行提交不需要与其他分支融合。

- 变更时间：2026-04-30 22:45:24 +08:00
- 变更类型：新增
- 变更项：一键部署数据库初始化显式触发规则
- 变更摘要：新增一键部署脚本默认禁止自动执行数据库初始化或迁移；仅允许显式设置 `RUN_DB_INIT=1` 时执行，且执行前必须生成 PostgreSQL 备份。
- 变更原因：避免重复执行阿里云启动脚本时对已有生产数据库做非预期结构或兼容回填，降低云端数据风险。

- 变更时间：2026-04-30 23:59:00 +08:00
- 变更类型：新增
- 变更项：管理员账号创建入口约束
- 变更摘要：新增管理员账号禁止通过公开网页注册或无登录初始化页面创建；首个管理员必须通过服务器侧受控脚本创建，后续管理员仅允许由已登录管理员在后台用户管理中创建或提升。
- 变更原因：用户明确要求管理员不能直接在网页注册，需沉淀为长期安全规则，避免公网初始化页面被滥用。

## 2026-05-02

- 变更时间：2026-05-02 17:30:00 +08:00
- 变更类型：新增
- 变更项：可用性检测日志短周期保留规则
- 变更摘要：新增可用性检测类日志独立于用户请求/响应日志清理，最长仅允许保留 6 小时，且用户请求/响应日志不得因该策略被提前清理。
- 变更原因：用户明确确认可用性检测日志超过一定时间无保留价值，而用户请求/响应日志需要支持长期保留。

- 变更时间：2026-05-02 17:55:00 +08:00
- 变更类型：替换
- 变更项：提供商模型配置编辑边界
- 变更摘要：将提供商模型配置边界替换为：模型基础价格与模型能力统一在模型管理维护；提供商特有的渠道倍率、优先级、权重等路由字段继续在提供商配置维护。
- 变更原因：用户确认此前把倍率、优先级、权重一并收口到模型管理不符合业务边界，需要明确区分模型基础属性与提供商特有路由属性。

- 变更时间：2026-05-02 18:35:00 +08:00
- 变更类型：新增
- 变更项：模型管理与提供商挂载同步规则
- 变更摘要：新增提供商新增模型必须自动补入模型管理；提供商移除模型不删除模型管理；模型管理删除模型必须同步删除所有提供商挂载；提供商挂载模型的共享配置必须以模型管理为准。
- 变更原因：用户明确要求模型管理作为所有可用模型的全集与共享配置源，同时允许模型管理中存在尚未挂载到任何提供商的模型。

- 变更时间：2026-05-02 21:05:12 +08:00
- 变更类型：新增
- 变更项：数据列表与矩阵服务端分页筛选规则
- 变更摘要：新增后台管理页与用户业务页中的数据列表、表格和矩阵类视图必须优先使用服务端分页查询和展示，并支持业务归属维度与关键词搜索。
- 变更原因：用户明确要求提供商模型挂载矩阵及同类列表视图统一采用分页展示、同排筛选，并支持按提供商和模型名称筛选。

- 变更时间：2026-05-02 21:14:04 +08:00
- 变更类型：新增
- 变更项：OpenAI 兼容入口互转重试规则
- 变更摘要：新增 `/v1/chat/completions` 与 `/v1/responses` 在上游端点或模型兼容性错误时必须自动互转重试一次，并保持客户端原请求入口的响应结构。
- 变更原因：部分上游模型只支持 `chat/completions` 或只支持 `responses`，单入口失败不能直接判定模型完全不可用。

- 变更时间：2026-05-02 21:43:00 +08:00
- 变更类型：替换
- 变更项：OpenAI 兼容入口互转安全边界
- 变更摘要：将入口互转重试规则收紧为仅允许简单文本、普通图片和基础流式请求互转；工具调用、状态上下文、reasoning、结构化输出选项或复杂多模态内容必须拒绝转换并返回明确错误；可用性检测必须区分原生支持与适配支持。
- 变更原因：用户明确要求避免高风险请求或响应在 `chat/completions` 与 `responses` 之间发生有损转换，同时需要在后台可用性检测与 trace 中保留原生成功和适配成功的可追踪差异。

- 变更时间：2026-05-02 22:05:00 +08:00
- 变更类型：新增
- 变更项：对外 `/v1/*` 错误响应可见性规则
- 变更摘要：新增外部请求一旦被 `/v1/*` 接收，失败时必须返回包含错误码、错误说明、`trace_id` 和可重试标记的可读错误；SSE 连接建立后发生错误时必须尽可能发送 `event: error` 并以 `data: [DONE]` 收尾。
- 变更原因：用户明确要求外部调用方不能只看到空响应或断流，必须知道失败原因，且不能只在本地日志中记录错误。

- 变更时间：2026-05-02 22:18:16 +08:00
- 变更类型：修改
- 变更项：对外代理主链路 Token 估算边界
- 变更摘要：将原“Token 估算必须全部后台完成”调整为：仅全局最大请求 Token 预检允许在进入上游路由前执行请求 Token 估算，其余费用、扣费和账单仍必须后台幂等完成。
- 变更原因：用户明确要求为所有外部请求增加全局最大请求 Token 前置拦截，避免超长上下文继续进入路由或写入大日志。

- 变更时间：2026-05-02 22:18:16 +08:00
- 变更类型：新增
- 变更项：超限请求日志保护规则
- 变更摘要：新增超过全局最大请求 Token 的 `/v1/*` 请求必须直接返回明确错误；对应 `request_logs` 仅记录请求元数据、估算 Token、错误码和错误原因，禁止落库请求正文、请求上下文或完整 `token_request_payload`。
- 变更原因：用户明确要求超长上下文请求不能造成本地日志或数据库膨胀，外部调用方也必须知道具体拒绝原因。

- 变更时间：2026-05-02 22:18:16 +08:00
- 变更类型：新增
- 变更项：模型上下文窗口配置规则
- 变更摘要：新增模型管理必须维护单模型最大上下文窗口 Token 配置，并支持把同一个上下文窗口 Token 批量应用到选中的多个模型。
- 变更原因：用户明确要求在模型管理中按模型维护上下文窗口，并提供批量应用能力。

- 变更时间：2026-05-02 23:24:49 +08:00
- 变更类型：新增
- 变更项：长上下文与长输出代理治理规则
- 变更摘要：新增 `/v1/*` 请求体应用层限量读取、快速 Token 估算优先、长输出非流式阈值拒绝、流式统计缓存上限和后台 Token 任务大 payload 保护规则。
- 变更原因：长上下文与长代码输出场景会放大入口内存、Token 统计 CPU、日志存储和后台任务队列风险，需要形成长期统一治理边界。

- 变更时间：2026-05-02 23:24:49 +08:00
- 变更类型：新增
- 变更项：模型能力字段与工具调用验收规则
- 变更摘要：新增模型必须维护最大输入/输出 Token、工具调用、原生 `chat/completions`、原生 `responses` 等能力字段；工具调用必须路由到原生支持渠道，可用性检测必须执行工具调用探测。
- 变更原因：写代码代理等客户端依赖工具调用和端点原生能力，不能仅通过字段透传或普通文本可用性检测宣称适配。

- 变更时间：2026-05-02 23:24:49 +08:00
- 变更类型：新增
- 变更项：云服务器例行更新数据库保护规则
- 变更摘要：新增云服务器例行更新项目代码、依赖、静态资源或服务配置时禁止同步更新、初始化、迁移、清空或重建云服务器数据库；数据库变更必须由用户明确要求后走独立迁移和备份流程。
- 变更原因：用户明确要求云服务器更新项目时长期禁止非预期修改云端数据库。

## 2026-05-03

- 变更时间：2026-05-03 00:20:00 +08:00
- 变更类型：新增
- 变更项：端点级请求体与非流式响应体限量规则
- 变更摘要：新增 `/v1/chat/completions` 与 `/v1/responses` 独立请求体上限按更严格正数执行；非流式上游响应体必须按系统设置限量读取，超限时返回明确错误。
- 变更原因：长上下文输入与长输出非流式响应都可能绕过反向代理保护直接占用应用内存，需要在应用层形成端到端字节治理。

- 变更时间：2026-05-03 00:20:00 +08:00
- 变更类型：新增
- 变更项：请求日志 metadata 独立压缩规则
- 变更摘要：新增请求日志中的 `metadata` 必须受独立字节上限控制，超过上限仅记录结构化摘要，禁止通过 `metadata` 绕过 payload 日志开关。
- 变更原因：客户端可能把长上下文、文件摘要或敏感内容放入 `metadata`，仅控制正文日志不足以覆盖性能和泄露风险。

- 变更时间：2026-05-03 09:20:00 +08:00
- 变更类型：新增
- 变更项：桌面侧栏滚动分区规则
- 变更摘要：新增左侧侧栏同时承载主导航与底部工具区时，桌面端必须拆分为主导航独立滚动区和底部工具固定区，禁止把两类区域放进同一无边界流式容器导致重叠。
- 变更原因：本次前端调整中，放大导航项后底部工具区被挤入主导航列表并发生按钮重叠，需要沉淀为长期可复用的布局约束。

- 变更时间：2026-05-03 18:25:00 +08:00
- 变更类型：新增
- 变更项：手动可用性检测并发编排与冷却规则
- 变更摘要：新增后台手动提供商可用性检测与“检查全部”批量可用性检测必须按“同类探针并行、异类探针串行”执行，阶段完成后前端立即显示反馈；同时必须设置 300 秒最小触发间隔，单个探针首次失败仅允许重试一次。
- 变更原因：批量可用性检测在模型数较多时若完全串行会明显拖慢后台排查效率，而无冷却与无限重试又会放大上游并发压力，需要形成统一长期规则。

- 变更时间：2026-05-03 19:05:00 +08:00
- 变更类型：新增
- 变更项：前端单位展示与帮助浮层规则
- 变更摘要：新增前端数值单位必须直接使用符号展示、token 单价统一使用 `$/1M`、高密度表格中的帮助浮层必须使用不透明背景且不得被父容器裁切。
- 变更原因：本次日志与设置页调整暴露出旧单位口径混杂、中文单位重复和 tooltip 被滚动容器遮挡的问题，需要沉淀为可复用的统一前端约束。

- 变更时间：2026-05-03 19:20:00 +08:00
- 变更类型：新增
- 变更项：前端点击结果反馈规则
- 变更摘要：新增全站按钮与其他点击型交互控件在触发后必须提供结果反馈；无需展示详细结果时，优先在原按钮位置短暂反馈成功或失败；需要展示详细结果时，必须使用合适的结果界面承载。
- 变更原因：用户明确要求全站前端交互避免“点击后无反馈”，需要统一沉淀按钮操作结果的反馈方式，提升整体可用性与可预期性。

- 变更时间：2026-05-04 00:40:00 +08:00
- 变更类型：新增
- 变更项：Responses 原生透传优先规则
- 变更摘要：新增 `/v1/responses` 图片请求及携带原生 Responses 字段的请求必须优先按原始协议透传上游；仅在上游明确返回端点或模型兼容性错误且满足安全转换条件时，才允许降级为 `/v1/chat/completions`。
- 变更原因：用户明确要求尽量兼容所有 OpenAI 协议模型与各类 agent，避免本地在进入上游前就把原生 Responses 请求错误压缩成 chat 适配子集。

- 变更时间：2026-05-04 20:30:00 +08:00
- 变更类型：新增
- 变更项：云服务器更新 GitHub 镜像拉取规则
- 变更摘要：新增云服务器例行更新脚本必须支持 GitHub 直连失败后的可配置镜像拉取，且镜像源必须允许通过环境变量覆盖。
- 变更原因：阿里云服务器执行 `update_aliyun.sh` 时经常无法直连 GitHub，需要将可替换的镜像回退机制沉淀为长期部署约束。

## 2026-05-05

- 变更时间：2026-05-05 21:58:00 +08:00
- 变更类型：新增
- 变更项：对外 `/v1/*` 请求日志完整可观测与请求体结构摘要规则
- 变更摘要：新增对外 `/v1/*` 请求只要到达应用层必须写入 `request_logs`，并记录成功状态、失败原因、`trace_id`、路径、方法、来源 IP 和可用鉴权上下文；请求体默认记录结构摘要，禁止默认记录完整正文或敏感大字段。
- 变更原因：第三方 GUI 与编程 Agent 接入排查需要确认请求是否到达、失败在哪一层、请求体结构是否符合协议，同时必须避免完整 prompt、图片 base64 或密钥进入日志造成日志膨胀和泄露风险。

## 2026-05-06

- 变更时间：2026-05-06 23:15:00 +08:00
- 变更类型：新增
- 变更项：对外请求 `type` 字段类型保护规则
- 变更摘要：新增对外 `/v1/*` 请求体在做图片识别、工具识别、内容类型识别或 Token 预估时，读取 `type` 字段必须先确认其值是字符串；非字符串值必须按普通 JSON 递归处理或计入 JSON 文本，禁止直接参与集合成员判断。
- 变更原因：Codex Responses 工具 schema 中可能出现 `type` 值为对象的情况，若直接做集合成员判断会触发 `TypeError: unhashable type: 'dict'` 并导致真实 Codex 请求在进入上游前 500。

## 2026-05-08

- 变更时间：2026-05-08 20:05:00 +08:00
- 变更类型：新增
- 变更项：图片验活链路分测与安全预览规则
- 变更摘要：新增后台可用性检测、用户接入自检、playground 等图片验活链路必须将图像理解与图片生成分开探测、分开展示；页面、报告和日志预览禁止直接回显图片 base64 或完整二进制结果，只允许展示结构摘要、图片数量、格式与截断标记。
- 变更原因：image_generation 链路若沿用识图探针或直接回显原始图片结果，会把“能看图”误判成“能生图”，同时带来页面、报告和日志膨胀风险，需要沉淀为统一长期规则。

- 变更时间：2026-05-08 22:05:00 +08:00
- 变更类型：新增
- 变更项：流式图片生成日志安全摘要留痕规则
- 变更摘要：新增对外流式图片生成请求在关闭完整流式响应持久化时，`request_logs` 仍必须保留图片数量、格式、近似字节数、partial 标记与截断标记等安全摘要，并允许落库 `[生成了 N 张图片]` 这类占位文本摘要。
- 变更原因：真实流式 `image_generation` 请求即使成功返回图片，若日志完全不留安全摘要，就会导致日志中心、自检和后续排障无法判断是否真的生成了图片。

- 变更时间：2026-05-08 22:08:00 +08:00
- 变更类型：新增
- 变更项：图片生成缺失 usage 时的 Token 补全约束
- 变更摘要：新增图片生成响应缺少上游 `usage` 时，日志补全必须优先保持未知或留空，禁止从 `[生成了 N 张图片]` 等安全占位文本或其他非真实正文反推 `completion_tokens`。
- 变更原因：纯生图链路的安全摘要文本不代表真实输出正文，若继续按文本回退补 Token，会让日志统计和计费判断产生系统性失真。

- 变更时间：2026-05-08 23:05:00 +08:00
- 变更类型：新增
- 变更项：对外图片生成接入文档统一 Responses 口径
- 变更摘要：新增对外 `使用文档`、用户侧 API Key 接入示例和 legacy 图片端点错误提示在涉及生图或编辑图时，必须统一明确声明正式公网入口为 `POST /v1/responses + image_generation`，并提供可直接复制的 generate / edit 示例。
- 变更原因：即使代理主链路已经支持图片生成，若外部接入文档和错误提示不把正式调用口径说清楚，调用方仍会误用旧版 `images/*` 或不知道 edit 请求应如何构造，导致“能力已支持但无法稳定接入”的反复沟通成本。

- 变更时间：2026-05-08 23:58:00 +08:00
- 变更类型：新增
- 变更项：legacy OpenAI 图片端点兼容层对外口径规则
- 变更摘要：新增若对外实现 `/v1/images/generations`、`/v1/images/edits` 等 legacy 图片端点兼容层，必须明确标注其本质是 `Responses + image_generation` 的兼容适配；正式推荐公网入口仍为 `/v1/responses`；`response_format=url` 在未提供对象存储托管时只能返回 data URL 兼容值。
- 变更原因：项目已补 legacy Images 兼容路由后，如果不在规范层明确“主路径”和“兼容路径”的边界，很容易再次把兼容层误宣传成正式主工作流，或让调用方误以为 `url` 一定是临时公网文件地址。

## 2026-05-29

- 变更时间：2026-05-29 23:45:00 +08:00
- 变更类型：替换
- 变更项：对外代理请求日志主链路写入方式
- 变更摘要：将“对外代理主链路必须同步写入 `request_logs` 最小核心字段”替换为“主链路必须可靠写入请求日志接收层，允许 Redis Stream、消息队列或等价可靠队列异步批量落库到 `request_logs`，队列不可用时允许回退同步落库”。
- 变更原因：真实压测显示同步 `request_logs` 落库会成为非流式 1000 RPS 目标的核心瓶颈；用户已确认按高性能目标修订规范并继续优化。

- 变更时间：2026-05-29 23:58:00 +08:00
- 变更类型：新增
- 变更项：Web 请求进程与后台队列 worker 分离部署规则
- 变更摘要：新增高并发生产部署必须支持 Web 请求进程通过 `ENABLE_BACKGROUND_WORKERS=false` 只负责接收与转发请求，并由独立 worker 进程消费 Redis 队列完成日志落库、Token 补全和计费结算；禁止在无人消费队列的生产形态下长期运行。
- 变更原因：真实压测显示将日志与 Token 后台 worker 混跑在每个 Web 进程内会争抢 CPU/DB 连接并限制横向扩展效率，分离数据面与后台消费面后非流式吞吐可稳定突破 1000 RPS。

## 2026-05-30

- 变更时间：2026-05-30 14:35:00 +08:00
- 变更类型：新增
- 变更项：高并发压测的 Redis 与异步队列排空规则
- 变更摘要：新增本地或 CI 高并发压测必须使用独立 Redis 端口或独立逻辑库隔离 benchmark 状态，并在压测前后清理 benchmark `request_logs` 与 Redis 队列；压测结束前必须等待请求日志落库队列和 Token finalize 后台任务进入空闲。
- 变更原因：真实压测发现复用共享 Redis、沿用历史 benchmark 队列或在压测结束后直接强杀进程，会导致异步日志丢失、后台积压串场和吞吐数据失真，需要沉淀为统一压测规则。

## 2026-05-31

- 变更时间：2026-05-31 21:27:44 +08:00
- 变更类型：新增
- 变更项：测试数据中文命名规则
- 变更摘要：新增项目内用于开发、联调、演示、验收的测试数据在不受第三方协议、接口字段、字符集或技术标识限制时，必须优先使用中文命名；仅在需要与外部协议、代码字段、脚本参数或固定英文标识对齐时，才允许使用英文或中英混合命名。
- 变更原因：用户明确要求项目内各类测试数据尽量使用中文命名，需要沉淀为长期统一、可执行的命名约束，减少示例数据和测试对象的命名漂移。

- 变更时间：2026-05-31 22:10:00 +08:00
- 变更类型：新增
- 变更项：全站主题系统的多风格预设规则
- 变更摘要：新增全站前端主题系统必须同时支持日间模式与暗黑模式，并提供不少于 10 套可一键切换的统一风格/配色预设；风格切换必须在全局壳层生效并持久化用户选择。
- 变更原因：用户明确要求项目不仅支持亮暗模式，还要支持项目整体风格/配色的一键替换，需要沉淀为长期统一的全站主题能力约束，避免后续页面各自实现局部换色。

- 变更时间：2026-05-31 23:05:00 +08:00
- 变更类型：新增
- 变更项：模型价格多形态与缓存口径统一规则
- 变更摘要：新增后台模型管理必须支持固定价、阶梯价和未定价三种价格形态；官方完整价格区间、缓存命中价、缓存写入价或等价分档信息必须以结构化数据附加保存并可查看；界面对外展示的缓存价默认按缓存命中价口径换算，若存在缓存写入或存储费用，必须同步展示并明确口径。
- 变更原因：真实官方价格页中，部分模型存在阶梯价、缓存命中价与缓存写入价等多口径信息，若仍只保留单一数值会丢失官方价格结构并导致后续导入、展示和计费口径不一致。

## 2026-06-01

- 变更时间：2026-06-01 21:40:54 +08:00
- 变更类型：新增
- 变更项：运维监控资源占用口径区分规则
- 变更摘要：新增运维监控页面展示资源占用时必须明确区分宿主机/分区级指标与当前项目/进程级指标；面向“项目占用”的磁盘指标必须使用项目目录实际占用，禁止把宿主机分区已用、可用或剩余容量标成项目占用。
- 变更原因：运维监控页曾将项目所在分区的磁盘使用率和分区可用空间展示在“磁盘”卡片中，容易被误解为当前项目占用几十 GB，需要沉淀为长期监控口径规则。

- 变更时间：2026-06-01 21:45:27 +08:00
- 变更类型：新增
- 变更项：后台任务积压监控统计口径规则
- 变更摘要：新增后台任务积压类监控指标必须只统计确实需要对应后台任务处理的正式候选数据；Token/计费补全积压仅允许统计正式模型调用日志，禁止混入鉴权失败、可用性检测、压测插入、审计或其它非计费日志。
- 变更原因：运维监控页曾把历史鉴权失败和压测日志计入 Token/计费补全积压，导致服务状态被误判为 degraded，需要固定后台积压类指标的统计口径。

- 变更时间：2026-06-01 21:57:12 +08:00
- 变更类型：新增
- 变更项：提供商整体可用性与模型可用性分层展示规则
- 变更摘要：新增提供商级可用状态只能作为启用模型可用性度的聚合可用性展示；模型路由、可用筛选、能力验收和故障定位必须以 `provider_models.health_status` 及能力探针结果为准，前端必须区分“整体可用性”与“模型可用性”。
- 变更原因：单个提供商可挂载多个模型，一个模型不可用不等于整个提供商完全不可用，若继续统一显示“状态/可用”会误导运维判断和故障定位。

- 变更时间：2026-06-01 22:04:53 +08:00
- 变更类型：修改
- 变更项：运维监控项目目录占用扫描缓存周期
- 变更摘要：将项目目录递归占用统计纳入运维监控资源口径规则，明确该类高成本扫盘操作的缓存周期必须不少于 6 小时。
- 变更原因：项目目录占用需要递归遍历文件树，若随监控刷新高频执行会增加本地运行时性能开销，需要固定为低频缓存指标。

- 变更时间：2026-06-01 22:20:00 +08:00
- 变更类型：修改
- 变更项：Token/计费补全积压与 backfill 候选排除内部请求日志
- 变更摘要：明确 Token/计费补全积压统计与对应 backfill 候选必须排除 `api_client_key_id` 为空的内部请求日志，禁止仅因 `billing_finalized_at` 为空就把内部管理、playground 或自检请求反复计入积压与重试。
- 变更原因：本地运维监控中的 11 条“后台积压”实为历史内部请求日志，并无外部 API Key 结算语义；若继续纳入口径会造成假积压并让 backfill 无意义重试。

- 变更时间：2026-06-01 22:07:47 +08:00
- 变更类型：新增
- 变更项：模型管理模型级可用性检测并行与状态规则
- 变更摘要：新增后台模型管理的模型级可用性检测必须同时按“不同模型并行”和“同一模型不同绑定渠道并行”执行；模型聚合状态仅向用户展示“可用”或“异常”，且存在任一可用绑定渠道即为可用。
- 变更原因：模型配置页需要支持单模型与批量模型可用性检测，若模型或渠道串行探测会导致操作耗时过长；同时模型启停状态与可用状态需要分层展示，避免运维判断歧义。

- 变更时间：2026-06-01 22:56:53 +08:00
- 变更类型：新增
- 变更项：后台即时验活按钮轻量文本探针规则
- 变更摘要：新增后台人工点击的“测试”“检查全部”“一键测试”等即时验活按钮默认必须执行轻量文本链路探针，并且只探测模型声明支持的正式文本端点；完整多模态能力验收必须放入专项探针、定时能力检查或明确的能力验收入口。
- 变更原因：即时测试按钮需要快速反馈真实文本链路可用性，若每次点击都默认触发 tools、图像理解、图片生成等高成本能力探针，会导致响应时间过长并放大上游压力。
## 2026-06-01

- 变更类型：新增
- 变更项：对外路由候选耗尽等待重试规则
- 变更摘要：规定 `/v1/*` 请求在候选因容量、失败率、熔断、模型临时不可用或上游临时失败而耗尽时，响应建立前必须按路由策略等待重试；默认最大等待窗口 600 s，并可在路由设置中配置 0-600 s。
- 变更原因：外部调用需要在所有渠道临时不可用时具备真实自动恢复等待能力，同时避免无限期占用请求。

- 变更类型：替换
- 变更项：后台即时验活按钮测试范围选择规则
- 变更摘要：将“即时验活默认仅轻量文本探针”替换为“点击测试前必须提供文本、图片理解、工具调用、图片生成多选框；默认仅选文本，且文本覆盖非流式与流式；图片生成仅在显式勾选时执行”。
- 变更原因：后台测试需要由用户按本次运维目标选择一个或多个能力，避免默认混入高成本能力，同时保留专项验证入口。

## 2026-06-02

- 变更时间：2026-06-02 23:56:37 +08:00
- 变更类型：修改
- 变更项：对外路由候选耗尽等待重试规则
- 变更摘要：将候选耗尽等待重试规则调整为正常模式继续遵守 0-600 s 最大等待窗口；仅在显式启用无限重试模式时，允许对非项目自身问题绕过最大等待窗口并持续等待重试。
- 变更原因：系统需要同时支持正常失败返回模式与长期无人值守的自动等待重试模式，且必须避免默认链路无限期占用请求。

## 2026-06-03

- 变更时间：2026-06-03 00:48:04 +08:00
- 变更类型：新增
- 变更项：思维等级字段协议透传规则
- 变更摘要：新增对外 `/v1/*` 代理链路在补充 `reasoning_effort`、`reasoning.effort` 等标准字段时，必须保留调用方原始思维等级字段与未识别扩展参数，禁止因日志识别或协议适配删除原始请求字段。
- 变更原因：全协议兼容目标要求对用户请求无损透明透传；历史实现会在补齐标准 reasoning 字段时删除 `model_reasoning_effort`，存在扩展参数丢失风险。

- 变更时间：2026-06-03 11:30:03 +08:00
- 变更类型：新增
- 变更项：对外模型映射路由规则
- 变更摘要：新增 `/v1/*` 请求进入路由前允许按管理员启用的模型映射规则解析目标模型；多目标映射必须在权限、能力、可用与容量约束内选择，并在请求日志 trace 中记录源模型、目标模型与选择策略。
- 变更原因：模型别名与多目标择优属于长期路由治理能力，需要统一约束权限边界、能力筛选和可观测性。

## 2026-06-04

- 变更时间：2026-06-04 00:00:00 +08:00
- 变更类型：修改
- 变更项：对外模型映射路由规则
- 变更摘要：将模型映射筛选条件从“权限、端点能力、模型能力、可用与容量”修订为“API Key 权限、渠道授权、可用与容量”，明确映射阶段禁止再以模型能力字段一致性作为前置拦截条件。
- 变更原因：用户已确认模型映射需要直接指向目标模型，不再要求映射阶段先做能力一致性拦截，以便兼容别名模型和手工映射场景。

- 变更时间：2026-06-04 00:00:00 +08:00
- 变更类型：新增
- 变更项：API Key 渠道授权管理规则
- 变更摘要：新增 API Key 可授权提供商集合只能由管理员配置；新建密钥默认授权全部提供商并启用自动同步；管理员必须支持单个密钥授权、批量统一授权以及一键将全部密钥恢复为全部提供商授权；提供商增删后自动同步模式下的密钥授权集合必须自动更新。
- 变更原因：用户要求将渠道授权完全收口到管理员侧，并确保现有与后续密钥都能自动随渠道集合变化保持全授权默认状态，避免手工补授权造成访问失败。

- 变更时间：2026-06-04 15:10:00 +08:00
- 变更类型：修改
- 变更项：`/v1/chat/completions` 与 `/v1/responses` 端点互转安全边界
- 变更摘要：将端点互转规则从“仅简单文本、普通图片和基础流式请求”修订为“额外允许可无损映射的简单工具调用请求与响应执行一次安全互转”；同时保留对 `reasoning`、`previous_response_id`、复杂多模态、结构化输出选项和其他复杂状态上下文的互转禁令。
- 变更原因：用户已确认需要把 DeepSeek、通义、智谱、豆包、Kimi、MiMo 等 OpenAI 兼容模型家族的工具调用能力统一纳入正式外部支持范围；若继续完全禁止工具调用的 chat/responses 互转，将导致部分只原生支持单一端点的模型无法在统一对外入口下完整支持工具调用。

- 变更时间：2026-06-04 23:53:54 +08:00
- 变更类型：替换
- 变更项：业务消费配额口径
- 变更摘要：将账户级调用次数、Token、金额总量及日/月配额规则替换为“业务消费拦截必须统一以账户可用余额为唯一基准”；Token 数量仅允许用于成本计算、日志统计和技术安全限制，禁止作为用户或密钥消费配额展示或执行。
- 变更原因：用户确认项目配额逻辑需要统一以余额为准，避免 Token 配额与账户余额并存造成调用被误拦截和余额理解歧义。

## 2026-06-05

- 变更时间：2026-06-05 13:50:00 +08:00
- 变更类型：新增
- 变更项：PowerShell 7 命令执行兼容规则
- 变更摘要：新增 Windows 本地命令必须使用 PowerShell 7；复杂命令需先处理外层解析；正则、变量、管道符、JSON、内联 Python 与 Git diff 参数顺序必须按可验证方式书写；禁止在 PowerShell 中直接套用 Bash heredoc。
- 变更原因：近期多次出现命令被 PowerShell 外层提前解析、Bash 语法不兼容、Git 参数顺序错误等问题，影响排查效率，需要固化为长期命令执行规范。

- 变更时间：2026-06-05 13:35:00 +08:00
- 变更类型：替换
- 变更项：Stateful Responses 模型绑定规则
- 变更摘要：将原“stateful Responses 禁止模型映射和映射故障转移更换目标模型”替换为“同会话优先映射回最近一次成功实际目标；该目标不可用或可恢复失败时允许按可用性优先候选故障转移；全程保留 trace；仍禁止端点互转”。
- 变更原因：用户确认 `not-gpt + 当前会话` 应先回到上一次成功使用的实际目标模型和提供商渠道；若该实际目标不可用，再尝试其它可用候选，以兼顾 stateful 续写与容灾。

- 变更时间：2026-06-05 13:35:00 +08:00
- 变更类型：修改
- 变更项：对外模型映射与提供商候选选择主策略
- 变更摘要：将“同一可用层级内才优先复用同会话近期目标”调整为“硬约束筛选后，只要同会话近期实际模型或提供商仍可用即优先复用；近期目标不可用时再按可用、负载、权重和路由得分分发”。
- 变更原因：用户明确要求模型选择和提供商渠道选择都要优先回到当前会话上一次成功使用的实际目标，而不是仅在相同可用层级内才保持粘性。

- 变更时间：2026-06-05 00:00:00 +08:00
- 变更类型：新增
- 变更项：`/v1/*` 统一错误结构与重试分类规则
- 变更摘要：新增对外 `/v1/*` 错误响应必须统一返回 OpenAI 兼容 `error` 对象，并稳定包含 `trace_id`、`retryable`、`recoverable`、`category`；重试策略必须区分逻辑/功能不可恢复错误与临时可恢复错误。
- 变更原因：外部调用方需要稳定解析所有错误类型，同时避免请求结构、鉴权、模型不存在或能力不支持等不可恢复问题进入无意义等待重试。

- 变更时间：2026-06-05 00:00:00 +08:00
- 变更类型：新增
- 变更项：可用层级与会话粘性优先路由规则
- 变更摘要：新增对外代理路由和模型映射必须先按可用层级选择候选，同层内优先保持同一会话模型/提供商粘性；可用候选不可用或失败后，才允许尝试近期不可用但未被完全熔断排除的候选。
- 变更原因：用户要求整体选路算法优先命中近期可用可用模型与提供商，并在性能可控前提下降低同一会话跨模型漂移。

- 变更时间：2026-06-05 00:00:00 +08:00
- 变更类型：新增
- 变更项：请求日志异步落库实时性规则
- 变更摘要：新增对外 `/v1/*` 请求日志异步队列落库的默认刷新实时性目标必须在 2 秒以内，正常情况下优先 1 秒以内；队列不可用或入队失败必须回退同步落库。
- 变更原因：请求日志是外部调用排障与结算观测的核心数据，异步落库不能牺牲用户刷新后的可见性。

- 变更时间：2026-06-05 00:00:00 +08:00
- 变更类型：新增
- 变更项：高频读取路径缓存命中率规则
- 变更摘要：新增高频读取路径缓存必须使用可配置 TTL，并在监控中暴露命中率、L1 命中率和缓存条目数；写操作必须主动失效受影响缓存，禁止用硬编码极短 TTL 代替失效机制。
- 变更原因：用户要求项目尽量确保缓存比率高；路由、模型列表、provider 快照和鉴权等热路径需要兼顾高命中率、低延迟和一致性可控。

- 变更时间：2026-06-05 00:00:00 +08:00
- 变更类型：修改
- 变更项：请求日志缓存 token 与缓存价格字段规则
- 变更摘要：将模型调用日志核心字段补充为包含 `channel_price_cache_per_1k` 与 `channel_price_cache_write_per_1k`；新增缓存读取、缓存写入 token 只能来自上游 `usage` 显式官方字段，禁止从总输入 token、缓存 miss token 或非 usage 文本反推。
- 变更原因：不同供应商的缓存命中、缓存 miss、缓存创建/写入字段语义不同；若混用会导致日志、缓存率和计费统计失真。

- 变更时间：2026-06-05 10:58:42 +08:00
- 变更类型：新增
- 变更项：用户请求 prompt cache 命中率规则
- 变更摘要：新增对外 `/v1/chat/completions` 与 `/v1/responses` 用户请求必须尽量保持上游 prompt cache 命中率；代理层禁止无业务必要地重排稳定前缀、插入每次变化的系统内容、改写用户稳定上下文或破坏同一会话路由粘性；provider 专属缓存控制字段仅在官方协议明确支持且不改变调用语义时允许补充。
- 变更原因：用户明确要求项目尽量确保缓存比例高；对外模型调用的 prompt cache 命中率依赖请求前缀稳定性、会话粘性和协议安全适配，需要作为长期实现约束固化。

- 变更时间：2026-06-05 11:38:27 +08:00
- 变更类型：替换
- 变更项：对外模型映射与提供商候选选择主策略
- 变更摘要：将原“可用层级与会话粘性优先路由规则”替换为统一可用性优先主策略：模型映射和提供商选择必须先按可用层级筛选；同一可用层级内仅复用同一会话最近一次成功使用的模型或提供商；新会话不套用会话粘性；无近期候选时按可用度、当前负载、权重和路由得分分发；提供商并发、流式并发和 QPS 容量作为硬约束，达到上限时禁止选取。
- 变更原因：用户明确要求模型分发和提供商渠道选取以可用性优先、近期会话缓存命中、负载均衡和并发上限硬排除作为项目主逻辑，避免所有请求集中到单一模型或渠道，并提高上游 prompt cache 读写比例。

- 变更时间：2026-06-05 12:03:56 +08:00
- 变更类型：新增
- 变更项：旧路由策略兼容字段前端展示规则
- 变更摘要：新增历史 `route_mode` 与模型映射 `strategy` 字段仅允许作为内部兼容字段存在；用户可见策略必须统一展示为“可用性优先”，禁止继续暴露手动优先、故障切换、权重分流、会话粘滞、优先级等旧策略可选项。
- 变更原因：后端主策略已收敛为可用性优先，但前端仍展示旧下拉选项会误导用户认为存在多套主策略，需要把界面、文档和能力说明口径同步收敛。

- 变更时间：2026-06-05 12:18:22 +08:00
- 变更类型：新增
- 变更项：前端共享数据唯一口径规则
- 变更摘要：新增同一业务数据若同时在服务端模板首屏、前端异步刷新接口或多个前端页面中展示，必须复用同一服务层序列化或汇总函数；禁止模板直接查表另算一套、API 再算一套。
- 变更原因：本次审查发现首页、提供商页和 API Key 页面存在首屏模板与异步刷新接口口径分叉风险，需要固化统一数据来源规则以避免跨页面和刷新前后数据不一致。

- 变更时间：2026-06-05 12:26:00 +08:00
- 变更类型：修改
- 变更项：提供商挂载模型统计口径
- 变更摘要：在提供商与模型管理父子关系规则中补充 挂载模型数必须按去重模型名统计，禁止把同一模型在多个提供商的挂载记录重复计为多个模型。
- 变更原因：用户明确要求提供商管理中的模型数量不得超过模型配置总数；原展示口径按挂载记录计数，多个提供商挂载同一模型时会造成数量理解歧义。

- 变更时间：2026-06-05 12:45:00 +08:00
- 变更类型：新增
- 变更项：提供商与 API Key 容量默认值及 RPM 硬约束
- 变更摘要：新增提供商默认最大活跃请求 20、最大流式请求 10、最大 QPS 20、每分钟最多请求 20；API Key 默认 QPS/RPM 为 20；全局、API Key、账户和提供商维度活跃并发默认 20、流式并发默认 10；提供商 RPM 与 QPS 一样必须作为路由硬约束，已有数据只能非破坏性回填。
- 变更原因：用户明确要求所有现有与后续新增提供商和密钥统一容量默认值，并新增提供商每分钟最多请求限制，达到上限后必须暂停该提供商路由直到窗口恢复。

- 变更时间：2026-06-05 13:18:00 +08:00
- 变更类型：新增
- 变更项：Stateful Responses 模型绑定规则
- 变更摘要：新增 `/v1/responses` 请求包含 `previous_response_id` 或 `encrypted_content` 等上游状态上下文时，禁止模型别名映射到不同目标模型，禁止模型映射故障转移更换目标模型；无法确认原始模型绑定时必须返回明确错误。
- 变更原因：本次日志显示 stateful Responses 请求被模型映射和故障转移发送到 `grok-4.3`，导致上游无法解密来自其它模型或上游的 `encrypted_content`。

- 变更时间：2026-06-05 13:18:00 +08:00
- 变更类型：新增
- 变更项：模型映射能力约束透传规则
- 变更摘要：新增模型映射候选评估必须透传本次请求的流式、视觉、工具调用、图片生成、原生 Chat 和原生 Responses 能力约束，禁止按普通文本能力粗选目标后再由二次路由或上游失败兜底。
- 变更原因：本次日志显示模型映射解析阶段跳过能力约束，先选中不支持原生 Responses 的目标模型，造成后续路由失败和错误目标故障转移。

- 变更时间：2026-06-05 13:35:00 +08:00
- 变更类型：新增
- 变更项：模型管理能力展示与提供商批量导入默认能力规则
- 变更摘要：新增模型管理页可见能力仅展示流式、仅文本、工具调用和图像理解，禁止展示图片生成能力；提供商批量导入模板的 `模型` 与 `模型能力` 字段允许为空，能力为空时默认按流式、文本、工具调用和图像理解创建模型配置。
- 变更原因：用户要求模型管理能力口径统一且不再出现图片生成能力，同时允许批量导入提供商时先导入渠道基础信息，后续再补模型或使用默认模型能力。

- 变更时间：2026-06-05 00:00:00 +08:00
- 变更类型：替换
- 变更项：`/v1/chat/completions` 与 `/v1/responses` 端点处理边界
- 变更摘要：将原允许简单请求在 Chat Completions 与 Responses 之间安全互转的规则，替换为两端点必须按用户请求入口独立处理、只路由到原生支持对应端点的模型渠道，禁止自动互转、预选互转或失败后互转重试。
- 变更原因：用户确认 Chat Completions API 与 Responses API 两种模式都应完整独立实现相同核心能力，不再通过端点互转兜底。

- 变更时间：2026-06-05 00:00:00 +08:00
- 变更类型：修改
- 变更项：端点级工具调用与图像理解能力探针规则
- 变更摘要：明确工具调用与图像理解能力必须按 `chat/completions` 与 `responses` 分别执行、记录并参与路由过滤，禁止用另一端点探测成功结果为当前入口背书。
- 变更原因：Chat Completions API 与 Responses API 已收敛为独立原生入口，工具和视觉能力也需要端点级验收，避免模型级探针结果造成错误路由。

- 变更时间：2026-06-05 21:36:04 +08:00
- 变更类型：新增
- 变更项：提供商协议字段与路由硬约束
- 变更摘要：新增提供商必须维护 `protocol_type` 协议字段，取值仅允许 `both`、`chat_completions`、`responses`；对外路由必须同时满足提供商协议字段和模型挂载端点能力。
- 变更原因：用户要求提供商明确标注支持 Responses API、Chat Completions API 或双协议，并在选取提供商渠道时按用户请求协议类型硬筛。

- 变更时间：2026-06-05 22:52:04 +08:00
- 变更类型：替换
- 变更项：工具调用请求路由硬约束
- 变更摘要：将原“工具请求必须只路由到明确标记支持工具调用的模型渠道”替换为“工具调用是 Chat Completions 与 Responses 的标准协议能力，不作为模型级路由硬过滤条件”；正式工具请求仍受请求入口原生端点、权限、容量、流式、图像理解、图片生成等其它硬约束限制；端点级工具探针保留为可用观测，不直接阻断正式流量。
- 变更原因：用户确认本项目所有模型都应具备工具调用能力，且 Chat Completions 与 Responses 两种协议都支持工具调用；旧的模型级 `supports_tools` 与工具探针硬过滤会把可用请求误判为 `model_tools_not_available`。

- 变更时间：2026-06-05 23:43:56 +08:00
- 变更类型：替换
- 变更项：模型级 Chat/Responses 协议支持判定
- 变更摘要：将模型级 Chat Completions / Responses 协议支持从模型名家族硬编码改为可用性检测和端点探针优先；未探测或新增模型默认两种协议都可路由，只有可用性检测、端点探针或明确配置证明对应端点不可用时才收紧。
- 变更原因：旧实现按模型名前缀把 DeepSeek、GLM、Kimi、MiMo 等模型标记为 `supports_responses=false`，导致未探测模型的 `/v1/responses` 请求在路由层被误拦截。

- 变更时间：2026-06-06 00:19:33 +08:00
- 变更类型：替换
- 变更项：Responses→Chat 单向兼容适配模式
- 变更摘要：将原绝对禁止 `/v1/responses` 与 `/v1/chat/completions` 互转的规则替换为默认仍禁止互转，但允许在显式开启、默认关闭的 Responses→Chat 单向兼容适配模式下，把配置允许的 Responses 请求转换为上游 Chat Completions 请求；适配层必须标记为兼容模式并覆盖文本、流式、图片输入、自定义工具调用和 `previous_response_id` 会话续写。
- 变更原因：用户确认需要让只能发送 `/v1/responses` 的 Agent 访问国内仅提供 Chat Completions API 的大模型，且要求该能力可选、默认关闭并严格单向。

- 变更时间：2026-06-06 15:21:00 +08:00
- 变更类型：新增
- 变更项：Responses→Chat 适配会话快照治理规则
- 变更摘要：新增适配层会话快照必须受 TTL、单条快照字节上限与后端清理策略约束；数据库存储必须定时清理过期快照，Redis 依赖 TTL，内存依赖惰性清理；`truncation=disabled` 且快照超限时必须返回明确错误。
- 变更原因：适配层会话快照会随对话增长而扩张，若无统一治理会持续占用数据库和缓存空间并放大延迟，属于长期可复用的项目级约束。

- 变更时间：2026-06-06 15:43:00 +08:00
- 变更类型：修改
- 变更项：Responses→Chat 适配 compaction 与 `truncation=disabled` 关系
- 变更摘要：将快照超限拒绝条件修订为“`truncation=disabled` 且未通过 `context_management` 显式请求 compaction”；显式 compaction 请求允许进入适配层本地压缩治理。
- 变更原因：官方 Responses compaction 属于独立的上下文压缩能力，不等同于普通自动截断；适配层需要区分调用方禁用截断与调用方显式请求上下文压缩。

- 变更时间：2026-06-06 11:24:17 +08:00
- 变更类型：新增
- 变更项：后台管理页按钮自适应宽度与输入按钮同行布局规则
- 变更摘要：新增后台管理页与用户业务页的表单操作按钮、行内操作按钮和同组输入框旁按钮必须默认按内容宽度自适应，使用紧凑横向内边距与 `inline-flex`；输入框与同组按钮必须优先使用同行 `flex` 布局，输入框占剩余空间，按钮自然收缩。
- 变更原因：用户要求按 B 端 PC 页面按钮收缩与输入框同行布局思想重构用户管理界面，并将该类反复出现的按钮布局问题固化为长期统一规则。

- 变更时间：2026-06-06 00:00:00 +08:00
- 变更类型：新增
- 变更项：端点协议或能力不支持错误详情规则
- 变更摘要：新增对外 `/v1/*` 请求因端点协议或能力不支持被拒绝时，错误详情必须结构化返回请求端点、所需端点、所需协议、缺失能力、主原因、原因明细和诊断样本。
- 变更原因：外部调用方需要明确知道如 `/v1/responses` 请求命中 Chat-only 提供商时的具体失败原因，避免仅收到泛化错误后自行猜测。

- 变更时间：2026-06-06 15:18:08 +08:00
- 变更类型：新增
- 变更项：路由最大候选数全局配置规则
- 变更摘要：新增对外代理路由最大候选数必须支持全局系统设置，默认值为 `10`，路由设置页面允许管理员配置 `1-500` 的自然数；API Key 级配置仅在显式填写时覆盖全局值。
- 变更原因：用户要求 `max_candidate_count` 不再固定为较小值，而是开放到路由设置页面可修改，并统一默认值与校验范围。

- 变更时间：2026-06-06 16:45:45 +08:00
- 变更类型：替换
- 变更项：提供商模型端点协议权威来源
- 变更摘要：将单模型端点协议权威来源由提供商级 `protocol_type` 替换为模型挂载矩阵中的挂载级协议配置；历史提供商级字段仅保留为兼容字段。
- 变更原因：同一提供商渠道下不同模型可能支持不同端点协议，提供商级协议字段无法准确表达每个模型的真实端点能力。

- 变更时间：2026-06-06 16:45:45 +08:00
- 变更类型：替换
- 变更项：单模型即时测试策略
- 变更摘要：单模型即时测试必须在 10 秒总预算内返回；默认文本仅测流式；每项能力只测一次且禁止重试；端点按挂载级协议优先 `responses`、不支持时改测 `chat_completions`，禁止同次测试同时探测两个端点。
- 变更原因：单模型测试属于人工即时反馈场景，原阶段串行、双端点探测和失败重试会把耗时串起来，影响模型配置界面的可用性。

- 变更时间：2026-06-06 00:00:00 +08:00
- 变更类型：新增
- 变更项：内容完整性防护独立模块规则
- 变更摘要：新增内容完整性防护必须作为相对独立模块维护，模块配置、检测接口、内部提供商检测和外部渠道检测必须统一封装；其它页面、路由、服务或监控链路调用内容防护能力时必须复用该模块服务或统一数据结构。
- 变更原因：用户要求内容完整性防护具备独立前端页面、独立接口、外部渠道检测能力，并避免其它层重复拼装配置口径或检测逻辑。

- 变更时间：2026-06-07 00:00:00 +08:00
- 变更类型：修改
- 变更项：Linux 部署与云服务器启动规范
- 变更摘要：将原“Linux 部署与阿里云启动规范”扩展为“Linux 部署与云服务器启动规范”；保留 `start_aliyun.sh` 作为历史兼容通用部署底座，允许腾讯云等云厂商通过包装脚本注入默认参数；新增 2C2G 轻量云服务器默认使用 2 个以内 Web worker、下调连接池并优先启用独立后台 worker 服务的规则。
- 变更原因：项目生产服务器从阿里云迁移到腾讯云 2C2G 轻量实例，需要长期支持云厂商包装入口和小规格实例资源约束，避免沿用阿里云高规格默认值导致响应延迟升高。

- 变更时间：2026-06-07 00:00:00 +08:00
- 变更类型：新增
- 变更项：内容完整性文本检测规则统一注册表
- 变更摘要：新增内容完整性文本检测规则必须通过内容防护模块的统一规则注册表读取、保存和执行；禁止代理链路、可用性探针、前端页面或其它业务层散落硬编码可增删的文本检测规则。
- 变更原因：用户要求内容完整性防护规则和配置可统一管理，并支持后续在统一基座上新增或减少检测规则。

- 变更时间：2026-06-07 11:47:36 +08:00
- 变更类型：新增
- 变更项：高密度对象列表渐进式披露规则
- 变更摘要：新增后台管理页与用户业务页中的高密度对象列表必须采用“核心主列 + 按需详情”结构；主表仅展示识别、状态、关键指标和高频操作，长文本、详细策略、凭据说明、低频操作等必须收纳到详情行、详情抽屉、弹层或更多菜单。
- 变更原因：提供商对象目录字段过多且同级平铺，影响扫读效率；该类问题会在其它对象管理列表中重复出现，需要形成长期统一的前端展示规则。

- 变更时间：2026-06-08 18:07:24 +08:00
- 变更类型：修改
- 变更项：路由耗尽无限重试开关层级
- 变更摘要：明确全局无限重试开关是总闸；API Key 级无限重试只有在全局开关同时开启时才生效；无 API Key 路由上下文的内部兼容调用仅受全局开关控制。
- 变更原因：用户确认 API Key 级无限重试应保留给用户自定义，但必须服从全局无限重试开关，避免局部配置绕过全局禁用策略。

- 变更时间：2026-06-07 12:30:47 +08:00
- 变更类型：新增
- 变更项：流式内容完整性防护切换边界
- 变更摘要：新增对外流式响应内容完整性防护规则：未向客户端发送 SSE 数据前允许切换候选 provider；已发送下游数据后禁止切换或拼接其它 provider 内容，必须发送标准错误事件并以 `[DONE]` 收尾，同时记录污染证据和终态日志。
- 变更原因：流式响应已输出后再切换渠道会造成客户端收到多个上游内容片段拼接，破坏回答一致性和协议语义，需要形成长期统一处理边界。

- 变更时间：2026-06-07 13:35:05 +08:00
- 变更类型：新增
- 变更项：低信任流式内容完整性双阈值预检规则
- 变更摘要：新增低信任 provider 的流式内容完整性预检默认采用“字节阈值或最大等待时间先到即检测”的双阈值模式，默认最大等待 300ms；首段放行后继续按 chunk 滑动窗口检测，并记录检测耗时、缓冲等待、重试 provider 数和最终处理策略。
- 变更原因：低信任流式响应需要在低延迟与污染拦截之间形成长期统一边界，避免只按字节缓冲造成首包不可控，或只透传扫描导致高风险内容已输出后无法无损切换。

- 变更时间：2026-06-07 00:00:00 +08:00
- 变更类型：新增
- 变更项：提供商模型挂载矩阵独立页面规则
- 变更摘要：新增提供商模型挂载矩阵必须作为独立后台页面通过左侧导航进入，禁止继续作为提供商对象目录页内附属卡片展示。
- 变更原因：挂载矩阵属于高密度配置与检测工作台，放在对象目录页内会造成信息层级混乱和页面负担，需要形成长期导航与页面边界规则。

- 变更时间：2026-06-07 00:00:00 +08:00
- 变更类型：新增
- 变更项：模型可用性与可信度独立状态规则
- 变更摘要：新增模型可用性仅表示可用性且用户可见状态为可用、异常、未检测；可信度仅表示内容是否正常可信且用户可见状态为可信、异常、未检测；两者手动编辑、测试更新和异常原因展示必须相互独立。
- 变更原因：模型可用性与内容可信性属于不同治理维度，混用会导致可用性检测、内容完整性检测和管理员判断互相覆盖。

- 变更时间：2026-06-07 00:00:00 +08:00
- 变更类型：新增
- 变更项：提供商对象目录可信属性汇总规则
- 变更摘要：新增对象目录状态字段必须区分可用性与可信属性；全部可用仅表示所有启用模型可用；可信属性由所有模型可信度汇总计算，支持可信、部分可信、不可信、未检测，并且非可信状态必须用统一问号帮助图标说明原因。
- 变更原因：提供商级可信状态需要可追溯地来自模型级可信度，避免把单模型可用、全部可用、内容可信等概念混淆。

- 变更时间：2026-06-07 00:00:00 +08:00
- 变更类型：替换
- 变更项：上游服务对象中文术语
- 变更摘要：将项目中文业务术语由历史旧称统一替换为“提供商”；前端界面、项目文档、提示语、审计摘要、错误说明和测试数据必须使用“提供商”，英文 `provider` 字段、接口路径、数据库表名和第三方协议标识继续保留。
- 变更原因：用户确认将原提供商管理相关历史命名统一升级为“提供商管理”，需要形成长期唯一术语，避免界面与文档继续混用。

- 变更时间：2026-06-07 00:00:00 +08:00
- 变更类型：新增
- 变更项：提供商最大重试次数规则
- 变更摘要：新增提供商最大重试次数必须统一默认并回填为 `2`；新增提供商默认值为 `2`；提供商最大重试次数必须小于等于全局最大重试次数；全局最大重试次数不得小于 `2`。
- 变更原因：用户要求所有提供商最大重试次数统一为 2，新增提供商默认也为 2，并且不得超过全局最大重试次数。

- 变更时间：2026-06-07 00:00:00 +08:00
- 变更类型：新增
- 变更项：只走可信提供商全局化规则
- 变更摘要：新增 `只走可信提供商` 只能作为全局路由设置维护，默认关闭；API Key、用户端、模板、批量授权或其它局部入口禁止独立配置该开关。
- 变更原因：API Key 级可信限定会导致不同密钥独立筛除路由候选，造成 `/v1/models` 返回空且配置口径分散；用户确认该能力必须收敛到全局路由配置。

- 变更时间：2026-06-08 02:00:05 +08:00
- 变更类型：替换
- 变更项：端点协议支持权威来源
- 变更摘要：将 Chat Completions / Responses 端点协议支持的权威来源统一替换为管理员在提供商模型挂载矩阵中的挂载级协议配置；可用性检测、端点测试和探针结果只能记录可用性或能力结果，禁止自动反推、改写或收紧协议支持。
- 变更原因：用户明确要求 provider / model 是否支持 `/chat/completions` 或 `/responses` 不再由代码探测判断，而由管理员后台手动设置，需删除旧的可用性探针反向判定口径。

- 变更时间：2026-06-08 00:00:00 +08:00
- 变更类型：替换
- 变更项：内容防护可信检测探针组成
- 变更摘要：内容防护可信检测禁止维护或触发拒答探针；可信状态必需探针收敛为固定答案完整性与外链广告识别，JSON、SSE、工具调用仅作为可选能力完整性探针。
- 变更原因：用户要求全面取消拒答探针，避免安全测试输入被上游安全层拒收并干扰可信检测结论。

- 变更时间：2026-06-08 00:00:00 +08:00
- 变更类型：替换
- 变更项：模型能力来源与前端标签
- 变更摘要：将模型能力权威来源由模型目录共享字段替换为提供商模型挂载矩阵的挂载级能力；模型配置列表按同一模型所有挂载提供商能力 OR 聚合展示；前端能力标签统一为 `流式`、`工具`、`图片`、`生图`，新增模型默认支持流式、工具、图片且默认不支持生图。
- 变更原因：同一模型在不同提供商下能力可能不同，模型配置列表若继续使用目录字段或模型名推断会与挂载矩阵和实际路由能力不一致。

- 变更时间：2026-06-08 00:00:00 +08:00
- 变更类型：替换
- 变更项：内容防护可信检测探针组成
- 变更摘要：将可信状态必需探针由固定答案完整性与外链广告识别替换为固定答案完整性、外链广告识别和流式污染检测；外链广告识别探针必须真实请求被测大模型并用本地规则检测返回内容，流式污染检测必须纳入可信状态判断，拒答探针仍禁止维护或触发。
- 变更原因：用户确认可信检测必须覆盖真实上游返回内容和流式污染场景，避免仅用本地样本自测或非流式固定答案误判提供商可信。

- 变更时间：2026-06-08 00:00:00 +08:00
- 变更类型：新增
- 变更项：内容防护三层职责与接口分组
- 变更摘要：新增内容防护必须拆分为预先防护 `ContentTrustProbeService`、请求过程中防护 `ContentRuntimeGuardService`、规则算法 `ContentGuardRuleService` 三层；预先防护、运行时防护和通用配置接口按 `/precheck/*`、`/runtime/*`、`/settings` 与 `/rules` 分组，历史接口仅作为兼容入口转调。
- 变更原因：用户要求内容防护模块从概念和工程接口上明确区分模型调用前可信检测与请求过程中的实时响应检测，避免探针、规则、代理运行时策略继续混在单一模块中。

- 变更时间：2026-06-08 00:00:00 +08:00
- 变更类型：新增
- 变更项：内容防护预先防护自动检测
- 变更摘要：新增内容防护预先防护必须提供独立自动检测开关和可配置检测间隔，自动检测由内容防护配置控制，不再依赖全局可用性检测自动开关；每次自动检测必须更新提供商模型可信度或内容完整性状态。
- 变更原因：用户要求在内容防护页面单独配置预先防护自动检测时间和启停状态，并让自动检测结果持续影响可信度与正式路由筛选。

- 变更时间：2026-06-08 00:00:00 +08:00
- 变更类型：修改
- 变更项：内容防护运行时流式预检延迟上限
- 变更摘要：将内容防护流式首段预检的可配置最大等待时间与运行时兜底上限统一限制为不超过 500ms；默认值仍为 300ms，首段放行后继续执行 chunk 滑动窗口检测。
- 变更原因：用户要求响应过程中的内容防护最多只增加 500ms 等待延迟，避免完整缓冲或过大配置拖慢正式流式响应。

- 变更时间：2026-06-08 00:00:00 +08:00
- 变更类型：新增
- 变更项：后台定时任务分布式锁失败处理
- 变更摘要：新增多 worker 或多进程共享后台定时任务必须先获取 Redis 分布式锁或等价共享锁；锁被占用或锁服务不可用时必须跳过本轮并记录后台任务日志，禁止降级为无锁执行。
- 变更原因：修复 Redis 锁不可用时可用性检测、内容探针、Token backfill、清理任务等可能在多 worker 生产环境重复执行的问题，需要沉淀为长期统一运行规则。

- 变更时间：2026-06-08 00:00:00 +08:00
- 变更类型：替换
- 变更项：API Key 局部路由策略与配额字段
- 变更摘要：删除 API Key 与策略模板中的旧路由策略字段、API Key 级最大候选数、API Key 级无限重试开关和 API Key 级 Token/请求/金额配额字段；API Key 路由上下文仅保留授权、安全约束和可用性优先偏好因子，路由最大候选数与无限重试均收敛为全局设置。
- 变更原因：旧字段未形成真实执行闭环且与可用性优先规范冲突，会造成用户可见配置、诊断结果和真实请求行为不一致。

- 变更时间：2026-06-08 00:00:00 +08:00
- 变更类型：新增
- 变更项：IP 管理模块独立性与默认关闭规则
- 变更摘要：新增 IP 管理必须作为独立功能模块维护，配置、可信代理解析、规则匹配、限流、事件和测试接口必须统一封装；默认所有检测、记录、限流和阻断能力关闭，关闭时不得改变 API Key 鉴权、请求日志、路由转发和现有来源 IP 白名单行为。
- 变更原因：用户要求按 IP 管理模块方案完整落地，并保证新增模块不影响现有功能。

- 变更时间：2026-06-08 00:00:00 +08:00
- 变更类型：新增
- 变更项：IP 可信代理解析与错误响应规则
- 变更摘要：新增安全判断禁止直接信任 `X-Forwarded-For`、`Forwarded`、`CF-Connecting-IP` 等可伪造请求头；仅直连来源命中可信代理 CIDR 时可使用转发头解析真实客户端 IP；对外 `/v1/*` 处置必须返回 OpenAI 兼容错误，对内 `/api/*` 处置必须返回内部 JSON 错误且不得污染外部鉴权日志。
- 变更原因：IP 识别属于安全决策，必须形成可信代理边界和内外响应分离的长期规则。

## 2026-06-09

- 变更时间：2026-06-09 23:20:00 +08:00
- 变更类型：替换
- 变更项：内容防护可选探针组成
- 变更摘要：内容防护可信检测禁止维护或触发工具调用探针；严格 JSON 探针保留为默认关闭的可选能力完整性探针，且只能通过内容防护功能页面配置启用。
- 变更原因：用户确认工具调用探针对内容防护用途不大，要求删除相关逻辑，并要求 JSON 探针默认关闭且配置入口收敛到内容防护页面。

- 变更时间：2026-06-09 21:00:00 +08:00
- 变更类型：修改 / 新增
- 变更项：对外路由候选数量与候选耗尽重试规则
- 变更摘要：将对外代理路由候选数表述调整为“基础最大候选数”，新增全局候选扩展数 `route_candidate_expand_count`，默认 `5`，路由设置页面可配置 `0-100`；同一轮必须尝试 `max_candidate_count + route_candidate_expand_count` 个排序候选，全部失败后才进入下一轮候选耗尽等待重试；候选耗尽原因不再包含独立失败率硬过滤。
- 变更原因：用户要求删除 provider 最大错误率硬过滤，并要求候选截断后同一轮自动扩展额外候选，降低故障切换遗漏风险。

## 2026-06-10

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：对外请求体慢上传保护
- 变更摘要：新增对外 `/v1/*` 请求体读取必须同时受总读取超时与空闲读取超时约束；客户端上传过慢或长期无数据时必须停止读取并返回统一超时错误。
- 变更原因：入口层仅限制请求体字节数无法覆盖慢客户端长期占用连接和 worker 的风险，需要沉淀为长期统一入口保护规则。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：无效 API Key 负缓存
- 变更摘要：新增对外 Bearer API Key 鉴权对重复无效 Key 必须使用短 TTL 的 SHA256 哈希负缓存或等价共享防护，并在 API Key 生命周期变更时同步清理正负缓存。
- 变更原因：重复无效鉴权会在高并发或恶意重试场景下放大数据库压力，且防护必须避免保存原始明文 Key。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：路由前入口全局并发租约
- 变更摘要：新增对外 `/v1/*` 请求必须在进入路由、鉴权和请求体解析前获取独立入口全局并发租约；入口租约必须使用独立 Redis 命名空间，禁止与业务并发租约复用同一计数键。
- 变更原因：业务并发租约若在请求体解析后才申请，瞬时并发突刺仍可能消耗鉴权、解析和 worker 资源，需要前置入口保护。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：共享 Redis 缓存前缀失效与 TTL 抖动
- 变更摘要：新增共享 Redis 缓存的前缀失效必须优先使用版本号、命名空间或等价 O(1) 机制，禁止在高频变更路径扫描大 keyspace；Redis 缓存 TTL 必须支持可配置抖动。
- 变更原因：配置、模型和提供商变更会频繁触发缓存失效，扫描删除容易阻塞 Redis；固定 TTL 又会造成热点缓存集中回源，需要沉淀为长期缓存治理规则。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：Token/计费补全候选部分索引
- 变更摘要：新增 Token/计费补全候选查询必须维护与候选条件一致的 PostgreSQL 部分索引，覆盖正式日志类型、成功状态、外部 API Key、非模型列表请求、未完成或 pending 计费状态和最大补全尝试次数。
- 变更原因：后台补全会持续扫描 `request_logs` 中未完成计费的正式请求，日志表膨胀后若缺少部分索引会造成慢查询和后台积压。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：宿主配置推荐带宽约束
- 变更摘要：新增运维监控宿主配置推荐必须把公网出口带宽作为容量硬约束；无法获得可信 Mbps 值时必须要求管理员输入带宽后再生成方案，且每个方案必须说明 CPU、内存、数据库、上游连接池、网络带宽和大 payload 限制的计算依据。
- 变更原因：宿主流量计数无法反推出云服务器公网带宽上限，容量推荐若忽略带宽会高估稳定并发，必须形成长期统一口径。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：修改
- 变更项：宿主配置推荐带宽输入来源
- 变更摘要：将公网出口带宽来源收敛为管理员手动输入，禁止自动获取或从运行时配置兜底。
- 变更原因：公网出口带宽属于云厂商规格或机房链路上限，自动推断和配置兜底都可能让推荐结果产生误导，用户明确要求改为手动输入。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：内容防护探针协议优先级
- 变更摘要：新增内容防护可信检测与预检探针必须默认优先使用 Chat Completions，并允许管理员在内容防护配置页切换探针协议优先级；该选择不得改变对外 `/v1/*` 正式代理协议语义。
- 变更原因：部分可用模型只在 Chat 协议下稳定响应，探针默认协议若与模型实际可用协议不一致会造成误判或超时，需要形成统一可配置口径。

- 变更时间：2026-06-10 16:57:53 +08:00
- 变更类型：新增
- 变更项：提供商内容完整性状态编辑边界
- 变更摘要：新增提供商内容完整性状态与评分必须由内容防护探针、自动隔离、手动恢复或模型挂载可信状态汇总链路维护，提供商基础编辑表单禁止暴露 `trust_level`、`content_integrity_status`、`content_integrity_score` 作为普通可编辑配置项。
- 变更原因：人工在提供商编辑表单填写信任等级、完整性状态和评分会与对象目录中的系统状态口径冲突，并可能覆盖自动检测沉淀结果。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：模型端点协议检测触发边界
- 变更摘要：新增模型端点协议检测只能由提供商管理页选中提供商按钮、挂载矩阵选中挂载按钮、新增提供商后自动检测、或新增模型挂载后自动检测触发；检测必须直连标准 Chat Completions 与 Responses 上游端点，禁止通过 fallback、协议适配、正式代理入口或其它链路隐式触发。
- 变更原因：端点协议检测用于修正模型挂载协议字段，若复用普通可用性检测、内容防护探针或正式代理 fallback，会掩盖真实端点支持状态并造成误判。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：上游探针统一限频与限频结果处理
- 变更摘要：新增可用性检测、内容防护、模型端点协议检测及后续所有上游探针必须先通过统一探针限频；同一提供商同一挂载模型默认每 60 秒最多 4 次真实上游探针请求；限频结果必须使用 `probe_rate_limited` 并展示具体原因，且禁止更新协议、可用、可信、内容完整性、失败次数或熔断等沉淀字段。
- 变更原因：探针被频率限制时不代表上游失败或模型不可用，必须形成统一错误语义和状态写入边界，避免检测误判污染长期状态。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：新增
- 变更项：非新会话最近提供商优先直连策略
- 变更摘要：新增对外 `/v1/*` 非新会话请求在硬约束通过时必须优先直连同一会话最近一次成功使用的提供商及模型挂载；该直连允许忽略可用与熔断状态，上游失败后必须触发一次可用性探针，探针可用才允许同提供商重试一次，否则排除该提供商并进入常规候选排序与候选耗尽策略。
- 变更原因：同一会话连续请求通常应保持提供商一致性，避免可用状态滞后或误判导致无必要切换；同时通过失败后探针确认与候选回退控制风险。

- 变更时间：2026-06-10 00:00:00 +08:00
- 变更类型：替换
- 变更项：提供商超时与重试配置口径
- 变更摘要：将“提供商最大重试次数必须小于等于全局最大重试次数”替换为“提供商请求超时与最大重试次数必须以提供商级具体配置为准”，禁止通过全局最大重试次数覆盖、钳制或回填提供商级配置。
- 变更原因：全局重试配置会与具体提供商配置冲突，导致管理员在提供商页面设置的超时和重试策略无法稳定生效。

- 变更时间：2026-06-10 20:30:00 +08:00
- 变更类型：新增
- 变更项：可用、熔断与内容完整性状态边界
- 变更摘要：新增可用状态仅表达上游可用性、熔断状态仅表达可用性故障后的路由保护、内容完整性状态仅表达可信风险；普通可用性探针禁止写入或清理内容完整性字段，内容完整性链路禁止通过可用或熔断字段表达内容风险。
- 变更原因：可用、熔断和内容完整性混写会导致前端、诊断与路由判断口径不一致，并可能把内容风险误判为上游不可用。

- 变更时间：2026-06-10 20:30:00 +08:00
- 变更类型：新增
- 变更项：有效可用状态返回口径
- 变更摘要：新增提供商、模型挂载、路由诊断和候选 trace 返回可用状态时必须统一提供 `db_health`、`runtime_health`、`effective_health`、`state_source` 和状态更新时间；若对象已有业务 `updated_at`，可用状态更新时间使用 `health_state_updated_at` 或诊断样本中的 `updated_at`。
- 变更原因：统一 DB 可用、运行时可用与有效可用的读取口径，减少前端页面、诊断接口和路由 trace 的状态展示差异。

- 变更时间：2026-06-14 00:00:00 +08:00
- 变更类型：新增
- 变更项：部署默认值与配置定义同步规则
- 变更摘要：新增当 `app/config.py` 新增或调整影响启动、并发、探针、计费、适配器或后台任务的环境变量时，部署脚本、`.env.example` 与例行更新脚本必须同步补齐对应默认值。
- 变更原因：项目配置项持续演进时，如果部署脚本与 `.env.example` 不同步，旧 `.env` 会缺失新键并在云服务器上形成不一致部署口径。

- 变更时间：2026-06-10 23:20:00 +08:00
- 变更类型：新增
- 变更项：腾讯云离线部署包排除规则
- 变更摘要：新增腾讯云离线部署包必须通过项目内置打包脚本生成，并默认排除 `.env`、`.venv`、`data/`、`logs/`、`node_modules/`、本地缓存、运行日志、数据库转储和临时文件。
- 变更原因：离线上传云服务器时若混入本地密钥、虚拟环境、运行态数据或缓存文件，会造成安全风险、包体膨胀和 Linux 运行环境不一致，需要形成长期统一打包口径。

- 变更时间：2026-06-10 23:40:00 +08:00
- 变更类型：新增
- 变更项：上游探针传输异常与非协议错误处理
- 变更摘要：新增可用性检测、内容防护和模型端点协议检测等上游探针必须优先请求 `Accept-Encoding: identity`；若上游返回额度不足、全部渠道不可提供、5xx、超时或压缩响应头异常等非协议语义错误，必须展示具体原因，禁止写成端点不支持、内容可信失败或长期状态降级。
- 变更原因：真实聚合渠道可能因额度、渠道池或错误压缩头导致探针失败，但这不等价于模型端点不支持或内容不可信，需要避免误判污染协议、可用和内容完整性状态。

- 变更时间：2026-06-11 00:00:00 +08:00
- 变更类型：新增
- 变更项：维护模式路由与自动探针边界
- 变更摘要：新增维护模式中的提供商必须从正式路由候选中排除；自动可用性、可信和端点协议检测禁止向其发起上游探针，意外命中时必须返回 `provider_maintenance_mode`，且禁止写入可用、熔断、协议、可信和内容完整性等沉淀状态；用户手动检测仍允许执行。
- 变更原因：维护模式是管理员主动声明的临时不可用状态，自动探针若继续请求或把维护中结果归类为上游失败，会污染路由和长期可用/可信状态。

- 变更时间：2026-06-11 17:30:00 +08:00
- 变更类型：新增
- 变更项：提供商编辑表单密钥回显与脱敏边界
- 变更摘要：新增提供商基础编辑表单中的上游 `API Key` 必须在编辑态完整明文回显，并作为修改当前密钥处理；列表、详情摘要、审计日志和非编辑态展示必须继续使用脱敏值。
- 变更原因：编辑提供商时密钥字段若空白或按新增语义处理，会造成管理员误判当前密钥状态；同时明文回显不能扩散到审计和普通展示链路。

- 变更时间：2026-06-11 17:45:00 +08:00
- 变更类型：新增
- 变更项：配置保存与慢速自动探针解耦
- 变更摘要：新增提供商和模型挂载的新增、编辑保存接口必须先完成配置持久化并快速返回明确结果；后续自动可用性、可信或端点协议检测必须转入后台执行并记录审计。
- 变更原因：保存操作若同步等待真实上游探针，会导致表单长时间无结果反馈，并把配置持久化与慢速外部网络检测耦合。

- 变更时间：2026-06-11 20:15:07 +08:00
- 变更类型：新增
- 变更项：Windows 本地 run.ps1 重启接管旧启动进程
- 变更摘要：新增 Windows 本地开发重启必须通过 `run.ps1` 管理同项目旧启动进程；脚本优先停止同项目旧 uvicorn 子进程并释放端口，若启动锁仍被旧 `run.ps1` 宿主持有，则允许停止旧宿主后接管启动锁。
- 变更原因：旧启动窗口或残留 `run.ps1` 宿主可能持有项目启动锁，直接抛错会阻断本地重启，需要形成统一的自动接管口径。

- 变更时间：2026-06-11 21:30:54 +08:00
- 变更类型：新增
- 变更项：小型控件与徽标圆角例外
- 变更摘要：新增后台管理页与用户业务页中的按钮、输入框、下拉框、标签、状态徽标、方法徽标、倍率徽标等小型交互或标识元素允许使用圆角或胶囊样式；页面容器、卡片、弹窗、抽屉、表格壳等大结构仍保持直角。
- 变更原因：日志列表等高密度界面需要通过小型徽标与紧凑控件提升扫读效率，完全直角会削弱状态识别与参考设计一致性。

- 变更时间：2026-06-11 21:30:54 +08:00
- 变更类型：新增
- 变更项：请求日志列表密钥展示边界
- 变更摘要：新增请求日志列表中的密钥列默认只展示密钥名称，具体密钥值必须收纳到悬浮提示、详情弹层或详情页，并提供一键复制能力，禁止在列表主列常驻展示完整密钥或密钥前缀。
- 变更原因：日志列表需要减少横向信息噪音并降低密钥暴露面，同时保留排障时查看与复制具体密钥的能力。

- 变更时间：2026-06-11 22:05:00 +08:00
- 变更类型：替换
- 变更项：前端圆角边界
- 变更摘要：将原“全站矩形控件必须直角”替换为“大结构保持直角、小型交互或标识元素允许圆角或胶囊”，并明确请求日志表格中的状态徽标、方法徽标、倍率徽标允许使用圆角胶囊样式。
- 变更原因：原直角规则与后续小元素圆角例外并存造成规范冲突，需要明确日志高密度表格的小型徽标可使用圆角胶囊，同时继续约束大结构直角。

- 变更时间：2026-06-11 22:49:33 +08:00
- 变更类型：新增
- 变更项：管理后台项目重启入口安全边界
- 变更摘要：新增管理后台项目重启入口必须仅允许管理员通过固定内部接口触发 `pwsh -NoLogo -NoProfile -ExecutionPolicy Bypass -File ".\run.ps1"`；接口禁止接收或拼接任意命令，必须快速返回触发结果并记录管理员审计。
- 变更原因：项目重启属于高风险本地运维动作，若前端或接口允许传入任意命令会扩大安全风险；同时重启命令会接管旧进程，接口必须避免长时间等待并保留审计追踪。

## 2026-06-12

- 变更时间：2026-06-12 12:21:11 +08:00
- 变更类型：新增
- 变更项：外部提供商密钥管理接口安全边界
- 变更摘要：新增外部脚本或第三方系统查询、更新提供商上游密钥时必须使用 `/api/provider-credentials/*` 管理接口，并在每次请求体中提交管理员账号密码完成鉴权；更新操作必须复用密钥轮换服务、刷新运行时缓存和相关可用状态、记录管理员审计，且审计内容只能保存脱敏密钥，禁止接入 `/v1/*` 或绕过审计直接写库。
- 变更原因：新增面向外部调用的提供商密钥查询与更新能力，属于高敏感管理入口，需要固化鉴权、路径、审计和状态刷新边界，避免后续实现分散或误接入正式代理入口。

- 变更时间：2026-06-12 00:00:00 +08:00
- 变更类型：替换
- 变更项：业务时间统一口径
- 变更摘要：将项目生成、存储、查询、统计、展示、日志、脚本和部署默认时间口径统一替换为北京时间（`Asia/Shanghai`，UTC+08:00），并明确禁止继续把 UTC 或其它时区作为业务默认值、界面选项、接口提交格式、统计自然日/月边界或兼容旧格式保留。
- 变更原因：用户确认前后端、数据库和时间相关选项必须全部使用北京时间，旧 UTC 统计与显示口径不再适用。

- 变更时间：2026-06-12 12:21:11 +08:00
- 变更类型：新增
- 变更项：前端交互控件点击命中范围
- 变更摘要：新增按钮、图标、开关、帮助提示等交互控件的点击命中范围必须限制在控件自身可见区域或明确可见的控件区域内；禁止父级 `label`、整行/整卡点击委托、透明覆盖层或过大绝对定位区域吞掉空白、相邻元素或其它控件点击。
- 变更原因：避免点击自动可用性检测等开关或按钮时被误判为点击问号帮助图标，统一约束全站交互命中边界。

- 变更时间：2026-06-12 16:28:48 +08:00
- 变更类型：替换
- 变更项：提供商模型端点协议枚举与原生协议探针
- 变更摘要：将提供商模型挂载协议字段扩展为 `responses`、`chat_completions`、`both`、`gemini`、`claude_messages` 五种取值；新增挂载默认协议按模型族推断，GPT 系列默认 `both`，中国国产模型系列默认 `chat_completions`，Gemini 系列默认 `gemini`，Claude 系列默认 `claude_messages`，未知模型默认 `chat_completions`；同步明确 Gemini、Claude 等原生协议挂载的可用性检测、可信检测、单模型即时测试和模型端点协议检测必须通过统一原生协议适配层请求官方原生端点，协议检测成功后保持原生协议值，禁止写成 OpenAI 兼容协议。
- 变更原因：新增 Gemini 与 Claude 模型接入后，旧规范只允许 OpenAI 兼容三态协议并要求协议检测只直连 `/v1/chat/completions` 与 `/v1/responses`，无法表达官方原生协议与探针路径，需要按用户确认的方案 B 固化长期可追踪规则。

- 变更时间：2026-06-12 16:49:43 +08:00
- 变更类型：替换
- 变更项：批量上游探针同提供商模型调度规则
- 变更摘要：将原可用性检测与模型级检测中的同阶段模型并行口径替换为“不同提供商并行、同一提供商内按模型顺序 10 秒错峰启动”；后一模型默认等待前一模型完成后启动，若前一模型超过 10 秒仍未完成则后一模型必须启动；前端所有探针按钮必须调用后端统一调度或遵守同等规则，并逐项反馈进度和结果。
- 变更原因：同一提供商内单个慢模型会拖垮其它模型检测速度，需要保留同提供商顺序约束的同时避免慢探针无限阻塞后续模型。

## 2026-06-13

- 变更时间：2026-06-13 00:12:39 +08:00
- 变更类型：新增
- 变更项：`cherry-studio-main/` 独立参考项目边界
- 变更摘要：新增 `cherry-studio-main/` 目录及其压缩包仅作为模型提供商接入、协议适配、请求/响应解析和多模型架构参考，不属于本项目运行时模块、依赖或可直接迁移代码；参考时必须按本项目边界和规范重新设计落地。
- 变更原因：该目录下是独立开源项目，若后续误认为本项目组成部分，容易引入错误依赖、重复架构或未经审查的代码迁移，需要固化参考边界。

## 2026-06-14

- 变更时间：2026-06-14 23:50:47 +08:00
- 变更类型：替换
- 变更项：云服务器例行更新数据库迁移边界
- 变更摘要：将“例行更新默认不触碰云端数据库”替换为“例行更新脚本默认可在重启 Web worker 前通过独立幂等入口自动补齐 PostgreSQL 表结构”，并要求迁移前必须生成 PostgreSQL 备份，禁止清空、重建或删除现有业务数据，备份或迁移失败时必须中止更新且不得继续重启服务。
- 变更原因：用户明确选择方案 B，要求云服务器一键更新时即使数据库表结构变化也能自动更新，同时原有数据库数据不能丢失。

- 变更时间：2026-06-14 15:52:20 +08:00
- 变更类型：替换
- 变更项：对外代理入口路径口径
- 变更摘要：将原“对外统一代理入口必须使用 `/v1/*`”替换为“OpenAI 兼容入口使用 `/v1/*`，Gemini、Claude 等官方原生协议入口按对应官方协议路径提供，并统一采用外部 Bearer API Key 鉴权”。
- 变更原因：Gemini 与 Claude 原生 SDK 使用的请求路径和请求体不同于 OpenAI 兼容协议，若强制全部收敛到 `/v1/*` 会导致原生协议请求 404、结构误解析或协议转换丢字段，需要固化多协议入口边界。

- 变更时间：2026-06-14 16:05:00 +08:00
- 变更类型：新增
- 变更项：前端体验质量规范
- 变更摘要：新增前端页面必须同时满足加载速度、稳定不卡顿、上手零门槛、容错性高、无障碍友好、视觉整洁、配色协调、图片清晰、风格统一、阅读舒适、反馈及时、交互符合习惯、动效克制、弹窗人性化、滚动流畅和表单交互贴心等长期可复用要求。
- 变更原因：用户明确要求将一组通用前端体验标准写入全局规范，用于后续页面、表单、弹窗、列表和交互实现的统一约束与验收检查。

- 变更时间：2026-06-14 16:09:00 +08:00
- 变更类型：新增
- 变更项：排行与动态数据可视化展示
- 变更摘要：新增排行类、趋势类、实时变化类和动态对比类数据必须优先使用清晰可视化方式展示，且可视化刷新或变化时必须流畅、平滑、清晰，避免闪烁、跳动、遮挡核心数值或造成误读。
- 变更原因：动态变化数据和排行数据若只用静态文字或生硬刷新展示，容易降低理解效率和观感质量，需要固化为前端数据展示的长期体验约束。

## 2026-06-16

- 变更时间：2026-06-16 00:00:00 +08:00
- 变更类型：新增
- 变更项：批量上游探针逐项展示
- 变更摘要：新增所有批量上游探针前端必须以单次探针为最小展示单元，每个提供商/模型挂载探针独立显示卡片或明确分区，并包含实时运行反馈、耗时、结果摘要、异常原因和详情入口；父级模型或提供商摘要只能作为分组与统计信息。
- 变更原因：批量检测某个模型的全部提供商或某个提供商的全部模型时，聚合结果会隐藏单个探针过程和真实错误，需要统一前端展示口径以支持排障和验收。

- 变更时间：2026-06-16 00:00:00 +08:00
- 变更类型：替换
- 变更项：可用性术语口径
- 变更摘要：将用户可见和业务文档中的旧状态口径统一替换为“可用性/可用/不可用”；`healthy` 展示为“可用”，`unhealthy` 展示为“不可用”，内部兼容字段、历史数据库列名和第三方协议标识可继续保留 `health_*` 命名。
- 变更原因：旧状态词只能表达上游是否可响应，不能表达内容可信；继续使用旧口径容易让管理员误以为响应可信，需要长期统一为可用性口径。

## 2026-06-17

- 变更时间：2026-06-17 00:00:00 +08:00
- 变更类型：新增
- 变更项：模型ID唯一标识口径
- 变更摘要：新增模型ID作为项目内唯一稳定模型标识的长期规则；对外请求中的 `model`、模型映射源与目标、API Key 白名单、路由、日志主键、缓存 key 和导入重复校验必须以模型ID为准；模型名称只能作为展示别名，历史 `model_name` 字段语义等同模型ID，`upstream_model_name` 仅作为上游模型ID用于探针、协议判断、价格匹配和代理转发上游。
- 变更原因：模型映射、提供商挂载、模型配置和对外请求之间长期混用“模型名称/模型ID”会导致路由、导入导出和上游转发语义不一致，需要固化统一标识口径。

## 2026-06-18

- 变更时间：2026-06-18 00:00:00 +08:00
- 变更类型：替换
- 变更项：请求完成后的准确 usage 即时计费
- 变更摘要：将原“费用计算、余额扣减和账单记录必须通过后台任务完成”的单一路径替换为“请求完成且日志已包含官方 usage 时必须在日志落库事务内立即完成计费，后台 Token/计费补全仅作为漏记、异常中断或历史数据修正的幂等补偿兜底”；同时明确本地 tokenizer、tiktoken、请求文本或响应文本估算 token 只能用于观测、预检或排障，禁止作为最终扣费、余额扣减或 `no_charge` 终态依据。
- 变更原因：用户明确要求能立即准确计算的请求必须立即计算并推送到前端日志记录，后续重算只能作为补偿兜底，且 token 数量必须确保准确无误。

## 2026-06-19

- 变更时间：2026-06-19 00:00:00 +08:00
- 变更类型：替换
- 变更项：管理员全局主路由策略
- 变更摘要：将原“前端、文档和对外能力说明中的路由主策略只能展示为可用性优先”替换为“管理员可在全局路由设置中选择可用性优先、低延迟优先或容量避让，默认仍为可用性优先”；同时明确 API Key、用户端、模板、批量授权等局部入口禁止覆盖全局主路由策略。
- 变更原因：用户确认方案 B，要求新增低延迟优先和容量避让策略并允许管理员全局配置，同时保持默认策略和局部入口边界清晰。
