# 日志完整审查

审查时间：2026-06-10

## 审查范围

- 后端请求日志：`request_logs`、`LogService`、`RequestLogQueueService`
- 类型化日志：请求链路子事件、异常、健康检查、计费、后台任务、用户操作、素材、后台审计、告警事件
- 前端展示：`/logs` 日志中心、用户日志页、后台审计页、告警页
- 数据库迁移与清理策略：类型化日志表迁移、保留周期、导出和分页查询

## 问题清单

1. 类型化日志迁移在 PostgreSQL 下主键不会自动生成
   - 影响：异常、健康检查、计费、后台任务、素材、请求链路子事件等类型化日志可能无法落库。
   - 状态：已完成

2. 告警日志统一适配器断链
   - 问题：`AlertLogRecorder.upsert_alert()` 调用 `AlertService.upsert_alert()`，但 `AlertService` 当前没有该方法。
   - 影响：告警类型无法通过统一日志适配器写入，统一日志模块注册入口不可用。
   - 状态：已完成

3. 告警事件没有进入统一日志中心
   - 问题：项目已有 `alert_events` 表和告警服务，但 `/api/logging/*` 没有告警事件查询/导出接口，`/logs` 页面也没有告警日志标签。
   - 影响：告警与异常、健康、计费等日志割裂，无法在日志中心按类型独立观测。
   - 状态：已完成

4. 用户操作日志接口存在但前端日志中心未展示
   - 问题：`/api/logging/user-operations` 已存在，但 `/logs` 页面没有“用户操作日志”标签，导出接口也没有覆盖该类型。
   - 影响：用户侧关键操作日志无法在统一日志中心观测和导出。
   - 状态：已完成

5. 后台审计日志没有纳入类型化日志中心
   - 问题：后台审计日志仍主要通过独立 `/audit-logs` 页面查看，`/api/logging/*` 和 `/logs` 没有统一展示与导出入口。
   - 影响：后台审计与其它类型化日志割裂，不符合所有日志模块统一管理维护的目标。
   - 状态：已完成

6. 类型化日志保留策略提示不完整
   - 问题：`_typed_log_retention()` 缺少 `user-operations`、后台审计、告警事件等类型映射，部分类型会落到请求子日志默认保留周期。
   - 影响：前端展示的保留策略与实际清理策略不一致。
   - 状态：已完成

7. 告警事件缺少独立清理策略
   - 问题：`DataRetentionService` 尚未清理 `alert_events`，系统设置中也没有告警事件保留周期。
   - 影响：告警事件长期累积后会影响查询性能和存储治理。
   - 状态：已完成

8. 请求日志与健康检查日志仍可在同一请求日志列表混合展示
   - 问题：请求日志筛选项仍包含健康检查类型，虽然默认隐藏健康检查，但用户可取消隐藏后在请求列表中混合查看；同时健康检查已有独立类型化日志标签。
   - 影响：不同日志类型边界不够清晰，容易把探针日志和正式流量日志混在一起分析。
   - 状态：已完成

9. 类型化日志刷新未等待事件队列空闲
   - 问题：请求日志列表支持 `wait_for_latest` 等待请求日志队列空闲，但 `/api/logging/*` 类型化日志接口只返回队列状态，没有提供等待队列落库的刷新参数。
   - 影响：管理员手动刷新异常、计费、后台任务等日志时，可能看到队列尚未落库前的旧数据。
   - 状态：已完成

10. 清空请求日志前未处理请求日志队列积压
    - 问题：`DELETE /api/logs` 直接清空数据库中的请求日志；若 Redis 请求日志队列仍有积压，清空后旧队列项可能继续落库，导致“清空后又出现旧日志”。
    - 影响：日志清理语义不可靠，用户容易误判清空操作失败。
    - 状态：已完成

11. 类型化请求链路事件主要依赖摘要回填，原生事件覆盖不完整
    - 问题：`RequestLogRecorder.build_events_from_summary()` 可从 `trace_json` 和请求摘要回填子事件，但部分关键阶段仍依赖推断，例如 SSE 错误是否发送、路由候选诊断、上游响应截断等。
    - 影响：复杂失败链路的时间线可能缺少真实阶段细节。
    - 状态：已完成

12. 前端日志详情缺少针对告警、审计、用户操作的专属详情展示
    - 问题：现有类型化详情渲染覆盖异常、健康、计费、内容防护、素材等类型，缺少告警、后台审计、用户操作的专属字段分组。
    - 影响：即便接口补齐后，详情面板仍可能退化为原始 JSON，不利于观测。
    - 状态：已完成

13. 告警事件缺少生产建表迁移
    - 问题：告警事件模型和服务已存在，但迁移目录缺少 `alert_events` 与告警订阅表的 PostgreSQL 建表脚本，新纳入统一日志中心后生产环境可能缺少对应表结构。
    - 影响：告警事件查询、写入、保留周期清理和告警中心页面在未执行 ORM 建表的生产环境中不可用。
    - 状态：已完成

14. 类型化日志摘要与队列状态展示被放在请求日志面板内
    - 问题：`typed-logs-summary` 和 `typed-logs-meta-strip` 位于请求日志面板中，切换到异常、计费、告警等类型化日志标签后，请求面板被隐藏，类型化摘要、队列积压和保留策略也随之不可见。
    - 影响：不同类型日志虽然已拆分标签，但类型化日志缺少首屏可观测摘要和队列/保留策略提示。
    - 状态：已完成

15. 请求日志与用户日志默认刷新未等待请求日志队列空闲
    - 问题：前端只有点击“刷新”时才传 `wait_for_latest=true`，页面首次加载、筛选切换、分页和页容量变化等默认刷新不会等待请求日志队列落库。
    - 影响：正式日志列表与用户日志列表默认刷新可能看到旧数据，不符合日志列表默认刷新实时性要求。
    - 状态：已完成

16. 请求日志导出与用户日志导出未等待请求日志队列空闲
    - 问题：`/api/logs/export` 和 `/user/logs/export` 没有 `wait_for_latest` 参数，前端导出也没有等待队列落库。
    - 影响：刚完成的请求可能已入队但尚未落库，导出的 CSV 与页面最新刷新语义不一致。
    - 状态：已完成

17. 内容防护类型化日志导出漏传请求路径筛选
    - 问题：前端类型化日志筛选使用 `request_path`，但 `/api/logging/export` 只接收 `path`，导出内容防护日志时把 `path` 传给 `request_path`，导致前端的请求路径筛选在导出中失效。
    - 影响：内容防护日志页面看到的是某个路径，导出 CSV 却可能包含其它路径日志，前后端筛选口径不一致。
    - 状态：已完成

18. 类型化日志等待队列后仍可能展示旧队列状态
    - 问题：`wait_for_typed_logging_queue()` 等待队列空闲后，`_typed_logging_queue_status()` 仍可能命中 2 秒缓存。
    - 影响：页面已等待最新日志，但元信息仍可能显示旧的待落库数量，造成观测误导。
    - 状态：已完成

19. 用户日志页缺少内容防护筛选控件导致初始化中断
    - 问题：`initUserLogs()` 将 `logs-content-guard-*` 控件作为必需元素，但 `user_logs.html` 没有这些控件，页面初始化会提前返回。
    - 影响：用户日志页可能不加载列表、不绑定刷新/导出/分页/详情事件。
    - 状态：已完成

20. 用户端“适配跳过”筛选前端发送但后端不处理
    - 问题：用户日志前端会发送 `content_guard_adaptation_skipped`，但 `/api/user/logs`、`/user/logs/export` 和 `UserPortalService.list_logs()` 未接收或透传该筛选。
    - 影响：用户端该筛选条件无效，页面、导出和后端查询口径不一致。
    - 状态：已完成

21. 用户日志总费用刷新后丢失美元单位
    - 问题：`data-log-summary-cost` 初始值为 `0 $`，但前端刷新后只写入数字。
    - 影响：用户日志汇总金额不符合项目金额展示规范，容易误读币种或单位。
    - 状态：已完成

22. 请求日志 CSV 导出表头仍是英文技术字段
    - 问题：`LogService.export_logs_csv()` 导出的请求日志 CSV 表头为 `created_at`、`log_type` 等英文字段。
    - 影响：后台和用户端日志导出与中文界面口径不一致，非技术用户阅读成本高。
    - 状态：已完成

23. 日志 CSV 导出缺少 UTF-8 BOM
    - 问题：请求日志、用户日志和类型化日志导出的 CSV 都未写入 UTF-8 BOM。
    - 影响：在部分 Windows/Excel 环境中，中文表头和中文日志内容可能乱码。
    - 状态：已完成

24. 类型化日志事件队列死信与失败计数未在日志中心展示
    - 问题：`/api/logging/*` 已返回 `dead_letter` 和 `failure_count`，但前端类型化日志元信息只展示待落库总数。
    - 影响：事件队列已经出现死信或持续失败时，日志中心仍可能只显示“待落库 0 条”，管理员无法及时发现落库失败出口。
    - 状态：已完成

25. 请求日志队列死信与失败计数在列表接口中丢失
    - 问题：`RequestLogQueueService.wait_until_idle()` 返回 `dead_letter` 和 `failure_count`，但 `/api/logs`、`/api/user/logs` 响应模型和前端提示只保留 queued/processing。
    - 影响：请求日志死信不会出现在后台和用户端日志刷新反馈中，可能误判日志已经全部成功落库。
    - 状态：已完成

26. 素材日志类型化导出漏用 Trace 筛选
    - 问题：素材日志页面支持 `trace_id` 筛选，列表接口也接收该参数，但 `/api/logging/export` 导出 `asset-events` 时没有把 `trace_id` 传入筛选条件。
    - 影响：素材日志页面和导出 CSV 的筛选口径不一致。
    - 状态：已完成

27. 计费日志按计费状态筛选时混入 Token 回填日志
    - 问题：`billing-events` 在未指定事件类型时即使选择 `billing_status`，仍会包含没有计费状态字段的 `token_finalize` 事件。
    - 影响：用户按“已计费/失败/重试”等计费状态筛选时，结果会混入无关 Token 回填日志，破坏不同计费日志子类型的观测边界。
    - 状态：已完成

28. 类型化日志导出与历史回填缺少后台审计记录
    - 问题：`/api/logging/export` 会导出异常、审计、用户操作、告警等敏感日志，`/api/logging/typed-events/backfill` 会写入历史类型化日志，但两者没有记录后台审计日志。
    - 影响：日志模块自身的敏感读取和修复动作不可追踪，不利于统一日志治理和追责。
    - 状态：已完成

29. 用户日志页 URL 中的内容防护筛选不会回显
    - 问题：用户端日志页支持内容防护结果、风险、动作、阶段、分类、是否切换提供商、是否跳过适配等筛选，但初始化时没有从 URL 参数回填这些控件。
    - 影响：用户刷新或打开带筛选条件的日志链接时，页面会按空筛选加载，前端展示与 URL/后端查询意图不一致。
    - 状态：已完成

30. 请求日志内容防护阶段和分类筛选未复用结构化子事件
    - 问题：请求日志列表按内容防护阶段筛选时只匹配 `trace_json` 中的固定文本片段，按分类筛选时只查 `request_logs.content_guard_categories_json`。
    - 影响：流式分块、首段检测或子事件中记录的内容防护阶段/分类可能被请求日志列表漏掉，且 JSON 文本格式变化会导致筛选失效。
    - 状态：已完成

31. 内容防护类型化日志模型筛选与前端语义不一致
    - 问题：前端提示“实际模型或请求模型”，但 `/api/logging/content-guard-events` 仅对 `model_name` 做精确匹配，未覆盖 `requested_model`，也不支持关键字匹配。
    - 影响：按模型查看内容防护日志时会漏掉只有请求模型或模型别名不同的事件，页面与导出结果都不完整。
    - 状态：已完成

32. 日志 CSV 导出值仍暴露英文布尔值和技术枚举
    - 问题：请求日志和类型化日志 CSV 表头已中文化，但 `true/false`、`success/failed`、`pass/block`、`high/medium` 等展示值仍直接输出英文技术值。
    - 影响：导出文件与前端中文展示口径不一致，业务用户排查日志时仍需理解底层枚举。
    - 状态：已完成

33. 请求日志详情时间线摘要使用英文键名
    - 问题：日志详情中的链路时间线摘要直接显示 `auth_result`、`status_code`、`guard_result` 等字段名。
    - 影响：详情展示与中文界面口径不一致，降低链路排查的可读性。
    - 状态：已完成
