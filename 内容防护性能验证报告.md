# 内容防护性能验证报告

验证目标：

- 内容完整性防护模块新增延迟不超过 `300 ms`。
- 64 并发下内容防护热路径不能把响应延迟推高到 `300 ms` 以上。

## 验证命令

```powershell
pwsh -NoLogo -NoProfile -Command ".\.venv\Scripts\python.exe scripts\benchmark_content_guard_overhead.py --iterations 2000 --concurrency 64"
```

该脚本直接测量内容防护在代理响应链路中的核心检测函数开销，包括：

- Chat Completions 正常响应
- Responses 正常响应
- Chat Completions 可疑内容命中响应

## 最终结果

| 场景 | 单请求 p99 新增延迟 | 单请求 max | 64 并发 p99 新增延迟 | 64 并发 max |
| --- | ---: | ---: | ---: | ---: |
| Chat 正常响应 | `0.175 ms` | `0.468 ms` | `0.294 ms` | `188.564 ms` |
| Responses 正常响应 | `0.191 ms` | `0.332 ms` | `0.236 ms` | `137.769 ms` |
| Chat 可疑内容命中 | `0.184 ms` | `0.422 ms` | `0.256 ms` | `126.344 ms` |

结论：本次验证中，内容防护核心检测热路径在单请求和 64 并发下均低于 `300 ms` 上限。

## 优化记录

初始 64KB 扫描窗口下，正常响应单请求开销约 `1-2 ms`，但 64 并发下尾延迟曾超过 `300 ms`。已完成两项优化：

- 正常低风险路径增加关键词预筛，避免无条件执行多组正则扫描。
- 默认内容扫描与流式缓冲窗口从 `65536 B` 调整为 `16384 B`，保留页面配置项可手动调大。

优化后，64 并发 max 延迟稳定落在 `300 ms` 以内。

## 注意事项

本报告验证的是内容完整性防护模块自身新增开销。完整 `/v1/*` 代理端到端延迟还会受到上游模型速度、网络、数据库、Redis、日志队列和服务器 CPU 抢占影响；这些因素应继续通过 `scripts/start_benchmark_stack.ps1` 与真实上游压测脚本单独评估。
