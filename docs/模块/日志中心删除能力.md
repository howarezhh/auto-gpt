# 日志中心删除能力

摘要：日志中心负责后台请求日志、异常事件、可用性检测日志、计费日志、内容防护日志、调度任务日志、后台审计日志、用户操作日志、告警事件和素材日志的查询、导出、详情查看、实时请求时间线与按筛选删除。关键词：request_logs、request_status、typed_logs、request_provider_attempt_events、request_route_decision_events、delete_filtered_logs、start_at、end_at、admin_audit。

最后维护时间：2026-06-18

## 职责边界

- 请求日志继续使用 `request_logs` 主表和 `LogService` 作为查询、导出和删除的统一入口。
- 请求日志的正式用户可见类型包括 OpenAI 兼容流量 `chat`、`responses`、`moderations`、`files`，以及原生协议流量 `gemini`、`claude_messages`；用户端日志页、后台日志中心、筛选项、导出和 API Key 关联日志必须使用同一口径。
- 对外请求日志必须在首次代理请求开始时立即创建或更新同一条主日志，主日志使用 `request_status` 表示实时状态：`请求中` 表示正在选择或重试提供商，`响应中` 表示已收到上游响应并正在接收剩余内容，`已完成` 表示成功、最终失败、客户端断开或中断后不再重试。历史进行中兼容状态码 `102` 仅作为回退判断。
- 每次内部提供商尝试、失败、等待重试、候选切换和候选耗尽等待必须写入请求子事件表；主日志仍保持一次外部请求一条，避免列表被内部重试刷爆。
- 类型化日志继续使用 `/api/logging/*` 路由内已有模型与筛选语义，不新增第二套日志模块。
- 删除操作只允许管理员在后台日志中心触发，用户端日志页面保持只读。
- 筛选删除必须至少包含一个明确筛选条件或时间范围；仅默认排除可用性检测不构成删除范围。清空操作必须由当前日志类型的显式清空入口触发。
- 删除请求日志不级联删除类型化子事件；各类型化日志可在对应标签页按筛选独立删除。
- 所有日志保留期配置统一限制为 1-7 天，后台设置页允许管理员在该范围内分别调整请求日志、请求链路子事件、异常、可用、计费、后台任务、审计、用户操作、告警和素材日志保留期；后端必须强制校验并归一化旧配置，禁止保留超过 7 天或永久保存语义。

## 管理接口

### 删除请求日志

- 路径：`DELETE /api/logs/filtered`
- 调用方：后台日志中心请求日志标签页。
- 核心参数：`log_type`、`provider_id`、`provider_trust_level`、`model_name`、`model_query`、`user_account_id`、`api_client_key_id`、`success`、`content_guard_*`、`conversation_key`、`tenant_name`、`project_name`、`app_name`、`environment_name`、`start_at`、`end_at`、`exclude_health_checks`。
- 行为：等待请求日志队列短暂空闲后，按当前筛选批量删除 `RequestLog`，同步清理请求日志 Redis 队列、处理中队列、死信队列和失败计数，并写入后台审计 `delete_filtered_logs`。
- 安全约束：没有筛选条件或时间范围时返回 `400`。

### 删除类型化日志

- 路径：`DELETE /api/logging/typed-events/{typed_log_type}`
- 调用方：后台日志中心类型化日志标签页。
- 支持类型：`exceptions`、`health-runs`、`billing-events`、`content-guard-events`、`background-jobs`、`admin-audits`、`user-operations`、`alert-events`、`asset-events`。
- 核心参数：复用对应列表/导出接口的筛选字段，包括 `keyword`、`start_at`、`end_at` 以及各类型专属字段。
- 清空当前类型：允许显式传入 `clear_all=true`，表示管理员已确认清空当前 `typed_log_type` 的全部日志；该参数只能表达当前类型全量清空，不能跨类型清空。
- 特殊行为：可用性检测日志删除 `HealthCheckRun` 时同步删除同批次 `HealthProbeEvent`；计费日志按 `TokenFinalizeEvent` 与 `BillingProcessEvent` 分别删除；内容防护日志通过 `RequestContentGuardEvent` 与 `RequestLog` 关联筛选后删除内容防护事件；后台任务日志只删除数据库日志，不删除 Redis 中的实时运行态。
- 持久化队列清理：类型化日志清空或筛选删除必须按当前日志类型清理对应类型化事件的 Redis 队列、处理中队列和死信队列，避免旧日志重新落库；告警事件和后台审计日志当前不经过类型化日志队列。
- 审计：所有类型化日志筛选删除均写入后台审计 `delete_typed_logs`，清空当前类型写入 `clear_typed_logs`，记录日志类型、筛选条件、底层删除数量和持久化队列清理数量。

## 前端交互

- 请求日志高级筛选增加 `开始时间` 与 `结束时间`。
- 请求日志详情必须展示完整时间线，至少包括鉴权、校验、模型权限、路由决策、提供商尝试、上游响应、流式事件、错误响应、内容防护和计费事件。
- 请求日志详情必须返回并展示原始完整响应字段，优先展示 `response_body_json`，若没有 JSON 则展示 `response_text`；普通请求体仍受日志大小与内容开关约束，非流式响应 JSON 不按 `max_logged_body_bytes` 摘要截断，但仍必须执行敏感字段脱敏和二进制图片内容摘要；列表接口仍保持轻量化，不在主列表常驻展示大响应体。
- 请求详情时间线中的提供商尝试必须展示提供商、挂载模型、路由轮次、同提供商重试序号、状态码、错误码、错误说明、是否可重试、等待秒数和等待计划；候选耗尽等待必须展示 `selection_guard=route_exhausted_wait_retry`、等待毫秒数和等待策略。
- 当主日志处于 `请求中` 或 `响应中` 时，用户端日志页和后台日志中心必须自动刷新列表；打开详情弹窗时必须继续轮询时间线，直到主日志进入 `已完成`。
- 请求日志工具栏提供 `删除筛选`，用于删除当前筛选命中的请求日志；保留原有 `清空请求日志` 兼容入口。
- 类型化日志工具栏提供 `删除筛选` 与 `清空当前类型`；`删除筛选` 使用当前标签页的关键词、时间范围和专属筛选项，`清空当前类型` 必须二次确认后发送 `clear_all=true`。
- 前端会在空筛选时先要求管理员选择开始时间或结束时间作为删除范围；后端仍保留相同安全校验作为最终兜底。
