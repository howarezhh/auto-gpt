# 工程文档索引

最后维护时间：2026-06-19

## 模块文档

| 文档名称 | 路径 | 所属模块 | 主要内容 | 关键词 | 关联代码目录 |
| --- | --- | --- | --- | --- | --- |
| 计费模块 | `docs/模块/计费模块.md` | 计费、价格、余额、用量统计、账单审计 | 原币种计费、多币种汇率快照、缓存写入价、非 token 费用组件、官方 usage 立即计费、历史 no_charge 补偿重算、billable 可计费口径、余额预占、未定价拦截、后台用户额度用量、前端计费展示契约 | billing、upstream_usage、immediate_billing、no_charge_recompute、billable、billable_reason、blocked_after_output、source_currency、billing_currency、exchange_rate_snapshot、cache_write_price_per_1k、fee_components、reservation、quota | `app/services/billing_service.py`、`app/services/token_usage_service.py`、`app/services/model_pricing_service.py`、`app/services/billing_reservation_service.py`、`app/services/proxy_service.py`、`app/services/user_portal_service.py`、`app/services/model_catalog_service.py`、`app/static/js/app.js`、`app/templates/users.html`、`app/templates/models.html`、`app/templates/user_billing.html`、`app/templates/user_models.html` |
| 提供商选择策略模块 | `docs/模块/提供商选择策略模块.md` | 路由、提供商候选、模型映射、容量、可用性、内容可信、Redis 展示降级、伪成功响应、候选耗尽重试 | API Key 路由上下文、模型映射目标选择、可插拔候选硬过滤、端点协议约束、可用性门槛、HTTP 200 固定失败文本识别、内容可信硬排除、容量过滤、Redis 不可用展示降级、路由得分、候选排序、同会话粘性、失败切换、候选耗尽等待重试、阶段化 trace、请求子事件、端到端总耗时、上游耗时、TPS 口径、诊断原因和边缘场景 | RoutingService、RoutePipeline、RouteRequest、RouteDecision、RouteFilterChain、CapacityFilter、StageTraceDiagnoser、route_strategy、availability_first、latency_first、capacity_avoidance、RoutePolicyContext、RouteCandidate、ProxyService、FixedSuccessResponseService、ModelMappingService、ProviderCapacityService、ProviderHealthStateService、ContentTrustProbeService、request_provider_attempt_events、request_route_decision_events、duration_ms、upstream_duration_ms、ttfb_ms、tps、route_score、stage_traces_json、route_exhausted_retry、diagnose_candidate_unavailability、redis_unavailable | `app/services/routing/`、`app/services/proxy_service.py`、`app/services/fixed_success_response_service.py`、`app/services/model_mapping_service.py`、`app/services/api_key_service.py`、`app/services/api_key_auth_cache.py`、`app/services/provider_capacity_service.py`、`app/services/provider_health_state_service.py`、`app/services/content_trust_probe_service.py`、`app/schemas/setting.py`、`app/services/setting_service.py`、`app/logging/adapters/request_adapter.py` |
| 提供商原生协议与模型分组 | `docs/模块/提供商原生协议与模型分组.md` | 提供商管理、模型挂载、模型配置、批量导入、批量治理、代理转发、可用性检测、内容防护 | 平台模型 ID 与上游模型 ID、Gemini 与 Claude 原生协议适配、默认端点协议矩阵、模型分组规则、批量导入字段规则、提供商对象目录分页筛选、批量启停、置为可用、置为可信、模型聚合列表分组筛选、模型配置选中测试、挂载矩阵可用性聚合、可用性/可信/端点协议探针边界、批量探针逐项展示、检测详情原始 JSON、日志字段 | provider、model_id、model_name、display_name、upstream_model_name、model_group、protocol_type、gemini、claude_messages、batch_import、batch_governance、provider_directory、availability、health_probe、model_catalog_test、provider_model_mount、batch_probe、raw_provider_response、probe_detail | `app/services/provider_service.py`、`app/services/model_catalog_service.py`、`app/services/proxy_service.py`、`app/services/health_service.py`、`app/services/content_guard_probe_service.py`、`app/routers/providers.py`、`app/static/js/app.js` |
| 日志中心删除能力 | `docs/模块/日志中心删除能力.md` | 日志中心、请求日志、类型化日志、请求时间线、后台审计 | 请求日志与类型化日志按时间范围或当前筛选删除、请求主日志三态、子事件时间线、删除接口、安全范围校验、审计记录、前端入口 | request_logs、request_status、typed_logs、request_provider_attempt_events、request_route_decision_events、delete_filtered_logs、delete_typed_logs、start_at、end_at、admin_audit | `app/routers/logs.py`、`app/routers/logging_api.py`、`app/services/log_service.py`、`app/logging/adapters/request_adapter.py`、`app/templates/logs.html`、`app/static/js/app.js` |
| IP 管理模块 | `docs/模块/IP管理模块.md` | IP 管理、可信代理、IP 规则、IP 限流、处置事件、请求日志关联 | 模块边界、作用域覆盖、默认关闭、可信代理解析、规则匹配、事件日志、请求日志回填、前端工作区和扩展约束 | IpManagementMiddleware、ClientIpResolver、ip_access_rules、ip_management_events、external_v1、internal_api、user_pages、request_log_id | `app/middleware/ip_management_middleware.py`、`app/services/ip_management_service.py`、`app/services/ip_management_resolver_service.py`、`app/services/ip_management_rule_service.py`、`app/services/ip_management_event_service.py`、`app/routers/ip_management.py`、`app/templates/ip_management.html`、`app/static/js/app.js` |

## 架构决策与改进方案

| 文档名称 | 路径 | 所属模块 | 主要内容 | 关键词 | 关联代码目录 |
| --- | --- | --- | --- | --- | --- |
| 提供商选择策略可插拔路由改造方案 | `docs/架构决策/提供商选择策略可插拔路由改造方案.md` | 路由、提供商选择、策略插件、架构改造 | 在保持现有行为不变前提下，将路由模块改造为 `context -> filters -> scorer -> orderer -> diagnoser` 可插拔策略流水线，收敛策略枚举和注册表，阶段化调试输出，ProxyService 职责收敛与测试矩阵 | RoutingService、RoutePipeline、RouteRequest、RouteDecision、RouteFilter、RouteScorer、RouteOrderer、RouteDiagnoser、策略注册表、stage_traces_json | `app/services/router_service.py`、`app/services/proxy_service.py`、`app/services/model_mapping_service.py`、`app/services/user_portal_service.py`、`app/services/routing/` |
