# aotu-gpt 项目性能优化实施计划

## 执行摘要

本文档详细规划了 aotu-gpt 项目的全面性能优化方案。基于架构分析，我们识别出 6 个高优先级瓶颈和 10 个优化方向。

**优化目标**：
- 🎯 响应时间：从 50-200ms 降低到 10-50ms
- 🎯 延迟：P99 延迟 < 100ms
- 🎯 数据一致性：强一致性保证
- 🎯 日志性能：异步批量写入，不阻塞主流程
- 🎯 并发能力：从 500 RPS/worker 提升到 2000+ RPS/worker

---

## 第一阶段：高优先级优化（立即执行）

### 优化1：为 ModelCatalog 添加 L1 内存缓存

**问题**：
- 每次请求都查询 `model_catalog` 表
- 路由匹配时频繁调用 `ModelCatalogService.get_model_catalog()`
- 无缓存，导致数据库压力大

**解决方案**：
```python
# 文件：app/services/model_catalog_service.py
# 添加 TTL-based L1 缓存

from functools import lru_cache
from datetime import datetime, timedelta

class ModelCatalogService:
    _cache = {}
    _cache_ttl = {}
    CACHE_TTL_SECONDS = 60  # 1分钟缓存

    @classmethod
    def get_model_catalog_cached(cls, db: Session, model_name: str):
        now = datetime.utcnow()
        cache_key = f"model:{model_name}"

        # 检查缓存
        if cache_key in cls._cache:
            if now < cls._cache_ttl.get(cache_key, now):
                return cls._cache[cache_key]

        # 查询数据库
        model = cls.get_model_catalog(db, model_name)

        # 更新缓存
        if model:
            cls._cache[cache_key] = model
            cls._cache_ttl[cache_key] = now + timedelta(seconds=cls.CACHE_TTL_SECONDS)

        return model

    @classmethod
    def invalidate_cache(cls, model_name: str = None):
        """使缓存失效"""
        if model_name:
            cache_key = f"model:{model_name}"
            cls._cache.pop(cache_key, None)
            cls._cache_ttl.pop(cache_key, None)
        else:
            cls._cache.clear()
            cls._cache_ttl.clear()
```

**预期收益**：
- 数据库查询减少 70-90%
- 响应时间降低 20-30ms
- 数据库连接压力降低

**风险**：
- 缓存失效延迟（1分钟内可能读到旧数据）
- 多 Worker 缓存不同步

**缓解措施**：
- 更新/删除模型时主动调用 `invalidate_cache()`
- 使用 Redis 作为 L2 缓存实现跨 Worker 同步

---

### 优化2：添加复合索引

**问题**：
- `provider_models` 表缺少 `(provider_id, model_name)` 复合索引
- 路由匹配时需要全表扫描
- `request_logs` 表的 `user_id` 查询慢

**解决方案**：
```sql
-- 文件：migrations/2026-06-06_add_performance_indexes.sql

-- 1. provider_models 表复合索引
CREATE INDEX IF NOT EXISTS idx_provider_models_provider_model
ON provider_models(provider_id, model_name);

-- 2. provider_models 启用状态索引
CREATE INDEX IF NOT EXISTS idx_provider_models_enabled_priority
ON provider_models(enabled, priority)
WHERE enabled = true;

-- 3. request_logs 用户查询索引
CREATE INDEX IF NOT EXISTS idx_request_logs_user_created
ON request_logs(user_id, created_at DESC);

-- 4. request_logs API Key 查询索引
CREATE INDEX IF NOT EXISTS idx_request_logs_apikey_created
ON request_logs(api_key_id, created_at DESC);

-- 5. api_keys 认证查询优化
CREATE INDEX IF NOT EXISTS idx_api_keys_hash_enabled
ON api_keys(key_hash, enabled)
WHERE enabled = true;

-- 6. providers 启用状态索引
CREATE INDEX IF NOT EXISTS idx_providers_enabled_priority
ON providers(enabled, priority)
WHERE enabled = true;
```

**预期收益**：
- 查询性能提升 10-100 倍
- 全表扫描变为索引扫描
- 数据库 CPU 使用降低

---

### 优化3：扩大数据库连接池

**问题**：
- 当前配置：`pool_size=5, max_overflow=7`（总共12个连接）
- 4个 Worker 并发，每个 Worker 最多 3 个连接
- 高并发时连接池耗尽，请求排队

**解决方案**：
```python
# 文件：app/database.py

# 修改前
SQLALCHEMY_DATABASE_URL = os.getenv("DATABASE_URL", "...")
engine = create_async_engine(
    SQLALCHEMY_DATABASE_URL,
    echo=False,
    pool_size=5,
    max_overflow=7,
    pool_pre_ping=True,
    pool_recycle=3600,
)

# 修改后
import math

# 动态计算连接池大小
WEB_CONCURRENCY = int(os.getenv("WEB_CONCURRENCY", 4))
CONNECTIONS_PER_WORKER = int(os.getenv("DB_CONNECTIONS_PER_WORKER", 10))
POOL_SIZE = WEB_CONCURRENCY * CONNECTIONS_PER_WORKER  # 4 * 10 = 40
MAX_OVERFLOW = int(os.getenv("DB_MAX_OVERFLOW", 20))  # 额外 20 个

engine = create_async_engine(
    SQLALCHEMY_DATABASE_URL,
    echo=False,
    pool_size=POOL_SIZE,  # 40
    max_overflow=MAX_OVERFLOW,  # 20
    pool_pre_ping=True,
    pool_recycle=3600,
    pool_timeout=30,  # 连接超时
    max_identifier_length=128,
)
```

**预期收益**：
- 连接池从 12 增加到 60
- 高并发下不再等待连接
- 吞吐量提升 2-3 倍

**注意事项**：
- PostgreSQL `max_connections` 需要调整到至少 100
- 监控连接使用情况

---

### 优化4：优化 API Key 认证缓存

**问题**：
- 每次请求都查询 `api_keys` 表
- 缓存使用 `CacheService`，但有锁竞争
- 缓存预热不足

**解决方案**：
```python
# 文件：app/services/cache_service.py

class CacheService:
    # ... 现有代码 ...

    @staticmethod
    def get_or_compute_async(
        key: str,
        compute_func: Callable[[], Awaitable[Any]],
        ttl: int = 300,
        lock_timeout: int = 5,
    ) -> Awaitable[Any]:
        """异步版本的 get_or_compute，避免锁竞争"""
        import asyncio

        async def _get_or_compute():
            # 先检查缓存
            cached = CacheService.get(key)
            if cached is not None:
                return cached

            # 使用异步锁
            lock_key = f"lock:{key}"
            lock_acquired = False

            try:
                # 尝试获取分布式锁
                if CacheService._redis_client:
                    lock_acquired = CacheService._redis_client.set(
                        lock_key, "1", nx=True, ex=lock_timeout
                    )

                if lock_acquired or not CacheService._redis_client:
                    # 获取锁成功，执行计算
                    result = await compute_func()
                    CacheService.set(key, result, ttl=ttl)
                    return result
                else:
                    # 获取锁失败，等待并重试
                    await asyncio.sleep(0.1)
                    cached = CacheService.get(key)
                    if cached is not None:
                        return cached
                    # 仍无缓存，直接计算
                    return await compute_func()
            finally:
                if lock_acquired and CacheService._redis_client:
                    CacheService._redis_client.delete(lock_key)

        return _get_or_compute()
```

**使用方式**：
```python
# 文件：app/services/user_auth_service.py

async def authenticate_api_key_cached(key_hash: str, db: Session):
    return await CacheService.get_or_compute_async(
        key=f"api_key:{key_hash}",
        compute_func=lambda: authenticate_api_key(key_hash, db),
        ttl=300,  # 5分钟
    )
```

**预期收益**：
- 锁竞争减少 80%
- 认证速度提升 5-10 倍
- 数据库查询减少 95%

---

### 优化5：实现 RequestLog 异步批量写入

**问题**：
- 每次请求都同步写入 `request_logs` 表
- 高并发下数据库写入成为瓶颈
- 阻塞主请求流程

**解决方案**：
```python
# 文件：app/services/log_service.py

import asyncio
from collections import deque
from datetime import datetime

class LogService:
    _log_queue = deque()
    _batch_size = 100
    _flush_interval = 1.0  # 1秒
    _background_task = None
    _lock = asyncio.Lock()

    @classmethod
    async def log_request_async(cls, log_data: dict):
        """异步添加日志到队列"""
        async with cls._lock:
            cls._log_queue.append(log_data)

            # 启动后台任务
            if cls._background_task is None or cls._background_task.done():
                cls._background_task = asyncio.create_task(cls._flush_logs_worker())

    @classmethod
    async def _flush_logs_worker(cls):
        """后台工作线程，批量写入日志"""
        from app.database import SessionLocal

        while True:
            try:
                await asyncio.sleep(cls._flush_interval)

                # 获取待写入日志
                async with cls._lock:
                    if not cls._log_queue:
                        continue

                    batch = []
                    while cls._log_queue and len(batch) < cls._batch_size:
                        batch.append(cls._log_queue.popleft())

                if not batch:
                    continue

                # 批量写入数据库
                db = SessionLocal()
                try:
                    for log_data in batch:
                        log_entry = RequestLog(**log_data)
                        db.add(log_entry)
                    await db.commit()
                except Exception as e:
                    await db.rollback()
                    # 日志写入失败，重新入队
                    async with cls._lock:
                        cls._log_queue.extend(batch)
                    print(f"Log flush error: {e}")
                finally:
                    await db.close()

            except Exception as e:
                print(f"Log worker error: {e}")
                await asyncio.sleep(5)  # 错误后等待5秒
```

**预期收益**：
- 主请求不再阻塞等待日志写入
- 数据库写入性能提升 10-100 倍
- 响应时间降低 10-20ms

**风险**：
- 应用崩溃时可能丢失队列中的日志
- 需要优雅关闭处理

**缓解措施**：
```python
# 文件：app/main.py

@app.on_event("shutdown")
async def shutdown_event():
    """应用关闭时刷新所有日志"""
    await LogService.flush_all_logs()
```

---

## 第二阶段：中优先级优化（后续执行）

### 优化6：分页查询结果缓存

**问题**：
- `/api/models`, `/api/providers` 等列表接口重复查询
- 分页结果未缓存

**解决方案**：
- 使用 Redis 缓存分页结果
- 缓存键包含查询条件和分页参数
- TTL 设置为 60秒

### 优化7：HTTP 连接池配置优化

**问题**：
- `httpx.AsyncClient` 连接池配置保守
- 上游 API 调用可能复用连接不足

**解决方案**：
```python
limits = httpx.Limits(
    max_keepalive_connections=100,  # 从 20 增加到 100
    max_connections=200,  # 从 100 增加到 200
    keepalive_expiry=60,
)
```

### 优化8：后台任务队列优化

**问题**：
- 使用内存队列，重启丢失
- 无持久化

**解决方案**：
- 使用 Redis 作为队列后端
- 实现任务持久化

---

## 第三阶段：长期优化（规划中）

### 优化9：分库分表

- `request_logs` 表按月分区
- 历史数据归档到时间序列数据库

### 优化10：读写分离

- 配置 PostgreSQL 主从复制
- 只读查询走从库

---

## 实施优先级

| 优先级 | 优化项 | 预期收益 | 实施难度 | 风险 |
|--------|--------|---------|---------|------|
| 🔴 P0 | 添加复合索引 | 10-100x | 低 | 低 |
| 🔴 P0 | 扩大连接池 | 2-3x | 低 | 低 |
| 🟠 P1 | ModelCatalog L1缓存 | 70-90% | 中 | 中 |
| 🟠 P1 | 认证缓存优化 | 5-10x | 中 | 低 |
| 🟠 P1 | 异步日志批量写入 | 10-100x | 高 | 中 |
| 🟡 P2 | 分页结果缓存 | 50% | 中 | 低 |
| 🟡 P2 | HTTP连接池优化 | 20% | 低 | 低 |
| 🟡 P2 | 队列持久化 | 可靠性 | 中 | 中 |

---

## 监控指标

优化后需要监控以下指标：

1. **响应时间**：P50, P95, P99
2. **吞吐量**：RPS (每秒请求数)
3. **数据库**：
   - 查询时间
   - 连接池使用率
   - 慢查询数量
4. **缓存**：
   - 命中率
   - 失效次数
5. **并发**：
   - 活跃连接数
   - 排队请求数

---

## 回滚计划

每个优化都需要准备回滚方案：

1. **索引**：`DROP INDEX IF EXISTS idx_name`
2. **连接池**：恢复原配置并重启
3. **缓存**：禁用缓存功能标志
4. **异步日志**：切回同步写入

---

## 下一步行动

立即执行 P0 优化：
1. ✅ 创建数据库索引迁移脚本
2. ✅ 修改 `database.py` 扩大连接池
3. ✅ 执行索引迁移
4. ✅ 重启应用
5. ✅ 监控性能指标

**预计完成时间**：30分钟
**预期性能提升**：2-5倍

---

**文档版本**：1.0
**创建时间**：2026-06-06 19:00:00
**负责人**：Claude Code
