# 统一日志管理系统改进方案

## 1. 目标

本方案用于把当前项目的日志体系升级为低耦合、可复用、可扩展的完整日志记录模块。升级后必须满足：

- 所有关键业务链路都必须有明确记录策略。
- 不同类型日志必须使用不同结构，禁止强行塞入同一套字段。
- 前端必须按日志类型提供不同展示页面、详情结构或专用卡片，禁止所有日志共用一个通用卡片。
- 对外 `/v1/*` 请求日志必须继续复用现有 `request_logs` 主表作为请求主记录，避免破坏当前统计、计费、用户日志和全局日志页面。
- 日志写入必须保持热路径低阻塞，优先异步队列，队列不可用时回退同步落库。
- 错误响应、日志、trace、告警必须继续复用统一错误目录，避免各模块自行拼错误文本。

## 2. 当前项目日志现状

### 2.1 已有日志落点

| 当前落点 | 文件 | 作用 | 问题 |
| --- | --- | --- | --- |
| `request_logs` | `app/models/request_log.py` | 对外请求、可用性检测、模型列表、内容防护、计费字段都混在一张宽表 | 字段过宽，不同事件结构混杂，前端难以按语义拆分 |
| `LogService.create_log` | `app/services/log_service.py` | 请求日志统一写入口 | 方法参数过多，业务模块和落库结构耦合重 |
| `RequestLogQueueService` | `app/services/request_log_queue_service.py` | 请求日志 Redis 队列、批量落库、失败回退 | 只服务请求日志，不能复用到后台任务、审计、资产等事件 |
| `admin_audit_logs` | `app/models/admin_audit_log.py` | 后台管理审计 | 已覆盖部分后台写操作，但 provider 管理、日志清空/导出、benchmark 等缺口明显 |
| `alert_events` | `app/models/alert_event.py` | 告警事件 | 已覆盖监控类告警，但与普通日志、后台任务状态未形成统一入口 |
| `api_client_billing_records` / `user_account_billing_records` | `app/models/api_client_billing_record.py`、`app/models/user_account_billing_record.py` | 账单流水 | 是计费事实记录，不等同于计费过程日志 |
| `logger.warning` / `logger.exception` | `app/tasks.py`、`app/services/request_log_queue_service.py`、`app/services/token_usage_service.py` 等 | 控制台日志 | 缺少可查询、可追踪、可告警的持久化结构 |

### 2.2 当前前端展示现状

| 页面 | 文件 | 当前用途 | 升级方向 |
| --- | --- | --- | --- |
| 日志中心 | `app/templates/logs.html` | 展示 `request_logs` 列表 | 保留为“请求日志中心”，新增请求详情时间线和分类卡片 |
| 用户日志 | `app/templates/user_logs.html` | 用户侧请求日志 | 仅展示用户 API Key 产生的模型调用日志 |
| 审计日志 | `app/templates/audit_logs.html` | 管理审计 | 拆出后台审计与用户操作审计，字段结构不同 |
| 告警中心 | `app/templates/alerts.html` | 告警事件与监控摘要 | 继续承载 `alert_events`，但告警 payload 按类型使用不同详情组件 |
| 运维监控 | `app/templates/operations.html` | Redis、DB、流量、provider 指标 | 应补充后台任务、队列、系统事件入口 |

## 3. 总体架构

### 3.1 核心原则

统一日志系统不应把所有日志合并成一张万能表，而应采用：

```text
统一上下文 Envelope
    +
按日志类型拆分的 Typed Event Payload
    +
按日志类型独立持久化表 / 适配器
    +
按日志类型独立 API 与前端展示组件
```

### 3.2 推荐目录结构

```text
app/
  logging/
    __init__.py
    context.py                 # 提取 trace、actor、source_ip、api_key、user、provider 等上下文
    envelope.py                # 所有日志共享的最小 Envelope
    event_types.py             # 日志类型、动作、结果、严重级别枚举
    dispatcher.py              # 统一入口：record(event)
    queue.py                   # 通用日志事件队列，可复用 Redis
    sinks.py                   # DB、console、alert 等 sink
    registry.py                # event_type -> adapter / table / serializer / frontend schema
    sanitizers.py              # 脱敏、截断、大字段摘要
    adapters/
      request_adapter.py
      exception_adapter.py
      health_adapter.py
      billing_adapter.py
      background_job_adapter.py
      audit_adapter.py
      user_operation_adapter.py
      asset_adapter.py
      alert_adapter.py
```

### 3.3 统一 Envelope

所有日志事件都必须有最小公共字段：

| 字段 | 含义 |
| --- | --- |
| `event_id` | 事件唯一 ID |
| `event_type` | 日志大类，例如 `external_request`、`health_check`、`billing` |
| `event_name` | 具体事件名，例如 `provider_attempt`、`token_finalize_failed` |
| `trace_id` | 请求/任务链路追踪 ID |
| `correlation_id` | 跨表关联 ID，例如 `request_log_id`、`job_run_id` |
| `module` | 来源模块，例如 `proxy`、`billing`、`scheduler` |
| `severity` | `info`、`warning`、`danger`、`critical` |
| `actor_type` | `api_client`、`admin_user`、`user`、`system`、`scheduler` |
| `actor_id` | 操作者 ID |
| `source_ip` | 来源 IP，可为空 |
| `result` | `success`、`failed`、`skipped`、`blocked`、`cancelled` |
| `occurred_at` | 事件时间 |

Envelope 只做跨类型检索和关联，具体字段必须进入类型化 payload 或类型化表。

## 4. 后端数据模型设计

### 4.1 外部 /v1/* 请求日志

保留现有 `request_logs` 作为请求主记录，并新增请求链路子事件表。这样既符合“API Key 相关请求日志必须继续复用 `request_logs`”的现行规范，也能让各子事件有独立结构。

#### 4.1.1 `request_logs` 主记录

继续记录请求级汇总：

- 请求身份：`trace_id`、`request_id`、`conversation_key`、`session_id`
- API Key / 用户：`api_client_key_id`、`api_client_key_name`、`user_account_id`
- 请求目标：`request_path`、`http_method`、`requested_model`、`model_name`
- 结果：`success`、`status_code`、`error_code`、`message`
- 性能：`latency_ms`、`ttfb_ms`、`duration_ms`、`tps`
- token 与计费汇总：`prompt_tokens`、`completion_tokens`、`total_tokens`、`total_cost`
- 内容防护汇总：`content_guard_result`、`content_guard_risk_level`

#### 4.1.2 `request_auth_events`

记录鉴权事件。

| 字段 | 含义 |
| --- | --- |
| `request_log_id` | 关联 `request_logs.id` |
| `trace_id` | trace |
| `auth_result` | `authenticated`、`missing_bearer`、`invalid_key`、`disabled`、`expired`、`quota_exceeded`、`rate_limited` |
| `api_client_key_id` | 命中的 API Key |
| `api_client_key_prefix` | Key 前缀 |
| `user_account_id` | 归属用户 |
| `remaining_tokens` | 剩余 token |
| `remaining_requests_daily` | 日剩余请求数 |
| `remaining_cost_daily` | 日剩余额度 |
| `policy_snapshot_json` | 策略快照 |
| `error_code` | 鉴权失败码 |

前端展示：请求详情页中的“鉴权卡片”，展示 Key、用户、剩余额度、失败原因。

#### 4.1.3 `request_validation_events`

记录请求体校验、大小限制、JSON 解析、Token 预检。

| 字段 | 含义 |
| --- | --- |
| `validation_stage` | `content_length`、`body_read`、`json_parse`、`schema_validation`、`token_precheck` |
| `passed` | 是否通过 |
| `limit_value` | 限制值 |
| `actual_value` | 实际值 |
| `request_body_summary_json` | 请求体结构摘要 |
| `error_code` | 失败码 |
| `safe_detail_json` | 安全详情 |

前端展示：请求详情页“请求校验卡片”，用分步骤状态展示。

#### 4.1.4 `request_model_permission_events`

记录模型权限和端点能力判断。

| 字段 | 含义 |
| --- | --- |
| `requested_model` | 请求模型 |
| `resolved_model` | 映射后的模型 |
| `endpoint_path` | 请求端点 |
| `required_capabilities_json` | 视觉、工具、图片生成、流式、responses/chat 原生支持 |
| `permission_result` | `allowed`、`model_not_allowed`、`capability_missing`、`endpoint_not_supported` |
| `reason_details_json` | 结构化原因 |

前端展示：请求详情页“模型与能力卡片”。

#### 4.1.5 `request_route_decision_events`

记录路由候选和选择结果。

| 字段 | 含义 |
| --- | --- |
| `route_round` | 第几轮路由 |
| `route_policy` | 当前策略，用户可见仍为可用性优先 |
| `candidate_count` | 候选数 |
| `selected_provider_id` | 选中 provider |
| `selected_provider_model_id` | 选中挂载 |
| `sticky_hit` | 是否命中会话粘性 |
| `excluded_summary_json` | 按容量、可用、权限、能力等原因排除的汇总 |
| `diagnostics_json` | 候选诊断 |
| `retry_wait_ms` | 候选耗尽等待时间 |

前端展示：请求详情页“路由决策卡片”，展示候选总览与排除原因，不把长诊断塞进主表。

#### 4.1.6 `request_provider_attempt_events`

记录每次 provider 尝试。

| 字段 | 含义 |
| --- | --- |
| `attempt_index` | 第几次尝试 |
| `provider_id` | provider |
| `provider_name` | provider 名称 |
| `provider_model_id` | 模型挂载 |
| `actual_model` | 实际模型 |
| `endpoint_path` | 实际端点 |
| `protocol_type` | `responses`、`chat_completions`、`both` |
| `capacity_lease_acquired` | 是否拿到容量租约 |
| `result` | `success`、`upstream_error`、`timeout`、`capacity_limited`、`circuit_open` |
| `status_code` | 上游状态码 |
| `latency_ms` | 本次尝试耗时 |
| `upstream_request_id` | 上游请求 ID |
| `error_code` | 错误码 |
| `retryable` | 是否可重试 |

前端展示：请求详情页“Provider 尝试时间线”，每次尝试一张折叠行/详情行。

#### 4.1.7 `request_upstream_response_events`

记录上游响应摘要。

| 字段 | 含义 |
| --- | --- |
| `provider_attempt_event_id` | 关联 provider 尝试 |
| `status_code` | 上游状态码 |
| `upstream_request_id` | 上游请求 ID |
| `finish_reason` | 完成原因 |
| `usage_json` | 上游原始 usage 摘要 |
| `response_summary_json` | 响应结构摘要 |
| `response_text_excerpt` | 安全短摘录 |
| `response_body_truncated` | 是否截断 |

前端展示：请求详情页“上游响应卡片”，区分 usage、内容摘要、截断状态。

#### 4.1.8 `request_stream_events`

记录 SSE 流式细节。

| 字段 | 含义 |
| --- | --- |
| `stream_result` | `completed`、`client_disconnected`、`upstream_error`、`empty_stream`、`timeout` |
| `first_token_latency_ms` | 首 token 延迟 |
| `ttfb_ms` | 首字节/首 token 口径 |
| `duration_ms` | 总持续时间 |
| `chunk_count` | chunk 数 |
| `captured_text_bytes` | 日志采集文本字节数 |
| `sse_error_sent` | 是否发送 `event: error` |
| `done_sent` | 是否发送 `[DONE]` |
| `disconnect_status_code` | 客户端断开用 499 或等价状态 |

前端展示：请求详情页“流式响应卡片”，非流式请求不显示。

#### 4.1.9 `request_error_response_events`

记录对调用方返回的错误响应。

| 字段 | 含义 |
| --- | --- |
| `status_code` | 对外状态码 |
| `error_type` | OpenAI 兼容错误类型 |
| `error_code` | 统一错误码 |
| `public_message` | 对外错误说明 |
| `category` | 错误分类 |
| `retryable` | 可重试 |
| `recoverable` | 可恢复 |
| `required_endpoint` | 若端点/能力不支持，记录要求端点 |
| `missing_capability` | 缺失能力 |
| `diagnostic_sample_json` | 安全诊断样本 |

前端展示：请求详情页“错误响应卡片”，与 provider 上游错误分开。

#### 4.1.10 `request_content_guard_events`

记录内容完整性防护。

| 字段 | 含义 |
| --- | --- |
| `guard_stage` | `request_policy`、`non_stream_response`、`stream_buffer`、`health_probe` |
| `guard_result` | `pass`、`review`、`block`、`error` |
| `risk_level` | `low`、`medium`、`high` |
| `matched_categories_json` | 命中类别 |
| `matched_rules_json` | 命中规则 |
| `reason` | 原因 |
| `action` | `allow`、`review`、`block`、`isolate_provider` |
| `excerpt` | 安全摘录 |
| `provider_status_after` | 处理后 provider 状态 |

前端展示：内容防护详情卡片，和告警中心的内容风险告警互相跳转。

#### 4.1.11 `request_billing_events`

记录请求级计费过程。

| 字段 | 含义 |
| --- | --- |
| `billing_event_id` | 计费事件 ID |
| `billing_stage` | `queued`、`token_filled`、`cost_computed`、`balance_deducted`、`finalized`、`failed` |
| `token_source` | `upstream_usage`、`estimated`、`backfill`、`missing` |
| `prompt_tokens` | 输入 token |
| `completion_tokens` | 输出 token |
| `cache_read_tokens` | 缓存读取 token |
| `cache_write_tokens` | 缓存写入 token |
| `prompt_cost` | 输入费用 |
| `completion_cost` | 输出费用 |
| `total_cost` | 总费用 |
| `balance_before` | 扣减前余额 |
| `balance_after` | 扣减后余额 |
| `attempt_count` | 计费尝试次数 |
| `error` | 失败原因 |

前端展示：请求详情页“计费卡片”，账单中心仍展示最终账单流水。

### 4.2 异常处理层日志

新增 `exception_events`，记录应用层异常，不再只依赖 `logger.exception`。

| 场景 | 记录策略 | 来源 |
| --- | --- | --- |
| trace_id 注入 | 不单独落表，进入 Envelope 和请求主记录 | `app/main.py` HTTP middleware |
| 请求验证错误 | 写 `exception_events` + 对外 `/v1/*` 写 `request_error_response_events` | `RequestValidationError` handler |
| `HTTPException` | 写 `exception_events`，若是 `/v1/*` 同步写请求错误事件 | `http_exception_handler` |
| 未捕获异常 | 写 `exception_events`，`severity=critical` | `unhandled_exception_handler` |
| 路由前拒绝 | 写 `request_validation_events` 或 `request_auth_events`，并写 `request_error_response_events` | `_log_v1_request_rejected_before_route` |

字段结构：

| 字段 | 含义 |
| --- | --- |
| `exception_type` | 异常类型 |
| `status_code` | HTTP 状态码 |
| `error_code` | 统一错误码 |
| `message` | 安全消息 |
| `handler_name` | 处理器 |
| `request_path` | 请求路径 |
| `method` | 方法 |
| `is_external_v1` | 是否对外入口 |
| `stack_hash` | 堆栈哈希 |
| `stack_excerpt` | 受控长度堆栈摘要 |

前端展示：新增“异常事件”页面，按异常类型、路径、错误码筛选；详情页显示错误目录上下文和关联请求。

### 4.3 可用性检测日志

新增两层结构：`health_check_runs` 和 `health_probe_events`。现有 `request_logs` 中的 `health_check_provider`、`health_check_model` 可以作为兼容汇总保留，但前端应以新结构展示可用性检测。

#### 4.3.1 `health_check_runs`

| 字段 | 含义 |
| --- | --- |
| `run_id` | 检查批次 |
| `trigger_type` | `manual_single`、`manual_batch`、`scheduled_l0`、`scheduled_l1`、`scheduled_l2`、`scheduled_l3` |
| `scope_type` | `provider`、`model`、`all` |
| `scope_id` | 对象 ID |
| `phase_keys_json` | 本次阶段 |
| `started_at` / `finished_at` | 开始/结束 |
| `duration_ms` | 总耗时 |
| `total_probes` | 探针总数 |
| `success_probes` | 成功数 |
| `failed_probes` | 失败数 |
| `overall_result` | `healthy`、`degraded`、`unhealthy`、`timeout` |

#### 4.3.2 `health_probe_events`

| 字段 | 含义 |
| --- | --- |
| `run_id` | 所属批次 |
| `provider_id` | provider |
| `provider_model_id` | 模型挂载 |
| `model_name` | 模型名 |
| `probe_type` | `connectivity`、`text_stream`、`tools`、`vision`、`image_generation`、`content_integrity` |
| `endpoint_path` | 探测端点 |
| `protocol_type` | 协议 |
| `success` | 是否成功 |
| `status_code` | 状态码 |
| `latency_ms` | 延迟 |
| `error_code` | 错误码 |
| `capability_result_json` | 能力探测结果 |
| `content_guard_result_json` | 内容完整性结果 |

前端展示：新增“可用性检测日志”页面，包括批次列表、阶段进度、探针矩阵；provider/model 页保留最近可用性检测摘要卡片。

### 4.4 计费与 Token 回填日志

计费日志必须与账单流水分离：

- 账单流水记录最终资金变动事实。
- 计费日志记录 token 回填、价格解析、余额扣减、失败重试过程。

新增：

| 表 | 作用 |
| --- | --- |
| `token_finalize_events` | token 补全队列、回填、失败、重试 |
| `billing_process_events` | 价格解析、扣费、账单生成、余额变更 |

#### `token_finalize_events`

| 字段 | 含义 |
| --- | --- |
| `request_log_id` | 请求日志 |
| `queue_source` | `request_log_queue`、`backfill_job`、`retry` |
| `attempt_count` | 尝试次数 |
| `usage_before_json` | 补全前 |
| `usage_after_json` | 补全后 |
| `token_source` | usage 来源 |
| `enable_usage_fill` | 是否允许估算/补全 |
| `result` | `filled`、`skipped`、`failed` |
| `error` | 失败原因 |

#### `billing_process_events`

| 字段 | 含义 |
| --- | --- |
| `request_log_id` | 请求日志 |
| `api_client_key_id` | API Key |
| `user_account_id` | 用户 |
| `pricing_source` | `provider_model`、`model_catalog`、`tier` |
| `pricing_snapshot_json` | 价格快照 |
| `cost_snapshot_json` | 成本快照 |
| `balance_delta` | 余额变化 |
| `balance_after` | 余额 |
| `billing_status` | `pending_tokens`、`billed`、`no_charge`、`failed` |
| `billing_record_id` | 账单流水 ID |
| `error` | 错误 |

前端展示：新增“计费日志”页面；API Key 详情和用户账单详情中加入“计费过程”抽屉。

### 4.5 后台任务日志

新增 `background_job_events`。当前 `app/tasks.py` 中 Redis 状态仍可保留，但必须同步落库，便于历史查询。

| 字段 | 含义 |
| --- | --- |
| `job_run_id` | 本次运行 ID |
| `job_name` | 任务名 |
| `trigger_type` | `scheduler`、`manual`、`startup`、`shutdown` |
| `lock_key` | 分布式锁 |
| `lock_status` | `acquired`、`skipped_locked`、`unavailable`、`released_failed` |
| `status` | `running`、`success`、`failed`、`cancelled`、`skipped` |
| `started_at` / `finished_at` | 时间 |
| `duration_ms` | 耗时 |
| `processed_count` | 处理数 |
| `success_count` | 成功数 |
| `failed_count` | 失败数 |
| `result_summary_json` | 结果摘要 |
| `error` | 失败原因 |

覆盖任务：

- `provider_l0_health_check`
- `model_l1_text_health_check`
- `model_l2_capability_health_check`
- `model_l3_content_integrity_health_check`
- `token_usage_backfill`
- `data_retention_cleanup`
- `responses_chat_adapter_session_cleanup`
- 请求日志队列 worker
- token finalize worker

前端展示：新增“后台任务日志”页面；运维监控页展示最近失败任务、队列积压和 worker 状态。

### 4.6 后台管理操作审计

保留并扩展 `admin_audit_logs`。已覆盖的 API Key、用户、模型、模型映射、设置、内容防护继续沿用；必须补齐：

| 模块 | 必须记录动作 |
| --- | --- |
| provider 管理 | 创建、更新、删除、密钥轮换、批量导入、模型发现、模型挂载更新 |
| 日志中心 | 清空日志、导出日志 |
| benchmark | 启动、停止、查看报告导出 |
| 告警 | 订阅更新、告警确认已覆盖，后续补告警静默/恢复 |
| 可用性检测 | 手动单 provider、单模型、全量检查触发 |

审计字段应扩展：

| 字段 | 含义 |
| --- | --- |
| `request_trace_id` | 操作请求 trace |
| `source_ip` | 操作者 IP |
| `before_json` | 变更前摘要 |
| `after_json` | 变更后摘要 |
| `changed_fields_json` | 字段级变化 |
| `risk_level` | `low`、`medium`、`high` |

前端展示：保留“审计日志”，但不同 `entity_type` 使用不同详情模板，例如 API Key 变更显示权限、额度、模型白名单，provider 变更显示 Base URL、协议、容量、信任等级。

### 4.7 用户端操作审计

新增 `user_operation_audit_logs`，不要混入后台管理员审计。

| 必须记录动作 | 来源 |
| --- | --- |
| 用户资料更新 | `/user/profile` |
| 用户密码修改 | `/user/password` |
| 用户 API Key 创建 | `/user/api-keys/create` |
| 用户 API Key 更新 | `/user/api-keys/{api_key_id}/update` |
| 用户 API Key 启停 | `/user/api-keys/{api_key_id}/toggle` |
| 用户 API Key 删除 | `/user/api-keys/{api_key_id}/delete` |
| 用户 API Key 轮换 | `/user/api-keys/{api_key_id}/rotate` |
| 用户日志导出 | `/user/logs/export` |
| 用户账单导出 | `/user/billing/export` |
| 用户自测运行 | `/api/user/self-test/run` |

字段结构：

| 字段 | 含义 |
| --- | --- |
| `user_account_id` | 用户 |
| `username` | 用户名 |
| `action` | 动作 |
| `entity_type` | `profile`、`password`、`api_key`、`log_export`、`billing_export`、`self_test` |
| `entity_id` | 对象 ID |
| `entity_name` | 对象名称 |
| `summary` | 中文摘要 |
| `detail_json` | 安全详情 |
| `source_ip` | 来源 IP |
| `trace_id` | trace |
| `result` | 成功/失败 |

前端展示：用户端新增“我的操作记录”页面；管理员用户详情页可查看某用户操作记录。

### 4.8 文件/素材日志

新增 `asset_events`，内部素材上传不能只写 `uploaded_assets`。

| 场景 | 记录策略 |
| --- | --- |
| playground 图片上传 | 写 `asset_events`，actor 为管理员 |
| 用户自测图片上传 | 写 `asset_events`，actor 为普通用户 |
| `/v1/files` 上传/删除/读取 | 继续写 `request_logs`，并补 `asset_events` 关联外部文件操作 |
| 图片生成结果摘要 | 继续写请求日志图片观测字段，必要时写 `asset_events` 记录生成资产摘要 |

字段结构：

| 字段 | 含义 |
| --- | --- |
| `asset_id` | 内部素材 ID |
| `asset_event_type` | `upload`、`reuse_existing`、`delete`、`external_file_upload`、`external_file_read` |
| `actor_type` | `admin_user`、`user`、`api_client` |
| `filename` | 文件名 |
| `content_type` | 类型 |
| `file_size_bytes` | 大小 |
| `sha256_prefix` | 哈希前缀，禁止默认暴露完整哈希 |
| `storage_scope` | `playground`、`user_self_test`、`external_v1_file` |
| `result` | 成功/失败 |
| `error` | 失败原因 |

前端展示：新增“素材日志”页面；playground 和用户自测页面只展示当前操作反馈，不堆静态说明卡。

### 4.9 可观测/告警日志

保留 `alert_events`，但按类型规范 payload：

| 告警类型 | payload 结构 |
| --- | --- |
| Redis 不可用 | `redis_status`、`latency_ms`、`active_requests`、`active_streams`、`error` |
| DB 不可用 | `database_status`、`dialect`、`pool_status`、`latency_ms`、`error` |
| 并发接近上限 | `scope`、`active`、`limit`、`threshold` |
| 队列积压 | `queue_name`、`queued`、`processing`、`threshold` |
| 5xx/429 突增 | `window_minutes`、`total_requests`、`error_count`、`error_rate` |
| provider 失败率 | `provider_id`、`provider_name`、`failure_rate`、`total_requests` |
| 内容风险隔离 | `provider_id`、`high_risk_count`、`window_minutes`、`auto_isolated` |
| 计费失败 | `request_log_id`、`trace_id`、`attempt_count`、`error` |

前端展示：告警中心按 `alert_type` 使用不同详情组件；运维监控页只展示实时摘要和最近活跃告警。

## 5. 前端页面与卡片体系

### 5.1 导航结构

在左侧主导航中保留：

- 日志中心
- 告警中心
- 审计日志
- 运维监控

日志中心内部使用二级标签或页面内分段导航，不新增独立快捷入口卡片：

```text
日志中心
  - 请求日志
  - 异常事件
  - 可用性检测日志
  - 计费日志
  - 后台任务日志
  - 素材日志
```

### 5.2 页面设计规则

- 所有日志列表必须服务端分页、服务端筛选。
- 筛选区使用同排等宽栅格，支持关键词、时间、状态、对象归属。
- 主列表只展示核心字段，长错误、trace、payload 放详情抽屉或详情页。
- 高密度主字段中的异常原因必须放问号 Tooltip，不塞进主列正文。
- 各日志类型必须有专用详情卡片，不共用一套万能 JSON 卡片。
- 卡片使用无阴影一体化视觉，保持项目现有设计规范。

### 5.3 各页面卡片结构

| 页面 | 列表主字段 | 详情卡片 |
| --- | --- | --- |
| 请求日志 | 时间、路径、模型、API Key、用户、provider、状态、耗时、费用 | 鉴权卡、校验卡、模型能力卡、路由卡、provider 尝试时间线、流式卡、错误响应卡、内容防护卡、计费卡 |
| 异常事件 | 时间、路径、异常类型、错误码、状态码、严重级别 | 错误目录卡、请求上下文卡、堆栈摘要卡、关联请求卡 |
| 可用性检测日志 | 批次、触发方式、范围、结果、探针数、耗时 | 阶段进度卡、探针矩阵、provider/model 结果卡、错误原因卡 |
| 计费日志 | 时间、API Key、用户、模型、金额、状态、请求日志 ID | token 回填卡、价格快照卡、扣费过程卡、失败重试卡 |
| 后台任务日志 | 任务名、触发方式、锁状态、运行状态、耗时、处理数 | 分布式锁卡、执行结果卡、错误卡、队列状态卡 |
| 审计日志 | 操作者、动作、对象、摘要、时间、风险等级 | 变更前后对比卡、字段变化卡、权限/额度/配置专用卡 |
| 用户操作日志 | 用户、动作、对象、结果、来源 IP、时间 | 用户上下文卡、操作详情卡、关联对象卡 |
| 素材日志 | 时间、来源、文件名、类型、大小、结果 | 文件元数据卡、存储卡、关联请求/自测卡 |
| 告警中心 | 告警类型、严重级别、对象、状态、最后出现时间 | Redis 卡、DB 卡、provider 卡、队列卡、内容风险卡、计费失败卡 |

## 6. 记录策略总表

| 日志范围 | 必须记录事件 | 推荐表/结构 | 当前代码入口 | 前端展示 |
| --- | --- | --- | --- | --- |
| 外部 `/v1/*` 请求 | 鉴权、校验、模型权限、路由候选、provider 尝试、上游响应、SSE 首 token、客户端断开、错误响应、内容防护、计费 | `request_logs` + 请求子事件表 | `app/routers/proxy.py`、`app/services/proxy_service.py` | 请求日志详情页专用卡片 |
| 异常处理层 | 验证错误、HTTPException、未捕获异常、路由前拒绝 | `exception_events` + 请求错误子事件 | `app/main.py` | 异常事件页 |
| 可用性检测 | provider 连通性、文本/工具/视觉/图片能力、批量/定时探测 | `health_check_runs` + `health_probe_events` | `app/services/health_service.py` | 可用性检测日志页 |
| 计费与 token 回填 | token 补全、账单生成、扣费、失败重试、告警 | `token_finalize_events` + `billing_process_events` | `app/services/billing_service.py`、`app/services/token_usage_service.py` | 计费日志页 |
| 后台任务 | 调度锁、任务状态、可用性检测、backfill、日志清理、adapter 清理 | `background_job_events` | `app/tasks.py` | 后台任务日志页 |
| 后台管理操作 | API Key、用户、模型、映射、设置、内容防护、provider、日志、benchmark | 扩展 `admin_audit_logs` | `app/routers/*` | 审计日志页 |
| 用户端操作 | 资料、密码、用户 API Key、导出、自测 | `user_operation_audit_logs` | `app/routers/user_portal.py` | 我的操作记录页 |
| 文件/素材 | playground 上传、用户图片上传、外部 `/v1/files` | `asset_events` | `app/services/asset_service.py`、`app/services/proxy_service.py` | 素材日志页 |
| 可观测/告警 | Redis、DB、并发、队列、5xx/429、provider 失败率、内容风险隔离 | `alert_events` typed payload | `app/services/system_metrics_service.py` | 告警中心 |

## 7. 服务接口设计

### 7.1 写入接口

```python
LoggingDispatcher.record(event: LogEvent) -> None
LoggingDispatcher.record_many(events: list[LogEvent]) -> None
LoggingDispatcher.enqueue(event: LogEvent) -> bool
```

### 7.2 类型化快捷方法

```python
RequestLogRecorder.record_auth(...)
RequestLogRecorder.record_validation(...)
RequestLogRecorder.record_route_decision(...)
RequestLogRecorder.record_provider_attempt(...)
RequestLogRecorder.record_stream(...)
RequestLogRecorder.record_error_response(...)
RequestLogRecorder.record_content_guard(...)
RequestLogRecorder.record_billing(...)

AuditLogRecorder.record_admin_action(...)
UserOperationLogRecorder.record_user_action(...)
HealthLogRecorder.start_run(...)
HealthLogRecorder.record_probe(...)
BillingLogRecorder.record_token_finalize(...)
BackgroundJobLogRecorder.record_job_event(...)
AssetLogRecorder.record_asset_event(...)
AlertLogRecorder.upsert_alert(...)
```

业务代码只依赖 recorder 接口，不直接依赖 SQLAlchemy model。

### 7.3 查询接口

```text
GET /api/logging/request-logs
GET /api/logging/request-logs/{id}
GET /api/logging/request-logs/{id}/timeline
GET /api/logging/exceptions
GET /api/logging/health-runs
GET /api/logging/health-runs/{run_id}
GET /api/logging/billing-events
GET /api/logging/background-jobs
GET /api/logging/admin-audits
GET /api/logging/user-operations
GET /api/logging/asset-events
GET /api/alerts/feed
```

## 8. 迁移实施步骤

### 阶段 1：新增统一日志模块，不改变现有行为

- 新增 `app/logging/`。
- 定义 Envelope、事件类型、脱敏工具、dispatcher、registry。
- 将现有 `LogService.create_log` 包装为 `RequestLogRecorder.record_summary`。
- 保持 `request_logs`、`admin_audit_logs`、`alert_events` 查询接口不变。

### 阶段 2：新增请求链路子事件

- 在代理主链路埋点：
  - 鉴权后写 `request_auth_events`
  - 请求体读取/校验后写 `request_validation_events`
  - 模型权限检查后写 `request_model_permission_events`
  - 路由候选排序后写 `request_route_decision_events`
  - 每次 provider 尝试写 `request_provider_attempt_events`
  - 上游响应后写 `request_upstream_response_events`
  - 流式生成器 finally 写 `request_stream_events`
  - 错误响应构造后写 `request_error_response_events`
  - 内容防护检查后写 `request_content_guard_events`
  - token/计费后台任务写 `request_billing_events`

### 阶段 3：补齐非请求类日志

- 异常处理层写 `exception_events`。
- 可用性检测写 `health_check_runs` 和 `health_probe_events`。
- token 回填写 `token_finalize_events`。
- 计费过程写 `billing_process_events`。
- 后台任务写 `background_job_events`。
- 内部素材操作写 `asset_events`。
- 用户端操作写 `user_operation_audit_logs`。

### 阶段 4：补齐审计缺口

- `providers.py`：创建、更新、删除、密钥轮换、批量导入、模型发现、模型挂载更新。
- `logs.py`：清空、导出。
- `benchmark.py`：启动、停止、报告访问。
- `provider_models.py`：单模型测试、批量测试触发。
- `auth.py`：登录失败、登录成功、注册成功、退出可写用户/安全事件，管理员初始化拒绝可写异常/安全事件。

### 阶段 5：前端拆分页面

- 日志中心保留请求日志列表。
- 新增请求详情页或抽屉，展示请求链路专用卡片。
- 新增异常事件、可用性检测日志、计费日志、后台任务日志、素材日志页面。
- 审计日志详情按对象类型使用不同详情组件。
- 用户端新增“我的操作记录”页面。

### 阶段 6：保留、清理与监控

- 系统设置增加各类型日志保留天数。
- `DataRetentionService` 扩展清理新表。
- 运维监控展示各队列积压和最近失败事件。
- 告警中心接收后台任务失败、队列积压、计费失败等事件。

## 9. 测试与验收

### 9.1 后端验收

- 每个日志 recorder 都有单元测试。
- `/v1/chat/completions` 成功请求产生主日志、鉴权、校验、路由、provider、上游响应、计费事件。
- `/v1/chat/completions` 流式请求产生首 token、完成或客户端断开事件。
- 鉴权失败请求产生鉴权事件和错误响应事件。
- 模型不可用请求产生模型权限/路由诊断事件。
- 内容防护阻断请求产生内容防护事件和告警关联。
- token 回填失败产生 token finalize 事件和告警。
- 定时任务执行成功、失败、跳过锁都产生后台任务事件。

### 9.2 前端验收

- 不同日志类型不共用同一详情卡片。
- 请求详情页至少包含鉴权、校验、路由、provider 尝试、错误/响应、计费卡片。
- 可用性检测日志使用批次 + 探针矩阵展示。
- 计费日志使用 token、价格、扣费、失败重试专用结构。
- 后台任务日志显示锁状态、执行状态、处理数量。
- 素材日志显示文件名、大小、来源、结果。
- 告警中心按不同告警类型显示不同详情。
- 所有列表服务端分页，筛选项支持服务端查询。

## 10. 风险与控制

| 风险 | 控制策略 |
| --- | --- |
| 表数量增加 | 只为高价值、结构差异明显的日志建表；低频日志可用 typed payload |
| 热路径写入变慢 | 所有子事件优先入队，失败时关键事件同步落库，非关键事件可降级 |
| 旧页面统计受影响 | `request_logs` 主表保留，不破坏现有统计和用户日志 |
| 日志数据过大 | 统一脱敏、截断、结构摘要和字节上限 |
| 前端页面膨胀 | 二级导航 + 详情抽屉 + 渐进式披露，禁止纯说明卡 |
| 计费重复扣费 | 计费事件必须幂等，继续使用 `billing_event_id` 和请求日志唯一关系 |

## 11. 最终状态

完成后，本项目日志体系应形成：

- 一套统一、低耦合的日志写入入口。
- 多套类型化日志结构，而不是万能字段表。
- 多个符合业务语义的前端日志页面和专用详情卡片。
- 对外请求、异常、可用性检测、计费、后台任务、审计、用户操作、素材、告警全覆盖。
- 保留现有 `request_logs` 兼容性，同时支持更细粒度的请求链路追踪。
- 让未来新增模块只需注册事件类型、payload schema、sink 和前端展示组件，即可接入统一日志系统。
